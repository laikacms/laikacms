{
  "name": "@laikacms/docs",
  "version": "1.0.1",
  "private": true,
  "description": "Laika CMS documentation site (VitePress).",
  "type": "module",
  "scripts": {
    "aggregate-package-docs": "node scripts/aggregate-package-docs.mjs",
    "generate-openapi": "node scripts/generate-openapi.mjs",
    "prebuild-docs": "npm run aggregate-package-docs && npm run generate-openapi",
    "dev": "npm run prebuild-docs && vitepress dev",
    "build": "npm run prebuild-docs && vitepress build",
    "preview": "vitepress preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "gray-matter": "catalog:",
    "laikacms": "workspace:*",
    "vitepress": "catalog:vue",
    "vitest": "catalog:tools",
    "vue": "catalog:vue"
  },
  "engines": {
    "node": ">=24"
  }
}
