{
  "name": "@laikacms/docs",
  "version": "1.0.1",
  "private": true,
  "description": "Laika CMS documentation site (VitePress).",
  "type": "module",
  "scripts": {
    "aggregate-package-docs": "node scripts/aggregate-package-docs.mjs",
    "dev": "npm run aggregate-package-docs && vitepress dev",
    "build": "npm run aggregate-package-docs && vitepress build",
    "preview": "vitepress preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "gray-matter": "catalog:",
    "vitepress": "catalog:vue",
    "vitest": "catalog:tools",
    "vue": "catalog:vue"
  },
  "engines": {
    "node": ">=24"
  }
}
