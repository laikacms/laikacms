Security
===







Errors
---

Make sure to use `effect`s `Secret` for secret values and make sure that you never include any sensitive data in any failed results. Any sensitive and internal data can be returned inside the `cause` field of `LaikaError`.




