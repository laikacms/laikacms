# Reference

Exhaustive lookup material — the API surface, the package map, and the shared vocabulary.

- **[JSON:API Reference](./json-api/)** — every HTTP endpoint across the Storage, Documents, Assets,
  and Catalog API servers, plus the shared error format.
- **[Packages](./packages)** — the `laikacms` subpath exports and the satellite packages
  (`@laikacms/server`, `@laikacms/aws`, `@laikacms/github`, `@laikacms/git-gateway`, `laikacli`, …).
- **[Glossary](./glossary)** — terse, load-bearing definitions of protocol, repository, adapter,
  version, sync token, change feed, and atom/folder.
