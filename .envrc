# Enable corepack for pnpm version management
export COREPACK_ENABLE_AUTO_PIN=0
corepack enable

# Use Node.js version from package.json engines
# Requires mise, fnm, or similar tool that reads package.json
