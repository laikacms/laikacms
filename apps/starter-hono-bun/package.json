{
  "name": "@laikacms/starter-hono-bun",
  "version": "0.0.1",
  "private": true,
  "description": "Starter blog built with Hono + Bun runtime + LaikaCMS. Demonstrates Bun.serve() WHATWG-native laika.fetch integration and the decapAdminHtml() helper (no admin bundle build step).",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@laikacms/decap-integrations": "workspace:*",
    "hono": "catalog:server",
    "laikacms": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "catalog:tools"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
