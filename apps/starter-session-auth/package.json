{
  "name": "@laikacms/starter-session-auth",
  "description": "Starter: LaikaCMS with login page + session cookies + per-request JWT injection into decapAdminHtml. Shows mode:'custom' auth with jose.",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@laikacms/decap-integrations": "workspace:*",
    "@hono/node-server": "catalog:server",
    "hono": "catalog:server",
    "jose": "catalog:server",
    "laikacms": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:types",
    "tsx": "catalog:tools",
    "typescript": "catalog:tools"
  }
}
