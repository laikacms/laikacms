{
  "name": "@laikacms/starter-koa-backend",
  "description": "Starter: Koa 2 backend with a web-standard fetch adapter, hosting LaikaCMS over JSON:API + a Decap admin shell.",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build:admin": "esbuild src/admin-client.ts --bundle --outfile=public/admin/bundle.js --format=esm --platform=browser",
    "dev": "pnpm build:admin && tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "pnpm build:admin && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@koa/router": "^13.1.0",
    "@laikacms/decap-integrations": "workspace:*",
    "decap-cms-app": "catalog:decap",
    "koa": "^2.15.3",
    "laikacms": "workspace:*"
  },
  "devDependencies": {
    "@types/koa": "^2.15.0",
    "@types/koa__router": "^12.0.4",
    "@types/node": "catalog:types",
    "esbuild": "catalog:tools",
    "tsx": "catalog:tools",
    "typescript": "catalog:tools"
  }
}
