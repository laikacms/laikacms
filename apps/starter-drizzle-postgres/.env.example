# PostgreSQL connection string
# Works with any Postgres-compatible server: self-hosted, Supabase, Railway,
# Fly.io, Render, etc. For Neon's serverless HTTP transport use starter-neon-blog.
DATABASE_URL=postgresql://postgres:password@localhost:5432/laikacms

# Optional: HTTP port (default: 3000)
# PORT=3000
