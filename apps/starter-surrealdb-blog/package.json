{
  "name": "@laikacms/starter-surrealdb-blog",
  "version": "0.0.1",
  "private": true,
  "description": "Starter blog backed by SurrealDB. Content stored in a SurrealDB cluster via the HTTP /sql endpoint; no schema migration needed — tables are created on first write. Decap admin from CDN.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "catalog:server",
    "@laikacms/decap-integrations": "workspace:*",
    "@laikacms/surrealdb": "workspace:*",
    "hono": "catalog:server",
    "laikacms": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:types",
    "tsx": "catalog:tools",
    "typescript": "catalog:tools"
  },
  "engines": {
    "node": "22.x"
  }
}
