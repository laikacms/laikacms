{
  "name": "@laikacms/starter-mongodb-blog",
  "version": "0.0.1",
  "private": true,
  "description": "Starter blog built with Hono + MongoDB + LaikaCMS. Uses @laikacms/mongodb/storage-mongodb — a MongoDB-backed storage adapter that accepts any MongoCollectionLike, so it works with the official mongodb driver, Mongoose collections, or a fetch-based Atlas Data API shim.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "catalog:server",
    "@laikacms/decap-integrations": "workspace:*",
    "@laikacms/mongodb": "workspace:*",
    "hono": "catalog:server",
    "laikacms": "workspace:*",
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "@types/node": "catalog:types",
    "tsx": "catalog:tools",
    "typescript": "catalog:tools"
  },
  "engines": {
    "node": ">=22"
  }
}
