# etcd gRPC gateway endpoint (default: http://localhost:2379)
ETCD_URL=http://localhost:2379

# Optional: bearer token if etcd auth is enabled
# ETCD_TOKEN=your-etcd-token

# Optional: HTTP port (default: 3000)
# PORT=3000
