{
  "name": "@laikacms/starter-cloudinary-media",
  "version": "0.0.1",
  "private": true,
  "description": "Starter photo gallery using CloudinaryAssetsRepository — the first AssetsRepository (not StorageRepository) in the Laika suite. Deterministic variation URLs (no API calls), SHA-1-signed uploads, HTTP Basic for Admin API. Decap admin not applicable; this starter demos the assets contract directly.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "catalog:server",
    "@laikacms/cloudinary": "workspace:*",
    "hono": "catalog:server",
    "laikacms": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:types",
    "tsx": "catalog:tools",
    "typescript": "catalog:tools"
  },
  "engines": {
    "node": "22.x"
  }
}
