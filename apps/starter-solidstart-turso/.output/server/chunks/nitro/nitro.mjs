import process from 'node:process';globalThis._importMeta_=globalThis._importMeta_||{url:"file:///_entry.js",env:process.env};import http, { Server as Server$1 } from 'node:http';
import https, { Server } from 'node:https';
import { EventEmitter } from 'node:events';
import { Buffer as Buffer$1 } from 'node:buffer';
import { promises, existsSync } from 'node:fs';
import { resolve as resolve$1, dirname as dirname$1, join } from 'node:path';
import { createHash } from 'node:crypto';
import { AsyncLocalStorage } from 'node:async_hooks';
import invariant from 'vinxi/lib/invariant';
import { virtualId, handlerModule, join as join$1 } from 'vinxi/lib/path';
import { pathToFileURL, fileURLToPath } from 'node:url';
import { sharedConfig, lazy, createComponent, catchError, ErrorBoundary, Suspense, onCleanup } from 'solid-js';
import { getRequestEvent, renderToString, isServer, ssrElement, escape as escape$2, mergeProps, ssr, renderToStream, createComponent as createComponent$1, ssrHydrationKey, NoHydration, useAssets, Hydration, ssrAttribute, HydrationScript } from 'solid-js/web';
import { provideRequestEvent } from 'solid-js/web/storage';
import { query, Router } from '@solidjs/router';
import require$$0 from 'process';
import require$$0$1 from 'buffer';
import require$$0$2 from 'fs';

const suspectProtoRx = /"(?:_|\\u0{2}5[Ff]){2}(?:p|\\u0{2}70)(?:r|\\u0{2}72)(?:o|\\u0{2}6[Ff])(?:t|\\u0{2}74)(?:o|\\u0{2}6[Ff])(?:_|\\u0{2}5[Ff]){2}"\s*:/;
const suspectConstructorRx = /"(?:c|\\u0063)(?:o|\\u006[Ff])(?:n|\\u006[Ee])(?:s|\\u0073)(?:t|\\u0074)(?:r|\\u0072)(?:u|\\u0075)(?:c|\\u0063)(?:t|\\u0074)(?:o|\\u006[Ff])(?:r|\\u0072)"\s*:/;
const JsonSigRx = /^\s*["[{]|^\s*-?\d{1,16}(\.\d{1,17})?([Ee][+-]?\d+)?\s*$/;
function jsonParseTransform(key, value) {
  if (key === "__proto__" || key === "constructor" && value && typeof value === "object" && "prototype" in value) {
    warnKeyDropped(key);
    return;
  }
  return value;
}
function warnKeyDropped(key) {
  console.warn(`[destr] Dropping "${key}" key to prevent prototype pollution.`);
}
function destr(value, options = {}) {
  if (typeof value !== "string") {
    return value;
  }
  if (value[0] === '"' && value[value.length - 1] === '"' && value.indexOf("\\") === -1) {
    return value.slice(1, -1);
  }
  const _value = value.trim();
  if (_value.length <= 9) {
    switch (_value.toLowerCase()) {
      case "true": {
        return true;
      }
      case "false": {
        return false;
      }
      case "undefined": {
        return void 0;
      }
      case "null": {
        return null;
      }
      case "nan": {
        return Number.NaN;
      }
      case "infinity": {
        return Number.POSITIVE_INFINITY;
      }
      case "-infinity": {
        return Number.NEGATIVE_INFINITY;
      }
    }
  }
  if (!JsonSigRx.test(value)) {
    if (options.strict) {
      throw new SyntaxError("[destr] Invalid JSON");
    }
    return value;
  }
  try {
    if (suspectProtoRx.test(value) || suspectConstructorRx.test(value)) {
      if (options.strict) {
        throw new Error("[destr] Possible prototype pollution");
      }
      return JSON.parse(value, jsonParseTransform);
    }
    return JSON.parse(value);
  } catch (error) {
    if (options.strict) {
      throw error;
    }
    return value;
  }
}

const HASH_RE = /#/g;
const AMPERSAND_RE = /&/g;
const SLASH_RE = /\//g;
const EQUAL_RE = /=/g;
const PLUS_RE = /\+/g;
const ENC_CARET_RE = /%5e/gi;
const ENC_BACKTICK_RE = /%60/gi;
const ENC_PIPE_RE = /%7c/gi;
const ENC_SPACE_RE = /%20/gi;
const ENC_SLASH_RE = /%2f/gi;
function encode(text) {
  return encodeURI("" + text).replace(ENC_PIPE_RE, "|");
}
function encodeQueryValue(input) {
  return encode(typeof input === "string" ? input : JSON.stringify(input)).replace(PLUS_RE, "%2B").replace(ENC_SPACE_RE, "+").replace(HASH_RE, "%23").replace(AMPERSAND_RE, "%26").replace(ENC_BACKTICK_RE, "`").replace(ENC_CARET_RE, "^").replace(SLASH_RE, "%2F");
}
function encodeQueryKey(text) {
  return encodeQueryValue(text).replace(EQUAL_RE, "%3D");
}
function decode$1(text = "") {
  try {
    return decodeURIComponent("" + text);
  } catch {
    return "" + text;
  }
}
function decodePath(text) {
  return decode$1(text.replace(ENC_SLASH_RE, "%252F"));
}
function decodeQueryKey(text) {
  return decode$1(text.replace(PLUS_RE, " "));
}
function decodeQueryValue(text) {
  return decode$1(text.replace(PLUS_RE, " "));
}

function parseQuery(parametersString = "") {
  const object = /* @__PURE__ */ Object.create(null);
  if (parametersString[0] === "?") {
    parametersString = parametersString.slice(1);
  }
  for (const parameter of parametersString.split("&")) {
    const s = parameter.match(/([^=]+)=?(.*)/) || [];
    if (s.length < 2) {
      continue;
    }
    const key = decodeQueryKey(s[1]);
    if (key === "__proto__" || key === "constructor") {
      continue;
    }
    const value = decodeQueryValue(s[2] || "");
    if (object[key] === void 0) {
      object[key] = value;
    } else if (Array.isArray(object[key])) {
      object[key].push(value);
    } else {
      object[key] = [object[key], value];
    }
  }
  return object;
}
function encodeQueryItem(key, value) {
  if (typeof value === "number" || typeof value === "boolean") {
    value = String(value);
  }
  if (!value) {
    return encodeQueryKey(key);
  }
  if (Array.isArray(value)) {
    return value.map(
      (_value) => `${encodeQueryKey(key)}=${encodeQueryValue(_value)}`
    ).join("&");
  }
  return `${encodeQueryKey(key)}=${encodeQueryValue(value)}`;
}
function stringifyQuery(query) {
  return Object.keys(query).filter((k) => query[k] !== void 0).map((k) => encodeQueryItem(k, query[k])).filter(Boolean).join("&");
}

const PROTOCOL_STRICT_REGEX = /^[\s\w\0+.-]{2,}:([/\\]{1,2})/;
const PROTOCOL_REGEX = /^[\s\w\0+.-]{2,}:([/\\]{2})?/;
const PROTOCOL_RELATIVE_REGEX = /^([/\\]\s*){2,}[^/\\]/;
const JOIN_LEADING_SLASH_RE = /^\.?\//;
function hasProtocol(inputString, opts = {}) {
  if (typeof opts === "boolean") {
    opts = { acceptRelative: opts };
  }
  if (opts.strict) {
    return PROTOCOL_STRICT_REGEX.test(inputString);
  }
  return PROTOCOL_REGEX.test(inputString) || (opts.acceptRelative ? PROTOCOL_RELATIVE_REGEX.test(inputString) : false);
}
function hasTrailingSlash(input = "", respectQueryAndFragment) {
  {
    return input.endsWith("/");
  }
}
function withoutTrailingSlash(input = "", respectQueryAndFragment) {
  {
    return (hasTrailingSlash(input) ? input.slice(0, -1) : input) || "/";
  }
}
function withTrailingSlash(input = "", respectQueryAndFragment) {
  {
    return input.endsWith("/") ? input : input + "/";
  }
}
function hasLeadingSlash(input = "") {
  return input.startsWith("/");
}
function withLeadingSlash(input = "") {
  return hasLeadingSlash(input) ? input : "/" + input;
}
function withBase(input, base) {
  if (isEmptyURL(base) || hasProtocol(input)) {
    return input;
  }
  const _base = withoutTrailingSlash(base);
  if (input.startsWith(_base)) {
    const nextChar = input[_base.length];
    if (!nextChar || nextChar === "/" || nextChar === "?") {
      return input;
    }
  }
  return joinURL(_base, input);
}
function withoutBase(input, base) {
  if (isEmptyURL(base)) {
    return input;
  }
  const _base = withoutTrailingSlash(base);
  if (!input.startsWith(_base)) {
    return input;
  }
  const nextChar = input[_base.length];
  if (nextChar && nextChar !== "/" && nextChar !== "?") {
    return input;
  }
  const trimmed = input.slice(_base.length).replace(/^\/+/, "");
  return "/" + trimmed;
}
function withQuery(input, query) {
  const parsed = parseURL(input);
  const mergedQuery = { ...parseQuery(parsed.search), ...query };
  parsed.search = stringifyQuery(mergedQuery);
  return stringifyParsedURL(parsed);
}
function getQuery(input) {
  return parseQuery(parseURL(input).search);
}
function isEmptyURL(url) {
  return !url || url === "/";
}
function isNonEmptyURL(url) {
  return url && url !== "/";
}
function joinURL(base, ...input) {
  let url = base || "";
  for (const segment of input.filter((url2) => isNonEmptyURL(url2))) {
    if (url) {
      const _segment = segment.replace(JOIN_LEADING_SLASH_RE, "");
      url = withTrailingSlash(url) + _segment;
    } else {
      url = segment;
    }
  }
  return url;
}

const protocolRelative = Symbol.for("ufo:protocolRelative");
function parseURL(input = "", defaultProto) {
  const _specialProtoMatch = input.match(
    /^[\s\0]*(blob:|data:|javascript:|vbscript:)(.*)/i
  );
  if (_specialProtoMatch) {
    const [, _proto, _pathname = ""] = _specialProtoMatch;
    return {
      protocol: _proto.toLowerCase(),
      pathname: _pathname,
      href: _proto + _pathname,
      auth: "",
      host: "",
      search: "",
      hash: ""
    };
  }
  if (!hasProtocol(input, { acceptRelative: true })) {
    return parsePath(input);
  }
  const [, protocol = "", auth, hostAndPath = ""] = input.replace(/\\/g, "/").match(/^[\s\0]*([\w+.-]{2,}:)?\/\/([^/@]+@)?(.*)/) || [];
  let [, host = "", path = ""] = hostAndPath.match(/([^#/?]*)(.*)?/) || [];
  if (protocol === "file:") {
    path = path.replace(/\/(?=[A-Za-z]:)/, "");
  }
  const { pathname, search, hash } = parsePath(path);
  return {
    protocol: protocol.toLowerCase(),
    auth: auth ? auth.slice(0, Math.max(0, auth.length - 1)) : "",
    host,
    pathname,
    search,
    hash,
    [protocolRelative]: !protocol
  };
}
function parsePath(input = "") {
  const [pathname = "", search = "", hash = ""] = (input.match(/([^#?]*)(\?[^#]*)?(#.*)?/) || []).splice(1);
  return {
    pathname,
    search,
    hash
  };
}
function stringifyParsedURL(parsed) {
  const pathname = parsed.pathname || "";
  const search = parsed.search ? (parsed.search.startsWith("?") ? "" : "?") + parsed.search : "";
  const hash = parsed.hash || "";
  const auth = parsed.auth ? parsed.auth + "@" : "";
  const host = parsed.host || "";
  const proto = parsed.protocol || parsed[protocolRelative] ? (parsed.protocol || "") + "//" : "";
  return proto + auth + host + pathname + search + hash;
}

const NullObject = /* @__PURE__ */ (() => {
  const C = function() {
  };
  C.prototype = /* @__PURE__ */ Object.create(null);
  return C;
})();
function parse$4(str, options) {
  if (typeof str !== "string") {
    throw new TypeError("argument str must be a string");
  }
  const obj = new NullObject();
  const opt = {};
  const dec = opt.decode || decode;
  let index = 0;
  while (index < str.length) {
    const eqIdx = str.indexOf("=", index);
    if (eqIdx === -1) {
      break;
    }
    let endIdx = str.indexOf(";", index);
    if (endIdx === -1) {
      endIdx = str.length;
    } else if (endIdx < eqIdx) {
      index = str.lastIndexOf(";", eqIdx - 1) + 1;
      continue;
    }
    const key = str.slice(index, eqIdx).trim();
    if (opt?.filter && !opt?.filter(key)) {
      index = endIdx + 1;
      continue;
    }
    if (void 0 === obj[key]) {
      let val = str.slice(eqIdx + 1, endIdx).trim();
      if (val.codePointAt(0) === 34) {
        val = val.slice(1, -1);
      }
      obj[key] = tryDecode(val, dec);
    }
    index = endIdx + 1;
  }
  return obj;
}
function decode(str) {
  return str.includes("%") ? decodeURIComponent(str) : str;
}
function tryDecode(str, decode2) {
  try {
    return decode2(str);
  } catch {
    return str;
  }
}

const fieldContentRegExp = /^[\u0009\u0020-\u007E\u0080-\u00FF]+$/;
function serialize$1(name, value, options) {
  const opt = options || {};
  const enc = opt.encode || encodeURIComponent;
  if (typeof enc !== "function") {
    throw new TypeError("option encode is invalid");
  }
  if (!fieldContentRegExp.test(name)) {
    throw new TypeError("argument name is invalid");
  }
  const encodedValue = enc(value);
  if (encodedValue && !fieldContentRegExp.test(encodedValue)) {
    throw new TypeError("argument val is invalid");
  }
  let str = name + "=" + encodedValue;
  if (void 0 !== opt.maxAge && opt.maxAge !== null) {
    const maxAge = opt.maxAge - 0;
    if (Number.isNaN(maxAge) || !Number.isFinite(maxAge)) {
      throw new TypeError("option maxAge is invalid");
    }
    str += "; Max-Age=" + Math.floor(maxAge);
  }
  if (opt.domain) {
    if (!fieldContentRegExp.test(opt.domain)) {
      throw new TypeError("option domain is invalid");
    }
    str += "; Domain=" + opt.domain;
  }
  if (opt.path) {
    if (!fieldContentRegExp.test(opt.path)) {
      throw new TypeError("option path is invalid");
    }
    str += "; Path=" + opt.path;
  }
  if (opt.expires) {
    if (!isDate$2(opt.expires) || Number.isNaN(opt.expires.valueOf())) {
      throw new TypeError("option expires is invalid");
    }
    str += "; Expires=" + opt.expires.toUTCString();
  }
  if (opt.httpOnly) {
    str += "; HttpOnly";
  }
  if (opt.secure) {
    str += "; Secure";
  }
  if (opt.priority) {
    const priority = typeof opt.priority === "string" ? opt.priority.toLowerCase() : opt.priority;
    switch (priority) {
      case "low": {
        str += "; Priority=Low";
        break;
      }
      case "medium": {
        str += "; Priority=Medium";
        break;
      }
      case "high": {
        str += "; Priority=High";
        break;
      }
      default: {
        throw new TypeError("option priority is invalid");
      }
    }
  }
  if (opt.sameSite) {
    const sameSite = typeof opt.sameSite === "string" ? opt.sameSite.toLowerCase() : opt.sameSite;
    switch (sameSite) {
      case true: {
        str += "; SameSite=Strict";
        break;
      }
      case "lax": {
        str += "; SameSite=Lax";
        break;
      }
      case "strict": {
        str += "; SameSite=Strict";
        break;
      }
      case "none": {
        str += "; SameSite=None";
        break;
      }
      default: {
        throw new TypeError("option sameSite is invalid");
      }
    }
  }
  if (opt.partitioned) {
    str += "; Partitioned";
  }
  return str;
}
function isDate$2(val) {
  return Object.prototype.toString.call(val) === "[object Date]" || val instanceof Date;
}

function parseSetCookie(setCookieValue, options) {
  const parts = (setCookieValue || "").split(";").filter((str) => typeof str === "string" && !!str.trim());
  const nameValuePairStr = parts.shift() || "";
  const parsed = _parseNameValuePair(nameValuePairStr);
  const name = parsed.name;
  let value = parsed.value;
  try {
    value = options?.decode === false ? value : (options?.decode || decodeURIComponent)(value);
  } catch {
  }
  const cookie = {
    name,
    value
  };
  for (const part of parts) {
    const sides = part.split("=");
    const partKey = (sides.shift() || "").trimStart().toLowerCase();
    const partValue = sides.join("=");
    switch (partKey) {
      case "expires": {
        cookie.expires = new Date(partValue);
        break;
      }
      case "max-age": {
        cookie.maxAge = Number.parseInt(partValue, 10);
        break;
      }
      case "secure": {
        cookie.secure = true;
        break;
      }
      case "httponly": {
        cookie.httpOnly = true;
        break;
      }
      case "samesite": {
        cookie.sameSite = partValue;
        break;
      }
      default: {
        cookie[partKey] = partValue;
      }
    }
  }
  return cookie;
}
function _parseNameValuePair(nameValuePairStr) {
  let name = "";
  let value = "";
  const nameValueArr = nameValuePairStr.split("=");
  if (nameValueArr.length > 1) {
    name = nameValueArr.shift();
    value = nameValueArr.join("=");
  } else {
    value = nameValuePairStr;
  }
  return { name, value };
}

const NODE_TYPES = {
  NORMAL: 0,
  WILDCARD: 1,
  PLACEHOLDER: 2
};

function createRouter$1(options = {}) {
  const ctx = {
    options,
    rootNode: createRadixNode(),
    staticRoutesMap: {}
  };
  const normalizeTrailingSlash = (p) => options.strictTrailingSlash ? p : p.replace(/\/$/, "") || "/";
  if (options.routes) {
    for (const path in options.routes) {
      insert(ctx, normalizeTrailingSlash(path), options.routes[path]);
    }
  }
  return {
    ctx,
    lookup: (path) => lookup(ctx, normalizeTrailingSlash(path)),
    insert: (path, data) => insert(ctx, normalizeTrailingSlash(path), data),
    remove: (path) => remove(ctx, normalizeTrailingSlash(path))
  };
}
function lookup(ctx, path) {
  const staticPathNode = ctx.staticRoutesMap[path];
  if (staticPathNode) {
    return staticPathNode.data;
  }
  const sections = path.split("/");
  const params = {};
  let paramsFound = false;
  let wildcardNode = null;
  let node = ctx.rootNode;
  let wildCardParam = null;
  for (let i = 0; i < sections.length; i++) {
    const section = sections[i];
    if (node.wildcardChildNode !== null) {
      wildcardNode = node.wildcardChildNode;
      wildCardParam = sections.slice(i).join("/");
    }
    const nextNode = node.children.get(section);
    if (nextNode === void 0) {
      if (node && node.placeholderChildren.length > 1) {
        const remaining = sections.length - i;
        node = node.placeholderChildren.find((c) => c.maxDepth === remaining) || null;
      } else {
        node = node.placeholderChildren[0] || null;
      }
      if (!node) {
        break;
      }
      if (node.paramName) {
        params[node.paramName] = section;
      }
      paramsFound = true;
    } else {
      node = nextNode;
    }
  }
  if ((node === null || node.data === null) && wildcardNode !== null) {
    node = wildcardNode;
    params[node.paramName || "_"] = wildCardParam;
    paramsFound = true;
  }
  if (!node) {
    return null;
  }
  if (paramsFound) {
    return {
      ...node.data,
      params: paramsFound ? params : void 0
    };
  }
  return node.data;
}
function insert(ctx, path, data) {
  let isStaticRoute = true;
  const sections = path.split("/");
  let node = ctx.rootNode;
  let _unnamedPlaceholderCtr = 0;
  const matchedNodes = [node];
  for (const section of sections) {
    let childNode;
    if (childNode = node.children.get(section)) {
      node = childNode;
    } else {
      const type = getNodeType(section);
      childNode = createRadixNode({ type, parent: node });
      node.children.set(section, childNode);
      if (type === NODE_TYPES.PLACEHOLDER) {
        childNode.paramName = section === "*" ? `_${_unnamedPlaceholderCtr++}` : section.slice(1);
        node.placeholderChildren.push(childNode);
        isStaticRoute = false;
      } else if (type === NODE_TYPES.WILDCARD) {
        node.wildcardChildNode = childNode;
        childNode.paramName = section.slice(
          3
          /* "**:" */
        ) || "_";
        isStaticRoute = false;
      }
      matchedNodes.push(childNode);
      node = childNode;
    }
  }
  for (const [depth, node2] of matchedNodes.entries()) {
    node2.maxDepth = Math.max(matchedNodes.length - depth, node2.maxDepth || 0);
  }
  node.data = data;
  if (isStaticRoute === true) {
    ctx.staticRoutesMap[path] = node;
  }
  return node;
}
function remove(ctx, path) {
  let success = false;
  const sections = path.split("/");
  let node = ctx.rootNode;
  for (const section of sections) {
    node = node.children.get(section);
    if (!node) {
      return success;
    }
  }
  if (node.data) {
    const lastSection = sections.at(-1) || "";
    node.data = null;
    if (Object.keys(node.children).length === 0 && node.parent) {
      node.parent.children.delete(lastSection);
      node.parent.wildcardChildNode = null;
      node.parent.placeholderChildren = [];
    }
    success = true;
  }
  return success;
}
function createRadixNode(options = {}) {
  return {
    type: options.type || NODE_TYPES.NORMAL,
    maxDepth: 0,
    parent: options.parent || null,
    children: /* @__PURE__ */ new Map(),
    data: options.data || null,
    paramName: options.paramName || null,
    wildcardChildNode: null,
    placeholderChildren: []
  };
}
function getNodeType(str) {
  if (str.startsWith("**")) {
    return NODE_TYPES.WILDCARD;
  }
  if (str[0] === ":" || str === "*") {
    return NODE_TYPES.PLACEHOLDER;
  }
  return NODE_TYPES.NORMAL;
}

function toRouteMatcher(router) {
  const table = _routerNodeToTable("", router.ctx.rootNode);
  return _createMatcher(table, router.ctx.options.strictTrailingSlash);
}
function _createMatcher(table, strictTrailingSlash) {
  return {
    ctx: { table },
    matchAll: (path) => _matchRoutes(path, table, strictTrailingSlash)
  };
}
function _createRouteTable() {
  return {
    static: /* @__PURE__ */ new Map(),
    wildcard: /* @__PURE__ */ new Map(),
    dynamic: /* @__PURE__ */ new Map()
  };
}
function _matchRoutes(path, table, strictTrailingSlash) {
  if (strictTrailingSlash !== true && path.endsWith("/")) {
    path = path.slice(0, -1) || "/";
  }
  const matches = [];
  for (const [key, value] of _sortRoutesMap(table.wildcard)) {
    if (path === key || path.startsWith(key + "/")) {
      matches.push(value);
    }
  }
  for (const [key, value] of _sortRoutesMap(table.dynamic)) {
    if (path.startsWith(key + "/")) {
      const subPath = "/" + path.slice(key.length).split("/").splice(2).join("/");
      matches.push(..._matchRoutes(subPath, value));
    }
  }
  const staticMatch = table.static.get(path);
  if (staticMatch) {
    matches.push(staticMatch);
  }
  return matches.filter(Boolean);
}
function _sortRoutesMap(m) {
  return [...m.entries()].sort((a, b) => a[0].length - b[0].length);
}
function _routerNodeToTable(initialPath, initialNode) {
  const table = _createRouteTable();
  function _addNode(path, node) {
    if (path) {
      if (node.type === NODE_TYPES.NORMAL && !(path.includes("*") || path.includes(":"))) {
        if (node.data) {
          table.static.set(path, node.data);
        }
      } else if (node.type === NODE_TYPES.WILDCARD) {
        table.wildcard.set(path.replace("/**", ""), node.data);
      } else if (node.type === NODE_TYPES.PLACEHOLDER) {
        const subTable = _routerNodeToTable("", node);
        if (node.data) {
          subTable.static.set("/", node.data);
        }
        table.dynamic.set(path.replace(/\/\*|\/:\w+/, ""), subTable);
        return;
      }
    }
    for (const [childPath, child] of node.children.entries()) {
      _addNode(`${path}/${childPath}`.replace("//", "/"), child);
    }
  }
  _addNode(initialPath, initialNode);
  return table;
}

function isPlainObject(value) {
  if (value === null || typeof value !== "object") {
    return false;
  }
  const prototype = Object.getPrototypeOf(value);
  if (prototype !== null && prototype !== Object.prototype && Object.getPrototypeOf(prototype) !== null) {
    return false;
  }
  if (Symbol.iterator in value) {
    return false;
  }
  if (Symbol.toStringTag in value) {
    return Object.prototype.toString.call(value) === "[object Module]";
  }
  return true;
}

function _defu(baseObject, defaults, namespace = ".", merger) {
  if (!isPlainObject(defaults)) {
    return _defu(baseObject, {}, namespace, merger);
  }
  const object = { ...defaults };
  for (const key of Object.keys(baseObject)) {
    if (key === "__proto__" || key === "constructor") {
      continue;
    }
    const value = baseObject[key];
    if (value === null || value === void 0) {
      continue;
    }
    if (merger && merger(object, key, value, namespace)) {
      continue;
    }
    if (Array.isArray(value) && Array.isArray(object[key])) {
      object[key] = [...value, ...object[key]];
    } else if (isPlainObject(value) && isPlainObject(object[key])) {
      object[key] = _defu(
        value,
        object[key],
        (namespace ? `${namespace}.` : "") + key.toString(),
        merger
      );
    } else {
      object[key] = value;
    }
  }
  return object;
}
function createDefu(merger) {
  return (...arguments_) => (
    // eslint-disable-next-line unicorn/no-array-reduce
    arguments_.reduce((p, c) => _defu(p, c, "", merger), {})
  );
}
const defu = createDefu();
const defuFn = createDefu((object, key, currentValue) => {
  if (object[key] !== void 0 && typeof currentValue === "function") {
    object[key] = currentValue(object[key]);
    return true;
  }
});

function o(n){throw new Error(`${n} is not implemented yet!`)}let i$4 = class i extends EventEmitter{__unenv__={};readableEncoding=null;readableEnded=true;readableFlowing=false;readableHighWaterMark=0;readableLength=0;readableObjectMode=false;readableAborted=false;readableDidRead=false;closed=false;errored=null;readable=false;destroyed=false;static from(e,t){return new i(t)}constructor(e){super();}_read(e){}read(e){}setEncoding(e){return this}pause(){return this}resume(){return this}isPaused(){return  true}unpipe(e){return this}unshift(e,t){}wrap(e){return this}push(e,t){return  false}_destroy(e,t){this.removeAllListeners();}destroy(e){return this.destroyed=true,this._destroy(e),this}pipe(e,t){return {}}compose(e,t){throw new Error("Method not implemented.")}[Symbol.asyncDispose](){return this.destroy(),Promise.resolve()}async*[Symbol.asyncIterator](){throw o("Readable.asyncIterator")}iterator(e){throw o("Readable.iterator")}map(e,t){throw o("Readable.map")}filter(e,t){throw o("Readable.filter")}forEach(e,t){throw o("Readable.forEach")}reduce(e,t,r){throw o("Readable.reduce")}find(e,t){throw o("Readable.find")}findIndex(e,t){throw o("Readable.findIndex")}some(e,t){throw o("Readable.some")}toArray(e){throw o("Readable.toArray")}every(e,t){throw o("Readable.every")}flatMap(e,t){throw o("Readable.flatMap")}drop(e,t){throw o("Readable.drop")}take(e,t){throw o("Readable.take")}asIndexedPairs(e){throw o("Readable.asIndexedPairs")}};let l$3 = class l extends EventEmitter{__unenv__={};writable=true;writableEnded=false;writableFinished=false;writableHighWaterMark=0;writableLength=0;writableObjectMode=false;writableCorked=0;closed=false;errored=null;writableNeedDrain=false;writableAborted=false;destroyed=false;_data;_encoding="utf8";constructor(e){super();}pipe(e,t){return {}}_write(e,t,r){if(this.writableEnded){r&&r();return}if(this._data===void 0)this._data=e;else {const s=typeof this._data=="string"?Buffer$1.from(this._data,this._encoding||t||"utf8"):this._data,a=typeof e=="string"?Buffer$1.from(e,t||this._encoding||"utf8"):e;this._data=Buffer$1.concat([s,a]);}this._encoding=t,r&&r();}_writev(e,t){}_destroy(e,t){}_final(e){}write(e,t,r){const s=typeof t=="string"?this._encoding:"utf8",a=typeof t=="function"?t:typeof r=="function"?r:void 0;return this._write(e,s,a),true}setDefaultEncoding(e){return this}end(e,t,r){const s=typeof e=="function"?e:typeof t=="function"?t:typeof r=="function"?r:void 0;if(this.writableEnded)return s&&s(),this;const a=e===s?void 0:e;if(a){const u=t===s?void 0:t;this.write(a,u,s);}return this.writableEnded=true,this.writableFinished=true,this.emit("close"),this.emit("finish"),this}cork(){}uncork(){}destroy(e){return this.destroyed=true,delete this._data,this.removeAllListeners(),this}compose(e,t){throw new Error("Method not implemented.")}[Symbol.asyncDispose](){return Promise.resolve()}};const c=class{allowHalfOpen=true;_destroy;constructor(e=new i$4,t=new l$3){Object.assign(this,e),Object.assign(this,t),this._destroy=m$3(e._destroy,t._destroy);}};function _$2(){return Object.assign(c.prototype,i$4.prototype),Object.assign(c.prototype,l$3.prototype),c}function m$3(...n){return function(...e){for(const t of n)t(...e);}}const g$3=_$2();let A$3 = class A extends g$3{__unenv__={};bufferSize=0;bytesRead=0;bytesWritten=0;connecting=false;destroyed=false;pending=false;localAddress="";localPort=0;remoteAddress="";remoteFamily="";remotePort=0;autoSelectFamilyAttemptedAddresses=[];readyState="readOnly";constructor(e){super();}write(e,t,r){return  false}connect(e,t,r){return this}end(e,t,r){return this}setEncoding(e){return this}pause(){return this}resume(){return this}setTimeout(e,t){return this}setNoDelay(e){return this}setKeepAlive(e,t){return this}address(){return {}}unref(){return this}ref(){return this}destroySoon(){this.destroy();}resetAndDestroy(){const e=new Error("ERR_SOCKET_CLOSED");return e.code="ERR_SOCKET_CLOSED",this.destroy(e),this}};let y$3 = class y extends i$4{aborted=false;httpVersion="1.1";httpVersionMajor=1;httpVersionMinor=1;complete=true;connection;socket;headers={};trailers={};method="GET";url="/";statusCode=200;statusMessage="";closed=false;errored=null;readable=false;constructor(e){super(),this.socket=this.connection=e||new A$3;}get rawHeaders(){const e=this.headers,t=[];for(const r in e)if(Array.isArray(e[r]))for(const s of e[r])t.push(r,s);else t.push(r,e[r]);return t}get rawTrailers(){return []}setTimeout(e,t){return this}get headersDistinct(){return p$2(this.headers)}get trailersDistinct(){return p$2(this.trailers)}};function p$2(n){const e={};for(const[t,r]of Object.entries(n))t&&(e[t]=(Array.isArray(r)?r:[r]).filter(Boolean));return e}let w$4 = class w extends l$3{statusCode=200;statusMessage="";upgrading=false;chunkedEncoding=false;shouldKeepAlive=false;useChunkedEncodingByDefault=false;sendDate=false;finished=false;headersSent=false;strictContentLength=false;connection=null;socket=null;req;_headers={};constructor(e){super(),this.req=e;}assignSocket(e){e._httpMessage=this,this.socket=e,this.connection=e,this.emit("socket",e),this._flush();}_flush(){this.flushHeaders();}detachSocket(e){}writeContinue(e){}writeHead(e,t,r){e&&(this.statusCode=e),typeof t=="string"&&(this.statusMessage=t,t=void 0);const s=r||t;if(s&&!Array.isArray(s))for(const a in s)this.setHeader(a,s[a]);return this.headersSent=true,this}writeProcessing(){}setTimeout(e,t){return this}appendHeader(e,t){e=e.toLowerCase();const r=this._headers[e],s=[...Array.isArray(r)?r:[r],...Array.isArray(t)?t:[t]].filter(Boolean);return this._headers[e]=s.length>1?s:s[0],this}setHeader(e,t){return this._headers[e.toLowerCase()]=t,this}setHeaders(e){for(const[t,r]of Object.entries(e))this.setHeader(t,r);return this}getHeader(e){return this._headers[e.toLowerCase()]}getHeaders(){return this._headers}getHeaderNames(){return Object.keys(this._headers)}hasHeader(e){return e.toLowerCase()in this._headers}removeHeader(e){delete this._headers[e.toLowerCase()];}addTrailers(e){}flushHeaders(){}writeEarlyHints(e,t){typeof t=="function"&&t();}};const E$1=(()=>{const n=function(){};return n.prototype=Object.create(null),n})();function R$6(n={}){const e=new E$1,t=Array.isArray(n)||H$4(n)?n:Object.entries(n);for(const[r,s]of t)if(s){if(e[r]===void 0){e[r]=s;continue}e[r]=[...Array.isArray(e[r])?e[r]:[e[r]],...Array.isArray(s)?s:[s]];}return e}function H$4(n){return typeof n?.entries=="function"}function v$5(n={}){if(n instanceof Headers)return n;const e=new Headers;for(const[t,r]of Object.entries(n))if(r!==void 0){if(Array.isArray(r)){for(const s of r)e.append(t,String(s));continue}e.set(t,String(r));}return e}const S$3=new Set([101,204,205,304]);async function b$5(n,e){const t=new y$3,r=new w$4(t);t.url=e.url?.toString()||"/";let s;if(!t.url.startsWith("/")){const d=new URL(t.url);s=d.host,t.url=d.pathname+d.search+d.hash;}t.method=e.method||"GET",t.headers=R$6(e.headers||{}),t.headers.host||(t.headers.host=e.host||s||"localhost"),t.connection.encrypted=t.connection.encrypted||e.protocol==="https",t.body=e.body||null,t.__unenv__=e.context,await n(t,r);let a=r._data;(S$3.has(r.statusCode)||t.method.toUpperCase()==="HEAD")&&(a=null,delete r._headers["content-length"]);const u={status:r.statusCode,statusText:r.statusMessage,headers:r._headers,body:a};return t.destroy(),r.destroy(),u}async function C$6(n,e,t={}){try{const r=await b$5(n,{url:e,...t});return new Response(r.body,{status:r.status,statusText:r.statusText,headers:v$5(r.headers)})}catch(r){return new Response(r.toString(),{status:Number.parseInt(r.statusCode||r.code)||500,statusText:r.statusText})}}

function hasProp(obj, prop) {
  try {
    return prop in obj;
  } catch {
    return false;
  }
}

class H3Error extends Error {
  static __h3_error__ = true;
  statusCode = 500;
  fatal = false;
  unhandled = false;
  statusMessage;
  data;
  cause;
  constructor(message, opts = {}) {
    super(message, opts);
    if (opts.cause && !this.cause) {
      this.cause = opts.cause;
    }
  }
  toJSON() {
    const obj = {
      message: this.message,
      statusCode: sanitizeStatusCode(this.statusCode, 500)
    };
    if (this.statusMessage) {
      obj.statusMessage = sanitizeStatusMessage(this.statusMessage);
    }
    if (this.data !== void 0) {
      obj.data = this.data;
    }
    return obj;
  }
}
function createError$1(input) {
  if (typeof input === "string") {
    return new H3Error(input);
  }
  if (isError$2(input)) {
    return input;
  }
  const err = new H3Error(input.message ?? input.statusMessage ?? "", {
    cause: input.cause || input
  });
  if (hasProp(input, "stack")) {
    try {
      Object.defineProperty(err, "stack", {
        get() {
          return input.stack;
        }
      });
    } catch {
      try {
        err.stack = input.stack;
      } catch {
      }
    }
  }
  if (input.data) {
    err.data = input.data;
  }
  if (input.statusCode) {
    err.statusCode = sanitizeStatusCode(input.statusCode, err.statusCode);
  } else if (input.status) {
    err.statusCode = sanitizeStatusCode(input.status, err.statusCode);
  }
  if (input.statusMessage) {
    err.statusMessage = input.statusMessage;
  } else if (input.statusText) {
    err.statusMessage = input.statusText;
  }
  if (err.statusMessage) {
    const originalMessage = err.statusMessage;
    const sanitizedMessage = sanitizeStatusMessage(err.statusMessage);
    if (sanitizedMessage !== originalMessage) {
      console.warn(
        "[h3] Please prefer using `message` for longer error messages instead of `statusMessage`. In the future, `statusMessage` will be sanitized by default."
      );
    }
  }
  if (input.fatal !== void 0) {
    err.fatal = input.fatal;
  }
  if (input.unhandled !== void 0) {
    err.unhandled = input.unhandled;
  }
  return err;
}
function sendError(event, error, debug) {
  if (event.handled) {
    return;
  }
  const h3Error = isError$2(error) ? error : createError$1(error);
  const responseBody = {
    statusCode: h3Error.statusCode,
    statusMessage: h3Error.statusMessage,
    stack: [],
    data: h3Error.data
  };
  if (debug) {
    responseBody.stack = (h3Error.stack || "").split("\n").map((l) => l.trim());
  }
  if (event.handled) {
    return;
  }
  const _code = Number.parseInt(h3Error.statusCode);
  setResponseStatus(event, _code, h3Error.statusMessage);
  event.node.res.setHeader("content-type", MIMES.json);
  event.node.res.end(JSON.stringify(responseBody, void 0, 2));
}
function isError$2(input) {
  return input?.constructor?.__h3_error__ === true;
}
function isMethod(event, expected, allowHead) {
  if (typeof expected === "string") {
    if (event.method === expected) {
      return true;
    }
  } else if (expected.includes(event.method)) {
    return true;
  }
  return false;
}
function assertMethod(event, expected, allowHead) {
  if (!isMethod(event, expected)) {
    throw createError$1({
      statusCode: 405,
      statusMessage: "HTTP method is not allowed."
    });
  }
}
function getRequestHeaders(event) {
  const _headers = {};
  for (const key in event.node.req.headers) {
    const val = event.node.req.headers[key];
    _headers[key] = Array.isArray(val) ? val.filter(Boolean).join(", ") : val;
  }
  return _headers;
}
function getRequestHeader(event, name) {
  const headers = getRequestHeaders(event);
  const value = headers[name.toLowerCase()];
  return value;
}
function getRequestHost(event, opts = {}) {
  if (opts.xForwardedHost) {
    const _header = event.node.req.headers["x-forwarded-host"];
    const xForwardedHost = (_header || "").split(",").shift()?.trim();
    if (xForwardedHost) {
      return xForwardedHost;
    }
  }
  return event.node.req.headers.host || "localhost";
}
function getRequestProtocol(event, opts = {}) {
  if (opts.xForwardedProto !== false && event.node.req.headers["x-forwarded-proto"] === "https") {
    return "https";
  }
  return event.node.req.connection?.encrypted ? "https" : "http";
}
function getRequestURL(event, opts = {}) {
  const host = getRequestHost(event, opts);
  const protocol = getRequestProtocol(event, opts);
  const path = (event.node.req.originalUrl || event.path).replace(
    /^[/\\]+/g,
    "/"
  );
  return new URL(path, `${protocol}://${host}`);
}
function getRequestIP(event, opts = {}) {
  if (event.context.clientAddress) {
    return event.context.clientAddress;
  }
  if (opts.xForwardedFor) {
    const xForwardedFor = getRequestHeader(event, "x-forwarded-for")?.split(",").shift()?.trim();
    if (xForwardedFor) {
      return xForwardedFor;
    }
  }
  if (event.node.req.socket.remoteAddress) {
    return event.node.req.socket.remoteAddress;
  }
}

const RawBodySymbol = Symbol.for("h3RawBody");
const PayloadMethods$1 = ["PATCH", "POST", "PUT", "DELETE"];
function readRawBody(event, encoding = "utf8") {
  assertMethod(event, PayloadMethods$1);
  const _rawBody = event._requestBody || event.web?.request?.body || event.node.req[RawBodySymbol] || event.node.req.rawBody || event.node.req.body;
  if (_rawBody) {
    const promise2 = Promise.resolve(_rawBody).then((_resolved) => {
      if (Buffer.isBuffer(_resolved)) {
        return _resolved;
      }
      if (typeof _resolved.pipeTo === "function") {
        return new Promise((resolve, reject) => {
          const chunks = [];
          _resolved.pipeTo(
            new WritableStream({
              write(chunk) {
                chunks.push(chunk);
              },
              close() {
                resolve(Buffer.concat(chunks));
              },
              abort(reason) {
                reject(reason);
              }
            })
          ).catch(reject);
        });
      } else if (typeof _resolved.pipe === "function") {
        return new Promise((resolve, reject) => {
          const chunks = [];
          _resolved.on("data", (chunk) => {
            chunks.push(chunk);
          }).on("end", () => {
            resolve(Buffer.concat(chunks));
          }).on("error", reject);
        });
      }
      if (_resolved.constructor === Object) {
        return Buffer.from(JSON.stringify(_resolved));
      }
      if (_resolved instanceof URLSearchParams) {
        return Buffer.from(_resolved.toString());
      }
      if (_resolved instanceof FormData) {
        return new Response(_resolved).bytes().then((uint8arr) => Buffer.from(uint8arr));
      }
      return Buffer.from(_resolved);
    });
    return encoding ? promise2.then((buff) => buff.toString(encoding)) : promise2;
  }
  if (!Number.parseInt(event.node.req.headers["content-length"] || "") && !/\bchunked\b/i.test(
    String(event.node.req.headers["transfer-encoding"] ?? "")
  )) {
    return Promise.resolve(void 0);
  }
  const promise = event.node.req[RawBodySymbol] = new Promise(
    (resolve, reject) => {
      const bodyData = [];
      event.node.req.on("error", (err) => {
        reject(err);
      }).on("data", (chunk) => {
        bodyData.push(chunk);
      }).on("end", () => {
        resolve(Buffer.concat(bodyData));
      });
    }
  );
  const result = encoding ? promise.then((buff) => buff.toString(encoding)) : promise;
  return result;
}
function getRequestWebStream(event) {
  if (!PayloadMethods$1.includes(event.method)) {
    return;
  }
  const bodyStream = event.web?.request?.body || event._requestBody;
  if (bodyStream) {
    return bodyStream;
  }
  const _hasRawBody = RawBodySymbol in event.node.req || "rawBody" in event.node.req || "body" in event.node.req || "__unenv__" in event.node.req;
  if (_hasRawBody) {
    return new ReadableStream({
      async start(controller) {
        const _rawBody = await readRawBody(event, false);
        if (_rawBody) {
          controller.enqueue(_rawBody);
        }
        controller.close();
      }
    });
  }
  return new ReadableStream({
    start: (controller) => {
      event.node.req.on("data", (chunk) => {
        controller.enqueue(chunk);
      });
      event.node.req.on("end", () => {
        controller.close();
      });
      event.node.req.on("error", (err) => {
        controller.error(err);
      });
    }
  });
}

function handleCacheHeaders(event, opts) {
  const cacheControls = ["public", ...opts.cacheControls || []];
  let cacheMatched = false;
  if (opts.maxAge !== void 0) {
    cacheControls.push(`max-age=${+opts.maxAge}`, `s-maxage=${+opts.maxAge}`);
  }
  if (opts.modifiedTime) {
    const modifiedTime = new Date(opts.modifiedTime);
    const ifModifiedSince = event.node.req.headers["if-modified-since"];
    event.node.res.setHeader("last-modified", modifiedTime.toUTCString());
    if (ifModifiedSince && new Date(ifModifiedSince) >= modifiedTime) {
      cacheMatched = true;
    }
  }
  if (opts.etag) {
    event.node.res.setHeader("etag", opts.etag);
    const ifNonMatch = event.node.req.headers["if-none-match"];
    if (ifNonMatch === opts.etag) {
      cacheMatched = true;
    }
  }
  event.node.res.setHeader("cache-control", cacheControls.join(", "));
  if (cacheMatched) {
    event.node.res.statusCode = 304;
    if (!event.handled) {
      event.node.res.end();
    }
    return true;
  }
  return false;
}

const MIMES = {
  html: "text/html",
  json: "application/json"
};

const DISALLOWED_STATUS_CHARS = /[^\u0009\u0020-\u007E]/g;
function sanitizeStatusMessage(statusMessage = "") {
  return statusMessage.replace(DISALLOWED_STATUS_CHARS, "");
}
function sanitizeStatusCode(statusCode, defaultStatusCode = 200) {
  if (!statusCode) {
    return defaultStatusCode;
  }
  if (typeof statusCode === "string") {
    statusCode = Number.parseInt(statusCode, 10);
  }
  if (statusCode < 100 || statusCode > 999) {
    return defaultStatusCode;
  }
  return statusCode;
}

function getDistinctCookieKey(name, opts) {
  return [name, opts.domain || "", opts.path || "/"].join(";");
}

function parseCookies(event) {
  return parse$4(event.node.req.headers.cookie || "");
}
function getCookie(event, name) {
  return parseCookies(event)[name];
}
function setCookie(event, name, value, serializeOptions = {}) {
  if (!serializeOptions.path) {
    serializeOptions = { path: "/", ...serializeOptions };
  }
  const newCookie = serialize$1(name, value, serializeOptions);
  const currentCookies = splitCookiesString(
    event.node.res.getHeader("set-cookie")
  );
  if (currentCookies.length === 0) {
    event.node.res.setHeader("set-cookie", newCookie);
    return;
  }
  const newCookieKey = getDistinctCookieKey(name, serializeOptions);
  event.node.res.removeHeader("set-cookie");
  for (const cookie of currentCookies) {
    const parsed = parseSetCookie(cookie);
    const key = getDistinctCookieKey(parsed.name, parsed);
    if (key === newCookieKey) {
      continue;
    }
    event.node.res.appendHeader("set-cookie", cookie);
  }
  event.node.res.appendHeader("set-cookie", newCookie);
}
function splitCookiesString(cookiesString) {
  if (Array.isArray(cookiesString)) {
    return cookiesString.flatMap((c) => splitCookiesString(c));
  }
  if (typeof cookiesString !== "string") {
    return [];
  }
  const cookiesStrings = [];
  let pos = 0;
  let start;
  let ch;
  let lastComma;
  let nextStart;
  let cookiesSeparatorFound;
  const skipWhitespace = () => {
    while (pos < cookiesString.length && /\s/.test(cookiesString.charAt(pos))) {
      pos += 1;
    }
    return pos < cookiesString.length;
  };
  const notSpecialChar = () => {
    ch = cookiesString.charAt(pos);
    return ch !== "=" && ch !== ";" && ch !== ",";
  };
  while (pos < cookiesString.length) {
    start = pos;
    cookiesSeparatorFound = false;
    while (skipWhitespace()) {
      ch = cookiesString.charAt(pos);
      if (ch === ",") {
        lastComma = pos;
        pos += 1;
        skipWhitespace();
        nextStart = pos;
        while (pos < cookiesString.length && notSpecialChar()) {
          pos += 1;
        }
        if (pos < cookiesString.length && cookiesString.charAt(pos) === "=") {
          cookiesSeparatorFound = true;
          pos = nextStart;
          cookiesStrings.push(cookiesString.slice(start, lastComma));
          start = pos;
        } else {
          pos = lastComma + 1;
        }
      } else {
        pos += 1;
      }
    }
    if (!cookiesSeparatorFound || pos >= cookiesString.length) {
      cookiesStrings.push(cookiesString.slice(start));
    }
  }
  return cookiesStrings;
}

const defer = typeof setImmediate === "undefined" ? (fn) => fn() : setImmediate;
function send(event, data, type) {
  if (type) {
    defaultContentType(event, type);
  }
  return new Promise((resolve) => {
    defer(() => {
      if (!event.handled) {
        event.node.res.end(data);
      }
      resolve();
    });
  });
}
function sendNoContent(event, code) {
  if (event.handled) {
    return;
  }
  if (!code && event.node.res.statusCode !== 200) {
    code = event.node.res.statusCode;
  }
  const _code = sanitizeStatusCode(code, 204);
  if (_code === 204) {
    event.node.res.removeHeader("content-length");
  }
  event.node.res.writeHead(_code);
  event.node.res.end();
}
function setResponseStatus(event, code, text) {
  if (code) {
    event.node.res.statusCode = sanitizeStatusCode(
      code,
      event.node.res.statusCode
    );
  }
  if (text) {
    event.node.res.statusMessage = sanitizeStatusMessage(text);
  }
}
function getResponseStatus(event) {
  return event.node.res.statusCode;
}
function getResponseStatusText(event) {
  return event.node.res.statusMessage;
}
function defaultContentType(event, type) {
  if (type && event.node.res.statusCode !== 304 && !event.node.res.getHeader("content-type")) {
    event.node.res.setHeader("content-type", type);
  }
}
function sendRedirect(event, location, code = 302) {
  event.node.res.statusCode = sanitizeStatusCode(
    code,
    event.node.res.statusCode
  );
  event.node.res.setHeader("location", location);
  const encodedLoc = location.replace(/"/g, "%22");
  const html = `<!DOCTYPE html><html><head><meta http-equiv="refresh" content="0; url=${encodedLoc}"></head></html>`;
  return send(event, html, MIMES.html);
}
function getResponseHeaders(event) {
  return event.node.res.getHeaders();
}
function getResponseHeader(event, name) {
  return event.node.res.getHeader(name);
}
function setResponseHeaders(event, headers) {
  for (const [name, value] of Object.entries(headers)) {
    event.node.res.setHeader(
      name,
      value
    );
  }
}
const setHeaders = setResponseHeaders;
function setResponseHeader(event, name, value) {
  event.node.res.setHeader(name, value);
}
const setHeader = setResponseHeader;
function appendResponseHeader(event, name, value) {
  let current = event.node.res.getHeader(name);
  if (!current) {
    event.node.res.setHeader(name, value);
    return;
  }
  if (!Array.isArray(current)) {
    current = [current.toString()];
  }
  event.node.res.setHeader(name, [...current, value]);
}
function removeResponseHeader(event, name) {
  return event.node.res.removeHeader(name);
}
function isStream(data) {
  if (!data || typeof data !== "object") {
    return false;
  }
  if (typeof data.pipe === "function") {
    if (typeof data._read === "function") {
      return true;
    }
    if (typeof data.abort === "function") {
      return true;
    }
  }
  if (typeof data.pipeTo === "function") {
    return true;
  }
  return false;
}
function isWebResponse(data) {
  return typeof Response !== "undefined" && data instanceof Response;
}
function sendStream(event, stream) {
  if (!stream || typeof stream !== "object") {
    throw new Error("[h3] Invalid stream provided.");
  }
  event.node.res._data = stream;
  if (!event.node.res.socket) {
    event._handled = true;
    return Promise.resolve();
  }
  if (hasProp(stream, "pipeTo") && typeof stream.pipeTo === "function") {
    return stream.pipeTo(
      new WritableStream({
        write(chunk) {
          event.node.res.write(chunk);
        }
      })
    ).then(() => {
      event.node.res.end();
    });
  }
  if (hasProp(stream, "pipe") && typeof stream.pipe === "function") {
    return new Promise((resolve, reject) => {
      stream.pipe(event.node.res);
      if (stream.on) {
        stream.on("end", () => {
          event.node.res.end();
          resolve();
        });
        stream.on("error", (error) => {
          reject(error);
        });
      }
      event.node.res.on("close", () => {
        if (stream.abort) {
          stream.abort();
        }
      });
    });
  }
  throw new Error("[h3] Invalid or incompatible stream provided.");
}
function sendWebResponse(event, response) {
  for (const [key, value] of response.headers) {
    if (key === "set-cookie") {
      event.node.res.appendHeader(key, splitCookiesString(value));
    } else {
      event.node.res.setHeader(key, value);
    }
  }
  if (response.status) {
    event.node.res.statusCode = sanitizeStatusCode(
      response.status,
      event.node.res.statusCode
    );
  }
  if (response.statusText) {
    event.node.res.statusMessage = sanitizeStatusMessage(response.statusText);
  }
  if (response.redirected) {
    event.node.res.setHeader("location", response.url);
  }
  if (!response.body) {
    event.node.res.end();
    return;
  }
  return sendStream(event, response.body);
}

const PayloadMethods = /* @__PURE__ */ new Set(["PATCH", "POST", "PUT", "DELETE"]);
const ignoredHeaders = /* @__PURE__ */ new Set([
  "transfer-encoding",
  "accept-encoding",
  "connection",
  "keep-alive",
  "upgrade",
  "expect",
  "host",
  "accept"
]);
async function proxyRequest(event, target, opts = {}) {
  let body;
  let duplex;
  if (PayloadMethods.has(event.method)) {
    if (opts.streamRequest) {
      body = getRequestWebStream(event);
      duplex = "half";
    } else {
      body = await readRawBody(event, false).catch(() => void 0);
    }
  }
  const method = opts.fetchOptions?.method || event.method;
  const fetchHeaders = mergeHeaders$1(
    getProxyRequestHeaders(event, { host: target.startsWith("/") }),
    opts.fetchOptions?.headers,
    opts.headers
  );
  return sendProxy(event, target, {
    ...opts,
    fetchOptions: {
      method,
      body,
      duplex,
      ...opts.fetchOptions,
      headers: fetchHeaders
    }
  });
}
async function sendProxy(event, target, opts = {}) {
  let response;
  try {
    response = await _getFetch(opts.fetch)(target, {
      headers: opts.headers,
      ignoreResponseError: true,
      // make $ofetch.raw transparent
      ...opts.fetchOptions
    });
  } catch (error) {
    throw createError$1({
      status: 502,
      statusMessage: "Bad Gateway",
      cause: error
    });
  }
  event.node.res.statusCode = sanitizeStatusCode(
    response.status,
    event.node.res.statusCode
  );
  event.node.res.statusMessage = sanitizeStatusMessage(response.statusText);
  const cookies = [];
  for (const [key, value] of response.headers.entries()) {
    if (key === "content-encoding") {
      continue;
    }
    if (key === "content-length") {
      continue;
    }
    if (key === "set-cookie") {
      cookies.push(...splitCookiesString(value));
      continue;
    }
    event.node.res.setHeader(key, value);
  }
  if (cookies.length > 0) {
    event.node.res.setHeader(
      "set-cookie",
      cookies.map((cookie) => {
        if (opts.cookieDomainRewrite) {
          cookie = rewriteCookieProperty(
            cookie,
            opts.cookieDomainRewrite,
            "domain"
          );
        }
        if (opts.cookiePathRewrite) {
          cookie = rewriteCookieProperty(
            cookie,
            opts.cookiePathRewrite,
            "path"
          );
        }
        return cookie;
      })
    );
  }
  if (opts.onResponse) {
    await opts.onResponse(event, response);
  }
  if (response._data !== void 0) {
    return response._data;
  }
  if (event.handled) {
    return;
  }
  if (opts.sendStream === false) {
    const data = new Uint8Array(await response.arrayBuffer());
    return event.node.res.end(data);
  }
  if (response.body) {
    for await (const chunk of response.body) {
      event.node.res.write(chunk);
    }
  }
  return event.node.res.end();
}
function getProxyRequestHeaders(event, opts) {
  const headers = /* @__PURE__ */ Object.create(null);
  const reqHeaders = getRequestHeaders(event);
  for (const name in reqHeaders) {
    if (!ignoredHeaders.has(name) || name === "host" && opts?.host) {
      headers[name] = reqHeaders[name];
    }
  }
  return headers;
}
function fetchWithEvent(event, req, init, options) {
  return _getFetch(options?.fetch)(req, {
    ...init,
    context: init?.context || event.context,
    headers: {
      ...getProxyRequestHeaders(event, {
        host: typeof req === "string" && req.startsWith("/")
      }),
      ...init?.headers
    }
  });
}
function _getFetch(_fetch) {
  if (_fetch) {
    return _fetch;
  }
  if (globalThis.fetch) {
    return globalThis.fetch;
  }
  throw new Error(
    "fetch is not available. Try importing `node-fetch-native/polyfill` for Node.js."
  );
}
function rewriteCookieProperty(header, map, property) {
  const _map = typeof map === "string" ? { "*": map } : map;
  return header.replace(
    new RegExp(`(;\\s*${property}=)([^;]+)`, "gi"),
    (match, prefix, previousValue) => {
      let newValue;
      if (previousValue in _map) {
        newValue = _map[previousValue];
      } else if ("*" in _map) {
        newValue = _map["*"];
      } else {
        return match;
      }
      return newValue ? prefix + newValue : "";
    }
  );
}
function mergeHeaders$1(defaults, ...inputs) {
  const _inputs = inputs.filter(Boolean);
  if (_inputs.length === 0) {
    return defaults;
  }
  const merged = new Headers(defaults);
  for (const input of _inputs) {
    const entries = Array.isArray(input) ? input : typeof input.entries === "function" ? input.entries() : Object.entries(input);
    for (const [key, value] of entries) {
      if (value !== void 0) {
        merged.set(key, value);
      }
    }
  }
  return merged;
}

class H3Event {
  "__is_event__" = true;
  // Context
  node;
  // Node
  web;
  // Web
  context = {};
  // Shared
  // Request
  _method;
  _path;
  _headers;
  _requestBody;
  // Response
  _handled = false;
  // Hooks
  _onBeforeResponseCalled;
  _onAfterResponseCalled;
  constructor(req, res) {
    this.node = { req, res };
  }
  // --- Request ---
  get method() {
    if (!this._method) {
      this._method = (this.node.req.method || "GET").toUpperCase();
    }
    return this._method;
  }
  get path() {
    return this._path || this.node.req.url || "/";
  }
  get headers() {
    if (!this._headers) {
      this._headers = _normalizeNodeHeaders(this.node.req.headers);
    }
    return this._headers;
  }
  // --- Respoonse ---
  get handled() {
    return this._handled || this.node.res.writableEnded || this.node.res.headersSent;
  }
  respondWith(response) {
    return Promise.resolve(response).then(
      (_response) => sendWebResponse(this, _response)
    );
  }
  // --- Utils ---
  toString() {
    return `[${this.method}] ${this.path}`;
  }
  toJSON() {
    return this.toString();
  }
  // --- Deprecated ---
  /** @deprecated Please use `event.node.req` instead. */
  get req() {
    return this.node.req;
  }
  /** @deprecated Please use `event.node.res` instead. */
  get res() {
    return this.node.res;
  }
}
function isEvent(input) {
  return hasProp(input, "__is_event__");
}
function createEvent(req, res) {
  return new H3Event(req, res);
}
function _normalizeNodeHeaders(nodeHeaders) {
  const headers = new Headers();
  for (const [name, value] of Object.entries(nodeHeaders)) {
    if (Array.isArray(value)) {
      for (const item of value) {
        headers.append(name, item);
      }
    } else if (value) {
      headers.set(name, value);
    }
  }
  return headers;
}

function defineEventHandler(handler) {
  if (typeof handler === "function") {
    handler.__is_handler__ = true;
    return handler;
  }
  const _hooks = {
    onRequest: _normalizeArray(handler.onRequest),
    onBeforeResponse: _normalizeArray(handler.onBeforeResponse)
  };
  const _handler = (event) => {
    return _callHandler(event, handler.handler, _hooks);
  };
  _handler.__is_handler__ = true;
  _handler.__resolve__ = handler.handler.__resolve__;
  _handler.__websocket__ = handler.websocket;
  return _handler;
}
function _normalizeArray(input) {
  return input ? Array.isArray(input) ? input : [input] : void 0;
}
async function _callHandler(event, handler, hooks) {
  if (hooks.onRequest) {
    for (const hook of hooks.onRequest) {
      await hook(event);
      if (event.handled) {
        return;
      }
    }
  }
  const body = await handler(event);
  const response = { body };
  if (hooks.onBeforeResponse) {
    for (const hook of hooks.onBeforeResponse) {
      await hook(event, response);
    }
  }
  return response.body;
}
const eventHandler = defineEventHandler;
function isEventHandler(input) {
  return hasProp(input, "__is_handler__");
}
function toEventHandler(input, _, _route) {
  return input;
}
function defineLazyEventHandler(factory) {
  let _promise;
  let _resolved;
  const resolveHandler = () => {
    if (_resolved) {
      return Promise.resolve(_resolved);
    }
    if (!_promise) {
      _promise = Promise.resolve(factory()).then((r) => {
        const handler2 = r.default || r;
        if (typeof handler2 !== "function") {
          throw new TypeError(
            "Invalid lazy handler result. It should be a function:",
            handler2
          );
        }
        _resolved = { handler: toEventHandler(r.default || r) };
        return _resolved;
      });
    }
    return _promise;
  };
  const handler = eventHandler((event) => {
    if (_resolved) {
      return _resolved.handler(event);
    }
    return resolveHandler().then((r) => r.handler(event));
  });
  handler.__resolve__ = resolveHandler;
  return handler;
}
const lazyEventHandler = defineLazyEventHandler;

function createApp(options = {}) {
  const stack = [];
  const handler = createAppEventHandler(stack, options);
  const resolve = createResolver(stack);
  handler.__resolve__ = resolve;
  const getWebsocket = cachedFn(() => websocketOptions(resolve, options));
  const app = {
    // @ts-expect-error
    use: (arg1, arg2, arg3) => use(app, arg1, arg2, arg3),
    resolve,
    handler,
    stack,
    options,
    get websocket() {
      return getWebsocket();
    }
  };
  return app;
}
function use(app, arg1, arg2, arg3) {
  if (Array.isArray(arg1)) {
    for (const i of arg1) {
      use(app, i, arg2, arg3);
    }
  } else if (Array.isArray(arg2)) {
    for (const i of arg2) {
      use(app, arg1, i, arg3);
    }
  } else if (typeof arg1 === "string") {
    app.stack.push(
      normalizeLayer({ ...arg3, route: arg1, handler: arg2 })
    );
  } else if (typeof arg1 === "function") {
    app.stack.push(normalizeLayer({ ...arg2, handler: arg1 }));
  } else {
    app.stack.push(normalizeLayer({ ...arg1 }));
  }
  return app;
}
function createAppEventHandler(stack, options) {
  const spacing = options.debug ? 2 : void 0;
  return eventHandler(async (event) => {
    event.node.req.originalUrl = event.node.req.originalUrl || event.node.req.url || "/";
    const _rawReqUrl = event.node.req.url || "/";
    const _reqPath = _decodePath(event._path || _rawReqUrl);
    event._path = _reqPath;
    const _needsRawUrl = _reqPath !== _rawReqUrl;
    let _layerPath;
    if (options.onRequest) {
      await options.onRequest(event);
    }
    for (const layer of stack) {
      if (layer.route.length > 1) {
        if (!_reqPath.startsWith(layer.route)) {
          continue;
        }
        _layerPath = _reqPath.slice(layer.route.length) || "/";
      } else {
        _layerPath = _reqPath;
      }
      if (layer.match && !layer.match(_layerPath, event)) {
        continue;
      }
      event._path = _layerPath;
      event.node.req.url = _needsRawUrl ? layer.route.length > 1 ? _rawReqUrl.slice(layer.route.length) || "/" : _rawReqUrl : _layerPath;
      const val = await layer.handler(event);
      const _body = val === void 0 ? void 0 : await val;
      if (_body !== void 0) {
        const _response = { body: _body };
        if (options.onBeforeResponse) {
          event._onBeforeResponseCalled = true;
          await options.onBeforeResponse(event, _response);
        }
        await handleHandlerResponse(event, _response.body, spacing);
        if (options.onAfterResponse) {
          event._onAfterResponseCalled = true;
          await options.onAfterResponse(event, _response);
        }
        return;
      }
      if (event.handled) {
        if (options.onAfterResponse) {
          event._onAfterResponseCalled = true;
          await options.onAfterResponse(event, void 0);
        }
        return;
      }
    }
    if (!event.handled) {
      throw createError$1({
        statusCode: 404,
        statusMessage: `Cannot find any path matching ${event.path || "/"}.`
      });
    }
    if (options.onAfterResponse) {
      event._onAfterResponseCalled = true;
      await options.onAfterResponse(event, void 0);
    }
  });
}
function createResolver(stack) {
  return async (path) => {
    let _layerPath;
    for (const layer of stack) {
      if (layer.route === "/" && !layer.handler.__resolve__) {
        continue;
      }
      if (!path.startsWith(layer.route)) {
        continue;
      }
      _layerPath = path.slice(layer.route.length) || "/";
      if (layer.match && !layer.match(_layerPath, void 0)) {
        continue;
      }
      let res = { route: layer.route, handler: layer.handler };
      if (res.handler.__resolve__) {
        const _res = await res.handler.__resolve__(_layerPath);
        if (!_res) {
          continue;
        }
        res = {
          ...res,
          ..._res,
          route: joinURL(res.route || "/", _res.route || "/")
        };
      }
      return res;
    }
  };
}
function normalizeLayer(input) {
  let handler = input.handler;
  if (handler.handler) {
    handler = handler.handler;
  }
  if (input.lazy) {
    handler = lazyEventHandler(handler);
  } else if (!isEventHandler(handler)) {
    handler = toEventHandler(handler, void 0, input.route);
  }
  return {
    route: withoutTrailingSlash(input.route),
    match: input.match,
    handler
  };
}
function handleHandlerResponse(event, val, jsonSpace) {
  if (val === null) {
    return sendNoContent(event);
  }
  if (val) {
    if (isWebResponse(val)) {
      return sendWebResponse(event, val);
    }
    if (isStream(val)) {
      return sendStream(event, val);
    }
    if (val.buffer) {
      return send(event, val);
    }
    if (val.arrayBuffer && typeof val.arrayBuffer === "function") {
      return val.arrayBuffer().then((arrayBuffer) => {
        return send(event, Buffer.from(arrayBuffer), val.type);
      });
    }
    if (val instanceof Error) {
      throw createError$1(val);
    }
    if (typeof val.end === "function") {
      return true;
    }
  }
  const valType = typeof val;
  if (valType === "string") {
    return send(event, val, MIMES.html);
  }
  if (valType === "object" || valType === "boolean" || valType === "number") {
    return send(event, JSON.stringify(val, void 0, jsonSpace), MIMES.json);
  }
  if (valType === "bigint") {
    return send(event, val.toString(), MIMES.json);
  }
  throw createError$1({
    statusCode: 500,
    statusMessage: `[h3] Cannot send ${valType} as response.`
  });
}
function cachedFn(fn) {
  let cache;
  return () => {
    if (!cache) {
      cache = fn();
    }
    return cache;
  };
}
function _decodePath(url) {
  const qIndex = url.indexOf("?");
  const path = qIndex === -1 ? url : url.slice(0, qIndex);
  const query = qIndex === -1 ? "" : url.slice(qIndex);
  const decodedPath = path.includes("%25") ? decodePath(path.replace(/%25/g, "%2525")) : decodePath(path);
  return decodedPath + query;
}
function websocketOptions(evResolver, appOptions) {
  return {
    ...appOptions.websocket,
    async resolve(info) {
      const url = info.request?.url || info.url || "/";
      const { pathname } = typeof url === "string" ? parseURL(url) : url;
      const resolved = await evResolver(pathname);
      return resolved?.handler?.__websocket__ || {};
    }
  };
}

const RouterMethods = [
  "connect",
  "delete",
  "get",
  "head",
  "options",
  "post",
  "put",
  "trace",
  "patch"
];
function createRouter(opts = {}) {
  const _router = createRouter$1({});
  const routes = {};
  let _matcher;
  const router = {};
  const addRoute = (path, handler, method) => {
    let route = routes[path];
    if (!route) {
      routes[path] = route = { path, handlers: {} };
      _router.insert(path, route);
    }
    if (Array.isArray(method)) {
      for (const m of method) {
        addRoute(path, handler, m);
      }
    } else {
      route.handlers[method] = toEventHandler(handler);
    }
    return router;
  };
  router.use = router.add = (path, handler, method) => addRoute(path, handler, method || "all");
  for (const method of RouterMethods) {
    router[method] = (path, handle) => router.add(path, handle, method);
  }
  const matchHandler = (path = "/", method = "get") => {
    const qIndex = path.indexOf("?");
    if (qIndex !== -1) {
      path = path.slice(0, Math.max(0, qIndex));
    }
    const matched = _router.lookup(path);
    if (!matched || !matched.handlers) {
      return {
        error: createError$1({
          statusCode: 404,
          name: "Not Found",
          statusMessage: `Cannot find any route matching ${path || "/"}.`
        })
      };
    }
    let handler = matched.handlers[method] || matched.handlers.all;
    if (!handler) {
      if (!_matcher) {
        _matcher = toRouteMatcher(_router);
      }
      const _matches = _matcher.matchAll(path).reverse();
      for (const _match of _matches) {
        if (_match.handlers[method]) {
          handler = _match.handlers[method];
          matched.handlers[method] = matched.handlers[method] || handler;
          break;
        }
        if (_match.handlers.all) {
          handler = _match.handlers.all;
          matched.handlers.all = matched.handlers.all || handler;
          break;
        }
      }
    }
    if (!handler) {
      return {
        error: createError$1({
          statusCode: 405,
          name: "Method Not Allowed",
          statusMessage: `Method ${method} is not allowed on this route.`
        })
      };
    }
    return { matched, handler };
  };
  const isPreemptive = opts.preemptive || opts.preemtive;
  router.handler = eventHandler((event) => {
    const match = matchHandler(
      event.path,
      event.method.toLowerCase()
    );
    if ("error" in match) {
      if (isPreemptive) {
        throw match.error;
      } else {
        return;
      }
    }
    event.context.matchedRoute = match.matched;
    const params = match.matched.params || {};
    event.context.params = params;
    return Promise.resolve(match.handler(event)).then((res) => {
      if (res === void 0 && isPreemptive) {
        return null;
      }
      return res;
    });
  });
  router.handler.__resolve__ = async (path) => {
    path = withLeadingSlash(path);
    const match = matchHandler(path);
    if ("error" in match) {
      return;
    }
    let res = {
      route: match.matched.path,
      handler: match.handler
    };
    if (match.handler.__resolve__) {
      const _res = await match.handler.__resolve__(path);
      if (!_res) {
        return;
      }
      res = { ...res, ..._res };
    }
    return res;
  };
  return router;
}
function toNodeListener(app) {
  const toNodeHandle = async function(req, res) {
    const event = createEvent(req, res);
    try {
      await app.handler(event);
    } catch (_error) {
      const error = createError$1(_error);
      if (!isError$2(_error)) {
        error.unhandled = true;
      }
      setResponseStatus(event, error.statusCode, error.statusMessage);
      if (app.options.onError) {
        await app.options.onError(error, event);
      }
      if (event.handled) {
        return;
      }
      if (error.unhandled || error.fatal) {
        console.error("[h3]", error.fatal ? "[fatal]" : "[unhandled]", error);
      }
      if (app.options.onBeforeResponse && !event._onBeforeResponseCalled) {
        await app.options.onBeforeResponse(event, { body: error });
      }
      await sendError(event, error, !!app.options.debug);
      if (app.options.onAfterResponse && !event._onAfterResponseCalled) {
        await app.options.onAfterResponse(event, { body: error });
      }
    }
  };
  return toNodeHandle;
}

function flatHooks(configHooks, hooks = {}, parentName) {
  for (const key in configHooks) {
    const subHook = configHooks[key];
    const name = parentName ? `${parentName}:${key}` : key;
    if (typeof subHook === "object" && subHook !== null) {
      flatHooks(subHook, hooks, name);
    } else if (typeof subHook === "function") {
      hooks[name] = subHook;
    }
  }
  return hooks;
}
const defaultTask = { run: (function_) => function_() };
const _createTask = () => defaultTask;
const createTask = typeof console.createTask !== "undefined" ? console.createTask : _createTask;
function serialTaskCaller(hooks, args) {
  const name = args.shift();
  const task = createTask(name);
  return hooks.reduce(
    (promise, hookFunction) => promise.then(() => task.run(() => hookFunction(...args))),
    Promise.resolve()
  );
}
function parallelTaskCaller(hooks, args) {
  const name = args.shift();
  const task = createTask(name);
  return Promise.all(hooks.map((hook) => task.run(() => hook(...args))));
}
function callEachWith(callbacks, arg0) {
  for (const callback of [...callbacks]) {
    callback(arg0);
  }
}

class Hookable {
  constructor() {
    this._hooks = {};
    this._before = void 0;
    this._after = void 0;
    this._deprecatedMessages = void 0;
    this._deprecatedHooks = {};
    this.hook = this.hook.bind(this);
    this.callHook = this.callHook.bind(this);
    this.callHookWith = this.callHookWith.bind(this);
  }
  hook(name, function_, options = {}) {
    if (!name || typeof function_ !== "function") {
      return () => {
      };
    }
    const originalName = name;
    let dep;
    while (this._deprecatedHooks[name]) {
      dep = this._deprecatedHooks[name];
      name = dep.to;
    }
    if (dep && !options.allowDeprecated) {
      let message = dep.message;
      if (!message) {
        message = `${originalName} hook has been deprecated` + (dep.to ? `, please use ${dep.to}` : "");
      }
      if (!this._deprecatedMessages) {
        this._deprecatedMessages = /* @__PURE__ */ new Set();
      }
      if (!this._deprecatedMessages.has(message)) {
        console.warn(message);
        this._deprecatedMessages.add(message);
      }
    }
    if (!function_.name) {
      try {
        Object.defineProperty(function_, "name", {
          get: () => "_" + name.replace(/\W+/g, "_") + "_hook_cb",
          configurable: true
        });
      } catch {
      }
    }
    this._hooks[name] = this._hooks[name] || [];
    this._hooks[name].push(function_);
    return () => {
      if (function_) {
        this.removeHook(name, function_);
        function_ = void 0;
      }
    };
  }
  hookOnce(name, function_) {
    let _unreg;
    let _function = (...arguments_) => {
      if (typeof _unreg === "function") {
        _unreg();
      }
      _unreg = void 0;
      _function = void 0;
      return function_(...arguments_);
    };
    _unreg = this.hook(name, _function);
    return _unreg;
  }
  removeHook(name, function_) {
    if (this._hooks[name]) {
      const index = this._hooks[name].indexOf(function_);
      if (index !== -1) {
        this._hooks[name].splice(index, 1);
      }
      if (this._hooks[name].length === 0) {
        delete this._hooks[name];
      }
    }
  }
  deprecateHook(name, deprecated) {
    this._deprecatedHooks[name] = typeof deprecated === "string" ? { to: deprecated } : deprecated;
    const _hooks = this._hooks[name] || [];
    delete this._hooks[name];
    for (const hook of _hooks) {
      this.hook(name, hook);
    }
  }
  deprecateHooks(deprecatedHooks) {
    Object.assign(this._deprecatedHooks, deprecatedHooks);
    for (const name in deprecatedHooks) {
      this.deprecateHook(name, deprecatedHooks[name]);
    }
  }
  addHooks(configHooks) {
    const hooks = flatHooks(configHooks);
    const removeFns = Object.keys(hooks).map(
      (key) => this.hook(key, hooks[key])
    );
    return () => {
      for (const unreg of removeFns.splice(0, removeFns.length)) {
        unreg();
      }
    };
  }
  removeHooks(configHooks) {
    const hooks = flatHooks(configHooks);
    for (const key in hooks) {
      this.removeHook(key, hooks[key]);
    }
  }
  removeAllHooks() {
    for (const key in this._hooks) {
      delete this._hooks[key];
    }
  }
  callHook(name, ...arguments_) {
    arguments_.unshift(name);
    return this.callHookWith(serialTaskCaller, name, ...arguments_);
  }
  callHookParallel(name, ...arguments_) {
    arguments_.unshift(name);
    return this.callHookWith(parallelTaskCaller, name, ...arguments_);
  }
  callHookWith(caller, name, ...arguments_) {
    const event = this._before || this._after ? { name, args: arguments_, context: {} } : void 0;
    if (this._before) {
      callEachWith(this._before, event);
    }
    const result = caller(
      name in this._hooks ? [...this._hooks[name]] : [],
      arguments_
    );
    if (result instanceof Promise) {
      return result.finally(() => {
        if (this._after && event) {
          callEachWith(this._after, event);
        }
      });
    }
    if (this._after && event) {
      callEachWith(this._after, event);
    }
    return result;
  }
  beforeEach(function_) {
    this._before = this._before || [];
    this._before.push(function_);
    return () => {
      if (this._before !== void 0) {
        const index = this._before.indexOf(function_);
        if (index !== -1) {
          this._before.splice(index, 1);
        }
      }
    };
  }
  afterEach(function_) {
    this._after = this._after || [];
    this._after.push(function_);
    return () => {
      if (this._after !== void 0) {
        const index = this._after.indexOf(function_);
        if (index !== -1) {
          this._after.splice(index, 1);
        }
      }
    };
  }
}
function createHooks() {
  return new Hookable();
}

const s$1=globalThis.Headers,i$3=globalThis.AbortController,l$2=globalThis.fetch||(()=>{throw new Error("[node-fetch-native] Failed to fetch: `globalThis.fetch` is not available!")});

class FetchError extends Error {
  constructor(message, opts) {
    super(message, opts);
    this.name = "FetchError";
    if (opts?.cause && !this.cause) {
      this.cause = opts.cause;
    }
  }
}
function createFetchError(ctx) {
  const errorMessage = ctx.error?.message || ctx.error?.toString() || "";
  const method = ctx.request?.method || ctx.options?.method || "GET";
  const url = ctx.request?.url || String(ctx.request) || "/";
  const requestStr = `[${method}] ${JSON.stringify(url)}`;
  const statusStr = ctx.response ? `${ctx.response.status} ${ctx.response.statusText}` : "<no response>";
  const message = `${requestStr}: ${statusStr}${errorMessage ? ` ${errorMessage}` : ""}`;
  const fetchError = new FetchError(
    message,
    ctx.error ? { cause: ctx.error } : void 0
  );
  for (const key of ["request", "options", "response"]) {
    Object.defineProperty(fetchError, key, {
      get() {
        return ctx[key];
      }
    });
  }
  for (const [key, refKey] of [
    ["data", "_data"],
    ["status", "status"],
    ["statusCode", "status"],
    ["statusText", "statusText"],
    ["statusMessage", "statusText"]
  ]) {
    Object.defineProperty(fetchError, key, {
      get() {
        return ctx.response && ctx.response[refKey];
      }
    });
  }
  return fetchError;
}

const payloadMethods = new Set(
  Object.freeze(["PATCH", "POST", "PUT", "DELETE"])
);
function isPayloadMethod(method = "GET") {
  return payloadMethods.has(method.toUpperCase());
}
function isJSONSerializable(value) {
  if (value === void 0) {
    return false;
  }
  const t = typeof value;
  if (t === "string" || t === "number" || t === "boolean" || t === null) {
    return true;
  }
  if (t !== "object") {
    return false;
  }
  if (Array.isArray(value)) {
    return true;
  }
  if (value.buffer) {
    return false;
  }
  if (value instanceof FormData || value instanceof URLSearchParams) {
    return false;
  }
  return value.constructor && value.constructor.name === "Object" || typeof value.toJSON === "function";
}
const textTypes = /* @__PURE__ */ new Set([
  "image/svg",
  "application/xml",
  "application/xhtml",
  "application/html"
]);
const JSON_RE = /^application\/(?:[\w!#$%&*.^`~-]*\+)?json(;.+)?$/i;
function detectResponseType(_contentType = "") {
  if (!_contentType) {
    return "json";
  }
  const contentType = _contentType.split(";").shift() || "";
  if (JSON_RE.test(contentType)) {
    return "json";
  }
  if (contentType === "text/event-stream") {
    return "stream";
  }
  if (textTypes.has(contentType) || contentType.startsWith("text/")) {
    return "text";
  }
  return "blob";
}
function resolveFetchOptions(request, input, defaults, Headers) {
  const headers = mergeHeaders(
    input?.headers ?? request?.headers,
    defaults?.headers,
    Headers
  );
  let query;
  if (defaults?.query || defaults?.params || input?.params || input?.query) {
    query = {
      ...defaults?.params,
      ...defaults?.query,
      ...input?.params,
      ...input?.query
    };
  }
  return {
    ...defaults,
    ...input,
    query,
    params: query,
    headers
  };
}
function mergeHeaders(input, defaults, Headers) {
  if (!defaults) {
    return new Headers(input);
  }
  const headers = new Headers(defaults);
  if (input) {
    for (const [key, value] of Symbol.iterator in input || Array.isArray(input) ? input : new Headers(input)) {
      headers.set(key, value);
    }
  }
  return headers;
}
async function callHooks(context, hooks) {
  if (hooks) {
    if (Array.isArray(hooks)) {
      for (const hook of hooks) {
        await hook(context);
      }
    } else {
      await hooks(context);
    }
  }
}

const retryStatusCodes = /* @__PURE__ */ new Set([
  408,
  // Request Timeout
  409,
  // Conflict
  425,
  // Too Early (Experimental)
  429,
  // Too Many Requests
  500,
  // Internal Server Error
  502,
  // Bad Gateway
  503,
  // Service Unavailable
  504
  // Gateway Timeout
]);
const nullBodyResponses = /* @__PURE__ */ new Set([101, 204, 205, 304]);
function createFetch(globalOptions = {}) {
  const {
    fetch = globalThis.fetch,
    Headers = globalThis.Headers,
    AbortController = globalThis.AbortController
  } = globalOptions;
  async function onError(context) {
    const isAbort = context.error && context.error.name === "AbortError" && !context.options.timeout || false;
    if (context.options.retry !== false && !isAbort) {
      let retries;
      if (typeof context.options.retry === "number") {
        retries = context.options.retry;
      } else {
        retries = isPayloadMethod(context.options.method) ? 0 : 1;
      }
      const responseCode = context.response && context.response.status || 500;
      if (retries > 0 && (Array.isArray(context.options.retryStatusCodes) ? context.options.retryStatusCodes.includes(responseCode) : retryStatusCodes.has(responseCode))) {
        const retryDelay = typeof context.options.retryDelay === "function" ? context.options.retryDelay(context) : context.options.retryDelay || 0;
        if (retryDelay > 0) {
          await new Promise((resolve) => setTimeout(resolve, retryDelay));
        }
        return $fetchRaw(context.request, {
          ...context.options,
          retry: retries - 1
        });
      }
    }
    const error = createFetchError(context);
    if (Error.captureStackTrace) {
      Error.captureStackTrace(error, $fetchRaw);
    }
    throw error;
  }
  const $fetchRaw = async function $fetchRaw2(_request, _options = {}) {
    const context = {
      request: _request,
      options: resolveFetchOptions(
        _request,
        _options,
        globalOptions.defaults,
        Headers
      ),
      response: void 0,
      error: void 0
    };
    if (context.options.method) {
      context.options.method = context.options.method.toUpperCase();
    }
    if (context.options.onRequest) {
      await callHooks(context, context.options.onRequest);
      if (!(context.options.headers instanceof Headers)) {
        context.options.headers = new Headers(
          context.options.headers || {}
          /* compat */
        );
      }
    }
    if (typeof context.request === "string") {
      if (context.options.baseURL) {
        context.request = withBase(context.request, context.options.baseURL);
      }
      if (context.options.query) {
        context.request = withQuery(context.request, context.options.query);
        delete context.options.query;
      }
      if ("query" in context.options) {
        delete context.options.query;
      }
      if ("params" in context.options) {
        delete context.options.params;
      }
    }
    if (context.options.body && isPayloadMethod(context.options.method)) {
      if (isJSONSerializable(context.options.body)) {
        const contentType = context.options.headers.get("content-type");
        if (typeof context.options.body !== "string") {
          context.options.body = contentType === "application/x-www-form-urlencoded" ? new URLSearchParams(
            context.options.body
          ).toString() : JSON.stringify(context.options.body);
        }
        if (!contentType) {
          context.options.headers.set("content-type", "application/json");
        }
        if (!context.options.headers.has("accept")) {
          context.options.headers.set("accept", "application/json");
        }
      } else if (
        // ReadableStream Body
        "pipeTo" in context.options.body && typeof context.options.body.pipeTo === "function" || // Node.js Stream Body
        typeof context.options.body.pipe === "function"
      ) {
        if (!("duplex" in context.options)) {
          context.options.duplex = "half";
        }
      }
    }
    let abortTimeout;
    if (!context.options.signal && context.options.timeout) {
      const controller = new AbortController();
      abortTimeout = setTimeout(() => {
        const error = new Error(
          "[TimeoutError]: The operation was aborted due to timeout"
        );
        error.name = "TimeoutError";
        error.code = 23;
        controller.abort(error);
      }, context.options.timeout);
      context.options.signal = controller.signal;
    }
    try {
      context.response = await fetch(
        context.request,
        context.options
      );
    } catch (error) {
      context.error = error;
      if (context.options.onRequestError) {
        await callHooks(
          context,
          context.options.onRequestError
        );
      }
      return await onError(context);
    } finally {
      if (abortTimeout) {
        clearTimeout(abortTimeout);
      }
    }
    const hasBody = (context.response.body || // https://github.com/unjs/ofetch/issues/324
    // https://github.com/unjs/ofetch/issues/294
    // https://github.com/JakeChampion/fetch/issues/1454
    context.response._bodyInit) && !nullBodyResponses.has(context.response.status) && context.options.method !== "HEAD";
    if (hasBody) {
      const responseType = (context.options.parseResponse ? "json" : context.options.responseType) || detectResponseType(context.response.headers.get("content-type") || "");
      switch (responseType) {
        case "json": {
          const data = await context.response.text();
          const parseFunction = context.options.parseResponse || destr;
          context.response._data = parseFunction(data);
          break;
        }
        case "stream": {
          context.response._data = context.response.body || context.response._bodyInit;
          break;
        }
        default: {
          context.response._data = await context.response[responseType]();
        }
      }
    }
    if (context.options.onResponse) {
      await callHooks(
        context,
        context.options.onResponse
      );
    }
    if (!context.options.ignoreResponseError && context.response.status >= 400 && context.response.status < 600) {
      if (context.options.onResponseError) {
        await callHooks(
          context,
          context.options.onResponseError
        );
      }
      return await onError(context);
    }
    return context.response;
  };
  const $fetch = async function $fetch2(request, options) {
    const r = await $fetchRaw(request, options);
    return r._data;
  };
  $fetch.raw = $fetchRaw;
  $fetch.native = (...args) => fetch(...args);
  $fetch.create = (defaultOptions = {}, customGlobalOptions = {}) => createFetch({
    ...globalOptions,
    ...customGlobalOptions,
    defaults: {
      ...globalOptions.defaults,
      ...customGlobalOptions.defaults,
      ...defaultOptions
    }
  });
  return $fetch;
}

function createNodeFetch() {
  const useKeepAlive = JSON.parse(process.env.FETCH_KEEP_ALIVE || "false");
  if (!useKeepAlive) {
    return l$2;
  }
  const agentOptions = { keepAlive: true };
  const httpAgent = new http.Agent(agentOptions);
  const httpsAgent = new https.Agent(agentOptions);
  const nodeFetchOptions = {
    agent(parsedURL) {
      return parsedURL.protocol === "http:" ? httpAgent : httpsAgent;
    }
  };
  return function nodeFetchWithKeepAlive(input, init) {
    return l$2(input, { ...nodeFetchOptions, ...init });
  };
}
const fetch = globalThis.fetch ? (...args) => globalThis.fetch(...args) : createNodeFetch();
const Headers$1 = globalThis.Headers || s$1;
const AbortController$1 = globalThis.AbortController || i$3;
createFetch({ fetch, Headers: Headers$1, AbortController: AbortController$1 });

function wrapToPromise(value) {
  if (!value || typeof value.then !== "function") {
    return Promise.resolve(value);
  }
  return value;
}
function asyncCall(function_, ...arguments_) {
  try {
    return wrapToPromise(function_(...arguments_));
  } catch (error) {
    return Promise.reject(error);
  }
}
function isPrimitive(value) {
  const type = typeof value;
  return value === null || type !== "object" && type !== "function";
}
function isPureObject(value) {
  const proto = Object.getPrototypeOf(value);
  return !proto || proto.isPrototypeOf(Object);
}
function stringify$d(value) {
  if (isPrimitive(value)) {
    return String(value);
  }
  if (isPureObject(value) || Array.isArray(value)) {
    return JSON.stringify(value);
  }
  if (typeof value.toJSON === "function") {
    return stringify$d(value.toJSON());
  }
  throw new Error("[unstorage] Cannot stringify value!");
}
const BASE64_PREFIX = "base64:";
function serializeRaw(value) {
  if (typeof value === "string") {
    return value;
  }
  return BASE64_PREFIX + base64Encode(value);
}
function deserializeRaw(value) {
  if (typeof value !== "string") {
    return value;
  }
  if (!value.startsWith(BASE64_PREFIX)) {
    return value;
  }
  return base64Decode(value.slice(BASE64_PREFIX.length));
}
function base64Decode(input) {
  if (globalThis.Buffer) {
    return Buffer.from(input, "base64");
  }
  return Uint8Array.from(
    globalThis.atob(input),
    (c) => c.codePointAt(0)
  );
}
function base64Encode(input) {
  if (globalThis.Buffer) {
    return Buffer.from(input).toString("base64");
  }
  return globalThis.btoa(String.fromCodePoint(...input));
}

const storageKeyProperties = [
  "has",
  "hasItem",
  "get",
  "getItem",
  "getItemRaw",
  "set",
  "setItem",
  "setItemRaw",
  "del",
  "remove",
  "removeItem",
  "getMeta",
  "setMeta",
  "removeMeta",
  "getKeys",
  "clear",
  "mount",
  "unmount"
];
function prefixStorage(storage, base) {
  base = normalizeBaseKey(base);
  if (!base) {
    return storage;
  }
  const nsStorage = { ...storage };
  for (const property of storageKeyProperties) {
    nsStorage[property] = (key = "", ...args) => (
      // @ts-ignore
      storage[property](base + key, ...args)
    );
  }
  nsStorage.getKeys = (key = "", ...arguments_) => storage.getKeys(base + key, ...arguments_).then((keys) => keys.map((key2) => key2.slice(base.length)));
  nsStorage.keys = nsStorage.getKeys;
  nsStorage.getItems = async (items, commonOptions) => {
    const prefixedItems = items.map(
      (item) => typeof item === "string" ? base + item : { ...item, key: base + item.key }
    );
    const results = await storage.getItems(prefixedItems, commonOptions);
    return results.map((entry) => ({
      key: entry.key.slice(base.length),
      value: entry.value
    }));
  };
  nsStorage.setItems = async (items, commonOptions) => {
    const prefixedItems = items.map((item) => ({
      key: base + item.key,
      value: item.value,
      options: item.options
    }));
    return storage.setItems(prefixedItems, commonOptions);
  };
  return nsStorage;
}
function normalizeKey$1(key) {
  if (!key) {
    return "";
  }
  return key.split("?")[0]?.replace(/[/\\]/g, ":").replace(/:+/g, ":").replace(/^:|:$/g, "") || "";
}
function joinKeys(...keys) {
  return normalizeKey$1(keys.join(":"));
}
function normalizeBaseKey(base) {
  base = normalizeKey$1(base);
  return base ? base + ":" : "";
}
function filterKeyByDepth(key, depth) {
  if (depth === void 0) {
    return true;
  }
  let substrCount = 0;
  let index = key.indexOf(":");
  while (index > -1) {
    substrCount++;
    index = key.indexOf(":", index + 1);
  }
  return substrCount <= depth;
}
function filterKeyByBase(key, base) {
  if (base) {
    return key.startsWith(base) && key[key.length - 1] !== "$";
  }
  return key[key.length - 1] !== "$";
}

function defineDriver$1(factory) {
  return factory;
}

const DRIVER_NAME$1 = "memory";
const memory = defineDriver$1(() => {
  const data = /* @__PURE__ */ new Map();
  return {
    name: DRIVER_NAME$1,
    getInstance: () => data,
    hasItem(key) {
      return data.has(key);
    },
    getItem(key) {
      return data.get(key) ?? null;
    },
    getItemRaw(key) {
      return data.get(key) ?? null;
    },
    setItem(key, value) {
      data.set(key, value);
    },
    setItemRaw(key, value) {
      data.set(key, value);
    },
    removeItem(key) {
      data.delete(key);
    },
    getKeys() {
      return [...data.keys()];
    },
    clear() {
      data.clear();
    },
    dispose() {
      data.clear();
    }
  };
});

function createStorage(options = {}) {
  const context = {
    mounts: { "": options.driver || memory() },
    mountpoints: [""],
    watching: false,
    watchListeners: [],
    unwatch: {}
  };
  const getMount = (key) => {
    for (const base of context.mountpoints) {
      if (key.startsWith(base)) {
        return {
          base,
          relativeKey: key.slice(base.length),
          driver: context.mounts[base]
        };
      }
    }
    return {
      base: "",
      relativeKey: key,
      driver: context.mounts[""]
    };
  };
  const getMounts = (base, includeParent) => {
    return context.mountpoints.filter(
      (mountpoint) => mountpoint.startsWith(base) || includeParent && base.startsWith(mountpoint)
    ).map((mountpoint) => ({
      relativeBase: base.length > mountpoint.length ? base.slice(mountpoint.length) : void 0,
      mountpoint,
      driver: context.mounts[mountpoint]
    }));
  };
  const onChange = (event, key) => {
    if (!context.watching) {
      return;
    }
    key = normalizeKey$1(key);
    for (const listener of context.watchListeners) {
      listener(event, key);
    }
  };
  const startWatch = async () => {
    if (context.watching) {
      return;
    }
    context.watching = true;
    for (const mountpoint in context.mounts) {
      context.unwatch[mountpoint] = await watch(
        context.mounts[mountpoint],
        onChange,
        mountpoint
      );
    }
  };
  const stopWatch = async () => {
    if (!context.watching) {
      return;
    }
    for (const mountpoint in context.unwatch) {
      await context.unwatch[mountpoint]();
    }
    context.unwatch = {};
    context.watching = false;
  };
  const runBatch = (items, commonOptions, cb) => {
    const batches = /* @__PURE__ */ new Map();
    const getBatch = (mount) => {
      let batch = batches.get(mount.base);
      if (!batch) {
        batch = {
          driver: mount.driver,
          base: mount.base,
          items: []
        };
        batches.set(mount.base, batch);
      }
      return batch;
    };
    for (const item of items) {
      const isStringItem = typeof item === "string";
      const key = normalizeKey$1(isStringItem ? item : item.key);
      const value = isStringItem ? void 0 : item.value;
      const options2 = isStringItem || !item.options ? commonOptions : { ...commonOptions, ...item.options };
      const mount = getMount(key);
      getBatch(mount).items.push({
        key,
        value,
        relativeKey: mount.relativeKey,
        options: options2
      });
    }
    return Promise.all([...batches.values()].map((batch) => cb(batch))).then(
      (r) => r.flat()
    );
  };
  const storage = {
    // Item
    hasItem(key, opts = {}) {
      key = normalizeKey$1(key);
      const { relativeKey, driver } = getMount(key);
      return asyncCall(driver.hasItem, relativeKey, opts);
    },
    getItem(key, opts = {}) {
      key = normalizeKey$1(key);
      const { relativeKey, driver } = getMount(key);
      return asyncCall(driver.getItem, relativeKey, opts).then(
        (value) => destr(value)
      );
    },
    getItems(items, commonOptions = {}) {
      return runBatch(items, commonOptions, (batch) => {
        if (batch.driver.getItems) {
          return asyncCall(
            batch.driver.getItems,
            batch.items.map((item) => ({
              key: item.relativeKey,
              options: item.options
            })),
            commonOptions
          ).then(
            (r) => r.map((item) => ({
              key: joinKeys(batch.base, item.key),
              value: destr(item.value)
            }))
          );
        }
        return Promise.all(
          batch.items.map((item) => {
            return asyncCall(
              batch.driver.getItem,
              item.relativeKey,
              item.options
            ).then((value) => ({
              key: item.key,
              value: destr(value)
            }));
          })
        );
      });
    },
    getItemRaw(key, opts = {}) {
      key = normalizeKey$1(key);
      const { relativeKey, driver } = getMount(key);
      if (driver.getItemRaw) {
        return asyncCall(driver.getItemRaw, relativeKey, opts);
      }
      return asyncCall(driver.getItem, relativeKey, opts).then(
        (value) => deserializeRaw(value)
      );
    },
    async setItem(key, value, opts = {}) {
      if (value === void 0) {
        return storage.removeItem(key);
      }
      key = normalizeKey$1(key);
      const { relativeKey, driver } = getMount(key);
      if (!driver.setItem) {
        return;
      }
      await asyncCall(driver.setItem, relativeKey, stringify$d(value), opts);
      if (!driver.watch) {
        onChange("update", key);
      }
    },
    async setItems(items, commonOptions) {
      await runBatch(items, commonOptions, async (batch) => {
        if (batch.driver.setItems) {
          return asyncCall(
            batch.driver.setItems,
            batch.items.map((item) => ({
              key: item.relativeKey,
              value: stringify$d(item.value),
              options: item.options
            })),
            commonOptions
          );
        }
        if (!batch.driver.setItem) {
          return;
        }
        await Promise.all(
          batch.items.map((item) => {
            return asyncCall(
              batch.driver.setItem,
              item.relativeKey,
              stringify$d(item.value),
              item.options
            );
          })
        );
      });
    },
    async setItemRaw(key, value, opts = {}) {
      if (value === void 0) {
        return storage.removeItem(key, opts);
      }
      key = normalizeKey$1(key);
      const { relativeKey, driver } = getMount(key);
      if (driver.setItemRaw) {
        await asyncCall(driver.setItemRaw, relativeKey, value, opts);
      } else if (driver.setItem) {
        await asyncCall(driver.setItem, relativeKey, serializeRaw(value), opts);
      } else {
        return;
      }
      if (!driver.watch) {
        onChange("update", key);
      }
    },
    async removeItem(key, opts = {}) {
      if (typeof opts === "boolean") {
        opts = { removeMeta: opts };
      }
      key = normalizeKey$1(key);
      const { relativeKey, driver } = getMount(key);
      if (!driver.removeItem) {
        return;
      }
      await asyncCall(driver.removeItem, relativeKey, opts);
      if (opts.removeMeta || opts.removeMata) {
        await asyncCall(driver.removeItem, relativeKey + "$", opts);
      }
      if (!driver.watch) {
        onChange("remove", key);
      }
    },
    // Meta
    async getMeta(key, opts = {}) {
      if (typeof opts === "boolean") {
        opts = { nativeOnly: opts };
      }
      key = normalizeKey$1(key);
      const { relativeKey, driver } = getMount(key);
      const meta = /* @__PURE__ */ Object.create(null);
      if (driver.getMeta) {
        Object.assign(meta, await asyncCall(driver.getMeta, relativeKey, opts));
      }
      if (!opts.nativeOnly) {
        const value = await asyncCall(
          driver.getItem,
          relativeKey + "$",
          opts
        ).then((value_) => destr(value_));
        if (value && typeof value === "object") {
          if (typeof value.atime === "string") {
            value.atime = new Date(value.atime);
          }
          if (typeof value.mtime === "string") {
            value.mtime = new Date(value.mtime);
          }
          Object.assign(meta, value);
        }
      }
      return meta;
    },
    setMeta(key, value, opts = {}) {
      return this.setItem(key + "$", value, opts);
    },
    removeMeta(key, opts = {}) {
      return this.removeItem(key + "$", opts);
    },
    // Keys
    async getKeys(base, opts = {}) {
      base = normalizeBaseKey(base);
      const mounts = getMounts(base, true);
      let maskedMounts = [];
      const allKeys = [];
      let allMountsSupportMaxDepth = true;
      for (const mount of mounts) {
        if (!mount.driver.flags?.maxDepth) {
          allMountsSupportMaxDepth = false;
        }
        const rawKeys = await asyncCall(
          mount.driver.getKeys,
          mount.relativeBase,
          opts
        );
        for (const key of rawKeys) {
          const fullKey = mount.mountpoint + normalizeKey$1(key);
          if (!maskedMounts.some((p) => fullKey.startsWith(p))) {
            allKeys.push(fullKey);
          }
        }
        maskedMounts = [
          mount.mountpoint,
          ...maskedMounts.filter((p) => !p.startsWith(mount.mountpoint))
        ];
      }
      const shouldFilterByDepth = opts.maxDepth !== void 0 && !allMountsSupportMaxDepth;
      return allKeys.filter(
        (key) => (!shouldFilterByDepth || filterKeyByDepth(key, opts.maxDepth)) && filterKeyByBase(key, base)
      );
    },
    // Utils
    async clear(base, opts = {}) {
      base = normalizeBaseKey(base);
      await Promise.all(
        getMounts(base, false).map(async (m) => {
          if (m.driver.clear) {
            return asyncCall(m.driver.clear, m.relativeBase, opts);
          }
          if (m.driver.removeItem) {
            const keys = await m.driver.getKeys(m.relativeBase || "", opts);
            return Promise.all(
              keys.map((key) => m.driver.removeItem(key, opts))
            );
          }
        })
      );
    },
    async dispose() {
      await Promise.all(
        Object.values(context.mounts).map((driver) => dispose(driver))
      );
    },
    async watch(callback) {
      await startWatch();
      context.watchListeners.push(callback);
      return async () => {
        context.watchListeners = context.watchListeners.filter(
          (listener) => listener !== callback
        );
        if (context.watchListeners.length === 0) {
          await stopWatch();
        }
      };
    },
    async unwatch() {
      context.watchListeners = [];
      await stopWatch();
    },
    // Mount
    mount(base, driver) {
      base = normalizeBaseKey(base);
      if (base && context.mounts[base]) {
        throw new Error(`already mounted at ${base}`);
      }
      if (base) {
        context.mountpoints.push(base);
        context.mountpoints.sort((a, b) => b.length - a.length);
      }
      context.mounts[base] = driver;
      if (context.watching) {
        Promise.resolve(watch(driver, onChange, base)).then((unwatcher) => {
          context.unwatch[base] = unwatcher;
        }).catch(console.error);
      }
      return storage;
    },
    async unmount(base, _dispose = true) {
      base = normalizeBaseKey(base);
      if (!base || !context.mounts[base]) {
        return;
      }
      if (context.watching && base in context.unwatch) {
        context.unwatch[base]?.();
        delete context.unwatch[base];
      }
      if (_dispose) {
        await dispose(context.mounts[base]);
      }
      context.mountpoints = context.mountpoints.filter((key) => key !== base);
      delete context.mounts[base];
    },
    getMount(key = "") {
      key = normalizeKey$1(key) + ":";
      const m = getMount(key);
      return {
        driver: m.driver,
        base: m.base
      };
    },
    getMounts(base = "", opts = {}) {
      base = normalizeKey$1(base);
      const mounts = getMounts(base, opts.parents);
      return mounts.map((m) => ({
        driver: m.driver,
        base: m.mountpoint
      }));
    },
    // Aliases
    keys: (base, opts = {}) => storage.getKeys(base, opts),
    get: (key, opts = {}) => storage.getItem(key, opts),
    set: (key, value, opts = {}) => storage.setItem(key, value, opts),
    has: (key, opts = {}) => storage.hasItem(key, opts),
    del: (key, opts = {}) => storage.removeItem(key, opts),
    remove: (key, opts = {}) => storage.removeItem(key, opts)
  };
  return storage;
}
function watch(driver, onChange, base) {
  return driver.watch ? driver.watch((event, key) => onChange(event, base + key)) : () => {
  };
}
async function dispose(driver) {
  if (typeof driver.dispose === "function") {
    await asyncCall(driver.dispose);
  }
}

const _assets = {

};

const normalizeKey = function normalizeKey(key) {
  if (!key) {
    return "";
  }
  return key.split("?")[0]?.replace(/[/\\]/g, ":").replace(/:+/g, ":").replace(/^:|:$/g, "") || "";
};

const assets$1 = {
  getKeys() {
    return Promise.resolve(Object.keys(_assets))
  },
  hasItem (id) {
    id = normalizeKey(id);
    return Promise.resolve(id in _assets)
  },
  getItem (id) {
    id = normalizeKey(id);
    return Promise.resolve(_assets[id] ? _assets[id].import() : null)
  },
  getMeta (id) {
    id = normalizeKey(id);
    return Promise.resolve(_assets[id] ? _assets[id].meta : {})
  }
};

function defineDriver(factory) {
  return factory;
}
function createError(driver, message, opts) {
  const err = new Error(`[unstorage] [${driver}] ${message}`, opts);
  if (Error.captureStackTrace) {
    Error.captureStackTrace(err, createError);
  }
  return err;
}
function createRequiredError(driver, name) {
  if (Array.isArray(name)) {
    return createError(
      driver,
      `Missing some of the required options ${name.map((n) => "`" + n + "`").join(", ")}`
    );
  }
  return createError(driver, `Missing required option \`${name}\`.`);
}

function ignoreNotfound(err) {
  return err.code === "ENOENT" || err.code === "EISDIR" ? null : err;
}
function ignoreExists(err) {
  return err.code === "EEXIST" ? null : err;
}
async function writeFile(path, data, encoding) {
  await ensuredir(dirname$1(path));
  return promises.writeFile(path, data, encoding);
}
function readFile(path, encoding) {
  return promises.readFile(path, encoding).catch(ignoreNotfound);
}
function unlink(path) {
  return promises.unlink(path).catch(ignoreNotfound);
}
function readdir(dir) {
  return promises.readdir(dir, { withFileTypes: true }).catch(ignoreNotfound).then((r) => r || []);
}
async function ensuredir(dir) {
  if (existsSync(dir)) {
    return;
  }
  await ensuredir(dirname$1(dir)).catch(ignoreExists);
  await promises.mkdir(dir).catch(ignoreExists);
}
async function readdirRecursive(dir, ignore, maxDepth) {
  if (ignore && ignore(dir)) {
    return [];
  }
  const entries = await readdir(dir);
  const files = [];
  await Promise.all(
    entries.map(async (entry) => {
      const entryPath = resolve$1(dir, entry.name);
      if (entry.isDirectory()) {
        if (maxDepth === void 0 || maxDepth > 0) {
          const dirFiles = await readdirRecursive(
            entryPath,
            ignore,
            maxDepth === void 0 ? void 0 : maxDepth - 1
          );
          files.push(...dirFiles.map((f) => entry.name + "/" + f));
        }
      } else {
        if (!(ignore && ignore(entry.name))) {
          files.push(entry.name);
        }
      }
    })
  );
  return files;
}
async function rmRecursive(dir) {
  const entries = await readdir(dir);
  await Promise.all(
    entries.map((entry) => {
      const entryPath = resolve$1(dir, entry.name);
      if (entry.isDirectory()) {
        return rmRecursive(entryPath).then(() => promises.rmdir(entryPath));
      } else {
        return promises.unlink(entryPath);
      }
    })
  );
}

const PATH_TRAVERSE_RE = /\.\.:|\.\.$/;
const DRIVER_NAME = "fs-lite";
const unstorage_47drivers_47fs_45lite = defineDriver((opts = {}) => {
  if (!opts.base) {
    throw createRequiredError(DRIVER_NAME, "base");
  }
  opts.base = resolve$1(opts.base);
  const r = (key) => {
    if (PATH_TRAVERSE_RE.test(key)) {
      throw createError(
        DRIVER_NAME,
        `Invalid key: ${JSON.stringify(key)}. It should not contain .. segments`
      );
    }
    const resolved = join(opts.base, key.replace(/:/g, "/"));
    return resolved;
  };
  return {
    name: DRIVER_NAME,
    options: opts,
    flags: {
      maxDepth: true
    },
    hasItem(key) {
      return existsSync(r(key));
    },
    getItem(key) {
      return readFile(r(key), "utf8");
    },
    getItemRaw(key) {
      return readFile(r(key));
    },
    async getMeta(key) {
      const { atime, mtime, size, birthtime, ctime } = await promises.stat(r(key)).catch(() => ({}));
      return { atime, mtime, size, birthtime, ctime };
    },
    setItem(key, value) {
      if (opts.readOnly) {
        return;
      }
      return writeFile(r(key), value, "utf8");
    },
    setItemRaw(key, value) {
      if (opts.readOnly) {
        return;
      }
      return writeFile(r(key), value);
    },
    removeItem(key) {
      if (opts.readOnly) {
        return;
      }
      return unlink(r(key));
    },
    getKeys(_base, topts) {
      return readdirRecursive(r("."), opts.ignore, topts?.maxDepth);
    },
    async clear() {
      if (opts.readOnly || opts.noClear) {
        return;
      }
      await rmRecursive(r("."));
    }
  };
});

const storage$2 = createStorage({});

storage$2.mount('/assets', assets$1);

storage$2.mount('data', unstorage_47drivers_47fs_45lite({"driver":"fsLite","base":"./.data/kv"}));

function useStorage(base = "") {
  return base ? prefixStorage(storage$2, base) : storage$2;
}

const e$2=globalThis.process?.getBuiltinModule?.("crypto")?.hash,r$2="sha256",s="base64url";function digest(t){if(e$2)return e$2(r$2,t,s);const o=createHash(r$2).update(t);return globalThis.process?.versions?.webcontainer?o.digest().toString(s):o.digest(s)}

const Hasher = /* @__PURE__ */ (() => {
  class Hasher2 {
    buff = "";
    #context = /* @__PURE__ */ new Map();
    write(str) {
      this.buff += str;
    }
    dispatch(value) {
      const type = value === null ? "null" : typeof value;
      return this[type](value);
    }
    object(object) {
      if (object && typeof object.toJSON === "function") {
        return this.object(object.toJSON());
      }
      const objString = Object.prototype.toString.call(object);
      let objType = "";
      const objectLength = objString.length;
      objType = objectLength < 10 ? "unknown:[" + objString + "]" : objString.slice(8, objectLength - 1);
      objType = objType.toLowerCase();
      let objectNumber = null;
      if ((objectNumber = this.#context.get(object)) === void 0) {
        this.#context.set(object, this.#context.size);
      } else {
        return this.dispatch("[CIRCULAR:" + objectNumber + "]");
      }
      if (typeof Buffer !== "undefined" && Buffer.isBuffer && Buffer.isBuffer(object)) {
        this.write("buffer:");
        return this.write(object.toString("utf8"));
      }
      if (objType !== "object" && objType !== "function" && objType !== "asyncfunction") {
        if (this[objType]) {
          this[objType](object);
        } else {
          this.unknown(object, objType);
        }
      } else {
        const keys = Object.keys(object).sort();
        const extraKeys = [];
        this.write("object:" + (keys.length + extraKeys.length) + ":");
        const dispatchForKey = (key) => {
          this.dispatch(key);
          this.write(":");
          this.dispatch(object[key]);
          this.write(",");
        };
        for (const key of keys) {
          dispatchForKey(key);
        }
        for (const key of extraKeys) {
          dispatchForKey(key);
        }
      }
    }
    array(arr, unordered) {
      unordered = unordered === void 0 ? false : unordered;
      this.write("array:" + arr.length + ":");
      if (!unordered || arr.length <= 1) {
        for (const entry of arr) {
          this.dispatch(entry);
        }
        return;
      }
      const contextAdditions = /* @__PURE__ */ new Map();
      const entries = arr.map((entry) => {
        const hasher = new Hasher2();
        hasher.dispatch(entry);
        for (const [key, value] of hasher.#context) {
          contextAdditions.set(key, value);
        }
        return hasher.toString();
      });
      this.#context = contextAdditions;
      entries.sort();
      return this.array(entries, false);
    }
    date(date) {
      return this.write("date:" + date.toJSON());
    }
    symbol(sym) {
      return this.write("symbol:" + sym.toString());
    }
    unknown(value, type) {
      this.write(type);
      if (!value) {
        return;
      }
      this.write(":");
      if (value && typeof value.entries === "function") {
        return this.array(
          [...value.entries()],
          true
          /* ordered */
        );
      }
    }
    error(err) {
      return this.write("error:" + err.toString());
    }
    boolean(bool) {
      return this.write("bool:" + bool);
    }
    string(string) {
      this.write("string:" + string.length + ":");
      this.write(string);
    }
    function(fn) {
      this.write("fn:");
      if (isNativeFunction(fn)) {
        this.dispatch("[native]");
      } else {
        this.dispatch(fn.toString());
      }
    }
    number(number) {
      return this.write("number:" + number);
    }
    null() {
      return this.write("Null");
    }
    undefined() {
      return this.write("Undefined");
    }
    regexp(regex) {
      return this.write("regex:" + regex.toString());
    }
    arraybuffer(arr) {
      this.write("arraybuffer:");
      return this.dispatch(new Uint8Array(arr));
    }
    url(url) {
      return this.write("url:" + url.toString());
    }
    map(map) {
      this.write("map:");
      const arr = [...map];
      return this.array(arr, false);
    }
    set(set) {
      this.write("set:");
      const arr = [...set];
      return this.array(arr, false);
    }
    bigint(number) {
      return this.write("bigint:" + number.toString());
    }
  }
  for (const type of [
    "uint8array",
    "uint8clampedarray",
    "unt8array",
    "uint16array",
    "unt16array",
    "uint32array",
    "unt32array",
    "float32array",
    "float64array"
  ]) {
    Hasher2.prototype[type] = function(arr) {
      this.write(type + ":");
      return this.array([...arr], false);
    };
  }
  function isNativeFunction(f) {
    if (typeof f !== "function") {
      return false;
    }
    return Function.prototype.toString.call(f).slice(
      -15
      /* "[native code] }".length */
    ) === "[native code] }";
  }
  return Hasher2;
})();
function serialize(object) {
  const hasher = new Hasher();
  hasher.dispatch(object);
  return hasher.buff;
}
function hash(value) {
  return digest(typeof value === "string" ? value : serialize(value)).replace(/[-_]/g, "").slice(0, 10);
}

function defaultCacheOptions() {
  return {
    name: "_",
    base: "/cache",
    swr: true,
    maxAge: 1
  };
}
function defineCachedFunction(fn, opts = {}) {
  opts = { ...defaultCacheOptions(), ...opts };
  const pending = {};
  const group = opts.group || "nitro/functions";
  const name = opts.name || fn.name || "_";
  const integrity = opts.integrity || hash([fn, opts]);
  const validate = opts.validate || ((entry) => entry.value !== void 0);
  async function get(key, resolver, shouldInvalidateCache, event) {
    const cacheKey = [opts.base, group, name, key + ".json"].filter(Boolean).join(":").replace(/:\/$/, ":index");
    let entry = await useStorage().getItem(cacheKey).catch((error) => {
      console.error(`[cache] Cache read error.`, error);
      useNitroApp().captureError(error, { event, tags: ["cache"] });
    }) || {};
    if (typeof entry !== "object") {
      entry = {};
      const error = new Error("Malformed data read from cache.");
      console.error("[cache]", error);
      useNitroApp().captureError(error, { event, tags: ["cache"] });
    }
    const ttl = (opts.maxAge ?? 0) * 1e3;
    if (ttl) {
      entry.expires = Date.now() + ttl;
    }
    const expired = shouldInvalidateCache || entry.integrity !== integrity || ttl && Date.now() - (entry.mtime || 0) > ttl || validate(entry) === false;
    const _resolve = async () => {
      const isPending = pending[key];
      if (!isPending) {
        if (entry.value !== void 0 && (opts.staleMaxAge || 0) >= 0 && opts.swr === false) {
          entry.value = void 0;
          entry.integrity = void 0;
          entry.mtime = void 0;
          entry.expires = void 0;
        }
        pending[key] = Promise.resolve(resolver());
      }
      try {
        entry.value = await pending[key];
      } catch (error) {
        if (!isPending) {
          delete pending[key];
        }
        throw error;
      }
      if (!isPending) {
        entry.mtime = Date.now();
        entry.integrity = integrity;
        delete pending[key];
        if (validate(entry) !== false) {
          let setOpts;
          if (opts.maxAge && !opts.swr) {
            setOpts = { ttl: opts.maxAge };
          }
          const promise = useStorage().setItem(cacheKey, entry, setOpts).catch((error) => {
            console.error(`[cache] Cache write error.`, error);
            useNitroApp().captureError(error, { event, tags: ["cache"] });
          });
          if (event?.waitUntil) {
            event.waitUntil(promise);
          }
        }
      }
    };
    const _resolvePromise = expired ? _resolve() : Promise.resolve();
    if (entry.value === void 0) {
      await _resolvePromise;
    } else if (expired && event && event.waitUntil) {
      event.waitUntil(_resolvePromise);
    }
    if (opts.swr && validate(entry) !== false) {
      _resolvePromise.catch((error) => {
        console.error(`[cache] SWR handler error.`, error);
        useNitroApp().captureError(error, { event, tags: ["cache"] });
      });
      return entry;
    }
    return _resolvePromise.then(() => entry);
  }
  return async (...args) => {
    const shouldBypassCache = await opts.shouldBypassCache?.(...args);
    if (shouldBypassCache) {
      return fn(...args);
    }
    const key = await (opts.getKey || getKey$2)(...args);
    const shouldInvalidateCache = await opts.shouldInvalidateCache?.(...args);
    const entry = await get(
      key,
      () => fn(...args),
      shouldInvalidateCache,
      args[0] && isEvent(args[0]) ? args[0] : void 0
    );
    let value = entry.value;
    if (opts.transform) {
      value = await opts.transform(entry, ...args) || value;
    }
    return value;
  };
}
function cachedFunction(fn, opts = {}) {
  return defineCachedFunction(fn, opts);
}
function getKey$2(...args) {
  return args.length > 0 ? hash(args) : "";
}
function escapeKey(key) {
  return String(key).replace(/\W/g, "");
}
function defineCachedEventHandler(handler, opts = defaultCacheOptions()) {
  const variableHeaderNames = (opts.varies || []).filter(Boolean).map((h) => h.toLowerCase()).sort();
  const _opts = {
    ...opts,
    getKey: async (event) => {
      const customKey = await opts.getKey?.(event);
      if (customKey) {
        return escapeKey(customKey);
      }
      const _path = event.node.req.originalUrl || event.node.req.url || event.path;
      let _pathname;
      try {
        _pathname = escapeKey(decodeURI(parseURL(_path).pathname)).slice(0, 16) || "index";
      } catch {
        _pathname = "-";
      }
      const _hashedPath = `${_pathname}.${hash(_path)}`;
      const _headers = variableHeaderNames.map((header) => [header, event.node.req.headers[header]]).map(([name, value]) => `${escapeKey(name)}.${hash(value)}`);
      return [_hashedPath, ..._headers].join(":");
    },
    validate: (entry) => {
      if (!entry.value) {
        return false;
      }
      if (entry.value.code >= 400) {
        return false;
      }
      if (entry.value.body === void 0) {
        return false;
      }
      if (entry.value.headers.etag === "undefined" || entry.value.headers["last-modified"] === "undefined") {
        return false;
      }
      return true;
    },
    group: opts.group || "nitro/handlers",
    integrity: opts.integrity || hash([handler, opts])
  };
  const _cachedHandler = cachedFunction(
    async (incomingEvent) => {
      const variableHeaders = {};
      for (const header of variableHeaderNames) {
        const value = incomingEvent.node.req.headers[header];
        if (value !== void 0) {
          variableHeaders[header] = value;
        }
      }
      const reqProxy = cloneWithProxy(incomingEvent.node.req, {
        headers: variableHeaders
      });
      const resHeaders = {};
      let _resSendBody;
      const resProxy = cloneWithProxy(incomingEvent.node.res, {
        statusCode: 200,
        writableEnded: false,
        writableFinished: false,
        headersSent: false,
        closed: false,
        getHeader(name) {
          return resHeaders[name];
        },
        setHeader(name, value) {
          resHeaders[name] = value;
          return this;
        },
        getHeaderNames() {
          return Object.keys(resHeaders);
        },
        hasHeader(name) {
          return name in resHeaders;
        },
        removeHeader(name) {
          delete resHeaders[name];
        },
        getHeaders() {
          return resHeaders;
        },
        end(chunk, arg2, arg3) {
          if (typeof chunk === "string") {
            _resSendBody = chunk;
          }
          if (typeof arg2 === "function") {
            arg2();
          }
          if (typeof arg3 === "function") {
            arg3();
          }
          return this;
        },
        write(chunk, arg2, arg3) {
          if (typeof chunk === "string") {
            _resSendBody = chunk;
          }
          if (typeof arg2 === "function") {
            arg2(void 0);
          }
          if (typeof arg3 === "function") {
            arg3();
          }
          return true;
        },
        writeHead(statusCode, headers2) {
          this.statusCode = statusCode;
          if (headers2) {
            if (Array.isArray(headers2) || typeof headers2 === "string") {
              throw new TypeError("Raw headers  is not supported.");
            }
            for (const header in headers2) {
              const value = headers2[header];
              if (value !== void 0) {
                this.setHeader(
                  header,
                  value
                );
              }
            }
          }
          return this;
        }
      });
      const event = createEvent(reqProxy, resProxy);
      event.fetch = (url, fetchOptions) => fetchWithEvent(event, url, fetchOptions, {
        fetch: useNitroApp().localFetch
      });
      event.$fetch = (url, fetchOptions) => fetchWithEvent(event, url, fetchOptions, {
        fetch: globalThis.$fetch
      });
      event.waitUntil = incomingEvent.waitUntil;
      event.context = incomingEvent.context;
      event.context.cache = {
        options: _opts
      };
      const body = await handler(event) || _resSendBody;
      const headers = event.node.res.getHeaders();
      headers.etag = String(
        headers.Etag || headers.etag || `W/"${hash(body)}"`
      );
      headers["last-modified"] = String(
        headers["Last-Modified"] || headers["last-modified"] || (/* @__PURE__ */ new Date()).toUTCString()
      );
      const cacheControl = [];
      if (opts.swr) {
        if (opts.maxAge) {
          cacheControl.push(`s-maxage=${opts.maxAge}`);
        }
        if (opts.staleMaxAge) {
          cacheControl.push(`stale-while-revalidate=${opts.staleMaxAge}`);
        } else {
          cacheControl.push("stale-while-revalidate");
        }
      } else if (opts.maxAge) {
        cacheControl.push(`max-age=${opts.maxAge}`);
      }
      if (cacheControl.length > 0) {
        headers["cache-control"] = cacheControl.join(", ");
      }
      const cacheEntry = {
        code: event.node.res.statusCode,
        headers,
        body
      };
      return cacheEntry;
    },
    _opts
  );
  return defineEventHandler(async (event) => {
    if (opts.headersOnly) {
      if (handleCacheHeaders(event, { maxAge: opts.maxAge })) {
        return;
      }
      return handler(event);
    }
    const response = await _cachedHandler(
      event
    );
    if (event.node.res.headersSent || event.node.res.writableEnded) {
      return response.body;
    }
    if (handleCacheHeaders(event, {
      modifiedTime: new Date(response.headers["last-modified"]),
      etag: response.headers.etag,
      maxAge: opts.maxAge
    })) {
      return;
    }
    event.node.res.statusCode = response.code;
    for (const name in response.headers) {
      const value = response.headers[name];
      if (name === "set-cookie") {
        event.node.res.appendHeader(
          name,
          splitCookiesString(value)
        );
      } else {
        if (value !== void 0) {
          event.node.res.setHeader(name, value);
        }
      }
    }
    return response.body;
  });
}
function cloneWithProxy(obj, overrides) {
  return new Proxy(obj, {
    get(target, property, receiver) {
      if (property in overrides) {
        return overrides[property];
      }
      return Reflect.get(target, property, receiver);
    },
    set(target, property, value, receiver) {
      if (property in overrides) {
        overrides[property] = value;
        return true;
      }
      return Reflect.set(target, property, value, receiver);
    }
  });
}
const cachedEventHandler = defineCachedEventHandler;

function klona(x) {
	if (typeof x !== 'object') return x;

	var k, tmp, str=Object.prototype.toString.call(x);

	if (str === '[object Object]') {
		if (x.constructor !== Object && typeof x.constructor === 'function') {
			tmp = new x.constructor();
			for (k in x) {
				if (x.hasOwnProperty(k) && tmp[k] !== x[k]) {
					tmp[k] = klona(x[k]);
				}
			}
		} else {
			tmp = {}; // null
			for (k in x) {
				if (k === '__proto__') {
					Object.defineProperty(tmp, k, {
						value: klona(x[k]),
						configurable: true,
						enumerable: true,
						writable: true,
					});
				} else {
					tmp[k] = klona(x[k]);
				}
			}
		}
		return tmp;
	}

	if (str === '[object Array]') {
		k = x.length;
		for (tmp=Array(k); k--;) {
			tmp[k] = klona(x[k]);
		}
		return tmp;
	}

	if (str === '[object Set]') {
		tmp = new Set;
		x.forEach(function (val) {
			tmp.add(klona(val));
		});
		return tmp;
	}

	if (str === '[object Map]') {
		tmp = new Map;
		x.forEach(function (val, key) {
			tmp.set(klona(key), klona(val));
		});
		return tmp;
	}

	if (str === '[object Date]') {
		return new Date(+x);
	}

	if (str === '[object RegExp]') {
		tmp = new RegExp(x.source, x.flags);
		tmp.lastIndex = x.lastIndex;
		return tmp;
	}

	if (str === '[object DataView]') {
		return new x.constructor( klona(x.buffer) );
	}

	if (str === '[object ArrayBuffer]') {
		return x.slice(0);
	}

	// ArrayBuffer.isView(x)
	// ~> `new` bcuz `Buffer.slice` => ref
	if (str.slice(-6) === 'Array]') {
		return new x.constructor(x);
	}

	return x;
}

const inlineAppConfig = {};



const appConfig$1 = defuFn(inlineAppConfig);

const NUMBER_CHAR_RE = /\d/;
const STR_SPLITTERS = ["-", "_", "/", "."];
function isUppercase(char = "") {
  if (NUMBER_CHAR_RE.test(char)) {
    return void 0;
  }
  return char !== char.toLowerCase();
}
function splitByCase(str, separators) {
  const splitters = STR_SPLITTERS;
  const parts = [];
  if (!str || typeof str !== "string") {
    return parts;
  }
  let buff = "";
  let previousUpper;
  let previousSplitter;
  for (const char of str) {
    const isSplitter = splitters.includes(char);
    if (isSplitter === true) {
      parts.push(buff);
      buff = "";
      previousUpper = void 0;
      continue;
    }
    const isUpper = isUppercase(char);
    if (previousSplitter === false) {
      if (previousUpper === false && isUpper === true) {
        parts.push(buff);
        buff = char;
        previousUpper = isUpper;
        continue;
      }
      if (previousUpper === true && isUpper === false && buff.length > 1) {
        const lastChar = buff.at(-1);
        parts.push(buff.slice(0, Math.max(0, buff.length - 1)));
        buff = lastChar + char;
        previousUpper = isUpper;
        continue;
      }
    }
    buff += char;
    previousUpper = isUpper;
    previousSplitter = isSplitter;
  }
  parts.push(buff);
  return parts;
}
function kebabCase(str, joiner) {
  return str ? (Array.isArray(str) ? str : splitByCase(str)).map((p) => p.toLowerCase()).join(joiner) : "";
}
function snakeCase(str) {
  return kebabCase(str || "", "_");
}

function getEnv(key, opts) {
  const envKey = snakeCase(key).toUpperCase();
  return destr(
    process.env[opts.prefix + envKey] ?? process.env[opts.altPrefix + envKey]
  );
}
function _isObject(input) {
  return typeof input === "object" && !Array.isArray(input);
}
function applyEnv(obj, opts, parentKey = "") {
  for (const key in obj) {
    const subKey = parentKey ? `${parentKey}_${key}` : key;
    const envValue = getEnv(subKey, opts);
    if (_isObject(obj[key])) {
      if (_isObject(envValue)) {
        obj[key] = { ...obj[key], ...envValue };
        applyEnv(obj[key], opts, subKey);
      } else if (envValue === void 0) {
        applyEnv(obj[key], opts, subKey);
      } else {
        obj[key] = envValue ?? obj[key];
      }
    } else {
      obj[key] = envValue ?? obj[key];
    }
    if (opts.envExpansion && typeof obj[key] === "string") {
      obj[key] = _expandFromEnv(obj[key]);
    }
  }
  return obj;
}
const envExpandRx = /\{\{([^{}]*)\}\}/g;
function _expandFromEnv(value) {
  return value.replace(envExpandRx, (match, key) => {
    return process.env[key] || match;
  });
}

const _inlineRuntimeConfig = {
  "app": {
    "baseURL": "/"
  },
  "nitro": {
    "routeRules": {
      "/_build/assets/**": {
        "headers": {
          "cache-control": "public, immutable, max-age=31536000"
        }
      }
    }
  }
};
const envOptions = {
  prefix: "NITRO_",
  altPrefix: _inlineRuntimeConfig.nitro.envPrefix ?? process.env.NITRO_ENV_PREFIX ?? "_",
  envExpansion: _inlineRuntimeConfig.nitro.envExpansion ?? process.env.NITRO_ENV_EXPANSION ?? false
};
const _sharedRuntimeConfig = _deepFreeze(
  applyEnv(klona(_inlineRuntimeConfig), envOptions)
);
function useRuntimeConfig(event) {
  {
    return _sharedRuntimeConfig;
  }
}
_deepFreeze(klona(appConfig$1));
function _deepFreeze(object) {
  const propNames = Object.getOwnPropertyNames(object);
  for (const name of propNames) {
    const value = object[name];
    if (value && typeof value === "object") {
      _deepFreeze(value);
    }
  }
  return Object.freeze(object);
}
new Proxy(/* @__PURE__ */ Object.create(null), {
  get: (_, prop) => {
    console.warn(
      "Please use `useRuntimeConfig()` instead of accessing config directly."
    );
    const runtimeConfig = useRuntimeConfig();
    if (prop in runtimeConfig) {
      return runtimeConfig[prop];
    }
    return void 0;
  }
});

function createContext(opts = {}) {
  let currentInstance;
  let isSingleton = false;
  const checkConflict = (instance) => {
    if (currentInstance && currentInstance !== instance) {
      throw new Error("Context conflict");
    }
  };
  let als;
  if (opts.asyncContext) {
    const _AsyncLocalStorage = opts.AsyncLocalStorage || globalThis.AsyncLocalStorage;
    if (_AsyncLocalStorage) {
      als = new _AsyncLocalStorage();
    } else {
      console.warn("[unctx] `AsyncLocalStorage` is not provided.");
    }
  }
  const _getCurrentInstance = () => {
    if (als) {
      const instance = als.getStore();
      if (instance !== void 0) {
        return instance;
      }
    }
    return currentInstance;
  };
  return {
    use: () => {
      const _instance = _getCurrentInstance();
      if (_instance === void 0) {
        throw new Error("Context is not available");
      }
      return _instance;
    },
    tryUse: () => {
      return _getCurrentInstance();
    },
    set: (instance, replace) => {
      if (!replace) {
        checkConflict(instance);
      }
      currentInstance = instance;
      isSingleton = true;
    },
    unset: () => {
      currentInstance = void 0;
      isSingleton = false;
    },
    call: (instance, callback) => {
      checkConflict(instance);
      currentInstance = instance;
      try {
        return als ? als.run(instance, callback) : callback();
      } finally {
        if (!isSingleton) {
          currentInstance = void 0;
        }
      }
    },
    async callAsync(instance, callback) {
      currentInstance = instance;
      const onRestore = () => {
        currentInstance = instance;
      };
      const onLeave = () => currentInstance === instance ? onRestore : void 0;
      asyncHandlers.add(onLeave);
      try {
        const r = als ? als.run(instance, callback) : callback();
        if (!isSingleton) {
          currentInstance = void 0;
        }
        return await r;
      } finally {
        asyncHandlers.delete(onLeave);
      }
    }
  };
}
function createNamespace(defaultOpts = {}) {
  const contexts = {};
  return {
    get(key, opts = {}) {
      if (!contexts[key]) {
        contexts[key] = createContext({ ...defaultOpts, ...opts });
      }
      return contexts[key];
    }
  };
}
const _globalThis = typeof globalThis !== "undefined" ? globalThis : typeof self !== "undefined" ? self : typeof global !== "undefined" ? global : {};
const globalKey = "__unctx__";
const defaultNamespace = _globalThis[globalKey] || (_globalThis[globalKey] = createNamespace());
const getContext = (key, opts = {}) => defaultNamespace.get(key, opts);
const asyncHandlersKey = "__unctx_async_handlers__";
const asyncHandlers = _globalThis[asyncHandlersKey] || (_globalThis[asyncHandlersKey] = /* @__PURE__ */ new Set());

const nitroAsyncContext = getContext("nitro-app", {
  asyncContext: true,
  AsyncLocalStorage: AsyncLocalStorage 
});

function isPathInScope(pathname, base) {
  let canonical;
  try {
    const pre = pathname.replace(/%2f/gi, "/").replace(/%5c/gi, "\\");
    canonical = new URL(pre, "http://_").pathname;
  } catch {
    return false;
  }
  return !base || canonical === base || canonical.startsWith(base + "/");
}

const config = useRuntimeConfig();
const _routeRulesMatcher = toRouteMatcher(
  createRouter$1({ routes: config.nitro.routeRules })
);
function createRouteRulesHandler(ctx) {
  return eventHandler((event) => {
    const routeRules = getRouteRules(event);
    if (routeRules.headers) {
      setHeaders(event, routeRules.headers);
    }
    if (routeRules.redirect) {
      let target = routeRules.redirect.to;
      if (target.endsWith("/**")) {
        let targetPath = event.path;
        const strpBase = routeRules.redirect._redirectStripBase;
        if (strpBase) {
          if (!isPathInScope(event.path.split("?")[0], strpBase)) {
            throw createError$1({ statusCode: 400 });
          }
          targetPath = withoutBase(targetPath, strpBase);
        } else if (targetPath.startsWith("//")) {
          targetPath = targetPath.replace(/^\/+/, "/");
        }
        target = joinURL(target.slice(0, -3), targetPath);
      } else if (event.path.includes("?")) {
        const query = getQuery(event.path);
        target = withQuery(target, query);
      }
      return sendRedirect(event, target, routeRules.redirect.statusCode);
    }
    if (routeRules.proxy) {
      let target = routeRules.proxy.to;
      if (target.endsWith("/**")) {
        let targetPath = event.path;
        const strpBase = routeRules.proxy._proxyStripBase;
        if (strpBase) {
          if (!isPathInScope(event.path.split("?")[0], strpBase)) {
            throw createError$1({ statusCode: 400 });
          }
          targetPath = withoutBase(targetPath, strpBase);
        } else if (targetPath.startsWith("//")) {
          targetPath = targetPath.replace(/^\/+/, "/");
        }
        target = joinURL(target.slice(0, -3), targetPath);
      } else if (event.path.includes("?")) {
        const query = getQuery(event.path);
        target = withQuery(target, query);
      }
      return proxyRequest(event, target, {
        fetch: ctx.localFetch,
        ...routeRules.proxy
      });
    }
  });
}
function getRouteRules(event) {
  event.context._nitro = event.context._nitro || {};
  if (!event.context._nitro.routeRules) {
    event.context._nitro.routeRules = getRouteRulesForPath(
      withoutBase(event.path.split("?")[0], useRuntimeConfig().app.baseURL)
    );
  }
  return event.context._nitro.routeRules;
}
function getRouteRulesForPath(path) {
  return defu({}, ..._routeRulesMatcher.matchAll(path).reverse());
}

function _captureError(error, type) {
  console.error(`[${type}]`, error);
  useNitroApp().captureError(error, { tags: [type] });
}
function trapUnhandledNodeErrors() {
  process.on(
    "unhandledRejection",
    (error) => _captureError(error, "unhandledRejection")
  );
  process.on(
    "uncaughtException",
    (error) => _captureError(error, "uncaughtException")
  );
}
function joinHeaders(value) {
  return Array.isArray(value) ? value.join(", ") : String(value);
}
function normalizeFetchResponse(response) {
  if (!response.headers.has("set-cookie")) {
    return response;
  }
  return new Response(response.body, {
    status: response.status,
    statusText: response.statusText,
    headers: normalizeCookieHeaders(response.headers)
  });
}
function normalizeCookieHeader(header = "") {
  return splitCookiesString(joinHeaders(header));
}
function normalizeCookieHeaders(headers) {
  const outgoingHeaders = new Headers();
  for (const [name, header] of headers) {
    if (name === "set-cookie") {
      for (const cookie of normalizeCookieHeader(header)) {
        outgoingHeaders.append("set-cookie", cookie);
      }
    } else {
      outgoingHeaders.set(name, joinHeaders(header));
    }
  }
  return outgoingHeaders;
}

function defineNitroErrorHandler(handler) {
  return handler;
}

const errorHandler$0 = defineNitroErrorHandler(
  function defaultNitroErrorHandler(error, event) {
    const res = defaultHandler(error, event);
    setResponseHeaders(event, res.headers);
    setResponseStatus(event, res.status, res.statusText);
    return send(event, JSON.stringify(res.body, null, 2));
  }
);
function defaultHandler(error, event, opts) {
  const isSensitive = error.unhandled || error.fatal;
  const statusCode = error.statusCode || 500;
  const statusMessage = error.statusMessage || "Server Error";
  const url = getRequestURL(event, { xForwardedHost: true, xForwardedProto: true });
  if (statusCode === 404) {
    const baseURL = "/";
    if (/^\/[^/]/.test(baseURL) && !url.pathname.startsWith(baseURL)) {
      const redirectTo = `${baseURL}${url.pathname.slice(1)}${url.search}`;
      return {
        status: 302,
        statusText: "Found",
        headers: { location: redirectTo },
        body: `Redirecting...`
      };
    }
  }
  if (isSensitive && !opts?.silent) {
    const tags = [error.unhandled && "[unhandled]", error.fatal && "[fatal]"].filter(Boolean).join(" ");
    console.error(`[request error] ${tags} [${event.method}] ${url}
`, error);
  }
  const headers = {
    "content-type": "application/json",
    // Prevent browser from guessing the MIME types of resources.
    "x-content-type-options": "nosniff",
    // Prevent error page from being embedded in an iframe
    "x-frame-options": "DENY",
    // Prevent browsers from sending the Referer header
    "referrer-policy": "no-referrer",
    // Disable the execution of any js
    "content-security-policy": "script-src 'none'; frame-ancestors 'none';"
  };
  setResponseStatus(event, statusCode, statusMessage);
  if (statusCode === 404 || !getResponseHeader(event, "cache-control")) {
    headers["cache-control"] = "no-cache";
  }
  const body = {
    error: true,
    url: url.href,
    statusCode,
    statusMessage,
    message: isSensitive ? "Server Error" : error.message,
    data: isSensitive ? void 0 : error.data
  };
  return {
    status: statusCode,
    statusText: statusMessage,
    headers,
    body
  };
}

const errorHandlers = [errorHandler$0];

async function errorHandler(error, event) {
  for (const handler of errorHandlers) {
    try {
      await handler(error, event, { defaultHandler });
      if (event.handled) {
        return; // Response handled
      }
    } catch(error) {
      // Handler itself thrown, log and continue
      console.error(error);
    }
  }
  // H3 will handle fallback
}

const appConfig = {"name":"vinxi","routers":[{"name":"public","type":"static","base":"/","dir":"./public","root":"/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso","order":0,"outDir":"/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/.vinxi/build/public"},{"name":"ssr","type":"http","link":{"client":"client"},"handler":"src/entry-server.tsx","extensions":["js","jsx","ts","tsx"],"target":"server","root":"/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso","base":"/","outDir":"/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/.vinxi/build/ssr","order":1},{"name":"client","type":"client","base":"/_build","handler":"src/entry-client.tsx","extensions":["js","jsx","ts","tsx"],"target":"browser","root":"/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso","outDir":"/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/.vinxi/build/client","order":2},{"name":"server-fns","type":"http","base":"/_server","handler":"../../node_modules/.pnpm/@solidjs+start@1.3.2_@testing-library+jest-dom@6.9.1_solid-js@1.9.13_vinxi@0.4.3_@azure_dc5711ca158a2ce412ca96d4be6a67d5/node_modules/@solidjs/start/dist/runtime/server-handler.js","target":"server","root":"/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso","outDir":"/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/.vinxi/build/server-fns","order":3}],"server":{"compressPublicAssets":{"brotli":true},"routeRules":{"/_build/assets/**":{"headers":{"cache-control":"public, immutable, max-age=31536000"}}},"experimental":{"asyncContext":true}},"root":"/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso"};
				const buildManifest = {"ssr":{"../../packages/decap/dist/decap-cms-backend-laika/index.js":{"file":"assets/index-IxgNq1MU.js","name":"index","src":"../../packages/decap/dist/decap-cms-backend-laika/index.js","isDynamicEntry":true,"imports":["_jsonapi-GRx6mPsC.js"]},"_content-BI6WYP-w.js":{"file":"assets/content-BI6WYP-w.js","name":"content","imports":["_laika-LDlRdEpI.js"]},"_decap-config-BYhjwbwa.js":{"file":"assets/decap-config-BYhjwbwa.js","name":"decap-config"},"_jsonapi-GRx6mPsC.js":{"file":"assets/jsonapi-GRx6mPsC.js","name":"jsonapi"},"_laika-LDlRdEpI.js":{"file":"assets/laika-LDlRdEpI.js","name":"laika","imports":["_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/admin.tsx?pick=default&pick=$css":{"file":"admin.js","name":"admin","src":"src/routes/admin.tsx?pick=default&pick=$css","isEntry":true,"isDynamicEntry":true,"imports":["_decap-config-BYhjwbwa.js"],"dynamicImports":["../../packages/decap/dist/decap-cms-backend-laika/index.js"]},"src/routes/api/decap/[...path].ts?pick=DELETE":{"file":"_...path_.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=DELETE","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=GET":{"file":"_...path_2.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=GET","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=HEAD":{"file":"_...path_3.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=HEAD","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=OPTIONS":{"file":"_...path_4.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=OPTIONS","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=PATCH":{"file":"_...path_5.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=PATCH","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=POST":{"file":"_...path_6.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=POST","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=PUT":{"file":"_...path_7.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=PUT","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/blog/[slug].tsx?pick=default&pick=$css":{"file":"_slug_.js","name":"_slug_","src":"src/routes/blog/[slug].tsx?pick=default&pick=$css","isEntry":true,"isDynamicEntry":true,"imports":["_content-BI6WYP-w.js","_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/index.tsx?pick=default&pick=$css":{"file":"index.js","name":"index","src":"src/routes/index.tsx?pick=default&pick=$css","isEntry":true,"isDynamicEntry":true,"imports":["_content-BI6WYP-w.js","_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"virtual:$vinxi/handler/ssr":{"file":"ssr.js","name":"ssr","src":"virtual:$vinxi/handler/ssr","isEntry":true,"imports":["_content-BI6WYP-w.js","_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"],"dynamicImports":["src/routes/admin.tsx?pick=default&pick=$css","src/routes/admin.tsx?pick=default&pick=$css","src/routes/index.tsx?pick=default&pick=$css","src/routes/index.tsx?pick=default&pick=$css","src/routes/blog/[slug].tsx?pick=default&pick=$css","src/routes/blog/[slug].tsx?pick=default&pick=$css","src/routes/api/decap/[...path].ts?pick=DELETE","src/routes/api/decap/[...path].ts?pick=DELETE","src/routes/api/decap/[...path].ts?pick=GET","src/routes/api/decap/[...path].ts?pick=GET","src/routes/api/decap/[...path].ts?pick=HEAD","src/routes/api/decap/[...path].ts?pick=HEAD","src/routes/api/decap/[...path].ts?pick=OPTIONS","src/routes/api/decap/[...path].ts?pick=OPTIONS","src/routes/api/decap/[...path].ts?pick=PATCH","src/routes/api/decap/[...path].ts?pick=PATCH","src/routes/api/decap/[...path].ts?pick=POST","src/routes/api/decap/[...path].ts?pick=POST","src/routes/api/decap/[...path].ts?pick=PUT","src/routes/api/decap/[...path].ts?pick=PUT"]}},"client":{"../../packages/decap/dist/decap-cms-backend-laika/index.js":{"file":"assets/index-BJeVjlIs.js","name":"index","src":"../../packages/decap/dist/decap-cms-backend-laika/index.js","isDynamicEntry":true},"_content-BnUEKs_s.js":{"file":"assets/content-BnUEKs_s.js","name":"content","imports":["_solid-BEU1w9CJ.js"]},"_preload-helper-CM3UJVvY.js":{"file":"assets/preload-helper-CM3UJVvY.js","name":"preload-helper"},"_solid-BEU1w9CJ.js":{"file":"assets/solid-BEU1w9CJ.js","name":"solid"},"src/routes/admin.tsx?pick=default&pick=$css":{"file":"assets/admin-D2eIpwgd.js","name":"admin","src":"src/routes/admin.tsx?pick=default&pick=$css","isEntry":true,"isDynamicEntry":true,"imports":["_preload-helper-CM3UJVvY.js","_solid-BEU1w9CJ.js"],"dynamicImports":["../../packages/decap/dist/decap-cms-backend-laika/index.js"]},"src/routes/blog/[slug].tsx?pick=default&pick=$css":{"file":"assets/_slug_-N26OP5_J.js","name":"_slug_","src":"src/routes/blog/[slug].tsx?pick=default&pick=$css","isEntry":true,"isDynamicEntry":true,"imports":["_content-BnUEKs_s.js","_solid-BEU1w9CJ.js"]},"src/routes/index.tsx?pick=default&pick=$css":{"file":"assets/index-YKGcSqIy.js","name":"index","src":"src/routes/index.tsx?pick=default&pick=$css","isEntry":true,"isDynamicEntry":true,"imports":["_content-BnUEKs_s.js","_solid-BEU1w9CJ.js"]},"virtual:$vinxi/handler/client":{"file":"assets/client-BqU-jLUE.js","name":"client","src":"virtual:$vinxi/handler/client","isEntry":true,"imports":["_content-BnUEKs_s.js","_solid-BEU1w9CJ.js","_preload-helper-CM3UJVvY.js"],"dynamicImports":["src/routes/admin.tsx?pick=default&pick=$css","src/routes/index.tsx?pick=default&pick=$css","src/routes/blog/[slug].tsx?pick=default&pick=$css"]}},"server-fns":{"../../packages/decap/dist/decap-cms-backend-laika/index.js":{"file":"assets/index-IxgNq1MU.js","name":"index","src":"../../packages/decap/dist/decap-cms-backend-laika/index.js","isDynamicEntry":true,"imports":["_jsonapi-GRx6mPsC.js"]},"_content-DMwGwsJ-.js":{"file":"assets/content-DMwGwsJ-.js","name":"content","imports":["_laika-LDlRdEpI.js"]},"_decap-config-BYhjwbwa.js":{"file":"assets/decap-config-BYhjwbwa.js","name":"decap-config"},"_jsonapi-GRx6mPsC.js":{"file":"assets/jsonapi-GRx6mPsC.js","name":"jsonapi"},"_laika-LDlRdEpI.js":{"file":"assets/laika-LDlRdEpI.js","name":"laika","imports":["_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"_server-fns-CK80douL.js":{"file":"assets/server-fns-CK80douL.js","name":"server-fns","imports":["_content-DMwGwsJ-.js"],"dynamicImports":["src/routes/admin.tsx?pick=default&pick=$css","src/routes/admin.tsx?pick=default&pick=$css","src/routes/index.tsx?pick=default&pick=$css","src/routes/index.tsx?pick=default&pick=$css","src/routes/blog/[slug].tsx?pick=default&pick=$css","src/routes/blog/[slug].tsx?pick=default&pick=$css","src/routes/api/decap/[...path].ts?pick=DELETE","src/routes/api/decap/[...path].ts?pick=DELETE","src/routes/api/decap/[...path].ts?pick=GET","src/routes/api/decap/[...path].ts?pick=GET","src/routes/api/decap/[...path].ts?pick=HEAD","src/routes/api/decap/[...path].ts?pick=HEAD","src/routes/api/decap/[...path].ts?pick=OPTIONS","src/routes/api/decap/[...path].ts?pick=OPTIONS","src/routes/api/decap/[...path].ts?pick=PATCH","src/routes/api/decap/[...path].ts?pick=PATCH","src/routes/api/decap/[...path].ts?pick=POST","src/routes/api/decap/[...path].ts?pick=POST","src/routes/api/decap/[...path].ts?pick=PUT","src/routes/api/decap/[...path].ts?pick=PUT","src/lib/content.ts?tsr-directive-use-server=","src/lib/content.ts?tsr-directive-use-server=","src/app.tsx"]},"src/app.tsx":{"file":"assets/app-JEG4qAOq.js","name":"app","src":"src/app.tsx","isDynamicEntry":true,"imports":["_server-fns-CK80douL.js","_content-DMwGwsJ-.js","_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/lib/content.ts?tsr-directive-use-server=":{"file":"assets/content-D0eQygkN.js","name":"content","src":"src/lib/content.ts?tsr-directive-use-server=","isDynamicEntry":true,"imports":["_content-DMwGwsJ-.js","_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/admin.tsx?pick=default&pick=$css":{"file":"admin.js","name":"admin","src":"src/routes/admin.tsx?pick=default&pick=$css","isEntry":true,"isDynamicEntry":true,"imports":["_decap-config-BYhjwbwa.js"],"dynamicImports":["../../packages/decap/dist/decap-cms-backend-laika/index.js"]},"src/routes/api/decap/[...path].ts?pick=DELETE":{"file":"_...path_.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=DELETE","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=GET":{"file":"_...path_2.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=GET","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=HEAD":{"file":"_...path_3.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=HEAD","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=OPTIONS":{"file":"_...path_4.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=OPTIONS","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=PATCH":{"file":"_...path_5.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=PATCH","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=POST":{"file":"_...path_6.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=POST","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/api/decap/[...path].ts?pick=PUT":{"file":"_...path_7.js","name":"_...path_","src":"src/routes/api/decap/[...path].ts?pick=PUT","isEntry":true,"isDynamicEntry":true,"imports":["_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/blog/[slug].tsx?pick=default&pick=$css":{"file":"_slug_.js","name":"_slug_","src":"src/routes/blog/[slug].tsx?pick=default&pick=$css","isEntry":true,"isDynamicEntry":true,"imports":["_content-DMwGwsJ-.js","_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"src/routes/index.tsx?pick=default&pick=$css":{"file":"index.js","name":"index","src":"src/routes/index.tsx?pick=default&pick=$css","isEntry":true,"isDynamicEntry":true,"imports":["_content-DMwGwsJ-.js","_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]},"virtual:$vinxi/handler/server-fns":{"file":"server-fns.js","name":"server-fns","src":"virtual:$vinxi/handler/server-fns","isEntry":true,"imports":["_server-fns-CK80douL.js","_content-DMwGwsJ-.js","_laika-LDlRdEpI.js","_jsonapi-GRx6mPsC.js","_decap-config-BYhjwbwa.js"]}}};

				const routeManifest = {"ssr":{},"client":{},"server-fns":{}};

        function createProdApp(appConfig) {
          return {
            config: { ...appConfig, buildManifest, routeManifest },
            getRouter(name) {
              return appConfig.routers.find(router => router.name === name)
            }
          }
        }

        function plugin$2(app) {
          const prodApp = createProdApp(appConfig);
          globalThis.app = prodApp;
        }

function plugin$1(app) {
	globalThis.$handle = (event) => app.h3App.handler(event);
}

/**
 * Traverses the module graph and collects assets for a given chunk
 *
 * @param {any} manifest Client manifest
 * @param {string} id Chunk id
 * @param {Map<string, string[]>} assetMap Cache of assets
 * @param {string[]} stack Stack of chunk ids to prevent circular dependencies
 * @returns Array of asset URLs
 */
function findAssetsInViteManifest(manifest, id, assetMap = new Map(), stack = []) {
	if (stack.includes(id)) {
		return [];
	}

	const cached = assetMap.get(id);
	if (cached) {
		return cached;
	}
	const chunk = manifest[id];
	if (!chunk) {
		return [];
	}

	const assets = [
		...(chunk.assets?.filter(Boolean) || []),
		...(chunk.css?.filter(Boolean) || [])
	];
	if (chunk.imports) {
		stack.push(id);
		for (let i = 0, l = chunk.imports.length; i < l; i++) {
			assets.push(...findAssetsInViteManifest(manifest, chunk.imports[i], assetMap, stack));
		}
		stack.pop();
	}
	assets.push(chunk.file);
	const all = Array.from(new Set(assets));
	assetMap.set(id, all);

	return all;
}

/** @typedef {import("../app.js").App & { config: { buildManifest: { [key:string]: any } }}} ProdApp */

function createHtmlTagsForAssets(router, app, assets) {
	return assets
		.filter(
			(asset) =>
				asset.endsWith(".css") ||
				asset.endsWith(".js") ||
				asset.endsWith(".mjs"),
		)
		.map((asset) => ({
			tag: "link",
			attrs: {
				href: joinURL(app.config.server.baseURL ?? "/", router.base, asset),
				key: join$1(app.config.server.baseURL ?? "", router.base, asset),
				...(asset.endsWith(".css")
					? { rel: "stylesheet", fetchPriority: "high" }
					: { rel: "modulepreload" }),
			},
		}));
}

/**
 *
 * @param {ProdApp} app
 * @returns
 */
function createProdManifest(app) {
	const manifest = new Proxy(
		{},
		{
			get(target, routerName) {
				invariant(typeof routerName === "string", "Bundler name expected");
				const router = app.getRouter(routerName);
				const bundlerManifest = app.config.buildManifest[routerName];

				invariant(
					router.type !== "static",
					"manifest not available for static router",
				);
				return {
					handler: router.handler,
					async assets() {
						/** @type {{ [key: string]: string[] }} */
						let assets = {};
						assets[router.handler] = await this.inputs[router.handler].assets();
						for (const route of (await router.internals.routes?.getRoutes()) ??
							[]) {
							assets[route.filePath] = await this.inputs[
								route.filePath
							].assets();
						}
						return assets;
					},
					async routes() {
						return (await router.internals.routes?.getRoutes()) ?? [];
					},
					async json() {
						/** @type {{ [key: string]: { output: string; assets: string[]} }} */
						let json = {};
						for (const input of Object.keys(this.inputs)) {
							json[input] = {
								output: this.inputs[input].output.path,
								assets: await this.inputs[input].assets(),
							};
						}
						return json;
					},
					chunks: new Proxy(
						{},
						{
							get(target, chunk) {
								invariant(typeof chunk === "string", "Chunk expected");
								const chunkPath = join$1(
									router.outDir,
									router.base,
									chunk + ".mjs",
								);
								return {
									import() {
										if (globalThis.$$chunks[chunk + ".mjs"]) {
											return globalThis.$$chunks[chunk + ".mjs"];
										}
										return import(
											/* @vite-ignore */ pathToFileURL(chunkPath).href
										);
									},
									output: {
										path: chunkPath,
									},
								};
							},
						},
					),
					inputs: new Proxy(
						{},
						{
							ownKeys(target) {
								const keys = Object.keys(bundlerManifest)
									.filter((id) => bundlerManifest[id].isEntry)
									.map((id) => id);
								return keys;
							},
							getOwnPropertyDescriptor(k) {
								return {
									enumerable: true,
									configurable: true,
								};
							},
							get(target, input) {
								invariant(typeof input === "string", "Input expected");
								if (router.target === "server") {
									const id =
										input === router.handler
											? virtualId(handlerModule(router))
											: input;
									return {
										assets() {
											return createHtmlTagsForAssets(
												router,
												app,
												findAssetsInViteManifest(bundlerManifest, id),
											);
										},
										output: {
											path: join$1(
												router.outDir,
												router.base,
												bundlerManifest[id].file,
											),
										},
									};
								} else if (router.target === "browser") {
									const id =
										input === router.handler && !input.endsWith(".html")
											? virtualId(handlerModule(router))
											: input;
									return {
										import() {
											return import(
												/* @vite-ignore */ joinURL(
													app.config.server.baseURL ?? "",
													router.base,
													bundlerManifest[id].file,
												)
											);
										},
										assets() {
											return createHtmlTagsForAssets(
												router,
												app,
												findAssetsInViteManifest(bundlerManifest, id),
											);
										},
										output: {
											path: joinURL(
												app.config.server.baseURL ?? "",
												router.base,
												bundlerManifest[id].file,
											),
										},
									};
								}
							},
						},
					),
				};
			},
		},
	);

	return manifest;
}

function plugin() {
	globalThis.MANIFEST =
		createProdManifest(globalThis.app)
			;
}

const chunks = {};
			 



			 function app() {
				 globalThis.$$chunks = chunks;
			 }

const plugins = [
  plugin$2,
plugin$1,
plugin,
app
];

const assets = {
  "/assets/content-BI6WYP-w.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"822-B66BdETkqI/JhsggIDQi5U5RoDA\"",
    "mtime": "2026-06-11T18:47:30.774Z",
    "size": 2082,
    "path": "../public/assets/content-BI6WYP-w.js.br"
  },
  "/assets/content-BI6WYP-w.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"90d-V5UfoVeW/4cbUZkM6YOxN+2qg8k\"",
    "mtime": "2026-06-11T18:47:30.774Z",
    "size": 2317,
    "path": "../public/assets/content-BI6WYP-w.js.gz"
  },
  "/assets/index-IxgNq1MU.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"a35a-OjuBU0/g4/eXFg6mbkVELuTRd0E\"",
    "mtime": "2026-06-11T18:47:30.973Z",
    "size": 41818,
    "path": "../public/assets/index-IxgNq1MU.js.br"
  },
  "/assets/index-IxgNq1MU.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"bbab-RW/JQPQydb+XqJdXWZIrt1JJqYM\"",
    "mtime": "2026-06-11T18:47:30.813Z",
    "size": 48043,
    "path": "../public/assets/index-IxgNq1MU.js.gz"
  },
  "/assets/jsonapi-GRx6mPsC.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"740a-EPkWpiNYZhypU9ehpNlTy3A87mU\"",
    "mtime": "2026-06-11T18:47:30.815Z",
    "size": 29706,
    "path": "../public/assets/jsonapi-GRx6mPsC.js.br"
  },
  "/assets/laika-LDlRdEpI.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"12cd0-TEfXhR+M9JCCbOwxaQWZQxVjytI\"",
    "mtime": "2026-06-11T18:47:31.106Z",
    "size": 77008,
    "path": "../public/assets/laika-LDlRdEpI.js.br"
  },
  "/assets/jsonapi-GRx6mPsC.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"834b-hdecBC8WTXoVFStvlwVaIFPWJKc\"",
    "mtime": "2026-06-11T18:47:30.813Z",
    "size": 33611,
    "path": "../public/assets/jsonapi-GRx6mPsC.js.gz"
  },
  "/assets/laika-LDlRdEpI.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"180f8-v0a09VWKrDMtbsDC54KgOuiP7OM\"",
    "mtime": "2026-06-11T18:47:30.816Z",
    "size": 98552,
    "path": "../public/assets/laika-LDlRdEpI.js.gz"
  },
  "/_build/.vite/manifest.json": {
    "type": "application/json",
    "encoding": null,
    "etag": "\"838-J630GyASpKTA/q0rn4OQgebRRug\"",
    "mtime": "2026-06-11T18:47:30.714Z",
    "size": 2104,
    "path": "../public/_build/.vite/manifest.json"
  },
  "/_build/.vite/manifest.json.br": {
    "type": "application/json",
    "encoding": "br",
    "etag": "\"1a3-YiEAR2FNxEKH9za4f5woAd46iXI\"",
    "mtime": "2026-06-11T18:47:30.774Z",
    "size": 419,
    "path": "../public/_build/.vite/manifest.json.br"
  },
  "/_build/.vite/manifest.json.gz": {
    "type": "application/json",
    "encoding": "gzip",
    "etag": "\"1dc-2atiGptI5BKi4DZjDPOx1BiDsSM\"",
    "mtime": "2026-06-11T18:47:30.774Z",
    "size": 476,
    "path": "../public/_build/.vite/manifest.json.gz"
  },
  "/_server/assets/content-D0eQygkN.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"1e2-1bvjaiiJ43qM6oBWKspon+R+Nz0\"",
    "mtime": "2026-06-11T18:47:30.832Z",
    "size": 482,
    "path": "../public/_server/assets/content-D0eQygkN.js.br"
  },
  "/_server/assets/content-D0eQygkN.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"228-fgEylbm+vzYwit5dORs3BUE/TSc\"",
    "mtime": "2026-06-11T18:47:30.832Z",
    "size": 552,
    "path": "../public/_server/assets/content-D0eQygkN.js.gz"
  },
  "/_server/assets/content-DMwGwsJ-.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"847-Yb4/JnzN/ElWZGISfIe7fHTvyjo\"",
    "mtime": "2026-06-11T18:47:30.843Z",
    "size": 2119,
    "path": "../public/_server/assets/content-DMwGwsJ-.js.br"
  },
  "/_server/assets/content-DMwGwsJ-.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"92e-r92pW1yLjXHKmDoJPIs0c0RIixA\"",
    "mtime": "2026-06-11T18:47:30.843Z",
    "size": 2350,
    "path": "../public/_server/assets/content-DMwGwsJ-.js.gz"
  },
  "/_server/assets/index-IxgNq1MU.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"a35a-OjuBU0/g4/eXFg6mbkVELuTRd0E\"",
    "mtime": "2026-06-11T18:47:31.011Z",
    "size": 41818,
    "path": "../public/_server/assets/index-IxgNq1MU.js.br"
  },
  "/_server/assets/index-IxgNq1MU.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"bbab-RW/JQPQydb+XqJdXWZIrt1JJqYM\"",
    "mtime": "2026-06-11T18:47:30.999Z",
    "size": 48043,
    "path": "../public/_server/assets/index-IxgNq1MU.js.gz"
  },
  "/_server/assets/jsonapi-GRx6mPsC.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"740a-EPkWpiNYZhypU9ehpNlTy3A87mU\"",
    "mtime": "2026-06-11T18:47:30.999Z",
    "size": 29706,
    "path": "../public/_server/assets/jsonapi-GRx6mPsC.js.br"
  },
  "/_server/assets/jsonapi-GRx6mPsC.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"834b-hdecBC8WTXoVFStvlwVaIFPWJKc\"",
    "mtime": "2026-06-11T18:47:30.999Z",
    "size": 33611,
    "path": "../public/_server/assets/jsonapi-GRx6mPsC.js.gz"
  },
  "/_server/assets/laika-LDlRdEpI.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"12cd0-TEfXhR+M9JCCbOwxaQWZQxVjytI\"",
    "mtime": "2026-06-11T18:47:31.162Z",
    "size": 77008,
    "path": "../public/_server/assets/laika-LDlRdEpI.js.br"
  },
  "/_server/assets/server-fns-CK80douL.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"414e-DP9gt4NztAuu3Uzd/JzyYpqqFME\"",
    "mtime": "2026-06-11T18:47:31.011Z",
    "size": 16718,
    "path": "../public/_server/assets/server-fns-CK80douL.js.gz"
  },
  "/_server/assets/laika-LDlRdEpI.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"180f8-v0a09VWKrDMtbsDC54KgOuiP7OM\"",
    "mtime": "2026-06-11T18:47:31.011Z",
    "size": 98552,
    "path": "../public/_server/assets/laika-LDlRdEpI.js.gz"
  },
  "/_build/assets/_slug_-N26OP5_J.js": {
    "type": "text/javascript; charset=utf-8",
    "encoding": null,
    "etag": "\"441-vOBBK6AUqtV+sOV0losVR4ug+Yw\"",
    "mtime": "2026-06-11T18:47:30.715Z",
    "size": 1089,
    "path": "../public/_build/assets/_slug_-N26OP5_J.js"
  },
  "/_server/assets/server-fns-CK80douL.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"3a31-OLqFfu4byZxxMN6Y6oyNVmnRz7Y\"",
    "mtime": "2026-06-11T18:47:31.011Z",
    "size": 14897,
    "path": "../public/_server/assets/server-fns-CK80douL.js.br"
  },
  "/_build/assets/_slug_-N26OP5_J.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"1fd-nhfyurvZvX0Vw9dKSPVVSfWKCRk\"",
    "mtime": "2026-06-11T18:47:30.774Z",
    "size": 509,
    "path": "../public/_build/assets/_slug_-N26OP5_J.js.br"
  },
  "/_build/assets/_slug_-N26OP5_J.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"268-/wgkFRGm7vYSrC+HnHDxQVMmIkk\"",
    "mtime": "2026-06-11T18:47:30.774Z",
    "size": 616,
    "path": "../public/_build/assets/_slug_-N26OP5_J.js.gz"
  },
  "/_build/assets/admin-D2eIpwgd.js": {
    "type": "text/javascript; charset=utf-8",
    "etag": "\"3ad-sTCQ4k6oHjblcCccY2dyzo7EIio\"",
    "mtime": "2026-06-11T18:47:30.715Z",
    "size": 941,
    "path": "../public/_build/assets/admin-D2eIpwgd.js"
  },
  "/_build/assets/client-BqU-jLUE.js": {
    "type": "text/javascript; charset=utf-8",
    "encoding": null,
    "etag": "\"23bc-I7Uxcm/dM/P5uLVwoUtJXlPrFEY\"",
    "mtime": "2026-06-11T18:47:30.715Z",
    "size": 9148,
    "path": "../public/_build/assets/client-BqU-jLUE.js"
  },
  "/_build/assets/client-BqU-jLUE.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"d2e-quIMIhoJrxFTBPyiHRSo3ZYPidE\"",
    "mtime": "2026-06-11T18:47:30.774Z",
    "size": 3374,
    "path": "../public/_build/assets/client-BqU-jLUE.js.br"
  },
  "/_build/assets/content-BnUEKs_s.js": {
    "type": "text/javascript; charset=utf-8",
    "encoding": null,
    "etag": "\"bdc8-O2npf/25JjmFgPe1r6sjqrD8uFc\"",
    "mtime": "2026-06-11T18:47:30.715Z",
    "size": 48584,
    "path": "../public/_build/assets/content-BnUEKs_s.js"
  },
  "/_build/assets/client-BqU-jLUE.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"eb6-qvfEDZ4sUPWgthVimfJq7a8D07I\"",
    "mtime": "2026-06-11T18:47:30.774Z",
    "size": 3766,
    "path": "../public/_build/assets/client-BqU-jLUE.js.gz"
  },
  "/_build/assets/content-BnUEKs_s.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"3b35-d+dnB0PFtgT8zfUe55M5wBngb0k\"",
    "mtime": "2026-06-11T18:47:30.775Z",
    "size": 15157,
    "path": "../public/_build/assets/content-BnUEKs_s.js.br"
  },
  "/_build/assets/content-BnUEKs_s.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"4148-ldZ38OrTGgPur7oYPQOP7FS3am0\"",
    "mtime": "2026-06-11T18:47:30.809Z",
    "size": 16712,
    "path": "../public/_build/assets/content-BnUEKs_s.js.gz"
  },
  "/_build/assets/index-BJeVjlIs.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"14da6-SJpArW1IM87J9xXbrpqf4mCDln4\"",
    "mtime": "2026-06-11T18:47:31.125Z",
    "size": 85414,
    "path": "../public/_build/assets/index-BJeVjlIs.js.br"
  },
  "/_build/assets/index-YKGcSqIy.js": {
    "type": "text/javascript; charset=utf-8",
    "encoding": null,
    "etag": "\"451-BkphNVAvUJ6ujvePNETTygPSbeM\"",
    "mtime": "2026-06-11T18:47:30.715Z",
    "size": 1105,
    "path": "../public/_build/assets/index-YKGcSqIy.js"
  },
  "/_build/assets/index-YKGcSqIy.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"22c-DusU7YVjuCdTjeyA/RJKMR2KBJ4\"",
    "mtime": "2026-06-11T18:47:30.815Z",
    "size": 556,
    "path": "../public/_build/assets/index-YKGcSqIy.js.br"
  },
  "/_build/assets/index-BJeVjlIs.js": {
    "type": "text/javascript; charset=utf-8",
    "encoding": null,
    "etag": "\"47c42-vinANmpgT/Fdci5SAUoS3vMUqMA\"",
    "mtime": "2026-06-11T18:47:30.715Z",
    "size": 293954,
    "path": "../public/_build/assets/index-BJeVjlIs.js"
  },
  "/_build/assets/preload-helper-CM3UJVvY.js": {
    "type": "text/javascript; charset=utf-8",
    "etag": "\"3e0-Q0Bp0QjKFMyDOuO3rqIIPlfnrHI\"",
    "mtime": "2026-06-11T18:47:30.715Z",
    "size": 992,
    "path": "../public/_build/assets/preload-helper-CM3UJVvY.js"
  },
  "/_build/assets/index-BJeVjlIs.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"17f5d-8lh9+DyHGJSekRciuFq956pCI2s\"",
    "mtime": "2026-06-11T18:47:30.832Z",
    "size": 98141,
    "path": "../public/_build/assets/index-BJeVjlIs.js.gz"
  },
  "/_build/assets/solid-BEU1w9CJ.js": {
    "type": "text/javascript; charset=utf-8",
    "encoding": null,
    "etag": "\"3a99-FdFxINVJHj3pQfWjp7HBOT65ynk\"",
    "mtime": "2026-06-11T18:47:30.715Z",
    "size": 15001,
    "path": "../public/_build/assets/solid-BEU1w9CJ.js"
  },
  "/_build/assets/solid-BEU1w9CJ.js.br": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "br",
    "etag": "\"1584-s3yxOBWteR27z2xLUXd4y60uApo\"",
    "mtime": "2026-06-11T18:47:30.832Z",
    "size": 5508,
    "path": "../public/_build/assets/solid-BEU1w9CJ.js.br"
  },
  "/_build/assets/solid-BEU1w9CJ.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"174b-8+eDg2sSlroTueyFc5sLWLBBjM8\"",
    "mtime": "2026-06-11T18:47:30.832Z",
    "size": 5963,
    "path": "../public/_build/assets/solid-BEU1w9CJ.js.gz"
  },
  "/_build/assets/index-YKGcSqIy.js.gz": {
    "type": "text/javascript; charset=utf-8",
    "encoding": "gzip",
    "etag": "\"29a-CZVnv/3SVt+68hULfaydU7YAZK4\"",
    "mtime": "2026-06-11T18:47:30.815Z",
    "size": 666,
    "path": "../public/_build/assets/index-YKGcSqIy.js.gz"
  }
};

const _DRIVE_LETTER_START_RE = /^[A-Za-z]:\//;
function normalizeWindowsPath(input = "") {
  if (!input) {
    return input;
  }
  return input.replace(/\\/g, "/").replace(_DRIVE_LETTER_START_RE, (r) => r.toUpperCase());
}
const _IS_ABSOLUTE_RE = /^[/\\](?![/\\])|^[/\\]{2}(?!\.)|^[A-Za-z]:[/\\]/;
const _DRIVE_LETTER_RE = /^[A-Za-z]:$/;
function cwd() {
  if (typeof process !== "undefined" && typeof process.cwd === "function") {
    return process.cwd().replace(/\\/g, "/");
  }
  return "/";
}
const resolve = function(...arguments_) {
  arguments_ = arguments_.map((argument) => normalizeWindowsPath(argument));
  let resolvedPath = "";
  let resolvedAbsolute = false;
  for (let index = arguments_.length - 1; index >= -1 && !resolvedAbsolute; index--) {
    const path = index >= 0 ? arguments_[index] : cwd();
    if (!path || path.length === 0) {
      continue;
    }
    resolvedPath = `${path}/${resolvedPath}`;
    resolvedAbsolute = isAbsolute(path);
  }
  resolvedPath = normalizeString(resolvedPath, !resolvedAbsolute);
  if (resolvedAbsolute && !isAbsolute(resolvedPath)) {
    return `/${resolvedPath}`;
  }
  return resolvedPath.length > 0 ? resolvedPath : ".";
};
function normalizeString(path, allowAboveRoot) {
  let res = "";
  let lastSegmentLength = 0;
  let lastSlash = -1;
  let dots = 0;
  let char = null;
  for (let index = 0; index <= path.length; ++index) {
    if (index < path.length) {
      char = path[index];
    } else if (char === "/") {
      break;
    } else {
      char = "/";
    }
    if (char === "/") {
      if (lastSlash === index - 1 || dots === 1) ; else if (dots === 2) {
        if (res.length < 2 || lastSegmentLength !== 2 || res[res.length - 1] !== "." || res[res.length - 2] !== ".") {
          if (res.length > 2) {
            const lastSlashIndex = res.lastIndexOf("/");
            if (lastSlashIndex === -1) {
              res = "";
              lastSegmentLength = 0;
            } else {
              res = res.slice(0, lastSlashIndex);
              lastSegmentLength = res.length - 1 - res.lastIndexOf("/");
            }
            lastSlash = index;
            dots = 0;
            continue;
          } else if (res.length > 0) {
            res = "";
            lastSegmentLength = 0;
            lastSlash = index;
            dots = 0;
            continue;
          }
        }
        if (allowAboveRoot) {
          res += res.length > 0 ? "/.." : "..";
          lastSegmentLength = 2;
        }
      } else {
        if (res.length > 0) {
          res += `/${path.slice(lastSlash + 1, index)}`;
        } else {
          res = path.slice(lastSlash + 1, index);
        }
        lastSegmentLength = index - lastSlash - 1;
      }
      lastSlash = index;
      dots = 0;
    } else if (char === "." && dots !== -1) {
      ++dots;
    } else {
      dots = -1;
    }
  }
  return res;
}
const isAbsolute = function(p) {
  return _IS_ABSOLUTE_RE.test(p);
};
const dirname = function(p) {
  const segments = normalizeWindowsPath(p).replace(/\/$/, "").split("/").slice(0, -1);
  if (segments.length === 1 && _DRIVE_LETTER_RE.test(segments[0])) {
    segments[0] += "/";
  }
  return segments.join("/") || (isAbsolute(p) ? "/" : ".");
};

function readAsset (id) {
  const serverDir = dirname(fileURLToPath(globalThis._importMeta_.url));
  return promises.readFile(resolve(serverDir, assets[id].path))
}

const publicAssetBases = {};

function isPublicAssetURL(id = '') {
  if (assets[id]) {
    return true
  }
  for (const base in publicAssetBases) {
    if (id.startsWith(base)) { return true }
  }
  return false
}

function getAsset (id) {
  return assets[id]
}

const METHODS = /* @__PURE__ */ new Set(["HEAD", "GET"]);
const EncodingMap = { gzip: ".gz", br: ".br" };
const _cVoaHD = eventHandler((event) => {
  if (event.method && !METHODS.has(event.method)) {
    return;
  }
  let id = decodePath(
    withLeadingSlash(withoutTrailingSlash(parseURL(event.path).pathname))
  );
  let asset;
  const encodingHeader = String(
    getRequestHeader(event, "accept-encoding") || ""
  );
  const encodings = [
    ...encodingHeader.split(",").map((e) => EncodingMap[e.trim()]).filter(Boolean).sort(),
    ""
  ];
  for (const encoding of encodings) {
    for (const _id of [id + encoding, joinURL(id, "index.html" + encoding)]) {
      const _asset = getAsset(_id);
      if (_asset) {
        asset = _asset;
        id = _id;
        break;
      }
    }
  }
  if (!asset) {
    if (isPublicAssetURL(id)) {
      removeResponseHeader(event, "Cache-Control");
      throw createError$1({ statusCode: 404 });
    }
    return;
  }
  if (asset.encoding !== void 0) {
    appendResponseHeader(event, "Vary", "Accept-Encoding");
  }
  const ifNotMatch = getRequestHeader(event, "if-none-match") === asset.etag;
  if (ifNotMatch) {
    setResponseStatus(event, 304, "Not Modified");
    return "";
  }
  const ifModifiedSinceH = getRequestHeader(event, "if-modified-since");
  const mtimeDate = new Date(asset.mtime);
  if (ifModifiedSinceH && asset.mtime && new Date(ifModifiedSinceH) >= mtimeDate) {
    setResponseStatus(event, 304, "Not Modified");
    return "";
  }
  if (asset.type && !getResponseHeader(event, "Content-Type")) {
    setResponseHeader(event, "Content-Type", asset.type);
  }
  if (asset.etag && !getResponseHeader(event, "ETag")) {
    setResponseHeader(event, "ETag", asset.etag);
  }
  if (asset.mtime && !getResponseHeader(event, "Last-Modified")) {
    setResponseHeader(event, "Last-Modified", mtimeDate.toUTCString());
  }
  if (asset.encoding && !getResponseHeader(event, "Content-Encoding")) {
    setResponseHeader(event, "Content-Encoding", asset.encoding);
  }
  if (asset.size > 0 && !getResponseHeader(event, "Content-Length")) {
    setResponseHeader(event, "Content-Length", asset.size);
  }
  return readAsset(id);
});

var __defProp$6 = Object.defineProperty;
var __defNormalProp$6 = (obj, key, value) => key in obj ? __defProp$6(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField$6 = (obj, key, value) => __defNormalProp$6(obj, typeof key !== "symbol" ? key + "" : key, value);
var _a2$3, _b$1, _c2$1, _d2$1, _e2$1, _f2$1, _g$1, _h2$1, _i2$1, _j$1, _k$1, _l2$1, _m2$1, _n2$1;
const ke$3 = (e, t) => {
  switch (t.length) {
    case 0:
      return e;
    case 1:
      return t[0](e);
    case 2:
      return t[1](t[0](e));
    case 3:
      return t[2](t[1](t[0](e)));
    case 4:
      return t[3](t[2](t[1](t[0](e))));
    case 5:
      return t[4](t[3](t[2](t[1](t[0](e)))));
    case 6:
      return t[5](t[4](t[3](t[2](t[1](t[0](e))))));
    case 7:
      return t[6](t[5](t[4](t[3](t[2](t[1](t[0](e)))))));
    case 8:
      return t[7](t[6](t[5](t[4](t[3](t[2](t[1](t[0](e))))))));
    case 9:
      return t[8](t[7](t[6](t[5](t[4](t[3](t[2](t[1](t[0](e)))))))));
    default: {
      let n = e;
      for (let r = 0, s = t.length; r < s; r++) n = t[r](n);
      return n;
    }
  }
}, fc$1 = { pipe() {
  return ke$3(this, arguments);
} }, Er$1 = (function() {
  function e() {
  }
  return e.prototype = fc$1, e;
})(), d$3 = function(e, t) {
  if (typeof e == "function") return function() {
    return e(arguments) ? t.apply(this, arguments) : (n) => t(n, ...arguments);
  };
  switch (e) {
    case 0:
    case 1:
      throw new RangeError(`Invalid arity ${e}`);
    case 2:
      return function(n, r) {
        return arguments.length >= 2 ? t(n, r) : function(s) {
          return t(s, n);
        };
      };
    case 3:
      return function(n, r, s) {
        return arguments.length >= 3 ? t(n, r, s) : function(o) {
          return t(o, n, r);
        };
      };
    default:
      return function() {
        if (arguments.length >= e) return t.apply(this, arguments);
        const n = arguments;
        return function(r) {
          return t(r, ...n);
        };
      };
  }
}, B$2 = (e) => e, Nt$2 = (e) => () => e, dc$1 = Nt$2(true), Gs$2 = Nt$2(void 0), qs$2 = Gs$2;
function pc$1(e, t, n, r, s, o, i, u, c) {
  switch (arguments.length) {
    case 1:
      return e;
    case 2:
      return function() {
        return t(e.apply(this, arguments));
      };
    case 3:
      return function() {
        return n(t(e.apply(this, arguments)));
      };
    case 4:
      return function() {
        return r(n(t(e.apply(this, arguments))));
      };
    case 5:
      return function() {
        return s(r(n(t(e.apply(this, arguments)))));
      };
    case 6:
      return function() {
        return o(s(r(n(t(e.apply(this, arguments))))));
      };
    case 7:
      return function() {
        return i(o(s(r(n(t(e.apply(this, arguments)))))));
      };
    case 8:
      return function() {
        return u(i(o(s(r(n(t(e.apply(this, arguments))))))));
      };
    case 9:
      return function() {
        return c(u(i(o(s(r(n(t(e.apply(this, arguments)))))))));
      };
  }
}
function De$3(e) {
  const t = /* @__PURE__ */ new WeakMap();
  return (n) => {
    if (t.has(n)) return t.get(n);
    const r = e(n);
    return t.set(n, r), r;
  };
}
const Xn$2 = (e) => {
  const t = new Set(Reflect.ownKeys(e));
  if (e.constructor === Object) return t;
  e instanceof Error && t.delete("stack");
  const n = Object.getPrototypeOf(e);
  let r = n;
  for (; r !== null && r !== Object.prototype; ) {
    const s = Reflect.ownKeys(r);
    for (let o = 0; o < s.length; o++) t.add(s[o]);
    r = Object.getPrototypeOf(r);
  }
  return t.has("constructor") && typeof e.constructor == "function" && n === e.constructor.prototype && t.delete("constructor"), t;
}, er$2 = /* @__PURE__ */ new WeakSet();
function Hs$2(e) {
  return typeof e == "string";
}
function Zs$2(e) {
  return typeof e == "number";
}
function hc$1(e) {
  return typeof e == "boolean";
}
function mc$1(e) {
  return typeof e == "symbol";
}
function gc$1(e) {
  return Hs$2(e) || Zs$2(e) || mc$1(e);
}
function br$1(e) {
  return typeof e == "function";
}
function yc$1(e) {
  return e != null;
}
function Qs$2(e) {
  return true;
}
function Ec$1(e) {
  return typeof e == "object" && e !== null || br$1(e);
}
const P$3 = d$3(2, (e, t) => Ec$1(e) && t in e), ee$4 = "~effect/interfaces/Hash", D$2 = (e) => {
  switch (typeof e) {
    case "number":
      return hn$2(e);
    case "bigint":
      return R$5(e.toString(10));
    case "boolean":
      return R$5(String(e));
    case "symbol":
      return R$5(String(e));
    case "string":
      return R$5(e);
    case "undefined":
      return R$5("undefined");
    case "function":
    case "object": {
      if (e === null) return R$5("null");
      if (e instanceof Date) return R$5(e.toISOString());
      if (e instanceof RegExp) return R$5(e.toString());
      {
        if (er$2.has(e)) return tr$2(e);
        if (Kn$2.has(e)) return Kn$2.get(e);
        const t = wc$1(e, () => bc$1(e) ? e[ee$4]() : typeof e == "function" ? tr$2(e) : Array.isArray(e) || ArrayBuffer.isView(e) ? Xs$2(e) : e instanceof Map ? _c$3(e) : e instanceof Set ? xc$1(e) : Tc$1(e));
        return Kn$2.set(e, t), t;
      }
    }
    default:
      throw new Error(`BUG: unhandled typeof ${typeof e} - please report an issue at https://github.com/Effect-TS/effect/issues`);
  }
}, tr$2 = (e) => (Jn$2.has(e) || Jn$2.set(e, hn$2(Math.floor(Math.random() * Number.MAX_SAFE_INTEGER))), Jn$2.get(e)), de$5 = d$3(2, (e, t) => e * 53 ^ t), pn$2 = (e) => e & 3221225471 | e >>> 1 & 1073741824, bc$1 = (e) => P$3(e, ee$4), hn$2 = (e) => {
  if (e !== e) return R$5("NaN");
  if (e === 1 / 0) return R$5("Infinity");
  if (e === -1 / 0) return R$5("-Infinity");
  let t = e | 0;
  for (t !== e && (t ^= e * 4294967295); e > 4294967295; ) t ^= e /= 4294967295;
  return pn$2(t);
}, R$5 = (e) => {
  let t = 5381, n = e.length;
  for (; n; ) t = t * 33 ^ e.charCodeAt(--n);
  return pn$2(t);
}, Sc$1 = (e, t) => {
  let n = 12289;
  for (const r of t) n ^= de$5(D$2(r), D$2(e[r]));
  return pn$2(n);
}, Tc$1 = (e) => Sc$1(e, Xn$2(e)), Sr$1 = (e, t) => (n) => {
  let r = e;
  for (const s of n) r ^= t(s);
  return pn$2(r);
}, Xs$2 = Sr$1(6151, D$2), _c$3 = Sr$1(R$5("Map"), ([e, t]) => de$5(D$2(e), D$2(t))), xc$1 = Sr$1(R$5("Set"), D$2), Jn$2 = /* @__PURE__ */ new WeakMap(), Kn$2 = /* @__PURE__ */ new WeakMap(), Bn$2 = /* @__PURE__ */ new WeakSet();
function wc$1(e, t) {
  if (Bn$2.has(e)) return R$5("[Circular]");
  Bn$2.add(e);
  const n = t();
  return Bn$2.delete(e), n;
}
const te$3 = "~effect/interfaces/Equal";
function oe$4() {
  return arguments.length === 1 ? (e) => Pe$3(e, arguments[0]) : Pe$3(arguments[0], arguments[1]);
}
function Pe$3(e, t) {
  if (e === t) return true;
  if (e == null || t == null) return false;
  const n = typeof e;
  return n !== typeof t ? false : n === "number" && e !== e && t !== t ? true : n !== "object" && n !== "function" || er$2.has(e) || er$2.has(t) ? false : kc$1(e, t, Ac$1);
}
function Oc$1(e, t, n) {
  const r = Yn$2.has(e), s = Wn$2.has(t);
  if (r && s) return true;
  if (r || s) return false;
  Yn$2.add(e), Wn$2.add(t);
  const o = n();
  return Yn$2.delete(e), Wn$2.delete(t), o;
}
const Yn$2 = /* @__PURE__ */ new WeakSet(), Wn$2 = /* @__PURE__ */ new WeakSet();
function Ac$1(e, t) {
  if (D$2(e) !== D$2(t)) return false;
  if (e instanceof Date) return t instanceof Date ? e.toISOString() === t.toISOString() : false;
  if (e instanceof RegExp) return t instanceof RegExp ? e.toString() === t.toString() : false;
  const n = ps$2(e), r = ps$2(t);
  if (n !== r) return false;
  const s = n && r;
  return typeof e == "function" && !s ? false : Oc$1(e, t, () => s ? e[te$3](t) : Array.isArray(e) ? !Array.isArray(t) || e.length !== t.length ? false : Ic$1(e, t) : ArrayBuffer.isView(e) ? !ArrayBuffer.isView(t) || e.byteLength !== t.byteLength ? false : vc$1(e, t) : e instanceof Map ? !(t instanceof Map) || e.size !== t.size ? false : Dc$1(e, t) : e instanceof Set ? !(t instanceof Set) || e.size !== t.size ? false : Fc$1(e, t) : Cc$1(e, t));
}
function kc$1(e, t, n) {
  let r = Wt$2.get(e);
  if (!r) r = /* @__PURE__ */ new WeakMap(), Wt$2.set(e, r);
  else if (r.has(t)) return r.get(t);
  const s = n(e, t);
  r.set(t, s);
  let o = Wt$2.get(t);
  return o || (o = /* @__PURE__ */ new WeakMap(), Wt$2.set(t, o)), o.set(e, s), s;
}
const Wt$2 = /* @__PURE__ */ new WeakMap();
function Ic$1(e, t) {
  for (let n = 0; n < e.length; n++) if (!Pe$3(e[n], t[n])) return false;
  return true;
}
function vc$1(e, t) {
  if (e.length !== t.length) return false;
  for (let n = 0; n < e.length; n++) if (e[n] !== t[n]) return false;
  return true;
}
function Cc$1(e, t) {
  const n = Xn$2(e), r = Xn$2(t);
  if (n.size !== r.size) return false;
  for (const s of n) if (!r.has(s) || !Pe$3(e[s], t[s])) return false;
  return true;
}
function $c$1(e, t) {
  return function(r, s) {
    for (const [o, i] of r) {
      let u = false;
      for (const [c, a] of s) if (e(o, c) && t(i, a)) {
        u = true;
        break;
      }
      if (!u) return false;
    }
    return true;
  };
}
const Dc$1 = $c$1(Pe$3, Pe$3);
function Uc$1(e) {
  return function(n, r) {
    for (const s of n) {
      let o = false;
      for (const i of r) if (e(s, i)) {
        o = true;
        break;
      }
      if (!o) return false;
    }
    return true;
  };
}
const Fc$1 = Uc$1(Pe$3), ps$2 = (e) => P$3(e, te$3), Nc$1 = () => oe$4, Tr$2 = /* @__PURE__ */ Symbol.for("~effect/Redactable"), Rc$1 = (e) => P$3(e, Tr$2);
function eo$2(e) {
  return Rc$1(e) ? to$2(e) : e;
}
function to$2(e) {
  var _a3, _b2;
  return e[Tr$2]((_b2 = (_a3 = globalThis[Tt$2]) == null ? void 0 : _a3.context) != null ? _b2 : Pc$1);
}
const Tt$2 = "~effect/Fiber/currentFiber", Pc$1 = { "~effect/Context": {}, mapUnsafe: /* @__PURE__ */ new Map(), pipe() {
  return ke$3(this, arguments);
} };
function L$3(e, t) {
  var _a3;
  const n = (_a3 = t == null ? void 0 : t.space) != null ? _a3 : 0, r = /* @__PURE__ */ new WeakSet(), s = n ? typeof n == "number" ? " ".repeat(n) : n : "", o = (a) => s.repeat(a), i = (a, l) => {
    const f = a == null ? void 0 : a.constructor;
    return f && f !== Object.prototype.constructor && f.name ? `${f.name}(${l})` : l;
  }, u = (a) => {
    try {
      return Reflect.ownKeys(a);
    } catch {
      return ["[ownKeys threw]"];
    }
  };
  function c(a, l = 0) {
    if (Array.isArray(a)) {
      if (r.has(a)) return hs$2;
      if (r.add(a), !s || a.length <= 1) return `[${a.map((p) => c(p, l)).join(",")}]`;
      const f = a.map((p) => c(p, l + 1)).join(`,
` + o(l + 1));
      return `[
${o(l + 1)}${f}
${o(l)}]`;
    }
    if (a instanceof Date) return jc$1(a);
    if (!(t == null ? void 0 : t.ignoreToString) && P$3(a, "toString") && typeof a.toString == "function" && a.toString !== Object.prototype.toString && a.toString !== Array.prototype.toString) {
      const f = Mc$1(a);
      return a instanceof Error && a.cause ? `${f} (cause: ${c(a.cause, l)})` : f;
    }
    if (typeof a == "string") return JSON.stringify(a);
    if (typeof a == "number" || a == null || typeof a == "boolean" || typeof a == "symbol") return String(a);
    if (typeof a == "bigint") return String(a) + "n";
    if (typeof a == "object" || typeof a == "function") {
      if (r.has(a)) return hs$2;
      if (r.add(a), Tr$2 in a) return L$3(to$2(a));
      if (Symbol.iterator in a) return `${a.constructor.name}(${c(Array.from(a), l)})`;
      const f = u(a);
      if (!s || f.length <= 1) {
        const E = `{${f.map((m) => `${Xt$2(m)}:${c(a[m], l)}`).join(",")}}`;
        return i(a, E);
      }
      const p = `{
${f.map((E) => `${o(l + 1)}${Xt$2(E)}: ${c(a[E], l + 1)}`).join(`,
`)}
${o(l)}}`;
      return i(a, p);
    }
    return String(a);
  }
  return c(e, 0);
}
const hs$2 = "[Circular]";
function Xt$2(e) {
  return typeof e == "string" ? JSON.stringify(e) : String(e);
}
function Lc$1(e) {
  return e.map((t) => `[${Xt$2(t)}]`).join("");
}
function jc$1(e) {
  try {
    return e.toISOString();
  } catch {
    return "Invalid Date";
  }
}
function Mc$1(e) {
  try {
    const t = e.toString();
    return typeof t == "string" ? t : String(t);
  } catch {
    return "[toString threw]";
  }
}
function no$2(e, t) {
  const n = [];
  return JSON.stringify(e, function(r, s) {
    const o = eo$2(s);
    if (typeof o != "object" || o === null) return o;
    for (; n.length > 0 && n[n.length - 1] !== this; ) n.pop();
    if (!n.includes(o)) return n.push(o), o;
  }, void 0 );
}
const mn$2 = /* @__PURE__ */ Symbol.for("nodejs.util.inspect.custom"), gn$2 = (e) => {
  try {
    if (P$3(e, "toJSON") && br$1(e.toJSON) && e.toJSON.length === 0) return e.toJSON();
    if (Array.isArray(e)) return e.map(gn$2);
  } catch {
    return "[toJSON threw]";
  }
  return eo$2(e);
};
let Rt$2 = class Rt {
  constructor(t) {
    __publicField$6(this, "called", false);
    __publicField$6(this, "self");
    this.self = t;
  }
  next(t) {
    return this.called ? { value: t, done: true } : (this.called = true, { value: this.self, done: false });
  }
  [Symbol.iterator]() {
    return new Rt(this.self);
  }
};
const Xe$2 = "~effect/Utils/internal", ro$2 = { [Xe$2]: (e) => e() }, zc$1 = { [Xe$2]: (e) => {
  try {
    return e();
  } finally {
  }
} }, Vc$1 = ((_a2$3 = ro$2[Xe$2](() => new Error().stack)) == null ? void 0 : _a2$3.includes(Xe$2)) === true, se$4 = Vc$1 ? ro$2[Xe$2] : zc$1[Xe$2], Ue$2 = "~effect/Effect", At$2 = "~effect/Exit", Jc$1 = { _A: B$2, _E: B$2, _R: B$2 }, so$2 = `${Ue$2}/identifier`, g$2 = `${Ue$2}/args`, U$2 = `${Ue$2}/evaluate`, Q$4 = `${Ue$2}/successCont`, ge$5 = `${Ue$2}/failureCont`, Le$2 = `${Ue$2}/ensureCont`, Ne$3 = /* @__PURE__ */ Symbol.for("effect/Effect/Yield"), Pt$2 = { pipe() {
  return ke$3(this, arguments);
}, toJSON() {
  return { ...this };
}, toString() {
  return L$3(this.toJSON(), { ignoreToString: true, space: 2 });
}, [mn$2]() {
  return this.toJSON();
} }, Kc$1 = { [Ue$2]: Jc$1, ...Pt$2, [Symbol.iterator]() {
  return new Rt$2(this);
}, toJSON() {
  return { _id: "Effect", op: this[so$2], ...g$2 in this ? { args: this[g$2] } : void 0 };
} }, _r$4 = (e) => P$3(e, Ue$2), Bc$1 = (e) => P$3(e, At$2), Gt$2 = "~effect/Cause", Gn$2 = "~effect/Cause/Reason", oo$2 = (e) => P$3(e, Gt$2);
let it$2 = class it {
  constructor(t) {
    __publicField$6(this, _b$1);
    __publicField$6(this, "reasons");
    this[Gt$2] = Gt$2, this.reasons = t;
  }
  pipe() {
    return ke$3(this, arguments);
  }
  toJSON() {
    return { _id: "Cause", failures: this.reasons.map((t) => t.toJSON()) };
  }
  toString() {
    return `Cause(${L$3(this.reasons)})`;
  }
  [(_b$1 = Gt$2, mn$2)]() {
    return this.toJSON();
  }
  [te$3](t) {
    return oo$2(t) && this.reasons.length === t.reasons.length && this.reasons.every((n, r) => oe$4(n, t.reasons[r]));
  }
  [ee$4]() {
    return Xs$2(this.reasons);
  }
};
const ms$2 = /* @__PURE__ */ new WeakMap();
let xr$2 = class xr {
  constructor(t, n, r) {
    __publicField$6(this, _c2$1);
    __publicField$6(this, "annotations");
    __publicField$6(this, "_tag");
    if (this[Gn$2] = Gn$2, this._tag = t, n !== yn$2 && typeof r == "object" && r !== null && n.size > 0) {
      const s = ms$2.get(r);
      s && (n = new Map([...s, ...n])), ms$2.set(r, n);
    }
    this.annotations = n;
  }
  annotate(t, n) {
    if (t.mapUnsafe.size === 0) return this;
    const r = new Map(this.annotations);
    t.mapUnsafe.forEach((o, i) => {
      (n == null ? void 0 : n.overwrite) !== true && r.has(i) || r.set(i, o);
    });
    const s = Object.assign(Object.create(Object.getPrototypeOf(this)), this);
    return s.annotations = r, s;
  }
  pipe() {
    return ke$3(this, arguments);
  }
  toString() {
    return L$3(this);
  }
  [(_c2$1 = Gn$2, mn$2)]() {
    return this.toString();
  }
};
const yn$2 = /* @__PURE__ */ new Map();
let Yc$1 = class Yc extends xr$2 {
  constructor(t, n = yn$2) {
    super("Fail", n, t);
    __publicField$6(this, "error");
    this.error = t;
  }
  toString() {
    return `Fail(${L$3(this.error)})`;
  }
  toJSON() {
    return { _tag: "Fail", error: this.error };
  }
  [te$3](t) {
    return co$1(t) && oe$4(this.error, t.error) && oe$4(this.annotations, t.annotations);
  }
  [ee$4]() {
    return de$5(R$5(this._tag))(de$5(D$2(this.error))(D$2(this.annotations)));
  }
};
const nr$2 = (e) => new it$2(e), io$2 = (e) => new it$2([new Yc$1(e)]);
let Wc$1 = class Wc extends xr$2 {
  constructor(t, n = yn$2) {
    super("Die", n, t);
    __publicField$6(this, "defect");
    this.defect = t;
  }
  toString() {
    return `Die(${L$3(this.defect)})`;
  }
  toJSON() {
    return { _tag: "Die", defect: this.defect };
  }
  [te$3](t) {
    return Gc$1(t) && oe$4(this.defect, t.defect) && oe$4(this.annotations, t.annotations);
  }
  [ee$4]() {
    return de$5(R$5(this._tag))(de$5(D$2(this.defect))(D$2(this.annotations)));
  }
};
const ao$2 = (e) => new it$2([new Wc$1(e)]), rr$2 = d$3((e) => oo$2(e[0]), (e, t, n) => t.mapUnsafe.size === 0 ? e : new it$2(e.reasons.map((r) => r.annotate(t, n)))), co$1 = (e) => e._tag === "Fail", Gc$1 = (e) => e._tag === "Die", uo$2 = (e) => e._tag === "Interrupt";
function qc$1(e) {
  return kt$2("Effect.evaluate: Not implemented");
}
const ze$2 = (e) => {
  var _a3;
  return { ...Kc$1, [so$2]: e.op, [U$2]: (_a3 = e[U$2]) != null ? _a3 : qc$1, [Q$4]: e[Q$4], [ge$5]: e[ge$5], [Le$2]: e[Le$2] };
}, he$3 = (e) => {
  const t = ze$2(e);
  return function() {
    const n = Object.create(t);
    return n[g$2] = e.single === false ? arguments : arguments[0], n;
  };
}, lo$1 = (e) => {
  const t = { ...ze$2(e), [At$2]: At$2, _tag: e.op, get [e.prop]() {
    return this[g$2];
  }, toString() {
    return `${e.op}(${L$3(this[g$2])})`;
  }, toJSON() {
    return { _id: "Exit", _tag: e.op, [e.prop]: this[g$2] };
  }, [te$3](n) {
    return Bc$1(n) && n._tag === this._tag && oe$4(this[g$2], n[g$2]);
  }, [ee$4]() {
    return de$5(R$5(e.op), D$2(this[g$2]));
  } };
  return function(n) {
    const r = Object.create(t);
    return r[g$2] = n, r;
  };
}, I$3 = lo$1({ op: "Success", prop: "value", [U$2](e) {
  const t = e.getCont(Q$4);
  return t ? t[Q$4](this[g$2], e, this) : e.yieldWith(this);
} }), En$2 = { key: "effect/Cause/StackTrace" }, Hc$1 = { key: "effect/Cause/InterruptorStackTrace" }, ie$4 = lo$1({ op: "Failure", prop: "cause", [U$2](e) {
  let t = this[g$2], n = false;
  e.currentStackFrame && (t = rr$2(t, { mapUnsafe: /* @__PURE__ */ new Map([[En$2.key, e.currentStackFrame]]) }), n = true);
  let r = e.getCont(ge$5);
  for (; e.interruptible && e._interruptedCause && r; ) r = e.getCont(ge$5);
  return r ? r[ge$5](t, e, n ? void 0 : this) : e.yieldWith(n ? this : ie$4(t));
} }), Ve$2 = (e) => ie$4(io$2(e)), kt$2 = (e) => ie$4(ao$2(e)), Y$4 = he$3({ op: "WithFiber", [U$2](e) {
  return this[g$2](e);
} }), Zc$1 = (function() {
  class e extends globalThis.Error {
  }
  const t = ze$2({ op: "YieldableError", [U$2]() {
    return Ve$2(this);
  } });
  return delete t.toString, Object.assign(e.prototype, t), e;
})(), Qc$1 = /* @__PURE__ */ (function() {
  const e = /* @__PURE__ */ Symbol.for("effect/Data/Error/plainArgs");
  return class extends Zc$1 {
    constructor(n) {
      super(n == null ? void 0 : n.message, (n == null ? void 0 : n.cause) ? { cause: n.cause } : void 0), n && (Object.assign(this, n), Object.defineProperty(this, e, { value: n, enumerable: false }));
    }
    toJSON() {
      return { ...this[e], ...this };
    }
  };
})(), wr$1 = (e) => {
  class t extends Qc$1 {
    constructor() {
      super(...arguments);
      __publicField$6(this, "_tag", e);
    }
  }
  return t.prototype.name = e, t;
}, It$2 = "~effect/Cause/Done", Xc$1 = (e) => P$3(e, It$2), fo$1 = { [It$2]: It$2, _tag: "Done", value: void 0 }, Or$2 = (e) => e === void 0 ? fo$1 : { [It$2]: It$2, _tag: "Done", value: e }, eu$1 = Ve$2(fo$1), tu$1 = (e) => e === void 0 ? eu$1 : Ve$2(Or$2(e)), nu$1 = (e) => ze$2({ op: e.label, [U$2]: e.evaluate }), ru$1 = (e) => (t, n) => t === n || e(t, n), po$1 = "~effect/data/Option", ho$1 = { [po$1]: { _A: (e) => e }, ...Pt$2, [Symbol.iterator]() {
  return new Rt$2(this);
} }, mo$1 = Object.assign(Object.create(ho$1), { _tag: "Some", _op: "Some", [te$3](e) {
  return go$2(e) && Eo$1(e) && oe$4(this.value, e.value);
}, [ee$4]() {
  return de$5(D$2(this._tag))(D$2(this.value));
}, toString() {
  return `some(${L$3(this.value)})`;
}, toJSON() {
  return { _id: "Option", _tag: this._tag, value: gn$2(this.value) };
} });
Object.defineProperty(mo$1, "valueOrUndefined", { get() {
  return this.value;
} });
const su$1 = D$2("None"), ou$1 = Object.assign(Object.create(ho$1), { _tag: "None", _op: "None", valueOrUndefined: void 0, [te$3](e) {
  return go$2(e) && yo$1(e);
}, [ee$4]() {
  return su$1;
}, toString() {
  return "none()";
}, toJSON() {
  return { _id: "Option", _tag: this._tag };
} }), go$2 = (e) => P$3(e, po$1), yo$1 = (e) => e._tag === "None", Eo$1 = (e) => e._tag === "Some", iu$1 = Object.create(ou$1), au$1 = (e) => {
  const t = Object.create(mo$1);
  return t.value = e, t;
}, bo$1 = "~effect/data/Result", So$2 = { [bo$1]: { _A: (e) => e, _E: (e) => e }, ...Pt$2, [Symbol.iterator]() {
  return new Rt$2(this);
} }, cu$1 = Object.assign(Object.create(So$2), { _tag: "Success", _op: "Success", [te$3](e) {
  return Ar$2(e) && _o$3(e) && oe$4(this.success, e.success);
}, [ee$4]() {
  return de$5(D$2(this._tag))(D$2(this.success));
}, toString() {
  return `success(${L$3(this.success)})`;
}, toJSON() {
  return { _id: "Result", _tag: this._tag, value: gn$2(this.success) };
} }), uu$1 = Object.assign(Object.create(So$2), { _tag: "Failure", _op: "Failure", [te$3](e) {
  return Ar$2(e) && To$1(e) && oe$4(this.failure, e.failure);
}, [ee$4]() {
  return de$5(D$2(this._tag))(D$2(this.failure));
}, toString() {
  return `failure(${L$3(this.failure)})`;
}, toJSON() {
  return { _id: "Result", _tag: this._tag, failure: gn$2(this.failure) };
} }), Ar$2 = (e) => P$3(e, bo$1), To$1 = (e) => e._tag === "Failure", _o$3 = (e) => e._tag === "Success", lu$1 = (e) => {
  const t = Object.create(uu$1);
  return t.failure = e, t;
}, fu$1 = (e) => {
  const t = Object.create(cu$1);
  return t.success = e, t;
};
function du$1(e) {
  return (t, n) => t === n ? 0 : e(t, n);
}
const pu$1 = du$1((e, t) => globalThis.Number.isNaN(e) && globalThis.Number.isNaN(t) ? 0 : globalThis.Number.isNaN(e) ? -1 : globalThis.Number.isNaN(t) ? 1 : e < t ? -1 : 1), hu$1 = (e) => d$3(2, (t, n) => e(t, n) !== -1), at$1 = () => iu$1, C$5 = au$1, Lt$2 = yo$1, gs$2 = Eo$1, mu$1 = d$3(2, (e, { onNone: t, onSome: n }) => Lt$2(e) ? t() : n(e.value)), gu$1 = (e) => (...t) => {
  try {
    return C$5(e(...t));
  } catch {
    return at$1();
  }
}, xo$1 = d$3(2, (e, t) => Lt$2(e) ? at$1() : C$5(t(e.value))), ys$2 = "~effect/Context/Service", wo$1 = function() {
  var _a3;
  const e = Error.stackTraceLimit;
  Error.stackTraceLimit = 2;
  const t = new Error();
  Error.stackTraceLimit = e;
  function n() {
  }
  const r = n;
  return Object.setPrototypeOf(r, yu$1), Object.defineProperty(r, "stack", { get() {
    return t.stack;
  } }), arguments.length > 0 ? (r.key = arguments[0], ((_a3 = arguments[1]) == null ? void 0 : _a3.defaultValue) && (r[sr$1] = sr$1, r.defaultValue = arguments[1].defaultValue), r) : function(s, o) {
    return r.key = s, (o == null ? void 0 : o.make) && (r.make = o.make), r;
  };
}, yu$1 = { [ys$2]: ys$2, ...nu$1({ label: "Service", evaluate(e) {
  return I$3(qt$2(e.context, this));
} }), toJSON() {
  return { _id: "Service", key: this.key, stack: this.stack };
}, of(e) {
  return e;
}, context(e) {
  return Ao$1(this, e);
}, use(e) {
  return Y$4((t) => e(qt$2(t.context, this)));
}, useSync(e) {
  return Y$4((t) => I$3(e(qt$2(t.context, this))));
} }, sr$1 = "~effect/Context/Reference", Oo$1 = "~effect/Context", bn$2 = (e) => {
  const t = Object.create(Eu$1);
  return t.mapUnsafe = e, t.mutable = false, t;
}, Eu$1 = { ...Pt$2, [Oo$1]: { _Services: (e) => e }, toJSON() {
  return { _id: "Context", services: Array.from(this.mapUnsafe).map(([e, t]) => ({ key: e, value: t })) };
}, [te$3](e) {
  if (!bu$1(e) || this.mapUnsafe.size !== e.mapUnsafe.size) return false;
  for (const t of this.mapUnsafe.keys()) if (!e.mapUnsafe.has(t) || !oe$4(this.mapUnsafe.get(t), e.mapUnsafe.get(t))) return false;
  return true;
}, [ee$4]() {
  return hn$2(this.mapUnsafe.size);
} }, bu$1 = (e) => P$3(e, Oo$1), ct$2 = () => Su$1, Su$1 = bn$2(/* @__PURE__ */ new Map()), Ao$1 = (e, t) => bn$2(/* @__PURE__ */ new Map([[e.key, t]])), ko$1 = d$3(3, (e, t, n) => Co$1(e, (r) => {
  r.set(t.key, n);
})), Tu$1 = d$3(2, (e, t) => {
  if (!e.mapUnsafe.has(t.key)) {
    if (sr$1 in t) return Io$1(t);
    throw xu$1(t);
  }
  return e.mapUnsafe.get(t.key);
}), qt$2 = Tu$1, _u$3 = (e, t) => e.mapUnsafe.has(t.key) ? e.mapUnsafe.get(t.key) : Io$1(t), qn$2 = "~effect/Context/defaultValue", Io$1 = (e) => qn$2 in e ? e[qn$2] : e[qn$2] = e.defaultValue(), xu$1 = (e) => {
  const t = new Error(`Service not found${e.key ? `: ${String(e.key)}` : ""}`);
  if (e.stack) {
    const n = e.stack.split(`
`);
    if (n.length > 2) {
      const r = n[2].match(/at (.*)/);
      r && (t.message = t.message + ` (defined at ${r[1]})`);
    }
  }
  if (t.stack) {
    const n = t.stack.split(`
`);
    n.splice(1, 3), t.stack = n.join(`
`);
  }
  return t;
}, vo$1 = d$3(2, (e, t) => e.mapUnsafe.size === 0 ? t : t.mapUnsafe.size === 0 ? e : Co$1(e, (n) => {
  t.mapUnsafe.forEach((r, s) => n.set(s, r));
})), Co$1 = (e, t) => {
  if (e.mutable) return t(e.mapUnsafe), e;
  const n = new Map(e.mapUnsafe);
  return t(n), bn$2(n);
}, ut$2 = wo$1, $o$1 = (e) => e.length > 0, lt$2 = fu$1, we$5 = lu$1, wu$1 = Ar$2, X$4 = To$1, jt$2 = _o$3, Ou$1 = d$3(2, (e, { onFailure: t, onSuccess: n }) => X$4(e) ? t(e.failure) : n(e.success)), Au$1 = d$3(2, (e, t) => {
  if (jt$2(e)) return e.success;
  throw t(e.failure);
}), vm$1 = Au$1(B$2), Es$2 = globalThis.Array, Oe$3 = (e) => Es$2.isArray(e) ? e : Es$2.from(e), Do$1 = d$3(2, (e, t) => [...e, t]), ku$1 = d$3(2, (e, t) => Oe$3(e).concat(Oe$3(t))), bs$2 = $o$1, vt$2 = $o$1;
function Iu$1(e, t) {
  return e < 0 || e >= t.length;
}
const vu$1 = d$3(2, (e, t) => {
  const n = Math.floor(t);
  if (Iu$1(n, e)) throw new Error(`Index out of bounds: ${n}`);
  return e[n];
}), Cu$1 = vu$1(0), $u$1 = (e) => e.slice(1), Du$1 = d$3(3, (e, t, n) => {
  const r = Oe$3(e), s = Oe$3(t);
  return vt$2(r) ? vt$2(s) ? Ru$1(n)(ku$1(r, s)) : r : s;
}), Uu$1 = d$3(2, (e, t) => Du$1(e, t, Nc$1())), Fu$1 = () => [], Nu$1 = d$3(2, (e, t) => e.map(t)), Ru$1 = d$3(2, (e, t) => {
  const n = Oe$3(e);
  if (vt$2(n)) {
    const r = [Cu$1(n)], s = $u$1(n);
    for (const o of s) r.every((i) => !t(o, i)) && r.push(o);
    return r;
  }
  return [];
}), Uo$1 = d$3(2, (e, t) => (n) => {
  const r = e(n);
  if (X$4(r)) return we$5(n);
  const s = t(r.success);
  return X$4(s) ? we$5(n) : s;
}), Fo$1 = ut$2("effect/Scheduler", { defaultValue: () => new kr$2() }), No$1 = "setImmediate" in globalThis ? (e) => {
  const t = globalThis.setImmediate(e);
  return () => globalThis.clearImmediate(t);
} : (e) => {
  const t = setTimeout(e, 0);
  return () => clearTimeout(t);
};
let Pu$1 = class Pu {
  constructor() {
    __publicField$6(this, "buckets", []);
  }
  scheduleTask(t, n) {
    const r = this.buckets, s = r.length;
    let o, i = 0;
    for (; i < s && !(r[i][0] > n); i++) o = r[i];
    o && o[0] === n ? o[1].push(t) : i === s ? r.push([n, [t]]) : r.splice(i, 0, [n, [t]]);
  }
  drain() {
    const t = this.buckets;
    return this.buckets = [], t;
  }
};
let kr$2 = class kr {
  constructor(t = "async", n = No$1) {
    __publicField$6(this, "executionMode");
    __publicField$6(this, "setImmediate");
    this.executionMode = t, this.setImmediate = n;
  }
  shouldYield(t) {
    return t.currentOpCount >= t.maxOpsBeforeYield;
  }
  makeDispatcher() {
    return new Lu$1(this.setImmediate);
  }
};
let Lu$1 = class Lu {
  constructor(t = No$1) {
    __publicField$6(this, "tasks", new Pu$1());
    __publicField$6(this, "running");
    __publicField$6(this, "setImmediate");
    __publicField$6(this, "afterScheduled", () => {
      this.running = void 0, this.runTasks();
    });
    this.setImmediate = t;
  }
  scheduleTask(t, n) {
    this.tasks.scheduleTask(t, n), this.running === void 0 && (this.running = this.setImmediate(this.afterScheduled));
  }
  runTasks() {
    const t = this.tasks.drain();
    for (let n = 0; n < t.length; n++) {
      const r = t[n][1];
      for (let s = 0; s < r.length; s++) r[s]();
    }
  }
  flush() {
    for (; this.tasks.buckets.length > 0; ) this.running !== void 0 && (this.running(), this.running = void 0), this.runTasks();
  }
};
const ju$1 = ut$2("effect/Scheduler/MaxOpsBeforeYield", { defaultValue: () => 2048 }), Mu$1 = ut$2("effect/Scheduler/PreventSchedulerYield", { defaultValue: () => false }), zu$1 = "effect/Tracer/ParentSpan", Vu$1 = "effect/Tracer", Ju$1 = "effect/observability/Metric/FiberRuntimeMetricsKey", Ku$1 = ut$2("effect/References/CurrentStackFrame", { defaultValue: Gs$2 }), Bu$1 = ut$2("effect/References/CurrentLogLevel", { defaultValue: () => "Info" }), Yu$1 = ut$2("effect/References/MinimumLogLevel", { defaultValue: () => "Info" }), Wu$1 = "dev";
let Gu$1 = class Gu extends xr$2 {
  constructor(t, n = yn$2) {
    super("Interrupt", n, "Interrupted");
    __publicField$6(this, "fiberId");
    this.fiberId = t;
  }
  toString() {
    return `Interrupt(${this.fiberId})`;
  }
  toJSON() {
    return { _tag: "Interrupt", fiberId: this.fiberId };
  }
  [te$3](t) {
    return uo$2(t) && this.fiberId === t.fiberId && this.annotations === t.annotations;
  }
  [ee$4]() {
    return de$5(R$5(`${this._tag}:${this.fiberId}`))(tr$2(this.annotations));
  }
};
const Ro$1 = (e) => new it$2([new Gu$1(e)]), Mt$2 = (e) => {
  for (let t = 0; t < e.reasons.length; t++) {
    const n = e.reasons[t];
    if (n._tag === "Fail") return lt$2(n.error);
  }
  return we$5(e);
}, qu$1 = (e) => e.reasons.some(uo$2), Hu$1 = d$3(2, (e, t) => {
  if (e.reasons.length === 0) return t;
  if (t.reasons.length === 0) return e;
  const n = new it$2(Uu$1(e.reasons, t.reasons));
  return oe$4(e, n) ? e : n;
}), Zu$1 = (e) => {
  const t = { Fail: [], Die: [], Interrupt: [] };
  for (let n = 0; n < e.reasons.length; n++) t[e.reasons[n]._tag].push(e.reasons[n]);
  return t;
}, Po$1 = (e) => {
  const t = Zu$1(e);
  return t.Fail.length > 0 ? t.Fail[0].error : t.Die.length > 0 ? t.Die[0].defect : t.Interrupt.length > 0 ? new globalThis.Error("All fibers interrupted without error") : new globalThis.Error("Empty cause");
}, Qu$1 = (e) => {
  const t = [], n = [];
  if (e.reasons.length === 0) return t;
  const r = Error.stackTraceLimit;
  Error.stackTraceLimit = 1;
  for (const s of e.reasons) {
    if (s._tag === "Interrupt") {
      n.push(s);
      continue;
    }
    t.push(or$1(s._tag === "Die" ? s.defect : s.error, s.annotations));
  }
  if (t.length === 0) {
    const s = new Error("The fiber was interrupted by:");
    s.name = "InterruptCause", s.stack = nl$1(s, n);
    const o = new globalThis.Error("All fibers interrupted without error", { cause: s });
    o.name = "InterruptError", o.stack = `${o.name}: ${o.message}`, t.push(or$1(o, n[0].annotations));
  }
  return Error.stackTraceLimit = r, t;
}, or$1 = (e, t) => {
  const n = typeof e;
  let r;
  if (e && n === "object") {
    if (r = new globalThis.Error(Xu$1(e), { cause: e.cause ? or$1(e.cause) : void 0 }), typeof e.name == "string" && (r.name = e.name), typeof e.stack == "string") r.stack = tl$1(e.stack, r, t);
    else {
      const s = `${r.name}: ${r.message}`;
      r.stack = t ? Lo$1(s, t) : s;
    }
    for (const s of Object.keys(e)) s in r || (r[s] = e[s]);
  } else r = new globalThis.Error(e ? n === "string" ? e : no$2(e) : `Unknown error: ${e}`);
  return r;
}, Xu$1 = (e) => {
  if (typeof e.message == "string") return e.message;
  if (typeof e.toString == "function" && e.toString !== Object.prototype.toString && e.toString !== Array.prototype.toString) try {
    return e.toString();
  } catch {
  }
  return no$2(e);
}, el$1 = /\((.*)\)/g, tl$1 = (e, t, n) => {
  const r = `${t.name}: ${t.message}`, s = (e.startsWith(r) ? e.slice(r.length) : e).split(`
`), o = [r];
  for (let i = 1; i < s.length && !/(?:Generator\.next|~effect\/Effect)/.test(s[i]); i++) o.push(s[i]);
  return n ? Lo$1(o.join(`
`), n) : o.join(`
`);
}, Lo$1 = (e, t) => {
  const n = t == null ? void 0 : t.get(En$2.key);
  return n && (e = `${e}
${jo$1(n)}`), e;
}, nl$1 = (e, t) => {
  const n = [`${e.name}: ${e.message}`];
  for (const r of t) {
    const s = r.fiberId !== void 0 ? `#${r.fiberId}` : "unknown", o = r.annotations.get(Hc$1.key);
    n.push(`    at fiber (${s})`), o && n.push(jo$1(o));
  }
  return n.join(`
`);
}, jo$1 = (e) => {
  const t = [];
  let n = e, r = 0;
  for (; n && r < 10; ) {
    const s = n.stack();
    if (s) {
      const o = s.matchAll(el$1);
      let i = false;
      for (const [, u] of o) i = true, t.push(`    at ${n.name} (${u})`);
      i || t.push(`    at ${n.name} (${s.replace(/^at /, "")})`);
    } else t.push(`    at ${n.name}`);
    n = n.parent, r++;
  }
  return t.join(`
`);
}, rl$1 = (e) => Qu$1(e).map((t) => t.cause ? `${t.stack} {
${Mo$1(t.cause, "  ")}
}` : t.stack).join(`
`), Mo$1 = (e, t) => {
  const n = e.stack.split(`
`);
  let r = `${t}[cause]: ${n[0]}`;
  for (let s = 1, o = n.length; s < o; s++) r += `
${t}${n[s]}`;
  return e.cause && (r += ` {
${Mo$1(e.cause, `${t}  `)}
${t}}`), r;
}, ir$1 = `~effect/Fiber/${Wu$1}`, sl$1 = { _A: B$2, _E: B$2 }, ol$1 = { id: 0 }, en$2 = () => globalThis[Tt$2];
_d2$1 = ir$1;
let zo$1 = class zo {
  constructor(t, n = true) {
    __publicField$6(this, _d2$1);
    __publicField$6(this, "id");
    __publicField$6(this, "interruptible");
    __publicField$6(this, "currentOpCount");
    __publicField$6(this, "currentLoopCount");
    __publicField$6(this, "_stack");
    __publicField$6(this, "_observers");
    __publicField$6(this, "_exit");
    __publicField$6(this, "_currentExit");
    __publicField$6(this, "_children");
    __publicField$6(this, "_interruptedCause");
    __publicField$6(this, "_yielded");
    __publicField$6(this, "context");
    __publicField$6(this, "currentScheduler");
    __publicField$6(this, "currentTracerContext");
    __publicField$6(this, "currentSpan");
    __publicField$6(this, "currentLogLevel");
    __publicField$6(this, "minimumLogLevel");
    __publicField$6(this, "currentStackFrame");
    __publicField$6(this, "runtimeMetrics");
    __publicField$6(this, "maxOpsBeforeYield");
    __publicField$6(this, "currentPreventYield");
    __publicField$6(this, "_dispatcher");
    var _a3;
    this[ir$1] = sl$1, this.setContext(t), this.id = ++ol$1.id, this.currentOpCount = 0, this.currentLoopCount = 0, this.interruptible = n, this._stack = [], this._observers = [], this._exit = void 0, this._children = void 0, this._interruptedCause = void 0, this._yielded = void 0, (_a3 = this.runtimeMetrics) == null ? void 0 : _a3.recordFiberStart(this.context);
  }
  get currentDispatcher() {
    var _a3;
    return (_a3 = this._dispatcher) != null ? _a3 : this._dispatcher = this.currentScheduler.makeDispatcher();
  }
  getRef(t) {
    return _u$3(this.context, t);
  }
  addObserver(t) {
    return this._exit ? (t(this._exit), qs$2) : (this._observers.push(t), () => {
      const n = this._observers.indexOf(t);
      n >= 0 && this._observers.splice(n, 1);
    });
  }
  interruptUnsafe(t, n) {
    if (this._exit) return;
    let r = Ro$1(t);
    this.currentStackFrame && (r = rr$2(r, Ao$1(En$2, this.currentStackFrame))), n && (r = rr$2(r, n)), this._interruptedCause = this._interruptedCause ? Hu$1(this._interruptedCause, r) : r, this.interruptible && this.evaluate(Ce$5(this._interruptedCause));
  }
  pollUnsafe() {
    return this._exit;
  }
  evaluate(t) {
    var _a3;
    if (this._exit) return;
    if (this._yielded !== void 0) {
      const r = this._yielded;
      this._yielded = void 0, r();
    }
    const n = this.runLoop(t);
    if (n !== Ne$3) {
      this._exit = n, (_a3 = this.runtimeMetrics) == null ? void 0 : _a3.recordFiberEnd(this.context, this._exit);
      for (let r = 0; r < this._observers.length; r++) this._observers[r](n);
      this._observers.length = 0;
    }
  }
  runLoop(t) {
    const n = globalThis[Tt$2];
    globalThis[Tt$2] = this;
    let r = false, s = t;
    this.currentOpCount = 0;
    const o = ++this.currentLoopCount;
    try {
      for (; ; ) {
        if (this.currentOpCount++, !r && !this.currentPreventYield && this.currentScheduler.shouldYield(this)) {
          r = true;
          const i = s;
          s = W$2(Ko$1, () => i);
        }
        if (s = this.currentTracerContext ? this.currentTracerContext(s, this) : s[U$2](this), o !== this.currentLoopCount) return Ne$3;
        if (s === Ne$3) {
          const i = this._yielded;
          return At$2 in i ? (this._yielded = void 0, i) : Ne$3;
        }
      }
    } catch (i) {
      return P$3(s, U$2) ? this.runLoop(kt$2(i)) : kt$2(`Fiber.runLoop: Not a valid effect: ${String(s)}`);
    } finally {
      globalThis[Tt$2] = n;
    }
  }
  getCont(t) {
    for (; ; ) {
      const n = this._stack.pop();
      if (!n) return;
      const r = n[Le$2] && n[Le$2](this);
      if (r) return r[t] = r, r;
      if (n[t]) return n;
    }
  }
  yieldWith(t) {
    return this._yielded = t, Ne$3;
  }
  children() {
    var _a3;
    return (_a3 = this._children) != null ? _a3 : this._children = /* @__PURE__ */ new Set();
  }
  pipe() {
    return ke$3(this, arguments);
  }
  setContext(t) {
    this.context = t;
    const n = this.getRef(Fo$1);
    n !== this.currentScheduler && (this.currentScheduler = n, this._dispatcher = void 0), this.currentSpan = t.mapUnsafe.get(zu$1), this.currentLogLevel = this.getRef(Bu$1), this.minimumLogLevel = this.getRef(Yu$1), this.currentStackFrame = t.mapUnsafe.get(Ku$1.key), this.maxOpsBeforeYield = this.getRef(ju$1), this.currentPreventYield = this.getRef(Mu$1), this.runtimeMetrics = t.mapUnsafe.get(Ju$1);
    const r = t.mapUnsafe.get(Vu$1);
    this.currentTracerContext = r ? r.context : void 0;
  }
  get currentSpanLocal() {
    var _a3;
    return ((_a3 = this.currentSpan) == null ? void 0 : _a3._tag) === "Span" ? this.currentSpan : void 0;
  }
};
const Sn$2 = (e) => {
  if (!e.currentStackFrame) return;
  const t = /* @__PURE__ */ new Map();
  return t.set(En$2.key, e.currentStackFrame), bn$2(t);
}, il$1 = (e) => {
  const t = e;
  return t._exit ? w$3(t._exit) : Fe$2((n) => t._exit ? n(w$3(t._exit)) : Z$4(e.addObserver((r) => n(w$3(r)))));
}, Vo$1 = (e) => Fe$2((t) => {
  const n = e[Symbol.iterator](), r = [];
  let s;
  function o() {
    let i = n.next();
    for (; !i.done; ) {
      if (i.value._exit) {
        r.push(i.value._exit), i = n.next();
        continue;
      }
      s = i.value.addObserver((u) => {
        r.push(u), o();
      });
      return;
    }
    t(w$3(r));
  }
  return o(), Z$4(() => s == null ? void 0 : s());
}), al$1 = (e) => {
  const t = e;
  return t._exit ? t._exit : Fe$2((n) => t._exit ? n(t._exit) : Z$4(e.addObserver(n)));
}, Jo$1 = (e) => Y$4((t) => cl$1(e, t.id)), cl$1 = d$3((e) => P$3(e[0], ir$1), (e, t, n) => Y$4((r) => {
  let s = Sn$2(r);
  return s = s && n ? vo$1(s, n) : s != null ? s : n, e.interruptUnsafe(t, s), Cr$2(il$1(e));
})), ul$1 = (e) => Y$4((t) => {
  const n = Sn$2(t);
  for (const r of e) r.interruptUnsafe(t.id, n);
  return Cr$2(Vo$1(e));
}), w$3 = I$3, Ce$5 = ie$4, et$2 = Ve$2, Z$4 = he$3({ op: "Sync", [U$2](e) {
  const t = this[g$2](), n = e.getCont(Q$4);
  return n ? n[Q$4](t, e) : e.yieldWith(I$3(t));
} }), M$3 = he$3({ op: "Suspend", [U$2](e) {
  return this[g$2]();
} }), ll$1 = Ou$1({ onFailure: et$2, onSuccess: w$3 }), fl$1 = he$3({ op: "Yield", [U$2](e) {
  var _a3;
  let t = false;
  return e.currentDispatcher.scheduleTask(() => {
    t || e.evaluate(ye$5);
  }, (_a3 = this[g$2]) != null ? _a3 : 0), e.yieldWith(() => {
    t = true;
  });
} }), Ko$1 = fl$1(0), dl$1 = (e) => w$3(C$5(e)), Bo$1 = w$3(at$1()), Yo$1 = (e) => kt$2(e), pl$1 = (e) => M$3(() => et$2(se$4(e))), le$5 = w$3(void 0), hl$1 = (e) => M$3(() => {
  try {
    return w$3(se$4(e.try));
  } catch (t) {
    return et$2(se$4(() => e.catch(t)));
  }
}), ml$1 = (e) => Ir$2(function(t, n) {
  se$4(() => e(n)).then((r) => t(w$3(r)), (r) => t(Yo$1(r)));
}, e.length !== 0), gl$1 = (e) => {
  const t = typeof e == "function" ? e : e.try, n = typeof e == "function" ? (r) => new bf$1(r, "An error occurred in Effect.tryPromise") : e.catch;
  return Ir$2(function(r, s) {
    try {
      se$4(() => t(s)).then((o) => r(w$3(o)), (o) => r(et$2(se$4(() => n(o)))));
    } catch (o) {
      r(et$2(se$4(() => n(o))));
    }
  }, eval.length !== 0);
}, yl$1 = (e) => Y$4((t) => e(t.id)), Ir$2 = he$3({ op: "Async", single: false, [U$2](e) {
  const t = se$4(() => this[g$2][0].bind(e.currentScheduler));
  let n = false, r = false;
  const s = this[g$2][1] ? new AbortController() : void 0, o = t((i) => {
    n || (n = true, r ? e.evaluate(i) : r = i);
  }, s == null ? void 0 : s.signal);
  return r !== false ? r : (r = true, e._yielded = () => {
    n = true;
  }, s === void 0 && o === void 0 || e._stack.push(El$1(() => (n = true, s == null ? void 0 : s.abort(), o != null ? o : ye$5))), Ne$3);
} }), El$1 = he$3({ op: "AsyncFinalizer", [Le$2](e) {
  e.interruptible && (e.interruptible = false, e._stack.push(Rr$1));
}, [ge$5](e, t) {
  return qu$1(e) ? W$2(this[g$2](), () => Ce$5(e)) : Ce$5(e);
} }), Fe$2 = (e) => Ir$2(e, e.length >= 2), bl$1 = (...e) => M$3(() => Ct$2(e.length === 1 ? e[0]() : e[1].call(e[0].self))), Wo$1 = (e, ...t) => {
  const n = t.length === 0 ? function() {
    return M$3(() => Ct$2(e.apply(this, arguments)));
  } : function() {
    let r = M$3(() => Ct$2(e.apply(this, arguments)));
    for (let s = 0; s < t.length; s++) r = t[s](r, ...arguments);
    return r;
  };
  return Go$1(e.length, n);
}, Go$1 = (e, t) => Object.defineProperty(t, "length", { value: e, configurable: true }), Sl$1 = (e, ...t) => Go$1(e.length, t.length === 0 ? function() {
  return Ss$2(() => e.apply(this, arguments));
} : function() {
  let n = Ss$2(() => e.apply(this, arguments));
  for (const r of t) n = r(n);
  return n;
}), Ss$2 = (e) => {
  try {
    const t = e();
    let n;
    for (; ; ) {
      const r = t.next(n);
      if (r.done) return w$3(r.value);
      const s = r.value;
      if (s && s._tag === "Success") {
        n = s.value;
        continue;
      } else {
        if (s && s._tag === "Failure") return r.value;
        {
          let o = true;
          return M$3(() => o ? (o = false, W$2(r.value, (i) => Ct$2(t, i))) : M$3(() => Ct$2(e())));
        }
      }
    }
  } catch (t) {
    return Yo$1(t);
  }
}, Ct$2 = he$3({ op: "Iterator", single: false, [Q$4](e, t) {
  const n = this[g$2][0];
  for (; ; ) {
    const r = n.next(e);
    if (r.done) return w$3(r.value);
    if (ae$4(r.value)) {
      if (r.value._tag === "Failure") return r.value;
    } else return t._stack.push(this), r.value;
    e = r.value.value;
  }
}, [U$2](e) {
  return this[Q$4](this[g$2][1], e);
} }), Tl$1 = d$3(2, (e, t) => {
  const n = w$3(t);
  return W$2(e, (r) => n);
}), _l$3 = (e) => $r$2(e, C$5), vr$1 = d$3(2, (e, t) => W$2(e, (n) => _r$4(t) ? t : se$4(() => t(n)))), Cr$2 = (e) => W$2(e, (t) => ye$5), W$2 = d$3(2, (e, t) => {
  const n = Object.create(xl$1);
  return n[g$2] = e, n[Q$4] = t.length !== 1 ? (r) => t(r) : t, n;
}), xl$1 = ze$2({ op: "OnSuccess", [U$2](e) {
  return e._stack.push(this), this[g$2];
} }), ae$4 = (e) => At$2 in e, wl$1 = d$3(2, (e, t) => ae$4(e) ? e._tag === "Success" ? t(e.value) : e : W$2(e, t)), Ol$1 = (e) => W$2(e, B$2), $r$2 = d$3(2, (e, t) => W$2(e, (n) => w$3(se$4(() => t(n))))), Al$1 = d$3(2, (e, t) => ae$4(e) ? $l$1(e, t) : $r$2(e, t)), kl$1 = d$3(2, (e, t) => ae$4(e) ? Dl$1(e, t) : Vl$1(e, t)), Il$1 = d$3(2, (e, t) => {
  if (ae$4(e)) {
    if (e._tag === "Success") return e;
    const n = Mt$2(e.cause);
    return X$4(n) ? e : t(n.success);
  }
  return Zo$1(e, t);
}), vl$1 = (e) => ie$4(Ro$1(e)), Dr$2 = (e) => e._tag === "Success", Cl$1 = (e) => e._tag === "Failure" ? lt$2(e.cause) : we$5(e), ye$5 = I$3(void 0), $l$1 = d$3(2, (e, t) => e._tag === "Success" ? I$3(t(e.value)) : e), Dl$1 = d$3(2, (e, t) => {
  if (e._tag === "Success") return e;
  const n = Mt$2(e.cause);
  return X$4(n) ? e : Ve$2(t(n.success));
}), Ul$1 = d$3(2, (e, t) => Dr$2(e) ? t : e), Fl$1 = (e) => {
  const t = [];
  for (const n of e) n._tag === "Failure" && t.push(...n.cause.reasons);
  return t.length === 0 ? ye$5 : ie$4(nr$2(t));
}, Nl$1 = (e) => Dr$2(e) ? C$5(e.value) : at$1(), Rl$1 = d$3(2, (e, t) => Y$4((n) => {
  const r = n.context, s = t(r);
  return r === s ? e : (n.setContext(s), Dt$2(e, () => {
    n.setContext(r);
  }));
})), Pl$1 = () => Ll$1, Ll$1 = Y$4((e) => w$3(e.context)), jl$1 = d$3(2, (e, t) => ae$4(e) ? e : Rl$1(e, vo$1(t))), Ml$1 = d$3((e) => _r$4(e[0]), (e, t) => of$1({ while: dc$1, body: Nt$2((t == null ? void 0 : t.disableYield) ? e : W$2(e, (n) => Ko$1)), step: qs$2 })), qo$1 = d$3(2, (e, t) => {
  const n = Object.create(zl$1);
  return n[g$2] = e, n[ge$5] = t.length !== 1 ? (r) => t(r) : t, n;
}), zl$1 = ze$2({ op: "OnFailure", [U$2](e) {
  return e._stack.push(this), this[g$2];
} }), Ho$1 = d$3(3, (e, t, n) => qo$1(e, (r) => {
  const s = t(r);
  return X$4(s) ? Ce$5(s.failure) : se$4(() => n(s.success, r));
})), Zo$1 = d$3(2, (e, t) => Ho$1(e, Mt$2, (n) => t(n))), Vl$1 = d$3(2, (e, t) => Zo$1(e, (n) => pl$1(() => t(n)))), Jl$1 = (e) => Bl$1(e, { onFailure: we$5, onSuccess: lt$2 }), Qo$1 = d$3(2, (e, t) => {
  const n = Object.create(Kl$1);
  return n[g$2] = e, n[Q$4] = t.onSuccess.length !== 1 ? (r) => t.onSuccess(r) : t.onSuccess, n[ge$5] = t.onFailure.length !== 1 ? (r) => t.onFailure(r) : t.onFailure, n;
}), Kl$1 = ze$2({ op: "OnSuccessAndFailure", [U$2](e) {
  return e._stack.push(this), this[g$2];
} }), Xo$1 = d$3(2, (e, t) => Qo$1(e, { onFailure: (n) => {
  const r = n.reasons.find(co$1);
  return r ? se$4(() => t.onFailure(r.error)) : Ce$5(n);
}, onSuccess: t.onSuccess })), ei$2 = d$3(2, (e, t) => Xo$1(e, { onFailure: (n) => Z$4(() => t.onFailure(n)), onSuccess: (n) => Z$4(() => t.onSuccess(n)) })), Bl$1 = d$3(2, (e, t) => {
  if (ae$4(e)) {
    if (e._tag === "Success") return I$3(t.onSuccess(e.value));
    const n = Mt$2(e.cause);
    return X$4(n) ? e : I$3(t.onFailure(n.success));
  }
  return ei$2(e, t);
}), Ur$2 = (e) => ae$4(e) ? I$3(e) : Yl$1(e), Yl$1 = he$3({ op: "Exit", [U$2](e) {
  return e._stack.push(this), this[g$2];
}, [Q$4](e, t, n) {
  return w$3(n != null ? n : I$3(e));
}, [ge$5](e, t, n) {
  return w$3(n != null ? n : ie$4(e));
} }), Ts$2 = "~effect/Scope", _s$4 = "~effect/Scope/Closeable", ti$2 = wo$1("effect/Scope"), ni$2 = (e, t) => M$3(() => {
  var _a3;
  return (_a3 = ri$2(e, t)) != null ? _a3 : le$5;
}), ri$2 = (e, t) => {
  if (e.state._tag === "Closed") return;
  const n = { _tag: "Closed", exit: t };
  if (e.state._tag === "Empty") {
    e.state = n;
    return;
  }
  const { finalizers: r } = e.state;
  if (e.state = n, r.size !== 0) return r.size === 1 ? r.values().next().value(t) : Wl$1(e, r, t);
}, Wl$1 = Wo$1(function* (e, t, n) {
  let r = [];
  const s = [], o = Array.from(t.values()), i = en$2();
  for (let u = o.length - 1; u >= 0; u--) {
    const c = o[u];
    e.strategy === "sequential" ? r.push(yield* Ur$2(c(n))) : s.push(Pr$2(i, c(n), true, true, "inherit"));
  }
  return s.length > 0 && (r = yield* Vo$1(s)), yield* Fl$1(r);
}), Gl$1 = (e, t) => {
  const n = Nr$2(t);
  if (e.state._tag === "Closed") return n.state = e.state, n;
  const r = {};
  return $t$2(e, r, (s) => ni$2(n, s)), $t$2(n, r, (s) => Z$4(() => Fr$1(e, r))), n;
}, ql$1 = (e, t) => M$3(() => e.state._tag === "Closed" ? t(e.state.exit) : ($t$2(e, {}, t), le$5)), Hl$1 = (e, t) => ql$1(e, Nt$2(t)), $t$2 = (e, t, n) => {
  e.state._tag === "Empty" ? e.state = { _tag: "Open", finalizers: /* @__PURE__ */ new Map([[t, n]]) } : e.state._tag === "Open" && e.state.finalizers.set(t, n);
}, Fr$1 = (e, t) => {
  e.state._tag === "Open" && e.state.finalizers.delete(t);
}, Nr$2 = (e = "sequential") => ({ [_s$4]: _s$4, [Ts$2]: Ts$2, strategy: e, state: Zl$1 }), Zl$1 = { _tag: "Empty" }, Ql$1 = (e) => Z$4(() => Nr$2(e)), Xl$1 = (e) => Y$4((t) => {
  const n = t.context, r = Nr$2();
  return t.setContext(ko$1(t.context, ti$2, r)), Dt$2(e, (s) => (t.setContext(n), ri$2(r, s)));
}), Dt$2 = he$3({ op: "OnExit", single: false, [U$2](e) {
  return e._stack.push(this), this[g$2][0];
}, [Le$2](e) {
  e.interruptible && this[g$2][2] !== true && (e._stack.push(Rr$1), e.interruptible = false);
}, [Q$4](e, t, n) {
  n != null ? n : n = I$3(e);
  const r = this[g$2][1](n);
  return r ? W$2(r, (s) => n) : n;
}, [ge$5](e, t, n) {
  n != null ? n : n = ie$4(e);
  const r = this[g$2][1](n);
  return r ? W$2(r, (s) => n) : n;
} }), ef$1 = d$3(2, Dt$2), tf$1 = d$3(3, (e, t, n) => ef$1(e, (r) => {
  const s = t(r);
  return X$4(s) ? le$5 : n(s.success, r);
})), nf$1 = d$3(2, (e, t) => tf$1(e, Cl$1, t)), si$2 = he$3({ op: "SetInterruptible", [Le$2](e) {
  if (e.interruptible = this[g$2], e._interruptedCause && e.interruptible) return () => Ce$5(e._interruptedCause);
} }), Rr$1 = si$2(true), rf$1 = si$2(false), sf$1 = (e) => Y$4((t) => t.interruptible ? e : (t.interruptible = true, t._stack.push(rf$1), t._interruptedCause ? Ce$5(t._interruptedCause) : e)), xs$2 = (e) => Y$4((t) => t.interruptible ? (t.interruptible = false, t._stack.push(Rr$1), e(sf$1)) : e(B$2)), of$1 = he$3({ op: "While", [Q$4](e, t) {
  return this[g$2].step(e), this[g$2].while() ? (t._stack.push(this), this[g$2].body()) : ye$5;
}, [U$2](e) {
  return this[g$2].while() ? (e._stack.push(this), this[g$2].body()) : ye$5;
} }), af$1 = (e) => {
  const t = e.onItem, n = e.step;
  return (r, s, o) => {
    var _a3, _b2, _c3;
    let i = (_a3 = o == null ? void 0 : o.start) != null ? _a3 : 0;
    const u = (_b2 = o == null ? void 0 : o.end) != null ? _b2 : s.length, c = (_c3 = o == null ? void 0 : o.concurrency) != null ? _c3 : 1;
    let a = false, l, f, p, E = false, m, y;
    const k = () => {
      let J = false;
      for (; !m && i < u; i++) {
        const _ = s[i], F = y != null ? y : t(r, _, i);
        if (ae$4(F)) {
          if (m = n(r, _, F, i), m) break;
        } else {
          if (c === 1) return W$2(Ur$2(F), (x) => {
            var _a4;
            return m = n(r, _, x, i), i++, (_a4 = m != null ? m : k()) != null ? _a4 : le$5;
          });
          if (l) {
            y = void 0;
            const x = Pr$2(l, F, true, true, "inherit");
            if (x._exit) {
              if (m = n(r, _, x._exit, i), m) break;
              continue;
            }
            f ? f.add(x) : f = /* @__PURE__ */ new Set([x]);
            const N = i;
            if (x.addObserver((Be) => {
              if (f.delete(x), m) {
                if (!E && Be._tag === "Failure") for (const q of Be.cause.reasons) q._tag !== "Interrupt" && (m._tag === "Failure" ? m.cause.reasons.push(q) : m = ie$4(nr$2([q])));
              } else {
                const q = n(r, _, Be, N);
                q && (m = q._tag === "Failure" ? ie$4(nr$2(q.cause.reasons.slice())) : q, k());
              }
              if (J) {
                const q = k();
                q && p(q);
              } else a && f.size === 0 && p(m != null ? m : le$5);
            }), f.size < c) continue;
            J = true, i++;
            return;
          } else return Fe$2((x) => {
            l = en$2(), y = F, p = x;
            const N = k();
            return N ? x(N) : M$3(() => (m = ye$5, E = true, f ? ul$1(f) : le$5));
          });
        }
      }
      if (a = true, m) {
        if (f && f.size > 0) {
          const _ = Sn$2(l);
          f.forEach((F) => F.interruptUnsafe(l.id, _));
          return;
        }
        if (p || m._tag === "Failure") return m;
      } else if (p) if (f) f.size === 0 && p(le$5);
      else return ye$5;
    };
    return k();
  };
}, Tn$2 = () => af$1, Pr$2 = (e, t, n = false, r = false, s = false) => {
  const o = s === "inherit" ? e.interruptible : !s, i = new zo$1(e.context, o);
  return n ? i.evaluate(t) : e.currentDispatcher.scheduleTask(() => i.evaluate(t), 0), !r && !i._exit && (e.children().add(i), i.addObserver(() => e._children.delete(i))), i;
}, cf$1 = d$3((e) => _r$4(e[0]), (e, t, n) => Y$4((r) => {
  const s = Pr$2(r, e, n == null ? void 0 : n.startImmediately, true, n == null ? void 0 : n.uninterruptible);
  if (!s._exit) if (t.state._tag !== "Closed") {
    const o = {};
    $t$2(t, o, () => yl$1((u) => u === s.id ? le$5 : Jo$1(s))), s.addObserver(() => Fr$1(t, o));
  } else s.interruptUnsafe(r.id, Sn$2(r));
  return w$3(s);
})), _n$4 = (e) => (t, n) => {
  const r = new zo$1((n == null ? void 0 : n.scheduler) ? ko$1(e, Fo$1, n.scheduler) : e, (n == null ? void 0 : n.uninterruptible) !== true);
  if (r.evaluate(t), r._exit) return r;
  if (n == null ? void 0 : n.signal) if (n.signal.aborted) r.interruptUnsafe();
  else {
    const s = () => r.interruptUnsafe();
    n.signal.addEventListener("abort", s, { once: true }), r.addObserver(() => n.signal.removeEventListener("abort", s));
  }
  return (n == null ? void 0 : n.onFiberStart) && n.onFiberStart(r), r;
}, uf$1 = d$3(2, (e, t) => {
  if (e._exit) return e;
  if (t.state._tag === "Closed") return e.interruptUnsafe(e.id), e;
  const n = {};
  return $t$2(t, n, () => Jo$1(e)), e.addObserver(() => Fr$1(t, n)), e;
}), lf$1 = _n$4(ct$2()), oi$2 = (e) => {
  const t = _n$4(e);
  return (n, r) => {
    const s = t(n, r);
    return new Promise((o) => {
      s.addObserver((i) => o(i));
    });
  };
}, ff$1 = oi$2(ct$2()), df$1 = (e) => {
  const t = oi$2(e);
  return (n, r) => t(n, r).then((s) => {
    if (s._tag === "Failure") throw Po$1(s.cause);
    return s.value;
  });
}, pf$1 = df$1(ct$2()), ii$2 = (e) => {
  const t = _n$4(e);
  return (n) => {
    var _a3, _b2;
    if (ae$4(n)) return n;
    const r = new kr$2("sync"), s = t(n, { scheduler: r });
    return (_a3 = s.currentDispatcher) == null ? void 0 : _a3.flush(), (_b2 = s._exit) != null ? _b2 : kt$2(new Ef$1(s));
  };
}, hf$1 = ii$2(ct$2()), mf$1 = (e) => {
  const t = ii$2(e);
  return (n) => {
    const r = t(n);
    if (r._tag === "Failure") throw Po$1(r.cause);
    return r.value;
  };
}, gf$1 = mf$1(ct$2()), ws$2 = "~effect/Cause/IllegalArgumentError";
let yf$1 = class yf extends (_f2$1 = wr$1("IllegalArgumentError"), _e2$1 = ws$2, _f2$1) {
  constructor(t) {
    super({ message: t });
    __publicField$6(this, _e2$1, ws$2);
  }
};
const Os$2 = "~effect/Cause/AsyncFiberError";
let Ef$1 = class Ef extends (_h2$1 = wr$1("AsyncFiberError"), _g$1 = Os$2, _h2$1) {
  constructor(t) {
    super({ message: "An asynchronous Effect was executed with Effect.runSync", fiber: t });
    __publicField$6(this, _g$1, Os$2);
  }
};
const As$2 = "~effect/Cause/UnknownError";
let bf$1 = class bf extends (_j$1 = wr$1("UnknownError"), _i2$1 = As$2, _j$1) {
  constructor(t, n) {
    super({ message: n, cause: t });
    __publicField$6(this, _i2$1, As$2);
  }
};
const Sf$1 = typeof process == "object" && process !== null && typeof process.stdout == "object" && process.stdout !== null;
Sf$1 && process.stdout.isTTY;
const Ht$2 = I$3, Lr$2 = ie$4, xn$2 = Ve$2, ai$2 = ye$5, wn$2 = Dr$2, Tf$1 = Nl$1, _f$1 = ti$2, xf$1 = Ql$1, ks$2 = Hl$1, ci$2 = Gl$1, jr$2 = ni$2, wf$1 = ao$2, On$2 = Mt$2, Of$1 = rl$1, Mr$2 = Xc$1, ui$2 = Or$2, zt$2 = tu$1, Af$1 = yf$1, ar$2 = "~effect/time/DateTime", kf$1 = { [ar$2]: ar$2, pipe() {
  return ke$3(this, arguments);
}, [mn$2]() {
  return this.toString();
}, toJSON() {
  return Jr$2(this).toJSON();
} }, If$1 = { ...kf$1, _tag: "Utc", [ee$4]() {
  return hn$2(this.epochMilliseconds);
}, [te$3](e) {
  return zr$2(e) && e._tag === "Utc" && this.epochMilliseconds === e.epochMilliseconds;
}, toString() {
  return `DateTime.Utc(${Jr$2(this).toJSON()})`;
} }, zr$2 = (e) => P$3(e, ar$2), vf$1 = (e) => e._tag === "Utc", Cf$1 = ru$1((e, t) => e.epochMilliseconds === t.epochMilliseconds), Vr$2 = (e) => {
  const t = Object.create(If$1);
  return t.epochMilliseconds = e, Object.defineProperty(t, "partsUtc", { value: void 0, enumerable: false, writable: true }), t;
}, mt$2 = (e) => {
  const t = e.getTime();
  if (Number.isNaN(t)) throw new Af$1("Invalid date");
  return Vr$2(t);
}, $f$1 = (e) => {
  if (zr$2(e)) return e;
  if (e instanceof Date) return mt$2(e);
  if (typeof e == "object") {
    if ("epochMilliseconds" in e) return Vr$2(e.epochMilliseconds);
    const t = /* @__PURE__ */ new Date(0);
    return Nf$1(t, e), mt$2(t);
  } else if (typeof e == "string" && !Df$1(e)) return mt$2(/* @__PURE__ */ new Date(e + "Z"));
  return mt$2(new Date(e));
}, Df$1 = (e) => /Z|GMT|[+-]\d{2}$|[+-]\d{2}:?\d{2}$|\]$/.test(e), Uf$1 = gu$1($f$1), Ff$1 = (e) => Vr$2(e.epochMilliseconds), Jr$2 = (e) => new Date(e.epochMilliseconds), Nf$1 = (e, t) => {
  if (t.year !== void 0 && e.setUTCFullYear(t.year), t.month !== void 0 && e.setUTCMonth(t.month - 1), t.day !== void 0 && e.setUTCDate(t.day), t.weekDay !== void 0) {
    const n = t.weekDay - e.getUTCDay();
    e.setUTCDate(e.getUTCDate() + n);
  }
  t.hour !== void 0 && e.setUTCHours(t.hour), t.minute !== void 0 && e.setUTCMinutes(t.minute), t.second !== void 0 && e.setUTCSeconds(t.second), t.millisecond !== void 0 && e.setUTCMilliseconds(t.millisecond);
}, Rf$1 = (e) => Jr$2(e).toISOString(), li$2 = d$3(2, (e, t) => Ho$1(e, An$2, (n) => t(n))), Is$2 = (e) => e.reasons.some(Pf$1), Pf$1 = (e) => e._tag === "Fail" && Mr$2(e.error), Lf$1 = Uo$1(On$2, (e) => Mr$2(e) ? lt$2(e) : we$5(e)), An$2 = Uo$1(On$2, (e) => Mr$2(e) ? lt$2(e.value) : we$5(e)), jf$1 = (e) => {
  const t = Lf$1(e);
  return X$4(t) ? Lr$2(t.failure) : Ht$2(t.success.value);
}, Dm$1 = ml$1, Um$1 = gl$1, $$3 = w$3, kn$2 = Bo$1, Mf$1 = dl$1, fi$2 = M$3, Kr$2 = Z$4, Vt$2 = le$5, ne$4 = bl$1, j$2 = et$2, Jt$2 = Ce$5, Fm$1 = hl$1, Nm$1 = ll$1, K$2 = W$2, zf$1 = Ol$1, Vf$1 = vr$1, di$2 = Jl$1, tn$2 = Ur$2, Kt$2 = $r$2, Jf$1 = Tl$1, Ye$2 = Cr$2, _t$4 = qo$1, Kf$1 = ei$2, Bf$1 = Qo$1, pi$2 = Xo$1, hi$2 = Pl$1, gt$2 = jl$1, mi$2 = Xl$1, vs$2 = nf$1, Hn$2 = Ml$1, xt$2 = cf$1, Yf$1 = lf$1, Wf$1 = _n$4, xe$5 = pf$1, Gf$1 = ff$1, gi$2 = gf$1, qf$1 = hf$1, Br$2 = Wo$1, Yr$2 = Al$1, In$2 = kl$1, We$2 = wl$1, Hf$1 = Il$1, cr$1 = Sl$1, yi$2 = "Data", Ei$2 = "RecoverableError", bi$2 = "Progress", tt$2 = (e) => ({ _tag: yi$2, value: e }), Si$2 = (e) => ({ _tag: Ei$2, error: e }), Ti$2 = (e) => ({ _tag: bi$2, progress: e }), Bt$2 = (e) => e._tag === yi$2, vn$2 = (e) => e._tag === Ei$2, _i$4 = (e) => e._tag === bi$2, Zf$1 = Object.freeze(Object.defineProperty({ __proto__: null, data: tt$2, isData: Bt$2, isProgress: _i$4, isRecoverableError: vn$2, progress: Ti$2, recoverableError: Si$2 }, Symbol.toStringTag, { value: "Module" })), Qf$1 = al$1, Xf$1 = uf$1, ed$1 = /* @__PURE__ */ Symbol.for("effect/MutableList/Empty"), td$1 = () => ({ head: void 0, tail: void 0, length: 0 }), Cs$2 = () => ({ array: [], mutable: true, offset: 0, next: void 0 }), He$5 = (e, t) => {
  e.tail ? e.tail.mutable || (e.tail.next = Cs$2(), e.tail = e.tail.next) : e.head = e.tail = Cs$2(), e.tail.array.push(t), e.length++;
}, nd$1 = (e, t) => rd$1(e, Oe$3(t), !Array.isArray(t)), rd$1 = (e, t, n = false) => {
  if (t.length === 0) return 0;
  const r = { array: t, mutable: n, offset: 0, next: void 0 };
  return e.head ? e.tail = e.tail.next = r : e.head = e.tail = r, e.length += t.length, t.length;
}, wt$2 = (e) => {
  e.head = e.tail = void 0, e.length = 0;
}, xi$2 = (e, t) => {
  var _a3;
  if (t <= 0 || !e.head) return [];
  if (t = Math.min(t, e.length), t === e.length && ((_a3 = e.head) == null ? void 0 : _a3.offset) === 0 && !e.head.next) {
    const o = e.head.array;
    return wt$2(e), o;
  }
  const n = new Array(t);
  let r = 0, s = e.head;
  for (; s; ) {
    for (; s.offset < s.array.length; ) if (n[r++] = s.array[s.offset], s.mutable && (s.array[s.offset] = void 0), s.offset++, r === t) return e.head = s, e.length -= t, e.length === 0 && wt$2(e), n;
    s = s.next;
  }
  return wt$2(e), n;
}, nn$2 = (e) => {
  if (!e.head) return ed$1;
  const t = e.head.array[e.head.offset];
  return e.head.mutable && (e.head.array[e.head.offset] = void 0), e.head.offset++, e.length--, e.head.offset === e.head.array.length && (e.head.next ? e.head = e.head.next : wt$2(e)), t;
}, sd$1 = "~effect/Queue", od$1 = "~effect/Queue/Enqueue", id$1 = "~effect/Queue/Dequeue", Zn$2 = { _A: B$2, _E: B$2 }, ad$1 = { [sd$1]: Zn$2, [od$1]: Zn$2, [id$1]: Zn$2, ...Pt$2, toJSON() {
  return { _id: "effect/Queue", state: this.state._tag, size: dd$1(this) };
} }, cd$1 = (e) => Y$4((t) => {
  var _a3, _b2;
  const n = Object.create(ad$1);
  return n.dispatcher = t.currentDispatcher, n.capacity = (_a3 = e == null ? void 0 : e.capacity) != null ? _a3 : Number.POSITIVE_INFINITY, n.strategy = (_b2 = e == null ? void 0 : e.strategy) != null ? _b2 : "suspend", n.messages = td$1(), n.scheduleRunning = false, n.state = { _tag: "Open", takers: /* @__PURE__ */ new Set(), offers: /* @__PURE__ */ new Set(), awaiters: /* @__PURE__ */ new Set() }, w$3(n);
}), rn$2 = (e) => cd$1({ capacity: e }), _e$7 = (e, t) => M$3(() => {
  if (e.state._tag !== "Open") return on$2;
  if (e.messages.length >= e.capacity) switch (e.strategy) {
    case "dropping":
      return on$2;
    case "suspend":
      return e.capacity <= 0 && e.state.takers.size > 0 ? (He$5(e.messages, t), ki$2(e), Zt$2) : gd$1(e, t);
    case "sliding":
      return nn$2(e.messages), He$5(e.messages, t), Zt$2;
  }
  return He$5(e.messages, t), lr$1(e), Zt$2;
}), ud$1 = (e, t) => M$3(() => {
  if (e.state._tag !== "Open") return w$3(Oe$3(t));
  const n = ld$1(e, t);
  return n.length === 0 ? I$3([]) : e.strategy === "dropping" ? w$3(n) : yd$1(e, n);
}), ld$1 = (e, t) => {
  if (e.state._tag !== "Open") return Oe$3(t);
  if (e.capacity === Number.POSITIVE_INFINITY || e.strategy === "sliding") return nd$1(e.messages, t), e.strategy === "sliding" && xi$2(e.messages, e.messages.length - e.capacity), lr$1(e), [];
  const n = e.capacity <= 0 ? e.state.takers.size : e.capacity - e.messages.length;
  if (n === 0) return Oe$3(t);
  const r = [];
  let s = 0;
  for (const o of t) s < n ? He$5(e.messages, o) : r.push(o), s++;
  return lr$1(e), r;
}, sn$2 = (e, t) => yt$2(e, io$2(t)), yt$2 = d$3(2, (e, t) => Z$4(() => wi$2(e, t))), wi$2 = (e, t) => {
  if (e.state._tag !== "Open") return false;
  const n = ie$4(t), r = Ul$1(n, pd$1);
  return e.state.offers.size === 0 && e.messages.length === 0 ? (Wr$2(e, r), true) : (e.state = { ...e.state, _tag: "Closing", exit: r }, true);
}, $s$2 = (e) => Z$4(() => {
  if (e.state._tag === "Done") return true;
  wt$2(e.messages);
  const t = e.state.offers;
  if (Wr$2(e, e.state._tag === "Open" ? hd$1 : e.state.exit), t.size > 0) {
    for (const n of t) n._tag === "Single" ? n.resume(on$2) : n.resume(I$3(n.remaining.slice(n.offset)));
    t.clear();
  }
  return true;
}), Oi$2 = (e) => Ai$2(e, 1, Number.POSITIVE_INFINITY), Ai$2 = (e, t, n) => M$3(() => {
  var _a3;
  return (_a3 = md$1(e, t, n)) != null ? _a3 : vr$1(Ii$2(e), Ai$2(e, 1, n));
}), ur$1 = (e) => M$3(() => {
  var _a3;
  return (_a3 = fd$1(e)) != null ? _a3 : vr$1(Ii$2(e), ur$1(e));
}), fd$1 = (e) => {
  if (e.state._tag === "Done") return e.state.exit;
  if (e.messages.length > 0) {
    const t = nn$2(e.messages);
    return Ze$3(e), I$3(t);
  } else if (e.capacity <= 0 && e.state.offers.size > 0) {
    e.capacity = 1, Ze$3(e), e.capacity = 0;
    const t = nn$2(e.messages);
    return Ze$3(e), I$3(t);
  }
}, dd$1 = (e) => e.state._tag === "Done" ? 0 : e.messages.length, on$2 = I$3(false), Zt$2 = I$3(true), pd$1 = Ve$2(Or$2()), hd$1 = vl$1(), ki$2 = (e) => {
  if (e.scheduleRunning = false, !(e.state._tag === "Done" || e.state.takers.size === 0)) {
    for (const t of e.state.takers) if (e.state.takers.delete(t), t(ye$5), e.messages.length === 0) break;
  }
}, lr$1 = (e) => {
  e.scheduleRunning || e.state._tag === "Done" || e.state.takers.size === 0 || (e.scheduleRunning = true, e.dispatcher.scheduleTask(() => ki$2(e), 0));
}, md$1 = (e, t, n) => {
  if (e.state._tag === "Done") return e.state.exit;
  if (n <= 0 || t <= 0) return I$3([]);
  if (e.capacity <= 0 && e.state.offers.size > 0) {
    e.capacity = 1, Ze$3(e), e.capacity = 0;
    const r = [nn$2(e.messages)];
    return Ze$3(e), I$3(r);
  }
  if (t = Math.min(t, e.capacity || 1), t <= e.messages.length) {
    const r = xi$2(e.messages, n);
    return Ze$3(e), I$3(r);
  }
}, gd$1 = (e, t) => Fe$2((n) => {
  if (e.state._tag !== "Open") return n(on$2);
  const r = { _tag: "Single", message: t, resume: n };
  return e.state.offers.add(r), Z$4(() => {
    e.state._tag === "Open" && e.state.offers.delete(r);
  });
}), yd$1 = (e, t) => Fe$2((n) => {
  if (e.state._tag !== "Open") return n(I$3(t));
  const r = { _tag: "Array", remaining: t, offset: 0, resume: n };
  return e.state.offers.add(r), Z$4(() => {
    e.state._tag === "Open" && e.state.offers.delete(r);
  });
}), Ze$3 = (e) => {
  if (e.state._tag === "Done") return Is$2(e.state.exit.cause);
  if (e.state.offers.size === 0) return e.state._tag === "Closing" && e.messages.length === 0 ? (Wr$2(e, e.state.exit), Is$2(e.state.exit.cause)) : false;
  let t = e.capacity - e.messages.length;
  for (const n of e.state.offers) {
    if (t === 0) break;
    if (n._tag === "Single") He$5(e.messages, n.message), t--, n.resume(Zt$2), e.state.offers.delete(n);
    else {
      for (; n.offset < n.remaining.length; n.offset++) {
        if (t === 0) return false;
        He$5(e.messages, n.remaining[n.offset]), t--;
      }
      n.resume(I$3([])), e.state.offers.delete(n);
    }
  }
  return false;
}, Ii$2 = (e) => Fe$2((t) => e.state._tag === "Done" ? t(e.state.exit) : (e.state.takers.add(t), Z$4(() => {
  e.state._tag !== "Done" && e.state.takers.delete(t);
}))), Wr$2 = (e, t) => {
  if (e.state._tag === "Done") return;
  const n = e.state;
  e.state = { _tag: "Done", exit: t };
  for (const r of n.takers) r(t);
  n.takers.clear();
  for (const r of n.awaiters) r(t);
  n.awaiters.clear();
}, vi$2 = (e) => new Ed$1(e);
let Ed$1 = class Ed {
  constructor(t) {
    __publicField$6(this, "waiters", /* @__PURE__ */ new Set());
    __publicField$6(this, "taken", 0);
    __publicField$6(this, "permits");
    __publicField$6(this, "withPermit", this.withPermits(1));
    this.permits = t;
  }
  get free() {
    return this.permits - this.taken;
  }
  take(t) {
    const n = M$3(() => this.free < t ? Fe$2((r) => {
      if (this.free >= t) return r(n);
      const s = () => {
        this.free < t || (this.waiters.delete(s), r(n));
      };
      return this.waiters.add(s), Z$4(() => {
        this.waiters.delete(s);
      });
    }) : (this.taken += t, w$3(t)));
    return n;
  }
  updateTakenUnsafe(t, n) {
    return this.taken = n(this.taken), this.waiters.size > 0 && t.currentDispatcher.scheduleTask(() => {
      const r = this.waiters.values();
      let s = r.next();
      for (; s.done === false && this.free > 0; ) s.value(), s = r.next();
    }, 0), this.free;
  }
  updateTaken(t) {
    return Y$4((n) => w$3(this.updateTakenUnsafe(n, t)));
  }
  resize(t) {
    return Y$4((n) => (this.permits = t, this.free < 0 || this.updateTakenUnsafe(n, (r) => r), le$5));
  }
  release(t) {
    return this.updateTaken((n) => n - t);
  }
  get releaseAll() {
    return this.updateTaken((t) => 0);
  }
  withPermits(t) {
    return (n) => xs$2((r) => W$2(r(this.take(t)), (s) => Dt$2(r(n), () => {
      this.updateTakenUnsafe(en$2(), (o) => o - s);
    }, true)));
  }
  withPermitsIfAvailable(t) {
    return (n) => xs$2((r) => this.free < t ? Bo$1 : (this.taken += t, Dt$2(r(_l$3(n)), () => {
      this.updateTakenUnsafe(en$2(), (s) => s - t);
    }, true)));
  }
};
const Ci$2 = "~effect/Channel", $i$2 = (e) => P$3(e, Ci$2), bd$1 = { [Ci$2]: { _Env: B$2, _InErr: B$2, _InElem: B$2, _OutErr: B$2, _OutElem: B$2 }, pipe() {
  return ke$3(this, arguments);
} }, Je$2 = (e) => {
  const t = Object.create(bd$1);
  return t.transform = (n, r) => _t$4(e(n, r), (s) => $$3(Jt$2(s))), t;
}, Gr$2 = (e, t) => Je$2((n, r) => K$2(je$2(e)(n, r), (s) => t(s, r))), qr$2 = (e) => Je$2((t, n) => e), Sd$1 = (e) => Je$2(Br$2(function* (t, n) {
  const r = ci$2(n), s = (i) => jr$2(r, jf$1(i)), o = yield* vs$2(e(t, n, r), s);
  return vs$2(o, s);
})), je$2 = (e) => e.transform, Di$2 = (e) => Td$1($$3(e)), Cn$2 = (e) => qr$2($$3(zt$2(e))), Ui$2 = (e) => qr$2($$3(j$2(e))), Td$1 = (e) => qr$2(Kr$2(() => {
  let t = false;
  return fi$2(() => t ? zt$2() : (t = true, e));
})), Fi$2 = d$3(2, (e, t) => Gr$2(e, (n) => Kr$2(() => {
  let r = 0;
  return Kt$2(n, (s) => t(s, r++));
}))), Ni$2 = d$3(2, (e, t) => _d$1(e, (n) => $$3(t(n)))), _d$1 = d$3(2, (e, t) => Gr$2(e, (n) => $$3(li$2(n, (r) => K$2(t(r), zt$2))))), xd$1 = (e) => e === void 0 || e !== "unbounded" && e <= 1, wd$1 = d$3((e) => $i$2(e[0]), (e, t, n) => xd$1(n == null ? void 0 : n.concurrency) ? Od$1(e, t) : Ad$1(e, t, n)), Od$1 = (e, t) => Je$2((n, r) => {
  let s = 0;
  return Kt$2(je$2(e)(n, r), K$2((o) => t(o, s++)));
}), Ad$1 = (e, t, n) => Sd$1(Br$2(function* (r, s, o) {
  let i = 0;
  const u = yield* je$2(e)(r, s), c = n.concurrency === "unbounded" ? Number.MAX_SAFE_INTEGER : n.concurrency, a = yield* rn$2(0);
  yield* ks$2(o, $s$2(a));
  const l = Wf$1(yield* hi$2()), f = Xf$1(o);
  if (n.unordered) {
    const p = vi$2(c), E = Nt$2(p.release(1)), m = Bf$1({ onFailure: (y) => K$2(yt$2(a, y), E), onSuccess: (y) => K$2(_e$7(a, y), E) });
    yield* p.take(1).pipe(K$2(() => u), K$2((y) => (f(l(m(t(y, i++)))), Vt$2)), Hn$2({ disableYield: true }), _t$4((y) => p.withPermits(c - 1)(yt$2(a, y))), xt$2(o));
  } else {
    const p = yield* rn$2(c - 2);
    yield* ks$2(o, $s$2(a)), yield* ur$1(p).pipe(zf$1, K$2((y) => _e$7(a, y)), Hn$2({ disableYield: true }), _t$4((y) => yt$2(a, y)), xt$2(o));
    let E;
    const m = (y) => {
      y._tag !== "Success" && (E = y.cause, wi$2(a, y.cause));
    };
    yield* u.pipe(K$2((y) => {
      if (E) return Jt$2(E);
      const k = l(t(y, i++));
      return f(k), k.addObserver(m), _e$7(p, Qf$1(k));
    }), Hn$2({ disableYield: true }), _t$4((y) => _e$7(p, Lr$2(y)).pipe(Vf$1(yt$2(p, y)))), xt$2(o));
  }
  return ur$1(a);
})), $n$2 = d$3((e) => $i$2(e[0]), (e, t, n) => wd$1(e, (r) => Jf$1(t(r), r), n)), kd$1 = d$3(2, (e, t) => Je$2((n, r) => Kr$2(() => {
  let s;
  const o = ci$2(r), i = K$2(je$2(e)(n, o), (u) => (s = li$2(u, (c) => jr$2(o, ai$2).pipe(K$2(() => je$2(t(c))(n, r)), K$2((a) => (s = a, a)))), s));
  return fi$2(() => s != null ? s : i);
}))), Ri$2 = d$3(2, (e, t) => kd$1(e, (n) => t)), Id$1 = d$3(2, (e, t) => Gr$2(e, (n) => $$3(K$2(n, function r(s) {
  const o = [];
  for (let i = 0; i < s.length; i++) t(s[i]) && o.push(s[i]);
  return vt$2(o) ? $$3(o) : K$2(n, r);
})))), Pi$2 = Br$2(function* (e) {
  const t = vi$2(1), n = yield* hi$2(), r = qt$2(n, _f$1);
  return (yield* je$2(e)(zt$2(), r)).pipe(gt$2(n), t.withPermits(1));
}, _t$4((e) => $$3(Jt$2(e)))), vd$1 = (e, t) => je$2(e)(zt$2(), t);
let b$4 = class b extends Error {
  constructor(t, n) {
    super(t, n);
    __publicField$6(this, "jsonApiSource");
    __publicField$6(this, "translation");
    __publicField$6(this, "status");
    __publicField$6(this, "code");
    __publicField$6(this, "title");
    this.status = new.target.STATUS, this.jsonApiSource = n == null ? void 0 : n.jsonApiSource, this.translation = n == null ? void 0 : n.translation, this.code = new.target.CODE, this.title = new.target.TITLE, Object.setPrototypeOf(this, new.target.prototype);
  }
  toResult() {
    return we$5(this);
  }
};
__publicField$6(b$4, "TITLE");
__publicField$6(b$4, "STATUS");
__publicField$6(b$4, "CODE");
const S$2 = { NOT_IMPLEMENTED: "not_implemented", ILLEGAL_STATE: "illegal_state", NOT_FOUND: "not_found", BAD_REQUEST: "bad_request", FORBIDDEN: "forbidden", AUTHENTICATION_ERROR: "unauthenticated", AUTHORIZATION_ERROR: "unauthorized", DIR_INSTEAD_OF_FILE: "dir_instead_of_file", INVALID_DATA: "invalid_data", INTERNAL_ERROR: "internal_error", FILE_INSTEAD_OF_DIR: "file_instead_of_dir", VERSIONING_MISMATCH: "version_mismatch", VALIDATION_ERROR: "validation_error", ENTRY_ALREADY_EXISTS: "entry_already_exists", CONFLICT: "conflict", TOO_MANY_REQUESTS: "too_many_requests", SERVICE_UNAVAILABLE: "service_unavailable", GATEWAY_TIMEOUT: "gateway_timeout", UNKNOWN_ERROR: "unknown_error", AUTHORIZER_FAILURE: "authorizer_failure", UNSUPPORTED_FILE_TYPE: "unsupported_file_type", DANGEROUS_FILE_TYPE: "dangerous_file_type", CORRUPTED_FILE: "corrupted_file", EMBEDDED_CONTENT: "embedded_content", FILE_TOO_LARGE: "file_too_large" }, T$2 = { NOT_IMPLEMENTED: 501, ILLEGAL_STATE: 500, NOT_FOUND: 404, BAD_REQUEST: 400, FORBIDDEN: 403, AUTHORIZATION_ERROR: 401, AUTHENTICATION_ERROR: 401, DIR_INSTEAD_OF_FILE: 403, FILE_INSTEAD_OF_DIR: 403, INVALID_DATA: 400, INTERNAL_ERROR: 500, VERSIONING_MISMATCH: 409, VALIDATION_ERROR: 400, ENTRY_ALREADY_EXISTS: 409, CONFLICT: 409, TOO_MANY_REQUESTS: 429, SERVICE_UNAVAILABLE: 503, GATEWAY_TIMEOUT: 504, UNKNOWN_ERROR: 500, AUTHORIZER_FAILURE: 500, UNSUPPORTED_FILE_TYPE: 415, DANGEROUS_FILE_TYPE: 415, CORRUPTED_FILE: 422, EMBEDDED_CONTENT: 422, FILE_TOO_LARGE: 413 };
let Li$2 = class Li extends b$4 {
};
__publicField$6(Li$2, "TITLE", "Not Implemented");
__publicField$6(Li$2, "CODE", S$2.NOT_IMPLEMENTED);
__publicField$6(Li$2, "STATUS", T$2.NOT_IMPLEMENTED);
let ji$2 = class ji extends b$4 {
};
__publicField$6(ji$2, "TITLE", "Illegal State");
__publicField$6(ji$2, "CODE", S$2.ILLEGAL_STATE);
__publicField$6(ji$2, "STATUS", T$2.ILLEGAL_STATE);
let Mi$2 = class Mi extends b$4 {
};
__publicField$6(Mi$2, "TITLE", "Not Found");
__publicField$6(Mi$2, "CODE", S$2.NOT_FOUND);
__publicField$6(Mi$2, "STATUS", T$2.NOT_FOUND);
let zi$2 = class zi extends b$4 {
};
__publicField$6(zi$2, "TITLE", "Bad Request");
__publicField$6(zi$2, "CODE", S$2.BAD_REQUEST);
__publicField$6(zi$2, "STATUS", T$2.BAD_REQUEST);
let Vi$2 = class Vi extends b$4 {
};
__publicField$6(Vi$2, "TITLE", "Forbidden");
__publicField$6(Vi$2, "CODE", S$2.FORBIDDEN);
__publicField$6(Vi$2, "STATUS", T$2.FORBIDDEN);
let Ji$2 = class Ji extends b$4 {
};
__publicField$6(Ji$2, "TITLE", "Directory Instead of File");
__publicField$6(Ji$2, "CODE", S$2.DIR_INSTEAD_OF_FILE);
__publicField$6(Ji$2, "STATUS", T$2.DIR_INSTEAD_OF_FILE);
let Ki$2 = class Ki extends b$4 {
};
__publicField$6(Ki$2, "TITLE", "File Instead of Directory");
__publicField$6(Ki$2, "CODE", S$2.FILE_INSTEAD_OF_DIR);
__publicField$6(Ki$2, "STATUS", T$2.FILE_INSTEAD_OF_DIR);
let Bi$2 = class Bi extends b$4 {
};
__publicField$6(Bi$2, "TITLE", "Invalid Data");
__publicField$6(Bi$2, "CODE", S$2.INVALID_DATA);
__publicField$6(Bi$2, "STATUS", T$2.INVALID_DATA);
let Ge$2 = class Ge extends b$4 {
};
__publicField$6(Ge$2, "TITLE", "Internal Error");
__publicField$6(Ge$2, "CODE", S$2.INTERNAL_ERROR);
__publicField$6(Ge$2, "STATUS", T$2.INTERNAL_ERROR);
let Yi$2 = class Yi extends b$4 {
};
__publicField$6(Yi$2, "TITLE", "Version Mismatch");
__publicField$6(Yi$2, "CODE", S$2.VERSIONING_MISMATCH);
__publicField$6(Yi$2, "STATUS", T$2.VERSIONING_MISMATCH);
let Hr$2 = class Hr extends b$4 {
};
__publicField$6(Hr$2, "TITLE", "Validation Error");
__publicField$6(Hr$2, "CODE", S$2.VALIDATION_ERROR);
__publicField$6(Hr$2, "STATUS", T$2.VALIDATION_ERROR);
let Wi$2 = class Wi extends b$4 {
};
__publicField$6(Wi$2, "TITLE", "Entry Already Exists");
__publicField$6(Wi$2, "CODE", S$2.ENTRY_ALREADY_EXISTS);
__publicField$6(Wi$2, "STATUS", T$2.ENTRY_ALREADY_EXISTS);
let Gi$2 = class Gi extends b$4 {
};
__publicField$6(Gi$2, "TITLE", "Authorization Error");
__publicField$6(Gi$2, "CODE", S$2.AUTHORIZATION_ERROR);
__publicField$6(Gi$2, "STATUS", T$2.AUTHORIZATION_ERROR);
let qi$2 = class qi extends b$4 {
};
__publicField$6(qi$2, "TITLE", "Authentication Error");
__publicField$6(qi$2, "CODE", S$2.AUTHENTICATION_ERROR);
__publicField$6(qi$2, "STATUS", T$2.AUTHENTICATION_ERROR);
let Hi$2 = class Hi extends b$4 {
};
__publicField$6(Hi$2, "TITLE", "Conflict");
__publicField$6(Hi$2, "CODE", S$2.CONFLICT);
__publicField$6(Hi$2, "STATUS", T$2.CONFLICT);
let Zi$2 = class Zi extends b$4 {
};
__publicField$6(Zi$2, "TITLE", "Too Many Requests");
__publicField$6(Zi$2, "CODE", S$2.TOO_MANY_REQUESTS);
__publicField$6(Zi$2, "STATUS", T$2.TOO_MANY_REQUESTS);
let qe$5 = class qe extends b$4 {
};
__publicField$6(qe$5, "TITLE", "Service Unavailable");
__publicField$6(qe$5, "CODE", S$2.SERVICE_UNAVAILABLE);
__publicField$6(qe$5, "STATUS", T$2.SERVICE_UNAVAILABLE);
let Qi$2 = class Qi extends b$4 {
};
__publicField$6(Qi$2, "TITLE", "Gateway Timeout");
__publicField$6(Qi$2, "CODE", S$2.GATEWAY_TIMEOUT);
__publicField$6(Qi$2, "STATUS", T$2.GATEWAY_TIMEOUT);
let nt$2 = class nt extends b$4 {
};
__publicField$6(nt$2, "TITLE", "Unknown Error");
__publicField$6(nt$2, "CODE", S$2.UNKNOWN_ERROR);
__publicField$6(nt$2, "STATUS", T$2.UNKNOWN_ERROR);
let Xi$2 = class Xi extends b$4 {
};
__publicField$6(Xi$2, "TITLE", "Authorizer Failure");
__publicField$6(Xi$2, "CODE", S$2.AUTHORIZER_FAILURE);
__publicField$6(Xi$2, "STATUS", T$2.INTERNAL_ERROR);
let ea$2 = class ea extends b$4 {
};
__publicField$6(ea$2, "TITLE", "Unsupported File Type");
__publicField$6(ea$2, "CODE", S$2.UNSUPPORTED_FILE_TYPE);
__publicField$6(ea$2, "STATUS", T$2.UNSUPPORTED_FILE_TYPE);
let ta$2 = class ta extends b$4 {
};
__publicField$6(ta$2, "TITLE", "Dangerous File Type");
__publicField$6(ta$2, "CODE", S$2.DANGEROUS_FILE_TYPE);
__publicField$6(ta$2, "STATUS", T$2.DANGEROUS_FILE_TYPE);
let na$2 = class na extends b$4 {
};
__publicField$6(na$2, "TITLE", "Corrupted File");
__publicField$6(na$2, "CODE", S$2.CORRUPTED_FILE);
__publicField$6(na$2, "STATUS", T$2.CORRUPTED_FILE);
let ra$2 = class ra extends b$4 {
};
__publicField$6(ra$2, "TITLE", "Embedded Content");
__publicField$6(ra$2, "CODE", S$2.EMBEDDED_CONTENT);
__publicField$6(ra$2, "STATUS", T$2.EMBEDDED_CONTENT);
let sa$2 = class sa extends b$4 {
};
__publicField$6(sa$2, "TITLE", "File Too Large");
__publicField$6(sa$2, "CODE", S$2.FILE_TOO_LARGE);
__publicField$6(sa$2, "STATUS", T$2.FILE_TOO_LARGE);
const Cd$1 = Object.freeze(Object.defineProperty({ __proto__: null, AuthenticationError: qi$2, AuthorizationError: Gi$2, AuthorizerFailureError: Xi$2, BadRequestError: zi$2, ConflictError: Hi$2, CorruptedFileError: na$2, DangerousFileTypeError: ta$2, DirInsteadOfFile: Ji$2, EmbeddedContentError: ra$2, EntryAlreadyExistsError: Wi$2, FileInsteadOfDir: Ki$2, FileTooLargeError: sa$2, ForbiddenError: Vi$2, GatewayTimeoutError: Qi$2, IllegalStateException: ji$2, InternalError: Ge$2, InvalidData: Bi$2, LaikaError: b$4, NotFoundError: Mi$2, NotImplementedError: Li$2, ServiceUnavailableError: qe$5, TooManyRequestsError: Zi$2, UnknownError: nt$2, UnsupportedFileTypeError: ea$2, ValidationError: Hr$2, VersionMismatchError: Yi$2, errorCode: S$2, errorStatus: T$2 }, Symbol.toStringTag, { value: "Module" })), $d$1 = Object.fromEntries(Object.entries(S$2).map(([e, t]) => [t, e])), Dd$1 = Object.fromEntries(Object.entries(S$2).map(([e, t]) => [t, T$2[e]])), Ud$1 = { ...Cd$1, LaikaError: void 0 }, oa$2 = Object.fromEntries(Object.entries(Ud$1).filter(([e, t]) => e !== "LaikaError" && t && "CODE" in t)), Fd$1 = Object.fromEntries(Object.entries(oa$2).map(([e, t]) => [t.CODE, t])), Nd$1 = Fd$1, Rd$1 = zr$2, Pd$1 = vf$1, Ld$1 = Cf$1, jd$1 = mt$2, Md$1 = Uf$1, zd$1 = Ff$1, Vd$1 = Rf$1;
function ia$2(e) {
  return e.checks ? e.checks[e.checks.length - 1].annotations : e.annotations;
}
function aa$2(e) {
  return (t) => {
    var _a3;
    return (_a3 = ia$2(t)) == null ? void 0 : _a3[e];
  };
}
const Jd$1 = aa$2("identifier"), Kd$1 = aa$2("brands"), Zr$2 = De$3((e) => {
  const t = Jd$1(e);
  return typeof t == "string" ? t : e.getExpected(Zr$2);
});
function Et$2(e, t, n) {
  return t === "__proto__" ? Object.defineProperty(e, t, { value: n, writable: true, enumerable: true, configurable: true }) : e[t] = n, e;
}
const Bd$1 = (e) => e.replace(/[/\\^$*+?.()|[\]{}]/g, "\\$&"), fr$1 = "~effect/SchemaIssue/Issue";
function Yd$1(e) {
  return P$3(e, fr$1);
}
let Se$3 = (_k$1 = fr$1, class {
  constructor() {
    __publicField$6(this, _k$1, fr$1);
  }
  toString() {
    return ep$1(this);
  }
}), Wd$1 = class Wd extends Se$3 {
  constructor(t, n, r) {
    super();
    __publicField$6(this, "_tag", "Filter");
    __publicField$6(this, "actual");
    __publicField$6(this, "filter");
    __publicField$6(this, "issue");
    this.actual = t, this.filter = n, this.issue = r;
  }
};
let Gd$1 = class Gd extends Se$3 {
  constructor(t, n, r) {
    super();
    __publicField$6(this, "_tag", "Encoding");
    __publicField$6(this, "ast");
    __publicField$6(this, "actual");
    __publicField$6(this, "issue");
    this.ast = t, this.actual = n, this.issue = r;
  }
};
let ft$2 = class ft extends Se$3 {
  constructor(t, n) {
    super();
    __publicField$6(this, "_tag", "Pointer");
    __publicField$6(this, "path");
    __publicField$6(this, "issue");
    this.path = t, this.issue = n;
  }
};
let ca$2 = class ca extends Se$3 {
  constructor(t) {
    super();
    __publicField$6(this, "_tag", "MissingKey");
    __publicField$6(this, "annotations");
    this.annotations = t;
  }
};
let ua$2 = class ua extends Se$3 {
  constructor(t, n) {
    super();
    __publicField$6(this, "_tag", "UnexpectedKey");
    __publicField$6(this, "ast");
    __publicField$6(this, "actual");
    this.ast = t, this.actual = n;
  }
};
let fe$5 = class fe extends Se$3 {
  constructor(t, n, r) {
    super();
    __publicField$6(this, "_tag", "Composite");
    __publicField$6(this, "ast");
    __publicField$6(this, "actual");
    __publicField$6(this, "issues");
    this.ast = t, this.actual = n, this.issues = r;
  }
};
let Yt$2 = class Yt extends Se$3 {
  constructor(t, n) {
    super();
    __publicField$6(this, "_tag", "InvalidType");
    __publicField$6(this, "ast");
    __publicField$6(this, "actual");
    this.ast = t, this.actual = n;
  }
};
let Ut$2 = class Ut extends Se$3 {
  constructor(t, n) {
    super();
    __publicField$6(this, "_tag", "InvalidValue");
    __publicField$6(this, "actual");
    __publicField$6(this, "annotations");
    this.actual = t, this.annotations = n;
  }
};
let Ds$2 = class Ds extends Se$3 {
  constructor(t, n, r) {
    super();
    __publicField$6(this, "_tag", "AnyOf");
    __publicField$6(this, "ast");
    __publicField$6(this, "actual");
    __publicField$6(this, "issues");
    this.ast = t, this.actual = n, this.issues = r;
  }
};
let qd$1 = class qd extends Se$3 {
  constructor(t, n, r) {
    super();
    __publicField$6(this, "_tag", "OneOf");
    __publicField$6(this, "ast");
    __publicField$6(this, "actual");
    __publicField$6(this, "successes");
    this.ast = t, this.actual = n, this.successes = r;
  }
};
function dr$1(e, t) {
  if (Yd$1(t)) return t;
  if (typeof t == "string") return new Ut$2(C$5(e), { message: t });
  const n = typeof t.issue == "string" ? new Ut$2(C$5(e), { message: t.issue }) : t.issue;
  return new ft$2(t.path, n);
}
function Hd$1(e, t) {
  if (t !== void 0) return typeof t == "boolean" ? t ? void 0 : new Ut$2(C$5(e)) : dr$1(e, t);
}
function Zd$1(e, t, n) {
  return Array.isArray(n) ? vt$2(n) ? n.length === 1 ? dr$1(e, n[0]) : new fe$5(t, C$5(e), Nu$1(n, (r) => dr$1(e, r))) : void 0 : Hd$1(e, n);
}
const la$2 = (e) => {
  const t = Ft$2(e);
  if (t !== void 0) return t;
  switch (e._tag) {
    case "InvalidType":
      return pr$1(Zr$2(e.ast), Us$2(e.actual));
    case "InvalidValue":
      return `Invalid data ${Us$2(e.actual)}`;
    case "MissingKey":
      return "Missing key";
    case "UnexpectedKey":
      return `Unexpected key with value ${L$3(e.actual)}`;
    case "Forbidden":
      return "Forbidden operation";
    case "OneOf":
      return `Expected exactly one member to match the input ${L$3(e.actual)}`;
  }
}, fa$2 = (e) => {
  var _a3;
  return (_a3 = Ft$2(e.issue)) != null ? _a3 : Ft$2(e);
};
function Qd$1(e) {
  return (t) => ({ issues: Re$3(t, [], la$2, fa$2) });
}
function pr$1(e, t) {
  return `Expected ${e}, got ${t}`;
}
function Re$3(e, t, n, r) {
  switch (e._tag) {
    case "Filter": {
      const s = r(e);
      if (s !== void 0) return [{ path: t, message: s }];
      switch (e.issue._tag) {
        case "InvalidValue":
          return [{ path: t, message: pr$1(da$2(e.filter), L$3(e.actual)) }];
        default:
          return Re$3(e.issue, t, n, r);
      }
    }
    case "Encoding":
      return Re$3(e.issue, t, n, r);
    case "Pointer":
      return Re$3(e.issue, [...t, ...e.path], n, r);
    case "Composite":
      return e.issues.flatMap((s) => Re$3(s, t, n, r));
    case "AnyOf": {
      const s = Ft$2(e);
      if (e.issues.length === 0) {
        if (s !== void 0) return [{ path: t, message: s }];
        const o = pr$1(Zr$2(e.ast), L$3(e.actual));
        return [{ path: t, message: o }];
      }
      return e.issues.flatMap((o) => Re$3(o, t, n, r));
    }
    default:
      return [{ path: t, message: n(e) }];
  }
}
function da$2(e) {
  var _a3;
  const t = (_a3 = e.annotations) == null ? void 0 : _a3.expected;
  if (typeof t == "string") return t;
  switch (e._tag) {
    case "Filter":
      return "<filter>";
    case "FilterGroup":
      return e.checks.map((n) => da$2(n)).join(" & ");
  }
}
function Xd$1() {
  return (e) => Re$3(e, [], la$2, fa$2).map(tp$1).join(`
`);
}
const ep$1 = Xd$1();
function tp$1(e) {
  let t = e.message;
  if (e.path && e.path.length > 0) {
    const n = Lc$1(e.path);
    t += `
  at ${n}`;
  }
  return t;
}
function Ft$2(e) {
  switch (e._tag) {
    case "InvalidType":
    case "OneOf":
    case "Composite":
    case "AnyOf":
      return ht$2(e.ast.annotations);
    case "InvalidValue":
    case "Forbidden":
      return ht$2(e.annotations);
    case "MissingKey":
      return ht$2(e.annotations, "messageMissingKey");
    case "UnexpectedKey":
      return ht$2(e.ast.annotations, "messageUnexpectedKey");
    case "Filter":
      return ht$2(e.filter.annotations);
    case "Encoding":
      return Ft$2(e.issue);
  }
}
function ht$2(e, t = "message") {
  const n = e == null ? void 0 : e[t];
  if (typeof n == "string") return n;
}
function Us$2(e) {
  return Lt$2(e) ? "no value provided" : L$3(e.value);
}
let rt$2 = class rt extends Er$1 {
  constructor(t) {
    super();
    __publicField$6(this, "run");
    this.run = t;
  }
  map(t) {
    return new rt((n, r) => this.run(n, r).pipe(Yr$2(xo$1(t))));
  }
  compose(t) {
    return Fs$2(this) ? t : Fs$2(t) ? this : new rt((n, r) => this.run(n, r).pipe(We$2((s) => t.run(s, r))));
  }
};
const pa$2 = new rt$2($$3);
function Fs$2(e) {
  return e.run === pa$2.run;
}
function Ns$2() {
  return pa$2;
}
function np$1(e) {
  return new rt$2((t, n) => Lt$2(t) ? kn$2 : e(t.value, n));
}
function Ae$5(e) {
  return rp$1(xo$1(e));
}
function Rs$2(e) {
  return np$1((t, n) => e(t, n).pipe(Yr$2(C$5)));
}
function rp$1(e) {
  return new rt$2((t) => $$3(e(t)));
}
function sp$1() {
  return Ae$5(globalThis.String);
}
function ha$2() {
  return Ae$5(globalThis.Number);
}
const hr$1 = "~effect/SchemaTransformation/Transformation";
_l2$1 = hr$1;
const _Ee$1 = class _Ee {
  constructor(t, n) {
    __publicField$6(this, _l2$1, hr$1);
    __publicField$6(this, "_tag", "Transformation");
    __publicField$6(this, "decode");
    __publicField$6(this, "encode");
    this.decode = t, this.encode = n;
  }
  flip() {
    return new _Ee(this.encode, this.decode);
  }
  compose(t) {
    return new _Ee(this.decode.compose(t.decode), t.encode.compose(this.encode));
  }
};
let Ee$4 = _Ee$1;
function op$1(e) {
  return P$3(e, hr$1);
}
const ma$2 = (e) => op$1(e) ? e : new Ee$4(e.decode, e.encode);
function ip$1(e) {
  return new Ee$4(Rs$2(e.decode), Rs$2(e.encode));
}
const ap$1 = new Ee$4(Ns$2(), Ns$2());
function cp$1() {
  return ap$1;
}
const Qr$2 = new Ee$4(ha$2(), sp$1()), ga$2 = ip$1({ decode: (e) => mu$1(Md$1(e), { onNone: () => j$2(new Ut$2(C$5(e), { message: `Invalid UTC DateTime string: ${e}` })), onSome: (t) => $$3(zd$1(t)) }), encode: (e) => $$3(Vd$1(e)) });
function Ke$2(e) {
  return (t) => t._tag === e;
}
const up$1 = Ke$2("Declaration"), lp$1 = Ke$2("Never"), an$2 = Ke$2("Literal"), fp$1 = Ke$2("UniqueSymbol"), dp$1 = Ke$2("Arrays"), pp$1 = Ke$2("Objects"), hp$1 = Ke$2("Union");
let pe$5 = class pe {
  constructor(t, n) {
    __publicField$6(this, "to");
    __publicField$6(this, "transformation");
    this.to = t, this.transformation = n;
  }
};
const mp$1 = {};
let cn$2 = class cn {
  constructor(t, n, r = void 0, s = void 0) {
    __publicField$6(this, "isOptional");
    __publicField$6(this, "isMutable");
    __publicField$6(this, "defaultValue");
    __publicField$6(this, "annotations");
    this.isOptional = t, this.isMutable = n, this.defaultValue = r, this.annotations = s;
  }
};
const Ps$2 = "~effect/Schema";
_m2$1 = Ps$2;
let ce$5 = class ce {
  constructor(t = void 0, n = void 0, r = void 0, s = void 0) {
    __publicField$6(this, _m2$1, Ps$2);
    __publicField$6(this, "annotations");
    __publicField$6(this, "checks");
    __publicField$6(this, "encoding");
    __publicField$6(this, "context");
    this.annotations = t, this.checks = n, this.encoding = r, this.context = s;
  }
  toString() {
    return `<${this._tag}>`;
  }
};
let Xr$2 = class Xr extends ce$5 {
  constructor(t, n, r, s, o, i) {
    super(r, s, o, i);
    __publicField$6(this, "_tag", "Declaration");
    __publicField$6(this, "typeParameters");
    __publicField$6(this, "run");
    this.typeParameters = t, this.run = n;
  }
  getParser() {
    const t = this.run(this.typeParameters);
    return (n, r) => Lt$2(n) ? kn$2 : Yr$2(t(n.value, this, r), C$5);
  }
  recur(t) {
    const n = st$1(this.typeParameters, t);
    return n === this.typeParameters ? this : new Xr(n, this.run, this.annotations, this.checks, void 0, this.context);
  }
  getExpected() {
    var _a3;
    const t = (_a3 = this.annotations) == null ? void 0 : _a3.expected;
    return typeof t == "string" ? t : "<Declaration>";
  }
};
let gp$1 = class gp extends ce$5 {
  constructor() {
    super(...arguments);
    __publicField$6(this, "_tag", "Null");
  }
  getParser() {
    return ss$2(this, null);
  }
  getExpected() {
    return "null";
  }
};
const yp$1 = new gp$1();
let Ep$1 = class Ep extends ce$5 {
  constructor() {
    super(...arguments);
    __publicField$6(this, "_tag", "Undefined");
  }
  getParser() {
    return ss$2(this, void 0);
  }
  toCodecJson() {
    return H$3(this, [bp$1]);
  }
  getExpected() {
    return "undefined";
  }
};
const bp$1 = new pe$5(yp$1, new Ee$4(Ae$5(() => {
}), Ae$5(() => null))), Sp$1 = new Ep$1();
let Tp$1 = class Tp extends ce$5 {
  constructor() {
    super(...arguments);
    __publicField$6(this, "_tag", "Any");
  }
  getParser() {
    return dt$2(this, Qs$2);
  }
  getExpected() {
    return "any";
  }
};
const _p$1 = new Tp$1();
let xp$1 = class xp extends ce$5 {
  constructor() {
    super(...arguments);
    __publicField$6(this, "_tag", "Unknown");
  }
  getParser() {
    return dt$2(this, Qs$2);
  }
  getExpected() {
    return "unknown";
  }
};
const wp$1 = new xp$1();
let Ot$2 = class Ot extends ce$5 {
  constructor(t, n, r, s, o) {
    super(n, r, s, o);
    __publicField$6(this, "_tag", "Literal");
    __publicField$6(this, "literal");
    if (typeof t == "number" && !globalThis.Number.isFinite(t)) throw new Error(`A numeric literal must be finite, got ${L$3(t)}`);
    this.literal = t;
  }
  getParser() {
    return ss$2(this, this.literal);
  }
  toCodecJson() {
    return typeof this.literal == "bigint" ? Ls$2(this) : this;
  }
  toCodecStringTree() {
    return typeof this.literal == "string" ? this : Ls$2(this);
  }
  getExpected() {
    return typeof this.literal == "string" ? JSON.stringify(this.literal) : globalThis.String(this.literal);
  }
};
function Ls$2(e) {
  const t = globalThis.String(e.literal);
  return H$3(e, [new pe$5(new Ot$2(t), new Ee$4(Ae$5(() => e.literal), Ae$5(() => t)))]);
}
let Op$1 = class Op extends ce$5 {
  constructor() {
    super(...arguments);
    __publicField$6(this, "_tag", "String");
  }
  getParser() {
    return dt$2(this, Hs$2);
  }
  getExpected() {
    return "string";
  }
};
const ya$2 = new Op$1();
let Ap$1 = class Ap extends ce$5 {
  constructor() {
    super(...arguments);
    __publicField$6(this, "_tag", "Number");
  }
  getParser() {
    return dt$2(this, Zs$2);
  }
  toCodecJson() {
    return this.checks && (bt$2(this.checks, "isFinite") || bt$2(this.checks, "isInt")) ? this : H$3(this, [Rp$1]);
  }
  toCodecStringTree() {
    return this.checks && (bt$2(this.checks, "isFinite") || bt$2(this.checks, "isInt")) ? H$3(this, [Zp$1]) : H$3(this, [Qp$1]);
  }
  getExpected() {
    return "number";
  }
};
function bt$2(e, t) {
  return e.some((n) => {
    var _a3, _b2;
    switch (n._tag) {
      case "Filter":
        return ((_b2 = (_a3 = n.annotations) == null ? void 0 : _a3.meta) == null ? void 0 : _b2._tag) === t;
      case "FilterGroup":
        return bt$2(n.checks, t);
    }
  });
}
const Ea$2 = new Ap$1();
let kp$1 = class kp extends ce$5 {
  constructor() {
    super(...arguments);
    __publicField$6(this, "_tag", "Boolean");
  }
  getParser() {
    return dt$2(this, hc$1);
  }
  getExpected() {
    return "boolean";
  }
};
const Ip$1 = new kp$1();
let es$2 = class es extends ce$5 {
  constructor(t, n, r, s, o, i, u) {
    super(s, o, i, u);
    __publicField$6(this, "_tag", "Arrays");
    __publicField$6(this, "isMutable");
    __publicField$6(this, "elements");
    __publicField$6(this, "rest");
    this.isMutable = t, this.elements = n, this.rest = r;
    const c = n.findIndex(ve$3);
    if (c !== -1 && (n.slice(c + 1).some((a) => !ve$3(a)) || r.length > 1)) throw new Error("A required element cannot follow an optional element. ts(1257)");
    if (r.length > 1 && r.slice(1).some(ve$3)) throw new Error("An optional element cannot follow a rest element. ts(1266)");
  }
  getParser(t) {
    const n = this, r = n.elements.map((l) => ({ ast: l, parser: t(l) })), s = n.rest.map((l) => ({ ast: l, parser: t(l) })), o = r.length, [i, ...u] = s, c = u.length;
    function a(l, f) {
      return f < o ? r[f] : f >= l ? u[f - l] : i;
    }
    return cr$1(function* (l, f) {
      if (l._tag === "None") return l;
      const p = l.value;
      if (!Array.isArray(p)) return yield* j$2(new Yt$2(n, l));
      const E = p.length, m = { ast: n, getParser: a, oinput: l, len: E, tailThreshold: Cp$1(E, o, c), output: new globalThis.Array(E), issues: void 0, options: f }, y = ts$2(f == null ? void 0 : f.concurrency), k = vp$1(m, p, { concurrency: y == null ? void 0 : y.concurrency, end: n.rest.length === 0 ? o : Math.max(E, o + c) });
      if (k && (yield* k), n.rest.length === 0 && E > o) for (let J = o; J <= E - 1; J++) {
        const _ = new ft$2([J], new ua$2(n, p[J]));
        if (f.errors === "all") m.issues ? m.issues.push(_) : m.issues = [_];
        else return yield* j$2(new fe$5(n, l, [_]));
      }
      return m.issues ? yield* j$2(new fe$5(n, l, m.issues)) : C$5(m.output);
    });
  }
  recur(t) {
    const n = st$1(this.elements, t), r = st$1(this.rest, t);
    return n === this.elements && r === this.rest ? this : new es(this.isMutable, n, r, this.annotations, this.checks, void 0, this.context);
  }
  getExpected() {
    return "array";
  }
};
const vp$1 = Tn$2()({ onItem(e, t, n) {
  const r = n < e.len ? C$5(t) : at$1();
  return e.getParser(e.tailThreshold, n).parser(r, e.options);
}, step(e, t, n, r) {
  var _a3;
  if (n._tag === "Failure") return un$2(e, e.ast, r, n);
  if (n.value._tag === "Some") e.output[r] = n.value.value;
  else {
    const s = e.getParser(e.tailThreshold, r);
    if (ve$3(s.ast)) return;
    const o = new ft$2([r], new ca$2((_a3 = s.ast.context) == null ? void 0 : _a3.annotations));
    if (e.options.errors === "all") e.issues ? e.issues.push(o) : e.issues = [o];
    else return xn$2(new fe$5(e.ast, e.oinput, [o]));
  }
} });
function Cp$1(e, t, n) {
  return Math.max(t, e - n);
}
const ts$2 = (e) => (e = e === "unbounded" ? 1 / 0 : e != null ? e : 1, e > 1 ? { concurrency: e } : void 0), un$2 = (e, t, n, r) => {
  const s = On$2(r.cause);
  if (X$4(s)) return r;
  const o = new ft$2([n], s.success);
  if (e.options.errors === "all") e.issues ? e.issues.push(o) : e.issues = [o];
  else return xn$2(new fe$5(t, e.oinput, [o]));
}, ns$2 = "[+-]?\\d*\\.?\\d+(?:[Ee][+-]?\\d+)?", $p$1 = new globalThis.RegExp(`(?:${ns$2}|Infinity|-Infinity|NaN)`);
function ba$2(e, t) {
  const n = Nn$2(t);
  switch (n._tag) {
    case "String":
      return Object.keys(e);
    case "TemplateLiteral": {
      const r = Yp$1(n);
      return Object.keys(e).filter((s) => r.test(s));
    }
    case "Symbol":
      return Object.getOwnPropertySymbols(e);
    case "Number":
      return Object.keys(e).filter((r) => $p$1.test(r));
    case "Union":
      return [...new Set(n.types.flatMap((r) => ba$2(e, r)))];
    default:
      return [];
  }
}
let rs$2 = class rs {
  constructor(t, n) {
    __publicField$6(this, "name");
    __publicField$6(this, "type");
    this.name = t, this.type = n;
  }
};
let Sa$2 = class Sa {
  constructor(t, n, r) {
    __publicField$6(this, "parameter");
    __publicField$6(this, "type");
    __publicField$6(this, "merge");
    if (this.parameter = t, this.type = n, this.merge = r, ve$3(n) && !Ra$2(n)) throw new Error("Cannot use `Schema.optionalKey` with index signatures, use `Schema.optional` instead.");
  }
};
let Dn$2 = class Dn extends ce$5 {
  constructor(t, n, r, s, o, i) {
    super(r, s, o, i);
    __publicField$6(this, "_tag", "Objects");
    __publicField$6(this, "propertySignatures");
    __publicField$6(this, "indexSignatures");
    this.propertySignatures = t, this.indexSignatures = n;
    const u = t.map((c) => c.name).filter((c, a, l) => l.indexOf(c) !== a);
    if (u.length > 0) throw new Error(`Duplicate identifiers: ${JSON.stringify(u)}. ts(2300)`);
  }
  getParser(t) {
    const n = this, r = [], s = /* @__PURE__ */ new Set(), o = [];
    for (const c of n.propertySignatures) r.push(c.name), s.add(c.name), o.push({ ps: c, parser: t(c.type), name: c.name, type: c.type });
    const i = n.indexSignatures.length;
    if (n.propertySignatures.length === 0 && n.indexSignatures.length === 0) return dt$2(n, yc$1);
    const u = i > 0 ? Tn$2()({ onItem: cr$1(function* (c, [a, l]) {
      const p = t(ja$2(l.parameter))(C$5(a), c.options), E = ae$4(p) ? p : yield* tn$2(p);
      if (E._tag === "Failure") {
        const _ = un$2(c, n, a, E);
        _ && (yield* _);
        return;
      }
      const m = C$5(c.input[a]), k = t(l.type)(m, c.options), J = ae$4(k) ? k : yield* tn$2(k);
      if (J._tag === "Failure") {
        const _ = un$2(c, n, a, J);
        _ && (yield* _);
        return;
      } else if (E.value._tag === "Some" && J.value._tag === "Some") {
        const _ = E.value.value, F = J.value.value;
        if (l.merge && l.merge.decode && Object.hasOwn(c.out, _)) {
          const [x, N] = l.merge.decode.combine([_, c.out[_]], [_, F]);
          Et$2(c.out, x, N);
        } else Et$2(c.out, _, F);
      }
    }), step: (c, a, l) => l._tag === "Failure" ? l : void 0 }) : void 0;
    return cr$1(function* (c, a) {
      if (c._tag === "None") return c;
      const l = c.value;
      if (!(typeof l == "object" && l !== null && !Array.isArray(l))) return yield* j$2(new Yt$2(n, c));
      const f = {}, p = { ast: n, oinput: c, input: l, out: f, issues: void 0, options: a }, E = a.errors === "all", m = a.onExcessProperty === "error", y = a.onExcessProperty === "preserve";
      let k;
      if (n.indexSignatures.length === 0 && (m || y)) {
        k = Reflect.ownKeys(l);
        for (let F = 0; F < k.length; F++) {
          const x = k[F];
          if (!s.has(x)) if (m) {
            const N = new ft$2([x], new ua$2(n, l[x]));
            if (E) {
              p.issues ? p.issues.push(N) : p.issues = [N];
              continue;
            } else return yield* j$2(new fe$5(n, c, [N]));
          } else Et$2(f, x, l[x]);
        }
      }
      const J = ts$2(a == null ? void 0 : a.concurrency), _ = Dp$1(p, o, J);
      if (_ && (yield* _), u) {
        const F = Fu$1();
        for (let N = 0; N < i; N++) {
          const Be = n.indexSignatures[N], q = ba$2(l, Be.parameter);
          for (let Vn = 0; Vn < q.length; Vn++) {
            const lc = q[Vn];
            F.push([lc, Be]);
          }
        }
        const x = u(p, F, J);
        x && (yield* x);
      }
      if (p.issues) return yield* j$2(new fe$5(n, c, p.issues));
      if (a.propertyOrder === "original") {
        const F = (k != null ? k : Reflect.ownKeys(l)).concat(r), x = {};
        for (const N of F) Object.hasOwn(f, N) && Et$2(x, N, f[N]);
        return C$5(x);
      }
      return C$5(f);
    });
  }
  rebuild(t, n) {
    const r = st$1(this.propertySignatures, (o) => {
      const i = t(o.type);
      return i === o.type ? o : new rs$2(o.name, i);
    }), s = st$1(this.indexSignatures, (o) => {
      var _a3;
      const i = t(o.parameter), u = t(o.type), c = n ? (_a3 = o.merge) == null ? void 0 : _a3.flip() : o.merge;
      return i === o.parameter && u === o.type && c === o.merge ? o : new Sa$2(i, u, c);
    });
    return r === this.propertySignatures && s === this.indexSignatures ? this : new Dn(r, s, this.annotations, this.checks, void 0, this.context);
  }
  flip(t) {
    return this.rebuild(t, true);
  }
  recur(t) {
    return this.rebuild(t, false);
  }
  getExpected() {
    return this.propertySignatures.length === 0 && this.indexSignatures.length === 0 ? "object | array" : "object";
  }
};
const Dp$1 = Tn$2()({ onItem(e, t) {
  const n = Object.hasOwn(e.input, t.name) ? C$5(e.input[t.name]) : at$1();
  return t.parser(n, e.options);
}, step(e, t, n) {
  var _a3;
  if (n._tag === "Failure") return un$2(e, e.ast, t.name, n);
  if (n.value._tag === "Some") Et$2(e.out, t.name, n.value.value);
  else if (!ve$3(t.type)) {
    const r = new ft$2([t.name], new ca$2((_a3 = t.type.context) == null ? void 0 : _a3.annotations));
    if (e.options.errors === "all") {
      e.issues ? e.issues.push(r) : e.issues = [r];
      return;
    } else return xn$2(new fe$5(e.ast, e.oinput, [r]));
  }
} });
function Ta$2(e, t, n) {
  return new Dn$2(Reflect.ownKeys(e).map((r) => new rs$2(r, e[r].ast)), [], n, t);
}
function _a$4(e) {
  return e.ast;
}
function xa$2(e, t, n) {
  return new Un$2(e.map(_a$4), t, void 0, n);
}
function wa$2(e) {
  switch (e._tag) {
    case "Null":
      return ["null"];
    case "Undefined":
    case "Void":
      return ["undefined"];
    case "String":
    case "TemplateLiteral":
      return ["string"];
    case "Number":
      return ["number"];
    case "Boolean":
      return ["boolean"];
    case "Symbol":
    case "UniqueSymbol":
      return ["symbol"];
    case "BigInt":
      return ["bigint"];
    case "Arrays":
      return ["array"];
    case "ObjectKeyword":
      return ["object", "array", "function"];
    case "Objects":
      return e.propertySignatures.length || e.indexSignatures.length ? ["object"] : ["object", "array"];
    case "Enum":
      return Array.from(new Set(e.enums.map(([, t]) => typeof t)));
    case "Literal":
      return [typeof e.literal];
    case "Union":
      return Array.from(new Set(e.types.flatMap(wa$2)));
    default:
      return ["null", "undefined", "string", "number", "boolean", "symbol", "bigint", "object", "array", "function"];
  }
}
function Oa$2(e) {
  var _a3;
  switch (e._tag) {
    default:
      return [];
    case "Declaration": {
      const t = (_a3 = e.annotations) == null ? void 0 : _a3["~sentinels"];
      return Array.isArray(t) ? t : [];
    }
    case "Objects":
      return e.propertySignatures.flatMap((t) => {
        const n = t.type;
        if (!ve$3(n)) {
          if (an$2(n)) return [{ key: t.name, literal: n.literal }];
          if (fp$1(n)) return [{ key: t.name, literal: n.symbol }];
        }
        return [];
      });
    case "Arrays":
      return e.elements.flatMap((t, n) => an$2(t) && !ve$3(t) ? [{ key: n, literal: t.literal }] : []);
    case "Suspend":
      return Oa$2(e.thunk());
  }
}
const js$2 = /* @__PURE__ */ new WeakMap();
function Up$1(e) {
  var _a3, _b2, _c3, _d3, _e3, _f3, _g2;
  let t = js$2.get(e);
  if (t) return t;
  t = {};
  for (const n of e) {
    const r = Nn$2(n);
    if (lp$1(r)) continue;
    const s = wa$2(r), o = Oa$2(r);
    (_a3 = t.byType) != null ? _a3 : t.byType = {};
    for (const i of s) ((_c3 = (_b2 = t.byType)[i]) != null ? _c3 : _b2[i] = []).push(n);
    if (o.length > 0) {
      (_d3 = t.bySentinel) != null ? _d3 : t.bySentinel = /* @__PURE__ */ new Map();
      for (const { key: i, literal: u } of o) {
        let c = t.bySentinel.get(i);
        c || t.bySentinel.set(i, c = /* @__PURE__ */ new Map());
        let a = c.get(u);
        a || c.set(u, a = []), a.push(n);
      }
    } else {
      (_e3 = t.otherwise) != null ? _e3 : t.otherwise = {};
      for (const i of s) ((_g2 = (_f3 = t.otherwise)[i]) != null ? _g2 : _f3[i] = []).push(n);
    }
  }
  return js$2.set(e, t), t;
}
function Ms$2(e) {
  return (t) => {
    const n = Nn$2(t);
    return n._tag === "Literal" ? n.literal === e : n._tag === "UniqueSymbol" ? n.symbol === e : true;
  };
}
function Fp$1(e, t) {
  var _a3, _b2, _c3, _d3;
  const n = Up$1(t), r = e === null ? "null" : Array.isArray(e) ? "array" : typeof e;
  if (n.bySentinel) {
    const s = (_b2 = (_a3 = n.otherwise) == null ? void 0 : _a3[r]) != null ? _b2 : [];
    if (r === "object" || r === "array") {
      for (const [o, i] of n.bySentinel) if (Object.hasOwn(e, o)) {
        const u = i.get(e[o]);
        if (u) return [...u, ...s].filter(Ms$2(e));
      }
    }
    return s;
  }
  return ((_d3 = (_c3 = n.byType) == null ? void 0 : _c3[r]) != null ? _d3 : []).filter(Ms$2(e));
}
let Un$2 = class Aa extends ce$5 {
  constructor(t, n, r, s, o, i) {
    super(r, s, o, i);
    __publicField$6(this, "_tag", "Union");
    __publicField$6(this, "types");
    __publicField$6(this, "mode");
    this.types = t, this.mode = n;
  }
  getParser(t) {
    const n = this;
    return (r, s) => {
      var _a3;
      if (r._tag === "None") return $$3(r);
      const o = r.value, i = Fp$1(o, n.types), u = { ast: n, recur: t, oinput: r, input: o, out: void 0, successes: [], issues: void 0, options: s }, c = ts$2(s == null ? void 0 : s.concurrency), a = Np$1(u, i, c);
      return a ? K$2(a, (l) => {
        var _a4;
        return u.out ? $$3(u.out) : j$2(new Ds$2(n, o, (_a4 = u.issues) != null ? _a4 : []));
      }) : u.out ? $$3(u.out) : j$2(new Ds$2(n, o, (_a3 = u.issues) != null ? _a3 : []));
    };
  }
  recur(t) {
    const n = st$1(this.types, t);
    return n === this.types ? this : new Aa(n, this.mode, this.annotations, this.checks, void 0, this.context);
  }
  getExpected(t) {
    var _a3;
    const n = (_a3 = this.annotations) == null ? void 0 : _a3.expected;
    if (typeof n == "string") return n;
    if (this.types.length === 0) return "never";
    const r = this.types.map((s) => {
      const o = Nn$2(s);
      switch (o._tag) {
        case "Arrays": {
          const i = o.elements.filter(an$2);
          if (i.length > 0) return `${zs$2(o.isMutable)}[ ${i.map((u) => {
            var _a4;
            return t(u) + Vs$2((_a4 = u.context) == null ? void 0 : _a4.isOptional);
          }).join(", ")}, ... ]`;
          break;
        }
        case "Objects": {
          const i = o.propertySignatures.filter((u) => an$2(u.type));
          if (i.length > 0) return `{ ${i.map((u) => {
            var _a4, _b2;
            return `${zs$2((_a4 = u.type.context) == null ? void 0 : _a4.isMutable)}${Xt$2(u.name)}${Vs$2((_b2 = u.type.context) == null ? void 0 : _b2.isOptional)}: ${t(u.type)}`;
          }).join(", ")}, ... }`;
          break;
        }
      }
      return t(o);
    });
    return Array.from(new Set(r)).join(" | ");
  }
};
const Np$1 = Tn$2()({ onItem(e, t) {
  return e.recur(t)(e.oinput, e.options);
}, step(e, t, n) {
  if (n._tag === "Failure") {
    const r = On$2(n.cause);
    if (X$4(r)) return n;
    e.issues ? e.issues.push(r.success) : e.issues = [r.success];
  } else {
    if (e.out && e.ast.mode === "oneOf") return e.successes.push(t), xn$2(new qd$1(e.ast, e.input, e.successes));
    if (e.out = n.value, e.successes.push(t), e.ast.mode === "anyOf") return ai$2;
  }
} }), ka$2 = new Un$2([new Ot$2("Infinity"), new Ot$2("-Infinity"), new Ot$2("NaN")], "anyOf"), Rp$1 = new pe$5(new Un$2([Ea$2, ka$2], "anyOf"), new Ee$4(ha$2(), Ae$5((e) => globalThis.Number.isFinite(e) ? e : globalThis.String(e))));
function zs$2(e) {
  return e ? "" : "readonly ";
}
function Vs$2(e) {
  return e ? "?" : "";
}
let ln$2 = class ln extends Er$1 {
  constructor(t, n = void 0, r = false) {
    super();
    __publicField$6(this, "_tag", "Filter");
    __publicField$6(this, "run");
    __publicField$6(this, "annotations");
    __publicField$6(this, "aborted");
    this.run = t, this.annotations = n, this.aborted = r;
  }
  annotate(t) {
    return new ln(this.run, { ...this.annotations, ...t }, this.aborted);
  }
  abort() {
    return new ln(this.run, this.annotations, true);
  }
  and(t, n) {
    return new fn$2([this, t], n);
  }
};
let fn$2 = class fn extends Er$1 {
  constructor(t, n = void 0) {
    super();
    __publicField$6(this, "_tag", "FilterGroup");
    __publicField$6(this, "checks");
    __publicField$6(this, "annotations");
    this.checks = t, this.annotations = n;
  }
  annotate(t) {
    return new fn(this.checks, { ...this.annotations, ...t });
  }
  and(t, n) {
    return new fn([this, t], n);
  }
};
function Ia$2(e, t, n = false) {
  return new ln$2((r, s, o) => Zd$1(r, s, e(r, s, o)), t, n);
}
function Pp$1(e, t) {
  const n = e.source;
  return Ia$2((r) => e.test(r), { expected: `a string matching the RegExp ${n}`, meta: { _tag: "isPattern", regExp: e }, toArbitraryConstraint: { string: { patterns: [e.source] } }, ...t });
}
function Fn$2(e, t) {
  const n = Object.getOwnPropertyDescriptors(e);
  return t(n), Object.create(Object.getPrototypeOf(e), n);
}
function H$3(e, t) {
  return e.encoding === t ? e : Fn$2(e, (n) => {
    n.encoding.value = t;
  });
}
function va$2(e, t) {
  return e.context === t ? e : Fn$2(e, (n) => {
    n.context.value = t;
  });
}
function Ca$2(e, t) {
  if (e.checks) {
    const n = e.checks[e.checks.length - 1];
    return $a$2(e, Do$1(e.checks.slice(0, -1), n.annotate(t)));
  }
  return Fn$2(e, (n) => {
    n.annotations.value = { ...n.annotations.value, ...t };
  });
}
function $a$2(e, t) {
  return e.checks === t ? e : Fn$2(e, (n) => {
    n.checks.value = t;
  });
}
function Da$2(e, t) {
  return $a$2(e, e.checks ? [...e.checks, ...t] : t);
}
function Ua$2(e, t) {
  const n = e, r = n[n.length - 1], s = t(r.to);
  return s !== r.to ? Do$1(e.slice(0, e.length - 1), new pe$5(s, r.transformation)) : e;
}
function Lp$1(e) {
  return (t) => t.encoding ? H$3(t, Ua$2(t.encoding, e)) : t;
}
function jp$1(e, t, n) {
  const r = new pe$5(e, t);
  return H$3(n, n.encoding ? [...n.encoding, r] : [r]);
}
function Mp$1(e, t) {
  const n = Kd$1(e), r = n ? [...n, t] : [t];
  return Ca$2(e, { brands: r });
}
function st$1(e, t) {
  let n = false;
  const r = new Array(e.length);
  for (let s = 0; s < e.length; s++) {
    const o = e[s], i = t(o);
    i !== o && (n = true), r[s] = i;
  }
  return n ? r : e;
}
function zp$1(e, t) {
  const n = e.context ? new cn$2(e.context.isOptional, e.context.isMutable, e.context.defaultValue, { ...e.context.annotations, ...t }) : new cn$2(false, false, void 0, t);
  return va$2(e, n);
}
const Vp$1 = Lp$1(Fa$2);
function Fa$2(e) {
  const t = e.context ? e.context.isOptional === false ? new cn$2(true, e.context.isMutable, e.context.defaultValue, e.context.annotations) : e.context : new cn$2(true, false);
  return Vp$1(va$2(e, t));
}
function Jp$1(e, t, n) {
  return jp$1(e, n, t);
}
function Na$2(e) {
  switch (e._tag) {
    case "Literal":
      return { literals: gc$1(e.literal) ? [e.literal] : [], parameters: [] };
    case "UniqueSymbol":
      return { literals: [e.symbol], parameters: [] };
    case "String":
    case "Number":
    case "Symbol":
    case "TemplateLiteral":
      return { literals: [], parameters: [e] };
    case "Union": {
      const t = { literals: [], parameters: [] };
      for (let n = 0; n < e.types.length; n++) {
        const r = Na$2(e.types[n]);
        t.literals = t.literals.concat(r.literals), t.parameters = t.parameters.concat(r.parameters);
      }
      return t;
    }
  }
  return { literals: [], parameters: [] };
}
function Kp$1(e, t, n) {
  const { literals: r, parameters: s } = Na$2(e);
  return new Dn$2(r.map((o) => new rs$2(o, t)), s.map((o) => new Sa$2(o, t, n)));
}
function ve$3(e) {
  var _a3, _b2;
  return (_b2 = (_a3 = e.context) == null ? void 0 : _a3.isOptional) != null ? _b2 : false;
}
const dn$2 = De$3((e) => {
  var _a3, _b2;
  if (e.encoding) return dn$2(H$3(e, void 0));
  const t = e;
  return (_b2 = (_a3 = t.recur) == null ? void 0 : _a3.call(t, dn$2)) != null ? _b2 : t;
}), Nn$2 = De$3((e) => dn$2(Qe$2(e)));
function Bp$1(e, t) {
  const n = t, r = n.length, s = n[r - 1], o = [new pe$5(Qe$2(H$3(e, void 0)), n[0].transformation.flip())];
  for (let u = 1; u < r; u++) o.unshift(new pe$5(Qe$2(n[u - 1].to), n[u].transformation.flip()));
  const i = Qe$2(s.to);
  return i.encoding ? H$3(i, [...i.encoding, ...o]) : H$3(i, o);
}
const Qe$2 = De$3((e) => {
  var _a3, _b2, _c3, _d3;
  if (e.encoding) return Bp$1(e, e.encoding);
  const t = e;
  return (_d3 = (_c3 = (_a3 = t.flip) == null ? void 0 : _a3.call(t, Qe$2)) != null ? _c3 : (_b2 = t.recur) == null ? void 0 : _b2.call(t, Qe$2)) != null ? _d3 : t;
});
function Ra$2(e) {
  switch (e._tag) {
    case "Undefined":
      return true;
    case "Union":
      return e.types.some(Ra$2);
    default:
      return false;
  }
}
function Pa$2(e, t) {
  return e.encodedParts.map((n) => Wp$1(n, La$2(n), t)).join("");
}
const Yp$1 = De$3((e) => new globalThis.RegExp(`^${Pa$2(e, true)}$`));
function La$2(e) {
  switch (e._tag) {
    case "Literal":
      return Bd$1(globalThis.String(e.literal));
    case "String":
      return qp$1;
    case "Number":
      return ns$2;
    case "BigInt":
      return Xp$1;
    case "TemplateLiteral":
      return Pa$2(e, false);
    case "Union":
      return e.types.map(La$2).join("|");
  }
}
function Wp$1(e, t, n) {
  if (hp$1(e)) {
    if (!n) return `(?:${t})`;
  } else if (!n) return t;
  return `(${t})`;
}
function ss$2(e, t) {
  const n = Mf$1(t);
  return (r) => r._tag === "None" ? kn$2 : r.value === t ? n : j$2(new Yt$2(e, r));
}
function dt$2(e, t) {
  return (n) => n._tag === "None" ? kn$2 : t(n.value) ? $$3(n) : j$2(new Yt$2(e, n));
}
function Gp$1(e) {
  function t(n) {
    return n.encoding ? H$3(n, Ua$2(n.encoding, t)) : e(n);
  }
  return De$3(t);
}
const ja$2 = Gp$1((e) => {
  switch (e._tag) {
    default:
      return e;
    case "Number":
      return e.toCodecStringTree();
    case "Union":
      return e.recur(ja$2);
  }
}), qp$1 = "[\\s\\S]*?", Js$2 = new globalThis.RegExp(`^${ns$2}$`);
function Hp$1(e) {
  return Pp$1(Js$2, { expected: "a string representing a finite number", meta: { _tag: "isStringFinite", regExp: Js$2 }, ...e });
}
const Ma$2 = Da$2(ya$2, [Hp$1()]), Zp$1 = new pe$5(Ma$2, Qr$2), Qp$1 = new pe$5(new Un$2([Ma$2, ka$2], "anyOf"), Qr$2), Xp$1 = "-?\\d+";
function mr$1(e, t, n, r, s) {
  for (let o = 0; o < e.length; o++) {
    const i = e[o];
    if (i._tag === "FilterGroup") mr$1(i.checks, t, n, r, s);
    else {
      const u = i.run(t, r, s);
      if (u && (n.push(new Wd$1(t, i, u)), i.aborted || (s == null ? void 0 : s.errors) !== "all")) return;
    }
  }
}
const eh$1 = "~effect/Schema/Class", th$1 = "~structural", Rn$2 = (e) => e, St$2 = De$3((e) => {
  var _a3;
  switch (e._tag) {
    case "Declaration": {
      const t = (_a3 = e.annotations) == null ? void 0 : _a3[eh$1];
      if (br$1(t)) {
        const n = t(e.typeParameters), r = St$2(n.to);
        return H$3(e, r === n.to ? [n] : [new pe$5(r, n.transformation)]);
      }
      return e;
    }
    case "Objects":
    case "Arrays":
      return e.recur((t) => {
        var _a4;
        const n = (_a4 = t.context) == null ? void 0 : _a4.defaultValue;
        return n ? H$3(St$2(t), n) : St$2(t);
      });
    case "Suspend":
      return e.recur(St$2);
    default:
      return e;
  }
});
function os$2(e) {
  const t = St$2(dn$2(e.ast)), n = Va$2(t);
  return (r, s) => n(r, (s == null ? void 0 : s.disableChecks) ? (s == null ? void 0 : s.parseOptions) ? { ...s.parseOptions, disableChecks: true } : { disableChecks: true } : s == null ? void 0 : s.parseOptions);
}
function nh$1(e) {
  const t = os$2(e);
  return (n, r) => Tf$1(qf$1(t(n, r)));
}
function rh$1(e) {
  const t = os$2(e);
  return (n, r) => gi$2(In$2(t(n, r), (s) => new Error(s.toString(), { cause: s })));
}
function za$2(e) {
  return Va$2(e.ast);
}
function sh$1(e) {
  return oh$1(za$2(e));
}
function Va$2(e) {
  const t = Qt$2(e);
  return (n, r) => We$2(t(C$5(n), r != null ? r : mp$1), (s) => s._tag === "None" ? j$2(new Ut$2(s)) : $$3(s.value));
}
function oh$1(e) {
  return (t, n) => gi$2(In$2(e(t, n), (r) => new Error(r.toString(), { cause: r })));
}
const Qt$2 = De$3((e) => {
  var _a3;
  let t;
  const n = (_a3 = ia$2(e)) == null ? void 0 : _a3.parseOptions;
  if (!e.context && !e.encoding && !e.checks) return (s, o) => (t != null ? t : t = e.getParser(Qt$2), n && (o = { ...o, ...n }), t(s, o));
  const r = dp$1(e) || pp$1(e) || up$1(e) && e.typeParameters.length > 0;
  return (s, o) => {
    n && (o = { ...o, ...n });
    const i = e.encoding;
    let u;
    if (i) {
      const a = i, l = a.length;
      for (let f = l - 1; f >= 0; f--) {
        const p = a[f], E = p.to, m = Qt$2(E);
        if (u = u ? We$2(u, (y) => m(y, o)) : m(s, o), p.transformation._tag === "Transformation") {
          const y = p.transformation.decode;
          u = We$2(u, (k) => y.run(k, o));
        } else u = p.transformation.decode(u, o);
      }
      u = In$2(u, (f) => new Gd$1(e, s, f));
    }
    t != null ? t : t = e.getParser(Qt$2);
    let c = u ? We$2(u, (a) => t(a, o)) : t(s, o);
    if (e.checks && !(o == null ? void 0 : o.disableChecks)) {
      const a = e.checks;
      (o == null ? void 0 : o.errors) === "all" && r && gs$2(s) && (c = Hf$1(c, (l) => {
        const f = [];
        mr$1(a.filter((E) => {
          var _a4;
          return (_a4 = E.annotations) == null ? void 0 : _a4[th$1];
        }), s.value, f, e, o);
        const p = bs$2(f) ? l._tag === "Composite" && l.ast === e ? new fe$5(e, l.actual, [...l.issues, ...f]) : new fe$5(e, s, [l, ...f]) : l;
        return j$2(p);
      })), c = We$2(c, (l) => {
        if (gs$2(l)) {
          const f = l.value, p = [];
          if (mr$1(a, f, p, e, o), bs$2(p)) return j$2(new fe$5(e, l, p));
        }
        return $$3(l);
      });
    }
    return c;
  };
}), Ks$2 = "~effect/Schema/Schema", ih$1 = { [Ks$2]: Ks$2, pipe() {
  return ke$3(this, arguments);
}, annotate(e) {
  return this.rebuild(Ca$2(this.ast, e));
}, annotateKey(e) {
  return this.rebuild(zp$1(this.ast, e));
}, check(...e) {
  return this.rebuild(Da$2(this.ast, e));
} };
function Ja$2(e, t) {
  const n = Object.create(ih$1);
  return t && Object.assign(n, t), n.ast = e, n.rebuild = (r) => Ja$2(r, t), n.makeEffect = pc$1(os$2(n), In$2((r) => new ah$1(r))), n.make = rh$1(n), n.makeOption = nh$1(n), n;
}
const Bs$2 = "~effect/Schema/SchemaError";
_n2$1 = Bs$2;
let ah$1 = class ah {
  constructor(t) {
    __publicField$6(this, _n2$1, Bs$2);
    __publicField$6(this, "_tag", "SchemaError");
    __publicField$6(this, "name", "SchemaError");
    __publicField$6(this, "issue");
    this.issue = t;
  }
  get message() {
    return this.issue.toString();
  }
  toString() {
    return `SchemaError(${this.message})`;
  }
};
function ch$1() {
  return (e, t, n) => z$3(new Xr$2(e.map(_a$4), (r) => t(r.map((s) => z$3(s))), n));
}
function uh$1(e, t) {
  return ch$1()([], () => (n, r) => e(n) ? $$3(n) : j$2(new Yt$2(r, C$5(n))), t);
}
function Ys$2(e) {
  return wn$2(e) ? e.value : { issues: [{ message: Of$1(e.cause) }] };
}
function re$3(e, t) {
  const n = za$2(e), r = { errors: "all", ...void 0  }, s = Qd$1(), o = (i) => {
    var _a3;
    const u = new kr$2(), c = Yf$1(Kf$1(n(i, r), { onFailure: s, onSuccess: (l) => ({ value: l }) }), { scheduler: u });
    (_a3 = c.currentDispatcher) == null ? void 0 : _a3.flush();
    const a = c.pollUnsafe();
    return a ? Ys$2(a) : new Promise((l) => {
      c.addObserver((f) => {
        l(Ys$2(f));
      });
    });
  };
  if ("~standard" in e) {
    const i = e;
    return "validate" in i["~standard"] || Object.assign(i["~standard"], { validate: o }), i;
  } else return Object.assign(e, { "~standard": { version: 1, vendor: "effect", validate: o } });
}
const pt$2 = sh$1, z$3 = Ja$2, lh$1 = Rn$2((e) => z$3(Fa$2(e.ast), { schema: e })), V$4 = Rn$2((e) => lh$1(ph$1(e)));
function O$3(e) {
  const t = z$3(new Ot$2(e), { literal: e, transform(n) {
    return t.pipe(as$2(O$3(n), { decode: Ae$5(() => n), encode: Ae$5(() => e) }));
  } });
  return t;
}
const Bm$1 = z$3(_p$1), is$2 = z$3(wp$1), fh$1 = z$3(Sp$1), h$4 = z$3(ya$2), Me$3 = z$3(Ea$2), Qn$2 = z$3(Ip$1);
function Ka$2(e, t) {
  return z$3(e, { fields: t, mapFields(n, r) {
    const s = n(this.fields);
    return Ka$2(Ta$2(s, (r == null ? void 0 : r.unsafePreserveChecks) ? this.ast.checks : void 0), s);
  } });
}
function A$2(e) {
  return Ka$2(Ta$2(e, void 0), e);
}
function Pn$2(e, t, n) {
  return z$3(Kp$1(e.ast, t.ast, void 0), { key: e, value: t });
}
const dh$1 = Rn$2((e) => z$3(new es$2(false, [], [e.ast]), { schema: e }));
function Ba$2(e, t) {
  return z$3(e, { members: t, mapMembers(n, r) {
    const s = n(this.members);
    return Ba$2(xa$2(s, this.ast.mode, (r == null ? void 0 : r.unsafePreserveChecks) ? this.ast.checks : void 0), s);
  } });
}
function be$3(e, t) {
  return Ba$2(xa$2(e, "anyOf", void 0), e);
}
const ph$1 = Rn$2((e) => be$3([e, fh$1]));
function $e$3(...e) {
  return (t) => t.check(...e);
}
function Ln$2(e) {
  return (t) => z$3(Mp$1(t.ast, e), { schema: t, identifier: e });
}
function as$2(e, t) {
  return (n) => z$3(Jp$1(n.ast, e.ast, t ? ma$2(t) : cp$1()), { from: n, to: e });
}
function hh$1() {
  return (e, t) => new pe$5(e.ast, ma$2(t));
}
const cs$2 = Ia$2;
function mh$1(e) {
  var _a3;
  const t = hu$1(e.order), n = (_a3 = e.formatter) != null ? _a3 : L$3;
  return (r, s) => {
    var _a4;
    return cs$2((o) => t(o, r), { expected: `a value greater than or equal to ${n(r)}`, ...(_a4 = e.annotate) == null ? void 0 : _a4.call(e, r), ...s });
  };
}
const ot$2 = mh$1({ order: pu$1, annotate: (e) => ({ meta: { _tag: "isGreaterThanOrEqualTo", minimum: e }, toArbitraryConstraint: { number: { min: e } } }) }), Ym$1 = h$4.annotate({ expected: "a string that will be decoded as a number" }).pipe(as$2(Me$3, Qr$2)), gh$1 = uh$1((e) => Rd$1(e) && Pd$1(e), { typeConstructor: { _tag: "effect/DateTime.Utc" }, generation: { runtime: "Schema.DateTimeUtc", Type: "DateTime.Utc", importDeclaration: 'import * as DateTime from "effect/DateTime"' }, expected: "DateTime.Utc", toCodecJson: () => hh$1()(h$4, ga$2), toArbitrary: () => (e, t) => {
  var _a3;
  return e.date({ noInvalidDate: true, ...(_a3 = t == null ? void 0 : t.constraints) == null ? void 0 : _a3.date }).map((n) => jd$1(n));
}, toFormatter: () => (e) => e.toString(), toEquivalence: () => Ld$1 }), yh$1 = h$4.annotate({ expected: "a string that will be decoded as a DateTime.Utc" }).pipe(as$2(gh$1, ga$2)), Eh$1 = yh$1, Ya$2 = [".html", ".htm", ".xhtml", ".md", ".markdown", ".txt", ".log", ".csv", ".tsv", ".json", ".yaml", ".yml", ".xml", ".toml", ".js", ".ts", ".jsx", ".tsx", ".css", ".scss", ".sass", ".less", ".styl", ".php", ".py", ".rb", ".java", ".c", ".cpp", ".h", ".hpp", ".cs", ".fs", ".fsx", ".fsi", ".go", ".rs", ".swift", ".kt", ".clj", ".cljs", ".cljc", ".edn", ".lua", ".pl", ".r", ".sh", ".bash", ".zsh", ".fish", ".awk", ".ps1", ".bat", ".cmd", ".vbs", ".mp3", ".wav", ".ogg", ".flac", ".aac", ".jpg", ".jpeg", ".png", ".gif", ".bmp", ".webp", ".svg", ".tiff", ".mp4", ".mov", ".avi", ".mkv", ".webm", ".flv", ".wmv", ".zip", ".rar", ".7z", ".pdf", ".exe", ".bin", ".iso"], bh$1 = new Set(Ya$2), Sh$1 = cs$2((e) => bh$1.has(e) ? void 0 : `Expected one of: ${Ya$2.join(", ")}`), Th$1 = h$4.pipe($e$3(Sh$1)), Wa$2 = ["text/html", "application/xhtml+xml", "text/markdown", "text/plain", "text/csv", "text/tab-separated-values", "application/json", "application/x-yaml", "application/xml", "application/toml", "application/javascript", "application/typescript", "text/jsx", "text/tsx", "text/css", "text/x-scss", "text/x-sass", "text/x-clojure", "application/edn", "text/x-lua", "text/x-perl", "text/x-r", "application/x-sh", "application/x-fish", "application/x-awk", "application/x-powershell", "application/x-msdos-program", "text/vbscript", "text/x-python", "text/x-ruby", "text/x-java-source", "text/x-c", "text/x-c++src", "text/x-c++hdr", "text/x-csharp", "text/x-go", "text/rust", "text/x-swift", "text/x-kotlin", "application/x-httpd-php", "audio/mpeg", "audio/wav", "audio/ogg", "audio/flac", "audio/aac", "image/jpeg", "image/png", "image/gif", "image/bmp", "image/webp", "image/svg+xml", "image/tiff", "video/mp4", "video/quicktime", "video/x-msvideo", "video/x-matroska", "video/webm", "video/x-flv", "video/x-ms-wmv", "application/zip", "application/vnd.rar", "application/x-7z-compressed", "application/pdf", "application/vnd.microsoft.portable-executable", "application/octet-stream", "application/x-iso9660-image"], _h$1 = new Set(Wa$2), xh$1 = cs$2((e) => _h$1.has(e) ? void 0 : `Expected one of: ${Wa$2.join(", ")}`), wh$1 = h$4.pipe($e$3(xh$1)), Oh$1 = (e) => e in gr$1 ? gr$1[e] : "application/octet-stream", gr$1 = { ".html": "text/html", ".htm": "text/html", ".xhtml": "application/xhtml+xml", ".md": "text/markdown", ".markdown": "text/markdown", ".txt": "text/plain", ".log": "text/plain", ".csv": "text/csv", ".tsv": "text/tab-separated-values", ".json": "application/json", ".yaml": "application/x-yaml", ".yml": "application/x-yaml", ".xml": "application/xml", ".toml": "application/toml", ".js": "application/javascript", ".ts": "application/typescript", ".jsx": "text/jsx", ".tsx": "text/tsx", ".css": "text/css", ".scss": "text/x-scss", ".sass": "text/x-sass", ".less": "text/css", ".styl": "text/plain", ".php": "application/x-httpd-php", ".py": "text/x-python", ".rb": "text/x-ruby", ".java": "text/x-java-source", ".c": "text/x-c", ".cpp": "text/x-c++src", ".h": "text/x-c", ".hpp": "text/x-c++hdr", ".cs": "text/x-csharp", ".fs": "text/plain", ".fsx": "text/plain", ".fsi": "text/plain", ".go": "text/x-go", ".rs": "text/rust", ".swift": "text/x-swift", ".kt": "text/x-kotlin", ".clj": "text/x-clojure", ".cljs": "text/x-clojure", ".cljc": "text/x-clojure", ".edn": "application/edn", ".lua": "text/x-lua", ".pl": "text/x-perl", ".r": "text/x-r", ".sh": "application/x-sh", ".bash": "application/x-sh", ".zsh": "application/x-sh", ".fish": "application/x-fish", ".awk": "application/x-awk", ".ps1": "application/x-powershell", ".bat": "application/x-msdos-program", ".cmd": "application/x-msdos-program", ".vbs": "text/vbscript", ".mp3": "audio/mpeg", ".wav": "audio/wav", ".ogg": "audio/ogg", ".flac": "audio/flac", ".aac": "audio/aac", ".jpg": "image/jpeg", ".jpeg": "image/jpeg", ".png": "image/png", ".gif": "image/gif", ".bmp": "image/bmp", ".webp": "image/webp", ".svg": "image/svg+xml", ".tiff": "image/tiff", ".mp4": "video/mp4", ".mov": "video/quicktime", ".avi": "video/x-msvideo", ".mkv": "video/x-matroska", ".webm": "video/webm", ".flv": "video/x-flv", ".wmv": "video/x-ms-wmv", ".zip": "application/zip", ".rar": "application/vnd.rar", ".7z": "application/x-7z-compressed", ".pdf": "application/pdf", ".exe": "application/vnd.microsoft.portable-executable", ".bin": "application/octet-stream", ".iso": "application/x-iso9660-image" }, Ga$2 = re$3(A$2({ page: Me$3.pipe($e$3(ot$2(1))), perPage: V$4(Me$3.pipe($e$3(ot$2(1)))) })), qa$2 = re$3(A$2({ before: V$4(h$4), perPage: V$4(Me$3.pipe($e$3(ot$2(1)))) })), Ha$2 = re$3(A$2({ after: V$4(h$4), perPage: V$4(Me$3.pipe($e$3(ot$2(1)))) })), Za$2 = re$3(A$2({ offset: Me$3.pipe($e$3(ot$2(0))), limit: V$4(Me$3.pipe($e$3(ot$2(1)))) })), Ah$1 = be$3([Ga$2, qa$2, Ha$2, Za$2]), ue$4 = { Owner: "owner", Admin: "admin", Editor: "editor", Author: "author", Guest: "guest" }, kh$1 = { [ue$4.Owner]: "Is the owner of the project. Can manage all settings and content.", [ue$4.Admin]: "Full access to all project settings and content. Can manage users and their roles.", [ue$4.Editor]: "Can create, edit, and publish all types of content. Cannot modify project settings or manage users.", [ue$4.Author]: "Can create and edit their own content, but cannot publish without approval from an Editor or Admin.", [ue$4.Guest]: "Read-only access to published content. Cannot create, edit, or publish any content." };
var v$4;
(function(e) {
  e.ManageSettings = "manage_settings", e.ManageUsers = "manage_users", e.CreateDocument = "create_document", e.EditDocument = "edit_document", e.PublishDocument = "publish_document", e.DeleteDocument = "delete_document", e.ViewDocument = "view_document", e.ManageCollections = "manage_collections", e.ViewCollection = "view_collection";
})(v$4 || (v$4 = {}));
const Ih$1 = { [v$4.ManageSettings]: "Allows users to modify and manage system or application settings, including configurations that affect the entire platform.", [v$4.ManageUsers]: "Grants the ability to manage user accounts, including creating, editing, and deleting users, as well as assigning roles and permissions.", [v$4.CreateDocument]: "Allows users to create new documents in the system.", [v$4.EditDocument]: "Grants the ability to edit or modify existing documents.", [v$4.PublishDocument]: "Enables users to publish documents, making them publicly available or accessible to others.", [v$4.DeleteDocument]: "Provides the ability to permanently delete documents from the system.", [v$4.ViewDocument]: "Grants permission to view documents, but without the ability to edit, publish, or delete them.", [v$4.ManageCollections]: "Allows users to create, edit, or delete collections of documents, as well as configure how collections are organized.", [v$4.ViewCollection]: "Grants read-only access to view collections of documents, but without the ability to modify them." }, Qa$2 = [v$4.ViewDocument, v$4.ViewCollection], Xa$2 = [...Qa$2, v$4.CreateDocument, v$4.EditDocument, v$4.PublishDocument], ec$1 = [...Xa$2, v$4.DeleteDocument, v$4.ManageCollections], tc$1 = [...ec$1, v$4.ManageSettings, v$4.ManageUsers], vh$1 = [...tc$1], Ch$1 = { [ue$4.Owner]: vh$1, [ue$4.Admin]: tc$1, [ue$4.Editor]: ec$1, [ue$4.Author]: Xa$2, [ue$4.Guest]: Qa$2 }, $h$1 = (e) => {
  let t = null;
  return () => (t || (t = e()), t);
}, Dh$1 = (e) => {
  let t = null;
  return async () => (t || (t = await e()), t);
}, Uh$1 = { toArray: async (e) => {
  const t = [];
  for await (const n of e) t.push(n);
  return t;
}, first: async (e) => {
  for await (const t of e) return t;
}, accumulateFirst: async (e) => {
  const t = [];
  for await (const n of e) {
    if (jt$2(n)) return lt$2(n.success);
    X$4(n) && t.push(n.failure);
  }
  return we$5(t);
} }, Fh$1 = (e) => {
  let t;
  return (n) => {
    if ((t == null ? void 0 : t[0]) === n) return t[1];
    const r = e(n);
    return t = [n, r], r;
  };
}, Ie$3 = { isAbsolute: (e) => typeof e == "string" && /^[a-zA-Z][a-zA-Z\d+\-.]*:/.test(e), join: (e, t) => (e = Ie$3.normalize(e), t = Ie$3.normalize(t), e ? t ? Ie$3.isAbsolute(t) ? t : e + t : e : t != null ? t : ""), normalize: (e) => (e && e.endsWith("/") && (e = e.slice(0, -1)), Ie$3.isAbsolute(e) || e && !e.startsWith("/") && (e = "/" + e), e), combine: (...e) => e.reduce((t, n) => Ie$3.join(t, n), "") || "" }, nc$1 = { pathToSegments: (e) => e.split("/").map((n) => n.trim()).filter((n) => n.length > 0), toSegments: (e) => nc$1.pathToSegments(e), combine: (...e) => e.map((n) => n.trim()).filter((n) => n.length > 0).join("/") }, Nh$1 = { url: (e, ...t) => e.reduce((r, s, o) => {
  if (o === t.length) return Ie$3.join(r, s);
  const i = String(t[o]);
  return Ie$3.combine(r, s, i);
}, "") }, yr$1 = { AuthorizationHeaderBearerRegex: /^Bearer\s+([A-Za-z0-9\-._~+/]+={0,2})$/, AuthorizationHeaderApiKeyRegex: /^ApiKey\s+([A-Za-z0-9\-._~+/]+={0,2})$/, ExtractAuthorizationBearerToken: (e) => {
  if (!e) return;
  const t = e.match(yr$1.AuthorizationHeaderBearerRegex);
  return t ? t[1] : void 0;
}, ExtractAuthorizationApiKey: (e) => {
  if (!e) return;
  const t = e.match(yr$1.AuthorizationHeaderApiKeyRegex);
  return t ? t[1] : void 0;
} }, jn$2 = "RecoverableError", rc$1 = "Progress", Rh$1 = (e) => ({ _tag: jn$2, error: e }), Ph$1 = (e) => ({ _tag: rc$1, progress: e }), Lh$1 = (e) => G$2(Cn$2(e)), jh$1 = (e) => G$2(Ui$2(e)), Mh$1 = (e) => sc$1((t) => ne$4(function* () {
  const { value: n, recoverableErrors: r } = yield* e;
  if (r) for (const s of r) yield* t.recoverableError(s);
  return n;
})), sc$1 = (e, t) => {
  var _a3;
  const n = (_a3 = t == null ? void 0 : t.capacity) != null ? _a3 : 16;
  return G$2(Je$2((r, s) => ne$4(function* () {
    const o = yield* rn$2(n), u = e({ recoverableError: (c) => Ye$2(_e$7(o, Rh$1(c))), progress: (c) => Ye$2(_e$7(o, Ph$1(c))) }).pipe(pi$2({ onSuccess: (c) => sn$2(o, ui$2(c)), onFailure: (c) => sn$2(o, c) }));
    return yield* xt$2(u, s), Oi$2(o);
  })));
}, oc$1 = (e) => {
  const t = [], n = [];
  return Kt$2(Mn$2(e, (r) => {
    for (const s of r) s._tag === jn$2 ? t.push(s.error) : n.push(s.progress);
    return Vt$2;
  }), (r) => ({ recoverableErrors: t, progress: n, value: r }));
}, us$2 = (e) => Mn$2(e, () => Vt$2), zh$1 = (e, t) => Mn$2(e, (n) => ne$4(function* () {
  for (const r of n) r._tag === jn$2 ? yield* t.recoverableError(r.error) : yield* t.progress(r.progress);
})), Vh$1 = (e) => xe$5(us$2(e)), Jh$1 = (e) => xe$5(oc$1(e)), Kh$1 = (e) => xe$5(di$2(us$2(e))), Mn$2 = (e, t) => mi$2(ne$4(function* () {
  const n = yield* Pi$2(e);
  for (; ; ) {
    const r = yield* tn$2(n);
    if (wn$2(r)) {
      yield* t(r.value);
      continue;
    }
    const s = An$2(r.cause);
    return jt$2(s) ? s.success : yield* Jt$2(s.failure);
  }
})), Bh$1 = (e, t) => G$2(Ni$2(e, t)), Yh$1 = (e, t) => G$2($n$2(e, (n) => ne$4(function* () {
  for (const r of n) r._tag === jn$2 && (yield* t(r.error));
}))), Wh$1 = (e, t) => G$2($n$2(e, (n) => ne$4(function* () {
  for (const r of n) r._tag === rc$1 && (yield* t(r.progress));
}))), Gh$1 = Object.freeze(Object.defineProperty({ __proto__: null, drainWithValue: Mn$2, fail: jh$1, fromEffect: Mh$1, make: sc$1, map: Bh$1, runCollect: oc$1, runPromise: Vh$1, runPromiseCollect: Jh$1, runPromiseResult: Kh$1, runValue: us$2, runValueForwarding: zh$1, succeed: Lh$1, tapProgress: Wh$1, tapRecoverableErrors: Yh$1 }, Symbol.toStringTag, { value: "Module" })), qh$1 = Object.freeze(Object.defineProperty({ __proto__: null, AsyncGenerator: Uh$1, AuthenticationError: qi$2, AuthorizationError: Gi$2, AuthorizerFailureError: Xi$2, BadRequestError: zi$2, ConflictError: Hi$2, CorruptedFileError: na$2, DangerousFileTypeError: ta$2, DirInsteadOfFile: Ji$2, EmbeddedContentError: ra$2, EntryAlreadyExistsError: Wi$2, ErrorClasses: oa$2, ErrorCodeToClassMap: Nd$1, ErrorCodeToKeyMap: $d$1, ErrorCodeToStatusMap: Dd$1, ExtNameSchema: Th$1, FileInsteadOfDir: Ki$2, FileTooLargeError: sa$2, ForbiddenError: Vi$2, GatewayTimeoutError: Qi$2, Header: yr$1, IllegalStateException: ji$2, InternalError: Ge$2, InvalidData: Bi$2, IsoDateWithFallbackSchema: Eh$1, LaikaElement: Zf$1, LaikaError: b$4, LaikaStream: pm$1, LaikaTask: Gh$1, MimeTypeSchema: wh$1, NotFoundError: Mi$2, NotImplementedError: Li$2, PaginationAfterSchema: Ha$2, PaginationBeforeSchema: qa$2, PaginationOffsetSchema: Za$2, PaginationPageBasedSchema: Ga$2, PaginationSchema: Ah$1, Paths: nc$1, get Permission() {
  return v$4;
}, Role: ue$4, RoleDescriptions: kh$1, ServiceUnavailableError: qe$5, TemplateLiteral: Nh$1, TooManyRequestsError: Zi$2, UnknownError: nt$2, UnsupportedFileTypeError: ea$2, Url: Ie$3, ValidationError: Hr$2, VersionMismatchError: Yi$2, errorCode: S$2, errorStatus: T$2, extNameToMimeType: Oh$1, lazy: $h$1, lazyAsync: Dh$1, memoize: Fh$1, mimeTypeMapper: gr$1, permissionDescriptions: Ih$1, rolePermissions: Ch$1 }, Symbol.toStringTag, { value: "Module" })), G$2 = (e) => Hh$1(e), Hh$1 = (e) => (Object.defineProperty(e, Symbol.asyncIterator, { value: () => {
  const t = ct$2();
  let n, r, s = false;
  const o = async () => n || (n = await xe$5(gt$2(xf$1(), t)), n), i = async () => {
    if (r) return r;
    const a = await o();
    return r = await xe$5(gt$2(vd$1(e, a), t)), r;
  }, u = async (a) => {
    if (!n) return;
    const l = n;
    n = void 0;
    try {
      await xe$5(gt$2(jr$2(l, a), t));
    } catch {
    }
  }, c = { async next() {
    if (s) return { value: void 0, done: true };
    let a;
    try {
      const p = await i();
      a = await Gf$1(gt$2(p, t));
    } catch (p) {
      throw s = true, await u(Ht$2(void 0)), p instanceof b$4 ? p : new nt$2(Ws$2(p), { cause: p });
    }
    if (wn$2(a)) return { value: a.value, done: false };
    const l = An$2(a.cause);
    if (jt$2(l)) return s = true, await u(Ht$2(void 0)), { value: l.success, done: true };
    s = true, await u(a);
    const f = Zh$1(l.failure);
    throw f instanceof b$4 ? f : new nt$2(Ws$2(f), { cause: f });
  }, async return(a) {
    return s || (s = true, await u(Ht$2(void 0))), { value: a, done: true };
  }, async throw(a) {
    throw s || (s = true, await u(Lr$2(wf$1(a)))), a;
  } };
  return c[Symbol.asyncIterator] = () => c, c;
}, enumerable: false, configurable: false, writable: false }), e), Ws$2 = (e) => e instanceof Error ? e.message : typeof e == "string" ? e : "LaikaStream iteration failed", Zh$1 = (e) => {
  for (const t of e.reasons) if (t._tag === "Fail") return t.error;
  for (const t of e.reasons) if (t._tag === "Die") return t.defect;
  return e;
}, ic$1 = (e) => G$2(Cn$2(e)), Qh$1 = (e, t) => G$2(Ri$2(Di$2([tt$2(e)]), Cn$2(t))), Xh$1 = (e, t) => e.length === 0 ? ic$1(t) : G$2(Ri$2(Di$2(e.map(tt$2)), Cn$2(t))), em$1 = (e) => G$2(Ui$2(e)), tm$1 = (e) => ls$2((t) => ne$4(function* () {
  const { value: n, done: r, recoverableErrors: s } = yield* e;
  if (s) for (const o of s) yield* t.recoverableError(o);
  return yield* t.data(n), r;
})), ls$2 = (e, t) => {
  var _a3;
  const n = (_a3 = t == null ? void 0 : t.capacity) != null ? _a3 : 16;
  return G$2(Je$2((r, s) => ne$4(function* () {
    const o = yield* rn$2(n), u = e({ data: (c) => Ye$2(_e$7(o, tt$2(c))), recoverableError: (c) => Ye$2(_e$7(o, Si$2(c))), progress: (c) => Ye$2(_e$7(o, Ti$2(c))), dataMany: (c) => Ye$2(ud$1(o, c.map(tt$2))) }).pipe(pi$2({ onSuccess: (c) => sn$2(o, ui$2(c)), onFailure: (c) => sn$2(o, c) }));
    return yield* xt$2(u, s), Oi$2(o);
  })));
}, nm$1 = (e, t) => ls$2((n) => ne$4(function* () {
  let r = e, s = 0, o;
  for (; ; ) {
    yield* n.progress({ stage: "paginate", current: s, message: r === void 0 ? "first page" : `cursor: ${r}` });
    const i = yield* t(r);
    if (i.items.length > 0 && (yield* n.dataMany(i.items)), o = i.done, s += 1, i.nextCursor === void 0) break;
    r = i.nextCursor;
  }
  return o;
})), zn$2 = (e, t) => mi$2(ne$4(function* () {
  const n = yield* Pi$2(e);
  for (; ; ) {
    const r = yield* tn$2(n);
    if (wn$2(r)) {
      yield* t(r.value);
      continue;
    }
    const s = An$2(r.cause);
    return jt$2(s) ? s.success : yield* Jt$2(s.failure);
  }
})), fs$4 = (e) => {
  const t = [], n = [], r = [];
  return Kt$2(zn$2(e, (s) => {
    for (const o of s) Bt$2(o) ? t.push(o.value) : vn$2(o) ? n.push(o.error) : r.push(o.progress);
    return Vt$2;
  }), (s) => ({ data: t, recoverableErrors: n, progress: r, done: s }));
}, ac$1 = (e) => zn$2(e, () => Vt$2), rm$1 = (e, t) => {
  const n = [];
  return Kt$2(zn$2(e, (r) => ne$4(function* () {
    for (const s of r) Bt$2(s) ? n.push(s.value) : vn$2(s) ? yield* t.recoverableError(s.error) : yield* t.progress(s.progress);
  })), (r) => ({ data: n, done: r }));
}, sm$1 = (e) => xe$5(ac$1(e)), om$1 = (e) => xe$5(fs$4(e)), im$1 = (e) => xe$5(di$2(fs$4(e))), am$1 = (e, t) => $n$2(e, (n) => ne$4(function* () {
  for (const r of n) vn$2(r) && (yield* t(r.error));
})), cm$1 = (e, t) => $n$2(e, (n) => ne$4(function* () {
  for (const r of n) _i$4(r) && (yield* t(r.progress));
})), um$1 = (e, t) => G$2(Fi$2(e, (n) => {
  const r = new Array(n.length);
  for (let s = 0; s < n.length; s++) {
    const o = n[s];
    r[s] = Bt$2(o) ? tt$2(t(o.value)) : o;
  }
  return r;
})), lm$1 = (e, t) => G$2(Fi$2(e, (n) => {
  const r = new Array(n.length);
  for (let s = 0; s < n.length; s++) r[s] = t(n[s]);
  return r;
})), fm$1 = (e, t) => G$2(Id$1(e, (n) => Bt$2(n) ? t(n.value) : true)), dm$1 = (e, t) => G$2(Ni$2(e, t)), pm$1 = Object.freeze(Object.defineProperty({ __proto__: null, drainWithDone: zn$2, empty: ic$1, fail: em$1, filter: fm$1, fromEffect: tm$1, make: ls$2, mapData: um$1, mapDone: dm$1, mapElements: lm$1, paginate: nm$1, runCollect: fs$4, runCollectForwarding: rm$1, runDone: ac$1, runPromise: sm$1, runPromiseCollect: om$1, runPromiseResult: im$1, succeed: Qh$1, succeedMany: Xh$1, tapProgress: cm$1, tapRecoverableErrors: am$1 }, Symbol.toStringTag, { value: "Module" })), hm$1 = h$4.pipe(Ln$2("StorageFormat")), mm$1 = h$4.pipe(Ln$2("StorageRepositoryCompatibilityDate"));
dh$1(h$4);
const cc$1 = A$2({ supported: O$3(false), description: h$4 }), gm$1 = A$2({ supported: O$3(true), description: h$4, supportedExtensions: Pn$2(h$4, A$2({ format: hm$1 })) }), ym$1 = A$2({ supported: O$3(true), description: h$4, styles: A$2({ offset: Qn$2, page: Qn$2, cursor: Qn$2 }) }), ds$2 = be$3([cc$1, ym$1]);
re$3(A$2({ compatibilityDate: mm$1, fileExtensions: be$3([cc$1, gm$1]), pagination: ds$2 }));
const Em$1 = h$4.pipe(Ln$2("AssetsRepositoryCompatibilityDate"));
re$3(A$2({ compatibilityDate: Em$1, pagination: ds$2 }));
let Wm$1 = class Wm {
};
const bm$1 = h$4.pipe(Ln$2("DocumentsRepositoryCompatibilityDate"));
re$3(A$2({ compatibilityDate: bm$1, pagination: ds$2 }));
let Gm$1 = class Gm {
};
var qm$1 = typeof globalThis < "u" ? globalThis : typeof global < "u" ? global : typeof self < "u" ? self : {};
function Hm$1(e) {
  return e && e.__esModule && Object.prototype.hasOwnProperty.call(e, "default") ? e.default : e;
}
function me$3(e, t, n) {
  const { [n]: r, ...s } = e;
  return { type: t, id: r, attributes: s };
}
function Te$1(e, t, n) {
  return { [n]: e.id, ...e.attributes };
}
const uc$1 = (e) => {
  console.error("errorToJsonApiMapper():", e);
  const t = typeof e == "string" ? new nt$2() : e;
  return typeof e == "object" && e !== null && "_tag" in e && e._tag === "ParseError" ? { errors: e.issues.map((r) => ({ status: "400", title: "Validation Error", code: "validation_error", detail: r.message, source: r.path.length > 0 ? { pointer: "/" + r.path.join("/") } : void 0 })), status: Hr$2.STATUS } : e instanceof b$4 ? { errors: [{ title: e.title, code: e.code, status: "" + e.status, detail: e.message }], status: e.status } : wu$1(e) && X$4(e) ? uc$1(e.failure) : typeof t == "object" && t !== null && ("name" in t && t.name === "NetworkingError" || "name" in t && t.name === "TimeoutError") ? { errors: [{ status: "" + qe$5.STATUS, code: qe$5.CODE, title: qe$5.TITLE, detail: "Cannot connect to a required service." }], status: qe$5.STATUS } : { errors: [{ status: "" + Ge$2.STATUS, code: Ge$2.CODE, title: Ge$2.TITLE, detail: "Internal Server Error" }], status: Ge$2.STATUS };
}, Zm$1 = (e) => {
  if (e.length !== 0) return e.map((t) => uc$1(t).errors[0]);
}, Sm$1 = (e) => {
  const t = { ...qh$1, LaikaError: void 0 }, n = Object.values(t).find((r) => typeof r == "function" && "CODE" in r && r.CODE === e);
  return !n || n === b$4 ? nt$2 : n;
}, Tm$1 = (e) => {
  const t = Sm$1(e.code);
  return new t(e.detail);
}, Qm$1 = (e) => {
  if (!e || typeof e != "object") return [];
  const t = e.warnings;
  return Array.isArray(t) ? t.filter((n) => !!n && typeof n == "object" && typeof n.code == "string").map(Tm$1) : [];
}, Xm$1 = (e) => e instanceof b$4, eg$1 = (e) => me$3(e, "published", "key"), tg$1 = (e) => Te$1(e, "published", "key"), ng$1 = (e) => me$3(e, "published", "key"), rg$1 = (e) => Te$1(e, "published", "key"), sg$1 = (e) => me$3(e, "published", "key"), og$1 = (e) => me$3(e, "published-summary", "key"), ig$1 = (e) => Te$1(e, "published-summary", "key"), ag$1 = (e) => me$3(e, "unpublished", "key"), cg$1 = (e) => Te$1(e, "unpublished", "key"), ug$1 = (e) => me$3(e, "unpublished", "key"), lg$1 = (e) => Te$1(e, "unpublished", "key"), fg$1 = (e) => me$3(e, "unpublished", "key"), dg$1 = (e) => Te$1(e, "unpublished", "key"), pg$1 = (e) => me$3(e, "unpublished-summary", "key"), hg$1 = (e) => Te$1(e, "unpublished-summary", "key"), mg$1 = (e) => me$3(e, "revision", "key"), gg$1 = (e) => Te$1(e, "revision", "key"), yg$1 = (e) => me$3(e, "revision", "key"), Eg$1 = (e) => Te$1(e, "revision", "key"), bg$1 = (e) => me$3(e, "revision-summary", "key"), Sg$1 = (e) => Te$1(e, "revision-summary", "key"), _m$1 = re$3(A$2({ type: O$3("published"), id: h$4, attributes: A$2({ type: O$3("published"), status: O$3("published"), content: Pn$2(h$4, is$2), createdAt: V$4(h$4), updatedAt: V$4(h$4) }) })), xm$1 = re$3(A$2({ type: be$3([O$3("published"), O$3("published-summary")]), id: h$4, attributes: A$2({ type: be$3([O$3("published-summary"), O$3("published")]), status: O$3("published"), createdAt: V$4(h$4), updatedAt: V$4(h$4) }) })), wm$1 = re$3(A$2({ type: O$3("unpublished"), id: h$4, attributes: A$2({ type: O$3("unpublished"), status: h$4, content: Pn$2(h$4, is$2), createdAt: V$4(h$4), updatedAt: V$4(h$4) }) })), Om$1 = re$3(A$2({ type: be$3([O$3("unpublished"), O$3("unpublished-summary")]), id: h$4, attributes: A$2({ type: be$3([O$3("unpublished-summary"), O$3("unpublished")]), status: h$4, createdAt: V$4(h$4), updatedAt: V$4(h$4) }) })), Am$1 = re$3(A$2({ type: O$3("revision"), id: h$4, attributes: A$2({ type: O$3("revision"), revision: h$4, content: Pn$2(h$4, is$2), createdAt: h$4, updatedAt: V$4(h$4) }) })), km$1 = re$3(A$2({ type: be$3([O$3("revision"), O$3("revision-summary")]), id: h$4, attributes: A$2({ type: be$3([O$3("revision-summary"), O$3("revision")]), revision: h$4, createdAt: h$4, updatedAt: V$4(h$4) }) }));
pt$2(_m$1);
pt$2(xm$1);
pt$2(wm$1);
pt$2(Om$1);
pt$2(Am$1);
pt$2(km$1);

const e$1 = [{ name: "posts", label: "Blog Posts", folder: "posts", create: true, slug: "{{slug}}", sortable_fields: ["title", "date"], fields: [{ label: "Title", name: "title", widget: "string" }, { label: "Date", name: "date", widget: "datetime" }, { label: "Description", name: "description", widget: "string", required: false }, { label: "Body", name: "body", widget: "markdown" }] }];

var __defProp$5 = Object.defineProperty;
var __typeError$1 = (msg) => {
  throw TypeError(msg);
};
var __defNormalProp$5 = (obj, key, value) => key in obj ? __defProp$5(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField$5 = (obj, key, value) => __defNormalProp$5(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck$1 = (obj, member, msg) => member.has(obj) || __typeError$1("Cannot " + msg);
var __privateGet$1 = (obj, member, getter) => (__accessCheck$1(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd$1 = (obj, member, value) => member.has(obj) ? __typeError$1("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet$1 = (obj, member, value, setter) => (__accessCheck$1(obj, member, "write to private field"), member.set(obj, value), value);
var __privateMethod$1 = (obj, member, method) => (__accessCheck$1(obj, member, "access private method"), method);
var _n$3, _i$3, _r$3, _e$6, _t$3, _a2$2, _c$2, _l$2, _s$3, _o$2, _u$2, _AST_instances$1, y_fn$1, _AST_static$1, f_fn$1, $_fn$1, h_fn$1, p_fn$1, S_fn$1, m_fn$1, E_fn$1, A_fn$1, b_fn$1, d_fn$1, g_fn$1, w_fn$1, _Minimatch_instances$1, n_fn$1, i_fn$1, r_fn$1, _a3$1;
const runTask$1$1 = (e5, t) => {
  const { onProgress: n } = {};
  return n ? xe$5(Mn$2(e5, (r) => {
    for (const s of r) n(s);
    return Vt$2;
  })) : Vh$1(e5);
}, collectStream$1 = async (e5, t) => {
  const { onProgress: n } = {};
  if (!n) {
    const o = [], a = await xe$5(zn$2(e5, (l) => {
      for (const c of l) Bt$2(c) && o.push(c.value);
      return Vt$2;
    }));
    return { items: o, done: a };
  }
  const r = [], s = await xe$5(zn$2(e5, (o) => {
    for (const a of o) Bt$2(a) ? r.push(a.value) : n(a);
    return Vt$2;
  }));
  return { items: r, done: s };
}, defaultDetermineExtension$1 = (e5, { metadata: t, defaultExtension: n }) => {
  var _a4;
  return (_a4 = t == null ? void 0 : t.extension) != null ? _a4 : n;
};
let StorageRepository$1 = class StorageRepository {
};
const pathToSegments$1 = (e5) => e5.split("/").map((n) => n.trim()).filter((n) => n.length > 0), pathCombine$1 = (...e5) => e5.map((n) => n.trim()).filter((n) => n.length > 0).join("/"), basename$1 = (e5) => {
  if (typeof e5 != "string" || e5.length === 0) return "";
  let t = e5.length - 1;
  for (; t > 0 && (e5[t] === "/" || e5[t] === "\\"); ) t--;
  if (t === 0 && (e5[0] === "/" || e5[0] === "\\")) return e5[0];
  let n = t;
  for (; n >= 0 && e5[n] !== "/" && e5[n] !== "\\"; ) n--;
  return e5.slice(n + 1, t + 1);
}, naturalCollator$1 = new Intl.Collator(void 0, { numeric: true, sensitivity: "base" }), naturalCompare$1 = (e5, t) => naturalCollator$1.compare(e5, t), applyPagination$1 = (e5, t) => {
  var _a4, _b;
  if (!t) return [...e5];
  if ("offset" in t) {
    const n = (_a4 = t.limit) != null ? _a4 : e5.length;
    return e5.slice(t.offset, t.offset + n);
  }
  if ("page" in t) {
    const n = (_b = t.perPage) != null ? _b : e5.length, r = Math.max(0, (t.page - 1) * n);
    return e5.slice(r, r + n);
  }
  return [...e5];
}, liftPromiseResult$1$1 = (e5) => K$2(Dm$1(() => e5), Nm$1);
function bytesToBase64$1(e5) {
  let t = "";
  for (let n = 0; n < e5.length; n++) t += String.fromCharCode(e5[n]);
  return btoa(t);
}
async function consumeBinary$1(e5) {
  if (e5 instanceof Uint8Array) return e5;
  if (e5 instanceof ArrayBuffer) return new Uint8Array(e5);
  const t = e5.getReader(), n = [];
  let r = 0;
  for (; ; ) {
    const { done: a, value: l } = await t.read();
    if (a) break;
    n.push(l), r += l.byteLength;
  }
  const s = new Uint8Array(r);
  let o = 0;
  for (const a of n) s.set(a, o), o += a.byteLength;
  return s;
}
let ContentBaseAssetsRepository$1 = class ContentBaseAssetsRepository extends Wm$1 {
  constructor(t, n) {
    super();
    __publicField$5(this, "storageRepository");
    __publicField$5(this, "settingsProvider");
    this.storageRepository = t, this.settingsProvider = n;
  }
  getCapabilities() {
    return sc$1((t) => ne$4({ self: this }, function* () {
      const n = yield* zh$1(this.storageRepository.getCapabilities(), t);
      return { compatibilityDate: Em$1.make("2026-05-11"), pagination: n.pagination };
    }));
  }
  parseKey(t) {
    const n = pathToSegments$1(t);
    if (n.length === 0) return { collection: "", remainder: "" };
    const [r, ...s] = n;
    return { collection: r, remainder: s.length > 0 ? pathCombine$1(...s) : "" };
  }
  async resolveCollection(t) {
    var _a4;
    const n = await this.settingsProvider.getMediaCollectionSettings(t);
    if (X$4(n)) return we$5(n.failure);
    const r = (_a4 = n.success.directory) != null ? _a4 : t;
    return lt$2({ directory: r, settings: n.success });
  }
  async getAssetPath(t) {
    const { collection: n, remainder: r } = this.parseKey(t);
    if (!n) return we$5(new zi$2(`Asset key '${t}' is missing a collection prefix`));
    const s = await this.resolveCollection(n);
    if (X$4(s)) return we$5(s.failure);
    const { directory: o, settings: a } = s.success, l = r ? pathCombine$1(o, r) : o;
    return lt$2({ physical: l, directory: o, collection: n, settings: a });
  }
  extractKeyFromPath(t, n, r) {
    const s = pathToSegments$1(t), o = pathToSegments$1(n);
    let a = s;
    return o.length <= s.length && o.every((l, c) => l === s[c]) && (a = s.slice(o.length)), a.length > 0 ? pathCombine$1(r, ...a) : r;
  }
  renderUrlTemplate(t, n) {
    const r = pathToSegments$1(n), s = r.length > 0 ? r[r.length - 1] : n;
    return t.replace(/\{key\}/g, n).replace(/\{filename\}/g, s).replace(/\{path\}/g, n);
  }
  toAssetContent(t) {
    const n = { size: typeof t.size == "number" ? t.size : 0, contentType: typeof t.mimeType == "string" ? t.mimeType : "application/octet-stream" };
    return t.customMetadata && typeof t.customMetadata == "object" && (n.customMetadata = t.customMetadata), n;
  }
  atomToResource(t, n, r) {
    const s = this.extractKeyFromPath(t.key, n, r);
    return t.type === "folder" ? { ...t, key: s } : { type: "asset", key: s, createdAt: t.createdAt, updatedAt: t.updatedAt, content: this.toAssetContent(t.content) };
  }
  summaryToResource(t, n, r) {
    const s = this.extractKeyFromPath(t.key, n, r);
    return t.type === "folder-summary" ? { type: "folder", key: s, createdAt: t.createdAt, updatedAt: t.updatedAt } : { type: "asset", key: s, createdAt: t.createdAt, updatedAt: t.updatedAt, content: {} };
  }
  getResource(t, n) {
    return sc$1((r) => ne$4({ self: this }, function* () {
      const s = yield* liftPromiseResult$1$1(this.getAssetPath(t)), o = yield* zh$1(this.storageRepository.getAtom(s.physical), r);
      return [this.atomToResource(o, s.directory, s.collection)];
    }));
  }
  listResources(t, n) {
    return ls$2((r) => ne$4({ self: this }, function* () {
      const { collection: s, remainder: o } = this.parseKey(t);
      if (!s) return yield* j$2(new zi$2(`folderKey '${t}' is missing a collection prefix`));
      const a = yield* liftPromiseResult$1$1(this.resolveCollection(s)), l = o ? pathCombine$1(a.directory, o) : a.directory, c = yield* Kt$2(rm$1(this.storageRepository.listAtomSummaries(l, { pagination: n.pagination, depth: n.depth }), r), (p) => p.data);
      let u = 0;
      for (const p of c) yield* r.data(this.summaryToResource(p, a.directory, s)), u += 1;
      return { total: u };
    }));
  }
  getAsset(t, n) {
    return sc$1((r) => ne$4({ self: this }, function* () {
      const s = yield* liftPromiseResult$1$1(this.getAssetPath(t)), o = yield* zh$1(this.storageRepository.getObject(s.physical), r);
      return { type: "asset", key: t, createdAt: o.createdAt, updatedAt: o.updatedAt, content: this.toAssetContent(o.content) };
    }));
  }
  createAsset(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* liftPromiseResult$1$1(this.getAssetPath(t.key));
      if (r.settings.accept && r.settings.accept.length > 0 && !r.settings.accept.includes(t.mimeType)) return yield* j$2(new zi$2(`MIME type '${t.mimeType}' is not allowed in collection '${r.settings.key}'. Allowed: ${r.settings.accept.join(", ")}`));
      const s = yield* Dm$1(() => consumeBinary$1(t.content)), o = { data: bytesToBase64$1(s), mimeType: t.mimeType, size: s.byteLength };
      t.filename && (o.filename = t.filename), t.customMetadata && (o.customMetadata = t.customMetadata), t.cacheControl && (o.cacheControl = t.cacheControl);
      const a = yield* zh$1(this.storageRepository.createOrUpdateObject({ type: "object", key: r.physical, content: o }), n);
      return { type: "asset", key: t.key, createdAt: a.createdAt, updatedAt: a.updatedAt, content: this.toAssetContent(o) };
    }));
  }
  updateAsset(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* liftPromiseResult$1$1(this.getAssetPath(t.key)), o = { ...(yield* zh$1(this.storageRepository.getObject(r.physical), n)).content };
      t.mimeType && (o.mimeType = t.mimeType), t.cacheControl && (o.cacheControl = t.cacheControl), t.customMetadata && (o.customMetadata = t.customMetadata);
      const a = yield* zh$1(this.storageRepository.updateObject({ key: r.physical, content: o }), n);
      return { type: "asset", key: t.key, createdAt: a.createdAt, updatedAt: a.updatedAt, content: this.toAssetContent(o) };
    }));
  }
  deleteAsset(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* liftPromiseResult$1$1(this.getAssetPath(t));
      yield* Kt$2(rm$1(this.storageRepository.removeAtoms([r.physical]), n), (s) => s.data);
    }));
  }
  deleteAssets(t) {
    return ls$2((n) => ne$4({ self: this }, function* () {
      var _a4;
      const r = /* @__PURE__ */ new Map(), s = [];
      for (const l of t) {
        const c = yield* di$2(liftPromiseResult$1$1(this.getAssetPath(l)));
        if (X$4(c)) {
          yield* n.recoverableError(c.failure);
          continue;
        }
        s.push(c.success.physical), r.set(c.success.physical, l);
      }
      if (s.length === 0) return { removed: 0, skipped: t.length };
      const o = yield* Kt$2(rm$1(this.storageRepository.removeAtoms(s), n), (l) => l.data);
      let a = 0;
      for (const l of o) yield* n.data((_a4 = r.get(l)) != null ? _a4 : l), a += 1;
      return { removed: a, skipped: t.length - a };
    }));
  }
  getVariations(t) {
    return ls$2((n) => ne$4(function* () {
      for (const r of t) yield* n.data({ key: r.key, variations: {} });
      return { total: t.length };
    }));
  }
  getUrls(t) {
    return ls$2((n) => ne$4({ self: this }, function* () {
      let r = 0;
      for (const s of t) {
        const o = yield* di$2(liftPromiseResult$1$1(this.getAssetPath(s.key)));
        if (X$4(o)) {
          yield* n.recoverableError(o.failure);
          continue;
        }
        const a = o.success.settings.url ? this.renderUrlTemplate(o.success.settings.url, s.key) : s.key;
        yield* n.data({ key: s.key, url: a }), r += 1;
      }
      return { total: r };
    }));
  }
  getMetadata(t) {
    return ls$2((n) => ne$4({ self: this }, function* () {
      let r = 0;
      for (const s of t) {
        const o = yield* di$2(liftPromiseResult$1$1(this.getAssetPath(s.key)));
        if (X$4(o)) {
          yield* n.recoverableError(o.failure);
          continue;
        }
        const a = yield* di$2(zh$1(this.storageRepository.getObject(o.success.physical), n));
        if (X$4(a)) {
          yield* n.recoverableError(a.failure);
          continue;
        }
        const l = a.success.content, c = { kind: "binary", size: typeof l.size == "number" ? l.size : 0, mimeType: typeof l.mimeType == "string" ? l.mimeType : "application/octet-stream", ...typeof l.filename == "string" ? { filename: l.filename } : {} };
        yield* n.data({ key: s.key, metadata: c }), r += 1;
      }
      return { total: r };
    }));
  }
  getFolder(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* liftPromiseResult$1$1(this.getAssetPath(t));
      return { ...yield* zh$1(this.storageRepository.getFolder(r.physical), n), key: t };
    }));
  }
  createFolder(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* liftPromiseResult$1$1(this.getAssetPath(t.key));
      return { ...yield* zh$1(this.storageRepository.createFolder({ type: "folder", key: r.physical }), n), key: t.key };
    }));
  }
  deleteFolder(t, n) {
    return sc$1((r) => ne$4({ self: this }, function* () {
      const s = yield* liftPromiseResult$1$1(this.getAssetPath(t));
      yield* Kt$2(rm$1(this.storageRepository.removeAtoms([s.physical]), r), (o) => o.data);
    }));
  }
};
let ContentBaseSettingsProvider$1 = class ContentBaseSettingsProvider {
};
function failAs$1(e5) {
  return we$5(e5);
}
async function firstResult$2$1(e5) {
  return xe$5(di$2(us$2(e5)));
}
const startCase$1 = (e5) => e5.charAt(0).toUpperCase() + e5.slice(1), readOnly$1 = (e5) => we$5(new Bi$2(`${e5} is not supported by DecapContentBaseSettingsProvider \u2014 the Decap config is the source of truth; edit it directly.`)), isFolderCollection$1 = (e5) => typeof e5.folder == "string";
let DecapContentBaseSettingsProvider$1 = class DecapContentBaseSettingsProvider extends ContentBaseSettingsProvider$1 {
  constructor(t) {
    super();
    __publicField$5(this, "storage");
    __publicField$5(this, "configKey");
    __publicField$5(this, "getSettings", async () => {
      const t = await this.readDecapConfig();
      return X$4(t) ? failAs$1(t.failure) : lt$2(this.buildSettings(t.success));
    });
    this.storage = t.storage, this.configKey = t.configKey, firstResult$2$1(this.storage.getCapabilities()).then(vm$1).then((n) => {
      n.fileExtensions.supported || console.warn("Underlying storage for DecapContentBaseSettingsProvider does not advertise file-extension support. The Decap config must be served from a path the storage can deserialize.");
    }).catch((n) => {
      console.warn("DecapContentBaseSettingsProvider: getCapabilities probe failed", n);
    });
  }
  async readDecapConfig() {
    const t = await firstResult$2$1(this.storage.getObject(this.configKey));
    if (X$4(t)) return failAs$1(t.failure);
    const n = t.success.content;
    return n === null || typeof n != "object" ? we$5(new Bi$2(`Decap config at '${this.configKey}' did not deserialize to an object`)) : lt$2(n);
  }
  translateCollection(t) {
    var _a4;
    if (!isFolderCollection$1(t)) return null;
    const r = t.publish !== false ? { draft: { directory: "draft", name: "Draft" }, pending_review: { directory: "pending_review", name: "Pending Review" }, pending_publish: { directory: "pending_publish", name: "Pending Publish" } } : void 0;
    return { type: "document", key: t.name, name: (_a4 = t.label) != null ? _a4 : startCase$1(t.name), directory: t.folder, recursive: !!t.nested, ...r ? { unpublishedStatuses: r } : {}, revisionDirectory: `.contentbase/revisions/${t.name}` };
  }
  buildSettings(t) {
    var _a4;
    const n = {};
    for (const r of (_a4 = t.collections) != null ? _a4 : []) {
      const s = this.translateCollection(r);
      s && (n[s.key] = s);
    }
    return { collections: n };
  }
  async getDocumentCollectionSettings(t) {
    var _a4;
    const n = await this.getSettings();
    if (X$4(n)) return failAs$1(n.failure);
    const r = (_a4 = n.success.collections) == null ? void 0 : _a4[t];
    return r ? r.type !== "document" ? we$5(new Bi$2(`Settings for document collection '${t}' are of type '${r.type}' not 'document'.`)) : lt$2(r) : lt$2(defaultDocumentCollectionSettings$1(t));
  }
  async getMediaCollectionSettings(t) {
    var _a4;
    const n = await this.readDecapConfig(), r = defaultMediaCollectionSettings$1(t);
    if (X$4(n)) return lt$2(r);
    const s = ((_a4 = n.success.public_folder) != null ? _a4 : "").replace(/\/+$/, "");
    return s ? lt$2({ ...r, url: `${s}/{filename}` }) : lt$2(r);
  }
  async getCollectionSchema(t) {
    var _a4;
    const n = await this.readDecapConfig();
    if (X$4(n)) return failAs$1(n.failure);
    const r = ((_a4 = n.success.collections) != null ? _a4 : []).find((s) => s.name === t);
    return r ? isFolderCollection$1(r) ? lt$2(decapFieldsToJsonSchema$1(r.fields)) : we$5(new Bi$2(`Decap collection '${t}' is a 'files' collection \u2014 its schema isn't a single JSONSchema7.`)) : we$5(new Mi$2(`No Decap collection '${t}' in config`));
  }
  async putSettings(t) {
    return readOnly$1("putSettings");
  }
  async putDocumentCollectionSettings(t, n) {
    return readOnly$1("putDocumentCollectionSettings");
  }
  async putMediaCollectionSettings(t, n) {
    return readOnly$1("putMediaCollectionSettings");
  }
  async putCollectionSchema(t, n) {
    return readOnly$1("putCollectionSchema");
  }
};
function decapFieldsToJsonSchema$1(e5) {
  const t = {}, n = [];
  for (const r of e5) t[r.name] = decapFieldToJsonSchema$1(r), r.required !== false && n.push(r.name);
  return { type: "object", properties: t, ...n.length > 0 ? { required: n } : {}, additionalProperties: true };
}
function decapFieldToJsonSchema$1(e5) {
  var _a4, _b, _c2;
  switch ((_a4 = e5.widget) != null ? _a4 : "string") {
    case "string":
    case "text":
    case "markdown":
    case "code":
    case "color":
    case "hidden":
      return { type: "string" };
    case "boolean":
      return { type: "boolean" };
    case "number":
      return e5.value_type === "int" ? { type: "integer" } : { type: "number" };
    case "date":
      return { type: "string", format: "date" };
    case "datetime":
      return { type: "string", format: "date-time" };
    case "object":
      return decapFieldsToJsonSchema$1((_b = e5.fields) != null ? _b : []);
    case "list": {
      let n;
      e5.fields ? n = decapFieldsToJsonSchema$1(e5.fields) : e5.field ? n = decapFieldToJsonSchema$1(e5.field) : n = { type: "string" };
      const r = { type: "array", items: n };
      return typeof e5.min == "number" && (r.minItems = e5.min), typeof e5.max == "number" && (r.maxItems = e5.max), r;
    }
    case "select": {
      const r = ((_c2 = e5.options) != null ? _c2 : []).map((a) => typeof a == "string" || typeof a == "number" ? a : a.value), o = r.every((a) => typeof a == "string") ? { type: "string", enum: r } : { type: ["string", "number"], enum: r };
      return e5.multiple ? { type: "array", items: o } : o;
    }
    case "image":
    case "file":
    case "relation":
      return { type: "string" };
    default:
      return {};
  }
}
const defaultDocumentCollectionSettings$1 = (e5) => ({ type: "document", key: e5, name: startCase$1(e5), directory: e5, recursive: true, revisionDirectory: `.contentbase/revisions/${e5}` }), defaultMediaCollectionSettings$1 = (e5) => ({ type: "media", key: e5, name: startCase$1(e5), directory: e5, recursive: true }), liftPromiseResult$2 = (e5) => K$2(Dm$1(() => e5), Nm$1), collectStreamData$1 = (e5, t) => Kt$2(rm$1(e5, t), (n) => n.data);
let ContentBaseDocumentsRepository$1 = class ContentBaseDocumentsRepository extends Gm$1 {
  constructor(t, n) {
    super();
    __publicField$5(this, "storageRepository");
    __publicField$5(this, "settingsProvider");
    this.storageRepository = t, this.settingsProvider = n;
  }
  getCapabilities() {
    return sc$1((t) => ne$4({ self: this }, function* () {
      const n = yield* zh$1(this.storageRepository.getCapabilities(), t);
      return { compatibilityDate: bm$1.make("2026-05-11"), pagination: n.pagination };
    }));
  }
  parseKey(t) {
    const n = pathToSegments$1(t);
    if (n.length === 0) return { collection: "", remainder: "" };
    const [r, ...s] = n;
    return { collection: r, remainder: s.length > 0 ? pathCombine$1(...s) : "" };
  }
  async getDocumentPath(t) {
    var _a4;
    const { collection: n, remainder: r } = this.parseKey(t);
    if (!n) return we$5(new zi$2(`Document key '${t}' is missing a collection prefix`));
    const s = await this.settingsProvider.getDocumentCollectionSettings(n);
    if (X$4(s)) return we$5(s.failure);
    const o = (_a4 = s.success.directory) != null ? _a4 : n;
    return lt$2(r ? pathCombine$1(o, r) : o);
  }
  async getUnpublishedPath(t, n) {
    const { collection: r, remainder: s } = this.parseKey(t);
    if (!r) return we$5(new zi$2(`Document key '${t}' is missing a collection prefix`));
    const o = await this.settingsProvider.getDocumentCollectionSettings(r);
    if (X$4(o)) return we$5(o.failure);
    const a = o.success.unpublishedStatuses || {}, l = a[n];
    if (!l) return we$5(new zi$2(`Unknown unpublished status '${n}' for collection '${r}'. Available statuses: ${Object.keys(a).join(", ")}`));
    const c = `.contentbase/${r}/${l.directory}`;
    return lt$2(s ? pathCombine$1(c, s) : c);
  }
  async getRevisionPath(t, n) {
    const { collection: r, remainder: s } = this.parseKey(t);
    if (!r) return we$5(new zi$2(`Document key '${t}' is missing a collection prefix`));
    const o = await this.settingsProvider.getDocumentCollectionSettings(r);
    if (X$4(o)) return we$5(o.failure);
    const a = o.success.revisionDirectory || `.contentbase/${r}/revisions`, l = s ? pathCombine$1(a, s) : a;
    return lt$2(n ? pathCombine$1(l, n) : l);
  }
  extractKeyFromPath(t, n, r) {
    const s = t.startsWith(n) ? t.substring(n.length) : t, o = pathToSegments$1(s);
    return o.length > 0 ? pathCombine$1(r, ...o) : r;
  }
  getDocument(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      var _a4;
      const r = yield* liftPromiseResult$2(this.getDocumentPath(t)), s = yield* zh$1(this.storageRepository.getObject(r), n);
      return { ...s, key: t, type: "published", language: (_a4 = s.content.language) != null ? _a4 : "und", status: "published" };
    }));
  }
  createDocument(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* liftPromiseResult$2(this.getDocumentPath(t.key)), s = yield* zh$1(this.storageRepository.createObject({ type: "object", key: r, content: t.content }), n), o = (/* @__PURE__ */ new Date()).toISOString();
      return { ...s, key: t.key, type: "published", status: "published", language: t.language, createdAt: o, updatedAt: o };
    }));
  }
  updateDocument(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      var _a4;
      const r = yield* liftPromiseResult$2(this.getDocumentPath(t.key)), s = yield* zh$1(this.getDocument(t.key), n), o = (_a4 = t.content) != null ? _a4 : s.content;
      return yield* zh$1(this.storageRepository.updateObject({ key: r, content: o }), n), { ...s, content: o, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    }));
  }
  deleteDocument(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* liftPromiseResult$2(this.getDocumentPath(t));
      yield* collectStreamData$1(this.storageRepository.removeAtoms([r]), n);
    }));
  }
  getUnpublished(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      var _a4;
      const { collection: r, remainder: s } = this.parseKey(t);
      if (!r) return yield* j$2(new zi$2(`Document key '${t}' is missing a collection prefix`));
      const a = (yield* liftPromiseResult$2(this.settingsProvider.getDocumentCollectionSettings(r))).unpublishedStatuses || {};
      for (const [l, c] of Object.entries(a)) {
        const u = `.contentbase/${r}/${c.directory}`, p = s ? pathCombine$1(u, s) : u, f = yield* di$2(zh$1(this.storageRepository.getObject(p), n));
        if (jt$2(f)) return { ...f.success, key: t, type: "unpublished", language: (_a4 = f.success.content.language) != null ? _a4 : "und", status: l };
      }
      return yield* j$2(new Mi$2(`Unpublished document '${t}' not found in collection '${r}'`));
    }));
  }
  createUnpublished(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* liftPromiseResult$2(this.getUnpublishedPath(t.key, t.status)), s = yield* zh$1(this.storageRepository.createObject({ type: "object", key: r, content: t.content }), n), o = (/* @__PURE__ */ new Date()).toISOString();
      return { ...s, key: t.key, type: "unpublished", status: t.status, language: t.language, createdAt: o, updatedAt: o };
    }));
  }
  updateUnpublished(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* zh$1(this.getUnpublished(t.key), n), s = t.content || r.content;
      if (t.status && t.status !== r.status) return yield* zh$1(this.updateUnpublishedStatus(t.key, t.status), n);
      const o = yield* liftPromiseResult$2(this.getUnpublishedPath(t.key, r.status));
      return yield* zh$1(this.storageRepository.updateObject({ key: o, content: s }), n), { ...r, content: s, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    }));
  }
  updateUnpublishedStatus(t, n) {
    return sc$1((r) => ne$4({ self: this }, function* () {
      const s = yield* zh$1(this.getUnpublished(t), r), o = yield* liftPromiseResult$2(this.getUnpublishedPath(t, s.status)), a = yield* liftPromiseResult$2(this.getUnpublishedPath(t, n));
      return yield* zh$1(this.storageRepository.createObject({ type: "object", key: a, content: s.content }), r), yield* collectStreamData$1(this.storageRepository.removeAtoms([o]), r), { ...s, status: n, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    }));
  }
  deleteUnpublished(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* zh$1(this.getUnpublished(t), n), s = yield* liftPromiseResult$2(this.getUnpublishedPath(t, r.status));
      yield* collectStreamData$1(this.storageRepository.removeAtoms([s]), n);
    }));
  }
  unpublish(t, n) {
    return sc$1((r) => ne$4({ self: this }, function* () {
      const s = yield* zh$1(this.getDocument(t), r), o = yield* liftPromiseResult$2(this.getDocumentPath(t)), a = yield* liftPromiseResult$2(this.getUnpublishedPath(t, n));
      return yield* zh$1(this.storageRepository.createObject({ type: "object", key: a, content: s.content }), r), yield* collectStreamData$1(this.storageRepository.removeAtoms([o]), r), { key: t, type: "unpublished", status: n, language: s.language, content: s.content, createdAt: s.createdAt, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    }));
  }
  publish(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* zh$1(this.getUnpublished(t), n), s = yield* liftPromiseResult$2(this.getUnpublishedPath(t, r.status)), o = yield* liftPromiseResult$2(this.getDocumentPath(t));
      return yield* zh$1(this.storageRepository.createObject({ type: "object", key: o, content: r.content }), n), yield* collectStreamData$1(this.storageRepository.removeAtoms([s]), n), { key: t, type: "published", status: "published", language: r.language, content: r.content, createdAt: r.createdAt, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    }));
  }
  listRecords(t) {
    return this.listRecordsInternal(t, "full");
  }
  listRecordSummaries(t) {
    return this.listRecordsInternal(t, "summary");
  }
  listRecordsInternal(t, n) {
    return ls$2((r) => ne$4({ self: this }, function* () {
      var _a4;
      if (!t.folder) return yield* j$2(new zi$2("listRecords requires `folder` (the collection name) to identify which collection to list"));
      const { collection: s, remainder: o } = this.parseKey(t.folder);
      if (!s) return yield* j$2(new zi$2(`folder '${t.folder}' is missing a collection prefix`));
      const a = yield* liftPromiseResult$2(this.settingsProvider.getDocumentCollectionSettings(s));
      let l = 0;
      if (t.type === "published" || t.type === void 0) {
        const c = (_a4 = a.directory) != null ? _a4 : s, u = o ? pathCombine$1(c, o) : c, p = { pagination: t.pagination, depth: t.depth };
        if (n === "full") {
          const f = yield* collectStreamData$1(this.storageRepository.listAtoms(u, p), r);
          for (const h of f) {
            if (h.type !== "object") continue;
            const m = this.extractKeyFromPath(h.key, c, s);
            yield* r.data({ ...h, key: m, type: "published", status: "published" }), l += 1;
          }
        } else {
          const f = yield* collectStreamData$1(this.storageRepository.listAtomSummaries(u, p), r);
          for (const h of f) {
            if (h.type !== "object-summary") continue;
            const m = this.extractKeyFromPath(h.key, c, s);
            yield* r.data({ ...h, key: m, type: "published-summary", status: "published" }), l += 1;
          }
        }
      }
      if (t.type === "unpublished" || t.type === void 0) {
        const c = a.unpublishedStatuses || {}, u = t.statuses || Object.keys(c);
        for (const p of u) {
          const f = c[p];
          if (!f) continue;
          const h = `.contentbase/${s}/${f.directory}`, m = o ? pathCombine$1(h, o) : h, g = { pagination: t.pagination, depth: t.depth };
          if (n === "full") {
            const d = yield* di$2(collectStreamData$1(this.storageRepository.listAtoms(m, g), r));
            if (X$4(d)) {
              d.failure.code !== Mi$2.CODE && (yield* r.recoverableError(d.failure));
              continue;
            }
            for (const y of d.success) {
              if (y.type !== "object") continue;
              const S = this.extractKeyFromPath(y.key, h, s);
              yield* r.data({ ...y, key: S, type: "unpublished", status: p }), l += 1;
            }
          } else {
            const d = yield* di$2(collectStreamData$1(this.storageRepository.listAtomSummaries(m, g), r));
            if (X$4(d)) {
              d.failure.code !== Mi$2.CODE && (yield* r.recoverableError(d.failure));
              continue;
            }
            for (const y of d.success) {
              if (y.type !== "object-summary") continue;
              const S = this.extractKeyFromPath(y.key, h, s);
              yield* r.data({ ...y, key: S, type: "unpublished-summary", status: p }), l += 1;
            }
          }
        }
      }
      return { total: l };
    }));
  }
  getRevision(t, n) {
    return sc$1((r) => ne$4({ self: this }, function* () {
      var _a4;
      const s = yield* liftPromiseResult$2(this.getRevisionPath(t, n)), o = yield* zh$1(this.storageRepository.getObject(s), r);
      return o.createdAt ? { ...o, createdAt: o.createdAt, language: (_a4 = o.content.language) != null ? _a4 : "und", revision: n, type: "revision", key: t } : yield* j$2(new Bi$2("Revision is missing createdAt date"));
    }));
  }
  createRevision(t) {
    return sc$1((n) => ne$4({ self: this }, function* () {
      const r = yield* liftPromiseResult$2(this.getRevisionPath(t.key, t.revision)), s = yield* zh$1(this.storageRepository.createObject({ type: "object", key: r, content: t.content }), n), o = (/* @__PURE__ */ new Date()).toISOString();
      return { ...s, key: t.key, revision: t.revision, language: t.language, type: "revision", createdAt: o, updatedAt: o };
    }));
  }
  listRevisions(t, n) {
    return ls$2((r) => ne$4({ self: this }, function* () {
      var _a4;
      const s = yield* liftPromiseResult$2(this.getRevisionPath(t)), o = yield* collectStreamData$1(this.storageRepository.listAtoms(s, { pagination: n.pagination, depth: 1 }), r);
      let a = 0;
      for (const l of o) {
        if (l.type !== "object") continue;
        const c = basename$1(l.key);
        yield* r.data({ ...l, type: "revision-summary", revision: c, language: (_a4 = l.content.language) != null ? _a4 : "und", key: t }), a += 1;
      }
      return { total: a };
    }));
  }
};
var composer$1$1 = {}, directives$2$1 = {}, identity$y$1 = {};
const ALIAS$1 = /* @__PURE__ */ Symbol.for("yaml.alias"), DOC$1 = /* @__PURE__ */ Symbol.for("yaml.document"), MAP$1 = /* @__PURE__ */ Symbol.for("yaml.map"), PAIR$1 = /* @__PURE__ */ Symbol.for("yaml.pair"), SCALAR$1$1 = /* @__PURE__ */ Symbol.for("yaml.scalar"), SEQ$1 = /* @__PURE__ */ Symbol.for("yaml.seq"), NODE_TYPE$1 = /* @__PURE__ */ Symbol.for("yaml.node.type"), isAlias$1 = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE$1] === ALIAS$1, isDocument$1 = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE$1] === DOC$1, isMap$1 = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE$1] === MAP$1, isPair$1 = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE$1] === PAIR$1, isScalar$1$1 = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE$1] === SCALAR$1$1, isSeq$1 = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE$1] === SEQ$1;
function isCollection$1$1(e5) {
  if (e5 && typeof e5 == "object") switch (e5[NODE_TYPE$1]) {
    case MAP$1:
    case SEQ$1:
      return true;
  }
  return false;
}
function isNode$1(e5) {
  if (e5 && typeof e5 == "object") switch (e5[NODE_TYPE$1]) {
    case ALIAS$1:
    case MAP$1:
    case SCALAR$1$1:
    case SEQ$1:
      return true;
  }
  return false;
}
const hasAnchor$1 = (e5) => (isScalar$1$1(e5) || isCollection$1$1(e5)) && !!e5.anchor;
identity$y$1.ALIAS = ALIAS$1;
identity$y$1.DOC = DOC$1;
identity$y$1.MAP = MAP$1;
identity$y$1.NODE_TYPE = NODE_TYPE$1;
identity$y$1.PAIR = PAIR$1;
identity$y$1.SCALAR = SCALAR$1$1;
identity$y$1.SEQ = SEQ$1;
identity$y$1.hasAnchor = hasAnchor$1;
identity$y$1.isAlias = isAlias$1;
identity$y$1.isCollection = isCollection$1$1;
identity$y$1.isDocument = isDocument$1;
identity$y$1.isMap = isMap$1;
identity$y$1.isNode = isNode$1;
identity$y$1.isPair = isPair$1;
identity$y$1.isScalar = isScalar$1$1;
identity$y$1.isSeq = isSeq$1;
var visit$6$1 = {}, identity$x$1 = identity$y$1;
const BREAK$1$1 = /* @__PURE__ */ Symbol("break visit"), SKIP$1$1 = /* @__PURE__ */ Symbol("skip children"), REMOVE$1$1 = /* @__PURE__ */ Symbol("remove node");
function visit$5$1(e5, t) {
  const n = initVisitor$1(t);
  identity$x$1.isDocument(e5) ? visit_$1(null, e5.contents, n, Object.freeze([e5])) === REMOVE$1$1 && (e5.contents = null) : visit_$1(null, e5, n, Object.freeze([]));
}
visit$5$1.BREAK = BREAK$1$1;
visit$5$1.SKIP = SKIP$1$1;
visit$5$1.REMOVE = REMOVE$1$1;
function visit_$1(e5, t, n, r) {
  const s = callVisitor$1(e5, t, n, r);
  if (identity$x$1.isNode(s) || identity$x$1.isPair(s)) return replaceNode$1(e5, r, s), visit_$1(e5, s, n, r);
  if (typeof s != "symbol") {
    if (identity$x$1.isCollection(t)) {
      r = Object.freeze(r.concat(t));
      for (let o = 0; o < t.items.length; ++o) {
        const a = visit_$1(o, t.items[o], n, r);
        if (typeof a == "number") o = a - 1;
        else {
          if (a === BREAK$1$1) return BREAK$1$1;
          a === REMOVE$1$1 && (t.items.splice(o, 1), o -= 1);
        }
      }
    } else if (identity$x$1.isPair(t)) {
      r = Object.freeze(r.concat(t));
      const o = visit_$1("key", t.key, n, r);
      if (o === BREAK$1$1) return BREAK$1$1;
      o === REMOVE$1$1 && (t.key = null);
      const a = visit_$1("value", t.value, n, r);
      if (a === BREAK$1$1) return BREAK$1$1;
      a === REMOVE$1$1 && (t.value = null);
    }
  }
  return s;
}
async function visitAsync$1(e5, t) {
  const n = initVisitor$1(t);
  identity$x$1.isDocument(e5) ? await visitAsync_$1(null, e5.contents, n, Object.freeze([e5])) === REMOVE$1$1 && (e5.contents = null) : await visitAsync_$1(null, e5, n, Object.freeze([]));
}
visitAsync$1.BREAK = BREAK$1$1;
visitAsync$1.SKIP = SKIP$1$1;
visitAsync$1.REMOVE = REMOVE$1$1;
async function visitAsync_$1(e5, t, n, r) {
  const s = await callVisitor$1(e5, t, n, r);
  if (identity$x$1.isNode(s) || identity$x$1.isPair(s)) return replaceNode$1(e5, r, s), visitAsync_$1(e5, s, n, r);
  if (typeof s != "symbol") {
    if (identity$x$1.isCollection(t)) {
      r = Object.freeze(r.concat(t));
      for (let o = 0; o < t.items.length; ++o) {
        const a = await visitAsync_$1(o, t.items[o], n, r);
        if (typeof a == "number") o = a - 1;
        else {
          if (a === BREAK$1$1) return BREAK$1$1;
          a === REMOVE$1$1 && (t.items.splice(o, 1), o -= 1);
        }
      }
    } else if (identity$x$1.isPair(t)) {
      r = Object.freeze(r.concat(t));
      const o = await visitAsync_$1("key", t.key, n, r);
      if (o === BREAK$1$1) return BREAK$1$1;
      o === REMOVE$1$1 && (t.key = null);
      const a = await visitAsync_$1("value", t.value, n, r);
      if (a === BREAK$1$1) return BREAK$1$1;
      a === REMOVE$1$1 && (t.value = null);
    }
  }
  return s;
}
function initVisitor$1(e5) {
  return typeof e5 == "object" && (e5.Collection || e5.Node || e5.Value) ? Object.assign({ Alias: e5.Node, Map: e5.Node, Scalar: e5.Node, Seq: e5.Node }, e5.Value && { Map: e5.Value, Scalar: e5.Value, Seq: e5.Value }, e5.Collection && { Map: e5.Collection, Seq: e5.Collection }, e5) : e5;
}
function callVisitor$1(e5, t, n, r) {
  var _a4, _b, _c2, _d, _e2;
  if (typeof n == "function") return n(e5, t, r);
  if (identity$x$1.isMap(t)) return (_a4 = n.Map) == null ? void 0 : _a4.call(n, e5, t, r);
  if (identity$x$1.isSeq(t)) return (_b = n.Seq) == null ? void 0 : _b.call(n, e5, t, r);
  if (identity$x$1.isPair(t)) return (_c2 = n.Pair) == null ? void 0 : _c2.call(n, e5, t, r);
  if (identity$x$1.isScalar(t)) return (_d = n.Scalar) == null ? void 0 : _d.call(n, e5, t, r);
  if (identity$x$1.isAlias(t)) return (_e2 = n.Alias) == null ? void 0 : _e2.call(n, e5, t, r);
}
function replaceNode$1(e5, t, n) {
  const r = t[t.length - 1];
  if (identity$x$1.isCollection(r)) r.items[e5] = n;
  else if (identity$x$1.isPair(r)) e5 === "key" ? r.key = n : r.value = n;
  else if (identity$x$1.isDocument(r)) r.contents = n;
  else {
    const s = identity$x$1.isAlias(r) ? "alias" : "scalar";
    throw new Error(`Cannot replace node with ${s} parent`);
  }
}
visit$6$1.visit = visit$5$1;
visit$6$1.visitAsync = visitAsync$1;
var identity$w$1 = identity$y$1, visit$4$1 = visit$6$1;
const escapeChars$1 = { "!": "%21", ",": "%2C", "[": "%5B", "]": "%5D", "{": "%7B", "}": "%7D" }, escapeTagName$1 = (e5) => e5.replace(/[!,[\]{}]/g, (t) => escapeChars$1[t]);
let Directives$1 = class Directives {
  constructor(t, n) {
    this.docStart = null, this.docEnd = false, this.yaml = Object.assign({}, Directives.defaultYaml, t), this.tags = Object.assign({}, Directives.defaultTags, n);
  }
  clone() {
    const t = new Directives(this.yaml, this.tags);
    return t.docStart = this.docStart, t;
  }
  atDocument() {
    const t = new Directives(this.yaml, this.tags);
    switch (this.yaml.version) {
      case "1.1":
        this.atNextDocument = true;
        break;
      case "1.2":
        this.atNextDocument = false, this.yaml = { explicit: Directives.defaultYaml.explicit, version: "1.2" }, this.tags = Object.assign({}, Directives.defaultTags);
        break;
    }
    return t;
  }
  add(t, n) {
    this.atNextDocument && (this.yaml = { explicit: Directives.defaultYaml.explicit, version: "1.1" }, this.tags = Object.assign({}, Directives.defaultTags), this.atNextDocument = false);
    const r = t.trim().split(/[ \t]+/), s = r.shift();
    switch (s) {
      case "%TAG": {
        if (r.length !== 2 && (n(0, "%TAG directive should contain exactly two parts"), r.length < 2)) return false;
        const [o, a] = r;
        return this.tags[o] = a, true;
      }
      case "%YAML": {
        if (this.yaml.explicit = true, r.length !== 1) return n(0, "%YAML directive should contain exactly one part"), false;
        const [o] = r;
        if (o === "1.1" || o === "1.2") return this.yaml.version = o, true;
        {
          const a = /^\d+\.\d+$/.test(o);
          return n(6, `Unsupported YAML version ${o}`, a), false;
        }
      }
      default:
        return n(0, `Unknown directive ${s}`, true), false;
    }
  }
  tagName(t, n) {
    if (t === "!") return "!";
    if (t[0] !== "!") return n(`Not a valid tag: ${t}`), null;
    if (t[1] === "<") {
      const a = t.slice(2, -1);
      return a === "!" || a === "!!" ? (n(`Verbatim tags aren't resolved, so ${t} is invalid.`), null) : (t[t.length - 1] !== ">" && n("Verbatim tags must end with a >"), a);
    }
    const [, r, s] = t.match(/^(.*!)([^!]*)$/s);
    s || n(`The ${t} tag has no suffix`);
    const o = this.tags[r];
    if (o) try {
      return o + decodeURIComponent(s);
    } catch (a) {
      return n(String(a)), null;
    }
    return r === "!" ? t : (n(`Could not resolve tag: ${t}`), null);
  }
  tagString(t) {
    for (const [n, r] of Object.entries(this.tags)) if (t.startsWith(r)) return n + escapeTagName$1(t.substring(r.length));
    return t[0] === "!" ? t : `!<${t}>`;
  }
  toString(t) {
    const n = this.yaml.explicit ? [`%YAML ${this.yaml.version || "1.2"}`] : [], r = Object.entries(this.tags);
    let s;
    if (t && r.length > 0 && identity$w$1.isNode(t.contents)) {
      const o = {};
      visit$4$1.visit(t.contents, (a, l) => {
        identity$w$1.isNode(l) && l.tag && (o[l.tag] = true);
      }), s = Object.keys(o);
    } else s = [];
    for (const [o, a] of r) o === "!!" && a === "tag:yaml.org,2002:" || (!t || s.some((l) => l.startsWith(a))) && n.push(`%TAG ${o} ${a}`);
    return n.join(`
`);
  }
};
Directives$1.defaultYaml = { explicit: false, version: "1.2" };
Directives$1.defaultTags = { "!!": "tag:yaml.org,2002:" };
directives$2$1.Directives = Directives$1;
var Document$4$1 = {}, Alias$4$1 = {}, anchors$3$1 = {}, identity$v$1 = identity$y$1, visit$3$1 = visit$6$1;
function anchorIsValid$1(e5) {
  if (/[\x00-\x19\s,[\]{}]/.test(e5)) {
    const n = `Anchor must not contain whitespace or control characters: ${JSON.stringify(e5)}`;
    throw new Error(n);
  }
  return true;
}
function anchorNames$1(e5) {
  const t = /* @__PURE__ */ new Set();
  return visit$3$1.visit(e5, { Value(n, r) {
    r.anchor && t.add(r.anchor);
  } }), t;
}
function findNewAnchor$1(e5, t) {
  for (let n = 1; ; ++n) {
    const r = `${e5}${n}`;
    if (!t.has(r)) return r;
  }
}
function createNodeAnchors$1(e5, t) {
  const n = [], r = /* @__PURE__ */ new Map();
  let s = null;
  return { onAnchor: (o) => {
    n.push(o), s != null ? s : s = anchorNames$1(e5);
    const a = findNewAnchor$1(t, s);
    return s.add(a), a;
  }, setAnchors: () => {
    for (const o of n) {
      const a = r.get(o);
      if (typeof a == "object" && a.anchor && (identity$v$1.isScalar(a.node) || identity$v$1.isCollection(a.node))) a.node.anchor = a.anchor;
      else {
        const l = new Error("Failed to resolve repeated object (this should not happen)");
        throw l.source = o, l;
      }
    }
  }, sourceObjects: r };
}
anchors$3$1.anchorIsValid = anchorIsValid$1;
anchors$3$1.anchorNames = anchorNames$1;
anchors$3$1.createNodeAnchors = createNodeAnchors$1;
anchors$3$1.findNewAnchor = findNewAnchor$1;
var Node$3$1 = {}, applyReviver$3$1 = {};
function applyReviver$2$1(e5, t, n, r) {
  if (r && typeof r == "object") if (Array.isArray(r)) for (let s = 0, o = r.length; s < o; ++s) {
    const a = r[s], l = applyReviver$2$1(e5, r, String(s), a);
    l === void 0 ? delete r[s] : l !== a && (r[s] = l);
  }
  else if (r instanceof Map) for (const s of Array.from(r.keys())) {
    const o = r.get(s), a = applyReviver$2$1(e5, r, s, o);
    a === void 0 ? r.delete(s) : a !== o && r.set(s, a);
  }
  else if (r instanceof Set) for (const s of Array.from(r)) {
    const o = applyReviver$2$1(e5, r, s, s);
    o === void 0 ? r.delete(s) : o !== s && (r.delete(s), r.add(o));
  }
  else for (const [s, o] of Object.entries(r)) {
    const a = applyReviver$2$1(e5, r, s, o);
    a === void 0 ? delete r[s] : a !== o && (r[s] = a);
  }
  return e5.call(t, n, r);
}
applyReviver$3$1.applyReviver = applyReviver$2$1;
var toJS$8$1 = {}, identity$u$1 = identity$y$1;
function toJS$7$1(e5, t, n) {
  if (Array.isArray(e5)) return e5.map((r, s) => toJS$7$1(r, String(s), n));
  if (e5 && typeof e5.toJSON == "function") {
    if (!n || !identity$u$1.hasAnchor(e5)) return e5.toJSON(t, n);
    const r = { aliasCount: 0, count: 1, res: void 0 };
    n.anchors.set(e5, r), n.onCreate = (o) => {
      r.res = o, delete n.onCreate;
    };
    const s = e5.toJSON(t, n);
    return n.onCreate && n.onCreate(s), s;
  }
  return typeof e5 == "bigint" && !(n == null ? void 0 : n.keep) ? Number(e5) : e5;
}
toJS$8$1.toJS = toJS$7$1;
var applyReviver$1$1 = applyReviver$3$1, identity$t$1 = identity$y$1, toJS$6$1 = toJS$8$1;
let NodeBase$1 = class NodeBase {
  constructor(t) {
    Object.defineProperty(this, identity$t$1.NODE_TYPE, { value: t });
  }
  clone() {
    const t = Object.create(Object.getPrototypeOf(this), Object.getOwnPropertyDescriptors(this));
    return this.range && (t.range = this.range.slice()), t;
  }
  toJS(t, { mapAsMap: n, maxAliasCount: r, onAnchor: s, reviver: o } = {}) {
    if (!identity$t$1.isDocument(t)) throw new TypeError("A document argument is required");
    const a = { anchors: /* @__PURE__ */ new Map(), doc: t, keep: true, mapAsMap: n === true, mapKeyWarned: false, maxAliasCount: typeof r == "number" ? r : 100 }, l = toJS$6$1.toJS(this, "", a);
    if (typeof s == "function") for (const { count: c, res: u } of a.anchors.values()) s(u, c);
    return typeof o == "function" ? applyReviver$1$1.applyReviver(o, { "": l }, "", l) : l;
  }
};
Node$3$1.NodeBase = NodeBase$1;
var anchors$2$1 = anchors$3$1, visit$2$1 = visit$6$1, identity$s$1 = identity$y$1, Node$2$1 = Node$3$1, toJS$5$1 = toJS$8$1;
let Alias$3$1 = class Alias$3 extends Node$2$1.NodeBase {
  constructor(t) {
    super(identity$s$1.ALIAS), this.source = t, Object.defineProperty(this, "tag", { set() {
      throw new Error("Alias nodes cannot have tags");
    } });
  }
  resolve(t, n) {
    if ((n == null ? void 0 : n.maxAliasCount) === 0) throw new ReferenceError("Alias resolution is disabled");
    let r;
    (n == null ? void 0 : n.aliasResolveCache) ? r = n.aliasResolveCache : (r = [], visit$2$1.visit(t, { Node: (o, a) => {
      (identity$s$1.isAlias(a) || identity$s$1.hasAnchor(a)) && r.push(a);
    } }), n && (n.aliasResolveCache = r));
    let s;
    for (const o of r) {
      if (o === this) break;
      o.anchor === this.source && (s = o);
    }
    return s;
  }
  toJSON(t, n) {
    if (!n) return { source: this.source };
    const { anchors: r, doc: s, maxAliasCount: o } = n, a = this.resolve(s, n);
    if (!a) {
      const c = `Unresolved alias (the anchor must be set before the alias): ${this.source}`;
      throw new ReferenceError(c);
    }
    let l = r.get(a);
    if (l || (toJS$5$1.toJS(a, null, n), l = r.get(a)), (l == null ? void 0 : l.res) === void 0) {
      const c = "This should not happen: Alias anchor was not resolved?";
      throw new ReferenceError(c);
    }
    if (o >= 0 && (l.count += 1, l.aliasCount === 0 && (l.aliasCount = getAliasCount$1(s, a, r)), l.count * l.aliasCount > o)) {
      const c = "Excessive alias count indicates a resource exhaustion attack";
      throw new ReferenceError(c);
    }
    return l.res;
  }
  toString(t, n, r) {
    const s = `*${this.source}`;
    if (t) {
      if (anchors$2$1.anchorIsValid(this.source), t.options.verifyAliasOrder && !t.anchors.has(this.source)) {
        const o = `Unresolved alias (the anchor must be set before the alias): ${this.source}`;
        throw new Error(o);
      }
      if (t.implicitKey) return `${s} `;
    }
    return s;
  }
};
function getAliasCount$1(e5, t, n) {
  if (identity$s$1.isAlias(t)) {
    const r = t.resolve(e5), s = n && r && n.get(r);
    return s ? s.count * s.aliasCount : 0;
  } else if (identity$s$1.isCollection(t)) {
    let r = 0;
    for (const s of t.items) {
      const o = getAliasCount$1(e5, s, n);
      o > r && (r = o);
    }
    return r;
  } else if (identity$s$1.isPair(t)) {
    const r = getAliasCount$1(e5, t.key, n), s = getAliasCount$1(e5, t.value, n);
    return Math.max(r, s);
  }
  return 1;
}
Alias$4$1.Alias = Alias$3$1;
var Collection$4$1 = {}, createNode$5$1 = {}, Scalar$k$1 = {}, identity$r$1 = identity$y$1, Node$1$1 = Node$3$1, toJS$4$1 = toJS$8$1;
const isScalarValue$1 = (e5) => !e5 || typeof e5 != "function" && typeof e5 != "object";
let Scalar$j$1 = class Scalar$j extends Node$1$1.NodeBase {
  constructor(t) {
    super(identity$r$1.SCALAR), this.value = t;
  }
  toJSON(t, n) {
    return (n == null ? void 0 : n.keep) ? this.value : toJS$4$1.toJS(this.value, t, n);
  }
  toString() {
    return String(this.value);
  }
};
Scalar$j$1.BLOCK_FOLDED = "BLOCK_FOLDED";
Scalar$j$1.BLOCK_LITERAL = "BLOCK_LITERAL";
Scalar$j$1.PLAIN = "PLAIN";
Scalar$j$1.QUOTE_DOUBLE = "QUOTE_DOUBLE";
Scalar$j$1.QUOTE_SINGLE = "QUOTE_SINGLE";
Scalar$k$1.Scalar = Scalar$j$1;
Scalar$k$1.isScalarValue = isScalarValue$1;
var Alias$2$1 = Alias$4$1, identity$q$1 = identity$y$1, Scalar$i$1 = Scalar$k$1;
const defaultTagPrefix$1 = "tag:yaml.org,2002:";
function findTagObject$1(e5, t, n) {
  var _a4;
  if (t) {
    const r = n.filter((o) => o.tag === t), s = (_a4 = r.find((o) => !o.format)) != null ? _a4 : r[0];
    if (!s) throw new Error(`Tag ${t} not found`);
    return s;
  }
  return n.find((r) => {
    var _a5;
    return ((_a5 = r.identify) == null ? void 0 : _a5.call(r, e5)) && !r.format;
  });
}
function createNode$4$1(e5, t, n) {
  var _a4, _b, _c2, _d;
  if (identity$q$1.isDocument(e5) && (e5 = e5.contents), identity$q$1.isNode(e5)) return e5;
  if (identity$q$1.isPair(e5)) {
    const f = (_b = (_a4 = n.schema[identity$q$1.MAP]).createNode) == null ? void 0 : _b.call(_a4, n.schema, null, n);
    return f.items.push(e5), f;
  }
  (e5 instanceof String || e5 instanceof Number || e5 instanceof Boolean || typeof BigInt < "u" && e5 instanceof BigInt) && (e5 = e5.valueOf());
  const { aliasDuplicateObjects: r, onAnchor: s, onTagObj: o, schema: a, sourceObjects: l } = n;
  let c;
  if (r && e5 && typeof e5 == "object") {
    if (c = l.get(e5), c) return (_c2 = c.anchor) != null ? _c2 : c.anchor = s(e5), new Alias$2$1.Alias(c.anchor);
    c = { anchor: null, node: null }, l.set(e5, c);
  }
  (t == null ? void 0 : t.startsWith("!!")) && (t = defaultTagPrefix$1 + t.slice(2));
  let u = findTagObject$1(e5, t, a.tags);
  if (!u) {
    if (e5 && typeof e5.toJSON == "function" && (e5 = e5.toJSON()), !e5 || typeof e5 != "object") {
      const f = new Scalar$i$1.Scalar(e5);
      return c && (c.node = f), f;
    }
    u = e5 instanceof Map ? a[identity$q$1.MAP] : Symbol.iterator in Object(e5) ? a[identity$q$1.SEQ] : a[identity$q$1.MAP];
  }
  o && (o(u), delete n.onTagObj);
  const p = (u == null ? void 0 : u.createNode) ? u.createNode(n.schema, e5, n) : typeof ((_d = u == null ? void 0 : u.nodeClass) == null ? void 0 : _d.from) == "function" ? u.nodeClass.from(n.schema, e5, n) : new Scalar$i$1.Scalar(e5);
  return t ? p.tag = t : u.default || (p.tag = u.tag), c && (c.node = p), p;
}
createNode$5$1.createNode = createNode$4$1;
var createNode$3$1 = createNode$5$1, identity$p$1 = identity$y$1, Node$4 = Node$3$1;
function collectionFromPath$1(e5, t, n) {
  let r = n;
  for (let s = t.length - 1; s >= 0; --s) {
    const o = t[s];
    if (typeof o == "number" && Number.isInteger(o) && o >= 0) {
      const a = [];
      a[o] = r, r = a;
    } else r = /* @__PURE__ */ new Map([[o, r]]);
  }
  return createNode$3$1.createNode(r, void 0, { aliasDuplicateObjects: false, keepUndefined: false, onAnchor: () => {
    throw new Error("This should not happen, please report a bug.");
  }, schema: e5, sourceObjects: /* @__PURE__ */ new Map() });
}
const isEmptyPath$1 = (e5) => e5 == null || typeof e5 == "object" && !!e5[Symbol.iterator]().next().done;
let Collection$3$1 = class Collection$3 extends Node$4.NodeBase {
  constructor(t, n) {
    super(t), Object.defineProperty(this, "schema", { value: n, configurable: true, enumerable: false, writable: true });
  }
  clone(t) {
    const n = Object.create(Object.getPrototypeOf(this), Object.getOwnPropertyDescriptors(this));
    return t && (n.schema = t), n.items = n.items.map((r) => identity$p$1.isNode(r) || identity$p$1.isPair(r) ? r.clone(t) : r), this.range && (n.range = this.range.slice()), n;
  }
  addIn(t, n) {
    if (isEmptyPath$1(t)) this.add(n);
    else {
      const [r, ...s] = t, o = this.get(r, true);
      if (identity$p$1.isCollection(o)) o.addIn(s, n);
      else if (o === void 0 && this.schema) this.set(r, collectionFromPath$1(this.schema, s, n));
      else throw new Error(`Expected YAML collection at ${r}. Remaining path: ${s}`);
    }
  }
  deleteIn(t) {
    const [n, ...r] = t;
    if (r.length === 0) return this.delete(n);
    const s = this.get(n, true);
    if (identity$p$1.isCollection(s)) return s.deleteIn(r);
    throw new Error(`Expected YAML collection at ${n}. Remaining path: ${r}`);
  }
  getIn(t, n) {
    const [r, ...s] = t, o = this.get(r, true);
    return s.length === 0 ? !n && identity$p$1.isScalar(o) ? o.value : o : identity$p$1.isCollection(o) ? o.getIn(s, n) : void 0;
  }
  hasAllNullValues(t) {
    return this.items.every((n) => {
      if (!identity$p$1.isPair(n)) return false;
      const r = n.value;
      return r == null || t && identity$p$1.isScalar(r) && r.value == null && !r.commentBefore && !r.comment && !r.tag;
    });
  }
  hasIn(t) {
    const [n, ...r] = t;
    if (r.length === 0) return this.has(n);
    const s = this.get(n, true);
    return identity$p$1.isCollection(s) ? s.hasIn(r) : false;
  }
  setIn(t, n) {
    const [r, ...s] = t;
    if (s.length === 0) this.set(r, n);
    else {
      const o = this.get(r, true);
      if (identity$p$1.isCollection(o)) o.setIn(s, n);
      else if (o === void 0 && this.schema) this.set(r, collectionFromPath$1(this.schema, s, n));
      else throw new Error(`Expected YAML collection at ${r}. Remaining path: ${s}`);
    }
  }
};
Collection$4$1.Collection = Collection$3$1;
Collection$4$1.collectionFromPath = collectionFromPath$1;
Collection$4$1.isEmptyPath = isEmptyPath$1;
var Pair$8$1 = {}, stringifyPair$2$1 = {}, stringify$b$1 = {}, stringifyComment$5$1 = {};
const stringifyComment$4$1 = (e5) => e5.replace(/^(?!$)(?: $)?/gm, "#");
function indentComment$1(e5, t) {
  return /^\n+$/.test(e5) ? e5.substring(1) : t ? e5.replace(/^(?! *$)/gm, t) : e5;
}
const lineComment$1 = (e5, t, n) => e5.endsWith(`
`) ? indentComment$1(n, t) : n.includes(`
`) ? `
` + indentComment$1(n, t) : (e5.endsWith(" ") ? "" : " ") + n;
stringifyComment$5$1.indentComment = indentComment$1;
stringifyComment$5$1.lineComment = lineComment$1;
stringifyComment$5$1.stringifyComment = stringifyComment$4$1;
var stringifyString$5$1 = {}, foldFlowLines$2$1 = {};
const FOLD_FLOW$1 = "flow", FOLD_BLOCK$1 = "block", FOLD_QUOTED$1 = "quoted";
function foldFlowLines$1$1(e5, t, n = "flow", { indentAtStart: r, lineWidth: s = 80, minContentWidth: o = 20, onFold: a, onOverflow: l } = {}) {
  if (!s || s < 0) return e5;
  s < o && (o = 0);
  const c = Math.max(1 + o, 1 + s - t.length);
  if (e5.length <= c) return e5;
  const u = [], p = {};
  let f = s - t.length;
  typeof r == "number" && (r > s - Math.max(2, o) ? u.push(0) : f = s - r);
  let h, m, g = false, d = -1, y = -1, S = -1;
  n === FOLD_BLOCK$1 && (d = consumeMoreIndentedLines$1(e5, d, t.length), d !== -1 && (f = d + c));
  for (let E; E = e5[d += 1]; ) {
    if (n === FOLD_QUOTED$1 && E === "\\") {
      switch (y = d, e5[d + 1]) {
        case "x":
          d += 3;
          break;
        case "u":
          d += 5;
          break;
        case "U":
          d += 9;
          break;
        default:
          d += 1;
      }
      S = d;
    }
    if (E === `
`) n === FOLD_BLOCK$1 && (d = consumeMoreIndentedLines$1(e5, d, t.length)), f = d + t.length + c, h = void 0;
    else {
      if (E === " " && m && m !== " " && m !== `
` && m !== "	") {
        const b = e5[d + 1];
        b && b !== " " && b !== `
` && b !== "	" && (h = d);
      }
      if (d >= f) if (h) u.push(h), f = h + c, h = void 0;
      else if (n === FOLD_QUOTED$1) {
        for (; m === " " || m === "	"; ) m = E, E = e5[d += 1], g = true;
        const b = d > S + 1 ? d - 2 : y - 1;
        if (p[b]) return e5;
        u.push(b), p[b] = true, f = b + c, h = void 0;
      } else g = true;
    }
    m = E;
  }
  if (g && l && l(), u.length === 0) return e5;
  a && a();
  let $ = e5.slice(0, u[0]);
  for (let E = 0; E < u.length; ++E) {
    const b = u[E], A = u[E + 1] || e5.length;
    b === 0 ? $ = `
${t}${e5.slice(0, A)}` : (n === FOLD_QUOTED$1 && p[b] && ($ += `${e5[b]}\\`), $ += `
${t}${e5.slice(b + 1, A)}`);
  }
  return $;
}
function consumeMoreIndentedLines$1(e5, t, n) {
  let r = t, s = t + 1, o = e5[s];
  for (; o === " " || o === "	"; ) if (t < s + n) o = e5[++t];
  else {
    do
      o = e5[++t];
    while (o && o !== `
`);
    r = t, s = t + 1, o = e5[s];
  }
  return r;
}
foldFlowLines$2$1.FOLD_BLOCK = FOLD_BLOCK$1;
foldFlowLines$2$1.FOLD_FLOW = FOLD_FLOW$1;
foldFlowLines$2$1.FOLD_QUOTED = FOLD_QUOTED$1;
foldFlowLines$2$1.foldFlowLines = foldFlowLines$1$1;
var Scalar$h$1 = Scalar$k$1, foldFlowLines$3 = foldFlowLines$2$1;
const getFoldOptions$1 = (e5, t) => ({ indentAtStart: t ? e5.indent.length : e5.indentAtStart, lineWidth: e5.options.lineWidth, minContentWidth: e5.options.minContentWidth }), containsDocumentMarker$1 = (e5) => /^(%|---|\.\.\.)/m.test(e5);
function lineLengthOverLimit$1(e5, t, n) {
  if (!t || t < 0) return false;
  const r = t - n, s = e5.length;
  if (s <= r) return false;
  for (let o = 0, a = 0; o < s; ++o) if (e5[o] === `
`) {
    if (o - a > r) return true;
    if (a = o + 1, s - a <= r) return false;
  }
  return true;
}
function doubleQuotedString$1(e5, t) {
  const n = JSON.stringify(e5);
  if (t.options.doubleQuotedAsJSON) return n;
  const { implicitKey: r } = t, s = t.options.doubleQuotedMinMultiLineLength, o = t.indent || (containsDocumentMarker$1(e5) ? "  " : "");
  let a = "", l = 0;
  for (let c = 0, u = n[c]; u; u = n[++c]) if (u === " " && n[c + 1] === "\\" && n[c + 2] === "n" && (a += n.slice(l, c) + "\\ ", c += 1, l = c, u = "\\"), u === "\\") switch (n[c + 1]) {
    case "u":
      {
        a += n.slice(l, c);
        const p = n.substr(c + 2, 4);
        switch (p) {
          case "0000":
            a += "\\0";
            break;
          case "0007":
            a += "\\a";
            break;
          case "000b":
            a += "\\v";
            break;
          case "001b":
            a += "\\e";
            break;
          case "0085":
            a += "\\N";
            break;
          case "00a0":
            a += "\\_";
            break;
          case "2028":
            a += "\\L";
            break;
          case "2029":
            a += "\\P";
            break;
          default:
            p.substr(0, 2) === "00" ? a += "\\x" + p.substr(2) : a += n.substr(c, 6);
        }
        c += 5, l = c + 1;
      }
      break;
    case "n":
      if (r || n[c + 2] === '"' || n.length < s) c += 1;
      else {
        for (a += n.slice(l, c) + `

`; n[c + 2] === "\\" && n[c + 3] === "n" && n[c + 4] !== '"'; ) a += `
`, c += 2;
        a += o, n[c + 2] === " " && (a += "\\"), c += 1, l = c + 1;
      }
      break;
    default:
      c += 1;
  }
  return a = l ? a + n.slice(l) : n, r ? a : foldFlowLines$3.foldFlowLines(a, o, foldFlowLines$3.FOLD_QUOTED, getFoldOptions$1(t, false));
}
function singleQuotedString$1(e5, t) {
  if (t.options.singleQuote === false || t.implicitKey && e5.includes(`
`) || /[ \t]\n|\n[ \t]/.test(e5)) return doubleQuotedString$1(e5, t);
  const n = t.indent || (containsDocumentMarker$1(e5) ? "  " : ""), r = "'" + e5.replace(/'/g, "''").replace(/\n+/g, `$&
${n}`) + "'";
  return t.implicitKey ? r : foldFlowLines$3.foldFlowLines(r, n, foldFlowLines$3.FOLD_FLOW, getFoldOptions$1(t, false));
}
function quotedString$1(e5, t) {
  const { singleQuote: n } = t.options;
  let r;
  if (n === false) r = doubleQuotedString$1;
  else {
    const s = e5.includes('"'), o = e5.includes("'");
    s && !o ? r = singleQuotedString$1 : o && !s ? r = doubleQuotedString$1 : r = n ? singleQuotedString$1 : doubleQuotedString$1;
  }
  return r(e5, t);
}
let blockEndNewlines$1;
try {
  blockEndNewlines$1 = new RegExp(`(^|(?<!
))
+(?!
|$)`, "g");
} catch {
  blockEndNewlines$1 = /\n+(?!\n|$)/g;
}
function blockString$1({ comment: e5, type: t, value: n }, r, s, o) {
  const { blockQuote: a, commentString: l, lineWidth: c } = r.options;
  if (!a || /\n[\t ]+$/.test(n)) return quotedString$1(n, r);
  const u = r.indent || (r.forceBlockIndent || containsDocumentMarker$1(n) ? "  " : ""), p = a === "literal" ? true : a === "folded" || t === Scalar$h$1.Scalar.BLOCK_FOLDED ? false : t === Scalar$h$1.Scalar.BLOCK_LITERAL ? true : !lineLengthOverLimit$1(n, c, u.length);
  if (!n) return p ? `|
` : `>
`;
  let f, h;
  for (h = n.length; h > 0; --h) {
    const A = n[h - 1];
    if (A !== `
` && A !== "	" && A !== " ") break;
  }
  let m = n.substring(h);
  const g = m.indexOf(`
`);
  g === -1 ? f = "-" : n === m || g !== m.length - 1 ? (f = "+", o && o()) : f = "", m && (n = n.slice(0, -m.length), m[m.length - 1] === `
` && (m = m.slice(0, -1)), m = m.replace(blockEndNewlines$1, `$&${u}`));
  let d = false, y, S = -1;
  for (y = 0; y < n.length; ++y) {
    const A = n[y];
    if (A === " ") d = true;
    else if (A === `
`) S = y;
    else break;
  }
  let $ = n.substring(0, S < y ? S + 1 : y);
  $ && (n = n.substring($.length), $ = $.replace(/\n+/g, `$&${u}`));
  let b = (d ? u ? "2" : "1" : "") + f;
  if (e5 && (b += " " + l(e5.replace(/ ?[\r\n]+/g, " ")), s && s()), !p) {
    const A = n.replace(/\n+/g, `
$&`).replace(/(?:^|\n)([\t ].*)(?:([\n\t ]*)\n(?![\n\t ]))?/g, "$1$2").replace(/\n+/g, `$&${u}`);
    let w = false;
    const v = getFoldOptions$1(r, true);
    a !== "folded" && t !== Scalar$h$1.Scalar.BLOCK_FOLDED && (v.onOverflow = () => {
      w = true;
    });
    const C = foldFlowLines$3.foldFlowLines(`${$}${A}${m}`, u, foldFlowLines$3.FOLD_BLOCK, v);
    if (!w) return `>${b}
${u}${C}`;
  }
  return n = n.replace(/\n+/g, `$&${u}`), `|${b}
${u}${$}${n}${m}`;
}
function plainString$1(e5, t, n, r) {
  const { type: s, value: o } = e5, { actualString: a, implicitKey: l, indent: c, indentStep: u, inFlow: p } = t;
  if (l && o.includes(`
`) || p && /[[\]{},]/.test(o)) return quotedString$1(o, t);
  if (/^[\n\t ,[\]{}#&*!|>'"%@`]|^[?-]$|^[?-][ \t]|[\n:][ \t]|[ \t]\n|[\n\t ]#|[\n\t :]$/.test(o)) return l || p || !o.includes(`
`) ? quotedString$1(o, t) : blockString$1(e5, t, n, r);
  if (!l && !p && s !== Scalar$h$1.Scalar.PLAIN && o.includes(`
`)) return blockString$1(e5, t, n, r);
  if (containsDocumentMarker$1(o)) {
    if (c === "") return t.forceBlockIndent = true, blockString$1(e5, t, n, r);
    if (l && c === u) return quotedString$1(o, t);
  }
  const f = o.replace(/\n+/g, `$&
${c}`);
  if (a) {
    const h = (d) => {
      var _a4;
      return d.default && d.tag !== "tag:yaml.org,2002:str" && ((_a4 = d.test) == null ? void 0 : _a4.test(f));
    }, { compat: m, tags: g } = t.doc.schema;
    if (g.some(h) || (m == null ? void 0 : m.some(h))) return quotedString$1(o, t);
  }
  return l ? f : foldFlowLines$3.foldFlowLines(f, c, foldFlowLines$3.FOLD_FLOW, getFoldOptions$1(t, false));
}
function stringifyString$4$1(e5, t, n, r) {
  const { implicitKey: s, inFlow: o } = t, a = typeof e5.value == "string" ? e5 : Object.assign({}, e5, { value: String(e5.value) });
  let { type: l } = e5;
  l !== Scalar$h$1.Scalar.QUOTE_DOUBLE && /[\x00-\x08\x0b-\x1f\x7f-\x9f\u{D800}-\u{DFFF}]/u.test(a.value) && (l = Scalar$h$1.Scalar.QUOTE_DOUBLE);
  const c = (p) => {
    switch (p) {
      case Scalar$h$1.Scalar.BLOCK_FOLDED:
      case Scalar$h$1.Scalar.BLOCK_LITERAL:
        return s || o ? quotedString$1(a.value, t) : blockString$1(a, t, n, r);
      case Scalar$h$1.Scalar.QUOTE_DOUBLE:
        return doubleQuotedString$1(a.value, t);
      case Scalar$h$1.Scalar.QUOTE_SINGLE:
        return singleQuotedString$1(a.value, t);
      case Scalar$h$1.Scalar.PLAIN:
        return plainString$1(a, t, n, r);
      default:
        return null;
    }
  };
  let u = c(l);
  if (u === null) {
    const { defaultKeyType: p, defaultStringType: f } = t.options, h = s && p || f;
    if (u = c(h), u === null) throw new Error(`Unsupported default string type ${h}`);
  }
  return u;
}
stringifyString$5$1.stringifyString = stringifyString$4$1;
var anchors$1$1 = anchors$3$1, identity$o$1 = identity$y$1, stringifyComment$3$1 = stringifyComment$5$1, stringifyString$3$1 = stringifyString$5$1;
function createStringifyContext$1(e5, t) {
  const n = Object.assign({ blockQuote: true, commentString: stringifyComment$3$1.stringifyComment, defaultKeyType: null, defaultStringType: "PLAIN", directives: null, doubleQuotedAsJSON: false, doubleQuotedMinMultiLineLength: 40, falseStr: "false", flowCollectionPadding: true, indentSeq: true, lineWidth: 80, minContentWidth: 20, nullStr: "null", simpleKeys: false, singleQuote: null, trailingComma: false, trueStr: "true", verifyAliasOrder: true }, e5.schema.toStringOptions, t);
  let r;
  switch (n.collectionStyle) {
    case "block":
      r = false;
      break;
    case "flow":
      r = true;
      break;
    default:
      r = null;
  }
  return { anchors: /* @__PURE__ */ new Set(), doc: e5, flowCollectionPadding: n.flowCollectionPadding ? " " : "", indent: "", indentStep: typeof n.indent == "number" ? " ".repeat(n.indent) : "  ", inFlow: r, options: n };
}
function getTagObject$1(e5, t) {
  var _a4, _b, _c2, _d;
  if (t.tag) {
    const s = e5.filter((o) => o.tag === t.tag);
    if (s.length > 0) return (_a4 = s.find((o) => o.format === t.format)) != null ? _a4 : s[0];
  }
  let n, r;
  if (identity$o$1.isScalar(t)) {
    r = t.value;
    let s = e5.filter((o) => {
      var _a5;
      return (_a5 = o.identify) == null ? void 0 : _a5.call(o, r);
    });
    if (s.length > 1) {
      const o = s.filter((a) => a.test);
      o.length > 0 && (s = o);
    }
    n = (_b = s.find((o) => o.format === t.format)) != null ? _b : s.find((o) => !o.format);
  } else r = t, n = e5.find((s) => s.nodeClass && r instanceof s.nodeClass);
  if (!n) {
    const s = (_d = (_c2 = r == null ? void 0 : r.constructor) == null ? void 0 : _c2.name) != null ? _d : r === null ? "null" : typeof r;
    throw new Error(`Tag not resolved for ${s} value`);
  }
  return n;
}
function stringifyProps$1(e5, t, { anchors: n, doc: r }) {
  var _a4;
  if (!r.directives) return "";
  const s = [], o = (identity$o$1.isScalar(e5) || identity$o$1.isCollection(e5)) && e5.anchor;
  o && anchors$1$1.anchorIsValid(o) && (n.add(o), s.push(`&${o}`));
  const a = (_a4 = e5.tag) != null ? _a4 : t.default ? null : t.tag;
  return a && s.push(r.directives.tagString(a)), s.join(" ");
}
function stringify$a$1(e5, t, n, r) {
  var _a4, _b;
  if (identity$o$1.isPair(e5)) return e5.toString(t, n, r);
  if (identity$o$1.isAlias(e5)) {
    if (t.doc.directives) return e5.toString(t);
    if ((_a4 = t.resolvedAliases) == null ? void 0 : _a4.has(e5)) throw new TypeError("Cannot stringify circular structure without alias nodes");
    t.resolvedAliases ? t.resolvedAliases.add(e5) : t.resolvedAliases = /* @__PURE__ */ new Set([e5]), e5 = e5.resolve(t.doc);
  }
  let s;
  const o = identity$o$1.isNode(e5) ? e5 : t.doc.createNode(e5, { onTagObj: (c) => s = c });
  s != null ? s : s = getTagObject$1(t.doc.schema.tags, o);
  const a = stringifyProps$1(o, s, t);
  a.length > 0 && (t.indentAtStart = ((_b = t.indentAtStart) != null ? _b : 0) + a.length + 1);
  const l = typeof s.stringify == "function" ? s.stringify(o, t, n, r) : identity$o$1.isScalar(o) ? stringifyString$3$1.stringifyString(o, t, n, r) : o.toString(t, n, r);
  return a ? identity$o$1.isScalar(o) || l[0] === "{" || l[0] === "[" ? `${a} ${l}` : `${a}
${t.indent}${l}` : l;
}
stringify$b$1.createStringifyContext = createStringifyContext$1;
stringify$b$1.stringify = stringify$a$1;
var identity$n$1 = identity$y$1, Scalar$g$1 = Scalar$k$1, stringify$9$1 = stringify$b$1, stringifyComment$2$1 = stringifyComment$5$1;
function stringifyPair$1$1({ key: e5, value: t }, n, r, s) {
  var _a4, _b;
  const { allNullValues: o, doc: a, indent: l, indentStep: c, options: { commentString: u, indentSeq: p, simpleKeys: f } } = n;
  let h = identity$n$1.isNode(e5) && e5.comment || null;
  if (f) {
    if (h) throw new Error("With simple keys, key nodes cannot have comments");
    if (identity$n$1.isCollection(e5) || !identity$n$1.isNode(e5) && typeof e5 == "object") {
      const v = "With simple keys, collection cannot be used as a key value";
      throw new Error(v);
    }
  }
  let m = !f && (!e5 || h && t == null && !n.inFlow || identity$n$1.isCollection(e5) || (identity$n$1.isScalar(e5) ? e5.type === Scalar$g$1.Scalar.BLOCK_FOLDED || e5.type === Scalar$g$1.Scalar.BLOCK_LITERAL : typeof e5 == "object"));
  n = Object.assign({}, n, { allNullValues: false, implicitKey: !m && (f || !o), indent: l + c });
  let g = false, d = false, y = stringify$9$1.stringify(e5, n, () => g = true, () => d = true);
  if (!m && !n.inFlow && y.length > 1024) {
    if (f) throw new Error("With simple keys, single line scalar must not span more than 1024 characters");
    m = true;
  }
  if (n.inFlow) {
    if (o || t == null) return g && r && r(), y === "" ? "?" : m ? `? ${y}` : y;
  } else if (o && !f || t == null && m) return y = `? ${y}`, h && !g ? y += stringifyComment$2$1.lineComment(y, n.indent, u(h)) : d && s && s(), y;
  g && (h = null), m ? (h && (y += stringifyComment$2$1.lineComment(y, n.indent, u(h))), y = `? ${y}
${l}:`) : (y = `${y}:`, h && (y += stringifyComment$2$1.lineComment(y, n.indent, u(h))));
  let S, $, E;
  identity$n$1.isNode(t) ? (S = !!t.spaceBefore, $ = t.commentBefore, E = t.comment) : (S = false, $ = null, E = null, t && typeof t == "object" && (t = a.createNode(t))), n.implicitKey = false, !m && !h && identity$n$1.isScalar(t) && (n.indentAtStart = y.length + 1), d = false, !p && c.length >= 2 && !n.inFlow && !m && identity$n$1.isSeq(t) && !t.flow && !t.tag && !t.anchor && (n.indent = n.indent.substring(2));
  let b = false;
  const A = stringify$9$1.stringify(t, n, () => b = true, () => d = true);
  let w = " ";
  if (h || S || $) {
    if (w = S ? `
` : "", $) {
      const v = u($);
      w += `
${stringifyComment$2$1.indentComment(v, n.indent)}`;
    }
    A === "" && !n.inFlow ? w === `
` && E && (w = `

`) : w += `
${n.indent}`;
  } else if (!m && identity$n$1.isCollection(t)) {
    const v = A[0], C = A.indexOf(`
`), T = C !== -1, O = (_b = (_a4 = n.inFlow) != null ? _a4 : t.flow) != null ? _b : t.items.length === 0;
    if (T || !O) {
      let L = false;
      if (T && (v === "&" || v === "!")) {
        let _ = A.indexOf(" ");
        v === "&" && _ !== -1 && _ < C && A[_ + 1] === "!" && (_ = A.indexOf(" ", _ + 1)), (_ === -1 || C < _) && (L = true);
      }
      L || (w = `
${n.indent}`);
    }
  } else (A === "" || A[0] === `
`) && (w = "");
  return y += w + A, n.inFlow ? b && r && r() : E && !b ? y += stringifyComment$2$1.lineComment(y, n.indent, u(E)) : d && s && s(), y;
}
stringifyPair$2$1.stringifyPair = stringifyPair$1$1;
var addPairToJSMap$3$1 = {}, log$2$1 = {}, node_process$2$1 = require$$0;
function debug$2(e5, ...t) {
  e5 === "debug" && console.log(...t);
}
function warn$1(e5, t) {
  (e5 === "debug" || e5 === "warn") && (typeof node_process$2$1.emitWarning == "function" ? node_process$2$1.emitWarning(t) : console.warn(t));
}
log$2$1.debug = debug$2;
log$2$1.warn = warn$1;
var merge$6$1 = {}, identity$m$1 = identity$y$1, Scalar$f$1 = Scalar$k$1;
const MERGE_KEY$1 = "<<", merge$5$1 = { identify: (e5) => e5 === MERGE_KEY$1 || typeof e5 == "symbol" && e5.description === MERGE_KEY$1, default: "key", tag: "tag:yaml.org,2002:merge", test: /^<<$/, resolve: () => Object.assign(new Scalar$f$1.Scalar(Symbol(MERGE_KEY$1)), { addToJSMap: addMergeToJSMap$1 }), stringify: () => MERGE_KEY$1 }, isMergeKey$1 = (e5, t) => (merge$5$1.identify(t) || identity$m$1.isScalar(t) && (!t.type || t.type === Scalar$f$1.Scalar.PLAIN) && merge$5$1.identify(t.value)) && (e5 == null ? void 0 : e5.doc.schema.tags.some((n) => n.tag === merge$5$1.tag && n.default));
function addMergeToJSMap$1(e5, t, n) {
  const r = resolveAliasValue$1(e5, n);
  if (identity$m$1.isSeq(r)) for (const s of r.items) mergeValue$1(e5, t, s);
  else if (Array.isArray(r)) for (const s of r) mergeValue$1(e5, t, s);
  else mergeValue$1(e5, t, r);
}
function mergeValue$1(e5, t, n) {
  const r = resolveAliasValue$1(e5, n);
  if (!identity$m$1.isMap(r)) throw new Error("Merge sources must be maps or map aliases");
  const s = r.toJSON(null, e5, Map);
  for (const [o, a] of s) t instanceof Map ? t.has(o) || t.set(o, a) : t instanceof Set ? t.add(o) : Object.prototype.hasOwnProperty.call(t, o) || Object.defineProperty(t, o, { value: a, writable: true, enumerable: true, configurable: true });
  return t;
}
function resolveAliasValue$1(e5, t) {
  return e5 && identity$m$1.isAlias(t) ? t.resolve(e5.doc, e5) : t;
}
merge$6$1.addMergeToJSMap = addMergeToJSMap$1;
merge$6$1.isMergeKey = isMergeKey$1;
merge$6$1.merge = merge$5$1;
var log$1$1 = log$2$1, merge$4$1 = merge$6$1, stringify$8$1 = stringify$b$1, identity$l$1 = identity$y$1, toJS$3$1 = toJS$8$1;
function addPairToJSMap$2$1(e5, t, { key: n, value: r }) {
  if (identity$l$1.isNode(n) && n.addToJSMap) n.addToJSMap(e5, t, r);
  else if (merge$4$1.isMergeKey(e5, n)) merge$4$1.addMergeToJSMap(e5, t, r);
  else {
    const s = toJS$3$1.toJS(n, "", e5);
    if (t instanceof Map) t.set(s, toJS$3$1.toJS(r, s, e5));
    else if (t instanceof Set) t.add(s);
    else {
      const o = stringifyKey$1(n, s, e5), a = toJS$3$1.toJS(r, o, e5);
      o in t ? Object.defineProperty(t, o, { value: a, writable: true, enumerable: true, configurable: true }) : t[o] = a;
    }
  }
  return t;
}
function stringifyKey$1(e5, t, n) {
  if (t === null) return "";
  if (typeof t != "object") return String(t);
  if (identity$l$1.isNode(e5) && (n == null ? void 0 : n.doc)) {
    const r = stringify$8$1.createStringifyContext(n.doc, {});
    r.anchors = /* @__PURE__ */ new Set();
    for (const o of n.anchors.keys()) r.anchors.add(o.anchor);
    r.inFlow = true, r.inStringifyKey = true;
    const s = e5.toString(r);
    if (!n.mapKeyWarned) {
      let o = JSON.stringify(s);
      o.length > 40 && (o = o.substring(0, 36) + '..."'), log$1$1.warn(n.doc.options.logLevel, `Keys with collection values will be stringified due to JS Object restrictions: ${o}. Set mapAsMap: true to use object keys.`), n.mapKeyWarned = true;
    }
    return s;
  }
  return JSON.stringify(t);
}
addPairToJSMap$3$1.addPairToJSMap = addPairToJSMap$2$1;
var createNode$2$1 = createNode$5$1, stringifyPair$3 = stringifyPair$2$1, addPairToJSMap$1$1 = addPairToJSMap$3$1, identity$k$1 = identity$y$1;
function createPair$1(e5, t, n) {
  const r = createNode$2$1.createNode(e5, void 0, n), s = createNode$2$1.createNode(t, void 0, n);
  return new Pair$7$1(r, s);
}
let Pair$7$1 = class P {
  constructor(t, n = null) {
    Object.defineProperty(this, identity$k$1.NODE_TYPE, { value: identity$k$1.PAIR }), this.key = t, this.value = n;
  }
  clone(t) {
    let { key: n, value: r } = this;
    return identity$k$1.isNode(n) && (n = n.clone(t)), identity$k$1.isNode(r) && (r = r.clone(t)), new P(n, r);
  }
  toJSON(t, n) {
    const r = (n == null ? void 0 : n.mapAsMap) ? /* @__PURE__ */ new Map() : {};
    return addPairToJSMap$1$1.addPairToJSMap(n, r, this);
  }
  toString(t, n, r) {
    return (t == null ? void 0 : t.doc) ? stringifyPair$3.stringifyPair(this, t, n, r) : JSON.stringify(this);
  }
};
Pair$8$1.Pair = Pair$7$1;
Pair$8$1.createPair = createPair$1;
var Schema$a$1 = {}, map$8$1 = {}, YAMLMap$8$1 = {}, stringifyCollection$3$1 = {}, identity$j$1 = identity$y$1, stringify$7$1 = stringify$b$1, stringifyComment$1$1 = stringifyComment$5$1;
function stringifyCollection$2$1(e5, t, n) {
  var _a4;
  return (((_a4 = t.inFlow) != null ? _a4 : e5.flow) ? stringifyFlowCollection$1 : stringifyBlockCollection$1)(e5, t, n);
}
function stringifyBlockCollection$1({ comment: e5, items: t }, n, { blockItemPrefix: r, flowChars: s, itemIndent: o, onChompKeep: a, onComment: l }) {
  const { indent: c, options: { commentString: u } } = n, p = Object.assign({}, n, { indent: o, type: null });
  let f = false;
  const h = [];
  for (let g = 0; g < t.length; ++g) {
    const d = t[g];
    let y = null;
    if (identity$j$1.isNode(d)) !f && d.spaceBefore && h.push(""), addCommentBefore$1(n, h, d.commentBefore, f), d.comment && (y = d.comment);
    else if (identity$j$1.isPair(d)) {
      const $ = identity$j$1.isNode(d.key) ? d.key : null;
      $ && (!f && $.spaceBefore && h.push(""), addCommentBefore$1(n, h, $.commentBefore, f));
    }
    f = false;
    let S = stringify$7$1.stringify(d, p, () => y = null, () => f = true);
    y && (S += stringifyComment$1$1.lineComment(S, o, u(y))), f && y && (f = false), h.push(r + S);
  }
  let m;
  if (h.length === 0) m = s.start + s.end;
  else {
    m = h[0];
    for (let g = 1; g < h.length; ++g) {
      const d = h[g];
      m += d ? `
${c}${d}` : `
`;
    }
  }
  return e5 ? (m += `
` + stringifyComment$1$1.indentComment(u(e5), c), l && l()) : f && a && a(), m;
}
function stringifyFlowCollection$1({ items: e5 }, t, { flowChars: n, itemIndent: r }) {
  const { indent: s, indentStep: o, flowCollectionPadding: a, options: { commentString: l } } = t;
  r += o;
  const c = Object.assign({}, t, { indent: r, inFlow: true, type: null });
  let u = false, p = 0;
  const f = [];
  for (let g = 0; g < e5.length; ++g) {
    const d = e5[g];
    let y = null;
    if (identity$j$1.isNode(d)) d.spaceBefore && f.push(""), addCommentBefore$1(t, f, d.commentBefore, false), d.comment && (y = d.comment);
    else if (identity$j$1.isPair(d)) {
      const $ = identity$j$1.isNode(d.key) ? d.key : null;
      $ && ($.spaceBefore && f.push(""), addCommentBefore$1(t, f, $.commentBefore, false), $.comment && (u = true));
      const E = identity$j$1.isNode(d.value) ? d.value : null;
      E ? (E.comment && (y = E.comment), E.commentBefore && (u = true)) : d.value == null && ($ == null ? void 0 : $.comment) && (y = $.comment);
    }
    y && (u = true);
    let S = stringify$7$1.stringify(d, c, () => y = null);
    u || (u = f.length > p || S.includes(`
`)), g < e5.length - 1 ? S += "," : t.options.trailingComma && (t.options.lineWidth > 0 && (u || (u = f.reduce(($, E) => $ + E.length + 2, 2) + (S.length + 2) > t.options.lineWidth)), u && (S += ",")), y && (S += stringifyComment$1$1.lineComment(S, r, l(y))), f.push(S), p = f.length;
  }
  const { start: h, end: m } = n;
  if (f.length === 0) return h + m;
  if (!u) {
    const g = f.reduce((d, y) => d + y.length + 2, 2);
    u = t.options.lineWidth > 0 && g > t.options.lineWidth;
  }
  if (u) {
    let g = h;
    for (const d of f) g += d ? `
${o}${s}${d}` : `
`;
    return `${g}
${s}${m}`;
  } else return `${h}${a}${f.join(" ")}${a}${m}`;
}
function addCommentBefore$1({ indent: e5, options: { commentString: t } }, n, r, s) {
  if (r && s && (r = r.replace(/^\n+/, "")), r) {
    const o = stringifyComment$1$1.indentComment(t(r), e5);
    n.push(o.trimStart());
  }
}
stringifyCollection$3$1.stringifyCollection = stringifyCollection$2$1;
var stringifyCollection$1$1 = stringifyCollection$3$1, addPairToJSMap$4 = addPairToJSMap$3$1, Collection$2$1 = Collection$4$1, identity$i$1 = identity$y$1, Pair$6$1 = Pair$8$1, Scalar$e$1 = Scalar$k$1;
function findPair$1(e5, t) {
  const n = identity$i$1.isScalar(t) ? t.value : t;
  for (const r of e5) if (identity$i$1.isPair(r) && (r.key === t || r.key === n || identity$i$1.isScalar(r.key) && r.key.value === n)) return r;
}
let YAMLMap$7$1 = class YAMLMap$7 extends Collection$2$1.Collection {
  static get tagName() {
    return "tag:yaml.org,2002:map";
  }
  constructor(t) {
    super(identity$i$1.MAP, t), this.items = [];
  }
  static from(t, n, r) {
    const { keepUndefined: s, replacer: o } = r, a = new this(t), l = (c, u) => {
      if (typeof o == "function") u = o.call(n, c, u);
      else if (Array.isArray(o) && !o.includes(c)) return;
      (u !== void 0 || s) && a.items.push(Pair$6$1.createPair(c, u, r));
    };
    if (n instanceof Map) for (const [c, u] of n) l(c, u);
    else if (n && typeof n == "object") for (const c of Object.keys(n)) l(c, n[c]);
    return typeof t.sortMapEntries == "function" && a.items.sort(t.sortMapEntries), a;
  }
  add(t, n) {
    var _a4;
    let r;
    identity$i$1.isPair(t) ? r = t : !t || typeof t != "object" || !("key" in t) ? r = new Pair$6$1.Pair(t, t == null ? void 0 : t.value) : r = new Pair$6$1.Pair(t.key, t.value);
    const s = findPair$1(this.items, r.key), o = (_a4 = this.schema) == null ? void 0 : _a4.sortMapEntries;
    if (s) {
      if (!n) throw new Error(`Key ${r.key} already set`);
      identity$i$1.isScalar(s.value) && Scalar$e$1.isScalarValue(r.value) ? s.value.value = r.value : s.value = r.value;
    } else if (o) {
      const a = this.items.findIndex((l) => o(r, l) < 0);
      a === -1 ? this.items.push(r) : this.items.splice(a, 0, r);
    } else this.items.push(r);
  }
  delete(t) {
    const n = findPair$1(this.items, t);
    return n ? this.items.splice(this.items.indexOf(n), 1).length > 0 : false;
  }
  get(t, n) {
    var _a4, _b;
    const s = (_a4 = findPair$1(this.items, t)) == null ? void 0 : _a4.value;
    return (_b = !n && identity$i$1.isScalar(s) ? s.value : s) != null ? _b : void 0;
  }
  has(t) {
    return !!findPair$1(this.items, t);
  }
  set(t, n) {
    this.add(new Pair$6$1.Pair(t, n), true);
  }
  toJSON(t, n, r) {
    const s = r ? new r() : (n == null ? void 0 : n.mapAsMap) ? /* @__PURE__ */ new Map() : {};
    (n == null ? void 0 : n.onCreate) && n.onCreate(s);
    for (const o of this.items) addPairToJSMap$4.addPairToJSMap(n, s, o);
    return s;
  }
  toString(t, n, r) {
    if (!t) return JSON.stringify(this);
    for (const s of this.items) if (!identity$i$1.isPair(s)) throw new Error(`Map items must all be pairs; found ${JSON.stringify(s)} instead`);
    return !t.allNullValues && this.hasAllNullValues(false) && (t = Object.assign({}, t, { allNullValues: true })), stringifyCollection$1$1.stringifyCollection(this, t, { blockItemPrefix: "", flowChars: { start: "{", end: "}" }, itemIndent: t.indent || "", onChompKeep: r, onComment: n });
  }
};
YAMLMap$8$1.YAMLMap = YAMLMap$7$1;
YAMLMap$8$1.findPair = findPair$1;
var identity$h$1 = identity$y$1, YAMLMap$6$1 = YAMLMap$8$1;
const map$7$1 = { collection: "map", default: true, nodeClass: YAMLMap$6$1.YAMLMap, tag: "tag:yaml.org,2002:map", resolve(e5, t) {
  return identity$h$1.isMap(e5) || t("Expected a mapping for this tag"), e5;
}, createNode: (e5, t, n) => YAMLMap$6$1.YAMLMap.from(e5, t, n) };
map$8$1.map = map$7$1;
var seq$8$1 = {}, YAMLSeq$7$1 = {}, createNode$1$1 = createNode$5$1, stringifyCollection$4 = stringifyCollection$3$1, Collection$1$1 = Collection$4$1, identity$g$1 = identity$y$1, Scalar$d$1 = Scalar$k$1, toJS$2$1 = toJS$8$1;
let YAMLSeq$6$1 = class YAMLSeq$6 extends Collection$1$1.Collection {
  static get tagName() {
    return "tag:yaml.org,2002:seq";
  }
  constructor(t) {
    super(identity$g$1.SEQ, t), this.items = [];
  }
  add(t) {
    this.items.push(t);
  }
  delete(t) {
    const n = asItemIndex$1(t);
    return typeof n != "number" ? false : this.items.splice(n, 1).length > 0;
  }
  get(t, n) {
    const r = asItemIndex$1(t);
    if (typeof r != "number") return;
    const s = this.items[r];
    return !n && identity$g$1.isScalar(s) ? s.value : s;
  }
  has(t) {
    const n = asItemIndex$1(t);
    return typeof n == "number" && n < this.items.length;
  }
  set(t, n) {
    const r = asItemIndex$1(t);
    if (typeof r != "number") throw new Error(`Expected a valid index, not ${t}.`);
    const s = this.items[r];
    identity$g$1.isScalar(s) && Scalar$d$1.isScalarValue(n) ? s.value = n : this.items[r] = n;
  }
  toJSON(t, n) {
    const r = [];
    (n == null ? void 0 : n.onCreate) && n.onCreate(r);
    let s = 0;
    for (const o of this.items) r.push(toJS$2$1.toJS(o, String(s++), n));
    return r;
  }
  toString(t, n, r) {
    return t ? stringifyCollection$4.stringifyCollection(this, t, { blockItemPrefix: "- ", flowChars: { start: "[", end: "]" }, itemIndent: (t.indent || "") + "  ", onChompKeep: r, onComment: n }) : JSON.stringify(this);
  }
  static from(t, n, r) {
    const { replacer: s } = r, o = new this(t);
    if (n && Symbol.iterator in Object(n)) {
      let a = 0;
      for (let l of n) {
        if (typeof s == "function") {
          const c = n instanceof Set ? l : String(a++);
          l = s.call(n, c, l);
        }
        o.items.push(createNode$1$1.createNode(l, void 0, r));
      }
    }
    return o;
  }
};
function asItemIndex$1(e5) {
  let t = identity$g$1.isScalar(e5) ? e5.value : e5;
  return t && typeof t == "string" && (t = Number(t)), typeof t == "number" && Number.isInteger(t) && t >= 0 ? t : null;
}
YAMLSeq$7$1.YAMLSeq = YAMLSeq$6$1;
var identity$f$1 = identity$y$1, YAMLSeq$5$1 = YAMLSeq$7$1;
const seq$7$1 = { collection: "seq", default: true, nodeClass: YAMLSeq$5$1.YAMLSeq, tag: "tag:yaml.org,2002:seq", resolve(e5, t) {
  return identity$f$1.isSeq(e5) || t("Expected a sequence for this tag"), e5;
}, createNode: (e5, t, n) => YAMLSeq$5$1.YAMLSeq.from(e5, t, n) };
seq$8$1.seq = seq$7$1;
var string$5$1 = {}, stringifyString$2$1 = stringifyString$5$1;
const string$4$1 = { identify: (e5) => typeof e5 == "string", default: true, tag: "tag:yaml.org,2002:str", resolve: (e5) => e5, stringify(e5, t, n, r) {
  return t = Object.assign({ actualString: true }, t), stringifyString$2$1.stringifyString(e5, t, n, r);
} };
string$5$1.string = string$4$1;
var tags$1$1 = {}, _null$5$1 = {}, Scalar$c$1 = Scalar$k$1;
const nullTag$1 = { identify: (e5) => e5 == null, createNode: () => new Scalar$c$1.Scalar(null), default: true, tag: "tag:yaml.org,2002:null", test: /^(?:~|[Nn]ull|NULL)?$/, resolve: () => new Scalar$c$1.Scalar(null), stringify: ({ source: e5 }, t) => typeof e5 == "string" && nullTag$1.test.test(e5) ? e5 : t.options.nullStr };
_null$5$1.nullTag = nullTag$1;
var bool$6$1 = {}, Scalar$b$1 = Scalar$k$1;
const boolTag$1 = { identify: (e5) => typeof e5 == "boolean", default: true, tag: "tag:yaml.org,2002:bool", test: /^(?:[Tt]rue|TRUE|[Ff]alse|FALSE)$/, resolve: (e5) => new Scalar$b$1.Scalar(e5[0] === "t" || e5[0] === "T"), stringify({ source: e5, value: t }, n) {
  if (e5 && boolTag$1.test.test(e5)) {
    const r = e5[0] === "t" || e5[0] === "T";
    if (t === r) return e5;
  }
  return t ? n.options.trueStr : n.options.falseStr;
} };
bool$6$1.boolTag = boolTag$1;
var float$8$1 = {}, stringifyNumber$6$1 = {};
function stringifyNumber$5$1({ format: e5, minFractionDigits: t, tag: n, value: r }) {
  if (typeof r == "bigint") return String(r);
  const s = typeof r == "number" ? r : Number(r);
  if (!isFinite(s)) return isNaN(s) ? ".nan" : s < 0 ? "-.inf" : ".inf";
  let o = Object.is(r, -0) ? "-0" : JSON.stringify(r);
  if (!e5 && t && (!n || n === "tag:yaml.org,2002:float") && /^-?\d/.test(o) && !o.includes("e")) {
    let a = o.indexOf(".");
    a < 0 && (a = o.length, o += ".");
    let l = t - (o.length - a - 1);
    for (; l-- > 0; ) o += "0";
  }
  return o;
}
stringifyNumber$6$1.stringifyNumber = stringifyNumber$5$1;
var Scalar$a$1 = Scalar$k$1, stringifyNumber$4$1 = stringifyNumber$6$1;
const floatNaN$1$1 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", test: /^(?:[-+]?\.(?:inf|Inf|INF)|\.nan|\.NaN|\.NAN)$/, resolve: (e5) => e5.slice(-3).toLowerCase() === "nan" ? NaN : e5[0] === "-" ? Number.NEGATIVE_INFINITY : Number.POSITIVE_INFINITY, stringify: stringifyNumber$4$1.stringifyNumber }, floatExp$1$1 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", format: "EXP", test: /^[-+]?(?:\.[0-9]+|[0-9]+(?:\.[0-9]*)?)[eE][-+]?[0-9]+$/, resolve: (e5) => parseFloat(e5), stringify(e5) {
  const t = Number(e5.value);
  return isFinite(t) ? t.toExponential() : stringifyNumber$4$1.stringifyNumber(e5);
} }, float$7$1 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", test: /^[-+]?(?:\.[0-9]+|[0-9]+\.[0-9]*)$/, resolve(e5) {
  const t = new Scalar$a$1.Scalar(parseFloat(e5)), n = e5.indexOf(".");
  return n !== -1 && e5[e5.length - 1] === "0" && (t.minFractionDigits = e5.length - n - 1), t;
}, stringify: stringifyNumber$4$1.stringifyNumber };
float$8$1.float = float$7$1;
float$8$1.floatExp = floatExp$1$1;
float$8$1.floatNaN = floatNaN$1$1;
var int$8$1 = {}, stringifyNumber$3$1 = stringifyNumber$6$1;
const intIdentify$2$1 = (e5) => typeof e5 == "bigint" || Number.isInteger(e5), intResolve$1$1 = (e5, t, n, { intAsBigInt: r }) => r ? BigInt(e5) : parseInt(e5.substring(t), n);
function intStringify$1$1(e5, t, n) {
  const { value: r } = e5;
  return intIdentify$2$1(r) && r >= 0 ? n + r.toString(t) : stringifyNumber$3$1.stringifyNumber(e5);
}
const intOct$1$1 = { identify: (e5) => intIdentify$2$1(e5) && e5 >= 0, default: true, tag: "tag:yaml.org,2002:int", format: "OCT", test: /^0o[0-7]+$/, resolve: (e5, t, n) => intResolve$1$1(e5, 2, 8, n), stringify: (e5) => intStringify$1$1(e5, 8, "0o") }, int$7$1 = { identify: intIdentify$2$1, default: true, tag: "tag:yaml.org,2002:int", test: /^[-+]?[0-9]+$/, resolve: (e5, t, n) => intResolve$1$1(e5, 0, 10, n), stringify: stringifyNumber$3$1.stringifyNumber }, intHex$1$1 = { identify: (e5) => intIdentify$2$1(e5) && e5 >= 0, default: true, tag: "tag:yaml.org,2002:int", format: "HEX", test: /^0x[0-9a-fA-F]+$/, resolve: (e5, t, n) => intResolve$1$1(e5, 2, 16, n), stringify: (e5) => intStringify$1$1(e5, 16, "0x") };
int$8$1.int = int$7$1;
int$8$1.intHex = intHex$1$1;
int$8$1.intOct = intOct$1$1;
var schema$8$1 = {}, map$6$1 = map$8$1, _null$4$1 = _null$5$1, seq$6$1 = seq$8$1, string$3$1 = string$5$1, bool$5$1 = bool$6$1, float$6$1 = float$8$1, int$6$1 = int$8$1;
const schema$7$1 = [map$6$1.map, seq$6$1.seq, string$3$1.string, _null$4$1.nullTag, bool$5$1.boolTag, int$6$1.intOct, int$6$1.int, int$6$1.intHex, float$6$1.floatNaN, float$6$1.floatExp, float$6$1.float];
schema$8$1.schema = schema$7$1;
var schema$6$1 = {}, Scalar$9$1 = Scalar$k$1, map$5$1 = map$8$1, seq$5$1 = seq$8$1;
function intIdentify$1$1(e5) {
  return typeof e5 == "bigint" || Number.isInteger(e5);
}
const stringifyJSON$1 = ({ value: e5 }) => JSON.stringify(e5), jsonScalars$1 = [{ identify: (e5) => typeof e5 == "string", default: true, tag: "tag:yaml.org,2002:str", resolve: (e5) => e5, stringify: stringifyJSON$1 }, { identify: (e5) => e5 == null, createNode: () => new Scalar$9$1.Scalar(null), default: true, tag: "tag:yaml.org,2002:null", test: /^null$/, resolve: () => null, stringify: stringifyJSON$1 }, { identify: (e5) => typeof e5 == "boolean", default: true, tag: "tag:yaml.org,2002:bool", test: /^true$|^false$/, resolve: (e5) => e5 === "true", stringify: stringifyJSON$1 }, { identify: intIdentify$1$1, default: true, tag: "tag:yaml.org,2002:int", test: /^-?(?:0|[1-9][0-9]*)$/, resolve: (e5, t, { intAsBigInt: n }) => n ? BigInt(e5) : parseInt(e5, 10), stringify: ({ value: e5 }) => intIdentify$1$1(e5) ? e5.toString() : JSON.stringify(e5) }, { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", test: /^-?(?:0|[1-9][0-9]*)(?:\.[0-9]*)?(?:[eE][-+]?[0-9]+)?$/, resolve: (e5) => parseFloat(e5), stringify: stringifyJSON$1 }], jsonError$1 = { default: true, tag: "", test: /^/, resolve(e5, t) {
  return t(`Unresolved plain scalar ${JSON.stringify(e5)}`), e5;
} }, schema$5$1 = [map$5$1.map, seq$5$1.seq].concat(jsonScalars$1, jsonError$1);
schema$6$1.schema = schema$5$1;
var binary$5$1 = {}, node_buffer$1 = require$$0$1, Scalar$8$1 = Scalar$k$1, stringifyString$1$1 = stringifyString$5$1;
const binary$4$1 = { identify: (e5) => e5 instanceof Uint8Array, default: false, tag: "tag:yaml.org,2002:binary", resolve(e5, t) {
  if (typeof node_buffer$1.Buffer == "function") return node_buffer$1.Buffer.from(e5, "base64");
  if (typeof atob == "function") {
    const n = atob(e5.replace(/[\n\r]/g, "")), r = new Uint8Array(n.length);
    for (let s = 0; s < n.length; ++s) r[s] = n.charCodeAt(s);
    return r;
  } else return t("This environment does not support reading binary tags; either Buffer or atob is required"), e5;
}, stringify({ comment: e5, type: t, value: n }, r, s, o) {
  if (!n) return "";
  const a = n;
  let l;
  if (typeof node_buffer$1.Buffer == "function") l = a instanceof node_buffer$1.Buffer ? a.toString("base64") : node_buffer$1.Buffer.from(a.buffer).toString("base64");
  else if (typeof btoa == "function") {
    let c = "";
    for (let u = 0; u < a.length; ++u) c += String.fromCharCode(a[u]);
    l = btoa(c);
  } else throw new Error("This environment does not support writing binary tags; either Buffer or btoa is required");
  if (t != null ? t : t = Scalar$8$1.Scalar.BLOCK_LITERAL, t !== Scalar$8$1.Scalar.QUOTE_DOUBLE) {
    const c = Math.max(r.options.lineWidth - r.indent.length, r.options.minContentWidth), u = Math.ceil(l.length / c), p = new Array(u);
    for (let f = 0, h = 0; f < u; ++f, h += c) p[f] = l.substr(h, c);
    l = p.join(t === Scalar$8$1.Scalar.BLOCK_LITERAL ? `
` : " ");
  }
  return stringifyString$1$1.stringifyString({ comment: e5, type: t, value: l }, r, s, o);
} };
binary$5$1.binary = binary$4$1;
var omap$5$1 = {}, pairs$6$1 = {}, identity$e$1 = identity$y$1, Pair$5$1 = Pair$8$1, Scalar$7$1 = Scalar$k$1, YAMLSeq$4$1 = YAMLSeq$7$1;
function resolvePairs$1(e5, t) {
  var _a4;
  if (identity$e$1.isSeq(e5)) for (let n = 0; n < e5.items.length; ++n) {
    let r = e5.items[n];
    if (!identity$e$1.isPair(r)) {
      if (identity$e$1.isMap(r)) {
        r.items.length > 1 && t("Each pair must have its own sequence indicator");
        const s = r.items[0] || new Pair$5$1.Pair(new Scalar$7$1.Scalar(null));
        if (r.commentBefore && (s.key.commentBefore = s.key.commentBefore ? `${r.commentBefore}
${s.key.commentBefore}` : r.commentBefore), r.comment) {
          const o = (_a4 = s.value) != null ? _a4 : s.key;
          o.comment = o.comment ? `${r.comment}
${o.comment}` : r.comment;
        }
        r = s;
      }
      e5.items[n] = identity$e$1.isPair(r) ? r : new Pair$5$1.Pair(r);
    }
  }
  else t("Expected a sequence for this tag");
  return e5;
}
function createPairs$1(e5, t, n) {
  const { replacer: r } = n, s = new YAMLSeq$4$1.YAMLSeq(e5);
  s.tag = "tag:yaml.org,2002:pairs";
  let o = 0;
  if (t && Symbol.iterator in Object(t)) for (let a of t) {
    typeof r == "function" && (a = r.call(t, String(o++), a));
    let l, c;
    if (Array.isArray(a)) if (a.length === 2) l = a[0], c = a[1];
    else throw new TypeError(`Expected [key, value] tuple: ${a}`);
    else if (a && a instanceof Object) {
      const u = Object.keys(a);
      if (u.length === 1) l = u[0], c = a[l];
      else throw new TypeError(`Expected tuple with one key, not ${u.length} keys`);
    } else l = a;
    s.items.push(Pair$5$1.createPair(l, c, n));
  }
  return s;
}
const pairs$5$1 = { collection: "seq", default: false, tag: "tag:yaml.org,2002:pairs", resolve: resolvePairs$1, createNode: createPairs$1 };
pairs$6$1.createPairs = createPairs$1;
pairs$6$1.pairs = pairs$5$1;
pairs$6$1.resolvePairs = resolvePairs$1;
var identity$d$1 = identity$y$1, toJS$1$1 = toJS$8$1, YAMLMap$5$1 = YAMLMap$8$1, YAMLSeq$3$1 = YAMLSeq$7$1, pairs$4$1 = pairs$6$1;
let YAMLOMap$1 = class YAMLOMap extends YAMLSeq$3$1.YAMLSeq {
  constructor() {
    super(), this.add = YAMLMap$5$1.YAMLMap.prototype.add.bind(this), this.delete = YAMLMap$5$1.YAMLMap.prototype.delete.bind(this), this.get = YAMLMap$5$1.YAMLMap.prototype.get.bind(this), this.has = YAMLMap$5$1.YAMLMap.prototype.has.bind(this), this.set = YAMLMap$5$1.YAMLMap.prototype.set.bind(this), this.tag = YAMLOMap.tag;
  }
  toJSON(t, n) {
    if (!n) return super.toJSON(t);
    const r = /* @__PURE__ */ new Map();
    (n == null ? void 0 : n.onCreate) && n.onCreate(r);
    for (const s of this.items) {
      let o, a;
      if (identity$d$1.isPair(s) ? (o = toJS$1$1.toJS(s.key, "", n), a = toJS$1$1.toJS(s.value, o, n)) : o = toJS$1$1.toJS(s, "", n), r.has(o)) throw new Error("Ordered maps must not include duplicate keys");
      r.set(o, a);
    }
    return r;
  }
  static from(t, n, r) {
    const s = pairs$4$1.createPairs(t, n, r), o = new this();
    return o.items = s.items, o;
  }
};
YAMLOMap$1.tag = "tag:yaml.org,2002:omap";
const omap$4$1 = { collection: "seq", identify: (e5) => e5 instanceof Map, nodeClass: YAMLOMap$1, default: false, tag: "tag:yaml.org,2002:omap", resolve(e5, t) {
  const n = pairs$4$1.resolvePairs(e5, t), r = [];
  for (const { key: s } of n.items) identity$d$1.isScalar(s) && (r.includes(s.value) ? t(`Ordered maps must not include duplicate keys: ${s.value}`) : r.push(s.value));
  return Object.assign(new YAMLOMap$1(), n);
}, createNode: (e5, t, n) => YAMLOMap$1.from(e5, t, n) };
omap$5$1.YAMLOMap = YAMLOMap$1;
omap$5$1.omap = omap$4$1;
var schema$4$1 = {}, bool$4$1 = {}, Scalar$6$1 = Scalar$k$1;
function boolStringify$1({ value: e5, source: t }, n) {
  return t && (e5 ? trueTag$1 : falseTag$1).test.test(t) ? t : e5 ? n.options.trueStr : n.options.falseStr;
}
const trueTag$1 = { identify: (e5) => e5 === true, default: true, tag: "tag:yaml.org,2002:bool", test: /^(?:Y|y|[Yy]es|YES|[Tt]rue|TRUE|[Oo]n|ON)$/, resolve: () => new Scalar$6$1.Scalar(true), stringify: boolStringify$1 }, falseTag$1 = { identify: (e5) => e5 === false, default: true, tag: "tag:yaml.org,2002:bool", test: /^(?:N|n|[Nn]o|NO|[Ff]alse|FALSE|[Oo]ff|OFF)$/, resolve: () => new Scalar$6$1.Scalar(false), stringify: boolStringify$1 };
bool$4$1.falseTag = falseTag$1;
bool$4$1.trueTag = trueTag$1;
var float$5$1 = {}, Scalar$5$1 = Scalar$k$1, stringifyNumber$2$1 = stringifyNumber$6$1;
const floatNaN$2 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", test: /^(?:[-+]?\.(?:inf|Inf|INF)|\.nan|\.NaN|\.NAN)$/, resolve: (e5) => e5.slice(-3).toLowerCase() === "nan" ? NaN : e5[0] === "-" ? Number.NEGATIVE_INFINITY : Number.POSITIVE_INFINITY, stringify: stringifyNumber$2$1.stringifyNumber }, floatExp$2 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", format: "EXP", test: /^[-+]?(?:[0-9][0-9_]*)?(?:\.[0-9_]*)?[eE][-+]?[0-9]+$/, resolve: (e5) => parseFloat(e5.replace(/_/g, "")), stringify(e5) {
  const t = Number(e5.value);
  return isFinite(t) ? t.toExponential() : stringifyNumber$2$1.stringifyNumber(e5);
} }, float$4$1 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", test: /^[-+]?(?:[0-9][0-9_]*)?\.[0-9_]*$/, resolve(e5) {
  const t = new Scalar$5$1.Scalar(parseFloat(e5.replace(/_/g, ""))), n = e5.indexOf(".");
  if (n !== -1) {
    const r = e5.substring(n + 1).replace(/_/g, "");
    r[r.length - 1] === "0" && (t.minFractionDigits = r.length);
  }
  return t;
}, stringify: stringifyNumber$2$1.stringifyNumber };
float$5$1.float = float$4$1;
float$5$1.floatExp = floatExp$2;
float$5$1.floatNaN = floatNaN$2;
var int$5$1 = {}, stringifyNumber$1$1 = stringifyNumber$6$1;
const intIdentify$3 = (e5) => typeof e5 == "bigint" || Number.isInteger(e5);
function intResolve$2(e5, t, n, { intAsBigInt: r }) {
  const s = e5[0];
  if ((s === "-" || s === "+") && (t += 1), e5 = e5.substring(t).replace(/_/g, ""), r) {
    switch (n) {
      case 2:
        e5 = `0b${e5}`;
        break;
      case 8:
        e5 = `0o${e5}`;
        break;
      case 16:
        e5 = `0x${e5}`;
        break;
    }
    const a = BigInt(e5);
    return s === "-" ? BigInt(-1) * a : a;
  }
  const o = parseInt(e5, n);
  return s === "-" ? -1 * o : o;
}
function intStringify$2(e5, t, n) {
  const { value: r } = e5;
  if (intIdentify$3(r)) {
    const s = r.toString(t);
    return r < 0 ? "-" + n + s.substr(1) : n + s;
  }
  return stringifyNumber$1$1.stringifyNumber(e5);
}
const intBin$1 = { identify: intIdentify$3, default: true, tag: "tag:yaml.org,2002:int", format: "BIN", test: /^[-+]?0b[0-1_]+$/, resolve: (e5, t, n) => intResolve$2(e5, 2, 2, n), stringify: (e5) => intStringify$2(e5, 2, "0b") }, intOct$2 = { identify: intIdentify$3, default: true, tag: "tag:yaml.org,2002:int", format: "OCT", test: /^[-+]?0[0-7_]+$/, resolve: (e5, t, n) => intResolve$2(e5, 1, 8, n), stringify: (e5) => intStringify$2(e5, 8, "0") }, int$4$1 = { identify: intIdentify$3, default: true, tag: "tag:yaml.org,2002:int", test: /^[-+]?[0-9][0-9_]*$/, resolve: (e5, t, n) => intResolve$2(e5, 0, 10, n), stringify: stringifyNumber$1$1.stringifyNumber }, intHex$2 = { identify: intIdentify$3, default: true, tag: "tag:yaml.org,2002:int", format: "HEX", test: /^[-+]?0x[0-9a-fA-F_]+$/, resolve: (e5, t, n) => intResolve$2(e5, 2, 16, n), stringify: (e5) => intStringify$2(e5, 16, "0x") };
int$5$1.int = int$4$1;
int$5$1.intBin = intBin$1;
int$5$1.intHex = intHex$2;
int$5$1.intOct = intOct$2;
var set$5$1 = {}, identity$c$1 = identity$y$1, Pair$4$1 = Pair$8$1, YAMLMap$4$1 = YAMLMap$8$1;
let YAMLSet$1 = class YAMLSet extends YAMLMap$4$1.YAMLMap {
  constructor(t) {
    super(t), this.tag = YAMLSet.tag;
  }
  add(t) {
    let n;
    identity$c$1.isPair(t) ? n = t : t && typeof t == "object" && "key" in t && "value" in t && t.value === null ? n = new Pair$4$1.Pair(t.key, null) : n = new Pair$4$1.Pair(t, null), YAMLMap$4$1.findPair(this.items, n.key) || this.items.push(n);
  }
  get(t, n) {
    const r = YAMLMap$4$1.findPair(this.items, t);
    return !n && identity$c$1.isPair(r) ? identity$c$1.isScalar(r.key) ? r.key.value : r.key : r;
  }
  set(t, n) {
    if (typeof n != "boolean") throw new Error(`Expected boolean value for set(key, value) in a YAML set, not ${typeof n}`);
    const r = YAMLMap$4$1.findPair(this.items, t);
    r && !n ? this.items.splice(this.items.indexOf(r), 1) : !r && n && this.items.push(new Pair$4$1.Pair(t));
  }
  toJSON(t, n) {
    return super.toJSON(t, n, Set);
  }
  toString(t, n, r) {
    if (!t) return JSON.stringify(this);
    if (this.hasAllNullValues(true)) return super.toString(Object.assign({}, t, { allNullValues: true }), n, r);
    throw new Error("Set items must all have null values");
  }
  static from(t, n, r) {
    const { replacer: s } = r, o = new this(t);
    if (n && Symbol.iterator in Object(n)) for (let a of n) typeof s == "function" && (a = s.call(n, a, a)), o.items.push(Pair$4$1.createPair(a, null, r));
    return o;
  }
};
YAMLSet$1.tag = "tag:yaml.org,2002:set";
const set$4$1 = { collection: "map", identify: (e5) => e5 instanceof Set, nodeClass: YAMLSet$1, default: false, tag: "tag:yaml.org,2002:set", createNode: (e5, t, n) => YAMLSet$1.from(e5, t, n), resolve(e5, t) {
  if (identity$c$1.isMap(e5)) {
    if (e5.hasAllNullValues(true)) return Object.assign(new YAMLSet$1(), e5);
    t("Set items must all have null values");
  } else t("Expected a mapping for this tag");
  return e5;
} };
set$5$1.YAMLSet = YAMLSet$1;
set$5$1.set = set$4$1;
var timestamp$5$1 = {}, stringifyNumber$7 = stringifyNumber$6$1;
function parseSexagesimal$1(e5, t) {
  const n = e5[0], r = n === "-" || n === "+" ? e5.substring(1) : e5, s = (a) => t ? BigInt(a) : Number(a), o = r.replace(/_/g, "").split(":").reduce((a, l) => a * s(60) + s(l), s(0));
  return n === "-" ? s(-1) * o : o;
}
function stringifySexagesimal$1(e5) {
  let { value: t } = e5, n = (a) => a;
  if (typeof t == "bigint") n = (a) => BigInt(a);
  else if (isNaN(t) || !isFinite(t)) return stringifyNumber$7.stringifyNumber(e5);
  let r = "";
  t < 0 && (r = "-", t *= n(-1));
  const s = n(60), o = [t % s];
  return t < 60 ? o.unshift(0) : (t = (t - o[0]) / s, o.unshift(t % s), t >= 60 && (t = (t - o[0]) / s, o.unshift(t))), r + o.map((a) => String(a).padStart(2, "0")).join(":").replace(/000000\d*$/, "");
}
const intTime$1 = { identify: (e5) => typeof e5 == "bigint" || Number.isInteger(e5), default: true, tag: "tag:yaml.org,2002:int", format: "TIME", test: /^[-+]?[0-9][0-9_]*(?::[0-5]?[0-9])+$/, resolve: (e5, t, { intAsBigInt: n }) => parseSexagesimal$1(e5, n), stringify: stringifySexagesimal$1 }, floatTime$1 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", format: "TIME", test: /^[-+]?[0-9][0-9_]*(?::[0-5]?[0-9])+\.[0-9_]*$/, resolve: (e5) => parseSexagesimal$1(e5, false), stringify: stringifySexagesimal$1 }, timestamp$4$1 = { identify: (e5) => e5 instanceof Date, default: true, tag: "tag:yaml.org,2002:timestamp", test: RegExp("^([0-9]{4})-([0-9]{1,2})-([0-9]{1,2})(?:(?:t|T|[ \\t]+)([0-9]{1,2}):([0-9]{1,2}):([0-9]{1,2}(\\.[0-9]+)?)(?:[ \\t]*(Z|[-+][012]?[0-9](?::[0-9]{2})?))?)?$"), resolve(e5) {
  const t = e5.match(timestamp$4$1.test);
  if (!t) throw new Error("!!timestamp expects a date, starting with yyyy-mm-dd");
  const [, n, r, s, o, a, l] = t.map(Number), c = t[7] ? Number((t[7] + "00").substr(1, 3)) : 0;
  let u = Date.UTC(n, r - 1, s, o || 0, a || 0, l || 0, c);
  const p = t[8];
  if (p && p !== "Z") {
    let f = parseSexagesimal$1(p, false);
    Math.abs(f) < 30 && (f *= 60), u -= 6e4 * f;
  }
  return new Date(u);
}, stringify: ({ value: e5 }) => {
  var _a4;
  return (_a4 = e5 == null ? void 0 : e5.toISOString().replace(/(T00:00:00)?\.000Z$/, "")) != null ? _a4 : "";
} };
timestamp$5$1.floatTime = floatTime$1;
timestamp$5$1.intTime = intTime$1;
timestamp$5$1.timestamp = timestamp$4$1;
var map$4$1 = map$8$1, _null$3$1 = _null$5$1, seq$4$1 = seq$8$1, string$2$1 = string$5$1, binary$3$1 = binary$5$1, bool$3$1 = bool$4$1, float$3$1 = float$5$1, int$3$1 = int$5$1, merge$3$1 = merge$6$1, omap$3$1 = omap$5$1, pairs$3$1 = pairs$6$1, set$3$1 = set$5$1, timestamp$3$1 = timestamp$5$1;
const schema$3$1 = [map$4$1.map, seq$4$1.seq, string$2$1.string, _null$3$1.nullTag, bool$3$1.trueTag, bool$3$1.falseTag, int$3$1.intBin, int$3$1.intOct, int$3$1.int, int$3$1.intHex, float$3$1.floatNaN, float$3$1.floatExp, float$3$1.float, binary$3$1.binary, merge$3$1.merge, omap$3$1.omap, pairs$3$1.pairs, set$3$1.set, timestamp$3$1.intTime, timestamp$3$1.floatTime, timestamp$3$1.timestamp];
schema$4$1.schema = schema$3$1;
var map$3$1 = map$8$1, _null$2$1 = _null$5$1, seq$3$1 = seq$8$1, string$1$1 = string$5$1, bool$2$1 = bool$6$1, float$2$1 = float$8$1, int$2$1 = int$8$1, schema$2$2 = schema$8$1, schema$1$1$1 = schema$6$1, binary$2$1 = binary$5$1, merge$2$1 = merge$6$1, omap$2$1 = omap$5$1, pairs$2$1 = pairs$6$1, schema$2$1$1 = schema$4$1, set$2$1 = set$5$1, timestamp$2$1 = timestamp$5$1;
const schemas$1 = /* @__PURE__ */ new Map([["core", schema$2$2.schema], ["failsafe", [map$3$1.map, seq$3$1.seq, string$1$1.string]], ["json", schema$1$1$1.schema], ["yaml11", schema$2$1$1.schema], ["yaml-1.1", schema$2$1$1.schema]]), tagsByName$1 = { binary: binary$2$1.binary, bool: bool$2$1.boolTag, float: float$2$1.float, floatExp: float$2$1.floatExp, floatNaN: float$2$1.floatNaN, floatTime: timestamp$2$1.floatTime, int: int$2$1.int, intHex: int$2$1.intHex, intOct: int$2$1.intOct, intTime: timestamp$2$1.intTime, map: map$3$1.map, merge: merge$2$1.merge, null: _null$2$1.nullTag, omap: omap$2$1.omap, pairs: pairs$2$1.pairs, seq: seq$3$1.seq, set: set$2$1.set, timestamp: timestamp$2$1.timestamp }, coreKnownTags$1 = { "tag:yaml.org,2002:binary": binary$2$1.binary, "tag:yaml.org,2002:merge": merge$2$1.merge, "tag:yaml.org,2002:omap": omap$2$1.omap, "tag:yaml.org,2002:pairs": pairs$2$1.pairs, "tag:yaml.org,2002:set": set$2$1.set, "tag:yaml.org,2002:timestamp": timestamp$2$1.timestamp };
function getTags$1(e5, t, n) {
  const r = schemas$1.get(t);
  if (r && !e5) return n && !r.includes(merge$2$1.merge) ? r.concat(merge$2$1.merge) : r.slice();
  let s = r;
  if (!s) if (Array.isArray(e5)) s = [];
  else {
    const o = Array.from(schemas$1.keys()).filter((a) => a !== "yaml11").map((a) => JSON.stringify(a)).join(", ");
    throw new Error(`Unknown schema "${t}"; use one of ${o} or define customTags array`);
  }
  if (Array.isArray(e5)) for (const o of e5) s = s.concat(o);
  else typeof e5 == "function" && (s = e5(s.slice()));
  return n && (s = s.concat(merge$2$1.merge)), s.reduce((o, a) => {
    const l = typeof a == "string" ? tagsByName$1[a] : a;
    if (!l) {
      const c = JSON.stringify(a), u = Object.keys(tagsByName$1).map((p) => JSON.stringify(p)).join(", ");
      throw new Error(`Unknown custom tag ${c}; use one of ${u}`);
    }
    return o.includes(l) || o.push(l), o;
  }, []);
}
tags$1$1.coreKnownTags = coreKnownTags$1;
tags$1$1.getTags = getTags$1;
var identity$b$1 = identity$y$1, map$2$1 = map$8$1, seq$2$1 = seq$8$1, string$6 = string$5$1, tags$2 = tags$1$1;
const sortMapEntriesByKey$1 = (e5, t) => e5.key < t.key ? -1 : e5.key > t.key ? 1 : 0;
let Schema$9$1 = class I {
  constructor({ compat: t, customTags: n, merge: r, resolveKnownTags: s, schema: o, sortMapEntries: a, toStringDefaults: l }) {
    this.compat = Array.isArray(t) ? tags$2.getTags(t, "compat") : t ? tags$2.getTags(null, t) : null, this.name = typeof o == "string" && o || "core", this.knownTags = s ? tags$2.coreKnownTags : {}, this.tags = tags$2.getTags(n, this.name, r), this.toStringOptions = l != null ? l : null, Object.defineProperty(this, identity$b$1.MAP, { value: map$2$1.map }), Object.defineProperty(this, identity$b$1.SCALAR, { value: string$6.string }), Object.defineProperty(this, identity$b$1.SEQ, { value: seq$2$1.seq }), this.sortMapEntries = typeof a == "function" ? a : a === true ? sortMapEntriesByKey$1 : null;
  }
  clone() {
    const t = Object.create(I.prototype, Object.getOwnPropertyDescriptors(this));
    return t.tags = this.tags.slice(), t;
  }
};
Schema$a$1.Schema = Schema$9$1;
var stringifyDocument$2$1 = {}, identity$a$1 = identity$y$1, stringify$6$1 = stringify$b$1, stringifyComment$6 = stringifyComment$5$1;
function stringifyDocument$1$1(e5, t) {
  var _a4;
  const n = [];
  let r = t.directives === true;
  if (t.directives !== false && e5.directives) {
    const c = e5.directives.toString(e5);
    c ? (n.push(c), r = true) : e5.directives.docStart && (r = true);
  }
  r && n.push("---");
  const s = stringify$6$1.createStringifyContext(e5, t), { commentString: o } = s.options;
  if (e5.commentBefore) {
    n.length !== 1 && n.unshift("");
    const c = o(e5.commentBefore);
    n.unshift(stringifyComment$6.indentComment(c, ""));
  }
  let a = false, l = null;
  if (e5.contents) {
    if (identity$a$1.isNode(e5.contents)) {
      if (e5.contents.spaceBefore && r && n.push(""), e5.contents.commentBefore) {
        const p = o(e5.contents.commentBefore);
        n.push(stringifyComment$6.indentComment(p, ""));
      }
      s.forceBlockIndent = !!e5.comment, l = e5.contents.comment;
    }
    const c = l ? void 0 : () => a = true;
    let u = stringify$6$1.stringify(e5.contents, s, () => l = null, c);
    l && (u += stringifyComment$6.lineComment(u, "", o(l))), (u[0] === "|" || u[0] === ">") && n[n.length - 1] === "---" ? n[n.length - 1] = `--- ${u}` : n.push(u);
  } else n.push(stringify$6$1.stringify(e5.contents, s));
  if ((_a4 = e5.directives) == null ? void 0 : _a4.docEnd) if (e5.comment) {
    const c = o(e5.comment);
    c.includes(`
`) ? (n.push("..."), n.push(stringifyComment$6.indentComment(c, ""))) : n.push(`... ${c}`);
  } else n.push("...");
  else {
    let c = e5.comment;
    c && a && (c = c.replace(/^\n+/, "")), c && ((!a || l) && n[n.length - 1] !== "" && n.push(""), n.push(stringifyComment$6.indentComment(o(c), "")));
  }
  return n.join(`
`) + `
`;
}
stringifyDocument$2$1.stringifyDocument = stringifyDocument$1$1;
var Alias$1$1 = Alias$4$1, Collection$5 = Collection$4$1, identity$9$1 = identity$y$1, Pair$3$1 = Pair$8$1, toJS$9 = toJS$8$1, Schema$8$1 = Schema$a$1, stringifyDocument$3 = stringifyDocument$2$1, anchors$4 = anchors$3$1, applyReviver$4 = applyReviver$3$1, createNode$6 = createNode$5$1, directives$1$1 = directives$2$1;
let Document$3$1 = class F {
  constructor(t, n, r) {
    this.commentBefore = null, this.comment = null, this.errors = [], this.warnings = [], Object.defineProperty(this, identity$9$1.NODE_TYPE, { value: identity$9$1.DOC });
    let s = null;
    typeof n == "function" || Array.isArray(n) ? s = n : r === void 0 && n && (r = n, n = void 0);
    const o = Object.assign({ intAsBigInt: false, keepSourceTokens: false, logLevel: "warn", prettyErrors: true, strict: true, stringKeys: false, uniqueKeys: true, version: "1.2" }, r);
    this.options = o;
    let { version: a } = o;
    (r == null ? void 0 : r._directives) ? (this.directives = r._directives.atDocument(), this.directives.yaml.explicit && (a = this.directives.yaml.version)) : this.directives = new directives$1$1.Directives({ version: a }), this.setSchema(a, r), this.contents = t === void 0 ? null : this.createNode(t, s, r);
  }
  clone() {
    const t = Object.create(F.prototype, { [identity$9$1.NODE_TYPE]: { value: identity$9$1.DOC } });
    return t.commentBefore = this.commentBefore, t.comment = this.comment, t.errors = this.errors.slice(), t.warnings = this.warnings.slice(), t.options = Object.assign({}, this.options), this.directives && (t.directives = this.directives.clone()), t.schema = this.schema.clone(), t.contents = identity$9$1.isNode(this.contents) ? this.contents.clone(t.schema) : this.contents, this.range && (t.range = this.range.slice()), t;
  }
  add(t) {
    assertCollection$1(this.contents) && this.contents.add(t);
  }
  addIn(t, n) {
    assertCollection$1(this.contents) && this.contents.addIn(t, n);
  }
  createAlias(t, n) {
    if (!t.anchor) {
      const r = anchors$4.anchorNames(this);
      t.anchor = !n || r.has(n) ? anchors$4.findNewAnchor(n || "a", r) : n;
    }
    return new Alias$1$1.Alias(t.anchor);
  }
  createNode(t, n, r) {
    let s;
    if (typeof n == "function") t = n.call({ "": t }, "", t), s = n;
    else if (Array.isArray(n)) {
      const y = ($) => typeof $ == "number" || $ instanceof String || $ instanceof Number, S = n.filter(y).map(String);
      S.length > 0 && (n = n.concat(S)), s = n;
    } else r === void 0 && n && (r = n, n = void 0);
    const { aliasDuplicateObjects: o, anchorPrefix: a, flow: l, keepUndefined: c, onTagObj: u, tag: p } = r != null ? r : {}, { onAnchor: f, setAnchors: h, sourceObjects: m } = anchors$4.createNodeAnchors(this, a || "a"), g = { aliasDuplicateObjects: o != null ? o : true, keepUndefined: c != null ? c : false, onAnchor: f, onTagObj: u, replacer: s, schema: this.schema, sourceObjects: m }, d = createNode$6.createNode(t, p, g);
    return l && identity$9$1.isCollection(d) && (d.flow = true), h(), d;
  }
  createPair(t, n, r = {}) {
    const s = this.createNode(t, null, r), o = this.createNode(n, null, r);
    return new Pair$3$1.Pair(s, o);
  }
  delete(t) {
    return assertCollection$1(this.contents) ? this.contents.delete(t) : false;
  }
  deleteIn(t) {
    return Collection$5.isEmptyPath(t) ? this.contents == null ? false : (this.contents = null, true) : assertCollection$1(this.contents) ? this.contents.deleteIn(t) : false;
  }
  get(t, n) {
    return identity$9$1.isCollection(this.contents) ? this.contents.get(t, n) : void 0;
  }
  getIn(t, n) {
    return Collection$5.isEmptyPath(t) ? !n && identity$9$1.isScalar(this.contents) ? this.contents.value : this.contents : identity$9$1.isCollection(this.contents) ? this.contents.getIn(t, n) : void 0;
  }
  has(t) {
    return identity$9$1.isCollection(this.contents) ? this.contents.has(t) : false;
  }
  hasIn(t) {
    return Collection$5.isEmptyPath(t) ? this.contents !== void 0 : identity$9$1.isCollection(this.contents) ? this.contents.hasIn(t) : false;
  }
  set(t, n) {
    this.contents == null ? this.contents = Collection$5.collectionFromPath(this.schema, [t], n) : assertCollection$1(this.contents) && this.contents.set(t, n);
  }
  setIn(t, n) {
    Collection$5.isEmptyPath(t) ? this.contents = n : this.contents == null ? this.contents = Collection$5.collectionFromPath(this.schema, Array.from(t), n) : assertCollection$1(this.contents) && this.contents.setIn(t, n);
  }
  setSchema(t, n = {}) {
    typeof t == "number" && (t = String(t));
    let r;
    switch (t) {
      case "1.1":
        this.directives ? this.directives.yaml.version = "1.1" : this.directives = new directives$1$1.Directives({ version: "1.1" }), r = { resolveKnownTags: false, schema: "yaml-1.1" };
        break;
      case "1.2":
      case "next":
        this.directives ? this.directives.yaml.version = t : this.directives = new directives$1$1.Directives({ version: t }), r = { resolveKnownTags: true, schema: "core" };
        break;
      case null:
        this.directives && delete this.directives, r = null;
        break;
      default: {
        const s = JSON.stringify(t);
        throw new Error(`Expected '1.1', '1.2' or null as first argument, but found: ${s}`);
      }
    }
    if (n.schema instanceof Object) this.schema = n.schema;
    else if (r) this.schema = new Schema$8$1.Schema(Object.assign(r, n));
    else throw new Error("With a null YAML version, the { schema: Schema } option is required");
  }
  toJS({ json: t, jsonArg: n, mapAsMap: r, maxAliasCount: s, onAnchor: o, reviver: a } = {}) {
    const l = { anchors: /* @__PURE__ */ new Map(), doc: this, keep: !t, mapAsMap: r === true, mapKeyWarned: false, maxAliasCount: typeof s == "number" ? s : 100 }, c = toJS$9.toJS(this.contents, n != null ? n : "", l);
    if (typeof o == "function") for (const { count: u, res: p } of l.anchors.values()) o(p, u);
    return typeof a == "function" ? applyReviver$4.applyReviver(a, { "": c }, "", c) : c;
  }
  toJSON(t, n) {
    return this.toJS({ json: true, jsonArg: t, mapAsMap: false, onAnchor: n });
  }
  toString(t = {}) {
    if (this.errors.length > 0) throw new Error("Document with errors cannot be stringified");
    if ("indent" in t && (!Number.isInteger(t.indent) || Number(t.indent) <= 0)) {
      const n = JSON.stringify(t.indent);
      throw new Error(`"indent" option must be a positive integer, not ${n}`);
    }
    return stringifyDocument$3.stringifyDocument(this, t);
  }
};
function assertCollection$1(e5) {
  if (identity$9$1.isCollection(e5)) return true;
  throw new Error("Expected a YAML collection as document contents");
}
Document$4$1.Document = Document$3$1;
var errors$4$1 = {};
let YAMLError$1 = class YAMLError extends Error {
  constructor(t, n, r, s) {
    super(), this.name = t, this.code = r, this.message = s, this.pos = n;
  }
};
let YAMLParseError$1 = class YAMLParseError extends YAMLError$1 {
  constructor(t, n, r) {
    super("YAMLParseError", t, n, r);
  }
};
let YAMLWarning$1 = class YAMLWarning extends YAMLError$1 {
  constructor(t, n, r) {
    super("YAMLWarning", t, n, r);
  }
};
const prettifyError$1 = (e5, t) => (n) => {
  if (n.pos[0] === -1) return;
  n.linePos = n.pos.map((l) => t.linePos(l));
  const { line: r, col: s } = n.linePos[0];
  n.message += ` at line ${r}, column ${s}`;
  let o = s - 1, a = e5.substring(t.lineStarts[r - 1], t.lineStarts[r]).replace(/[\n\r]+$/, "");
  if (o >= 60 && a.length > 80) {
    const l = Math.min(o - 39, a.length - 79);
    a = "\u2026" + a.substring(l), o -= l - 1;
  }
  if (a.length > 80 && (a = a.substring(0, 79) + "\u2026"), r > 1 && /^ *$/.test(a.substring(0, o))) {
    let l = e5.substring(t.lineStarts[r - 2], t.lineStarts[r - 1]);
    l.length > 80 && (l = l.substring(0, 79) + `\u2026
`), a = l + a;
  }
  if (/[^ ]/.test(a)) {
    let l = 1;
    const c = n.linePos[1];
    (c == null ? void 0 : c.line) === r && c.col > s && (l = Math.max(1, Math.min(c.col - s, 80 - o)));
    const u = " ".repeat(o) + "^".repeat(l);
    n.message += `:

${a}
${u}
`;
  }
};
errors$4$1.YAMLError = YAMLError$1;
errors$4$1.YAMLParseError = YAMLParseError$1;
errors$4$1.YAMLWarning = YAMLWarning$1;
errors$4$1.prettifyError = prettifyError$1;
var composeDoc$2$1 = {}, composeNode$4$1 = {}, composeCollection$2$1 = {}, resolveBlockMap$2$1 = {}, resolveProps$5$1 = {};
function resolveProps$4$1(e5, { flow: t, indicator: n, next: r, offset: s, onError: o, parentIndent: a, startOnNewline: l }) {
  let c = false, u = l, p = l, f = "", h = "", m = false, g = false, d = null, y = null, S = null, $ = null, E = null, b = null, A = null;
  for (const C of e5) switch (g && (C.type !== "space" && C.type !== "newline" && C.type !== "comma" && o(C.offset, "MISSING_CHAR", "Tags and anchors must be separated from the next token by white space"), g = false), d && (u && C.type !== "comment" && C.type !== "newline" && o(d, "TAB_AS_INDENT", "Tabs are not allowed as indentation"), d = null), C.type) {
    case "space":
      !t && (n !== "doc-start" || (r == null ? void 0 : r.type) !== "flow-collection") && C.source.includes("	") && (d = C), p = true;
      break;
    case "comment": {
      p || o(C, "MISSING_CHAR", "Comments must be separated from other tokens by white space characters");
      const T = C.source.substring(1) || " ";
      f ? f += h + T : f = T, h = "", u = false;
      break;
    }
    case "newline":
      u ? f ? f += C.source : (!b || n !== "seq-item-ind") && (c = true) : h += C.source, u = true, m = true, (y || S) && ($ = C), p = true;
      break;
    case "anchor":
      y && o(C, "MULTIPLE_ANCHORS", "A node can have at most one anchor"), C.source.endsWith(":") && o(C.offset + C.source.length - 1, "BAD_ALIAS", "Anchor ending in : is ambiguous", true), y = C, A != null ? A : A = C.offset, u = false, p = false, g = true;
      break;
    case "tag": {
      S && o(C, "MULTIPLE_TAGS", "A node can have at most one tag"), S = C, A != null ? A : A = C.offset, u = false, p = false, g = true;
      break;
    }
    case n:
      (y || S) && o(C, "BAD_PROP_ORDER", `Anchors and tags must be after the ${C.source} indicator`), b && o(C, "UNEXPECTED_TOKEN", `Unexpected ${C.source} in ${t != null ? t : "collection"}`), b = C, u = n === "seq-item-ind" || n === "explicit-key-ind", p = false;
      break;
    case "comma":
      if (t) {
        E && o(C, "UNEXPECTED_TOKEN", `Unexpected , in ${t}`), E = C, u = false, p = false;
        break;
      }
    default:
      o(C, "UNEXPECTED_TOKEN", `Unexpected ${C.type} token`), u = false, p = false;
  }
  const w = e5[e5.length - 1], v = w ? w.offset + w.source.length : s;
  return g && r && r.type !== "space" && r.type !== "newline" && r.type !== "comma" && (r.type !== "scalar" || r.source !== "") && o(r.offset, "MISSING_CHAR", "Tags and anchors must be separated from the next token by white space"), d && (u && d.indent <= a || (r == null ? void 0 : r.type) === "block-map" || (r == null ? void 0 : r.type) === "block-seq") && o(d, "TAB_AS_INDENT", "Tabs are not allowed as indentation"), { comma: E, found: b, spaceBefore: c, comment: f, hasNewline: m, anchor: y, tag: S, newlineAfterProp: $, end: v, start: A != null ? A : v };
}
resolveProps$5$1.resolveProps = resolveProps$4$1;
var utilContainsNewline$3$1 = {};
function containsNewline$1(e5) {
  if (!e5) return null;
  switch (e5.type) {
    case "alias":
    case "scalar":
    case "double-quoted-scalar":
    case "single-quoted-scalar":
      if (e5.source.includes(`
`)) return true;
      if (e5.end) {
        for (const t of e5.end) if (t.type === "newline") return true;
      }
      return false;
    case "flow-collection":
      for (const t of e5.items) {
        for (const n of t.start) if (n.type === "newline") return true;
        if (t.sep) {
          for (const n of t.sep) if (n.type === "newline") return true;
        }
        if (containsNewline$1(t.key) || containsNewline$1(t.value)) return true;
      }
      return false;
    default:
      return true;
  }
}
utilContainsNewline$3$1.containsNewline = containsNewline$1;
var utilFlowIndentCheck$2$1 = {}, utilContainsNewline$2$1 = utilContainsNewline$3$1;
function flowIndentCheck$1(e5, t, n) {
  if ((t == null ? void 0 : t.type) === "flow-collection") {
    const r = t.end[0];
    r.indent === e5 && (r.source === "]" || r.source === "}") && utilContainsNewline$2$1.containsNewline(t) && n(r, "BAD_INDENT", "Flow end indicator should be more indented than parent", true);
  }
}
utilFlowIndentCheck$2$1.flowIndentCheck = flowIndentCheck$1;
var utilMapIncludes$2$1 = {}, identity$8$1 = identity$y$1;
function mapIncludes$1(e5, t, n) {
  const { uniqueKeys: r } = e5.options;
  if (r === false) return false;
  const s = typeof r == "function" ? r : (o, a) => o === a || identity$8$1.isScalar(o) && identity$8$1.isScalar(a) && o.value === a.value;
  return t.some((o) => s(o.key, n));
}
utilMapIncludes$2$1.mapIncludes = mapIncludes$1;
var Pair$2$1 = Pair$8$1, YAMLMap$3$1 = YAMLMap$8$1, resolveProps$3$1 = resolveProps$5$1, utilContainsNewline$1$1 = utilContainsNewline$3$1, utilFlowIndentCheck$1$1 = utilFlowIndentCheck$2$1, utilMapIncludes$1$1 = utilMapIncludes$2$1;
const startColMsg$1 = "All mapping items must start at the same column";
function resolveBlockMap$1$1({ composeNode: e5, composeEmptyNode: t }, n, r, s, o) {
  var _a4, _b;
  const a = (_a4 = o == null ? void 0 : o.nodeClass) != null ? _a4 : YAMLMap$3$1.YAMLMap, l = new a(n.schema);
  n.atRoot && (n.atRoot = false);
  let c = r.offset, u = null;
  for (const p of r.items) {
    const { start: f, key: h, sep: m, value: g } = p, d = resolveProps$3$1.resolveProps(f, { indicator: "explicit-key-ind", next: h != null ? h : m == null ? void 0 : m[0], offset: c, onError: s, parentIndent: r.indent, startOnNewline: true }), y = !d.found;
    if (y) {
      if (h && (h.type === "block-seq" ? s(c, "BLOCK_AS_IMPLICIT_KEY", "A block sequence may not be used as an implicit map key") : "indent" in h && h.indent !== r.indent && s(c, "BAD_INDENT", startColMsg$1)), !d.anchor && !d.tag && !m) {
        u = d.end, d.comment && (l.comment ? l.comment += `
` + d.comment : l.comment = d.comment);
        continue;
      }
      (d.newlineAfterProp || utilContainsNewline$1$1.containsNewline(h)) && s(h != null ? h : f[f.length - 1], "MULTILINE_IMPLICIT_KEY", "Implicit keys need to be on a single line");
    } else ((_b = d.found) == null ? void 0 : _b.indent) !== r.indent && s(c, "BAD_INDENT", startColMsg$1);
    n.atKey = true;
    const S = d.end, $ = h ? e5(n, h, d, s) : t(n, S, f, null, d, s);
    n.schema.compat && utilFlowIndentCheck$1$1.flowIndentCheck(r.indent, h, s), n.atKey = false, utilMapIncludes$1$1.mapIncludes(n, l.items, $) && s(S, "DUPLICATE_KEY", "Map keys must be unique");
    const E = resolveProps$3$1.resolveProps(m != null ? m : [], { indicator: "map-value-ind", next: g, offset: $.range[2], onError: s, parentIndent: r.indent, startOnNewline: !h || h.type === "block-scalar" });
    if (c = E.end, E.found) {
      y && ((g == null ? void 0 : g.type) === "block-map" && !E.hasNewline && s(c, "BLOCK_AS_IMPLICIT_KEY", "Nested mappings are not allowed in compact mappings"), n.options.strict && d.start < E.found.offset - 1024 && s($.range, "KEY_OVER_1024_CHARS", "The : indicator must be at most 1024 chars after the start of an implicit block mapping key"));
      const b = g ? e5(n, g, E, s) : t(n, c, m, null, E, s);
      n.schema.compat && utilFlowIndentCheck$1$1.flowIndentCheck(r.indent, g, s), c = b.range[2];
      const A = new Pair$2$1.Pair($, b);
      n.options.keepSourceTokens && (A.srcToken = p), l.items.push(A);
    } else {
      y && s($.range, "MISSING_CHAR", "Implicit map keys need to be followed by map values"), E.comment && ($.comment ? $.comment += `
` + E.comment : $.comment = E.comment);
      const b = new Pair$2$1.Pair($);
      n.options.keepSourceTokens && (b.srcToken = p), l.items.push(b);
    }
  }
  return u && u < c && s(u, "IMPOSSIBLE", "Map comment with trailing content"), l.range = [r.offset, c, u != null ? u : c], l;
}
resolveBlockMap$2$1.resolveBlockMap = resolveBlockMap$1$1;
var resolveBlockSeq$2$1 = {}, YAMLSeq$2$1 = YAMLSeq$7$1, resolveProps$2$1 = resolveProps$5$1, utilFlowIndentCheck$3 = utilFlowIndentCheck$2$1;
function resolveBlockSeq$1$1({ composeNode: e5, composeEmptyNode: t }, n, r, s, o) {
  var _a4;
  const a = (_a4 = o == null ? void 0 : o.nodeClass) != null ? _a4 : YAMLSeq$2$1.YAMLSeq, l = new a(n.schema);
  n.atRoot && (n.atRoot = false), n.atKey && (n.atKey = false);
  let c = r.offset, u = null;
  for (const { start: p, value: f } of r.items) {
    const h = resolveProps$2$1.resolveProps(p, { indicator: "seq-item-ind", next: f, offset: c, onError: s, parentIndent: r.indent, startOnNewline: true });
    if (!h.found) if (h.anchor || h.tag || f) (f == null ? void 0 : f.type) === "block-seq" ? s(h.end, "BAD_INDENT", "All sequence items must start at the same column") : s(c, "MISSING_CHAR", "Sequence item without - indicator");
    else {
      u = h.end, h.comment && (l.comment = h.comment);
      continue;
    }
    const m = f ? e5(n, f, h, s) : t(n, h.end, p, null, h, s);
    n.schema.compat && utilFlowIndentCheck$3.flowIndentCheck(r.indent, f, s), c = m.range[2], l.items.push(m);
  }
  return l.range = [r.offset, c, u != null ? u : c], l;
}
resolveBlockSeq$2$1.resolveBlockSeq = resolveBlockSeq$1$1;
var resolveFlowCollection$2$1 = {}, resolveEnd$6$1 = {};
function resolveEnd$5$1(e5, t, n, r) {
  let s = "";
  if (e5) {
    let o = false, a = "";
    for (const l of e5) {
      const { source: c, type: u } = l;
      switch (u) {
        case "space":
          o = true;
          break;
        case "comment": {
          n && !o && r(l, "MISSING_CHAR", "Comments must be separated from other tokens by white space characters");
          const p = c.substring(1) || " ";
          s ? s += a + p : s = p, a = "";
          break;
        }
        case "newline":
          s && (a += c), o = true;
          break;
        default:
          r(l, "UNEXPECTED_TOKEN", `Unexpected ${u} at node end`);
      }
      t += c.length;
    }
  }
  return { comment: s, offset: t };
}
resolveEnd$6$1.resolveEnd = resolveEnd$5$1;
var identity$7$1 = identity$y$1, Pair$1$1 = Pair$8$1, YAMLMap$2$1 = YAMLMap$8$1, YAMLSeq$1$1 = YAMLSeq$7$1, resolveEnd$4$1 = resolveEnd$6$1, resolveProps$1$1 = resolveProps$5$1, utilContainsNewline$4 = utilContainsNewline$3$1, utilMapIncludes$3 = utilMapIncludes$2$1;
const blockMsg$1 = "Block collections are not allowed within flow collections", isBlock$1 = (e5) => e5 && (e5.type === "block-map" || e5.type === "block-seq");
function resolveFlowCollection$1$1({ composeNode: e5, composeEmptyNode: t }, n, r, s, o) {
  var _a4, _b, _c2;
  const a = r.start.source === "{", l = a ? "flow map" : "flow sequence", c = (_a4 = o == null ? void 0 : o.nodeClass) != null ? _a4 : a ? YAMLMap$2$1.YAMLMap : YAMLSeq$1$1.YAMLSeq, u = new c(n.schema);
  u.flow = true;
  const p = n.atRoot;
  p && (n.atRoot = false), n.atKey && (n.atKey = false);
  let f = r.offset + r.start.source.length;
  for (let y = 0; y < r.items.length; ++y) {
    const S = r.items[y], { start: $, key: E, sep: b, value: A } = S, w = resolveProps$1$1.resolveProps($, { flow: l, indicator: "explicit-key-ind", next: E != null ? E : b == null ? void 0 : b[0], offset: f, onError: s, parentIndent: r.indent, startOnNewline: false });
    if (!w.found) {
      if (!w.anchor && !w.tag && !b && !A) {
        y === 0 && w.comma ? s(w.comma, "UNEXPECTED_TOKEN", `Unexpected , in ${l}`) : y < r.items.length - 1 && s(w.start, "UNEXPECTED_TOKEN", `Unexpected empty item in ${l}`), w.comment && (u.comment ? u.comment += `
` + w.comment : u.comment = w.comment), f = w.end;
        continue;
      }
      !a && n.options.strict && utilContainsNewline$4.containsNewline(E) && s(E, "MULTILINE_IMPLICIT_KEY", "Implicit keys of flow sequence pairs need to be on a single line");
    }
    if (y === 0) w.comma && s(w.comma, "UNEXPECTED_TOKEN", `Unexpected , in ${l}`);
    else if (w.comma || s(w.start, "MISSING_CHAR", `Missing , between ${l} items`), w.comment) {
      let v = "";
      e: for (const C of $) switch (C.type) {
        case "comma":
        case "space":
          break;
        case "comment":
          v = C.source.substring(1);
          break e;
        default:
          break e;
      }
      if (v) {
        let C = u.items[u.items.length - 1];
        identity$7$1.isPair(C) && (C = (_b = C.value) != null ? _b : C.key), C.comment ? C.comment += `
` + v : C.comment = v, w.comment = w.comment.substring(v.length + 1);
      }
    }
    if (!a && !b && !w.found) {
      const v = A ? e5(n, A, w, s) : t(n, w.end, b, null, w, s);
      u.items.push(v), f = v.range[2], isBlock$1(A) && s(v.range, "BLOCK_IN_FLOW", blockMsg$1);
    } else {
      n.atKey = true;
      const v = w.end, C = E ? e5(n, E, w, s) : t(n, v, $, null, w, s);
      isBlock$1(E) && s(C.range, "BLOCK_IN_FLOW", blockMsg$1), n.atKey = false;
      const T = resolveProps$1$1.resolveProps(b != null ? b : [], { flow: l, indicator: "map-value-ind", next: A, offset: C.range[2], onError: s, parentIndent: r.indent, startOnNewline: false });
      if (T.found) {
        if (!a && !w.found && n.options.strict) {
          if (b) for (const _ of b) {
            if (_ === T.found) break;
            if (_.type === "newline") {
              s(_, "MULTILINE_IMPLICIT_KEY", "Implicit keys of flow sequence pairs need to be on a single line");
              break;
            }
          }
          w.start < T.found.offset - 1024 && s(T.found, "KEY_OVER_1024_CHARS", "The : indicator must be at most 1024 chars after the start of an implicit flow sequence key");
        }
      } else A && ("source" in A && ((_c2 = A.source) == null ? void 0 : _c2[0]) === ":" ? s(A, "MISSING_CHAR", `Missing space after : in ${l}`) : s(T.start, "MISSING_CHAR", `Missing , or : between ${l} items`));
      const O = A ? e5(n, A, T, s) : T.found ? t(n, T.end, b, null, T, s) : null;
      O ? isBlock$1(A) && s(O.range, "BLOCK_IN_FLOW", blockMsg$1) : T.comment && (C.comment ? C.comment += `
` + T.comment : C.comment = T.comment);
      const L = new Pair$1$1.Pair(C, O);
      if (n.options.keepSourceTokens && (L.srcToken = S), a) {
        const _ = u;
        utilMapIncludes$3.mapIncludes(n, _.items, C) && s(v, "DUPLICATE_KEY", "Map keys must be unique"), _.items.push(L);
      } else {
        const _ = new YAMLMap$2$1.YAMLMap(n.schema);
        _.flow = true, _.items.push(L);
        const N = (O != null ? O : C).range;
        _.range = [C.range[0], N[1], N[2]], u.items.push(_);
      }
      f = O ? O.range[2] : T.end;
    }
  }
  const h = a ? "}" : "]", [m, ...g] = r.end;
  let d = f;
  if ((m == null ? void 0 : m.source) === h) d = m.offset + m.source.length;
  else {
    const y = l[0].toUpperCase() + l.substring(1), S = p ? `${y} must end with a ${h}` : `${y} in block collection must be sufficiently indented and end with a ${h}`;
    s(f, p ? "MISSING_CHAR" : "BAD_INDENT", S), m && m.source.length !== 1 && g.unshift(m);
  }
  if (g.length > 0) {
    const y = resolveEnd$4$1.resolveEnd(g, d, n.options.strict, s);
    y.comment && (u.comment ? u.comment += `
` + y.comment : u.comment = y.comment), u.range = [r.offset, d, y.offset];
  } else u.range = [r.offset, d, d];
  return u;
}
resolveFlowCollection$2$1.resolveFlowCollection = resolveFlowCollection$1$1;
var identity$6$1 = identity$y$1, Scalar$4$1 = Scalar$k$1, YAMLMap$1$1 = YAMLMap$8$1, YAMLSeq$8 = YAMLSeq$7$1, resolveBlockMap$3 = resolveBlockMap$2$1, resolveBlockSeq$3 = resolveBlockSeq$2$1, resolveFlowCollection$3 = resolveFlowCollection$2$1;
function resolveCollection$1(e5, t, n, r, s, o) {
  const a = n.type === "block-map" ? resolveBlockMap$3.resolveBlockMap(e5, t, n, r, o) : n.type === "block-seq" ? resolveBlockSeq$3.resolveBlockSeq(e5, t, n, r, o) : resolveFlowCollection$3.resolveFlowCollection(e5, t, n, r, o), l = a.constructor;
  return s === "!" || s === l.tagName ? (a.tag = l.tagName, a) : (s && (a.tag = s), a);
}
function composeCollection$1$1(e5, t, n, r, s) {
  var _a4, _b, _c2;
  const o = r.tag, a = o ? t.directives.tagName(o.source, (h) => s(o, "TAG_RESOLVE_FAILED", h)) : null;
  if (n.type === "block-seq") {
    const { anchor: h, newlineAfterProp: m } = r, g = h && o ? h.offset > o.offset ? h : o : h != null ? h : o;
    g && (!m || m.offset < g.offset) && s(g, "MISSING_CHAR", "Missing newline after block sequence props");
  }
  const l = n.type === "block-map" ? "map" : n.type === "block-seq" ? "seq" : n.start.source === "{" ? "map" : "seq";
  if (!o || !a || a === "!" || a === YAMLMap$1$1.YAMLMap.tagName && l === "map" || a === YAMLSeq$8.YAMLSeq.tagName && l === "seq") return resolveCollection$1(e5, t, n, s, a);
  let c = t.schema.tags.find((h) => h.tag === a && h.collection === l);
  if (!c) {
    const h = t.schema.knownTags[a];
    if ((h == null ? void 0 : h.collection) === l) t.schema.tags.push(Object.assign({}, h, { default: false })), c = h;
    else return h ? s(o, "BAD_COLLECTION_TYPE", `${h.tag} used for ${l} collection, but expects ${(_a4 = h.collection) != null ? _a4 : "scalar"}`, true) : s(o, "TAG_RESOLVE_FAILED", `Unresolved tag: ${a}`, true), resolveCollection$1(e5, t, n, s, a);
  }
  const u = resolveCollection$1(e5, t, n, s, a, c), p = (_c2 = (_b = c.resolve) == null ? void 0 : _b.call(c, u, (h) => s(o, "TAG_RESOLVE_FAILED", h), t.options)) != null ? _c2 : u, f = identity$6$1.isNode(p) ? p : new Scalar$4$1.Scalar(p);
  return f.range = u.range, f.tag = a, (c == null ? void 0 : c.format) && (f.format = c.format), f;
}
composeCollection$2$1.composeCollection = composeCollection$1$1;
var composeScalar$2$1 = {}, resolveBlockScalar$3$1 = {}, Scalar$3$1 = Scalar$k$1;
function resolveBlockScalar$2$1(e5, t, n) {
  const r = t.offset, s = parseBlockScalarHeader$1(t, e5.options.strict, n);
  if (!s) return { value: "", type: null, comment: "", range: [r, r, r] };
  const o = s.mode === ">" ? Scalar$3$1.Scalar.BLOCK_FOLDED : Scalar$3$1.Scalar.BLOCK_LITERAL, a = t.source ? splitLines$1(t.source) : [];
  let l = a.length;
  for (let d = a.length - 1; d >= 0; --d) {
    const y = a[d][1];
    if (y === "" || y === "\r") l = d;
    else break;
  }
  if (l === 0) {
    const d = s.chomp === "+" && a.length > 0 ? `
`.repeat(Math.max(1, a.length - 1)) : "";
    let y = r + s.length;
    return t.source && (y += t.source.length), { value: d, type: o, comment: s.comment, range: [r, y, y] };
  }
  let c = t.indent + s.indent, u = t.offset + s.length, p = 0;
  for (let d = 0; d < l; ++d) {
    const [y, S] = a[d];
    if (S === "" || S === "\r") s.indent === 0 && y.length > c && (c = y.length);
    else {
      y.length < c && n(u + y.length, "MISSING_CHAR", "Block scalars with more-indented leading empty lines must use an explicit indentation indicator"), s.indent === 0 && (c = y.length), p = d, c === 0 && !e5.atRoot && n(u, "BAD_INDENT", "Block scalar values in collections must be indented");
      break;
    }
    u += y.length + S.length + 1;
  }
  for (let d = a.length - 1; d >= l; --d) a[d][0].length > c && (l = d + 1);
  let f = "", h = "", m = false;
  for (let d = 0; d < p; ++d) f += a[d][0].slice(c) + `
`;
  for (let d = p; d < l; ++d) {
    let [y, S] = a[d];
    u += y.length + S.length + 1;
    const $ = S[S.length - 1] === "\r";
    if ($ && (S = S.slice(0, -1)), S && y.length < c) {
      const b = `Block scalar lines must not be less indented than their ${s.indent ? "explicit indentation indicator" : "first line"}`;
      n(u - S.length - ($ ? 2 : 1), "BAD_INDENT", b), y = "";
    }
    o === Scalar$3$1.Scalar.BLOCK_LITERAL ? (f += h + y.slice(c) + S, h = `
`) : y.length > c || S[0] === "	" ? (h === " " ? h = `
` : !m && h === `
` && (h = `

`), f += h + y.slice(c) + S, h = `
`, m = true) : S === "" ? h === `
` ? f += `
` : h = `
` : (f += h + S, h = " ", m = false);
  }
  switch (s.chomp) {
    case "-":
      break;
    case "+":
      for (let d = l; d < a.length; ++d) f += `
` + a[d][0].slice(c);
      f[f.length - 1] !== `
` && (f += `
`);
      break;
    default:
      f += `
`;
  }
  const g = r + s.length + t.source.length;
  return { value: f, type: o, comment: s.comment, range: [r, g, g] };
}
function parseBlockScalarHeader$1({ offset: e5, props: t }, n, r) {
  if (t[0].type !== "block-scalar-header") return r(t[0], "IMPOSSIBLE", "Block scalar header not found"), null;
  const { source: s } = t[0], o = s[0];
  let a = 0, l = "", c = -1;
  for (let h = 1; h < s.length; ++h) {
    const m = s[h];
    if (!l && (m === "-" || m === "+")) l = m;
    else {
      const g = Number(m);
      !a && g ? a = g : c === -1 && (c = e5 + h);
    }
  }
  c !== -1 && r(c, "UNEXPECTED_TOKEN", `Block scalar header includes extra characters: ${s}`);
  let u = false, p = "", f = s.length;
  for (let h = 1; h < t.length; ++h) {
    const m = t[h];
    switch (m.type) {
      case "space":
        u = true;
      case "newline":
        f += m.source.length;
        break;
      case "comment":
        n && !u && r(m, "MISSING_CHAR", "Comments must be separated from other tokens by white space characters"), f += m.source.length, p = m.source.substring(1);
        break;
      case "error":
        r(m, "UNEXPECTED_TOKEN", m.message), f += m.source.length;
        break;
      default: {
        const g = `Unexpected token in block scalar header: ${m.type}`;
        r(m, "UNEXPECTED_TOKEN", g);
        const d = m.source;
        d && typeof d == "string" && (f += d.length);
      }
    }
  }
  return { mode: o, indent: a, chomp: l, comment: p, length: f };
}
function splitLines$1(e5) {
  const t = e5.split(/\n( *)/), n = t[0], r = n.match(/^( *)/), o = [(r == null ? void 0 : r[1]) ? [r[1], n.slice(r[1].length)] : ["", n]];
  for (let a = 1; a < t.length; a += 2) o.push([t[a], t[a + 1]]);
  return o;
}
resolveBlockScalar$3$1.resolveBlockScalar = resolveBlockScalar$2$1;
var resolveFlowScalar$3$1 = {}, Scalar$2$1 = Scalar$k$1, resolveEnd$3$1 = resolveEnd$6$1;
function resolveFlowScalar$2$1(e5, t, n) {
  const { offset: r, type: s, source: o, end: a } = e5;
  let l, c;
  const u = (h, m, g) => n(r + h, m, g);
  switch (s) {
    case "scalar":
      l = Scalar$2$1.Scalar.PLAIN, c = plainValue$1(o, u);
      break;
    case "single-quoted-scalar":
      l = Scalar$2$1.Scalar.QUOTE_SINGLE, c = singleQuotedValue$1(o, u);
      break;
    case "double-quoted-scalar":
      l = Scalar$2$1.Scalar.QUOTE_DOUBLE, c = doubleQuotedValue$1(o, u);
      break;
    default:
      return n(e5, "UNEXPECTED_TOKEN", `Expected a flow scalar value, but found: ${s}`), { value: "", type: null, comment: "", range: [r, r + o.length, r + o.length] };
  }
  const p = r + o.length, f = resolveEnd$3$1.resolveEnd(a, p, t, n);
  return { value: c, type: l, comment: f.comment, range: [r, p, f.offset] };
}
function plainValue$1(e5, t) {
  let n = "";
  switch (e5[0]) {
    case "	":
      n = "a tab character";
      break;
    case ",":
      n = "flow indicator character ,";
      break;
    case "%":
      n = "directive indicator character %";
      break;
    case "|":
    case ">": {
      n = `block scalar indicator ${e5[0]}`;
      break;
    }
    case "@":
    case "`": {
      n = `reserved character ${e5[0]}`;
      break;
    }
  }
  return n && t(0, "BAD_SCALAR_START", `Plain value cannot start with ${n}`), foldLines$1(e5);
}
function singleQuotedValue$1(e5, t) {
  return (e5[e5.length - 1] !== "'" || e5.length === 1) && t(e5.length, "MISSING_CHAR", "Missing closing 'quote"), foldLines$1(e5.slice(1, -1)).replace(/''/g, "'");
}
function foldLines$1(e5) {
  var _a4;
  let t, n;
  try {
    t = new RegExp(`(.*?)(?<![ 	])[ 	]*\r?
`, "sy"), n = new RegExp(`[ 	]*(.*?)(?:(?<![ 	])[ 	]*)?\r?
`, "sy");
  } catch {
    t = /(.*?)[ \t]*\r?\n/sy, n = /[ \t]*(.*?)[ \t]*\r?\n/sy;
  }
  let r = t.exec(e5);
  if (!r) return e5;
  let s = r[1], o = " ", a = t.lastIndex;
  for (n.lastIndex = a; r = n.exec(e5); ) r[1] === "" ? o === `
` ? s += o : o = `
` : (s += o + r[1], o = " "), a = n.lastIndex;
  const l = /[ \t]*(.*)/sy;
  return l.lastIndex = a, r = l.exec(e5), s + o + ((_a4 = r == null ? void 0 : r[1]) != null ? _a4 : "");
}
function doubleQuotedValue$1(e5, t) {
  let n = "";
  for (let r = 1; r < e5.length - 1; ++r) {
    const s = e5[r];
    if (!(s === "\r" && e5[r + 1] === `
`)) if (s === `
`) {
      const { fold: o, offset: a } = foldNewline$1(e5, r);
      n += o, r = a;
    } else if (s === "\\") {
      let o = e5[++r];
      const a = escapeCodes$1[o];
      if (a) n += a;
      else if (o === `
`) for (o = e5[r + 1]; o === " " || o === "	"; ) o = e5[++r + 1];
      else if (o === "\r" && e5[r + 1] === `
`) for (o = e5[++r + 1]; o === " " || o === "	"; ) o = e5[++r + 1];
      else if (o === "x" || o === "u" || o === "U") {
        const l = o === "x" ? 2 : o === "u" ? 4 : 8;
        n += parseCharCode$1(e5, r + 1, l, t), r += l;
      } else {
        const l = e5.substr(r - 1, 2);
        t(r - 1, "BAD_DQ_ESCAPE", `Invalid escape sequence ${l}`), n += l;
      }
    } else if (s === " " || s === "	") {
      const o = r;
      let a = e5[r + 1];
      for (; a === " " || a === "	"; ) a = e5[++r + 1];
      a !== `
` && !(a === "\r" && e5[r + 2] === `
`) && (n += r > o ? e5.slice(o, r + 1) : s);
    } else n += s;
  }
  return (e5[e5.length - 1] !== '"' || e5.length === 1) && t(e5.length, "MISSING_CHAR", 'Missing closing "quote'), n;
}
function foldNewline$1(e5, t) {
  let n = "", r = e5[t + 1];
  for (; (r === " " || r === "	" || r === `
` || r === "\r") && !(r === "\r" && e5[t + 2] !== `
`); ) r === `
` && (n += `
`), t += 1, r = e5[t + 1];
  return n || (n = " "), { fold: n, offset: t };
}
const escapeCodes$1 = { 0: "\0", a: "\x07", b: "\b", e: "\x1B", f: "\f", n: `
`, r: "\r", t: "	", v: "\v", N: "\x85", _: "\xA0", L: "\u2028", P: "\u2029", " ": " ", '"': '"', "/": "/", "\\": "\\", "	": "	" };
function parseCharCode$1(e5, t, n, r) {
  const s = e5.substr(t, n), a = s.length === n && /^[0-9a-fA-F]+$/.test(s) ? parseInt(s, 16) : NaN;
  try {
    return String.fromCodePoint(a);
  } catch {
    const l = e5.substr(t - 2, n + 2);
    return r(t - 2, "BAD_DQ_ESCAPE", `Invalid escape sequence ${l}`), l;
  }
}
resolveFlowScalar$3$1.resolveFlowScalar = resolveFlowScalar$2$1;
var identity$5$1 = identity$y$1, Scalar$1$1 = Scalar$k$1, resolveBlockScalar$1$1 = resolveBlockScalar$3$1, resolveFlowScalar$1$1 = resolveFlowScalar$3$1;
function composeScalar$1$1(e5, t, n, r) {
  const { value: s, type: o, comment: a, range: l } = t.type === "block-scalar" ? resolveBlockScalar$1$1.resolveBlockScalar(e5, t, r) : resolveFlowScalar$1$1.resolveFlowScalar(t, e5.options.strict, r), c = n ? e5.directives.tagName(n.source, (f) => r(n, "TAG_RESOLVE_FAILED", f)) : null;
  let u;
  e5.options.stringKeys && e5.atKey ? u = e5.schema[identity$5$1.SCALAR] : c ? u = findScalarTagByName$1(e5.schema, s, c, n, r) : t.type === "scalar" ? u = findScalarTagByTest$1(e5, s, t, r) : u = e5.schema[identity$5$1.SCALAR];
  let p;
  try {
    const f = u.resolve(s, (h) => r(n != null ? n : t, "TAG_RESOLVE_FAILED", h), e5.options);
    p = identity$5$1.isScalar(f) ? f : new Scalar$1$1.Scalar(f);
  } catch (f) {
    const h = f instanceof Error ? f.message : String(f);
    r(n != null ? n : t, "TAG_RESOLVE_FAILED", h), p = new Scalar$1$1.Scalar(s);
  }
  return p.range = l, p.source = s, o && (p.type = o), c && (p.tag = c), u.format && (p.format = u.format), a && (p.comment = a), p;
}
function findScalarTagByName$1(e5, t, n, r, s) {
  var _a4;
  if (n === "!") return e5[identity$5$1.SCALAR];
  const o = [];
  for (const l of e5.tags) if (!l.collection && l.tag === n) if (l.default && l.test) o.push(l);
  else return l;
  for (const l of o) if ((_a4 = l.test) == null ? void 0 : _a4.test(t)) return l;
  const a = e5.knownTags[n];
  return a && !a.collection ? (e5.tags.push(Object.assign({}, a, { default: false, test: void 0 })), a) : (s(r, "TAG_RESOLVE_FAILED", `Unresolved tag: ${n}`, n !== "tag:yaml.org,2002:str"), e5[identity$5$1.SCALAR]);
}
function findScalarTagByTest$1({ atKey: e5, directives: t, schema: n }, r, s, o) {
  var _a4;
  const a = n.tags.find((l) => {
    var _a5;
    return (l.default === true || e5 && l.default === "key") && ((_a5 = l.test) == null ? void 0 : _a5.test(r));
  }) || n[identity$5$1.SCALAR];
  if (n.compat) {
    const l = (_a4 = n.compat.find((c) => {
      var _a5;
      return c.default && ((_a5 = c.test) == null ? void 0 : _a5.test(r));
    })) != null ? _a4 : n[identity$5$1.SCALAR];
    if (a.tag !== l.tag) {
      const c = t.tagString(a.tag), u = t.tagString(l.tag), p = `Value may be parsed as either ${c} or ${u}`;
      o(s, "TAG_RESOLVE_FAILED", p, true);
    }
  }
  return a;
}
composeScalar$2$1.composeScalar = composeScalar$1$1;
var utilEmptyScalarPosition$1$1 = {};
function emptyScalarPosition$1(e5, t, n) {
  if (t) {
    n != null ? n : n = t.length;
    for (let r = n - 1; r >= 0; --r) {
      let s = t[r];
      switch (s.type) {
        case "space":
        case "comment":
        case "newline":
          e5 -= s.source.length;
          continue;
      }
      for (s = t[++r]; (s == null ? void 0 : s.type) === "space"; ) e5 += s.source.length, s = t[++r];
      break;
    }
  }
  return e5;
}
utilEmptyScalarPosition$1$1.emptyScalarPosition = emptyScalarPosition$1;
var Alias$5 = Alias$4$1, identity$4$1 = identity$y$1, composeCollection$3 = composeCollection$2$1, composeScalar$3 = composeScalar$2$1, resolveEnd$2$1 = resolveEnd$6$1, utilEmptyScalarPosition$2 = utilEmptyScalarPosition$1$1;
const CN$1 = { composeNode: composeNode$3$1, composeEmptyNode: composeEmptyNode$1 };
function composeNode$3$1(e5, t, n, r) {
  const s = e5.atKey, { spaceBefore: o, comment: a, anchor: l, tag: c } = n;
  let u, p = true;
  switch (t.type) {
    case "alias":
      u = composeAlias$1(e5, t, r), (l || c) && r(t, "ALIAS_PROPS", "An alias node must not specify any properties");
      break;
    case "scalar":
    case "single-quoted-scalar":
    case "double-quoted-scalar":
    case "block-scalar":
      u = composeScalar$3.composeScalar(e5, t, c, r), l && (u.anchor = l.source.substring(1));
      break;
    case "block-map":
    case "block-seq":
    case "flow-collection":
      try {
        u = composeCollection$3.composeCollection(CN$1, e5, t, n, r), l && (u.anchor = l.source.substring(1));
      } catch (f) {
        const h = f instanceof Error ? f.message : String(f);
        r(t, "RESOURCE_EXHAUSTION", h);
      }
      break;
    default: {
      const f = t.type === "error" ? t.message : `Unsupported token (type: ${t.type})`;
      r(t, "UNEXPECTED_TOKEN", f), p = false;
    }
  }
  return u != null ? u : u = composeEmptyNode$1(e5, t.offset, void 0, null, n, r), l && u.anchor === "" && r(l, "BAD_ALIAS", "Anchor cannot be an empty string"), s && e5.options.stringKeys && (!identity$4$1.isScalar(u) || typeof u.value != "string" || u.tag && u.tag !== "tag:yaml.org,2002:str") && r(c != null ? c : t, "NON_STRING_KEY", "With stringKeys, all keys must be strings"), o && (u.spaceBefore = true), a && (t.type === "scalar" && t.source === "" ? u.comment = a : u.commentBefore = a), e5.options.keepSourceTokens && p && (u.srcToken = t), u;
}
function composeEmptyNode$1(e5, t, n, r, { spaceBefore: s, comment: o, anchor: a, tag: l, end: c }, u) {
  const p = { type: "scalar", offset: utilEmptyScalarPosition$2.emptyScalarPosition(t, n, r), indent: -1, source: "" }, f = composeScalar$3.composeScalar(e5, p, l, u);
  return a && (f.anchor = a.source.substring(1), f.anchor === "" && u(a, "BAD_ALIAS", "Anchor cannot be an empty string")), s && (f.spaceBefore = true), o && (f.comment = o, f.range[2] = c), f;
}
function composeAlias$1({ options: e5 }, { offset: t, source: n, end: r }, s) {
  const o = new Alias$5.Alias(n.substring(1));
  o.source === "" && s(t, "BAD_ALIAS", "Alias cannot be an empty string"), o.source.endsWith(":") && s(t + n.length - 1, "BAD_ALIAS", "Alias ending in : is ambiguous", true);
  const a = t + n.length, l = resolveEnd$2$1.resolveEnd(r, a, e5.strict, s);
  return o.range = [t, a, l.offset], l.comment && (o.comment = l.comment), o;
}
composeNode$4$1.composeEmptyNode = composeEmptyNode$1;
composeNode$4$1.composeNode = composeNode$3$1;
var Document$2$1 = Document$4$1, composeNode$2$1 = composeNode$4$1, resolveEnd$1$1 = resolveEnd$6$1, resolveProps$6 = resolveProps$5$1;
function composeDoc$1$1(e5, t, { offset: n, start: r, value: s, end: o }, a) {
  const l = Object.assign({ _directives: t }, e5), c = new Document$2$1.Document(void 0, l), u = { atKey: false, atRoot: true, directives: c.directives, options: c.options, schema: c.schema }, p = resolveProps$6.resolveProps(r, { indicator: "doc-start", next: s != null ? s : o == null ? void 0 : o[0], offset: n, onError: a, parentIndent: 0, startOnNewline: true });
  p.found && (c.directives.docStart = true, s && (s.type === "block-map" || s.type === "block-seq") && !p.hasNewline && a(p.end, "MISSING_CHAR", "Block collection cannot start on same line with directives-end marker")), c.contents = s ? composeNode$2$1.composeNode(u, s, p, a) : composeNode$2$1.composeEmptyNode(u, p.end, r, null, p, a);
  const f = c.contents.range[2], h = resolveEnd$1$1.resolveEnd(o, f, false, a);
  return h.comment && (c.comment = h.comment), c.range = [n, f, h.offset], c;
}
composeDoc$2$1.composeDoc = composeDoc$1$1;
var node_process$1$1 = require$$0, directives$3 = directives$2$1, Document$1$1 = Document$4$1, errors$3$1 = errors$4$1, identity$3$1 = identity$y$1, composeDoc$3 = composeDoc$2$1, resolveEnd$7 = resolveEnd$6$1;
function getErrorPos$1(e5) {
  if (typeof e5 == "number") return [e5, e5 + 1];
  if (Array.isArray(e5)) return e5.length === 2 ? e5 : [e5[0], e5[1]];
  const { offset: t, source: n } = e5;
  return [t, t + (typeof n == "string" ? n.length : 1)];
}
function parsePrelude$1(e5) {
  var _a4;
  let t = "", n = false, r = false;
  for (let s = 0; s < e5.length; ++s) {
    const o = e5[s];
    switch (o[0]) {
      case "#":
        t += (t === "" ? "" : r ? `

` : `
`) + (o.substring(1) || " "), n = true, r = false;
        break;
      case "%":
        ((_a4 = e5[s + 1]) == null ? void 0 : _a4[0]) !== "#" && (s += 1), n = false;
        break;
      default:
        n || (r = true), n = false;
    }
  }
  return { comment: t, afterEmptyLine: r };
}
let Composer$1 = class Composer {
  constructor(t = {}) {
    this.doc = null, this.atDirectives = false, this.prelude = [], this.errors = [], this.warnings = [], this.onError = (n, r, s, o) => {
      const a = getErrorPos$1(n);
      o ? this.warnings.push(new errors$3$1.YAMLWarning(a, r, s)) : this.errors.push(new errors$3$1.YAMLParseError(a, r, s));
    }, this.directives = new directives$3.Directives({ version: t.version || "1.2" }), this.options = t;
  }
  decorate(t, n) {
    const { comment: r, afterEmptyLine: s } = parsePrelude$1(this.prelude);
    if (r) {
      const o = t.contents;
      if (n) t.comment = t.comment ? `${t.comment}
${r}` : r;
      else if (s || t.directives.docStart || !o) t.commentBefore = r;
      else if (identity$3$1.isCollection(o) && !o.flow && o.items.length > 0) {
        let a = o.items[0];
        identity$3$1.isPair(a) && (a = a.key);
        const l = a.commentBefore;
        a.commentBefore = l ? `${r}
${l}` : r;
      } else {
        const a = o.commentBefore;
        o.commentBefore = a ? `${r}
${a}` : r;
      }
    }
    n ? (Array.prototype.push.apply(t.errors, this.errors), Array.prototype.push.apply(t.warnings, this.warnings)) : (t.errors = this.errors, t.warnings = this.warnings), this.prelude = [], this.errors = [], this.warnings = [];
  }
  streamInfo() {
    return { comment: parsePrelude$1(this.prelude).comment, directives: this.directives, errors: this.errors, warnings: this.warnings };
  }
  *compose(t, n = false, r = -1) {
    for (const s of t) yield* this.next(s);
    yield* this.end(n, r);
  }
  *next(t) {
    switch (node_process$1$1.env.LOG_STREAM && console.dir(t, { depth: null }), t.type) {
      case "directive":
        this.directives.add(t.source, (n, r, s) => {
          const o = getErrorPos$1(t);
          o[0] += n, this.onError(o, "BAD_DIRECTIVE", r, s);
        }), this.prelude.push(t.source), this.atDirectives = true;
        break;
      case "document": {
        const n = composeDoc$3.composeDoc(this.options, this.directives, t, this.onError);
        this.atDirectives && !n.directives.docStart && this.onError(t, "MISSING_CHAR", "Missing directives-end/doc-start indicator line"), this.decorate(n, false), this.doc && (yield this.doc), this.doc = n, this.atDirectives = false;
        break;
      }
      case "byte-order-mark":
      case "space":
        break;
      case "comment":
      case "newline":
        this.prelude.push(t.source);
        break;
      case "error": {
        const n = t.source ? `${t.message}: ${JSON.stringify(t.source)}` : t.message, r = new errors$3$1.YAMLParseError(getErrorPos$1(t), "UNEXPECTED_TOKEN", n);
        this.atDirectives || !this.doc ? this.errors.push(r) : this.doc.errors.push(r);
        break;
      }
      case "doc-end": {
        if (!this.doc) {
          const r = "Unexpected doc-end without preceding document";
          this.errors.push(new errors$3$1.YAMLParseError(getErrorPos$1(t), "UNEXPECTED_TOKEN", r));
          break;
        }
        this.doc.directives.docEnd = true;
        const n = resolveEnd$7.resolveEnd(t.end, t.offset + t.source.length, this.doc.options.strict, this.onError);
        if (this.decorate(this.doc, true), n.comment) {
          const r = this.doc.comment;
          this.doc.comment = r ? `${r}
${n.comment}` : n.comment;
        }
        this.doc.range[2] = n.offset;
        break;
      }
      default:
        this.errors.push(new errors$3$1.YAMLParseError(getErrorPos$1(t), "UNEXPECTED_TOKEN", `Unsupported token ${t.type}`));
    }
  }
  *end(t = false, n = -1) {
    if (this.doc) this.decorate(this.doc, true), yield this.doc, this.doc = null;
    else if (t) {
      const r = Object.assign({ _directives: this.directives }, this.options), s = new Document$1$1.Document(void 0, r);
      this.atDirectives && this.onError(n, "MISSING_CHAR", "Missing directives-end indicator line"), s.range = [0, n, n], this.decorate(s, false), yield s;
    }
  }
};
composer$1$1.Composer = Composer$1;
var cst$2$1 = {}, cstScalar$1$1 = {}, resolveBlockScalar$4 = resolveBlockScalar$3$1, resolveFlowScalar$4 = resolveFlowScalar$3$1, errors$2$1 = errors$4$1, stringifyString$6 = stringifyString$5$1;
function resolveAsScalar$1(e5, t = true, n) {
  if (e5) {
    const r = (s, o, a) => {
      const l = typeof s == "number" ? s : Array.isArray(s) ? s[0] : s.offset;
      if (n) n(l, o, a);
      else throw new errors$2$1.YAMLParseError([l, l + 1], o, a);
    };
    switch (e5.type) {
      case "scalar":
      case "single-quoted-scalar":
      case "double-quoted-scalar":
        return resolveFlowScalar$4.resolveFlowScalar(e5, t, r);
      case "block-scalar":
        return resolveBlockScalar$4.resolveBlockScalar({ options: { strict: t } }, e5, r);
    }
  }
  return null;
}
function createScalarToken$1(e5, t) {
  var _a4;
  const { implicitKey: n = false, indent: r, inFlow: s = false, offset: o = -1, type: a = "PLAIN" } = t, l = stringifyString$6.stringifyString({ type: a, value: e5 }, { implicitKey: n, indent: r > 0 ? " ".repeat(r) : "", inFlow: s, options: { blockQuote: true, lineWidth: -1 } }), c = (_a4 = t.end) != null ? _a4 : [{ type: "newline", offset: -1, indent: r, source: `
` }];
  switch (l[0]) {
    case "|":
    case ">": {
      const u = l.indexOf(`
`), p = l.substring(0, u), f = l.substring(u + 1) + `
`, h = [{ type: "block-scalar-header", offset: o, indent: r, source: p }];
      return addEndtoBlockProps$1(h, c) || h.push({ type: "newline", offset: -1, indent: r, source: `
` }), { type: "block-scalar", offset: o, indent: r, props: h, source: f };
    }
    case '"':
      return { type: "double-quoted-scalar", offset: o, indent: r, source: l, end: c };
    case "'":
      return { type: "single-quoted-scalar", offset: o, indent: r, source: l, end: c };
    default:
      return { type: "scalar", offset: o, indent: r, source: l, end: c };
  }
}
function setScalarValue$1(e5, t, n = {}) {
  let { afterKey: r = false, implicitKey: s = false, inFlow: o = false, type: a } = n, l = "indent" in e5 ? e5.indent : null;
  if (r && typeof l == "number" && (l += 2), !a) switch (e5.type) {
    case "single-quoted-scalar":
      a = "QUOTE_SINGLE";
      break;
    case "double-quoted-scalar":
      a = "QUOTE_DOUBLE";
      break;
    case "block-scalar": {
      const u = e5.props[0];
      if (u.type !== "block-scalar-header") throw new Error("Invalid block scalar header");
      a = u.source[0] === ">" ? "BLOCK_FOLDED" : "BLOCK_LITERAL";
      break;
    }
    default:
      a = "PLAIN";
  }
  const c = stringifyString$6.stringifyString({ type: a, value: t }, { implicitKey: s || l === null, indent: l !== null && l > 0 ? " ".repeat(l) : "", inFlow: o, options: { blockQuote: true, lineWidth: -1 } });
  switch (c[0]) {
    case "|":
    case ">":
      setBlockScalarValue$1(e5, c);
      break;
    case '"':
      setFlowScalarValue$1(e5, c, "double-quoted-scalar");
      break;
    case "'":
      setFlowScalarValue$1(e5, c, "single-quoted-scalar");
      break;
    default:
      setFlowScalarValue$1(e5, c, "scalar");
  }
}
function setBlockScalarValue$1(e5, t) {
  const n = t.indexOf(`
`), r = t.substring(0, n), s = t.substring(n + 1) + `
`;
  if (e5.type === "block-scalar") {
    const o = e5.props[0];
    if (o.type !== "block-scalar-header") throw new Error("Invalid block scalar header");
    o.source = r, e5.source = s;
  } else {
    const { offset: o } = e5, a = "indent" in e5 ? e5.indent : -1, l = [{ type: "block-scalar-header", offset: o, indent: a, source: r }];
    addEndtoBlockProps$1(l, "end" in e5 ? e5.end : void 0) || l.push({ type: "newline", offset: -1, indent: a, source: `
` });
    for (const c of Object.keys(e5)) c !== "type" && c !== "offset" && delete e5[c];
    Object.assign(e5, { type: "block-scalar", indent: a, props: l, source: s });
  }
}
function addEndtoBlockProps$1(e5, t) {
  if (t) for (const n of t) switch (n.type) {
    case "space":
    case "comment":
      e5.push(n);
      break;
    case "newline":
      return e5.push(n), true;
  }
  return false;
}
function setFlowScalarValue$1(e5, t, n) {
  switch (e5.type) {
    case "scalar":
    case "double-quoted-scalar":
    case "single-quoted-scalar":
      e5.type = n, e5.source = t;
      break;
    case "block-scalar": {
      const r = e5.props.slice(1);
      let s = t.length;
      e5.props[0].type === "block-scalar-header" && (s -= e5.props[0].source.length);
      for (const o of r) o.offset += s;
      delete e5.props, Object.assign(e5, { type: n, source: t, end: r });
      break;
    }
    case "block-map":
    case "block-seq": {
      const s = { type: "newline", offset: e5.offset + t.length, indent: e5.indent, source: `
` };
      delete e5.items, Object.assign(e5, { type: n, source: t, end: [s] });
      break;
    }
    default: {
      const r = "indent" in e5 ? e5.indent : -1, s = "end" in e5 && Array.isArray(e5.end) ? e5.end.filter((o) => o.type === "space" || o.type === "comment" || o.type === "newline") : [];
      for (const o of Object.keys(e5)) o !== "type" && o !== "offset" && delete e5[o];
      Object.assign(e5, { type: n, indent: r, source: t, end: s });
    }
  }
}
cstScalar$1$1.createScalarToken = createScalarToken$1;
cstScalar$1$1.resolveAsScalar = resolveAsScalar$1;
cstScalar$1$1.setScalarValue = setScalarValue$1;
var cstStringify$1$1 = {};
const stringify$5$1 = (e5) => "type" in e5 ? stringifyToken$1(e5) : stringifyItem$1(e5);
function stringifyToken$1(e5) {
  switch (e5.type) {
    case "block-scalar": {
      let t = "";
      for (const n of e5.props) t += stringifyToken$1(n);
      return t + e5.source;
    }
    case "block-map":
    case "block-seq": {
      let t = "";
      for (const n of e5.items) t += stringifyItem$1(n);
      return t;
    }
    case "flow-collection": {
      let t = e5.start.source;
      for (const n of e5.items) t += stringifyItem$1(n);
      for (const n of e5.end) t += n.source;
      return t;
    }
    case "document": {
      let t = stringifyItem$1(e5);
      if (e5.end) for (const n of e5.end) t += n.source;
      return t;
    }
    default: {
      let t = e5.source;
      if ("end" in e5 && e5.end) for (const n of e5.end) t += n.source;
      return t;
    }
  }
}
function stringifyItem$1({ start: e5, key: t, sep: n, value: r }) {
  let s = "";
  for (const o of e5) s += o.source;
  if (t && (s += stringifyToken$1(t)), n) for (const o of n) s += o.source;
  return r && (s += stringifyToken$1(r)), s;
}
cstStringify$1$1.stringify = stringify$5$1;
var cstVisit$1$1 = {};
const BREAK$2 = /* @__PURE__ */ Symbol("break visit"), SKIP$2 = /* @__PURE__ */ Symbol("skip children"), REMOVE$2 = /* @__PURE__ */ Symbol("remove item");
function visit$1$1(e5, t) {
  "type" in e5 && e5.type === "document" && (e5 = { start: e5.start, value: e5.value }), _visit$1(Object.freeze([]), e5, t);
}
visit$1$1.BREAK = BREAK$2;
visit$1$1.SKIP = SKIP$2;
visit$1$1.REMOVE = REMOVE$2;
visit$1$1.itemAtPath = (e5, t) => {
  let n = e5;
  for (const [r, s] of t) {
    const o = n == null ? void 0 : n[r];
    if (o && "items" in o) n = o.items[s];
    else return;
  }
  return n;
};
visit$1$1.parentCollection = (e5, t) => {
  const n = visit$1$1.itemAtPath(e5, t.slice(0, -1)), r = t[t.length - 1][0], s = n == null ? void 0 : n[r];
  if (s && "items" in s) return s;
  throw new Error("Parent collection not found");
};
function _visit$1(e5, t, n) {
  let r = n(t, e5);
  if (typeof r == "symbol") return r;
  for (const s of ["key", "value"]) {
    const o = t[s];
    if (o && "items" in o) {
      for (let a = 0; a < o.items.length; ++a) {
        const l = _visit$1(Object.freeze(e5.concat([[s, a]])), o.items[a], n);
        if (typeof l == "number") a = l - 1;
        else {
          if (l === BREAK$2) return BREAK$2;
          l === REMOVE$2 && (o.items.splice(a, 1), a -= 1);
        }
      }
      typeof r == "function" && s === "key" && (r = r(t, e5));
    }
  }
  return typeof r == "function" ? r(t, e5) : r;
}
cstVisit$1$1.visit = visit$1$1;
var cstScalar$2 = cstScalar$1$1, cstStringify$2 = cstStringify$1$1, cstVisit$2 = cstVisit$1$1;
const BOM$1 = "\uFEFF", DOCUMENT$1 = "", FLOW_END$1 = "", SCALAR$2 = "", isCollection$2 = (e5) => !!e5 && "items" in e5, isScalar$2 = (e5) => !!e5 && (e5.type === "scalar" || e5.type === "single-quoted-scalar" || e5.type === "double-quoted-scalar" || e5.type === "block-scalar");
function prettyToken$1(e5) {
  switch (e5) {
    case BOM$1:
      return "<BOM>";
    case DOCUMENT$1:
      return "<DOC>";
    case FLOW_END$1:
      return "<FLOW_END>";
    case SCALAR$2:
      return "<SCALAR>";
    default:
      return JSON.stringify(e5);
  }
}
function tokenType$1(e5) {
  switch (e5) {
    case BOM$1:
      return "byte-order-mark";
    case DOCUMENT$1:
      return "doc-mode";
    case FLOW_END$1:
      return "flow-error-end";
    case SCALAR$2:
      return "scalar";
    case "---":
      return "doc-start";
    case "...":
      return "doc-end";
    case "":
    case `
`:
    case `\r
`:
      return "newline";
    case "-":
      return "seq-item-ind";
    case "?":
      return "explicit-key-ind";
    case ":":
      return "map-value-ind";
    case "{":
      return "flow-map-start";
    case "}":
      return "flow-map-end";
    case "[":
      return "flow-seq-start";
    case "]":
      return "flow-seq-end";
    case ",":
      return "comma";
  }
  switch (e5[0]) {
    case " ":
    case "	":
      return "space";
    case "#":
      return "comment";
    case "%":
      return "directive-line";
    case "*":
      return "alias";
    case "&":
      return "anchor";
    case "!":
      return "tag";
    case "'":
      return "single-quoted-scalar";
    case '"':
      return "double-quoted-scalar";
    case "|":
    case ">":
      return "block-scalar-header";
  }
  return null;
}
cst$2$1.createScalarToken = cstScalar$2.createScalarToken;
cst$2$1.resolveAsScalar = cstScalar$2.resolveAsScalar;
cst$2$1.setScalarValue = cstScalar$2.setScalarValue;
cst$2$1.stringify = cstStringify$2.stringify;
cst$2$1.visit = cstVisit$2.visit;
cst$2$1.BOM = BOM$1;
cst$2$1.DOCUMENT = DOCUMENT$1;
cst$2$1.FLOW_END = FLOW_END$1;
cst$2$1.SCALAR = SCALAR$2;
cst$2$1.isCollection = isCollection$2;
cst$2$1.isScalar = isScalar$2;
cst$2$1.prettyToken = prettyToken$1;
cst$2$1.tokenType = tokenType$1;
var lexer$1$1 = {}, cst$1$1 = cst$2$1;
function isEmpty$1(e5) {
  switch (e5) {
    case void 0:
    case " ":
    case `
`:
    case "\r":
    case "	":
      return true;
    default:
      return false;
  }
}
const hexDigits$1 = new Set("0123456789ABCDEFabcdef"), tagChars$1 = new Set("0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-#;/?:@&=+$_.!~*'()"), flowIndicatorChars$1 = new Set(",[]{}"), invalidAnchorChars$1 = new Set(` ,[]{}
\r	`), isNotAnchorChar$1 = (e5) => !e5 || invalidAnchorChars$1.has(e5);
let Lexer$1 = class Lexer {
  constructor() {
    this.atEnd = false, this.blockScalarIndent = -1, this.blockScalarKeep = false, this.buffer = "", this.flowKey = false, this.flowLevel = 0, this.indentNext = 0, this.indentValue = 0, this.lineEndPos = null, this.next = null, this.pos = 0;
  }
  *lex(t, n = false) {
    var _a4;
    if (t) {
      if (typeof t != "string") throw TypeError("source is not a string");
      this.buffer = this.buffer ? this.buffer + t : t, this.lineEndPos = null;
    }
    this.atEnd = !n;
    let r = (_a4 = this.next) != null ? _a4 : "stream";
    for (; r && (n || this.hasChars(1)); ) r = yield* this.parseNext(r);
  }
  atLineEnd() {
    let t = this.pos, n = this.buffer[t];
    for (; n === " " || n === "	"; ) n = this.buffer[++t];
    return !n || n === "#" || n === `
` ? true : n === "\r" ? this.buffer[t + 1] === `
` : false;
  }
  charAt(t) {
    return this.buffer[this.pos + t];
  }
  continueScalar(t) {
    let n = this.buffer[t];
    if (this.indentNext > 0) {
      let r = 0;
      for (; n === " "; ) n = this.buffer[++r + t];
      if (n === "\r") {
        const s = this.buffer[r + t + 1];
        if (s === `
` || !s && !this.atEnd) return t + r + 1;
      }
      return n === `
` || r >= this.indentNext || !n && !this.atEnd ? t + r : -1;
    }
    if (n === "-" || n === ".") {
      const r = this.buffer.substr(t, 3);
      if ((r === "---" || r === "...") && isEmpty$1(this.buffer[t + 3])) return -1;
    }
    return t;
  }
  getLine() {
    let t = this.lineEndPos;
    return (typeof t != "number" || t !== -1 && t < this.pos) && (t = this.buffer.indexOf(`
`, this.pos), this.lineEndPos = t), t === -1 ? this.atEnd ? this.buffer.substring(this.pos) : null : (this.buffer[t - 1] === "\r" && (t -= 1), this.buffer.substring(this.pos, t));
  }
  hasChars(t) {
    return this.pos + t <= this.buffer.length;
  }
  setNext(t) {
    return this.buffer = this.buffer.substring(this.pos), this.pos = 0, this.lineEndPos = null, this.next = t, null;
  }
  peek(t) {
    return this.buffer.substr(this.pos, t);
  }
  *parseNext(t) {
    switch (t) {
      case "stream":
        return yield* this.parseStream();
      case "line-start":
        return yield* this.parseLineStart();
      case "block-start":
        return yield* this.parseBlockStart();
      case "doc":
        return yield* this.parseDocument();
      case "flow":
        return yield* this.parseFlowCollection();
      case "quoted-scalar":
        return yield* this.parseQuotedScalar();
      case "block-scalar":
        return yield* this.parseBlockScalar();
      case "plain-scalar":
        return yield* this.parsePlainScalar();
    }
  }
  *parseStream() {
    let t = this.getLine();
    if (t === null) return this.setNext("stream");
    if (t[0] === cst$1$1.BOM && (yield* this.pushCount(1), t = t.substring(1)), t[0] === "%") {
      let n = t.length, r = t.indexOf("#");
      for (; r !== -1; ) {
        const o = t[r - 1];
        if (o === " " || o === "	") {
          n = r - 1;
          break;
        } else r = t.indexOf("#", r + 1);
      }
      for (; ; ) {
        const o = t[n - 1];
        if (o === " " || o === "	") n -= 1;
        else break;
      }
      const s = (yield* this.pushCount(n)) + (yield* this.pushSpaces(true));
      return yield* this.pushCount(t.length - s), this.pushNewline(), "stream";
    }
    if (this.atLineEnd()) {
      const n = yield* this.pushSpaces(true);
      return yield* this.pushCount(t.length - n), yield* this.pushNewline(), "stream";
    }
    return yield cst$1$1.DOCUMENT, yield* this.parseLineStart();
  }
  *parseLineStart() {
    const t = this.charAt(0);
    if (!t && !this.atEnd) return this.setNext("line-start");
    if (t === "-" || t === ".") {
      if (!this.atEnd && !this.hasChars(4)) return this.setNext("line-start");
      const n = this.peek(3);
      if ((n === "---" || n === "...") && isEmpty$1(this.charAt(3))) return yield* this.pushCount(3), this.indentValue = 0, this.indentNext = 0, n === "---" ? "doc" : "stream";
    }
    return this.indentValue = yield* this.pushSpaces(false), this.indentNext > this.indentValue && !isEmpty$1(this.charAt(1)) && (this.indentNext = this.indentValue), yield* this.parseBlockStart();
  }
  *parseBlockStart() {
    const [t, n] = this.peek(2);
    if (!n && !this.atEnd) return this.setNext("block-start");
    if ((t === "-" || t === "?" || t === ":") && isEmpty$1(n)) {
      const r = (yield* this.pushCount(1)) + (yield* this.pushSpaces(true));
      return this.indentNext = this.indentValue + 1, this.indentValue += r, yield* this.parseBlockStart();
    }
    return "doc";
  }
  *parseDocument() {
    yield* this.pushSpaces(true);
    const t = this.getLine();
    if (t === null) return this.setNext("doc");
    let n = yield* this.pushIndicators();
    switch (t[n]) {
      case "#":
        yield* this.pushCount(t.length - n);
      case void 0:
        return yield* this.pushNewline(), yield* this.parseLineStart();
      case "{":
      case "[":
        return yield* this.pushCount(1), this.flowKey = false, this.flowLevel = 1, "flow";
      case "}":
      case "]":
        return yield* this.pushCount(1), "doc";
      case "*":
        return yield* this.pushUntil(isNotAnchorChar$1), "doc";
      case '"':
      case "'":
        return yield* this.parseQuotedScalar();
      case "|":
      case ">":
        return n += yield* this.parseBlockScalarHeader(), n += yield* this.pushSpaces(true), yield* this.pushCount(t.length - n), yield* this.pushNewline(), yield* this.parseBlockScalar();
      default:
        return yield* this.parsePlainScalar();
    }
  }
  *parseFlowCollection() {
    let t, n, r = -1;
    do
      t = yield* this.pushNewline(), t > 0 ? (n = yield* this.pushSpaces(false), this.indentValue = r = n) : n = 0, n += yield* this.pushSpaces(true);
    while (t + n > 0);
    const s = this.getLine();
    if (s === null) return this.setNext("flow");
    if ((r !== -1 && r < this.indentNext && s[0] !== "#" || r === 0 && (s.startsWith("---") || s.startsWith("...")) && isEmpty$1(s[3])) && !(r === this.indentNext - 1 && this.flowLevel === 1 && (s[0] === "]" || s[0] === "}"))) return this.flowLevel = 0, yield cst$1$1.FLOW_END, yield* this.parseLineStart();
    let o = 0;
    for (; s[o] === ","; ) o += yield* this.pushCount(1), o += yield* this.pushSpaces(true), this.flowKey = false;
    switch (o += yield* this.pushIndicators(), s[o]) {
      case void 0:
        return "flow";
      case "#":
        return yield* this.pushCount(s.length - o), "flow";
      case "{":
      case "[":
        return yield* this.pushCount(1), this.flowKey = false, this.flowLevel += 1, "flow";
      case "}":
      case "]":
        return yield* this.pushCount(1), this.flowKey = true, this.flowLevel -= 1, this.flowLevel ? "flow" : "doc";
      case "*":
        return yield* this.pushUntil(isNotAnchorChar$1), "flow";
      case '"':
      case "'":
        return this.flowKey = true, yield* this.parseQuotedScalar();
      case ":": {
        const a = this.charAt(1);
        if (this.flowKey || isEmpty$1(a) || a === ",") return this.flowKey = false, yield* this.pushCount(1), yield* this.pushSpaces(true), "flow";
      }
      default:
        return this.flowKey = false, yield* this.parsePlainScalar();
    }
  }
  *parseQuotedScalar() {
    const t = this.charAt(0);
    let n = this.buffer.indexOf(t, this.pos + 1);
    if (t === "'") for (; n !== -1 && this.buffer[n + 1] === "'"; ) n = this.buffer.indexOf("'", n + 2);
    else for (; n !== -1; ) {
      let o = 0;
      for (; this.buffer[n - 1 - o] === "\\"; ) o += 1;
      if (o % 2 === 0) break;
      n = this.buffer.indexOf('"', n + 1);
    }
    const r = this.buffer.substring(0, n);
    let s = r.indexOf(`
`, this.pos);
    if (s !== -1) {
      for (; s !== -1; ) {
        const o = this.continueScalar(s + 1);
        if (o === -1) break;
        s = r.indexOf(`
`, o);
      }
      s !== -1 && (n = s - (r[s - 1] === "\r" ? 2 : 1));
    }
    if (n === -1) {
      if (!this.atEnd) return this.setNext("quoted-scalar");
      n = this.buffer.length;
    }
    return yield* this.pushToIndex(n + 1, false), this.flowLevel ? "flow" : "doc";
  }
  *parseBlockScalarHeader() {
    this.blockScalarIndent = -1, this.blockScalarKeep = false;
    let t = this.pos;
    for (; ; ) {
      const n = this.buffer[++t];
      if (n === "+") this.blockScalarKeep = true;
      else if (n > "0" && n <= "9") this.blockScalarIndent = Number(n) - 1;
      else if (n !== "-") break;
    }
    return yield* this.pushUntil((n) => isEmpty$1(n) || n === "#");
  }
  *parseBlockScalar() {
    let t = this.pos - 1, n = 0, r;
    e: for (let o = this.pos; r = this.buffer[o]; ++o) switch (r) {
      case " ":
        n += 1;
        break;
      case `
`:
        t = o, n = 0;
        break;
      case "\r": {
        const a = this.buffer[o + 1];
        if (!a && !this.atEnd) return this.setNext("block-scalar");
        if (a === `
`) break;
      }
      default:
        break e;
    }
    if (!r && !this.atEnd) return this.setNext("block-scalar");
    if (n >= this.indentNext) {
      this.blockScalarIndent === -1 ? this.indentNext = n : this.indentNext = this.blockScalarIndent + (this.indentNext === 0 ? 1 : this.indentNext);
      do {
        const o = this.continueScalar(t + 1);
        if (o === -1) break;
        t = this.buffer.indexOf(`
`, o);
      } while (t !== -1);
      if (t === -1) {
        if (!this.atEnd) return this.setNext("block-scalar");
        t = this.buffer.length;
      }
    }
    let s = t + 1;
    for (r = this.buffer[s]; r === " "; ) r = this.buffer[++s];
    if (r === "	") {
      for (; r === "	" || r === " " || r === "\r" || r === `
`; ) r = this.buffer[++s];
      t = s - 1;
    } else if (!this.blockScalarKeep) do {
      let o = t - 1, a = this.buffer[o];
      a === "\r" && (a = this.buffer[--o]);
      const l = o;
      for (; a === " "; ) a = this.buffer[--o];
      if (a === `
` && o >= this.pos && o + 1 + n > l) t = o;
      else break;
    } while (true);
    return yield cst$1$1.SCALAR, yield* this.pushToIndex(t + 1, true), yield* this.parseLineStart();
  }
  *parsePlainScalar() {
    const t = this.flowLevel > 0;
    let n = this.pos - 1, r = this.pos - 1, s;
    for (; s = this.buffer[++r]; ) if (s === ":") {
      const o = this.buffer[r + 1];
      if (isEmpty$1(o) || t && flowIndicatorChars$1.has(o)) break;
      n = r;
    } else if (isEmpty$1(s)) {
      let o = this.buffer[r + 1];
      if (s === "\r" && (o === `
` ? (r += 1, s = `
`, o = this.buffer[r + 1]) : n = r), o === "#" || t && flowIndicatorChars$1.has(o)) break;
      if (s === `
`) {
        const a = this.continueScalar(r + 1);
        if (a === -1) break;
        r = Math.max(r, a - 2);
      }
    } else {
      if (t && flowIndicatorChars$1.has(s)) break;
      n = r;
    }
    return !s && !this.atEnd ? this.setNext("plain-scalar") : (yield cst$1$1.SCALAR, yield* this.pushToIndex(n + 1, true), t ? "flow" : "doc");
  }
  *pushCount(t) {
    return t > 0 ? (yield this.buffer.substr(this.pos, t), this.pos += t, t) : 0;
  }
  *pushToIndex(t, n) {
    const r = this.buffer.slice(this.pos, t);
    return r ? (yield r, this.pos += r.length, r.length) : (n && (yield ""), 0);
  }
  *pushIndicators() {
    switch (this.charAt(0)) {
      case "!":
        return (yield* this.pushTag()) + (yield* this.pushSpaces(true)) + (yield* this.pushIndicators());
      case "&":
        return (yield* this.pushUntil(isNotAnchorChar$1)) + (yield* this.pushSpaces(true)) + (yield* this.pushIndicators());
      case "-":
      case "?":
      case ":": {
        const t = this.flowLevel > 0, n = this.charAt(1);
        if (isEmpty$1(n) || t && flowIndicatorChars$1.has(n)) return t ? this.flowKey && (this.flowKey = false) : this.indentNext = this.indentValue + 1, (yield* this.pushCount(1)) + (yield* this.pushSpaces(true)) + (yield* this.pushIndicators());
      }
    }
    return 0;
  }
  *pushTag() {
    if (this.charAt(1) === "<") {
      let t = this.pos + 2, n = this.buffer[t];
      for (; !isEmpty$1(n) && n !== ">"; ) n = this.buffer[++t];
      return yield* this.pushToIndex(n === ">" ? t + 1 : t, false);
    } else {
      let t = this.pos + 1, n = this.buffer[t];
      for (; n; ) if (tagChars$1.has(n)) n = this.buffer[++t];
      else if (n === "%" && hexDigits$1.has(this.buffer[t + 1]) && hexDigits$1.has(this.buffer[t + 2])) n = this.buffer[t += 3];
      else break;
      return yield* this.pushToIndex(t, false);
    }
  }
  *pushNewline() {
    const t = this.buffer[this.pos];
    return t === `
` ? yield* this.pushCount(1) : t === "\r" && this.charAt(1) === `
` ? yield* this.pushCount(2) : 0;
  }
  *pushSpaces(t) {
    let n = this.pos - 1, r;
    do
      r = this.buffer[++n];
    while (r === " " || t && r === "	");
    const s = n - this.pos;
    return s > 0 && (yield this.buffer.substr(this.pos, s), this.pos = n), s;
  }
  *pushUntil(t) {
    let n = this.pos, r = this.buffer[n];
    for (; !t(r); ) r = this.buffer[++n];
    return yield* this.pushToIndex(n, false);
  }
};
lexer$1$1.Lexer = Lexer$1;
var lineCounter$1$1 = {};
let LineCounter$1 = class LineCounter {
  constructor() {
    this.lineStarts = [], this.addNewLine = (t) => this.lineStarts.push(t), this.linePos = (t) => {
      let n = 0, r = this.lineStarts.length;
      for (; n < r; ) {
        const o = n + r >> 1;
        this.lineStarts[o] < t ? n = o + 1 : r = o;
      }
      if (this.lineStarts[n] === t) return { line: n + 1, col: 1 };
      if (n === 0) return { line: 0, col: t };
      const s = this.lineStarts[n - 1];
      return { line: n, col: t - s + 1 };
    };
  }
};
lineCounter$1$1.LineCounter = LineCounter$1;
var parser$1$1 = {}, node_process$3 = require$$0, cst$3 = cst$2$1, lexer$2 = lexer$1$1;
function includesToken$1(e5, t) {
  for (let n = 0; n < e5.length; ++n) if (e5[n].type === t) return true;
  return false;
}
function findNonEmptyIndex$1(e5) {
  for (let t = 0; t < e5.length; ++t) switch (e5[t].type) {
    case "space":
    case "comment":
    case "newline":
      break;
    default:
      return t;
  }
  return -1;
}
function isFlowToken$1(e5) {
  switch (e5 == null ? void 0 : e5.type) {
    case "alias":
    case "scalar":
    case "single-quoted-scalar":
    case "double-quoted-scalar":
    case "flow-collection":
      return true;
    default:
      return false;
  }
}
function getPrevProps$1(e5) {
  var _a4;
  switch (e5.type) {
    case "document":
      return e5.start;
    case "block-map": {
      const t = e5.items[e5.items.length - 1];
      return (_a4 = t.sep) != null ? _a4 : t.start;
    }
    case "block-seq":
      return e5.items[e5.items.length - 1].start;
    default:
      return [];
  }
}
function getFirstKeyStartProps$1(e5) {
  var _a4;
  if (e5.length === 0) return [];
  let t = e5.length;
  e: for (; --t >= 0; ) switch (e5[t].type) {
    case "doc-start":
    case "explicit-key-ind":
    case "map-value-ind":
    case "seq-item-ind":
    case "newline":
      break e;
  }
  for (; ((_a4 = e5[++t]) == null ? void 0 : _a4.type) === "space"; ) ;
  return e5.splice(t, e5.length);
}
function fixFlowSeqItems$1(e5) {
  if (e5.start.type === "flow-seq-start") for (const t of e5.items) t.sep && !t.value && !includesToken$1(t.start, "explicit-key-ind") && !includesToken$1(t.sep, "map-value-ind") && (t.key && (t.value = t.key), delete t.key, isFlowToken$1(t.value) ? t.value.end ? Array.prototype.push.apply(t.value.end, t.sep) : t.value.end = t.sep : Array.prototype.push.apply(t.start, t.sep), delete t.sep);
}
let Parser$1 = class Parser {
  constructor(t) {
    this.atNewLine = true, this.atScalar = false, this.indent = 0, this.offset = 0, this.onKeyLine = false, this.stack = [], this.source = "", this.type = "", this.lexer = new lexer$2.Lexer(), this.onNewLine = t;
  }
  *parse(t, n = false) {
    this.onNewLine && this.offset === 0 && this.onNewLine(0);
    for (const r of this.lexer.lex(t, n)) yield* this.next(r);
    n || (yield* this.end());
  }
  *next(t) {
    if (this.source = t, node_process$3.env.LOG_TOKENS && console.log("|", cst$3.prettyToken(t)), this.atScalar) {
      this.atScalar = false, yield* this.step(), this.offset += t.length;
      return;
    }
    const n = cst$3.tokenType(t);
    if (n) if (n === "scalar") this.atNewLine = false, this.atScalar = true, this.type = "scalar";
    else {
      switch (this.type = n, yield* this.step(), n) {
        case "newline":
          this.atNewLine = true, this.indent = 0, this.onNewLine && this.onNewLine(this.offset + t.length);
          break;
        case "space":
          this.atNewLine && t[0] === " " && (this.indent += t.length);
          break;
        case "explicit-key-ind":
        case "map-value-ind":
        case "seq-item-ind":
          this.atNewLine && (this.indent += t.length);
          break;
        case "doc-mode":
        case "flow-error-end":
          return;
        default:
          this.atNewLine = false;
      }
      this.offset += t.length;
    }
    else {
      const r = `Not a YAML token: ${t}`;
      yield* this.pop({ type: "error", offset: this.offset, message: r, source: t }), this.offset += t.length;
    }
  }
  *end() {
    for (; this.stack.length > 0; ) yield* this.pop();
  }
  get sourceToken() {
    return { type: this.type, offset: this.offset, indent: this.indent, source: this.source };
  }
  *step() {
    const t = this.peek(1);
    if (this.type === "doc-end" && (t == null ? void 0 : t.type) !== "doc-end") {
      for (; this.stack.length > 0; ) yield* this.pop();
      this.stack.push({ type: "doc-end", offset: this.offset, source: this.source });
      return;
    }
    if (!t) return yield* this.stream();
    switch (t.type) {
      case "document":
        return yield* this.document(t);
      case "alias":
      case "scalar":
      case "single-quoted-scalar":
      case "double-quoted-scalar":
        return yield* this.scalar(t);
      case "block-scalar":
        return yield* this.blockScalar(t);
      case "block-map":
        return yield* this.blockMap(t);
      case "block-seq":
        return yield* this.blockSequence(t);
      case "flow-collection":
        return yield* this.flowCollection(t);
      case "doc-end":
        return yield* this.documentEnd(t);
    }
    yield* this.pop();
  }
  peek(t) {
    return this.stack[this.stack.length - t];
  }
  *pop(t) {
    const n = t != null ? t : this.stack.pop();
    if (!n) yield { type: "error", offset: this.offset, source: "", message: "Tried to pop an empty stack" };
    else if (this.stack.length === 0) yield n;
    else {
      const r = this.peek(1);
      switch (n.type === "block-scalar" ? n.indent = "indent" in r ? r.indent : 0 : n.type === "flow-collection" && r.type === "document" && (n.indent = 0), n.type === "flow-collection" && fixFlowSeqItems$1(n), r.type) {
        case "document":
          r.value = n;
          break;
        case "block-scalar":
          r.props.push(n);
          break;
        case "block-map": {
          const s = r.items[r.items.length - 1];
          if (s.value) {
            r.items.push({ start: [], key: n, sep: [] }), this.onKeyLine = true;
            return;
          } else if (s.sep) s.value = n;
          else {
            Object.assign(s, { key: n, sep: [] }), this.onKeyLine = !s.explicitKey;
            return;
          }
          break;
        }
        case "block-seq": {
          const s = r.items[r.items.length - 1];
          s.value ? r.items.push({ start: [], value: n }) : s.value = n;
          break;
        }
        case "flow-collection": {
          const s = r.items[r.items.length - 1];
          !s || s.value ? r.items.push({ start: [], key: n, sep: [] }) : s.sep ? s.value = n : Object.assign(s, { key: n, sep: [] });
          return;
        }
        default:
          yield* this.pop(), yield* this.pop(n);
      }
      if ((r.type === "document" || r.type === "block-map" || r.type === "block-seq") && (n.type === "block-map" || n.type === "block-seq")) {
        const s = n.items[n.items.length - 1];
        s && !s.sep && !s.value && s.start.length > 0 && findNonEmptyIndex$1(s.start) === -1 && (n.indent === 0 || s.start.every((o) => o.type !== "comment" || o.indent < n.indent)) && (r.type === "document" ? r.end = s.start : r.items.push({ start: s.start }), n.items.splice(-1, 1));
      }
    }
  }
  *stream() {
    switch (this.type) {
      case "directive-line":
        yield { type: "directive", offset: this.offset, source: this.source };
        return;
      case "byte-order-mark":
      case "space":
      case "comment":
      case "newline":
        yield this.sourceToken;
        return;
      case "doc-mode":
      case "doc-start": {
        const t = { type: "document", offset: this.offset, start: [] };
        this.type === "doc-start" && t.start.push(this.sourceToken), this.stack.push(t);
        return;
      }
    }
    yield { type: "error", offset: this.offset, message: `Unexpected ${this.type} token in YAML stream`, source: this.source };
  }
  *document(t) {
    if (t.value) return yield* this.lineEnd(t);
    switch (this.type) {
      case "doc-start": {
        findNonEmptyIndex$1(t.start) !== -1 ? (yield* this.pop(), yield* this.step()) : t.start.push(this.sourceToken);
        return;
      }
      case "anchor":
      case "tag":
      case "space":
      case "comment":
      case "newline":
        t.start.push(this.sourceToken);
        return;
    }
    const n = this.startBlockValue(t);
    n ? this.stack.push(n) : yield { type: "error", offset: this.offset, message: `Unexpected ${this.type} token in YAML document`, source: this.source };
  }
  *scalar(t) {
    if (this.type === "map-value-ind") {
      const n = getPrevProps$1(this.peek(2)), r = getFirstKeyStartProps$1(n);
      let s;
      t.end ? (s = t.end, s.push(this.sourceToken), delete t.end) : s = [this.sourceToken];
      const o = { type: "block-map", offset: t.offset, indent: t.indent, items: [{ start: r, key: t, sep: s }] };
      this.onKeyLine = true, this.stack[this.stack.length - 1] = o;
    } else yield* this.lineEnd(t);
  }
  *blockScalar(t) {
    switch (this.type) {
      case "space":
      case "comment":
      case "newline":
        t.props.push(this.sourceToken);
        return;
      case "scalar":
        if (t.source = this.source, this.atNewLine = true, this.indent = 0, this.onNewLine) {
          let n = this.source.indexOf(`
`) + 1;
          for (; n !== 0; ) this.onNewLine(this.offset + n), n = this.source.indexOf(`
`, n) + 1;
        }
        yield* this.pop();
        break;
      default:
        yield* this.pop(), yield* this.step();
    }
  }
  *blockMap(t) {
    var _a4, _b, _c2;
    const n = t.items[t.items.length - 1];
    switch (this.type) {
      case "newline":
        if (this.onKeyLine = false, n.value) {
          const r = "end" in n.value ? n.value.end : void 0;
          ((_a4 = Array.isArray(r) ? r[r.length - 1] : void 0) == null ? void 0 : _a4.type) === "comment" ? r == null ? void 0 : r.push(this.sourceToken) : t.items.push({ start: [this.sourceToken] });
        } else n.sep ? n.sep.push(this.sourceToken) : n.start.push(this.sourceToken);
        return;
      case "space":
      case "comment":
        if (n.value) t.items.push({ start: [this.sourceToken] });
        else if (n.sep) n.sep.push(this.sourceToken);
        else {
          if (this.atIndentedComment(n.start, t.indent)) {
            const s = (_c2 = (_b = t.items[t.items.length - 2]) == null ? void 0 : _b.value) == null ? void 0 : _c2.end;
            if (Array.isArray(s)) {
              Array.prototype.push.apply(s, n.start), s.push(this.sourceToken), t.items.pop();
              return;
            }
          }
          n.start.push(this.sourceToken);
        }
        return;
    }
    if (this.indent >= t.indent) {
      const r = !this.onKeyLine && this.indent === t.indent, s = r && (n.sep || n.explicitKey) && this.type !== "seq-item-ind";
      let o = [];
      if (s && n.sep && !n.value) {
        const a = [];
        for (let l = 0; l < n.sep.length; ++l) {
          const c = n.sep[l];
          switch (c.type) {
            case "newline":
              a.push(l);
              break;
            case "space":
              break;
            case "comment":
              c.indent > t.indent && (a.length = 0);
              break;
            default:
              a.length = 0;
          }
        }
        a.length >= 2 && (o = n.sep.splice(a[1]));
      }
      switch (this.type) {
        case "anchor":
        case "tag":
          s || n.value ? (o.push(this.sourceToken), t.items.push({ start: o }), this.onKeyLine = true) : n.sep ? n.sep.push(this.sourceToken) : n.start.push(this.sourceToken);
          return;
        case "explicit-key-ind":
          !n.sep && !n.explicitKey ? (n.start.push(this.sourceToken), n.explicitKey = true) : s || n.value ? (o.push(this.sourceToken), t.items.push({ start: o, explicitKey: true })) : this.stack.push({ type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: [this.sourceToken], explicitKey: true }] }), this.onKeyLine = true;
          return;
        case "map-value-ind":
          if (n.explicitKey) if (n.sep) if (n.value) t.items.push({ start: [], key: null, sep: [this.sourceToken] });
          else if (includesToken$1(n.sep, "map-value-ind")) this.stack.push({ type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: o, key: null, sep: [this.sourceToken] }] });
          else if (isFlowToken$1(n.key) && !includesToken$1(n.sep, "newline")) {
            const a = getFirstKeyStartProps$1(n.start), l = n.key, c = n.sep;
            c.push(this.sourceToken), delete n.key, delete n.sep, this.stack.push({ type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: a, key: l, sep: c }] });
          } else o.length > 0 ? n.sep = n.sep.concat(o, this.sourceToken) : n.sep.push(this.sourceToken);
          else if (includesToken$1(n.start, "newline")) Object.assign(n, { key: null, sep: [this.sourceToken] });
          else {
            const a = getFirstKeyStartProps$1(n.start);
            this.stack.push({ type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: a, key: null, sep: [this.sourceToken] }] });
          }
          else n.sep ? n.value || s ? t.items.push({ start: o, key: null, sep: [this.sourceToken] }) : includesToken$1(n.sep, "map-value-ind") ? this.stack.push({ type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: [], key: null, sep: [this.sourceToken] }] }) : n.sep.push(this.sourceToken) : Object.assign(n, { key: null, sep: [this.sourceToken] });
          this.onKeyLine = true;
          return;
        case "alias":
        case "scalar":
        case "single-quoted-scalar":
        case "double-quoted-scalar": {
          const a = this.flowScalar(this.type);
          s || n.value ? (t.items.push({ start: o, key: a, sep: [] }), this.onKeyLine = true) : n.sep ? this.stack.push(a) : (Object.assign(n, { key: a, sep: [] }), this.onKeyLine = true);
          return;
        }
        default: {
          const a = this.startBlockValue(t);
          if (a) {
            if (a.type === "block-seq") {
              if (!n.explicitKey && n.sep && !includesToken$1(n.sep, "newline")) {
                yield* this.pop({ type: "error", offset: this.offset, message: "Unexpected block-seq-ind on same line with key", source: this.source });
                return;
              }
            } else r && t.items.push({ start: o });
            this.stack.push(a);
            return;
          }
        }
      }
    }
    yield* this.pop(), yield* this.step();
  }
  *blockSequence(t) {
    var _a4, _b, _c2;
    const n = t.items[t.items.length - 1];
    switch (this.type) {
      case "newline":
        if (n.value) {
          const r = "end" in n.value ? n.value.end : void 0;
          ((_a4 = Array.isArray(r) ? r[r.length - 1] : void 0) == null ? void 0 : _a4.type) === "comment" ? r == null ? void 0 : r.push(this.sourceToken) : t.items.push({ start: [this.sourceToken] });
        } else n.start.push(this.sourceToken);
        return;
      case "space":
      case "comment":
        if (n.value) t.items.push({ start: [this.sourceToken] });
        else {
          if (this.atIndentedComment(n.start, t.indent)) {
            const s = (_c2 = (_b = t.items[t.items.length - 2]) == null ? void 0 : _b.value) == null ? void 0 : _c2.end;
            if (Array.isArray(s)) {
              Array.prototype.push.apply(s, n.start), s.push(this.sourceToken), t.items.pop();
              return;
            }
          }
          n.start.push(this.sourceToken);
        }
        return;
      case "anchor":
      case "tag":
        if (n.value || this.indent <= t.indent) break;
        n.start.push(this.sourceToken);
        return;
      case "seq-item-ind":
        if (this.indent !== t.indent) break;
        n.value || includesToken$1(n.start, "seq-item-ind") ? t.items.push({ start: [this.sourceToken] }) : n.start.push(this.sourceToken);
        return;
    }
    if (this.indent > t.indent) {
      const r = this.startBlockValue(t);
      if (r) {
        this.stack.push(r);
        return;
      }
    }
    yield* this.pop(), yield* this.step();
  }
  *flowCollection(t) {
    const n = t.items[t.items.length - 1];
    if (this.type === "flow-error-end") {
      let r;
      do
        yield* this.pop(), r = this.peek(1);
      while ((r == null ? void 0 : r.type) === "flow-collection");
    } else if (t.end.length === 0) {
      switch (this.type) {
        case "comma":
        case "explicit-key-ind":
          !n || n.sep ? t.items.push({ start: [this.sourceToken] }) : n.start.push(this.sourceToken);
          return;
        case "map-value-ind":
          !n || n.value ? t.items.push({ start: [], key: null, sep: [this.sourceToken] }) : n.sep ? n.sep.push(this.sourceToken) : Object.assign(n, { key: null, sep: [this.sourceToken] });
          return;
        case "space":
        case "comment":
        case "newline":
        case "anchor":
        case "tag":
          !n || n.value ? t.items.push({ start: [this.sourceToken] }) : n.sep ? n.sep.push(this.sourceToken) : n.start.push(this.sourceToken);
          return;
        case "alias":
        case "scalar":
        case "single-quoted-scalar":
        case "double-quoted-scalar": {
          const s = this.flowScalar(this.type);
          !n || n.value ? t.items.push({ start: [], key: s, sep: [] }) : n.sep ? this.stack.push(s) : Object.assign(n, { key: s, sep: [] });
          return;
        }
        case "flow-map-end":
        case "flow-seq-end":
          t.end.push(this.sourceToken);
          return;
      }
      const r = this.startBlockValue(t);
      r ? this.stack.push(r) : (yield* this.pop(), yield* this.step());
    } else {
      const r = this.peek(2);
      if (r.type === "block-map" && (this.type === "map-value-ind" && r.indent === t.indent || this.type === "newline" && !r.items[r.items.length - 1].sep)) yield* this.pop(), yield* this.step();
      else if (this.type === "map-value-ind" && r.type !== "flow-collection") {
        const s = getPrevProps$1(r), o = getFirstKeyStartProps$1(s);
        fixFlowSeqItems$1(t);
        const a = t.end.splice(1, t.end.length);
        a.push(this.sourceToken);
        const l = { type: "block-map", offset: t.offset, indent: t.indent, items: [{ start: o, key: t, sep: a }] };
        this.onKeyLine = true, this.stack[this.stack.length - 1] = l;
      } else yield* this.lineEnd(t);
    }
  }
  flowScalar(t) {
    if (this.onNewLine) {
      let n = this.source.indexOf(`
`) + 1;
      for (; n !== 0; ) this.onNewLine(this.offset + n), n = this.source.indexOf(`
`, n) + 1;
    }
    return { type: t, offset: this.offset, indent: this.indent, source: this.source };
  }
  startBlockValue(t) {
    switch (this.type) {
      case "alias":
      case "scalar":
      case "single-quoted-scalar":
      case "double-quoted-scalar":
        return this.flowScalar(this.type);
      case "block-scalar-header":
        return { type: "block-scalar", offset: this.offset, indent: this.indent, props: [this.sourceToken], source: "" };
      case "flow-map-start":
      case "flow-seq-start":
        return { type: "flow-collection", offset: this.offset, indent: this.indent, start: this.sourceToken, items: [], end: [] };
      case "seq-item-ind":
        return { type: "block-seq", offset: this.offset, indent: this.indent, items: [{ start: [this.sourceToken] }] };
      case "explicit-key-ind": {
        this.onKeyLine = true;
        const n = getPrevProps$1(t), r = getFirstKeyStartProps$1(n);
        return r.push(this.sourceToken), { type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: r, explicitKey: true }] };
      }
      case "map-value-ind": {
        this.onKeyLine = true;
        const n = getPrevProps$1(t), r = getFirstKeyStartProps$1(n);
        return { type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: r, key: null, sep: [this.sourceToken] }] };
      }
    }
    return null;
  }
  atIndentedComment(t, n) {
    return this.type !== "comment" || this.indent <= n ? false : t.every((r) => r.type === "newline" || r.type === "space");
  }
  *documentEnd(t) {
    this.type !== "doc-mode" && (t.end ? t.end.push(this.sourceToken) : t.end = [this.sourceToken], this.type === "newline" && (yield* this.pop()));
  }
  *lineEnd(t) {
    switch (this.type) {
      case "comma":
      case "doc-start":
      case "doc-end":
      case "flow-seq-end":
      case "flow-map-end":
      case "map-value-ind":
        yield* this.pop(), yield* this.step();
        break;
      case "newline":
        this.onKeyLine = false;
      case "space":
      case "comment":
      default:
        t.end ? t.end.push(this.sourceToken) : t.end = [this.sourceToken], this.type === "newline" && (yield* this.pop());
    }
  }
};
parser$1$1.Parser = Parser$1;
var publicApi$1$1 = {}, composer$2 = composer$1$1, Document$5 = Document$4$1, errors$1$1 = errors$4$1, log$3 = log$2$1, identity$2$1 = identity$y$1, lineCounter$2 = lineCounter$1$1, parser$2 = parser$1$1;
function parseOptions$1(e5) {
  const t = e5.prettyErrors !== false;
  return { lineCounter: e5.lineCounter || t && new lineCounter$2.LineCounter() || null, prettyErrors: t };
}
function parseAllDocuments$1(e5, t = {}) {
  const { lineCounter: n, prettyErrors: r } = parseOptions$1(t), s = new parser$2.Parser(n == null ? void 0 : n.addNewLine), o = new composer$2.Composer(t), a = Array.from(o.compose(s.parse(e5)));
  if (r && n) for (const l of a) l.errors.forEach(errors$1$1.prettifyError(e5, n)), l.warnings.forEach(errors$1$1.prettifyError(e5, n));
  return a.length > 0 ? a : Object.assign([], { empty: true }, o.streamInfo());
}
function parseDocument$1(e5, t = {}) {
  const { lineCounter: n, prettyErrors: r } = parseOptions$1(t), s = new parser$2.Parser(n == null ? void 0 : n.addNewLine), o = new composer$2.Composer(t);
  let a = null;
  for (const l of o.compose(s.parse(e5), true, e5.length)) if (!a) a = l;
  else if (a.options.logLevel !== "silent") {
    a.errors.push(new errors$1$1.YAMLParseError(l.range.slice(0, 2), "MULTIPLE_DOCS", "Source contains multiple documents; please use YAML.parseAllDocuments()"));
    break;
  }
  return r && n && (a.errors.forEach(errors$1$1.prettifyError(e5, n)), a.warnings.forEach(errors$1$1.prettifyError(e5, n))), a;
}
function parse$2$1(e5, t, n) {
  let r;
  typeof t == "function" ? r = t : n === void 0 && t && typeof t == "object" && (n = t);
  const s = parseDocument$1(e5, n);
  if (!s) return null;
  if (s.warnings.forEach((o) => log$3.warn(s.options.logLevel, o)), s.errors.length > 0) {
    if (s.options.logLevel !== "silent") throw s.errors[0];
    s.errors = [];
  }
  return s.toJS(Object.assign({ reviver: r }, n));
}
function stringify$4$1(e5, t, n) {
  var _a4;
  let r = null;
  if (typeof t == "function" || Array.isArray(t) ? r = t : n === void 0 && t && (n = t), typeof n == "string" && (n = n.length), typeof n == "number") {
    const s = Math.round(n);
    n = s < 1 ? void 0 : s > 8 ? { indent: 8 } : { indent: s };
  }
  if (e5 === void 0) {
    const { keepUndefined: s } = (_a4 = n != null ? n : t) != null ? _a4 : {};
    if (!s) return;
  }
  return identity$2$1.isDocument(e5) && !r ? e5.toString(n) : new Document$5.Document(e5, r, n).toString(n);
}
publicApi$1$1.parse = parse$2$1;
publicApi$1$1.parseAllDocuments = parseAllDocuments$1;
publicApi$1$1.parseDocument = parseDocument$1;
publicApi$1$1.stringify = stringify$4$1;
var errors$5 = errors$4$1, identity$1$1 = identity$y$1, Pair$9 = Pair$8$1, Scalar$l = Scalar$k$1, YAMLMap$9 = YAMLMap$8$1, publicApi$2 = publicApi$1$1, visit$7 = visit$6$1;
errors$5.YAMLError;
errors$5.YAMLParseError;
errors$5.YAMLWarning;
identity$1$1.isAlias;
identity$1$1.isCollection;
identity$1$1.isDocument;
identity$1$1.isMap;
identity$1$1.isNode;
identity$1$1.isPair;
identity$1$1.isScalar;
identity$1$1.isSeq;
Pair$9.Pair;
Scalar$l.Scalar;
YAMLMap$9.YAMLMap;
var stringify$3$1 = publicApi$2.stringify;
visit$7.visit;
visit$7.visitAsync;
function assetToJsonApi$1(e5, t) {
  const { key: n, ...r } = e5, s = t == null ? void 0 : t.advertiseRelationships, o = (s == null ? void 0 : s.urls) || (s == null ? void 0 : s.variations) ? { ...s.urls ? { urls: { data: { type: "asset-url", id: n } } } : {}, ...s.variations ? { variations: { data: { type: "asset-variation", id: n } } } : {} } : void 0, a = { type: "asset", id: n, attributes: r };
  return (t == null ? void 0 : t.metadata) && (a.meta = t.metadata), o && (a.relationships = o), a;
}
function folderToJsonApi$1$1(e5) {
  const { key: t, ...n } = e5;
  return { type: "folder", id: t, attributes: n };
}
function resourceToJsonApi$1(e5, t) {
  return e5.type === "asset" ? assetToJsonApi$1(e5, t) : folderToJsonApi$1$1(e5);
}
function assetVariationsToJsonApi$1(e5) {
  const { key: t, ...n } = e5;
  return { type: "asset-variation", id: t, attributes: n };
}
function assetUrlToJsonApi$1(e5) {
  const { key: t, ...n } = e5;
  return { type: "asset-url", id: t, attributes: n };
}
function parseIncludeQuery$1(e5) {
  if (!e5) return { urls: false, variations: false };
  const t = e5.split(",").map((n) => n.trim().toLowerCase());
  return { urls: t.includes("urls"), variations: t.includes("variations") };
}
function parseMetaQuery$1(e5) {
  if (!e5) return { metadata: false };
  const t = e5.trim().toLowerCase();
  return { metadata: t === "true" || t === "1" || t === "yes" };
}
function parsePaginationQuery$1$1(e5) {
  return { limit: e5["page[limit]"] ? parseInt(e5["page[limit]"], 10) : void 0, cursor: e5["page[cursor]"], direction: e5["page[direction]"] === "backward" ? "backward" : "forward" };
}
function buildPaginationLinks$1$1(e5, t, n, r, s) {
  const o = new URL(e5), a = { self: e5, first: null, last: null, prev: null, next: null }, l = new URL(o);
  if (l.searchParams.delete("page[cursor]"), t.limit && l.searchParams.set("page[limit]", String(t.limit)), a.first = l.toString(), n && r) {
    const c = new URL(o);
    c.searchParams.set("page[cursor]", r), t.limit && c.searchParams.set("page[limit]", String(t.limit)), a.next = c.toString();
  }
  if (s) {
    const c = new URL(o);
    c.searchParams.set("page[cursor]", s), c.searchParams.set("page[direction]", "backward"), t.limit && c.searchParams.set("page[limit]", String(t.limit)), a.prev = c.toString();
  }
  return a;
}
const isAtLeast1$1 = cs$2((e5) => e5 >= 1 ? void 0 : "Must be at least 1"), isAtLeast0$1 = cs$2((e5) => e5 >= 0 ? void 0 : "Must be at least 0"), PaginationPageBasedSchema$1 = re$3(A$2({ page: Me$3.pipe($e$3(isAtLeast1$1)), perPage: V$4(Me$3.pipe($e$3(isAtLeast1$1))) })), PaginationBeforeSchema$1 = re$3(A$2({ before: V$4(h$4), perPage: V$4(Me$3.pipe($e$3(isAtLeast1$1))) })), PaginationAfterSchema$1 = re$3(A$2({ after: V$4(h$4), perPage: V$4(Me$3.pipe($e$3(isAtLeast1$1))) })), PaginationOffsetSchema$1 = re$3(A$2({ offset: Me$3.pipe($e$3(isAtLeast0$1)), limit: V$4(Me$3.pipe($e$3(isAtLeast1$1))) }));
re$3(be$3([PaginationPageBasedSchema$1, PaginationBeforeSchema$1, PaginationAfterSchema$1, PaginationOffsetSchema$1]));
function isPageBased$1(e5) {
  return "page" in e5 && typeof e5.page == "number";
}
function isOffsetBased$1(e5) {
  return "offset" in e5 && typeof e5.offset == "number";
}
function isAfterBased$1(e5) {
  return "after" in e5;
}
function isBeforeBased$1(e5) {
  return "before" in e5 && !("after" in e5);
}
function buildPaginationLinks$2(e5, t, n, r, s, o) {
  const a = { self: e5 };
  if (isAfterBased$1(t)) {
    const l = t.perPage;
    t.after && s && (a.prev = `${e5}?page[before]=${encodeURIComponent(s)}`, l && (a.prev += `&page[size]=${l}`)), n && o && (a.next = `${e5}?page[after]=${encodeURIComponent(o)}`, l && (a.next += `&page[size]=${l}`));
  } else if (isBeforeBased$1(t)) {
    const l = t.perPage;
    n && s && (a.prev = `${e5}?page[before]=${encodeURIComponent(s)}`, l && (a.prev += `&page[size]=${l}`)), t.before && o && (a.next = `${e5}?page[after]=${encodeURIComponent(o)}`, l && (a.next += `&page[size]=${l}`));
  } else if (isPageBased$1(t)) {
    const l = t.page, c = t.perPage || 10;
    a.first = `${e5}?page[number]=1&page[size]=${c}`, l > 1 && (a.prev = `${e5}?page[number]=${l - 1}&page[size]=${c}`), n && (a.next = `${e5}?page[number]=${l + 1}&page[size]=${c}`);
  } else if (isOffsetBased$1(t)) {
    const l = t.offset, c = t.limit || 10;
    if (a.first = `${e5}?page[offset]=0&page[limit]=${c}`, l > 0) {
      const u = Math.max(0, l - c);
      a.prev = `${e5}?page[offset]=${u}&page[limit]=${c}`;
    }
    n && (a.next = `${e5}?page[offset]=${l + c}&page[limit]=${c}`);
  }
  return a;
}
function parsePaginationQuery$2(e5) {
  const t = e5["page[after]"], n = e5["page[before]"], r = e5["page[number]"], s = e5["page[size]"], o = e5["page[offset]"], a = e5["page[limit]"];
  return t ? { after: Array.isArray(t) ? t[0] : t, perPage: s ? parseInt(Array.isArray(s) ? s[0] : s) : void 0 } : n ? { before: Array.isArray(n) ? n[0] : n, perPage: s ? parseInt(Array.isArray(s) ? s[0] : s) : void 0 } : r ? { page: parseInt(Array.isArray(r) ? r[0] : r), perPage: s ? parseInt(Array.isArray(s) ? s[0] : s) : void 0 } : o !== void 0 ? { offset: parseInt(Array.isArray(o) ? o[0] : o), limit: a ? parseInt(Array.isArray(a) ? a[0] : a) : void 0 } : { after: void 0, perPage: 10 };
}
const toLaikaError$2$1 = (e5) => e5 instanceof b$4 ? e5 : e5 instanceof Error ? new Ge$2(e5.message, { cause: e5 }) : new Ge$2(String(e5)), firstResultWithMetadata$1$1 = async (e5) => {
  try {
    const t = await xe$5(di$2(oc$1(e5)));
    return X$4(t) ? we$5(t.failure) : lt$2({ value: t.success.value, recoverableErrors: t.success.recoverableErrors });
  } catch (t) {
    return we$5(toLaikaError$2$1(t));
  }
}, firstResult$1$1 = async (e5) => {
  try {
    return await xe$5(di$2(us$2(e5)));
  } catch (t) {
    return we$5(toLaikaError$2$1(t));
  }
}, runStream$1$1 = async (e5) => {
  try {
    const t = await xe$5(di$2(fs$4(e5)));
    return X$4(t) ? we$5(t.failure) : lt$2(t.success.data);
  } catch (t) {
    return we$5(toLaikaError$2$1(t));
  }
}, runStreamWithDone$1$1 = async (e5) => {
  try {
    const t = await xe$5(di$2(fs$4(e5)));
    return X$4(t) ? we$5(t.failure) : lt$2({ data: t.success.data, done: t.success.done, recoverableErrors: t.success.recoverableErrors });
  } catch (t) {
    return we$5(toLaikaError$2$1(t));
  }
}, json$4$1 = (e5, t = 200, n = {}) => new Response(JSON.stringify(e5), { status: t, headers: { "Content-Type": "application/vnd.api+json", "Cache-Control": "no-store", ...n } });
function respondError$2$1(e5, t = 400) {
  return json$4$1({ errors: [{ status: String(t), code: e5.code, detail: e5.message }] }, t);
}
function respondValidationError$1(e5, t = 400) {
  return json$4$1({ errors: e5.map((n) => ({ status: String(t), code: "validation_error", detail: n.message })) }, t);
}
const assetsSelfPathFor$1 = (e5, t, n) => {
  const r = encodeURIComponent(n);
  switch (t) {
    case "asset":
    case "folder":
      return `${e5}/resources/${r}`;
    case "asset-url":
    case "asset-variation":
      return;
    case "assets-capabilities":
      return `${e5}/capabilities`;
    default:
      return;
  }
}, withAssetsSelfLink$1 = (e5, t) => {
  var _a4;
  const n = assetsSelfPathFor$1(t, e5.type, e5.id);
  return n ? { ...e5, links: { ...(_a4 = e5.links) != null ? _a4 : {}, self: n } } : e5;
};
function respondResource$1$1(e5, t, n = "", r) {
  const s = { data: withAssetsSelfLink$1(e5, n) };
  t && t.length > 0 && (s.included = t);
  const o = r ? Zm$1(r) : void 0;
  return o && (s.meta = { warnings: o }), json$4$1(s);
}
function respondCollection$1$1(e5, t, n, r, s = "") {
  const o = { data: e5.map((a) => withAssetsSelfLink$1(a, s)) };
  return t && t.length > 0 && (o.included = t), n && (o.links = n), r && (o.meta = r), json$4$1(o);
}
const JsonApiAssetCreateSchema$1 = re$3(A$2({ type: O$3("asset"), id: h$4, attributes: A$2({ mimeType: V$4(h$4), filename: V$4(h$4), cacheControl: V$4(h$4), customMetadata: V$4(Pn$2(h$4, h$4)), content: V$4(h$4) }) })), JsonApiAssetUpdateSchema$1 = re$3(A$2({ type: O$3("asset"), id: h$4, attributes: A$2({ mimeType: V$4(h$4), cacheControl: V$4(h$4), customMetadata: V$4(Pn$2(h$4, h$4)) }) })), JsonApiFolderCreateSchema$2$1 = re$3(A$2({ type: O$3("folder"), id: h$4, attributes: A$2({ type: V$4(O$3("folder")) }) }));
function buildAssetsApi$1(e5) {
  const { repository: t, basePath: n = "/api/assets" } = e5, r = pt$2(JsonApiAssetCreateSchema$1), s = pt$2(JsonApiAssetUpdateSchema$1), o = pt$2(JsonApiFolderCreateSchema$2$1);
  return { async fetch(a) {
    var _a4;
    const l = new URL(a.url), c = l.pathname, u = a.method.toUpperCase(), p = {};
    l.searchParams.forEach((g, d) => {
      p[d] = g;
    });
    const f = parseIncludeQuery$1(p.include), m = { metadata: parseMetaQuery$1(p.meta).metadata, urls: f.urls, variations: f.variations };
    if (c === `${n}/capabilities` && u === "GET") {
      const g = await firstResult$1$1(t.getCapabilities());
      return X$4(g) ? respondError$2$1(g.failure, T$2.INTERNAL_ERROR) : respondResource$1$1({ type: "assets-capabilities", id: "self", attributes: g.success, links: { self: `${n}/capabilities` } });
    }
    if (c === `${n}/resources` && u === "GET") {
      const g = p.folder || p["filter[prefix]"] || "", d = parsePaginationQuery$1$1(p), y = p["filter[depth]"] || p.depth, S = y ? Math.max(1, parseInt(y, 10) || 1) : 1, $ = { offset: 0, limit: d.limit || 100 }, E = [];
      let b = false, A;
      const w = await runStreamWithDone$1$1(t.listResources(g, { pagination: $, depth: S, hints: m }));
      if (X$4(w)) return respondError$2$1(w.failure, T$2.BAD_REQUEST);
      const v = w.success.data, C = w.success.done, T = v.filter((k) => k.type === "asset"), O = /* @__PURE__ */ new Map();
      if (m.metadata) {
        const k = await runStream$1$1(t.getMetadata(T));
        if (jt$2(k)) for (const R of k.success) O.set(R.key, R.metadata);
      }
      if (m.urls) {
        const k = await runStream$1$1(t.getUrls(T));
        if (jt$2(k)) for (const R of k.success) E.push(assetUrlToJsonApi$1(R));
      }
      if (m.variations) {
        const k = await runStream$1$1(t.getVariations(T));
        if (jt$2(k)) for (const R of k.success) E.push(assetVariationsToJsonApi$1(R));
      }
      const L = v.map((k) => resourceToJsonApi$1(k, { metadata: k.type === "asset" ? O.get(k.key) : void 0, advertiseRelationships: { urls: m.urls, variations: m.variations } }));
      v.length >= $.limit && (b = true, A = (_a4 = v[v.length - 1]) == null ? void 0 : _a4.key);
      const _ = buildPaginationLinks$1$1(l.toString(), d, b, A, d.cursor), N = Zm$1(w.success.recoverableErrors), x = typeof C.total == "number" ? { total: C.total } : void 0, M = x || N ? { ...x ? { page: x } : {}, ...N ? { warnings: N } : {} } : void 0;
      return respondCollection$1$1(L, E, _, M, n);
    }
    if (c.startsWith(`${n}/resources/`) && u === "GET") {
      const g = decodeURIComponent(c.slice(`${n}/resources/`.length)), d = await firstResultWithMetadata$1$1(t.getResource(g, { hints: m }));
      if (X$4(d)) return respondError$2$1(d.failure, T$2.NOT_FOUND);
      const y = d.success.value[0];
      if (!y) return respondError$2$1(new zi$2("Resource not found"), T$2.NOT_FOUND);
      const S = [];
      let $;
      if (y.type === "asset") {
        if (m.metadata) {
          const b = await runStream$1$1(t.getMetadata([y]));
          jt$2(b) && b.success[0] && ($ = b.success[0].metadata);
        }
        if (m.urls) {
          const b = await runStream$1$1(t.getUrls([y]));
          jt$2(b) && b.success[0] && S.push(assetUrlToJsonApi$1(b.success[0]));
        }
        if (m.variations) {
          const b = await runStream$1$1(t.getVariations([y]));
          jt$2(b) && b.success[0] && S.push(assetVariationsToJsonApi$1(b.success[0]));
        }
      }
      const E = resourceToJsonApi$1(y, { metadata: $, advertiseRelationships: { urls: m.urls, variations: m.variations } });
      return respondResource$1$1(E, S, n, d.success.recoverableErrors);
    }
    if (c === `${n}/resources` && u === "POST") {
      const g = a.headers.get("Content-Type") || "";
      if (g.includes("multipart/form-data")) {
        const d = await a.formData(), y = d.get("file"), S = d.get("metadata"), $ = d.get("key"), E = d.get("mimeType"), b = d.get("filename"), A = d.get("cacheControl"), w = d.get("customMetadata");
        if (!y) return respondError$2$1(new zi$2("Missing file in multipart form data"), T$2.BAD_REQUEST);
        let v;
        if (S) try {
          v = JSON.parse(S);
        } catch {
          return respondError$2$1(new zi$2("Invalid metadata JSON"), T$2.BAD_REQUEST);
        }
        let C = v == null ? void 0 : v.customMetadata;
        if (w) try {
          C = JSON.parse(w);
        } catch {
        }
        const T = $ || (v == null ? void 0 : v.key) || y.name, O = E || (v == null ? void 0 : v.mimeType) || y.type || "application/octet-stream", L = b || (v == null ? void 0 : v.filename) || y.name, _ = A || (v == null ? void 0 : v.cacheControl), N = await y.arrayBuffer(), x = await firstResultWithMetadata$1$1(t.createAsset({ key: T, mimeType: O, content: N, filename: L, customMetadata: C, cacheControl: _ }));
        return X$4(x) ? respondError$2$1(x.failure, T$2.BAD_REQUEST) : respondResource$1$1(assetToJsonApi$1(x.success.value), void 0, n, x.success.recoverableErrors);
      }
      if (g.includes("application/vnd.api+json") || g.includes("application/json")) {
        const y = (await a.json()).data;
        if (y.type === "folder") {
          let S;
          try {
            S = o(y);
          } catch {
            return respondValidationError$1([{ message: "Invalid folder create data" }], T$2.BAD_REQUEST);
          }
          const $ = { key: S.id, type: "folder" }, E = await firstResultWithMetadata$1$1(t.createFolder($));
          return X$4(E) ? respondError$2$1(E.failure, T$2.BAD_REQUEST) : respondResource$1$1(folderToJsonApi$1$1(E.success.value), void 0, n, E.success.recoverableErrors);
        }
        if (y.type === "asset") {
          let S;
          try {
            S = r(y);
          } catch {
            return respondValidationError$1([{ message: "Invalid asset create data" }], T$2.BAD_REQUEST);
          }
          const $ = S.attributes.content;
          if (!$) return respondError$2$1(new zi$2("Missing content in asset creation. Use multipart/form-data for binary uploads."), T$2.BAD_REQUEST);
          const E = atob($), b = new Uint8Array(E.length);
          for (let v = 0; v < E.length; v++) b[v] = E.charCodeAt(v);
          const A = { key: S.id, mimeType: S.attributes.mimeType || "application/octet-stream", filename: S.attributes.filename, cacheControl: S.attributes.cacheControl, customMetadata: S.attributes.customMetadata, content: b.buffer }, w = await firstResultWithMetadata$1$1(t.createAsset(A));
          return X$4(w) ? respondError$2$1(w.failure, T$2.BAD_REQUEST) : respondResource$1$1(assetToJsonApi$1(w.success.value), void 0, n, w.success.recoverableErrors);
        }
        return respondError$2$1(new zi$2('Invalid resource type. Must be "asset" or "folder".'), T$2.BAD_REQUEST);
      }
      return respondError$2$1(new zi$2("Unsupported Content-Type. Use multipart/form-data or application/vnd.api+json."), T$2.BAD_REQUEST);
    }
    if (c.startsWith(`${n}/resources/`) && u === "PATCH") {
      const g = decodeURIComponent(c.slice(`${n}/resources/`.length)), y = { ...(await a.json()).data, id: g };
      let S;
      try {
        S = s(y);
      } catch {
        return respondValidationError$1([{ message: "Invalid asset update data" }], T$2.BAD_REQUEST);
      }
      const $ = { key: S.id, mimeType: S.attributes.mimeType, cacheControl: S.attributes.cacheControl, customMetadata: S.attributes.customMetadata }, E = await firstResultWithMetadata$1$1(t.updateAsset($));
      return X$4(E) ? respondError$2$1(E.failure, T$2.BAD_REQUEST) : respondResource$1$1(assetToJsonApi$1(E.success.value), void 0, n, E.success.recoverableErrors);
    }
    if (c.startsWith(`${n}/resources/`) && u === "DELETE") {
      const g = decodeURIComponent(c.slice(`${n}/resources/`.length)), d = p.recursive === "true", y = await firstResult$1$1(t.getResource(g));
      if (X$4(y)) return respondError$2$1(y.failure, T$2.NOT_FOUND);
      const S = y.success[0];
      if (!S) return respondError$2$1(new zi$2("Resource not found"), T$2.NOT_FOUND);
      const E = S.type === "folder" ? t.deleteFolder(g, d) : t.deleteAsset(g), b = await firstResultWithMetadata$1$1(E);
      if (X$4(b)) return respondError$2$1(b.failure, T$2.BAD_REQUEST);
      const A = Zm$1(b.success.recoverableErrors);
      return A ? json$4$1({ meta: { deleted: true, warnings: A } }) : new Response(null, { status: 204 });
    }
    return json$4$1({ errors: [{ status: "404", code: "not_found", detail: `Route not found: ${u} ${c}` }] }, 404);
  } };
}
const TIMING_CONSTANTS$1 = { DEFAULT_JITTER_MS: 50 };
async function addTimingJitter$1(e5 = TIMING_CONSTANTS$1.DEFAULT_JITTER_MS) {
  const t = crypto.getRandomValues(new Uint8Array(1))[0] % e5;
  await new Promise((n) => setTimeout(n, t));
}
const json$3$1 = (e5, t = 200) => new Response(JSON.stringify(e5), { status: t, headers: { "Content-Type": "application/vnd.api+json", "Cache-Control": "no-store" } });
function respondError$1$1(e5, t = 400) {
  if (jt$2(e5)) throw new Error("respondError called with success result");
  return json$3$1(uc$1(e5), t);
}
const docsSelfPathFor$1 = (e5, t) => {
  const n = encodeURIComponent(t);
  switch (e5) {
    case "published":
    case "published-summary":
      return `/published/${n}`;
    case "unpublished":
    case "unpublished-summary":
      return `/unpublished/${n}`;
    case "revision":
    case "revision-summary":
      return `/revisions/${n}`;
    case "documents-capabilities":
      return "/capabilities";
    default:
      return;
  }
}, withDocsSelfLink$1 = (e5, t) => {
  var _a4;
  const n = docsSelfPathFor$1(e5.type, e5.id);
  return n ? { ...e5, links: { ...(_a4 = e5.links) != null ? _a4 : {}, self: `${t}${n}` } } : e5;
};
function respondResource$2(e5, t, n, r) {
  var _a4;
  if (X$4(e5)) {
    const o = e5.failure.code === Mi$2.CODE || ((_a4 = e5.failure.message) == null ? void 0 : _a4.toLowerCase().includes("not found"));
    return respondError$1$1(e5, o ? 404 : 400);
  }
  const s = r ? Zm$1(r) : void 0;
  return json$3$1({ data: withDocsSelfLink$1(t(e5.success), n), ...s ? { meta: { warnings: s } } : {} });
}
async function respondResourceWithWarnings$1(e5, t, n) {
  const r = await firstResultWithMetadata$2(e5);
  return X$4(r) ? respondResource$2(we$5(r.failure), t, n) : respondResource$2(lt$2(r.success.value), t, n, r.success.recoverableErrors);
}
function respondVoid$1(e5, t) {
  if (X$4(e5)) return respondError$1$1(e5);
  const n = t ? Zm$1(t) : void 0;
  return json$3$1({ meta: { deleted: true, ...n ? { warnings: n } : {} } });
}
async function respondVoidWithWarnings$1(e5) {
  const t = await firstResultWithMetadata$2(e5);
  return X$4(t) ? respondVoid$1(we$5(t.failure)) : respondVoid$1(lt$2(void 0), t.success.recoverableErrors);
}
async function respondCollection$2(e5, t, n, r, s, o, a) {
  var _a4;
  const l = new URL(e5.url), c = Object.fromEntries(l.searchParams.entries()), u = parsePaginationQuery$2(c), p = t[t.length - 1];
  (_a4 = p == null ? void 0 : p.id) != null ? _a4 : p == null ? void 0 : p.key;
  const f = "limit" in u ? u.limit : "perPage" in u ? u.perPage : void 0, h = typeof f == "number" ? t.length === f : false, m = buildPaginationLinks$2(r, u, h), g = a ? Zm$1(a) : void 0, d = typeof (o == null ? void 0 : o.total) == "number" ? { total: o.total } : void 0, y = d || g ? { ...d ? { page: d } : {}, ...g ? { warnings: g } : {} } : void 0, S = { data: t.map(($) => withDocsSelfLink$1(n($), s)), links: m, ...y ? { meta: y } : {} };
  return json$3$1(S);
}
function toLaikaError$1$1(e5) {
  return e5 instanceof b$4 ? e5 : e5 instanceof Error ? new Ge$2(e5.message, { cause: e5 }) : new Ge$2(String(e5));
}
async function firstResult$3(e5) {
  try {
    return await xe$5(di$2(us$2(e5)));
  } catch (t) {
    return we$5(toLaikaError$1$1(t));
  }
}
async function firstResultWithMetadata$2(e5) {
  try {
    const t = await xe$5(di$2(oc$1(e5)));
    return X$4(t) ? we$5(t.failure) : lt$2({ value: t.success.value, recoverableErrors: t.success.recoverableErrors });
  } catch (t) {
    return we$5(toLaikaError$1$1(t));
  }
}
async function runStreamWithDone$2(e5) {
  try {
    const t = await xe$5(di$2(fs$4(e5)));
    return X$4(t) ? we$5(t.failure) : lt$2({ data: t.success.data, done: t.success.done, recoverableErrors: t.success.recoverableErrors });
  } catch (t) {
    return we$5(toLaikaError$1$1(t));
  }
}
const RecordsQuerySchema$1 = re$3(A$2({ "filter[type]": V$4(be$3([O$3("published"), O$3("unpublished"), O$3("all")])), "filter[folder]": V$4(h$4), "filter[depth]": V$4(Ym$1.pipe($e$3(cs$2((e5) => e5 >= 1 ? void 0 : "Depth must be at least 1")))) })), DocumentCreateBodySchema$1 = re$3(A$2({ data: A$2({ type: O$3("published"), id: V$4(h$4), attributes: Pn$2(h$4, Bm$1) }) })), UnpublishedCreateBodySchema$1 = re$3(A$2({ data: A$2({ type: O$3("unpublished"), id: V$4(h$4), attributes: Pn$2(h$4, Bm$1) }) })), UnpublishedUpdateBodySchema$1 = re$3(A$2({ data: A$2({ type: O$3("unpublished"), id: h$4, attributes: Pn$2(h$4, Bm$1) }) })), UnpublishBodySchema$1 = re$3(A$2({ data: A$2({ type: O$3("unpublished"), attributes: A$2({ status: h$4 }) }) })), RevisionCreateBodySchema$1 = re$3(A$2({ data: A$2({ type: O$3("revision"), id: V$4(h$4), attributes: Pn$2(h$4, Bm$1) }) })), RefSchema$1 = re$3(A$2({ id: h$4, type: be$3([O$3("document"), O$3("unpublished"), O$3("revision")]) })), AddUnpublishedOpSchema$1 = re$3(A$2({ op: O$3("add"), data: A$2({ type: O$3("unpublished"), id: V$4(h$4), attributes: Pn$2(h$4, Bm$1) }) })), AddDocumentOpSchema$1 = re$3(A$2({ op: O$3("add"), data: A$2({ type: O$3("published"), id: V$4(h$4), attributes: Pn$2(h$4, Bm$1) }) })), StateTransitionOpSchema$1 = re$3(A$2({ op: O$3("update"), href: be$3([O$3("/publish"), O$3("/unpublish")]), ref: RefSchema$1, data: V$4(A$2({ type: O$3("unpublished"), attributes: A$2({ status: h$4 }) })) })), UpdateUnpublishedOpSchema$1 = re$3(A$2({ op: O$3("update"), data: A$2({ type: O$3("unpublished"), id: h$4, attributes: Pn$2(h$4, Bm$1) }) })), RemoveOpSchema$1 = re$3(A$2({ op: O$3("remove"), ref: RefSchema$1 })), AtomicOperationSchema$1$1 = be$3([AddUnpublishedOpSchema$1, AddDocumentOpSchema$1, StateTransitionOpSchema$1, UpdateUnpublishedOpSchema$1, RemoveOpSchema$1]), OperationsSchema$1 = re$3(A$2({ "atomic:operations": dh$1(AtomicOperationSchema$1$1) })), decodeRecordsQuery$1 = pt$2(RecordsQuerySchema$1), decodeDocumentCreateBody$1 = pt$2(DocumentCreateBodySchema$1), decodeUnpublishedCreateBody$1 = pt$2(UnpublishedCreateBodySchema$1), decodeUnpublishedUpdateBody$1 = pt$2(UnpublishedUpdateBodySchema$1), decodeUnpublishBody$1 = pt$2(UnpublishBodySchema$1), decodeRevisionCreateBody$1 = pt$2(RevisionCreateBodySchema$1), decodeOperations$1 = pt$2(OperationsSchema$1);
function buildJsonApi$1$1(e5) {
  const { repo: t, basePath: n = "" } = e5;
  return { async fetch(r) {
    var _a4, _b, _c2, _d;
    const s = new URL(r.url);
    let o = s.pathname.substring(n.length);
    if (o.startsWith("/") && (o = o.substring(1)), o.endsWith("/") && (o = o.slice(0, -1)), o === "" && r.method === "GET") return json$3$1({ data: { type: "api-info", id: "documents", attributes: { name: "Documents API", version: "1.0.0", endpoints: [{ path: "/capabilities", methods: ["GET"], description: "Underlying documents repository capabilities" }, { path: "/records", methods: ["GET"], description: "List full records (published + unpublished view per key)" }, { path: "/record-summaries", methods: ["GET"], description: "List record summaries (lightweight listing)" }, { path: "/published/{key}", methods: ["GET", "POST", "PATCH", "DELETE"], description: "Read, create, update, or remove a published document" }, { path: "/published/{key}/unpublish", methods: ["POST"], description: "State transition: move a published document to unpublished" }, { path: "/unpublished/{key}", methods: ["GET", "POST", "PATCH", "DELETE"], description: "Read, create, update, or remove an unpublished draft" }, { path: "/unpublished/{key}/publish", methods: ["POST"], description: "State transition: publish an unpublished draft" }, { path: "/revisions", methods: ["POST"], description: "Create a revision for a document" }, { path: "/revisions/{key}", methods: ["GET"], description: "List revisions for a document" }, { path: "/revisions/{key}/{revisionId}", methods: ["GET"], description: "Read a specific revision of a document" }, { path: "/operations", methods: ["POST"], description: "Atomic operations (add/update/remove + publish/unpublish transitions)" }] } } });
    const a = o.split("/"), l = a[0], c = a[1] ? decodeURIComponent(a[1]) : void 0, u = a[2], p = Object.fromEntries(s.searchParams.entries()), f = async () => {
      var _a5, _b2, _c3;
      const g = decodeRecordsQuery$1(p), d = g["filter[type]"] === "all" ? void 0 : (_a5 = g["filter[type]"]) != null ? _a5 : "published", y = (_b2 = g["filter[folder]"]) != null ? _b2 : "", S = (_c3 = g["filter[depth]"]) != null ? _c3 : 1, $ = await runStreamWithDone$2(t.listRecords({ pagination: parsePaginationQuery$2(p), folder: y, type: d, depth: S }));
      if (X$4($)) return respondError$1$1($);
      const b = $.success.data.map((A) => {
        switch (A.type) {
          case "published":
            return eg$1(A);
          case "unpublished":
            return ag$1(A);
          default:
            throw new Error(`Unknown entry type: ${A.type}`);
        }
      });
      return respondCollection$2(r, b, (A) => A, r.url, n, { total: $.success.done.total }, $.success.recoverableErrors);
    }, h = async () => {
      var _a5, _b2, _c3;
      const g = decodeRecordsQuery$1(p), d = g["filter[type]"] === "all" ? void 0 : (_a5 = g["filter[type]"]) != null ? _a5 : "published", y = (_b2 = g["filter[folder]"]) != null ? _b2 : "", S = (_c3 = g["filter[depth]"]) != null ? _c3 : 1, $ = await runStreamWithDone$2(t.listRecordSummaries({ pagination: parsePaginationQuery$2(p), folder: y, type: d, depth: S }));
      if (X$4($)) return respondError$1$1($);
      const b = $.success.data.map((A) => {
        switch (A.type) {
          case "published":
            return og$1({ ...A, type: "published-summary" });
          case "unpublished":
            return pg$1({ ...A, type: "unpublished-summary" });
          default:
            throw new Error(`Unknown entry type: ${A.type}`);
        }
      });
      return respondCollection$2(r, b, (A) => A, r.url, n, { total: $.success.done.total }, $.success.recoverableErrors);
    };
    if (l === "capabilities" && r.method === "GET") {
      const g = await firstResult$3(t.getCapabilities());
      return X$4(g) ? respondError$1$1(g) : json$3$1({ data: { type: "documents-capabilities", id: "self", attributes: g.success, links: { self: `${n}/capabilities` } } });
    }
    if (l === "records" && r.method === "GET") return f();
    if (l === "record-summaries" && r.method === "GET") return h();
    if (l === "published" && r.method === "GET" && c) return respondResourceWithWarnings$1(t.getDocument(c), eg$1, n);
    if (l === "published" && u === "unpublish" && r.method === "POST" && c) {
      const g = await r.json(), { data: d } = decodeUnpublishBody$1(g);
      return respondResourceWithWarnings$1(t.unpublish(c, d.attributes.status), ag$1, n);
    }
    if (l === "published" && r.method === "POST") {
      const g = await r.json(), { data: d } = decodeDocumentCreateBody$1(g), y = rg$1({ id: (_a4 = d.id) != null ? _a4 : "", attributes: d.attributes });
      return respondResourceWithWarnings$1(t.createDocument(y), eg$1, n);
    }
    if (l === "published" && r.method === "PATCH" && c) {
      const g = await r.json(), { data: d } = decodeDocumentCreateBody$1(g), y = { key: c, ...d.attributes };
      return respondResourceWithWarnings$1(t.updateDocument(y), eg$1, n);
    }
    if (l === "published" && r.method === "DELETE" && c) return respondVoidWithWarnings$1(t.deleteDocument(c));
    if (l === "unpublished" && r.method === "GET" && c) return respondResourceWithWarnings$1(t.getUnpublished(c), ag$1, n);
    if (l === "unpublished" && u === "publish" && r.method === "POST" && c) return respondResourceWithWarnings$1(t.publish(c), eg$1, n);
    if (l === "unpublished" && r.method === "POST") {
      const g = await r.json(), { data: d } = decodeUnpublishedCreateBody$1(g), y = lg$1({ id: (_b = d.id) != null ? _b : "", attributes: d.attributes });
      return respondResourceWithWarnings$1(t.createUnpublished(y), ag$1, n);
    }
    if (l === "unpublished" && r.method === "PATCH" && c) {
      const g = await r.json(), { data: d } = decodeUnpublishedUpdateBody$1(g), y = dg$1({ id: d.id, attributes: d.attributes });
      return respondResourceWithWarnings$1(t.updateUnpublished({ ...y, key: c }), ag$1, n);
    }
    if (l === "unpublished" && r.method === "DELETE" && c) return respondVoidWithWarnings$1(t.deleteUnpublished(c));
    if (l === "revisions" && r.method === "POST") {
      const g = await r.json(), { data: d } = decodeRevisionCreateBody$1(g), y = Eg$1({ id: (_c2 = d.id) != null ? _c2 : "", attributes: d.attributes });
      return respondResourceWithWarnings$1(t.createRevision(y), mg$1, n);
    }
    if (l === "revisions" && r.method === "GET" && c && !u) {
      const g = await runStreamWithDone$2(t.listRevisions(c, { pagination: parsePaginationQuery$2(p) }));
      return X$4(g) ? respondError$1$1(g) : respondCollection$2(r, g.success.data, bg$1, r.url, n, { total: g.success.done.total }, g.success.recoverableErrors);
    }
    if (l === "revisions" && r.method === "GET" && c && u) return respondResourceWithWarnings$1(t.getRevision(c, u), mg$1, n);
    if (l === "operations" && r.method === "POST") {
      const g = await r.json(), y = decodeOperations$1(g)["atomic:operations"].map(async (E) => {
        var _a5, _b2, _c3;
        let b, A = null;
        if (E.op === "add") {
          if ("data" in E && E.data.type === "unpublished") {
            const w = E, v = lg$1({ id: (_a5 = w.data.id) != null ? _a5 : "", attributes: w.data.attributes });
            b = await firstResultWithMetadata$2(t.createUnpublished(v)), A = ag$1;
          } else if ("data" in E && E.data.type === "published") {
            const w = E, v = rg$1({ id: (_b2 = w.data.id) != null ? _b2 : "", attributes: w.data.attributes });
            b = await firstResultWithMetadata$2(t.createDocument(v)), A = eg$1;
          } else b = we$5(new zi$2(`Cannot add type: ${(_c3 = E.data) == null ? void 0 : _c3.type}`));
          return { op: b, operation: E, transformer: A };
        }
        if (E.op === "update") {
          if ("href" in E && "ref" in E) {
            const { href: w, ref: v, data: C } = E;
            switch (w) {
              case "/publish":
                v.type === "unpublished" ? (b = await firstResultWithMetadata$2(t.publish(v.id)), A = eg$1) : b = we$5(new zi$2(`Cannot publish ${v.type}`));
                break;
              case "/unpublish":
                if (v.type === "document") {
                  if (!C) {
                    b = we$5(new zi$2("Missing data for unpublish operation"));
                    break;
                  }
                  b = await firstResultWithMetadata$2(t.unpublish(v.id, C.attributes.status)), A = ag$1;
                } else b = we$5(new zi$2(`Cannot unpublish ${v.type}`));
                break;
              default:
                b = we$5(new zi$2(`Unknown action: ${w}`));
            }
          } else if ("data" in E) {
            const w = E, v = dg$1({ id: w.data.id, attributes: w.data.attributes });
            b = await firstResultWithMetadata$2(t.updateUnpublished(v)), A = ag$1;
          } else b = we$5(new zi$2("Invalid update operation"));
          return { op: b, operation: E, transformer: A };
        }
        if (E.op === "remove") {
          const { ref: w } = E;
          return w.type === "document" ? (b = await firstResultWithMetadata$2(t.deleteDocument(w.id)), A = null) : w.type === "unpublished" ? (b = await firstResultWithMetadata$2(t.deleteUnpublished(w.id)), A = null) : b = we$5(new zi$2(`Cannot remove ${w.type}`)), { op: b, operation: E, transformer: A };
        }
        return { op: we$5(new zi$2(`Unsupported operation: ${E.op}`)), operation: E, transformer: null };
      }), $ = (await Promise.allSettled(y)).map((E) => {
        if (E.status === "rejected") return { errors: [{ status: "500", title: "Operation Failed", detail: E.reason.message }] };
        const { op: b, operation: A, transformer: w } = E.value;
        if (X$4(b)) return { errors: [{ status: "400", title: "Operation Failed", detail: b.failure.message }] };
        if (A.op === "remove") {
          const v = A, C = b.success, T = C ? Zm$1(C.recoverableErrors) : void 0;
          return { meta: T ? { deleted: true, ref: v.ref, warnings: T } : { deleted: true, ref: v.ref } };
        }
        if (w) {
          const v = b.success, C = Zm$1(v.recoverableErrors), T = w(v.value);
          return C ? { data: T, meta: { warnings: C } } : { data: T };
        }
        return { data: null };
      });
      return json$3$1({ "atomic:results": $ });
    }
    (_d = e5.logger) == null ? void 0 : _d.debug("Documents endpoint not found:", o);
    const m = new Mi$2("Endpoint not found");
    return respondError$1$1(we$5(m), 404);
  } };
}
function storageObjectToJsonApi$1(e5) {
  const { key: t, metadata: n, ...r } = e5;
  return { type: "object", id: t, attributes: r, ...n ? { meta: n } : {} };
}
function folderToJsonApi$2(e5) {
  const { key: t, ...n } = e5;
  return { type: "folder", id: t, attributes: n };
}
function storageObjectSummaryToJsonApi$1(e5) {
  const { key: t, ...n } = e5;
  return { type: "object-summary", id: t, attributes: n };
}
function folderSummaryToJsonApi$1(e5) {
  const { key: t, ...n } = e5;
  return { type: "folder-summary", id: t, attributes: n };
}
function atomToJsonApi$1(e5) {
  return e5.type === "object" ? storageObjectToJsonApi$1(e5) : folderToJsonApi$2(e5);
}
function atomSummaryToJsonApi$1(e5) {
  return e5.type === "object-summary" ? storageObjectSummaryToJsonApi$1(e5) : folderSummaryToJsonApi$1(e5);
}
function withSelfLink$1(e5, t) {
  var _a4;
  const n = selfPathFor$1(e5.type, e5.id);
  return n ? { ...e5, links: { ...(_a4 = e5.links) != null ? _a4 : {}, self: `${t}${n}` } } : e5;
}
const selfPathFor$1 = (e5, t) => {
  const n = encodeURIComponent(t);
  switch (e5) {
    case "object":
    case "object-summary":
      return `/objects/${n}`;
    case "folder":
    case "folder-summary":
      return `/folders/${n}`;
    case "storage-capabilities":
      return "/capabilities";
    default:
      return;
  }
}, JsonApiStorageObjectSchema$1 = re$3(A$2({ type: O$3("object"), id: h$4, attributes: Pn$2(h$4, is$2), meta: V$4(Pn$2(h$4, is$2)) })), JsonApiStorageObjectCreateSchema$1$1 = re$3(A$2({ type: O$3("object"), id: h$4, attributes: Pn$2(h$4, is$2), meta: V$4(Pn$2(h$4, is$2)) })), JsonApiStorageObjectUpdateSchema$1$1 = re$3(A$2({ type: O$3("object"), id: h$4, attributes: Pn$2(h$4, is$2), meta: V$4(Pn$2(h$4, is$2)) })), JsonApiFolderSchema$1 = re$3(A$2({ type: O$3("folder"), id: h$4, attributes: Pn$2(h$4, is$2) })), JsonApiFolderCreateSchema$1$1 = re$3(A$2({ type: O$3("folder"), id: h$4, attributes: Pn$2(h$4, is$2) })), JsonApiStorageObjectSummarySchema$1 = re$3(A$2({ type: O$3("object-summary"), id: h$4, attributes: Pn$2(h$4, is$2) })), JsonApiFolderSummarySchema$1 = re$3(A$2({ type: O$3("folder-summary"), id: h$4, attributes: Pn$2(h$4, is$2) })), JsonApiAtomSchema$1 = be$3([JsonApiStorageObjectSchema$1, JsonApiFolderSchema$1]), JsonApiAtomSummarySchema$1 = be$3([JsonApiStorageObjectSummarySchema$1, JsonApiFolderSummarySchema$1]);
pt$2(JsonApiStorageObjectSchema$1);
pt$2(JsonApiStorageObjectCreateSchema$1$1);
pt$2(JsonApiStorageObjectUpdateSchema$1$1);
pt$2(JsonApiFolderSchema$1);
pt$2(JsonApiFolderCreateSchema$1$1);
pt$2(JsonApiAtomSchema$1);
pt$2(JsonApiAtomSummarySchema$1);
const json$2$1 = (e5, t = 200) => new Response(JSON.stringify(e5), { status: t, headers: { "Content-Type": "application/vnd.api+json", "Cache-Control": "no-store" } }), safeDecode$1 = (e5) => {
  try {
    return decodeURIComponent(e5);
  } catch {
    return e5;
  }
};
function respondError$3(e5, t = 400) {
  if (jt$2(e5)) throw new Ge$2("respondError called with success result");
  return json$2$1(uc$1(e5), t);
}
function respondResourceWithConverter$1(e5, t, n, r) {
  if (X$4(e5)) return respondError$3(e5);
  const s = r ? Zm$1(r) : void 0;
  return json$2$1({ data: withSelfLink$1(t(e5.success), n), ...s ? { meta: { warnings: s } } : {} });
}
function respondCollectionWithConverter$1(e5, t, n, r, s, o, a) {
  const l = new URL(e5.url), c = Object.fromEntries(l.searchParams.entries()), u = parsePaginationQuery$2(c);
  let p, f;
  if (t.length > 0) {
    const E = t[0], b = t[t.length - 1];
    p = E.id || E.key, f = b.id || b.key;
  }
  const h = "limit" in u ? u.limit : "perPage" in u ? u.perPage : void 0, m = (o == null ? void 0 : o.pagination) !== void 0 || (typeof (o == null ? void 0 : o.total) == "number" && typeof h == "number" ? t.length === h : false), g = buildPaginationLinks$2(r, u, m, f, p, f), d = a ? Zm$1(a) : void 0, y = typeof (o == null ? void 0 : o.total) == "number" ? { total: o.total } : void 0, S = y || d ? { ...y ? { page: y } : {}, ...d ? { warnings: d } : {} } : void 0, $ = { data: t.map((E) => withSelfLink$1(n(E), s)), links: g, ...S ? { meta: S } : {} };
  return json$2$1($);
}
const toLaikaError$3 = (e5) => e5 instanceof b$4 ? e5 : e5 instanceof Error ? new Ge$2(e5.message, { cause: e5 }) : new Ge$2(String(e5)), runTask$2 = async (e5) => {
  try {
    return await xe$5(di$2(us$2(e5)));
  } catch (t) {
    return we$5(toLaikaError$3(t));
  }
}, runTaskWithMetadata$1 = async (e5) => {
  try {
    const t = await xe$5(di$2(oc$1(e5)));
    return X$4(t) ? we$5(t.failure) : lt$2({ value: t.success.value, recoverableErrors: t.success.recoverableErrors });
  } catch (t) {
    return we$5(toLaikaError$3(t));
  }
}, runStream$2 = async (e5) => {
  try {
    return await xe$5(di$2(fs$4(e5)));
  } catch (t) {
    return we$5(toLaikaError$3(t));
  }
}, JsonApiStorageObjectCreateSchema$2 = re$3(A$2({ type: O$3("object"), id: h$4, attributes: A$2({ type: V$4(O$3("object")), content: V$4(Pn$2(h$4, Bm$1)) }), meta: V$4(Pn$2(h$4, Bm$1)) })), JsonApiStorageObjectUpdateSchema$2 = re$3(A$2({ type: O$3("object"), id: h$4, attributes: A$2({ type: V$4(O$3("object")), content: V$4(Pn$2(h$4, Bm$1)) }), meta: V$4(Pn$2(h$4, Bm$1)) })), JsonApiFolderCreateSchema$3 = re$3(A$2({ type: O$3("folder"), id: h$4, attributes: A$2({ type: V$4(O$3("folder")) }) })), StorageObjectCreateBodySchema$1 = re$3(A$2({ data: JsonApiStorageObjectCreateSchema$2 })), StorageObjectUpdateBodySchema$1 = re$3(A$2({ data: JsonApiStorageObjectUpdateSchema$2 })), RemoveOperationSchema$1 = re$3(A$2({ op: O$3("remove"), ref: A$2({ type: be$3([O$3("object"), O$3("folder"), O$3("atom")]), id: h$4 }) })), AddOperationSchema$1 = re$3(A$2({ op: O$3("add"), data: be$3([JsonApiStorageObjectCreateSchema$2, JsonApiFolderCreateSchema$3]) })), UpdateOperationSchema$1 = re$3(A$2({ op: O$3("update"), data: JsonApiStorageObjectUpdateSchema$2 })), AtomicOperationSchema$2 = be$3([RemoveOperationSchema$1, AddOperationSchema$1, UpdateOperationSchema$1]), AtomicOperationsRequestSchema$1 = re$3(A$2({ "atomic:operations": dh$1(AtomicOperationSchema$2) }));
function buildJsonApi$2(e5) {
  const { repo: t, basePath: n = "" } = e5, r = pt$2(StorageObjectCreateBodySchema$1), s = pt$2(StorageObjectUpdateBodySchema$1), o = pt$2(AtomicOperationsRequestSchema$1);
  return { async fetch(a) {
    var _a4, _b, _c2, _d, _e2;
    let c = new URL(a.url).pathname.substring(n.length);
    if (c.startsWith("/") && (c = c.substring(1)), c.endsWith("/") && (c = c.slice(0, -1)), c === "" && a.method === "GET") return json$2$1({ data: { type: "api-info", id: "storage", attributes: { name: "Storage API", version: "1.0.0", endpoints: [{ path: "/capabilities", methods: ["GET"], description: "Underlying storage repository capabilities" }, { path: "/atoms/{key}", methods: ["GET"], description: "List atoms in a folder" }, { path: "/atom-summaries/{key}", methods: ["GET"], description: "List atom summaries (lightweight listing) in a folder" }, { path: "/objects/{key}", methods: ["GET", "POST", "PATCH"], description: "Read, create, or update storage objects" }, { path: "/folders/{key}", methods: ["GET"], description: "Read a folder" }, { path: "/operations", methods: ["POST"], description: "Atomic operations (add, update, remove)" }] } } });
    const [u, p] = c.split("/"), f = p === void 0 ? void 0 : safeDecode$1(p), h = async () => {
      const g = { depth: 1, pagination: { perPage: 10 } }, d = await runStream$2(t.listAtoms(f, g));
      if (X$4(d)) {
        const y = d.failure.code;
        return respondError$3(d, Dd$1[y] || 400);
      }
      return respondCollectionWithConverter$1(a, d.success.data, atomToJsonApi$1, a.url, n, d.success.done, d.success.recoverableErrors);
    }, m = async () => {
      const g = { depth: 1, pagination: { perPage: 10 } }, d = await runStream$2(t.listAtomSummaries(f, g));
      if (X$4(d)) {
        const y = d.failure.code;
        return respondError$3(d, Dd$1[y] || 400);
      }
      return respondCollectionWithConverter$1(a, d.success.data, atomSummaryToJsonApi$1, a.url, n, d.success.done, d.success.recoverableErrors);
    };
    if (u === "capabilities" && a.method === "GET") {
      const g = await runTask$2(t.getCapabilities());
      if (X$4(g)) {
        const d = (_a4 = Dd$1[g.failure.code]) != null ? _a4 : 500;
        return respondError$3(g, d);
      }
      return json$2$1({ data: { type: "storage-capabilities", id: "self", attributes: g.success, links: { self: `${n}/capabilities` } } });
    }
    if (u === "atoms" && a.method === "GET") return h();
    if (u === "atom-summaries" && a.method === "GET") return m();
    if (u === "objects" && a.method === "GET") {
      if (!f) return respondError$3(we$5(new Bi$2("Missing object key")), 400);
      const g = await runTaskWithMetadata$1(t.getObject(f));
      if (X$4(g)) {
        const d = (_b = Dd$1[g.failure.code]) != null ? _b : 400;
        return respondError$3(g, d);
      }
      return respondResourceWithConverter$1(lt$2(g.success.value), storageObjectToJsonApi$1, n, g.success.recoverableErrors);
    } else if (u === "folders" && a.method === "GET") {
      if (!f) return respondError$3(we$5(new Bi$2("Missing folder key")), 400);
      const g = await runTaskWithMetadata$1(t.getFolder(f));
      if (X$4(g)) {
        const d = (_c2 = Dd$1[g.failure.code]) != null ? _c2 : 400;
        return respondError$3(g, d);
      }
      return respondResourceWithConverter$1(lt$2(g.success.value), folderToJsonApi$2, n, g.success.recoverableErrors);
    } else if (u === "objects" && a.method === "POST") {
      let g;
      try {
        const S = await a.json();
        g = r(S);
      } catch {
        return respondError$3(we$5(new Bi$2("Invalid request body")), 400);
      }
      const d = { key: g.data.id, type: "object", content: g.data.attributes.content || {}, ...g.data.meta ? { metadata: g.data.meta } : {} }, y = await runTaskWithMetadata$1(t.createObject(d));
      return X$4(y) ? respondError$3(y) : respondResourceWithConverter$1(lt$2(y.success.value), storageObjectToJsonApi$1, n, y.success.recoverableErrors);
    }
    if (c.startsWith("objects") && a.method === "PATCH") {
      const [, g] = c.split("/"), d = g === void 0 ? void 0 : safeDecode$1(g);
      let y;
      try {
        const E = await a.json();
        y = s(E);
      } catch {
        return respondError$3(we$5(new Bi$2("Invalid request body")), 400);
      }
      if (y.data.id !== d) return respondError$3(we$5(new Bi$2("Key in URL does not match key in body")), Dd$1[Bi$2.CODE]);
      const S = { key: y.data.id, type: "object", content: y.data.attributes.content, ...y.data.meta ? { metadata: y.data.meta } : {} }, $ = await runTaskWithMetadata$1(t.updateObject(S));
      return X$4($) ? respondError$3($) : respondResourceWithConverter$1(lt$2($.success.value), storageObjectToJsonApi$1, n, $.success.recoverableErrors);
    } else if (c === "operations" && a.method === "POST") {
      let g;
      try {
        const A = await a.json();
        g = o(A);
      } catch {
        return respondError$3(we$5(new Bi$2("Invalid atomic operations request")), 400);
      }
      const d = [], y = (A) => new Promise((w, v) => d.push([A, w, v])), S = g["atomic:operations"].map((A) => {
        switch (A.op) {
          case "add":
            if (A.data.type === "object") {
              const w = { key: A.data.id, type: "object", content: A.data.attributes.content || {} };
              return runTaskWithMetadata$1(t.createObject(w)).then((v) => ({ op: v, operation: A }));
            } else if (A.data.type === "folder") {
              const w = { key: A.data.id, type: "folder" };
              return runTaskWithMetadata$1(t.createFolder(w)).then((v) => ({ op: v, operation: A }));
            }
            return Promise.resolve({ op: we$5(new Bi$2("Unsupported add type")), operation: A });
          case "update":
            if (A.data.type === "object") {
              const w = { key: A.data.id, type: "object", content: A.data.attributes.content };
              return runTaskWithMetadata$1(t.updateObject(w)).then((v) => ({ op: v, operation: A }));
            }
            return Promise.resolve({ op: we$5(new Bi$2("Unsupported update type")), operation: A });
          case "remove":
            return y(A.ref.id).then((w) => ({ op: w, operation: A }));
        }
      }), $ = await runStream$2(t.removeAtoms(d.map(([A]) => A)));
      if (X$4($)) return respondError$3($);
      for (const A of $.success.data) {
        const w = d.find(([v]) => v === A);
        if (w) {
          const [, v] = w;
          v(lt$2({ type: "atom", key: A }));
        }
      }
      for (const [A, w] of d) $.success.data.includes(A) || w(we$5((_d = $.success.recoverableErrors[0]) != null ? _d : new Mi$2(`Failed to remove ${A}`)));
      const E = await Promise.allSettled(S), b = [];
      for (const A of E) {
        if (A.status === "rejected") continue;
        const w = A.value, v = w.op;
        if (jt$2(v)) {
          if (w.operation.op === "add" || w.operation.op === "update") {
            const C = v.success, T = C.value, O = Zm$1(C.recoverableErrors);
            if (typeof T == "object" && T !== null && "type" in T) {
              const _ = T.type === "object" ? storageObjectToJsonApi$1(T) : folderToJsonApi$2(T);
              b.push(O ? { data: _, meta: { warnings: O } } : { data: _ });
            }
          } else if (w.operation.op === "remove") {
            const C = v.success;
            b.push({ meta: { deleted: true, ref: { type: C.type, id: C.key } } });
          }
        }
      }
      return json$2$1({ "atomic:results": b });
    } else return (_e2 = e5.logger) == null ? void 0 : _e2.debug("storage endpoint not found:", c), respondError$3(we$5(new Mi$2("Storage endpoint not found")), 404);
  } };
}
const SECURITY_DEFAULTS$1 = { MAX_TOKEN_LENGTH: 2048 };
function validateTokenInput$1(e5) {
  return !e5 || typeof e5 != "string" || e5.length > SECURITY_DEFAULTS$1.MAX_TOKEN_LENGTH ? null : e5;
}
const SECURITY_HEADERS$1 = { "Content-Type": "application/vnd.api+json", "X-Content-Type-Options": "nosniff", "Cache-Control": "no-store, no-cache, must-revalidate", Pragma: "no-cache" }, decapApi$1 = (e5) => {
  const { documents: t, storage: n, assets: r, authenticateAccessToken: s, authenticateApiToken: o, basePath: a } = e5, l = Ie$3.normalize(a != null ? a : ""), c = Nh$1.url`${l}/health`, u = Nh$1.url`${l}/storage`, p = Nh$1.url`${l}/documents`, f = Nh$1.url`${l}/assets`, h = Nh$1.url`${l}/session`, m = async (g) => {
    var _a4, _b, _c2;
    const d = g.headers.get("Authorization") || void 0, y = g.headers.get("X-API-Key") || void 0, S = d ? yr$1.ExtractAuthorizationApiKey(d) : void 0;
    new URL(g.url).searchParams.has("api_key") && ((_a4 = e5.logger) == null ? void 0 : _a4.warn("Rejecting api_key supplied via URL query string. Use the X-API-Key header or Authorization: ApiKey <key>."));
    try {
      const $ = y || S, E = $ ? validateTokenInput$1($) : null;
      if ($ && !E) throw new qi$2("Invalid API key format");
      if (E) {
        if (!o) throw (_b = e5.logger) == null ? void 0 : _b.error("API key authentication not configured"), new qi$2("API key authentication not configured");
        return await o(E);
      } else {
        const b = yr$1.ExtractAuthorizationBearerToken(d), A = validateTokenInput$1(b);
        if (!A) throw new qi$2("Invalid or missing authentication token");
        return await s(A);
      }
    } catch ($) {
      (_c2 = e5.logger) == null ? void 0 : _c2.error("Authentication failed:", $);
      const E = Xm$1($) ? $ : new qi$2("Authentication failed");
      return await addTimingJitter$1(), new Response(JSON.stringify(uc$1(E)), { status: 401, headers: SECURITY_HEADERS$1 });
    }
  };
  return { authenticateRequest: m, async fetch(g) {
    var _a4, _b, _c2;
    const d = new URL(g.url), y = Ie$3.normalize(d.pathname);
    if (y === c) return (_a4 = e5.logger) == null ? void 0 : _a4.debug("Health check endpoint"), new Response(JSON.stringify({ status: "ok", timestamp: (/* @__PURE__ */ new Date()).toISOString() }), { status: 200, headers: { ...SECURITY_HEADERS$1, "Content-Type": "application/json" } });
    const S = await m(g);
    if (S instanceof Response) return S;
    const $ = S;
    if (y.startsWith(h)) {
      (_b = e5.logger) == null ? void 0 : _b.debug("Session endpoint for user:", $.id);
      const { passwordHash: E, ...b } = $;
      return new Response(JSON.stringify({ data: { type: "session", id: $.id || $.email, attributes: { ...b } } }), { status: 200, headers: SECURITY_HEADERS$1 });
    } else return y.startsWith(u) ? buildJsonApi$2({ repo: n, basePath: `${l}/storage`, logger: e5.logger }).fetch(g) : y.startsWith(p) ? buildJsonApi$1$1({ repo: t, basePath: `${l}/documents`, logger: e5.logger }).fetch(g) : r && y.startsWith(f) ? buildAssetsApi$1({ repository: r, basePath: `${l}/assets` }).fetch(g) : ((_c2 = e5.logger) == null ? void 0 : _c2.debug("Endpoint not found:", y), new Response(JSON.stringify(uc$1(new Mi$2("Endpoint not found"))), { status: 404, headers: SECURITY_HEADERS$1 }));
  } };
}, balanced$1 = (e5, t, n) => {
  const r = e5 instanceof RegExp ? maybeMatch$1(e5, n) : e5, s = t instanceof RegExp ? maybeMatch$1(t, n) : t, o = r !== null && s != null && range$1(r, s, n);
  return o && { start: o[0], end: o[1], pre: n.slice(0, o[0]), body: n.slice(o[0] + r.length, o[1]), post: n.slice(o[1] + s.length) };
}, maybeMatch$1 = (e5, t) => {
  const n = t.match(e5);
  return n ? n[0] : null;
}, range$1 = (e5, t, n) => {
  let r, s, o, a, l, c = n.indexOf(e5), u = n.indexOf(t, c + 1), p = c;
  if (c >= 0 && u > 0) {
    if (e5 === t) return [c, u];
    for (r = [], o = n.length; p >= 0 && !l; ) {
      if (p === c) r.push(p), c = n.indexOf(e5, p + 1);
      else if (r.length === 1) {
        const f = r.pop();
        f !== void 0 && (l = [f, u]);
      } else s = r.pop(), s !== void 0 && s < o && (o = s, a = u), u = n.indexOf(t, p + 1);
      p = c < u && c >= 0 ? c : u;
    }
    r.length && a !== void 0 && (l = [o, a]);
  }
  return l;
}, escSlash$1 = "\0SLASH" + Math.random() + "\0", escOpen$1 = "\0OPEN" + Math.random() + "\0", escClose$1 = "\0CLOSE" + Math.random() + "\0", escComma$1 = "\0COMMA" + Math.random() + "\0", escPeriod$1 = "\0PERIOD" + Math.random() + "\0", escSlashPattern$1 = new RegExp(escSlash$1, "g"), escOpenPattern$1 = new RegExp(escOpen$1, "g"), escClosePattern$1 = new RegExp(escClose$1, "g"), escCommaPattern$1 = new RegExp(escComma$1, "g"), escPeriodPattern$1 = new RegExp(escPeriod$1, "g"), slashPattern$1 = /\\\\/g, openPattern$1 = /\\{/g, closePattern$1 = /\\}/g, commaPattern$1 = /\\,/g, periodPattern$1 = /\\\./g, EXPANSION_MAX$1 = 1e5;
function numeric$1(e5) {
  return isNaN(e5) ? e5.charCodeAt(0) : parseInt(e5, 10);
}
function escapeBraces$1(e5) {
  return e5.replace(slashPattern$1, escSlash$1).replace(openPattern$1, escOpen$1).replace(closePattern$1, escClose$1).replace(commaPattern$1, escComma$1).replace(periodPattern$1, escPeriod$1);
}
function unescapeBraces$1(e5) {
  return e5.replace(escSlashPattern$1, "\\").replace(escOpenPattern$1, "{").replace(escClosePattern$1, "}").replace(escCommaPattern$1, ",").replace(escPeriodPattern$1, ".");
}
function parseCommaParts$1(e5) {
  if (!e5) return [""];
  const t = [], n = balanced$1("{", "}", e5);
  if (!n) return e5.split(",");
  const { pre: r, body: s, post: o } = n, a = r.split(",");
  a[a.length - 1] += "{" + s + "}";
  const l = parseCommaParts$1(o);
  return o.length && (a[a.length - 1] += l.shift(), a.push.apply(a, l)), t.push.apply(t, a), t;
}
function expand$1(e5, t = {}) {
  if (!e5) return [];
  const { max: n = EXPANSION_MAX$1 } = t;
  return e5.slice(0, 2) === "{}" && (e5 = "\\{\\}" + e5.slice(2)), expand_$1(escapeBraces$1(e5), n, true).map(unescapeBraces$1);
}
function embrace$1(e5) {
  return "{" + e5 + "}";
}
function isPadded$1(e5) {
  return /^-?0\d/.test(e5);
}
function lte$1(e5, t) {
  return e5 <= t;
}
function gte$1(e5, t) {
  return e5 >= t;
}
function expand_$1(e5, t, n) {
  const r = [], s = balanced$1("{", "}", e5);
  if (!s) return [e5];
  const o = s.pre, a = s.post.length ? expand_$1(s.post, t, false) : [""];
  if (/\$$/.test(s.pre)) for (let l = 0; l < a.length && l < t; l++) {
    const c = o + "{" + s.body + "}" + a[l];
    r.push(c);
  }
  else {
    const l = /^-?\d+\.\.-?\d+(?:\.\.-?\d+)?$/.test(s.body), c = /^[a-zA-Z]\.\.[a-zA-Z](?:\.\.-?\d+)?$/.test(s.body), u = l || c, p = s.body.indexOf(",") >= 0;
    if (!u && !p) return s.post.match(/,(?!,).*\}/) ? (e5 = s.pre + "{" + s.body + escClose$1 + s.post, expand_$1(e5, t, true)) : [e5];
    let f;
    if (u) f = s.body.split(/\.\./);
    else if (f = parseCommaParts$1(s.body), f.length === 1 && f[0] !== void 0 && (f = expand_$1(f[0], t, false).map(embrace$1), f.length === 1)) return a.map((m) => s.pre + f[0] + m);
    let h;
    if (u && f[0] !== void 0 && f[1] !== void 0) {
      const m = numeric$1(f[0]), g = numeric$1(f[1]), d = Math.max(f[0].length, f[1].length);
      let y = f.length === 3 && f[2] !== void 0 ? Math.max(Math.abs(numeric$1(f[2])), 1) : 1, S = lte$1;
      g < m && (y *= -1, S = gte$1);
      const E = f.some(isPadded$1);
      h = [];
      for (let b = m; S(b, g) && h.length < t; b += y) {
        let A;
        if (c) A = String.fromCharCode(b), A === "\\" && (A = "");
        else if (A = String(b), E) {
          const w = d - A.length;
          if (w > 0) {
            const v = new Array(w + 1).join("0");
            b < 0 ? A = "-" + v + A.slice(1) : A = v + A;
          }
        }
        h.push(A);
      }
    } else {
      h = [];
      for (let m = 0; m < f.length; m++) h.push.apply(h, expand_$1(f[m], t, false));
    }
    for (let m = 0; m < h.length; m++) for (let g = 0; g < a.length && r.length < t; g++) {
      const d = o + h[m] + a[g];
      (!n || u || d) && r.push(d);
    }
  }
  return r;
}
const MAX_PATTERN_LENGTH$1 = 1024 * 64, assertValidPattern$1 = (e5) => {
  if (typeof e5 != "string") throw new TypeError("invalid pattern");
  if (e5.length > MAX_PATTERN_LENGTH$1) throw new TypeError("pattern is too long");
}, posixClasses$1 = { "[:alnum:]": ["\\p{L}\\p{Nl}\\p{Nd}", true], "[:alpha:]": ["\\p{L}\\p{Nl}", true], "[:ascii:]": ["\\x00-\\x7f", false], "[:blank:]": ["\\p{Zs}\\t", true], "[:cntrl:]": ["\\p{Cc}", true], "[:digit:]": ["\\p{Nd}", true], "[:graph:]": ["\\p{Z}\\p{C}", true, true], "[:lower:]": ["\\p{Ll}", true], "[:print:]": ["\\p{C}", true], "[:punct:]": ["\\p{P}", true], "[:space:]": ["\\p{Z}\\t\\r\\n\\v\\f", true], "[:upper:]": ["\\p{Lu}", true], "[:word:]": ["\\p{L}\\p{Nl}\\p{Nd}\\p{Pc}", true], "[:xdigit:]": ["A-Fa-f0-9", false] }, braceEscape$1 = (e5) => e5.replace(/[[\]\\-]/g, "\\$&"), regexpEscape$1 = (e5) => e5.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), rangesToString$1 = (e5) => e5.join(""), parseClass$1 = (e5, t) => {
  const n = t;
  if (e5.charAt(n) !== "[") throw new Error("not in a brace expression");
  const r = [], s = [];
  let o = n + 1, a = false, l = false, c = false, u = false, p = n, f = "";
  e: for (; o < e5.length; ) {
    const d = e5.charAt(o);
    if ((d === "!" || d === "^") && o === n + 1) {
      u = true, o++;
      continue;
    }
    if (d === "]" && a && !c) {
      p = o + 1;
      break;
    }
    if (a = true, d === "\\" && !c) {
      c = true, o++;
      continue;
    }
    if (d === "[" && !c) {
      for (const [y, [S, $, E]] of Object.entries(posixClasses$1)) if (e5.startsWith(y, o)) {
        if (f) return ["$.", false, e5.length - n, true];
        o += y.length, E ? s.push(S) : r.push(S), l = l || $;
        continue e;
      }
    }
    if (c = false, f) {
      d > f ? r.push(braceEscape$1(f) + "-" + braceEscape$1(d)) : d === f && r.push(braceEscape$1(d)), f = "", o++;
      continue;
    }
    if (e5.startsWith("-]", o + 1)) {
      r.push(braceEscape$1(d + "-")), o += 2;
      continue;
    }
    if (e5.startsWith("-", o + 1)) {
      f = d, o += 2;
      continue;
    }
    r.push(braceEscape$1(d)), o++;
  }
  if (p < o) return ["", false, 0, false];
  if (!r.length && !s.length) return ["$.", false, e5.length - n, true];
  if (s.length === 0 && r.length === 1 && /^\\?.$/.test(r[0]) && !u) {
    const d = r[0].length === 2 ? r[0].slice(-1) : r[0];
    return [regexpEscape$1(d), false, p - n, false];
  }
  const h = "[" + (u ? "^" : "") + rangesToString$1(r) + "]", m = "[" + (u ? "" : "^") + rangesToString$1(s) + "]";
  return [r.length && s.length ? "(" + h + "|" + m + ")" : r.length ? h : m, l, p - n, true];
}, unescape$1 = (e5, { windowsPathsNoEscape: t = false, magicalBraces: n = true } = {}) => n ? t ? e5.replace(/\[([^/\\])\]/g, "$1") : e5.replace(/((?!\\).|^)\[([^/\\])\]/g, "$1$2").replace(/\\([^/])/g, "$1") : t ? e5.replace(/\[([^/\\{}])\]/g, "$1") : e5.replace(/((?!\\).|^)\[([^/\\{}])\]/g, "$1$2").replace(/\\([^/{}])/g, "$1");
var _a$3;
const types$1$1 = /* @__PURE__ */ new Set(["!", "?", "+", "*", "@"]), isExtglobType$1 = (e5) => types$1$1.has(e5), isExtglobAST$1 = (e5) => isExtglobType$1(e5.type), adoptionMap$1 = /* @__PURE__ */ new Map([["!", ["@"]], ["?", ["?", "@"]], ["@", ["@"]], ["*", ["*", "+", "?", "@"]], ["+", ["+", "@"]]]), adoptionWithSpaceMap$1 = /* @__PURE__ */ new Map([["!", ["?"]], ["@", ["?"]], ["+", ["?", "*"]]]), adoptionAnyMap$1 = /* @__PURE__ */ new Map([["!", ["?", "@"]], ["?", ["?", "@"]], ["@", ["?", "@"]], ["*", ["*", "+", "?", "@"]], ["+", ["+", "@", "?", "*"]]]), usurpMap$1 = /* @__PURE__ */ new Map([["!", /* @__PURE__ */ new Map([["!", "@"]])], ["?", /* @__PURE__ */ new Map([["*", "*"], ["+", "*"]])], ["@", /* @__PURE__ */ new Map([["!", "!"], ["?", "?"], ["@", "@"], ["*", "*"], ["+", "+"]])], ["+", /* @__PURE__ */ new Map([["?", "*"], ["*", "*"]])]]), startNoTraversal$1 = "(?!(?:^|/)\\.\\.?(?:$|/))", startNoDot$1 = "(?!\\.)", addPatternStart$1 = /* @__PURE__ */ new Set(["[", "."]), justDots$1 = /* @__PURE__ */ new Set(["..", "."]), reSpecials$1 = new Set("().*{}+?[]^$\\!"), regExpEscape$1$1 = (e5) => e5.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), qmark$1$1 = "[^/]", star$1$1 = qmark$1$1 + "*?", starNoEmpty$1 = qmark$1$1 + "+?";
let ID$1 = 0;
let AST$1 = class AST {
  constructor(t, n, r = {}) {
    __privateAdd$1(this, _AST_instances$1);
    __publicField$5(this, "type");
    __privateAdd$1(this, _n$3);
    __privateAdd$1(this, _i$3);
    __privateAdd$1(this, _r$3, false);
    __privateAdd$1(this, _e$6, []);
    __privateAdd$1(this, _t$3);
    __privateAdd$1(this, _a2$2);
    __privateAdd$1(this, _c$2);
    __privateAdd$1(this, _l$2, false);
    __privateAdd$1(this, _s$3);
    __privateAdd$1(this, _o$2);
    __privateAdd$1(this, _u$2, false);
    __publicField$5(this, "id", ++ID$1);
    this.type = t, t && __privateSet$1(this, _i$3, true), __privateSet$1(this, _t$3, n), __privateSet$1(this, _n$3, __privateGet$1(this, _t$3) ? __privateGet$1(__privateGet$1(this, _t$3), _n$3) : this), __privateSet$1(this, _s$3, __privateGet$1(this, _n$3) === this ? r : __privateGet$1(__privateGet$1(this, _n$3), _s$3)), __privateSet$1(this, _c$2, __privateGet$1(this, _n$3) === this ? [] : __privateGet$1(__privateGet$1(this, _n$3), _c$2)), t === "!" && !__privateGet$1(__privateGet$1(this, _n$3), _l$2) && __privateGet$1(this, _c$2).push(this), __privateSet$1(this, _a2$2, __privateGet$1(this, _t$3) ? __privateGet$1(__privateGet$1(this, _t$3), _e$6).length : 0);
  }
  get depth() {
    var _a4, _b;
    return ((_b = (_a4 = __privateGet$1(this, _t$3)) == null ? void 0 : _a4.depth) != null ? _b : -1) + 1;
  }
  [/* @__PURE__ */ Symbol.for("nodejs.util.inspect.custom")]() {
    var _a4;
    return { "@@type": "AST", id: this.id, type: this.type, root: __privateGet$1(this, _n$3).id, parent: (_a4 = __privateGet$1(this, _t$3)) == null ? void 0 : _a4.id, depth: this.depth, partsLength: __privateGet$1(this, _e$6).length, parts: __privateGet$1(this, _e$6) };
  }
  get hasMagic() {
    if (__privateGet$1(this, _i$3) !== void 0) return __privateGet$1(this, _i$3);
    for (const t of __privateGet$1(this, _e$6)) if (typeof t != "string" && (t.type || t.hasMagic)) return __privateSet$1(this, _i$3, true);
    return __privateGet$1(this, _i$3);
  }
  toString() {
    return __privateGet$1(this, _o$2) !== void 0 ? __privateGet$1(this, _o$2) : this.type ? __privateSet$1(this, _o$2, this.type + "(" + __privateGet$1(this, _e$6).map((t) => String(t)).join("|") + ")") : __privateSet$1(this, _o$2, __privateGet$1(this, _e$6).map((t) => String(t)).join(""));
  }
  push(...t) {
    for (const n of t) if (n !== "") {
      if (typeof n != "string" && !(n instanceof _a$3 && __privateGet$1(n, _t$3) === this)) throw new Error("invalid part: " + n);
      __privateGet$1(this, _e$6).push(n);
    }
  }
  toJSON() {
    var _a4;
    const t = this.type === null ? __privateGet$1(this, _e$6).slice().map((n) => typeof n == "string" ? n : n.toJSON()) : [this.type, ...__privateGet$1(this, _e$6).map((n) => n.toJSON())];
    return this.isStart() && !this.type && t.unshift([]), this.isEnd() && (this === __privateGet$1(this, _n$3) || __privateGet$1(__privateGet$1(this, _n$3), _l$2) && ((_a4 = __privateGet$1(this, _t$3)) == null ? void 0 : _a4.type) === "!") && t.push({}), t;
  }
  isStart() {
    var _a4;
    if (__privateGet$1(this, _n$3) === this) return true;
    if (!((_a4 = __privateGet$1(this, _t$3)) == null ? void 0 : _a4.isStart())) return false;
    if (__privateGet$1(this, _a2$2) === 0) return true;
    const t = __privateGet$1(this, _t$3);
    for (let n = 0; n < __privateGet$1(this, _a2$2); n++) {
      const r = __privateGet$1(t, _e$6)[n];
      if (!(r instanceof _a$3 && r.type === "!")) return false;
    }
    return true;
  }
  isEnd() {
    var _a4, _b, _c2;
    if (__privateGet$1(this, _n$3) === this || ((_a4 = __privateGet$1(this, _t$3)) == null ? void 0 : _a4.type) === "!") return true;
    if (!((_b = __privateGet$1(this, _t$3)) == null ? void 0 : _b.isEnd())) return false;
    if (!this.type) return (_c2 = __privateGet$1(this, _t$3)) == null ? void 0 : _c2.isEnd();
    const t = __privateGet$1(this, _t$3) ? __privateGet$1(__privateGet$1(this, _t$3), _e$6).length : 0;
    return __privateGet$1(this, _a2$2) === t - 1;
  }
  copyIn(t) {
    typeof t == "string" ? this.push(t) : this.push(t.clone(this));
  }
  clone(t) {
    const n = new _a$3(this.type, t);
    for (const r of __privateGet$1(this, _e$6)) n.copyIn(r);
    return n;
  }
  static fromGlob(t, n = {}) {
    var _a4;
    const r = new _a$3(null, void 0, n);
    return __privateMethod$1(_a4 = _a$3, _AST_static$1, f_fn$1).call(_a4, t, r, 0, n, 0), r;
  }
  toMMPattern() {
    if (this !== __privateGet$1(this, _n$3)) return __privateGet$1(this, _n$3).toMMPattern();
    const t = this.toString(), [n, r, s, o] = this.toRegExpSource();
    if (!(s || __privateGet$1(this, _i$3) || __privateGet$1(this, _s$3).nocase && !__privateGet$1(this, _s$3).nocaseMagicOnly && t.toUpperCase() !== t.toLowerCase())) return r;
    const l = (__privateGet$1(this, _s$3).nocase ? "i" : "") + (o ? "u" : "");
    return Object.assign(new RegExp(`^${n}$`, l), { _src: n, _glob: t });
  }
  get options() {
    return __privateGet$1(this, _s$3);
  }
  toRegExpSource(t) {
    var _a4;
    const n = t != null ? t : !!__privateGet$1(this, _s$3).dot;
    if (__privateGet$1(this, _n$3) === this && (__privateMethod$1(this, _AST_instances$1, d_fn$1).call(this), __privateMethod$1(this, _AST_instances$1, y_fn$1).call(this)), !isExtglobAST$1(this)) {
      const c = this.isStart() && this.isEnd() && !__privateGet$1(this, _e$6).some((m) => typeof m != "string"), u = __privateGet$1(this, _e$6).map((m) => {
        var _a5;
        const [g, d, y, S] = typeof m == "string" ? __privateMethod$1(_a5 = _a$3, _AST_static$1, w_fn$1).call(_a5, m, __privateGet$1(this, _i$3), c) : m.toRegExpSource(t);
        return __privateSet$1(this, _i$3, __privateGet$1(this, _i$3) || y), __privateSet$1(this, _r$3, __privateGet$1(this, _r$3) || S), g;
      }).join("");
      let p = "";
      if (this.isStart() && typeof __privateGet$1(this, _e$6)[0] == "string" && !(__privateGet$1(this, _e$6).length === 1 && justDots$1.has(__privateGet$1(this, _e$6)[0]))) {
        const g = addPatternStart$1, d = n && g.has(u.charAt(0)) || u.startsWith("\\.") && g.has(u.charAt(2)) || u.startsWith("\\.\\.") && g.has(u.charAt(4)), y = !n && !t && g.has(u.charAt(0));
        p = d ? startNoTraversal$1 : y ? startNoDot$1 : "";
      }
      let f = "";
      return this.isEnd() && __privateGet$1(__privateGet$1(this, _n$3), _l$2) && ((_a4 = __privateGet$1(this, _t$3)) == null ? void 0 : _a4.type) === "!" && (f = "(?:$|\\/)"), [p + u + f, unescape$1(u), __privateSet$1(this, _i$3, !!__privateGet$1(this, _i$3)), __privateGet$1(this, _r$3)];
    }
    const r = this.type === "*" || this.type === "+", s = this.type === "!" ? "(?:(?!(?:" : "(?:";
    let o = __privateMethod$1(this, _AST_instances$1, g_fn$1).call(this, n);
    if (this.isStart() && this.isEnd() && !o && this.type !== "!") {
      const c = this.toString(), u = this;
      return __privateSet$1(u, _e$6, [c]), u.type = null, __privateSet$1(u, _i$3, void 0), [c, unescape$1(this.toString()), false, false];
    }
    let a = !r || t || n || !startNoDot$1 ? "" : __privateMethod$1(this, _AST_instances$1, g_fn$1).call(this, true);
    a === o && (a = ""), a && (o = `(?:${o})(?:${a})*?`);
    let l = "";
    if (this.type === "!" && __privateGet$1(this, _u$2)) l = (this.isStart() && !n ? startNoDot$1 : "") + starNoEmpty$1;
    else {
      const c = this.type === "!" ? "))" + (this.isStart() && !n && !t ? startNoDot$1 : "") + star$1$1 + ")" : this.type === "@" ? ")" : this.type === "?" ? ")?" : this.type === "+" && a ? ")" : this.type === "*" && a ? ")?" : `)${this.type}`;
      l = s + o + c;
    }
    return [l, unescape$1(o), __privateSet$1(this, _i$3, !!__privateGet$1(this, _i$3)), __privateGet$1(this, _r$3)];
  }
};
_n$3 = new WeakMap();
_i$3 = new WeakMap();
_r$3 = new WeakMap();
_e$6 = new WeakMap();
_t$3 = new WeakMap();
_a2$2 = new WeakMap();
_c$2 = new WeakMap();
_l$2 = new WeakMap();
_s$3 = new WeakMap();
_o$2 = new WeakMap();
_u$2 = new WeakMap();
_AST_instances$1 = new WeakSet();
y_fn$1 = function() {
  if (this !== __privateGet$1(this, _n$3)) throw new Error("should only call on root");
  if (__privateGet$1(this, _l$2)) return this;
  this.toString(), __privateSet$1(this, _l$2, true);
  let t;
  for (; t = __privateGet$1(this, _c$2).pop(); ) {
    if (t.type !== "!") continue;
    let n = t, r = __privateGet$1(n, _t$3);
    for (; r; ) {
      for (let s = __privateGet$1(n, _a2$2) + 1; !r.type && s < __privateGet$1(r, _e$6).length; s++) for (const o of __privateGet$1(t, _e$6)) {
        if (typeof o == "string") throw new Error("string part in extglob AST??");
        o.copyIn(__privateGet$1(r, _e$6)[s]);
      }
      n = r, r = __privateGet$1(n, _t$3);
    }
  }
  return this;
};
_AST_static$1 = new WeakSet();
f_fn$1 = function(t, n, r, s, o) {
  var _a4, _b, _c2, _d, _e2;
  const a = (_a4 = s.maxExtglobRecursion) != null ? _a4 : 2;
  let l = false, c = false, u = -1, p = false;
  if (n.type === null) {
    let d = r, y = "";
    for (; d < t.length; ) {
      const S = t.charAt(d++);
      if (l || S === "\\") {
        l = !l, y += S;
        continue;
      }
      if (c) {
        d === u + 1 ? (S === "^" || S === "!") && (p = true) : S === "]" && !(d === u + 2 && p) && (c = false), y += S;
        continue;
      } else if (S === "[") {
        c = true, u = d, p = false, y += S;
        continue;
      }
      if (!s.noext && isExtglobType$1(S) && t.charAt(d) === "(" && o <= a) {
        n.push(y), y = "";
        const E = new _a$3(S, n);
        d = __privateMethod$1(_b = _a$3, _AST_static$1, f_fn$1).call(_b, t, E, d, s, o + 1), n.push(E);
        continue;
      }
      y += S;
    }
    return n.push(y), d;
  }
  let f = r + 1, h = new _a$3(null, n);
  const m = [];
  let g = "";
  for (; f < t.length; ) {
    const d = t.charAt(f++);
    if (l || d === "\\") {
      l = !l, g += d;
      continue;
    }
    if (c) {
      f === u + 1 ? (d === "^" || d === "!") && (p = true) : d === "]" && !(f === u + 2 && p) && (c = false), g += d;
      continue;
    } else if (d === "[") {
      c = true, u = f, p = false, g += d;
      continue;
    }
    if (!s.noext && isExtglobType$1(d) && t.charAt(f) === "(" && (o <= a || n && __privateMethod$1(_c2 = n, _AST_instances$1, p_fn$1).call(_c2, d))) {
      const S = n && __privateMethod$1(_d = n, _AST_instances$1, p_fn$1).call(_d, d) ? 0 : 1;
      h.push(g), g = "";
      const $ = new _a$3(d, h);
      h.push($), f = __privateMethod$1(_e2 = _a$3, _AST_static$1, f_fn$1).call(_e2, t, $, f, s, o + S);
      continue;
    }
    if (d === "|") {
      h.push(g), g = "", m.push(h), h = new _a$3(null, n);
      continue;
    }
    if (d === ")") return g === "" && __privateGet$1(n, _e$6).length === 0 && __privateSet$1(n, _u$2, true), h.push(g), g = "", n.push(...m, h), f;
    g += d;
  }
  return n.type = null, __privateSet$1(n, _i$3, void 0), __privateSet$1(n, _e$6, [t.substring(r - 1)]), f;
};
$_fn$1 = function(t) {
  return __privateMethod$1(this, _AST_instances$1, h_fn$1).call(this, t, adoptionWithSpaceMap$1);
};
h_fn$1 = function(t, n = adoptionMap$1) {
  if (!t || typeof t != "object" || t.type !== null || __privateGet$1(t, _e$6).length !== 1 || this.type === null) return false;
  const r = __privateGet$1(t, _e$6)[0];
  return !r || typeof r != "object" || r.type === null ? false : __privateMethod$1(this, _AST_instances$1, p_fn$1).call(this, r.type, n);
};
p_fn$1 = function(t, n = adoptionAnyMap$1) {
  var _a4;
  return !!((_a4 = n.get(this.type)) == null ? void 0 : _a4.includes(t));
};
S_fn$1 = function(t, n) {
  const r = __privateGet$1(t, _e$6)[0], s = new _a$3(null, r, this.options);
  __privateGet$1(s, _e$6).push(""), r.push(s), __privateMethod$1(this, _AST_instances$1, m_fn$1).call(this, t, n);
};
m_fn$1 = function(t, n) {
  const r = __privateGet$1(t, _e$6)[0];
  __privateGet$1(this, _e$6).splice(n, 1, ...__privateGet$1(r, _e$6));
  for (const s of __privateGet$1(r, _e$6)) typeof s == "object" && __privateSet$1(s, _t$3, this);
  __privateSet$1(this, _o$2, void 0);
};
E_fn$1 = function(t) {
  var _a4;
  return !!((_a4 = usurpMap$1.get(this.type)) == null ? void 0 : _a4.has(t));
};
A_fn$1 = function(t) {
  if (!t || typeof t != "object" || t.type !== null || __privateGet$1(t, _e$6).length !== 1 || this.type === null || __privateGet$1(this, _e$6).length !== 1) return false;
  const n = __privateGet$1(t, _e$6)[0];
  return !n || typeof n != "object" || n.type === null ? false : __privateMethod$1(this, _AST_instances$1, E_fn$1).call(this, n.type);
};
b_fn$1 = function(t) {
  const n = usurpMap$1.get(this.type), r = __privateGet$1(t, _e$6)[0], s = n == null ? void 0 : n.get(r.type);
  if (!s) return false;
  __privateSet$1(this, _e$6, __privateGet$1(r, _e$6));
  for (const o of __privateGet$1(this, _e$6)) typeof o == "object" && __privateSet$1(o, _t$3, this);
  this.type = s, __privateSet$1(this, _o$2, void 0), __privateSet$1(this, _u$2, false);
};
d_fn$1 = function() {
  var _a4, _b;
  if (isExtglobAST$1(this)) {
    let t = 0, n = false;
    do {
      n = true;
      for (let r = 0; r < __privateGet$1(this, _e$6).length; r++) {
        const s = __privateGet$1(this, _e$6)[r];
        typeof s == "object" && (__privateMethod$1(_a4 = s, _AST_instances$1, d_fn$1).call(_a4), __privateMethod$1(this, _AST_instances$1, h_fn$1).call(this, s) ? (n = false, __privateMethod$1(this, _AST_instances$1, m_fn$1).call(this, s, r)) : __privateMethod$1(this, _AST_instances$1, $_fn$1).call(this, s) ? (n = false, __privateMethod$1(this, _AST_instances$1, S_fn$1).call(this, s, r)) : __privateMethod$1(this, _AST_instances$1, A_fn$1).call(this, s) && (n = false, __privateMethod$1(this, _AST_instances$1, b_fn$1).call(this, s)));
      }
    } while (!n && ++t < 10);
  } else for (const t of __privateGet$1(this, _e$6)) typeof t == "object" && __privateMethod$1(_b = t, _AST_instances$1, d_fn$1).call(_b);
  __privateSet$1(this, _o$2, void 0);
};
g_fn$1 = function(t) {
  return __privateGet$1(this, _e$6).map((n) => {
    if (typeof n == "string") throw new Error("string type in extglob ast??");
    const [r, s, o, a] = n.toRegExpSource(t);
    return __privateSet$1(this, _r$3, __privateGet$1(this, _r$3) || a), r;
  }).filter((n) => !(this.isStart() && this.isEnd()) || !!n).join("|");
};
w_fn$1 = function(t, n, r = false) {
  let s = false, o = "", a = false, l = false;
  for (let c = 0; c < t.length; c++) {
    const u = t.charAt(c);
    if (s) {
      s = false, o += (reSpecials$1.has(u) ? "\\" : "") + u;
      continue;
    }
    if (u === "*") {
      if (l) continue;
      l = true, o += r && /^[*]+$/.test(t) ? starNoEmpty$1 : star$1$1, n = true;
      continue;
    } else l = false;
    if (u === "\\") {
      c === t.length - 1 ? o += "\\\\" : s = true;
      continue;
    }
    if (u === "[") {
      const [p, f, h, m] = parseClass$1(t, c);
      if (h) {
        o += p, a = a || f, c += h - 1, n = n || m;
        continue;
      }
    }
    if (u === "?") {
      o += qmark$1$1, n = true;
      continue;
    }
    o += regExpEscape$1$1(u);
  }
  return [o, unescape$1(t), !!n, a];
};
__privateAdd$1(AST$1, _AST_static$1);
_a$3 = AST$1;
const escape$1 = (e5, { windowsPathsNoEscape: t = false, magicalBraces: n = false } = {}) => n ? t ? e5.replace(/[?*()[\]{}]/g, "[$&]") : e5.replace(/[?*()[\]\\{}]/g, "\\$&") : t ? e5.replace(/[?*()[\]]/g, "[$&]") : e5.replace(/[?*()[\]\\]/g, "\\$&"), minimatch$1 = (e5, t, n = {}) => (assertValidPattern$1(t), !n.nocomment && t.charAt(0) === "#" ? false : new Minimatch$1(t, n).match(e5)), starDotExtRE$1 = /^\*+([^+@!?*[(]*)$/, starDotExtTest$1 = (e5) => (t) => !t.startsWith(".") && t.endsWith(e5), starDotExtTestDot$1 = (e5) => (t) => t.endsWith(e5), starDotExtTestNocase$1 = (e5) => (e5 = e5.toLowerCase(), (t) => !t.startsWith(".") && t.toLowerCase().endsWith(e5)), starDotExtTestNocaseDot$1 = (e5) => (e5 = e5.toLowerCase(), (t) => t.toLowerCase().endsWith(e5)), starDotStarRE$1 = /^\*+\.\*+$/, starDotStarTest$1 = (e5) => !e5.startsWith(".") && e5.includes("."), starDotStarTestDot$1 = (e5) => e5 !== "." && e5 !== ".." && e5.includes("."), dotStarRE$1 = /^\.\*+$/, dotStarTest$1 = (e5) => e5 !== "." && e5 !== ".." && e5.startsWith("."), starRE$1 = /^\*+$/, starTest$1 = (e5) => e5.length !== 0 && !e5.startsWith("."), starTestDot$1 = (e5) => e5.length !== 0 && e5 !== "." && e5 !== "..", qmarksRE$1 = /^\?+([^+@!?*[(]*)?$/, qmarksTestNocase$1 = ([e5, t = ""]) => {
  const n = qmarksTestNoExt$1([e5]);
  return t ? (t = t.toLowerCase(), (r) => n(r) && r.toLowerCase().endsWith(t)) : n;
}, qmarksTestNocaseDot$1 = ([e5, t = ""]) => {
  const n = qmarksTestNoExtDot$1([e5]);
  return t ? (t = t.toLowerCase(), (r) => n(r) && r.toLowerCase().endsWith(t)) : n;
}, qmarksTestDot$1 = ([e5, t = ""]) => {
  const n = qmarksTestNoExtDot$1([e5]);
  return t ? (r) => n(r) && r.endsWith(t) : n;
}, qmarksTest$1 = ([e5, t = ""]) => {
  const n = qmarksTestNoExt$1([e5]);
  return t ? (r) => n(r) && r.endsWith(t) : n;
}, qmarksTestNoExt$1 = ([e5]) => {
  const t = e5.length;
  return (n) => n.length === t && !n.startsWith(".");
}, qmarksTestNoExtDot$1 = ([e5]) => {
  const t = e5.length;
  return (n) => n.length === t && n !== "." && n !== "..";
}, defaultPlatform$1 = typeof process == "object" && process ? typeof process.env == "object" && process.env && process.env.__MINIMATCH_TESTING_PLATFORM__ || process.platform : "posix", path$2 = { win32: { sep: "\\" }, posix: { sep: "/" } }, sep$1 = defaultPlatform$1 === "win32" ? path$2.win32.sep : path$2.posix.sep;
minimatch$1.sep = sep$1;
const GLOBSTAR$1 = /* @__PURE__ */ Symbol("globstar **");
minimatch$1.GLOBSTAR = GLOBSTAR$1;
const qmark$2 = "[^/]", star$2 = qmark$2 + "*?", twoStarDot$1 = "(?:(?!(?:\\/|^)(?:\\.{1,2})($|\\/)).)*?", twoStarNoDot$1 = "(?:(?!(?:\\/|^)\\.).)*?", filter$1 = (e5, t = {}) => (n) => minimatch$1(n, e5, t);
minimatch$1.filter = filter$1;
const ext$1 = (e5, t = {}) => Object.assign({}, e5, t), defaults$5$1 = (e5) => {
  if (!e5 || typeof e5 != "object" || !Object.keys(e5).length) return minimatch$1;
  const t = minimatch$1;
  return Object.assign((r, s, o = {}) => t(r, s, ext$1(e5, o)), { Minimatch: class extends t.Minimatch {
    constructor(s, o = {}) {
      super(s, ext$1(e5, o));
    }
    static defaults(s) {
      return t.defaults(ext$1(e5, s)).Minimatch;
    }
  }, AST: class extends t.AST {
    constructor(s, o, a = {}) {
      super(s, o, ext$1(e5, a));
    }
    static fromGlob(s, o = {}) {
      return t.AST.fromGlob(s, ext$1(e5, o));
    }
  }, unescape: (r, s = {}) => t.unescape(r, ext$1(e5, s)), escape: (r, s = {}) => t.escape(r, ext$1(e5, s)), filter: (r, s = {}) => t.filter(r, ext$1(e5, s)), defaults: (r) => t.defaults(ext$1(e5, r)), makeRe: (r, s = {}) => t.makeRe(r, ext$1(e5, s)), braceExpand: (r, s = {}) => t.braceExpand(r, ext$1(e5, s)), match: (r, s, o = {}) => t.match(r, s, ext$1(e5, o)), sep: t.sep, GLOBSTAR: GLOBSTAR$1 });
};
minimatch$1.defaults = defaults$5$1;
const braceExpand$1 = (e5, t = {}) => (assertValidPattern$1(e5), t.nobrace || !/\{(?:(?!\{).)*\}/.test(e5) ? [e5] : expand$1(e5, { max: t.braceExpandMax }));
minimatch$1.braceExpand = braceExpand$1;
const makeRe$1 = (e5, t = {}) => new Minimatch$1(e5, t).makeRe();
minimatch$1.makeRe = makeRe$1;
const match$1 = (e5, t, n = {}) => {
  const r = new Minimatch$1(t, n);
  return e5 = e5.filter((s) => r.match(s)), r.options.nonull && !e5.length && e5.push(t), e5;
};
minimatch$1.match = match$1;
const globMagic$1 = /[?*]|[+@!]\(.*?\)|\[|\]/, regExpEscape$2 = (e5) => e5.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&");
let Minimatch$1 = class Minimatch {
  constructor(t, n = {}) {
    __privateAdd$1(this, _Minimatch_instances$1);
    __publicField$5(this, "options");
    __publicField$5(this, "set");
    __publicField$5(this, "pattern");
    __publicField$5(this, "windowsPathsNoEscape");
    __publicField$5(this, "nonegate");
    __publicField$5(this, "negate");
    __publicField$5(this, "comment");
    __publicField$5(this, "empty");
    __publicField$5(this, "preserveMultipleSlashes");
    __publicField$5(this, "partial");
    __publicField$5(this, "globSet");
    __publicField$5(this, "globParts");
    __publicField$5(this, "nocase");
    __publicField$5(this, "isWindows");
    __publicField$5(this, "platform");
    __publicField$5(this, "windowsNoMagicRoot");
    __publicField$5(this, "maxGlobstarRecursion");
    __publicField$5(this, "regexp");
    var _a4;
    assertValidPattern$1(t), n = n || {}, this.options = n, this.maxGlobstarRecursion = (_a4 = n.maxGlobstarRecursion) != null ? _a4 : 200, this.pattern = t, this.platform = n.platform || defaultPlatform$1, this.isWindows = this.platform === "win32";
    const r = "allowWindowsEscape";
    this.windowsPathsNoEscape = !!n.windowsPathsNoEscape || n[r] === false, this.windowsPathsNoEscape && (this.pattern = this.pattern.replace(/\\/g, "/")), this.preserveMultipleSlashes = !!n.preserveMultipleSlashes, this.regexp = null, this.negate = false, this.nonegate = !!n.nonegate, this.comment = false, this.empty = false, this.partial = !!n.partial, this.nocase = !!this.options.nocase, this.windowsNoMagicRoot = n.windowsNoMagicRoot !== void 0 ? n.windowsNoMagicRoot : !!(this.isWindows && this.nocase), this.globSet = [], this.globParts = [], this.set = [], this.make();
  }
  hasMagic() {
    if (this.options.magicalBraces && this.set.length > 1) return true;
    for (const t of this.set) for (const n of t) if (typeof n != "string") return true;
    return false;
  }
  debug(...t) {
  }
  make() {
    const t = this.pattern, n = this.options;
    if (!n.nocomment && t.charAt(0) === "#") {
      this.comment = true;
      return;
    }
    if (!t) {
      this.empty = true;
      return;
    }
    this.parseNegate(), this.globSet = [...new Set(this.braceExpand())], n.debug && (this.debug = (...o) => console.error(...o)), this.debug(this.pattern, this.globSet);
    const r = this.globSet.map((o) => this.slashSplit(o));
    this.globParts = this.preprocess(r), this.debug(this.pattern, this.globParts);
    let s = this.globParts.map((o, a, l) => {
      if (this.isWindows && this.windowsNoMagicRoot) {
        const c = o[0] === "" && o[1] === "" && (o[2] === "?" || !globMagic$1.test(o[2])) && !globMagic$1.test(o[3]), u = /^[a-z]:/i.test(o[0]);
        if (c) return [...o.slice(0, 4), ...o.slice(4).map((p) => this.parse(p))];
        if (u) return [o[0], ...o.slice(1).map((p) => this.parse(p))];
      }
      return o.map((c) => this.parse(c));
    });
    if (this.debug(this.pattern, s), this.set = s.filter((o) => o.indexOf(false) === -1), this.isWindows) for (let o = 0; o < this.set.length; o++) {
      const a = this.set[o];
      a[0] === "" && a[1] === "" && this.globParts[o][2] === "?" && typeof a[3] == "string" && /^[a-z]:$/i.test(a[3]) && (a[2] = "?");
    }
    this.debug(this.pattern, this.set);
  }
  preprocess(t) {
    if (this.options.noglobstar) for (const r of t) for (let s = 0; s < r.length; s++) r[s] === "**" && (r[s] = "*");
    const { optimizationLevel: n = 1 } = this.options;
    return n >= 2 ? (t = this.firstPhasePreProcess(t), t = this.secondPhasePreProcess(t)) : n >= 1 ? t = this.levelOneOptimize(t) : t = this.adjascentGlobstarOptimize(t), t;
  }
  adjascentGlobstarOptimize(t) {
    return t.map((n) => {
      let r = -1;
      for (; (r = n.indexOf("**", r + 1)) !== -1; ) {
        let s = r;
        for (; n[s + 1] === "**"; ) s++;
        s !== r && n.splice(r, s - r);
      }
      return n;
    });
  }
  levelOneOptimize(t) {
    return t.map((n) => (n = n.reduce((r, s) => {
      const o = r[r.length - 1];
      return s === "**" && o === "**" ? r : s === ".." && o && o !== ".." && o !== "." && o !== "**" ? (r.pop(), r) : (r.push(s), r);
    }, []), n.length === 0 ? [""] : n));
  }
  levelTwoFileOptimize(t) {
    Array.isArray(t) || (t = this.slashSplit(t));
    let n = false;
    do {
      if (n = false, !this.preserveMultipleSlashes) {
        for (let s = 1; s < t.length - 1; s++) {
          const o = t[s];
          s === 1 && o === "" && t[0] === "" || (o === "." || o === "") && (n = true, t.splice(s, 1), s--);
        }
        t[0] === "." && t.length === 2 && (t[1] === "." || t[1] === "") && (n = true, t.pop());
      }
      let r = 0;
      for (; (r = t.indexOf("..", r + 1)) !== -1; ) {
        const s = t[r - 1];
        s && s !== "." && s !== ".." && s !== "**" && !(this.isWindows && /^[a-z]:$/i.test(s)) && (n = true, t.splice(r - 1, 2), r -= 2);
      }
    } while (n);
    return t.length === 0 ? [""] : t;
  }
  firstPhasePreProcess(t) {
    let n = false;
    do {
      n = false;
      for (let r of t) {
        let s = -1;
        for (; (s = r.indexOf("**", s + 1)) !== -1; ) {
          let a = s;
          for (; r[a + 1] === "**"; ) a++;
          a > s && r.splice(s + 1, a - s);
          let l = r[s + 1];
          const c = r[s + 2], u = r[s + 3];
          if (l !== ".." || !c || c === "." || c === ".." || !u || u === "." || u === "..") continue;
          n = true, r.splice(s, 1);
          const p = r.slice(0);
          p[s] = "**", t.push(p), s--;
        }
        if (!this.preserveMultipleSlashes) {
          for (let a = 1; a < r.length - 1; a++) {
            const l = r[a];
            a === 1 && l === "" && r[0] === "" || (l === "." || l === "") && (n = true, r.splice(a, 1), a--);
          }
          r[0] === "." && r.length === 2 && (r[1] === "." || r[1] === "") && (n = true, r.pop());
        }
        let o = 0;
        for (; (o = r.indexOf("..", o + 1)) !== -1; ) {
          const a = r[o - 1];
          if (a && a !== "." && a !== ".." && a !== "**") {
            n = true;
            const c = o === 1 && r[o + 1] === "**" ? ["."] : [];
            r.splice(o - 1, 2, ...c), r.length === 0 && r.push(""), o -= 2;
          }
        }
      }
    } while (n);
    return t;
  }
  secondPhasePreProcess(t) {
    for (let n = 0; n < t.length - 1; n++) for (let r = n + 1; r < t.length; r++) {
      const s = this.partsMatch(t[n], t[r], !this.preserveMultipleSlashes);
      if (s) {
        t[n] = [], t[r] = s;
        break;
      }
    }
    return t.filter((n) => n.length);
  }
  partsMatch(t, n, r = false) {
    let s = 0, o = 0, a = [], l = "";
    for (; s < t.length && o < n.length; ) if (t[s] === n[o]) a.push(l === "b" ? n[o] : t[s]), s++, o++;
    else if (r && t[s] === "**" && n[o] === t[s + 1]) a.push(t[s]), s++;
    else if (r && n[o] === "**" && t[s] === n[o + 1]) a.push(n[o]), o++;
    else if (t[s] === "*" && n[o] && (this.options.dot || !n[o].startsWith(".")) && n[o] !== "**") {
      if (l === "b") return false;
      l = "a", a.push(t[s]), s++, o++;
    } else if (n[o] === "*" && t[s] && (this.options.dot || !t[s].startsWith(".")) && t[s] !== "**") {
      if (l === "a") return false;
      l = "b", a.push(n[o]), s++, o++;
    } else return false;
    return t.length === n.length && a;
  }
  parseNegate() {
    if (this.nonegate) return;
    const t = this.pattern;
    let n = false, r = 0;
    for (let s = 0; s < t.length && t.charAt(s) === "!"; s++) n = !n, r++;
    r && (this.pattern = t.slice(r)), this.negate = n;
  }
  matchOne(t, n, r = false) {
    let s = 0, o = 0;
    if (this.isWindows) {
      const l = typeof t[0] == "string" && /^[a-z]:$/i.test(t[0]), c = !l && t[0] === "" && t[1] === "" && t[2] === "?" && /^[a-z]:$/i.test(t[3]), u = typeof n[0] == "string" && /^[a-z]:$/i.test(n[0]), p = !u && n[0] === "" && n[1] === "" && n[2] === "?" && typeof n[3] == "string" && /^[a-z]:$/i.test(n[3]), f = c ? 3 : l ? 0 : void 0, h = p ? 3 : u ? 0 : void 0;
      if (typeof f == "number" && typeof h == "number") {
        const [m, g] = [t[f], n[h]];
        m.toLowerCase() === g.toLowerCase() && (n[h] = m, o = h, s = f);
      }
    }
    const { optimizationLevel: a = 1 } = this.options;
    return a >= 2 && (t = this.levelTwoFileOptimize(t)), n.includes(GLOBSTAR$1) ? __privateMethod$1(this, _Minimatch_instances$1, n_fn$1).call(this, t, n, r, s, o) : __privateMethod$1(this, _Minimatch_instances$1, r_fn$1).call(this, t, n, r, s, o);
  }
  braceExpand() {
    return braceExpand$1(this.pattern, this.options);
  }
  parse(t) {
    assertValidPattern$1(t);
    const n = this.options;
    if (t === "**") return GLOBSTAR$1;
    if (t === "") return "";
    let r, s = null;
    (r = t.match(starRE$1)) ? s = n.dot ? starTestDot$1 : starTest$1 : (r = t.match(starDotExtRE$1)) ? s = (n.nocase ? n.dot ? starDotExtTestNocaseDot$1 : starDotExtTestNocase$1 : n.dot ? starDotExtTestDot$1 : starDotExtTest$1)(r[1]) : (r = t.match(qmarksRE$1)) ? s = (n.nocase ? n.dot ? qmarksTestNocaseDot$1 : qmarksTestNocase$1 : n.dot ? qmarksTestDot$1 : qmarksTest$1)(r) : (r = t.match(starDotStarRE$1)) ? s = n.dot ? starDotStarTestDot$1 : starDotStarTest$1 : (r = t.match(dotStarRE$1)) && (s = dotStarTest$1);
    const o = AST$1.fromGlob(t, this.options).toMMPattern();
    return s && typeof o == "object" && Reflect.defineProperty(o, "test", { value: s }), o;
  }
  makeRe() {
    if (this.regexp || this.regexp === false) return this.regexp;
    const t = this.set;
    if (!t.length) return this.regexp = false, this.regexp;
    const n = this.options, r = n.noglobstar ? star$2 : n.dot ? twoStarDot$1 : twoStarNoDot$1, s = new Set(n.nocase ? ["i"] : []);
    let o = t.map((c) => {
      const u = c.map((f) => {
        if (f instanceof RegExp) for (const h of f.flags.split("")) s.add(h);
        return typeof f == "string" ? regExpEscape$2(f) : f === GLOBSTAR$1 ? GLOBSTAR$1 : f._src;
      });
      u.forEach((f, h) => {
        const m = u[h + 1], g = u[h - 1];
        f !== GLOBSTAR$1 || g === GLOBSTAR$1 || (g === void 0 ? m !== void 0 && m !== GLOBSTAR$1 ? u[h + 1] = "(?:\\/|" + r + "\\/)?" + m : u[h] = r : m === void 0 ? u[h - 1] = g + "(?:\\/|\\/" + r + ")?" : m !== GLOBSTAR$1 && (u[h - 1] = g + "(?:\\/|\\/" + r + "\\/)" + m, u[h + 1] = GLOBSTAR$1));
      });
      const p = u.filter((f) => f !== GLOBSTAR$1);
      if (this.partial && p.length >= 1) {
        const f = [];
        for (let h = 1; h <= p.length; h++) f.push(p.slice(0, h).join("/"));
        return "(?:" + f.join("|") + ")";
      }
      return p.join("/");
    }).join("|");
    const [a, l] = t.length > 1 ? ["(?:", ")"] : ["", ""];
    o = "^" + a + o + l + "$", this.partial && (o = "^(?:\\/|" + a + o.slice(1, -1) + l + ")$"), this.negate && (o = "^(?!" + o + ").+$");
    try {
      this.regexp = new RegExp(o, [...s].join(""));
    } catch {
      this.regexp = false;
    }
    return this.regexp;
  }
  slashSplit(t) {
    return this.preserveMultipleSlashes ? t.split("/") : this.isWindows && /^\/\/[^/]+/.test(t) ? ["", ...t.split(/\/+/)] : t.split(/\/+/);
  }
  match(t, n = this.partial) {
    if (this.debug("match", t, this.pattern), this.comment) return false;
    if (this.empty) return t === "";
    if (t === "/" && n) return true;
    const r = this.options;
    this.isWindows && (t = t.split("\\").join("/"));
    const s = this.slashSplit(t);
    this.debug(this.pattern, "split", s);
    const o = this.set;
    this.debug(this.pattern, "set", o);
    let a = s[s.length - 1];
    if (!a) for (let l = s.length - 2; !a && l >= 0; l--) a = s[l];
    for (const l of o) {
      let c = s;
      if (r.matchBase && l.length === 1 && (c = [a]), this.matchOne(c, l, n)) return r.flipNegate ? true : !this.negate;
    }
    return r.flipNegate ? false : this.negate;
  }
  static defaults(t) {
    return minimatch$1.defaults(t).Minimatch;
  }
};
_Minimatch_instances$1 = new WeakSet();
n_fn$1 = function(t, n, r, s, o) {
  const a = n.indexOf(GLOBSTAR$1, o), l = n.lastIndexOf(GLOBSTAR$1), [c, u, p] = r ? [n.slice(o, a), n.slice(a + 1), []] : [n.slice(o, a), n.slice(a + 1, l), n.slice(l + 1)];
  if (c.length) {
    const $ = t.slice(s, s + c.length);
    if (!__privateMethod$1(this, _Minimatch_instances$1, r_fn$1).call(this, $, c, r, 0, 0)) return false;
    s += c.length, o += c.length;
  }
  let f = 0;
  if (p.length) {
    if (p.length + s > t.length) return false;
    let $ = t.length - p.length;
    if (__privateMethod$1(this, _Minimatch_instances$1, r_fn$1).call(this, t, p, r, $, 0)) f = p.length;
    else {
      if (t[t.length - 1] !== "" || s + p.length === t.length || ($--, !__privateMethod$1(this, _Minimatch_instances$1, r_fn$1).call(this, t, p, r, $, 0))) return false;
      f = p.length + 1;
    }
  }
  if (!u.length) {
    let $ = !!f;
    for (let E = s; E < t.length - f; E++) {
      const b = String(t[E]);
      if ($ = true, b === "." || b === ".." || !this.options.dot && b.startsWith(".")) return false;
    }
    return r || $;
  }
  const h = [[[], 0]];
  let m = h[0], g = 0;
  const d = [0];
  for (const $ of u) $ === GLOBSTAR$1 ? (d.push(g), m = [[], 0], h.push(m)) : (m[0].push($), g++);
  let y = h.length - 1;
  const S = t.length - f;
  for (const $ of h) $[1] = S - (d[y--] + $[0].length);
  return !!__privateMethod$1(this, _Minimatch_instances$1, i_fn$1).call(this, t, h, s, 0, r, 0, !!f);
};
i_fn$1 = function(t, n, r, s, o, a, l) {
  const c = n[s];
  if (!c) {
    for (let f = r; f < t.length; f++) {
      l = true;
      const h = t[f];
      if (h === "." || h === ".." || !this.options.dot && h.startsWith(".")) return false;
    }
    return l;
  }
  const [u, p] = c;
  for (; r <= p; ) {
    if (__privateMethod$1(this, _Minimatch_instances$1, r_fn$1).call(this, t.slice(0, r + u.length), u, o, r, 0) && a < this.maxGlobstarRecursion) {
      const m = __privateMethod$1(this, _Minimatch_instances$1, i_fn$1).call(this, t, n, r + u.length, s + 1, o, a + 1, l);
      if (m !== false) return m;
    }
    const h = t[r];
    if (h === "." || h === ".." || !this.options.dot && h.startsWith(".")) return false;
    r++;
  }
  return o || null;
};
r_fn$1 = function(t, n, r, s, o) {
  let a, l, c, u;
  for (a = s, l = o, u = t.length, c = n.length; a < u && l < c; a++, l++) {
    this.debug("matchOne loop");
    let p = n[l], f = t[a];
    if (this.debug(n, p, f), p === false || p === GLOBSTAR$1) return false;
    let h;
    if (typeof p == "string" ? (h = f === p, this.debug("string match", p, f, h)) : (h = p.test(f), this.debug("pattern match", p, f, h)), !h) return false;
  }
  if (a === u && l === c) return true;
  if (a === u) return r;
  if (l === c) return a === u - 1 && t[a] === "";
  throw new Error("wtf?");
};
minimatch$1.AST = AST$1;
minimatch$1.Minimatch = Minimatch$1;
minimatch$1.escape = escape$1;
minimatch$1.unescape = unescape$1;
const jsonSerializer$1 = { format: "json", async serializeDocumentFileContents(e5, t) {
  return JSON.stringify(e5, null, 2);
}, async deserializeDocumentFileContents(e5, t) {
  return JSON.parse(e5);
} };
var toString$1 = Object.prototype.toString, kindOf$1 = function(t) {
  if (t === void 0) return "undefined";
  if (t === null) return "null";
  var n = typeof t;
  if (n === "boolean") return "boolean";
  if (n === "string") return "string";
  if (n === "number") return "number";
  if (n === "symbol") return "symbol";
  if (n === "function") return isGeneratorFn$1(t) ? "generatorfunction" : "function";
  if (isArray$1(t)) return "array";
  if (isBuffer$1$1(t)) return "buffer";
  if (isArguments$1(t)) return "arguments";
  if (isDate$1(t)) return "date";
  if (isError$1(t)) return "error";
  if (isRegexp$1(t)) return "regexp";
  switch (ctorName$1(t)) {
    case "Symbol":
      return "symbol";
    case "Promise":
      return "promise";
    case "WeakMap":
      return "weakmap";
    case "WeakSet":
      return "weakset";
    case "Map":
      return "map";
    case "Set":
      return "set";
    case "Int8Array":
      return "int8array";
    case "Uint8Array":
      return "uint8array";
    case "Uint8ClampedArray":
      return "uint8clampedarray";
    case "Int16Array":
      return "int16array";
    case "Uint16Array":
      return "uint16array";
    case "Int32Array":
      return "int32array";
    case "Uint32Array":
      return "uint32array";
    case "Float32Array":
      return "float32array";
    case "Float64Array":
      return "float64array";
  }
  if (isGeneratorObj$1(t)) return "generator";
  switch (n = toString$1.call(t), n) {
    case "[object Object]":
      return "object";
    case "[object Map Iterator]":
      return "mapiterator";
    case "[object Set Iterator]":
      return "setiterator";
    case "[object String Iterator]":
      return "stringiterator";
    case "[object Array Iterator]":
      return "arrayiterator";
  }
  return n.slice(8, -1).toLowerCase().replace(/\s/g, "");
};
function ctorName$1(e5) {
  return typeof e5.constructor == "function" ? e5.constructor.name : null;
}
function isArray$1(e5) {
  return Array.isArray ? Array.isArray(e5) : e5 instanceof Array;
}
function isError$1(e5) {
  return e5 instanceof Error || typeof e5.message == "string" && e5.constructor && typeof e5.constructor.stackTraceLimit == "number";
}
function isDate$1(e5) {
  return e5 instanceof Date ? true : typeof e5.toDateString == "function" && typeof e5.getDate == "function" && typeof e5.setDate == "function";
}
function isRegexp$1(e5) {
  return e5 instanceof RegExp ? true : typeof e5.flags == "string" && typeof e5.ignoreCase == "boolean" && typeof e5.multiline == "boolean" && typeof e5.global == "boolean";
}
function isGeneratorFn$1(e5, t) {
  return ctorName$1(e5) === "GeneratorFunction";
}
function isGeneratorObj$1(e5) {
  return typeof e5.throw == "function" && typeof e5.return == "function" && typeof e5.next == "function";
}
function isArguments$1(e5) {
  try {
    if (typeof e5.length == "number" && typeof e5.callee == "function") return true;
  } catch (t) {
    if (t.message.indexOf("callee") !== -1) return true;
  }
  return false;
}
function isBuffer$1$1(e5) {
  return e5.constructor && typeof e5.constructor.isBuffer == "function" ? e5.constructor.isBuffer(e5) : false;
}
/*!
* is-extendable <https://github.com/jonschlinkert/is-extendable>
*
* Copyright (c) 2015, Jon Schlinkert.
* Licensed under the MIT License.
*/
var isExtendable$1 = function(t) {
  return typeof t < "u" && t !== null && (typeof t == "object" || typeof t == "function");
}, isObject$2$1 = isExtendable$1, extendShallow$1 = function(t) {
  isObject$2$1(t) || (t = {});
  for (var n = arguments.length, r = 1; r < n; r++) {
    var s = arguments[r];
    isObject$2$1(s) && assign$1(t, s);
  }
  return t;
};
function assign$1(e5, t) {
  for (var n in t) hasOwn$1(t, n) && (e5[n] = t[n]);
}
function hasOwn$1(e5, t) {
  return Object.prototype.hasOwnProperty.call(e5, t);
}
var typeOf$2$1 = kindOf$1, extend$2$1 = extendShallow$1, sectionMatter$1 = function(e5, t) {
  typeof t == "function" && (t = { parse: t });
  var n = toObject$1(e5), r = { section_delimiter: "---", parse: identity$z }, s = extend$2$1({}, r, t), o = s.section_delimiter, a = n.content.split(/\r?\n/), l = null, c = createSection$1(), u = [], p = [];
  function f(S) {
    n.content = S, l = [], u = [];
  }
  function h(S) {
    p.length && (c.key = getKey$1(p[0], o), c.content = S, s.parse(c, l), l.push(c), c = createSection$1(), u = [], p = []);
  }
  for (var m = 0; m < a.length; m++) {
    var g = a[m], d = p.length, y = g.trim();
    if (isDelimiter$1(y, o)) {
      if (y.length === 3 && m !== 0) {
        if (d === 0 || d === 2) {
          u.push(g);
          continue;
        }
        p.push(y), c.data = u.join(`
`), u = [];
        continue;
      }
      l === null && f(u.join(`
`)), d === 2 && h(u.join(`
`)), p.push(y);
      continue;
    }
    u.push(g);
  }
  return l === null ? f(u.join(`
`)) : h(u.join(`
`)), n.sections = l, n;
};
function isDelimiter$1(e5, t) {
  return !(e5.slice(0, t.length) !== t || e5.charAt(t.length + 1) === t.slice(-1));
}
function toObject$1(e5) {
  if (typeOf$2$1(e5) !== "object" && (e5 = { content: e5 }), typeof e5.content != "string" && !isBuffer$2(e5.content)) throw new TypeError("expected a buffer or string");
  return e5.content = e5.content.toString(), e5.sections = [], e5;
}
function getKey$1(e5, t) {
  return e5 ? e5.slice(t.length).trim() : "";
}
function createSection$1() {
  return { key: "", data: "", content: "" };
}
function identity$z(e5) {
  return e5;
}
function isBuffer$2(e5) {
  return e5 && e5.constructor && typeof e5.constructor.isBuffer == "function" ? e5.constructor.isBuffer(e5) : false;
}
var engines$2$1 = { exports: {} }, jsYaml$2$1 = {}, loader$2$1 = {}, common$7$1 = {};
function isNothing$1$1(e5) {
  return typeof e5 > "u" || e5 === null;
}
function isObject$1$1(e5) {
  return typeof e5 == "object" && e5 !== null;
}
function toArray$1$1(e5) {
  return Array.isArray(e5) ? e5 : isNothing$1$1(e5) ? [] : [e5];
}
function extend$1$1(e5, t) {
  var n, r, s, o;
  if (t) for (o = Object.keys(t), n = 0, r = o.length; n < r; n += 1) s = o[n], e5[s] = t[s];
  return e5;
}
function repeat$1$1(e5, t) {
  var n = "", r;
  for (r = 0; r < t; r += 1) n += e5;
  return n;
}
function isNegativeZero$1$1(e5) {
  return e5 === 0 && Number.NEGATIVE_INFINITY === 1 / e5;
}
common$7$1.isNothing = isNothing$1$1;
common$7$1.isObject = isObject$1$1;
common$7$1.toArray = toArray$1$1;
common$7$1.repeat = repeat$1$1;
common$7$1.isNegativeZero = isNegativeZero$1$1;
common$7$1.extend = extend$1$1;
function YAMLException$6$1(e5, t) {
  Error.call(this), this.name = "YAMLException", this.reason = e5, this.mark = t, this.message = (this.reason || "(unknown reason)") + (this.mark ? " " + this.mark.toString() : ""), Error.captureStackTrace ? Error.captureStackTrace(this, this.constructor) : this.stack = new Error().stack || "";
}
YAMLException$6$1.prototype = Object.create(Error.prototype);
YAMLException$6$1.prototype.constructor = YAMLException$6$1;
YAMLException$6$1.prototype.toString = function(t) {
  var n = this.name + ": ";
  return n += this.reason || "(unknown reason)", !t && this.mark && (n += " " + this.mark.toString()), n;
};
var exception$1$1 = YAMLException$6$1, common$6$1 = common$7$1;
function Mark$1$1(e5, t, n, r, s) {
  this.name = e5, this.buffer = t, this.position = n, this.line = r, this.column = s;
}
Mark$1$1.prototype.getSnippet = function(t, n) {
  var r, s, o, a, l;
  if (!this.buffer) return null;
  for (t = t || 4, n = n || 75, r = "", s = this.position; s > 0 && `\0\r
\x85\u2028\u2029`.indexOf(this.buffer.charAt(s - 1)) === -1; ) if (s -= 1, this.position - s > n / 2 - 1) {
    r = " ... ", s += 5;
    break;
  }
  for (o = "", a = this.position; a < this.buffer.length && `\0\r
\x85\u2028\u2029`.indexOf(this.buffer.charAt(a)) === -1; ) if (a += 1, a - this.position > n / 2 - 1) {
    o = " ... ", a -= 5;
    break;
  }
  return l = this.buffer.slice(s, a), common$6$1.repeat(" ", t) + r + l + o + `
` + common$6$1.repeat(" ", t + this.position - s + r.length) + "^";
};
Mark$1$1.prototype.toString = function(t) {
  var n, r = "";
  return this.name && (r += 'in "' + this.name + '" '), r += "at line " + (this.line + 1) + ", column " + (this.column + 1), t || (n = this.getSnippet(), n && (r += `:
` + n)), r;
};
var mark$1 = Mark$1$1, YAMLException$5$1 = exception$1$1, TYPE_CONSTRUCTOR_OPTIONS$1$1 = ["kind", "resolve", "construct", "instanceOf", "predicate", "represent", "defaultStyle", "styleAliases"], YAML_NODE_KINDS$1$1 = ["scalar", "sequence", "mapping"];
function compileStyleAliases$1$1(e5) {
  var t = {};
  return e5 !== null && Object.keys(e5).forEach(function(n) {
    e5[n].forEach(function(r) {
      t[String(r)] = n;
    });
  }), t;
}
function Type$j$1(e5, t) {
  if (t = t || {}, Object.keys(t).forEach(function(n) {
    if (TYPE_CONSTRUCTOR_OPTIONS$1$1.indexOf(n) === -1) throw new YAMLException$5$1('Unknown option "' + n + '" is met in definition of "' + e5 + '" YAML type.');
  }), this.tag = e5, this.kind = t.kind || null, this.resolve = t.resolve || function() {
    return true;
  }, this.construct = t.construct || function(n) {
    return n;
  }, this.instanceOf = t.instanceOf || null, this.predicate = t.predicate || null, this.represent = t.represent || null, this.defaultStyle = t.defaultStyle || null, this.styleAliases = compileStyleAliases$1$1(t.styleAliases || null), YAML_NODE_KINDS$1$1.indexOf(this.kind) === -1) throw new YAMLException$5$1('Unknown kind "' + this.kind + '" is specified for "' + e5 + '" YAML type.');
}
var type$1$1 = Type$j$1, common$5$1 = common$7$1, YAMLException$4$1 = exception$1$1, Type$i$1 = type$1$1;
function compileList$1$1(e5, t, n) {
  var r = [];
  return e5.include.forEach(function(s) {
    n = compileList$1$1(s, t, n);
  }), e5[t].forEach(function(s) {
    n.forEach(function(o, a) {
      o.tag === s.tag && o.kind === s.kind && r.push(a);
    }), n.push(s);
  }), n.filter(function(s, o) {
    return r.indexOf(o) === -1;
  });
}
function compileMap$1$1() {
  var e5 = { scalar: {}, sequence: {}, mapping: {}, fallback: {} }, t, n;
  function r(s) {
    e5[s.kind][s.tag] = e5.fallback[s.tag] = s;
  }
  for (t = 0, n = arguments.length; t < n; t += 1) arguments[t].forEach(r);
  return e5;
}
function Schema$7$1(e5) {
  this.include = e5.include || [], this.implicit = e5.implicit || [], this.explicit = e5.explicit || [], this.implicit.forEach(function(t) {
    if (t.loadKind && t.loadKind !== "scalar") throw new YAMLException$4$1("There is a non-scalar type in the implicit list of a schema. Implicit resolving of such types is not supported.");
  }), this.compiledImplicit = compileList$1$1(this, "implicit", []), this.compiledExplicit = compileList$1$1(this, "explicit", []), this.compiledTypeMap = compileMap$1$1(this.compiledImplicit, this.compiledExplicit);
}
Schema$7$1.DEFAULT = null;
Schema$7$1.create = function() {
  var t, n;
  switch (arguments.length) {
    case 1:
      t = Schema$7$1.DEFAULT, n = arguments[0];
      break;
    case 2:
      t = arguments[0], n = arguments[1];
      break;
    default:
      throw new YAMLException$4$1("Wrong number of arguments for Schema.create function");
  }
  if (t = common$5$1.toArray(t), n = common$5$1.toArray(n), !t.every(function(r) {
    return r instanceof Schema$7$1;
  })) throw new YAMLException$4$1("Specified list of super schemas (or a single Schema object) contains a non-Schema object.");
  if (!n.every(function(r) {
    return r instanceof Type$i$1;
  })) throw new YAMLException$4$1("Specified list of YAML types (or a single Type object) contains a non-Type object.");
  return new Schema$7$1({ include: t, explicit: n });
};
var schema$1$2 = Schema$7$1, Type$h$1 = type$1$1, str$1$1 = new Type$h$1("tag:yaml.org,2002:str", { kind: "scalar", construct: function(e5) {
  return e5 !== null ? e5 : "";
} }), Type$g$1 = type$1$1, seq$1$1 = new Type$g$1("tag:yaml.org,2002:seq", { kind: "sequence", construct: function(e5) {
  return e5 !== null ? e5 : [];
} }), Type$f$1 = type$1$1, map$1$1 = new Type$f$1("tag:yaml.org,2002:map", { kind: "mapping", construct: function(e5) {
  return e5 !== null ? e5 : {};
} }), Schema$6$1 = schema$1$2, failsafe$1$1 = new Schema$6$1({ explicit: [str$1$1, seq$1$1, map$1$1] }), Type$e$1 = type$1$1;
function resolveYamlNull$1$1(e5) {
  if (e5 === null) return true;
  var t = e5.length;
  return t === 1 && e5 === "~" || t === 4 && (e5 === "null" || e5 === "Null" || e5 === "NULL");
}
function constructYamlNull$1$1() {
  return null;
}
function isNull$1$1(e5) {
  return e5 === null;
}
var _null$1$1 = new Type$e$1("tag:yaml.org,2002:null", { kind: "scalar", resolve: resolveYamlNull$1$1, construct: constructYamlNull$1$1, predicate: isNull$1$1, represent: { canonical: function() {
  return "~";
}, lowercase: function() {
  return "null";
}, uppercase: function() {
  return "NULL";
}, camelcase: function() {
  return "Null";
} }, defaultStyle: "lowercase" }), Type$d$1 = type$1$1;
function resolveYamlBoolean$1$1(e5) {
  if (e5 === null) return false;
  var t = e5.length;
  return t === 4 && (e5 === "true" || e5 === "True" || e5 === "TRUE") || t === 5 && (e5 === "false" || e5 === "False" || e5 === "FALSE");
}
function constructYamlBoolean$1$1(e5) {
  return e5 === "true" || e5 === "True" || e5 === "TRUE";
}
function isBoolean$1$1(e5) {
  return Object.prototype.toString.call(e5) === "[object Boolean]";
}
var bool$1$1 = new Type$d$1("tag:yaml.org,2002:bool", { kind: "scalar", resolve: resolveYamlBoolean$1$1, construct: constructYamlBoolean$1$1, predicate: isBoolean$1$1, represent: { lowercase: function(e5) {
  return e5 ? "true" : "false";
}, uppercase: function(e5) {
  return e5 ? "TRUE" : "FALSE";
}, camelcase: function(e5) {
  return e5 ? "True" : "False";
} }, defaultStyle: "lowercase" }), common$4$1 = common$7$1, Type$c$1 = type$1$1;
function isHexCode$1$1(e5) {
  return 48 <= e5 && e5 <= 57 || 65 <= e5 && e5 <= 70 || 97 <= e5 && e5 <= 102;
}
function isOctCode$1$1(e5) {
  return 48 <= e5 && e5 <= 55;
}
function isDecCode$1$1(e5) {
  return 48 <= e5 && e5 <= 57;
}
function resolveYamlInteger$1$1(e5) {
  if (e5 === null) return false;
  var t = e5.length, n = 0, r = false, s;
  if (!t) return false;
  if (s = e5[n], (s === "-" || s === "+") && (s = e5[++n]), s === "0") {
    if (n + 1 === t) return true;
    if (s = e5[++n], s === "b") {
      for (n++; n < t; n++) if (s = e5[n], s !== "_") {
        if (s !== "0" && s !== "1") return false;
        r = true;
      }
      return r && s !== "_";
    }
    if (s === "x") {
      for (n++; n < t; n++) if (s = e5[n], s !== "_") {
        if (!isHexCode$1$1(e5.charCodeAt(n))) return false;
        r = true;
      }
      return r && s !== "_";
    }
    for (; n < t; n++) if (s = e5[n], s !== "_") {
      if (!isOctCode$1$1(e5.charCodeAt(n))) return false;
      r = true;
    }
    return r && s !== "_";
  }
  if (s === "_") return false;
  for (; n < t; n++) if (s = e5[n], s !== "_") {
    if (s === ":") break;
    if (!isDecCode$1$1(e5.charCodeAt(n))) return false;
    r = true;
  }
  return !r || s === "_" ? false : s !== ":" ? true : /^(:[0-5]?[0-9])+$/.test(e5.slice(n));
}
function constructYamlInteger$1$1(e5) {
  var t = e5, n = 1, r, s, o = [];
  return t.indexOf("_") !== -1 && (t = t.replace(/_/g, "")), r = t[0], (r === "-" || r === "+") && (r === "-" && (n = -1), t = t.slice(1), r = t[0]), t === "0" ? 0 : r === "0" ? t[1] === "b" ? n * parseInt(t.slice(2), 2) : t[1] === "x" ? n * parseInt(t, 16) : n * parseInt(t, 8) : t.indexOf(":") !== -1 ? (t.split(":").forEach(function(a) {
    o.unshift(parseInt(a, 10));
  }), t = 0, s = 1, o.forEach(function(a) {
    t += a * s, s *= 60;
  }), n * t) : n * parseInt(t, 10);
}
function isInteger$1$1(e5) {
  return Object.prototype.toString.call(e5) === "[object Number]" && e5 % 1 === 0 && !common$4$1.isNegativeZero(e5);
}
var int$1$1 = new Type$c$1("tag:yaml.org,2002:int", { kind: "scalar", resolve: resolveYamlInteger$1$1, construct: constructYamlInteger$1$1, predicate: isInteger$1$1, represent: { binary: function(e5) {
  return e5 >= 0 ? "0b" + e5.toString(2) : "-0b" + e5.toString(2).slice(1);
}, octal: function(e5) {
  return e5 >= 0 ? "0" + e5.toString(8) : "-0" + e5.toString(8).slice(1);
}, decimal: function(e5) {
  return e5.toString(10);
}, hexadecimal: function(e5) {
  return e5 >= 0 ? "0x" + e5.toString(16).toUpperCase() : "-0x" + e5.toString(16).toUpperCase().slice(1);
} }, defaultStyle: "decimal", styleAliases: { binary: [2, "bin"], octal: [8, "oct"], decimal: [10, "dec"], hexadecimal: [16, "hex"] } }), common$3$1 = common$7$1, Type$b$1 = type$1$1, YAML_FLOAT_PATTERN$1$1 = new RegExp("^(?:[-+]?(?:0|[1-9][0-9_]*)(?:\\.[0-9_]*)?(?:[eE][-+]?[0-9]+)?|\\.[0-9_]+(?:[eE][-+]?[0-9]+)?|[-+]?[0-9][0-9_]*(?::[0-5]?[0-9])+\\.[0-9_]*|[-+]?\\.(?:inf|Inf|INF)|\\.(?:nan|NaN|NAN))$");
function resolveYamlFloat$1$1(e5) {
  return !(e5 === null || !YAML_FLOAT_PATTERN$1$1.test(e5) || e5[e5.length - 1] === "_");
}
function constructYamlFloat$1$1(e5) {
  var t, n, r, s;
  return t = e5.replace(/_/g, "").toLowerCase(), n = t[0] === "-" ? -1 : 1, s = [], "+-".indexOf(t[0]) >= 0 && (t = t.slice(1)), t === ".inf" ? n === 1 ? Number.POSITIVE_INFINITY : Number.NEGATIVE_INFINITY : t === ".nan" ? NaN : t.indexOf(":") >= 0 ? (t.split(":").forEach(function(o) {
    s.unshift(parseFloat(o, 10));
  }), t = 0, r = 1, s.forEach(function(o) {
    t += o * r, r *= 60;
  }), n * t) : n * parseFloat(t, 10);
}
var SCIENTIFIC_WITHOUT_DOT$1$1 = /^[-+]?[0-9]+e/;
function representYamlFloat$1$1(e5, t) {
  var n;
  if (isNaN(e5)) switch (t) {
    case "lowercase":
      return ".nan";
    case "uppercase":
      return ".NAN";
    case "camelcase":
      return ".NaN";
  }
  else if (Number.POSITIVE_INFINITY === e5) switch (t) {
    case "lowercase":
      return ".inf";
    case "uppercase":
      return ".INF";
    case "camelcase":
      return ".Inf";
  }
  else if (Number.NEGATIVE_INFINITY === e5) switch (t) {
    case "lowercase":
      return "-.inf";
    case "uppercase":
      return "-.INF";
    case "camelcase":
      return "-.Inf";
  }
  else if (common$3$1.isNegativeZero(e5)) return "-0.0";
  return n = e5.toString(10), SCIENTIFIC_WITHOUT_DOT$1$1.test(n) ? n.replace("e", ".e") : n;
}
function isFloat$1$1(e5) {
  return Object.prototype.toString.call(e5) === "[object Number]" && (e5 % 1 !== 0 || common$3$1.isNegativeZero(e5));
}
var float$1$1 = new Type$b$1("tag:yaml.org,2002:float", { kind: "scalar", resolve: resolveYamlFloat$1$1, construct: constructYamlFloat$1$1, predicate: isFloat$1$1, represent: representYamlFloat$1$1, defaultStyle: "lowercase" }), Schema$5$1 = schema$1$2, json$1$1 = new Schema$5$1({ include: [failsafe$1$1], implicit: [_null$1$1, bool$1$1, int$1$1, float$1$1] }), Schema$4$1 = schema$1$2, core$1$1 = new Schema$4$1({ include: [json$1$1] }), Type$a$1 = type$1$1, YAML_DATE_REGEXP$1$1 = new RegExp("^([0-9][0-9][0-9][0-9])-([0-9][0-9])-([0-9][0-9])$"), YAML_TIMESTAMP_REGEXP$1$1 = new RegExp("^([0-9][0-9][0-9][0-9])-([0-9][0-9]?)-([0-9][0-9]?)(?:[Tt]|[ \\t]+)([0-9][0-9]?):([0-9][0-9]):([0-9][0-9])(?:\\.([0-9]*))?(?:[ \\t]*(Z|([-+])([0-9][0-9]?)(?::([0-9][0-9]))?))?$");
function resolveYamlTimestamp$1$1(e5) {
  return e5 === null ? false : YAML_DATE_REGEXP$1$1.exec(e5) !== null || YAML_TIMESTAMP_REGEXP$1$1.exec(e5) !== null;
}
function constructYamlTimestamp$1$1(e5) {
  var t, n, r, s, o, a, l, c = 0, u = null, p, f, h;
  if (t = YAML_DATE_REGEXP$1$1.exec(e5), t === null && (t = YAML_TIMESTAMP_REGEXP$1$1.exec(e5)), t === null) throw new Error("Date resolve error");
  if (n = +t[1], r = +t[2] - 1, s = +t[3], !t[4]) return new Date(Date.UTC(n, r, s));
  if (o = +t[4], a = +t[5], l = +t[6], t[7]) {
    for (c = t[7].slice(0, 3); c.length < 3; ) c += "0";
    c = +c;
  }
  return t[9] && (p = +t[10], f = +(t[11] || 0), u = (p * 60 + f) * 6e4, t[9] === "-" && (u = -u)), h = new Date(Date.UTC(n, r, s, o, a, l, c)), u && h.setTime(h.getTime() - u), h;
}
function representYamlTimestamp$1$1(e5) {
  return e5.toISOString();
}
var timestamp$1$1 = new Type$a$1("tag:yaml.org,2002:timestamp", { kind: "scalar", resolve: resolveYamlTimestamp$1$1, construct: constructYamlTimestamp$1$1, instanceOf: Date, represent: representYamlTimestamp$1$1 }), Type$9$1 = type$1$1;
function resolveYamlMerge$1$1(e5) {
  return e5 === "<<" || e5 === null;
}
var merge$1$1 = new Type$9$1("tag:yaml.org,2002:merge", { kind: "scalar", resolve: resolveYamlMerge$1$1 });
function commonjsRequire$1(e5) {
  throw new Error('Could not dynamically require "' + e5 + '". Please configure the dynamicRequireTargets or/and ignoreDynamicRequires option of @rollup/plugin-commonjs appropriately for this require call to work.');
}
var NodeBuffer$1;
try {
  var _require$1$1 = commonjsRequire$1;
  NodeBuffer$1 = _require$1$1("buffer").Buffer;
} catch {
}
var Type$8$1 = type$1$1, BASE64_MAP$1$1 = `ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=
\r`;
function resolveYamlBinary$1$1(e5) {
  if (e5 === null) return false;
  var t, n, r = 0, s = e5.length, o = BASE64_MAP$1$1;
  for (n = 0; n < s; n++) if (t = o.indexOf(e5.charAt(n)), !(t > 64)) {
    if (t < 0) return false;
    r += 6;
  }
  return r % 8 === 0;
}
function constructYamlBinary$1$1(e5) {
  var t, n, r = e5.replace(/[\r\n=]/g, ""), s = r.length, o = BASE64_MAP$1$1, a = 0, l = [];
  for (t = 0; t < s; t++) t % 4 === 0 && t && (l.push(a >> 16 & 255), l.push(a >> 8 & 255), l.push(a & 255)), a = a << 6 | o.indexOf(r.charAt(t));
  return n = s % 4 * 6, n === 0 ? (l.push(a >> 16 & 255), l.push(a >> 8 & 255), l.push(a & 255)) : n === 18 ? (l.push(a >> 10 & 255), l.push(a >> 2 & 255)) : n === 12 && l.push(a >> 4 & 255), NodeBuffer$1 ? NodeBuffer$1.from ? NodeBuffer$1.from(l) : new NodeBuffer$1(l) : l;
}
function representYamlBinary$1$1(e5) {
  var t = "", n = 0, r, s, o = e5.length, a = BASE64_MAP$1$1;
  for (r = 0; r < o; r++) r % 3 === 0 && r && (t += a[n >> 18 & 63], t += a[n >> 12 & 63], t += a[n >> 6 & 63], t += a[n & 63]), n = (n << 8) + e5[r];
  return s = o % 3, s === 0 ? (t += a[n >> 18 & 63], t += a[n >> 12 & 63], t += a[n >> 6 & 63], t += a[n & 63]) : s === 2 ? (t += a[n >> 10 & 63], t += a[n >> 4 & 63], t += a[n << 2 & 63], t += a[64]) : s === 1 && (t += a[n >> 2 & 63], t += a[n << 4 & 63], t += a[64], t += a[64]), t;
}
function isBinary$1$1(e5) {
  return NodeBuffer$1 && NodeBuffer$1.isBuffer(e5);
}
var binary$1$1 = new Type$8$1("tag:yaml.org,2002:binary", { kind: "scalar", resolve: resolveYamlBinary$1$1, construct: constructYamlBinary$1$1, predicate: isBinary$1$1, represent: representYamlBinary$1$1 }), Type$7$1 = type$1$1, _hasOwnProperty$7$1 = Object.prototype.hasOwnProperty, _toString$5$1 = Object.prototype.toString;
function resolveYamlOmap$1$1(e5) {
  if (e5 === null) return true;
  var t = [], n, r, s, o, a, l = e5;
  for (n = 0, r = l.length; n < r; n += 1) {
    if (s = l[n], a = false, _toString$5$1.call(s) !== "[object Object]") return false;
    for (o in s) if (_hasOwnProperty$7$1.call(s, o)) if (!a) a = true;
    else return false;
    if (!a) return false;
    if (t.indexOf(o) === -1) t.push(o);
    else return false;
  }
  return true;
}
function constructYamlOmap$1$1(e5) {
  return e5 !== null ? e5 : [];
}
var omap$1$1 = new Type$7$1("tag:yaml.org,2002:omap", { kind: "sequence", resolve: resolveYamlOmap$1$1, construct: constructYamlOmap$1$1 }), Type$6$1 = type$1$1, _toString$4$1 = Object.prototype.toString;
function resolveYamlPairs$1$1(e5) {
  if (e5 === null) return true;
  var t, n, r, s, o, a = e5;
  for (o = new Array(a.length), t = 0, n = a.length; t < n; t += 1) {
    if (r = a[t], _toString$4$1.call(r) !== "[object Object]" || (s = Object.keys(r), s.length !== 1)) return false;
    o[t] = [s[0], r[s[0]]];
  }
  return true;
}
function constructYamlPairs$1$1(e5) {
  if (e5 === null) return [];
  var t, n, r, s, o, a = e5;
  for (o = new Array(a.length), t = 0, n = a.length; t < n; t += 1) r = a[t], s = Object.keys(r), o[t] = [s[0], r[s[0]]];
  return o;
}
var pairs$1$1 = new Type$6$1("tag:yaml.org,2002:pairs", { kind: "sequence", resolve: resolveYamlPairs$1$1, construct: constructYamlPairs$1$1 }), Type$5$1 = type$1$1, _hasOwnProperty$6$1 = Object.prototype.hasOwnProperty;
function resolveYamlSet$1$1(e5) {
  if (e5 === null) return true;
  var t, n = e5;
  for (t in n) if (_hasOwnProperty$6$1.call(n, t) && n[t] !== null) return false;
  return true;
}
function constructYamlSet$1$1(e5) {
  return e5 !== null ? e5 : {};
}
var set$1$1 = new Type$5$1("tag:yaml.org,2002:set", { kind: "mapping", resolve: resolveYamlSet$1$1, construct: constructYamlSet$1$1 }), Schema$3$1 = schema$1$2, default_safe$1 = new Schema$3$1({ include: [core$1$1], implicit: [timestamp$1$1, merge$1$1], explicit: [binary$1$1, omap$1$1, pairs$1$1, set$1$1] }), Type$4$1 = type$1$1;
function resolveJavascriptUndefined$1() {
  return true;
}
function constructJavascriptUndefined$1() {
}
function representJavascriptUndefined$1() {
  return "";
}
function isUndefined$1(e5) {
  return typeof e5 > "u";
}
var _undefined$1 = new Type$4$1("tag:yaml.org,2002:js/undefined", { kind: "scalar", resolve: resolveJavascriptUndefined$1, construct: constructJavascriptUndefined$1, predicate: isUndefined$1, represent: representJavascriptUndefined$1 }), Type$3$1 = type$1$1;
function resolveJavascriptRegExp$1(e5) {
  if (e5 === null || e5.length === 0) return false;
  var t = e5, n = /\/([gim]*)$/.exec(e5), r = "";
  return !(t[0] === "/" && (n && (r = n[1]), r.length > 3 || t[t.length - r.length - 1] !== "/"));
}
function constructJavascriptRegExp$1(e5) {
  var t = e5, n = /\/([gim]*)$/.exec(e5), r = "";
  return t[0] === "/" && (n && (r = n[1]), t = t.slice(1, t.length - r.length - 1)), new RegExp(t, r);
}
function representJavascriptRegExp$1(e5) {
  var t = "/" + e5.source + "/";
  return e5.global && (t += "g"), e5.multiline && (t += "m"), e5.ignoreCase && (t += "i"), t;
}
function isRegExp$1(e5) {
  return Object.prototype.toString.call(e5) === "[object RegExp]";
}
var regexp$1 = new Type$3$1("tag:yaml.org,2002:js/regexp", { kind: "scalar", resolve: resolveJavascriptRegExp$1, construct: constructJavascriptRegExp$1, predicate: isRegExp$1, represent: representJavascriptRegExp$1 }), esprima$1;
try {
  var _require$2 = commonjsRequire$1;
  esprima$1 = _require$2("esprima");
} catch {
}
var Type$2$1 = type$1$1;
function resolveJavascriptFunction$1(e5) {
  if (e5 === null) return false;
  try {
    var t = "(" + e5 + ")", n = esprima$1.parse(t, { range: true });
    return !(n.type !== "Program" || n.body.length !== 1 || n.body[0].type !== "ExpressionStatement" || n.body[0].expression.type !== "ArrowFunctionExpression" && n.body[0].expression.type !== "FunctionExpression");
  } catch {
    return false;
  }
}
function constructJavascriptFunction$1(e5) {
  var t = "(" + e5 + ")", n = esprima$1.parse(t, { range: true }), r = [], s;
  if (n.type !== "Program" || n.body.length !== 1 || n.body[0].type !== "ExpressionStatement" || n.body[0].expression.type !== "ArrowFunctionExpression" && n.body[0].expression.type !== "FunctionExpression") throw new Error("Failed to resolve function");
  return n.body[0].expression.params.forEach(function(o) {
    r.push(o.name);
  }), s = n.body[0].expression.body.range, n.body[0].expression.body.type === "BlockStatement" ? new Function(r, t.slice(s[0] + 1, s[1] - 1)) : new Function(r, "return " + t.slice(s[0], s[1]));
}
function representJavascriptFunction$1(e5) {
  return e5.toString();
}
function isFunction$1(e5) {
  return Object.prototype.toString.call(e5) === "[object Function]";
}
var _function$1 = new Type$2$1("tag:yaml.org,2002:js/function", { kind: "scalar", resolve: resolveJavascriptFunction$1, construct: constructJavascriptFunction$1, predicate: isFunction$1, represent: representJavascriptFunction$1 }), Schema$2$1 = schema$1$2, default_full$1 = Schema$2$1.DEFAULT = new Schema$2$1({ include: [default_safe$1], explicit: [_undefined$1, regexp$1, _function$1] }), common$2$1 = common$7$1, YAMLException$3$1 = exception$1$1, Mark$2 = mark$1, DEFAULT_SAFE_SCHEMA$1$1 = default_safe$1, DEFAULT_FULL_SCHEMA$1$1 = default_full$1, _hasOwnProperty$5$1 = Object.prototype.hasOwnProperty, CONTEXT_FLOW_IN$1$1 = 1, CONTEXT_FLOW_OUT$1$1 = 2, CONTEXT_BLOCK_IN$1$1 = 3, CONTEXT_BLOCK_OUT$1$1 = 4, CHOMPING_CLIP$1$1 = 1, CHOMPING_STRIP$1$1 = 2, CHOMPING_KEEP$1$1 = 3, PATTERN_NON_PRINTABLE$1$1 = /[\x00-\x08\x0B\x0C\x0E-\x1F\x7F-\x84\x86-\x9F\uFFFE\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF]/, PATTERN_NON_ASCII_LINE_BREAKS$1$1 = /[\x85\u2028\u2029]/, PATTERN_FLOW_INDICATORS$1$1 = /[,\[\]\{\}]/, PATTERN_TAG_HANDLE$1$1 = /^(?:!|!!|![a-z\-]+!)$/i, PATTERN_TAG_URI$1$1 = /^(?:!|[^,\[\]\{\}])(?:%[0-9a-f]{2}|[0-9a-z\-#;\/\?:@&=\+\$,_\.!~\*'\(\)\[\]])*$/i;
function _class$1$1(e5) {
  return Object.prototype.toString.call(e5);
}
function is_EOL$1$1(e5) {
  return e5 === 10 || e5 === 13;
}
function is_WHITE_SPACE$1$1(e5) {
  return e5 === 9 || e5 === 32;
}
function is_WS_OR_EOL$1$1(e5) {
  return e5 === 9 || e5 === 32 || e5 === 10 || e5 === 13;
}
function is_FLOW_INDICATOR$1$1(e5) {
  return e5 === 44 || e5 === 91 || e5 === 93 || e5 === 123 || e5 === 125;
}
function fromHexCode$1$1(e5) {
  var t;
  return 48 <= e5 && e5 <= 57 ? e5 - 48 : (t = e5 | 32, 97 <= t && t <= 102 ? t - 97 + 10 : -1);
}
function escapedHexLen$1$1(e5) {
  return e5 === 120 ? 2 : e5 === 117 ? 4 : e5 === 85 ? 8 : 0;
}
function fromDecimalCode$1$1(e5) {
  return 48 <= e5 && e5 <= 57 ? e5 - 48 : -1;
}
function simpleEscapeSequence$1$1(e5) {
  return e5 === 48 ? "\0" : e5 === 97 ? "\x07" : e5 === 98 ? "\b" : e5 === 116 || e5 === 9 ? "	" : e5 === 110 ? `
` : e5 === 118 ? "\v" : e5 === 102 ? "\f" : e5 === 114 ? "\r" : e5 === 101 ? "\x1B" : e5 === 32 ? " " : e5 === 34 ? '"' : e5 === 47 ? "/" : e5 === 92 ? "\\" : e5 === 78 ? "\x85" : e5 === 95 ? "\xA0" : e5 === 76 ? "\u2028" : e5 === 80 ? "\u2029" : "";
}
function charFromCodepoint$1$1(e5) {
  return e5 <= 65535 ? String.fromCharCode(e5) : String.fromCharCode((e5 - 65536 >> 10) + 55296, (e5 - 65536 & 1023) + 56320);
}
function setProperty$1$1(e5, t, n) {
  t === "__proto__" ? Object.defineProperty(e5, t, { configurable: true, enumerable: true, writable: true, value: n }) : e5[t] = n;
}
var simpleEscapeCheck$1$1 = new Array(256), simpleEscapeMap$1$1 = new Array(256);
for (var i$1$1 = 0; i$1$1 < 256; i$1$1++) simpleEscapeCheck$1$1[i$1$1] = simpleEscapeSequence$1$1(i$1$1) ? 1 : 0, simpleEscapeMap$1$1[i$1$1] = simpleEscapeSequence$1$1(i$1$1);
function State$3$1(e5, t) {
  this.input = e5, this.filename = t.filename || null, this.schema = t.schema || DEFAULT_FULL_SCHEMA$1$1, this.onWarning = t.onWarning || null, this.legacy = t.legacy || false, this.json = t.json || false, this.listener = t.listener || null, this.implicitTypes = this.schema.compiledImplicit, this.typeMap = this.schema.compiledTypeMap, this.length = e5.length, this.position = 0, this.line = 0, this.lineStart = 0, this.lineIndent = 0, this.documents = [];
}
function generateError$1$1(e5, t) {
  return new YAMLException$3$1(t, new Mark$2(e5.filename, e5.input, e5.position, e5.line, e5.position - e5.lineStart));
}
function throwError$1$1(e5, t) {
  throw generateError$1$1(e5, t);
}
function throwWarning$1$1(e5, t) {
  e5.onWarning && e5.onWarning.call(null, generateError$1$1(e5, t));
}
var directiveHandlers$1$1 = { YAML: function(t, n, r) {
  var s, o, a;
  t.version !== null && throwError$1$1(t, "duplication of %YAML directive"), r.length !== 1 && throwError$1$1(t, "YAML directive accepts exactly one argument"), s = /^([0-9]+)\.([0-9]+)$/.exec(r[0]), s === null && throwError$1$1(t, "ill-formed argument of the YAML directive"), o = parseInt(s[1], 10), a = parseInt(s[2], 10), o !== 1 && throwError$1$1(t, "unacceptable YAML version of the document"), t.version = r[0], t.checkLineBreaks = a < 2, a !== 1 && a !== 2 && throwWarning$1$1(t, "unsupported YAML version of the document");
}, TAG: function(t, n, r) {
  var s, o;
  r.length !== 2 && throwError$1$1(t, "TAG directive accepts exactly two arguments"), s = r[0], o = r[1], PATTERN_TAG_HANDLE$1$1.test(s) || throwError$1$1(t, "ill-formed tag handle (first argument) of the TAG directive"), _hasOwnProperty$5$1.call(t.tagMap, s) && throwError$1$1(t, 'there is a previously declared suffix for "' + s + '" tag handle'), PATTERN_TAG_URI$1$1.test(o) || throwError$1$1(t, "ill-formed tag prefix (second argument) of the TAG directive"), t.tagMap[s] = o;
} };
function captureSegment$1$1(e5, t, n, r) {
  var s, o, a, l;
  if (t < n) {
    if (l = e5.input.slice(t, n), r) for (s = 0, o = l.length; s < o; s += 1) a = l.charCodeAt(s), a === 9 || 32 <= a && a <= 1114111 || throwError$1$1(e5, "expected valid JSON character");
    else PATTERN_NON_PRINTABLE$1$1.test(l) && throwError$1$1(e5, "the stream contains non-printable characters");
    e5.result += l;
  }
}
function mergeMappings$1$1(e5, t, n, r) {
  var s, o, a, l;
  for (common$2$1.isObject(n) || throwError$1$1(e5, "cannot merge mappings; the provided source object is unacceptable"), s = Object.keys(n), a = 0, l = s.length; a < l; a += 1) o = s[a], _hasOwnProperty$5$1.call(t, o) || (setProperty$1$1(t, o, n[o]), r[o] = true);
}
function storeMappingPair$1$1(e5, t, n, r, s, o, a, l) {
  var c, u;
  if (Array.isArray(s)) for (s = Array.prototype.slice.call(s), c = 0, u = s.length; c < u; c += 1) Array.isArray(s[c]) && throwError$1$1(e5, "nested arrays are not supported inside keys"), typeof s == "object" && _class$1$1(s[c]) === "[object Object]" && (s[c] = "[object Object]");
  if (typeof s == "object" && _class$1$1(s) === "[object Object]" && (s = "[object Object]"), s = String(s), t === null && (t = {}), r === "tag:yaml.org,2002:merge") if (Array.isArray(o)) for (c = 0, u = o.length; c < u; c += 1) mergeMappings$1$1(e5, t, o[c], n);
  else mergeMappings$1$1(e5, t, o, n);
  else !e5.json && !_hasOwnProperty$5$1.call(n, s) && _hasOwnProperty$5$1.call(t, s) && (e5.line = a || e5.line, e5.position = l || e5.position, throwError$1$1(e5, "duplicated mapping key")), setProperty$1$1(t, s, o), delete n[s];
  return t;
}
function readLineBreak$1$1(e5) {
  var t;
  t = e5.input.charCodeAt(e5.position), t === 10 ? e5.position++ : t === 13 ? (e5.position++, e5.input.charCodeAt(e5.position) === 10 && e5.position++) : throwError$1$1(e5, "a line break is expected"), e5.line += 1, e5.lineStart = e5.position;
}
function skipSeparationSpace$1$1(e5, t, n) {
  for (var r = 0, s = e5.input.charCodeAt(e5.position); s !== 0; ) {
    for (; is_WHITE_SPACE$1$1(s); ) s = e5.input.charCodeAt(++e5.position);
    if (t && s === 35) do
      s = e5.input.charCodeAt(++e5.position);
    while (s !== 10 && s !== 13 && s !== 0);
    if (is_EOL$1$1(s)) for (readLineBreak$1$1(e5), s = e5.input.charCodeAt(e5.position), r++, e5.lineIndent = 0; s === 32; ) e5.lineIndent++, s = e5.input.charCodeAt(++e5.position);
    else break;
  }
  return n !== -1 && r !== 0 && e5.lineIndent < n && throwWarning$1$1(e5, "deficient indentation"), r;
}
function testDocumentSeparator$1$1(e5) {
  var t = e5.position, n;
  return n = e5.input.charCodeAt(t), !!((n === 45 || n === 46) && n === e5.input.charCodeAt(t + 1) && n === e5.input.charCodeAt(t + 2) && (t += 3, n = e5.input.charCodeAt(t), n === 0 || is_WS_OR_EOL$1$1(n)));
}
function writeFoldedLines$1$1(e5, t) {
  t === 1 ? e5.result += " " : t > 1 && (e5.result += common$2$1.repeat(`
`, t - 1));
}
function readPlainScalar$1$1(e5, t, n) {
  var r, s, o, a, l, c, u, p, f = e5.kind, h = e5.result, m;
  if (m = e5.input.charCodeAt(e5.position), is_WS_OR_EOL$1$1(m) || is_FLOW_INDICATOR$1$1(m) || m === 35 || m === 38 || m === 42 || m === 33 || m === 124 || m === 62 || m === 39 || m === 34 || m === 37 || m === 64 || m === 96 || (m === 63 || m === 45) && (s = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL$1$1(s) || n && is_FLOW_INDICATOR$1$1(s))) return false;
  for (e5.kind = "scalar", e5.result = "", o = a = e5.position, l = false; m !== 0; ) {
    if (m === 58) {
      if (s = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL$1$1(s) || n && is_FLOW_INDICATOR$1$1(s)) break;
    } else if (m === 35) {
      if (r = e5.input.charCodeAt(e5.position - 1), is_WS_OR_EOL$1$1(r)) break;
    } else {
      if (e5.position === e5.lineStart && testDocumentSeparator$1$1(e5) || n && is_FLOW_INDICATOR$1$1(m)) break;
      if (is_EOL$1$1(m)) if (c = e5.line, u = e5.lineStart, p = e5.lineIndent, skipSeparationSpace$1$1(e5, false, -1), e5.lineIndent >= t) {
        l = true, m = e5.input.charCodeAt(e5.position);
        continue;
      } else {
        e5.position = a, e5.line = c, e5.lineStart = u, e5.lineIndent = p;
        break;
      }
    }
    l && (captureSegment$1$1(e5, o, a, false), writeFoldedLines$1$1(e5, e5.line - c), o = a = e5.position, l = false), is_WHITE_SPACE$1$1(m) || (a = e5.position + 1), m = e5.input.charCodeAt(++e5.position);
  }
  return captureSegment$1$1(e5, o, a, false), e5.result ? true : (e5.kind = f, e5.result = h, false);
}
function readSingleQuotedScalar$1$1(e5, t) {
  var n, r, s;
  if (n = e5.input.charCodeAt(e5.position), n !== 39) return false;
  for (e5.kind = "scalar", e5.result = "", e5.position++, r = s = e5.position; (n = e5.input.charCodeAt(e5.position)) !== 0; ) if (n === 39) if (captureSegment$1$1(e5, r, e5.position, true), n = e5.input.charCodeAt(++e5.position), n === 39) r = e5.position, e5.position++, s = e5.position;
  else return true;
  else is_EOL$1$1(n) ? (captureSegment$1$1(e5, r, s, true), writeFoldedLines$1$1(e5, skipSeparationSpace$1$1(e5, false, t)), r = s = e5.position) : e5.position === e5.lineStart && testDocumentSeparator$1$1(e5) ? throwError$1$1(e5, "unexpected end of the document within a single quoted scalar") : (e5.position++, s = e5.position);
  throwError$1$1(e5, "unexpected end of the stream within a single quoted scalar");
}
function readDoubleQuotedScalar$1$1(e5, t) {
  var n, r, s, o, a, l;
  if (l = e5.input.charCodeAt(e5.position), l !== 34) return false;
  for (e5.kind = "scalar", e5.result = "", e5.position++, n = r = e5.position; (l = e5.input.charCodeAt(e5.position)) !== 0; ) {
    if (l === 34) return captureSegment$1$1(e5, n, e5.position, true), e5.position++, true;
    if (l === 92) {
      if (captureSegment$1$1(e5, n, e5.position, true), l = e5.input.charCodeAt(++e5.position), is_EOL$1$1(l)) skipSeparationSpace$1$1(e5, false, t);
      else if (l < 256 && simpleEscapeCheck$1$1[l]) e5.result += simpleEscapeMap$1$1[l], e5.position++;
      else if ((a = escapedHexLen$1$1(l)) > 0) {
        for (s = a, o = 0; s > 0; s--) l = e5.input.charCodeAt(++e5.position), (a = fromHexCode$1$1(l)) >= 0 ? o = (o << 4) + a : throwError$1$1(e5, "expected hexadecimal character");
        e5.result += charFromCodepoint$1$1(o), e5.position++;
      } else throwError$1$1(e5, "unknown escape sequence");
      n = r = e5.position;
    } else is_EOL$1$1(l) ? (captureSegment$1$1(e5, n, r, true), writeFoldedLines$1$1(e5, skipSeparationSpace$1$1(e5, false, t)), n = r = e5.position) : e5.position === e5.lineStart && testDocumentSeparator$1$1(e5) ? throwError$1$1(e5, "unexpected end of the document within a double quoted scalar") : (e5.position++, r = e5.position);
  }
  throwError$1$1(e5, "unexpected end of the stream within a double quoted scalar");
}
function readFlowCollection$1$1(e5, t) {
  var n = true, r, s = e5.tag, o, a = e5.anchor, l, c, u, p, f, h = {}, m, g, d, y;
  if (y = e5.input.charCodeAt(e5.position), y === 91) c = 93, f = false, o = [];
  else if (y === 123) c = 125, f = true, o = {};
  else return false;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = o), y = e5.input.charCodeAt(++e5.position); y !== 0; ) {
    if (skipSeparationSpace$1$1(e5, true, t), y = e5.input.charCodeAt(e5.position), y === c) return e5.position++, e5.tag = s, e5.anchor = a, e5.kind = f ? "mapping" : "sequence", e5.result = o, true;
    n || throwError$1$1(e5, "missed comma between flow collection entries"), g = m = d = null, u = p = false, y === 63 && (l = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL$1$1(l) && (u = p = true, e5.position++, skipSeparationSpace$1$1(e5, true, t))), r = e5.line, composeNode$1$1(e5, t, CONTEXT_FLOW_IN$1$1, false, true), g = e5.tag, m = e5.result, skipSeparationSpace$1$1(e5, true, t), y = e5.input.charCodeAt(e5.position), (p || e5.line === r) && y === 58 && (u = true, y = e5.input.charCodeAt(++e5.position), skipSeparationSpace$1$1(e5, true, t), composeNode$1$1(e5, t, CONTEXT_FLOW_IN$1$1, false, true), d = e5.result), f ? storeMappingPair$1$1(e5, o, h, g, m, d) : u ? o.push(storeMappingPair$1$1(e5, null, h, g, m, d)) : o.push(m), skipSeparationSpace$1$1(e5, true, t), y = e5.input.charCodeAt(e5.position), y === 44 ? (n = true, y = e5.input.charCodeAt(++e5.position)) : n = false;
  }
  throwError$1$1(e5, "unexpected end of the stream within a flow collection");
}
function readBlockScalar$1$1(e5, t) {
  var n, r, s = CHOMPING_CLIP$1$1, o = false, a = false, l = t, c = 0, u = false, p, f;
  if (f = e5.input.charCodeAt(e5.position), f === 124) r = false;
  else if (f === 62) r = true;
  else return false;
  for (e5.kind = "scalar", e5.result = ""; f !== 0; ) if (f = e5.input.charCodeAt(++e5.position), f === 43 || f === 45) CHOMPING_CLIP$1$1 === s ? s = f === 43 ? CHOMPING_KEEP$1$1 : CHOMPING_STRIP$1$1 : throwError$1$1(e5, "repeat of a chomping mode identifier");
  else if ((p = fromDecimalCode$1$1(f)) >= 0) p === 0 ? throwError$1$1(e5, "bad explicit indentation width of a block scalar; it cannot be less than one") : a ? throwError$1$1(e5, "repeat of an indentation width identifier") : (l = t + p - 1, a = true);
  else break;
  if (is_WHITE_SPACE$1$1(f)) {
    do
      f = e5.input.charCodeAt(++e5.position);
    while (is_WHITE_SPACE$1$1(f));
    if (f === 35) do
      f = e5.input.charCodeAt(++e5.position);
    while (!is_EOL$1$1(f) && f !== 0);
  }
  for (; f !== 0; ) {
    for (readLineBreak$1$1(e5), e5.lineIndent = 0, f = e5.input.charCodeAt(e5.position); (!a || e5.lineIndent < l) && f === 32; ) e5.lineIndent++, f = e5.input.charCodeAt(++e5.position);
    if (!a && e5.lineIndent > l && (l = e5.lineIndent), is_EOL$1$1(f)) {
      c++;
      continue;
    }
    if (e5.lineIndent < l) {
      s === CHOMPING_KEEP$1$1 ? e5.result += common$2$1.repeat(`
`, o ? 1 + c : c) : s === CHOMPING_CLIP$1$1 && o && (e5.result += `
`);
      break;
    }
    for (r ? is_WHITE_SPACE$1$1(f) ? (u = true, e5.result += common$2$1.repeat(`
`, o ? 1 + c : c)) : u ? (u = false, e5.result += common$2$1.repeat(`
`, c + 1)) : c === 0 ? o && (e5.result += " ") : e5.result += common$2$1.repeat(`
`, c) : e5.result += common$2$1.repeat(`
`, o ? 1 + c : c), o = true, a = true, c = 0, n = e5.position; !is_EOL$1$1(f) && f !== 0; ) f = e5.input.charCodeAt(++e5.position);
    captureSegment$1$1(e5, n, e5.position, false);
  }
  return true;
}
function readBlockSequence$1$1(e5, t) {
  var n, r = e5.tag, s = e5.anchor, o = [], a, l = false, c;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = o), c = e5.input.charCodeAt(e5.position); c !== 0 && !(c !== 45 || (a = e5.input.charCodeAt(e5.position + 1), !is_WS_OR_EOL$1$1(a))); ) {
    if (l = true, e5.position++, skipSeparationSpace$1$1(e5, true, -1) && e5.lineIndent <= t) {
      o.push(null), c = e5.input.charCodeAt(e5.position);
      continue;
    }
    if (n = e5.line, composeNode$1$1(e5, t, CONTEXT_BLOCK_IN$1$1, false, true), o.push(e5.result), skipSeparationSpace$1$1(e5, true, -1), c = e5.input.charCodeAt(e5.position), (e5.line === n || e5.lineIndent > t) && c !== 0) throwError$1$1(e5, "bad indentation of a sequence entry");
    else if (e5.lineIndent < t) break;
  }
  return l ? (e5.tag = r, e5.anchor = s, e5.kind = "sequence", e5.result = o, true) : false;
}
function readBlockMapping$1$1(e5, t, n) {
  var r, s, o, a, l = e5.tag, c = e5.anchor, u = {}, p = {}, f = null, h = null, m = null, g = false, d = false, y;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = u), y = e5.input.charCodeAt(e5.position); y !== 0; ) {
    if (r = e5.input.charCodeAt(e5.position + 1), o = e5.line, a = e5.position, (y === 63 || y === 58) && is_WS_OR_EOL$1$1(r)) y === 63 ? (g && (storeMappingPair$1$1(e5, u, p, f, h, null), f = h = m = null), d = true, g = true, s = true) : g ? (g = false, s = true) : throwError$1$1(e5, "incomplete explicit mapping pair; a key node is missed; or followed by a non-tabulated empty line"), e5.position += 1, y = r;
    else if (composeNode$1$1(e5, n, CONTEXT_FLOW_OUT$1$1, false, true)) if (e5.line === o) {
      for (y = e5.input.charCodeAt(e5.position); is_WHITE_SPACE$1$1(y); ) y = e5.input.charCodeAt(++e5.position);
      if (y === 58) y = e5.input.charCodeAt(++e5.position), is_WS_OR_EOL$1$1(y) || throwError$1$1(e5, "a whitespace character is expected after the key-value separator within a block mapping"), g && (storeMappingPair$1$1(e5, u, p, f, h, null), f = h = m = null), d = true, g = false, s = false, f = e5.tag, h = e5.result;
      else if (d) throwError$1$1(e5, "can not read an implicit mapping pair; a colon is missed");
      else return e5.tag = l, e5.anchor = c, true;
    } else if (d) throwError$1$1(e5, "can not read a block mapping entry; a multiline key may not be an implicit key");
    else return e5.tag = l, e5.anchor = c, true;
    else break;
    if ((e5.line === o || e5.lineIndent > t) && (composeNode$1$1(e5, t, CONTEXT_BLOCK_OUT$1$1, true, s) && (g ? h = e5.result : m = e5.result), g || (storeMappingPair$1$1(e5, u, p, f, h, m, o, a), f = h = m = null), skipSeparationSpace$1$1(e5, true, -1), y = e5.input.charCodeAt(e5.position)), e5.lineIndent > t && y !== 0) throwError$1$1(e5, "bad indentation of a mapping entry");
    else if (e5.lineIndent < t) break;
  }
  return g && storeMappingPair$1$1(e5, u, p, f, h, null), d && (e5.tag = l, e5.anchor = c, e5.kind = "mapping", e5.result = u), d;
}
function readTagProperty$1$1(e5) {
  var t, n = false, r = false, s, o, a;
  if (a = e5.input.charCodeAt(e5.position), a !== 33) return false;
  if (e5.tag !== null && throwError$1$1(e5, "duplication of a tag property"), a = e5.input.charCodeAt(++e5.position), a === 60 ? (n = true, a = e5.input.charCodeAt(++e5.position)) : a === 33 ? (r = true, s = "!!", a = e5.input.charCodeAt(++e5.position)) : s = "!", t = e5.position, n) {
    do
      a = e5.input.charCodeAt(++e5.position);
    while (a !== 0 && a !== 62);
    e5.position < e5.length ? (o = e5.input.slice(t, e5.position), a = e5.input.charCodeAt(++e5.position)) : throwError$1$1(e5, "unexpected end of the stream within a verbatim tag");
  } else {
    for (; a !== 0 && !is_WS_OR_EOL$1$1(a); ) a === 33 && (r ? throwError$1$1(e5, "tag suffix cannot contain exclamation marks") : (s = e5.input.slice(t - 1, e5.position + 1), PATTERN_TAG_HANDLE$1$1.test(s) || throwError$1$1(e5, "named tag handle cannot contain such characters"), r = true, t = e5.position + 1)), a = e5.input.charCodeAt(++e5.position);
    o = e5.input.slice(t, e5.position), PATTERN_FLOW_INDICATORS$1$1.test(o) && throwError$1$1(e5, "tag suffix cannot contain flow indicator characters");
  }
  return o && !PATTERN_TAG_URI$1$1.test(o) && throwError$1$1(e5, "tag name cannot contain such characters: " + o), n ? e5.tag = o : _hasOwnProperty$5$1.call(e5.tagMap, s) ? e5.tag = e5.tagMap[s] + o : s === "!" ? e5.tag = "!" + o : s === "!!" ? e5.tag = "tag:yaml.org,2002:" + o : throwError$1$1(e5, 'undeclared tag handle "' + s + '"'), true;
}
function readAnchorProperty$1$1(e5) {
  var t, n;
  if (n = e5.input.charCodeAt(e5.position), n !== 38) return false;
  for (e5.anchor !== null && throwError$1$1(e5, "duplication of an anchor property"), n = e5.input.charCodeAt(++e5.position), t = e5.position; n !== 0 && !is_WS_OR_EOL$1$1(n) && !is_FLOW_INDICATOR$1$1(n); ) n = e5.input.charCodeAt(++e5.position);
  return e5.position === t && throwError$1$1(e5, "name of an anchor node must contain at least one character"), e5.anchor = e5.input.slice(t, e5.position), true;
}
function readAlias$1$1(e5) {
  var t, n, r;
  if (r = e5.input.charCodeAt(e5.position), r !== 42) return false;
  for (r = e5.input.charCodeAt(++e5.position), t = e5.position; r !== 0 && !is_WS_OR_EOL$1$1(r) && !is_FLOW_INDICATOR$1$1(r); ) r = e5.input.charCodeAt(++e5.position);
  return e5.position === t && throwError$1$1(e5, "name of an alias node must contain at least one character"), n = e5.input.slice(t, e5.position), _hasOwnProperty$5$1.call(e5.anchorMap, n) || throwError$1$1(e5, 'unidentified alias "' + n + '"'), e5.result = e5.anchorMap[n], skipSeparationSpace$1$1(e5, true, -1), true;
}
function composeNode$1$1(e5, t, n, r, s) {
  var o, a, l, c = 1, u = false, p = false, f, h, m, g, d;
  if (e5.listener !== null && e5.listener("open", e5), e5.tag = null, e5.anchor = null, e5.kind = null, e5.result = null, o = a = l = CONTEXT_BLOCK_OUT$1$1 === n || CONTEXT_BLOCK_IN$1$1 === n, r && skipSeparationSpace$1$1(e5, true, -1) && (u = true, e5.lineIndent > t ? c = 1 : e5.lineIndent === t ? c = 0 : e5.lineIndent < t && (c = -1)), c === 1) for (; readTagProperty$1$1(e5) || readAnchorProperty$1$1(e5); ) skipSeparationSpace$1$1(e5, true, -1) ? (u = true, l = o, e5.lineIndent > t ? c = 1 : e5.lineIndent === t ? c = 0 : e5.lineIndent < t && (c = -1)) : l = false;
  if (l && (l = u || s), (c === 1 || CONTEXT_BLOCK_OUT$1$1 === n) && (CONTEXT_FLOW_IN$1$1 === n || CONTEXT_FLOW_OUT$1$1 === n ? g = t : g = t + 1, d = e5.position - e5.lineStart, c === 1 ? l && (readBlockSequence$1$1(e5, d) || readBlockMapping$1$1(e5, d, g)) || readFlowCollection$1$1(e5, g) ? p = true : (a && readBlockScalar$1$1(e5, g) || readSingleQuotedScalar$1$1(e5, g) || readDoubleQuotedScalar$1$1(e5, g) ? p = true : readAlias$1$1(e5) ? (p = true, (e5.tag !== null || e5.anchor !== null) && throwError$1$1(e5, "alias node should not have any properties")) : readPlainScalar$1$1(e5, g, CONTEXT_FLOW_IN$1$1 === n) && (p = true, e5.tag === null && (e5.tag = "?")), e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result)) : c === 0 && (p = l && readBlockSequence$1$1(e5, d))), e5.tag !== null && e5.tag !== "!") if (e5.tag === "?") {
    for (e5.result !== null && e5.kind !== "scalar" && throwError$1$1(e5, 'unacceptable node kind for !<?> tag; it should be "scalar", not "' + e5.kind + '"'), f = 0, h = e5.implicitTypes.length; f < h; f += 1) if (m = e5.implicitTypes[f], m.resolve(e5.result)) {
      e5.result = m.construct(e5.result), e5.tag = m.tag, e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result);
      break;
    }
  } else _hasOwnProperty$5$1.call(e5.typeMap[e5.kind || "fallback"], e5.tag) ? (m = e5.typeMap[e5.kind || "fallback"][e5.tag], e5.result !== null && m.kind !== e5.kind && throwError$1$1(e5, "unacceptable node kind for !<" + e5.tag + '> tag; it should be "' + m.kind + '", not "' + e5.kind + '"'), m.resolve(e5.result) ? (e5.result = m.construct(e5.result), e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result)) : throwError$1$1(e5, "cannot resolve a node with !<" + e5.tag + "> explicit tag")) : throwError$1$1(e5, "unknown tag !<" + e5.tag + ">");
  return e5.listener !== null && e5.listener("close", e5), e5.tag !== null || e5.anchor !== null || p;
}
function readDocument$1$1(e5) {
  var t = e5.position, n, r, s, o = false, a;
  for (e5.version = null, e5.checkLineBreaks = e5.legacy, e5.tagMap = {}, e5.anchorMap = {}; (a = e5.input.charCodeAt(e5.position)) !== 0 && (skipSeparationSpace$1$1(e5, true, -1), a = e5.input.charCodeAt(e5.position), !(e5.lineIndent > 0 || a !== 37)); ) {
    for (o = true, a = e5.input.charCodeAt(++e5.position), n = e5.position; a !== 0 && !is_WS_OR_EOL$1$1(a); ) a = e5.input.charCodeAt(++e5.position);
    for (r = e5.input.slice(n, e5.position), s = [], r.length < 1 && throwError$1$1(e5, "directive name must not be less than one character in length"); a !== 0; ) {
      for (; is_WHITE_SPACE$1$1(a); ) a = e5.input.charCodeAt(++e5.position);
      if (a === 35) {
        do
          a = e5.input.charCodeAt(++e5.position);
        while (a !== 0 && !is_EOL$1$1(a));
        break;
      }
      if (is_EOL$1$1(a)) break;
      for (n = e5.position; a !== 0 && !is_WS_OR_EOL$1$1(a); ) a = e5.input.charCodeAt(++e5.position);
      s.push(e5.input.slice(n, e5.position));
    }
    a !== 0 && readLineBreak$1$1(e5), _hasOwnProperty$5$1.call(directiveHandlers$1$1, r) ? directiveHandlers$1$1[r](e5, r, s) : throwWarning$1$1(e5, 'unknown document directive "' + r + '"');
  }
  if (skipSeparationSpace$1$1(e5, true, -1), e5.lineIndent === 0 && e5.input.charCodeAt(e5.position) === 45 && e5.input.charCodeAt(e5.position + 1) === 45 && e5.input.charCodeAt(e5.position + 2) === 45 ? (e5.position += 3, skipSeparationSpace$1$1(e5, true, -1)) : o && throwError$1$1(e5, "directives end mark is expected"), composeNode$1$1(e5, e5.lineIndent - 1, CONTEXT_BLOCK_OUT$1$1, false, true), skipSeparationSpace$1$1(e5, true, -1), e5.checkLineBreaks && PATTERN_NON_ASCII_LINE_BREAKS$1$1.test(e5.input.slice(t, e5.position)) && throwWarning$1$1(e5, "non-ASCII line breaks are interpreted as content"), e5.documents.push(e5.result), e5.position === e5.lineStart && testDocumentSeparator$1$1(e5)) {
    e5.input.charCodeAt(e5.position) === 46 && (e5.position += 3, skipSeparationSpace$1$1(e5, true, -1));
    return;
  }
  if (e5.position < e5.length - 1) throwError$1$1(e5, "end of the stream or a document separator is expected");
  else return;
}
function loadDocuments$1$1(e5, t) {
  e5 = String(e5), t = t || {}, e5.length !== 0 && (e5.charCodeAt(e5.length - 1) !== 10 && e5.charCodeAt(e5.length - 1) !== 13 && (e5 += `
`), e5.charCodeAt(0) === 65279 && (e5 = e5.slice(1)));
  var n = new State$3$1(e5, t), r = e5.indexOf("\0");
  for (r !== -1 && (n.position = r, throwError$1$1(n, "null byte is not allowed in input")), n.input += "\0"; n.input.charCodeAt(n.position) === 32; ) n.lineIndent += 1, n.position += 1;
  for (; n.position < n.length - 1; ) readDocument$1$1(n);
  return n.documents;
}
function loadAll$2$1(e5, t, n) {
  t !== null && typeof t == "object" && typeof n > "u" && (n = t, t = null);
  var r = loadDocuments$1$1(e5, n);
  if (typeof t != "function") return r;
  for (var s = 0, o = r.length; s < o; s += 1) t(r[s]);
}
function load$2$1(e5, t) {
  var n = loadDocuments$1$1(e5, t);
  if (n.length !== 0) {
    if (n.length === 1) return n[0];
    throw new YAMLException$3$1("expected a single document in the stream, but found more");
  }
}
function safeLoadAll$1$1(e5, t, n) {
  return typeof t == "object" && t !== null && typeof n > "u" && (n = t, t = null), loadAll$2$1(e5, t, common$2$1.extend({ schema: DEFAULT_SAFE_SCHEMA$1$1 }, n));
}
function safeLoad$1$1(e5, t) {
  return load$2$1(e5, common$2$1.extend({ schema: DEFAULT_SAFE_SCHEMA$1$1 }, t));
}
loader$2$1.loadAll = loadAll$2$1;
loader$2$1.load = load$2$1;
loader$2$1.safeLoadAll = safeLoadAll$1$1;
loader$2$1.safeLoad = safeLoad$1$1;
var dumper$2$1 = {}, common$1$1 = common$7$1, YAMLException$2$1 = exception$1$1, DEFAULT_FULL_SCHEMA$2 = default_full$1, DEFAULT_SAFE_SCHEMA$2 = default_safe$1, _toString$3$1 = Object.prototype.toString, _hasOwnProperty$4$1 = Object.prototype.hasOwnProperty, CHAR_TAB$1$1 = 9, CHAR_LINE_FEED$1$1 = 10, CHAR_CARRIAGE_RETURN$1$1 = 13, CHAR_SPACE$1$1 = 32, CHAR_EXCLAMATION$1$1 = 33, CHAR_DOUBLE_QUOTE$1$1 = 34, CHAR_SHARP$1$1 = 35, CHAR_PERCENT$1$1 = 37, CHAR_AMPERSAND$1$1 = 38, CHAR_SINGLE_QUOTE$1$1 = 39, CHAR_ASTERISK$1$1 = 42, CHAR_COMMA$1$1 = 44, CHAR_MINUS$1$1 = 45, CHAR_COLON$1$1 = 58, CHAR_EQUALS$1$1 = 61, CHAR_GREATER_THAN$1$1 = 62, CHAR_QUESTION$1$1 = 63, CHAR_COMMERCIAL_AT$1$1 = 64, CHAR_LEFT_SQUARE_BRACKET$1$1 = 91, CHAR_RIGHT_SQUARE_BRACKET$1$1 = 93, CHAR_GRAVE_ACCENT$1$1 = 96, CHAR_LEFT_CURLY_BRACKET$1$1 = 123, CHAR_VERTICAL_LINE$1$1 = 124, CHAR_RIGHT_CURLY_BRACKET$1$1 = 125, ESCAPE_SEQUENCES$1$1 = {};
ESCAPE_SEQUENCES$1$1[0] = "\\0";
ESCAPE_SEQUENCES$1$1[7] = "\\a";
ESCAPE_SEQUENCES$1$1[8] = "\\b";
ESCAPE_SEQUENCES$1$1[9] = "\\t";
ESCAPE_SEQUENCES$1$1[10] = "\\n";
ESCAPE_SEQUENCES$1$1[11] = "\\v";
ESCAPE_SEQUENCES$1$1[12] = "\\f";
ESCAPE_SEQUENCES$1$1[13] = "\\r";
ESCAPE_SEQUENCES$1$1[27] = "\\e";
ESCAPE_SEQUENCES$1$1[34] = '\\"';
ESCAPE_SEQUENCES$1$1[92] = "\\\\";
ESCAPE_SEQUENCES$1$1[133] = "\\N";
ESCAPE_SEQUENCES$1$1[160] = "\\_";
ESCAPE_SEQUENCES$1$1[8232] = "\\L";
ESCAPE_SEQUENCES$1$1[8233] = "\\P";
var DEPRECATED_BOOLEANS_SYNTAX$1$1 = ["y", "Y", "yes", "Yes", "YES", "on", "On", "ON", "n", "N", "no", "No", "NO", "off", "Off", "OFF"];
function compileStyleMap$1$1(e5, t) {
  var n, r, s, o, a, l, c;
  if (t === null) return {};
  for (n = {}, r = Object.keys(t), s = 0, o = r.length; s < o; s += 1) a = r[s], l = String(t[a]), a.slice(0, 2) === "!!" && (a = "tag:yaml.org,2002:" + a.slice(2)), c = e5.compiledTypeMap.fallback[a], c && _hasOwnProperty$4$1.call(c.styleAliases, l) && (l = c.styleAliases[l]), n[a] = l;
  return n;
}
function encodeHex$1$1(e5) {
  var t, n, r;
  if (t = e5.toString(16).toUpperCase(), e5 <= 255) n = "x", r = 2;
  else if (e5 <= 65535) n = "u", r = 4;
  else if (e5 <= 4294967295) n = "U", r = 8;
  else throw new YAMLException$2$1("code point within a string may not be greater than 0xFFFFFFFF");
  return "\\" + n + common$1$1.repeat("0", r - t.length) + t;
}
function State$2$1(e5) {
  this.schema = e5.schema || DEFAULT_FULL_SCHEMA$2, this.indent = Math.max(1, e5.indent || 2), this.noArrayIndent = e5.noArrayIndent || false, this.skipInvalid = e5.skipInvalid || false, this.flowLevel = common$1$1.isNothing(e5.flowLevel) ? -1 : e5.flowLevel, this.styleMap = compileStyleMap$1$1(this.schema, e5.styles || null), this.sortKeys = e5.sortKeys || false, this.lineWidth = e5.lineWidth || 80, this.noRefs = e5.noRefs || false, this.noCompatMode = e5.noCompatMode || false, this.condenseFlow = e5.condenseFlow || false, this.implicitTypes = this.schema.compiledImplicit, this.explicitTypes = this.schema.compiledExplicit, this.tag = null, this.result = "", this.duplicates = [], this.usedDuplicates = null;
}
function indentString$1$1(e5, t) {
  for (var n = common$1$1.repeat(" ", t), r = 0, s = -1, o = "", a, l = e5.length; r < l; ) s = e5.indexOf(`
`, r), s === -1 ? (a = e5.slice(r), r = l) : (a = e5.slice(r, s + 1), r = s + 1), a.length && a !== `
` && (o += n), o += a;
  return o;
}
function generateNextLine$1$1(e5, t) {
  return `
` + common$1$1.repeat(" ", e5.indent * t);
}
function testImplicitResolving$1$1(e5, t) {
  var n, r, s;
  for (n = 0, r = e5.implicitTypes.length; n < r; n += 1) if (s = e5.implicitTypes[n], s.resolve(t)) return true;
  return false;
}
function isWhitespace$1$1(e5) {
  return e5 === CHAR_SPACE$1$1 || e5 === CHAR_TAB$1$1;
}
function isPrintable$1$1(e5) {
  return 32 <= e5 && e5 <= 126 || 161 <= e5 && e5 <= 55295 && e5 !== 8232 && e5 !== 8233 || 57344 <= e5 && e5 <= 65533 && e5 !== 65279 || 65536 <= e5 && e5 <= 1114111;
}
function isNsChar$1(e5) {
  return isPrintable$1$1(e5) && !isWhitespace$1$1(e5) && e5 !== 65279 && e5 !== CHAR_CARRIAGE_RETURN$1$1 && e5 !== CHAR_LINE_FEED$1$1;
}
function isPlainSafe$1$1(e5, t) {
  return isPrintable$1$1(e5) && e5 !== 65279 && e5 !== CHAR_COMMA$1$1 && e5 !== CHAR_LEFT_SQUARE_BRACKET$1$1 && e5 !== CHAR_RIGHT_SQUARE_BRACKET$1$1 && e5 !== CHAR_LEFT_CURLY_BRACKET$1$1 && e5 !== CHAR_RIGHT_CURLY_BRACKET$1$1 && e5 !== CHAR_COLON$1$1 && (e5 !== CHAR_SHARP$1$1 || t && isNsChar$1(t));
}
function isPlainSafeFirst$1$1(e5) {
  return isPrintable$1$1(e5) && e5 !== 65279 && !isWhitespace$1$1(e5) && e5 !== CHAR_MINUS$1$1 && e5 !== CHAR_QUESTION$1$1 && e5 !== CHAR_COLON$1$1 && e5 !== CHAR_COMMA$1$1 && e5 !== CHAR_LEFT_SQUARE_BRACKET$1$1 && e5 !== CHAR_RIGHT_SQUARE_BRACKET$1$1 && e5 !== CHAR_LEFT_CURLY_BRACKET$1$1 && e5 !== CHAR_RIGHT_CURLY_BRACKET$1$1 && e5 !== CHAR_SHARP$1$1 && e5 !== CHAR_AMPERSAND$1$1 && e5 !== CHAR_ASTERISK$1$1 && e5 !== CHAR_EXCLAMATION$1$1 && e5 !== CHAR_VERTICAL_LINE$1$1 && e5 !== CHAR_EQUALS$1$1 && e5 !== CHAR_GREATER_THAN$1$1 && e5 !== CHAR_SINGLE_QUOTE$1$1 && e5 !== CHAR_DOUBLE_QUOTE$1$1 && e5 !== CHAR_PERCENT$1$1 && e5 !== CHAR_COMMERCIAL_AT$1$1 && e5 !== CHAR_GRAVE_ACCENT$1$1;
}
function needIndentIndicator$1$1(e5) {
  var t = /^\n* /;
  return t.test(e5);
}
var STYLE_PLAIN$1$1 = 1, STYLE_SINGLE$1$1 = 2, STYLE_LITERAL$1$1 = 3, STYLE_FOLDED$1$1 = 4, STYLE_DOUBLE$1$1 = 5;
function chooseScalarStyle$1$1(e5, t, n, r, s) {
  var o, a, l, c = false, u = false, p = r !== -1, f = -1, h = isPlainSafeFirst$1$1(e5.charCodeAt(0)) && !isWhitespace$1$1(e5.charCodeAt(e5.length - 1));
  if (t) for (o = 0; o < e5.length; o++) {
    if (a = e5.charCodeAt(o), !isPrintable$1$1(a)) return STYLE_DOUBLE$1$1;
    l = o > 0 ? e5.charCodeAt(o - 1) : null, h = h && isPlainSafe$1$1(a, l);
  }
  else {
    for (o = 0; o < e5.length; o++) {
      if (a = e5.charCodeAt(o), a === CHAR_LINE_FEED$1$1) c = true, p && (u = u || o - f - 1 > r && e5[f + 1] !== " ", f = o);
      else if (!isPrintable$1$1(a)) return STYLE_DOUBLE$1$1;
      l = o > 0 ? e5.charCodeAt(o - 1) : null, h = h && isPlainSafe$1$1(a, l);
    }
    u = u || p && o - f - 1 > r && e5[f + 1] !== " ";
  }
  return !c && !u ? h && !s(e5) ? STYLE_PLAIN$1$1 : STYLE_SINGLE$1$1 : n > 9 && needIndentIndicator$1$1(e5) ? STYLE_DOUBLE$1$1 : u ? STYLE_FOLDED$1$1 : STYLE_LITERAL$1$1;
}
function writeScalar$1$1(e5, t, n, r) {
  e5.dump = (function() {
    if (t.length === 0) return "''";
    if (!e5.noCompatMode && DEPRECATED_BOOLEANS_SYNTAX$1$1.indexOf(t) !== -1) return "'" + t + "'";
    var s = e5.indent * Math.max(1, n), o = e5.lineWidth === -1 ? -1 : Math.max(Math.min(e5.lineWidth, 40), e5.lineWidth - s), a = r || e5.flowLevel > -1 && n >= e5.flowLevel;
    function l(c) {
      return testImplicitResolving$1$1(e5, c);
    }
    switch (chooseScalarStyle$1$1(t, a, e5.indent, o, l)) {
      case STYLE_PLAIN$1$1:
        return t;
      case STYLE_SINGLE$1$1:
        return "'" + t.replace(/'/g, "''") + "'";
      case STYLE_LITERAL$1$1:
        return "|" + blockHeader$1$1(t, e5.indent) + dropEndingNewline$1$1(indentString$1$1(t, s));
      case STYLE_FOLDED$1$1:
        return ">" + blockHeader$1$1(t, e5.indent) + dropEndingNewline$1$1(indentString$1$1(foldString$1$1(t, o), s));
      case STYLE_DOUBLE$1$1:
        return '"' + escapeString$1$1(t) + '"';
      default:
        throw new YAMLException$2$1("impossible error: invalid scalar style");
    }
  })();
}
function blockHeader$1$1(e5, t) {
  var n = needIndentIndicator$1$1(e5) ? String(t) : "", r = e5[e5.length - 1] === `
`, s = r && (e5[e5.length - 2] === `
` || e5 === `
`), o = s ? "+" : r ? "" : "-";
  return n + o + `
`;
}
function dropEndingNewline$1$1(e5) {
  return e5[e5.length - 1] === `
` ? e5.slice(0, -1) : e5;
}
function foldString$1$1(e5, t) {
  for (var n = /(\n+)([^\n]*)/g, r = (function() {
    var u = e5.indexOf(`
`);
    return u = u !== -1 ? u : e5.length, n.lastIndex = u, foldLine$1$1(e5.slice(0, u), t);
  })(), s = e5[0] === `
` || e5[0] === " ", o, a; a = n.exec(e5); ) {
    var l = a[1], c = a[2];
    o = c[0] === " ", r += l + (!s && !o && c !== "" ? `
` : "") + foldLine$1$1(c, t), s = o;
  }
  return r;
}
function foldLine$1$1(e5, t) {
  if (e5 === "" || e5[0] === " ") return e5;
  for (var n = / [^ ]/g, r, s = 0, o, a = 0, l = 0, c = ""; r = n.exec(e5); ) l = r.index, l - s > t && (o = a > s ? a : l, c += `
` + e5.slice(s, o), s = o + 1), a = l;
  return c += `
`, e5.length - s > t && a > s ? c += e5.slice(s, a) + `
` + e5.slice(a + 1) : c += e5.slice(s), c.slice(1);
}
function escapeString$1$1(e5) {
  for (var t = "", n, r, s, o = 0; o < e5.length; o++) {
    if (n = e5.charCodeAt(o), n >= 55296 && n <= 56319 && (r = e5.charCodeAt(o + 1), r >= 56320 && r <= 57343)) {
      t += encodeHex$1$1((n - 55296) * 1024 + r - 56320 + 65536), o++;
      continue;
    }
    s = ESCAPE_SEQUENCES$1$1[n], t += !s && isPrintable$1$1(n) ? e5[o] : s || encodeHex$1$1(n);
  }
  return t;
}
function writeFlowSequence$1$1(e5, t, n) {
  var r = "", s = e5.tag, o, a;
  for (o = 0, a = n.length; o < a; o += 1) writeNode$1$1(e5, t, n[o], false, false) && (o !== 0 && (r += "," + (e5.condenseFlow ? "" : " ")), r += e5.dump);
  e5.tag = s, e5.dump = "[" + r + "]";
}
function writeBlockSequence$1$1(e5, t, n, r) {
  var s = "", o = e5.tag, a, l;
  for (a = 0, l = n.length; a < l; a += 1) writeNode$1$1(e5, t + 1, n[a], true, true) && ((!r || a !== 0) && (s += generateNextLine$1$1(e5, t)), e5.dump && CHAR_LINE_FEED$1$1 === e5.dump.charCodeAt(0) ? s += "-" : s += "- ", s += e5.dump);
  e5.tag = o, e5.dump = s || "[]";
}
function writeFlowMapping$1$1(e5, t, n) {
  var r = "", s = e5.tag, o = Object.keys(n), a, l, c, u, p;
  for (a = 0, l = o.length; a < l; a += 1) p = "", a !== 0 && (p += ", "), e5.condenseFlow && (p += '"'), c = o[a], u = n[c], writeNode$1$1(e5, t, c, false, false) && (e5.dump.length > 1024 && (p += "? "), p += e5.dump + (e5.condenseFlow ? '"' : "") + ":" + (e5.condenseFlow ? "" : " "), writeNode$1$1(e5, t, u, false, false) && (p += e5.dump, r += p));
  e5.tag = s, e5.dump = "{" + r + "}";
}
function writeBlockMapping$1$1(e5, t, n, r) {
  var s = "", o = e5.tag, a = Object.keys(n), l, c, u, p, f, h;
  if (e5.sortKeys === true) a.sort();
  else if (typeof e5.sortKeys == "function") a.sort(e5.sortKeys);
  else if (e5.sortKeys) throw new YAMLException$2$1("sortKeys must be a boolean or a function");
  for (l = 0, c = a.length; l < c; l += 1) h = "", (!r || l !== 0) && (h += generateNextLine$1$1(e5, t)), u = a[l], p = n[u], writeNode$1$1(e5, t + 1, u, true, true, true) && (f = e5.tag !== null && e5.tag !== "?" || e5.dump && e5.dump.length > 1024, f && (e5.dump && CHAR_LINE_FEED$1$1 === e5.dump.charCodeAt(0) ? h += "?" : h += "? "), h += e5.dump, f && (h += generateNextLine$1$1(e5, t)), writeNode$1$1(e5, t + 1, p, true, f) && (e5.dump && CHAR_LINE_FEED$1$1 === e5.dump.charCodeAt(0) ? h += ":" : h += ": ", h += e5.dump, s += h));
  e5.tag = o, e5.dump = s || "{}";
}
function detectType$1$1(e5, t, n) {
  var r, s, o, a, l, c;
  for (s = n ? e5.explicitTypes : e5.implicitTypes, o = 0, a = s.length; o < a; o += 1) if (l = s[o], (l.instanceOf || l.predicate) && (!l.instanceOf || typeof t == "object" && t instanceof l.instanceOf) && (!l.predicate || l.predicate(t))) {
    if (e5.tag = n ? l.tag : "?", l.represent) {
      if (c = e5.styleMap[l.tag] || l.defaultStyle, _toString$3$1.call(l.represent) === "[object Function]") r = l.represent(t, c);
      else if (_hasOwnProperty$4$1.call(l.represent, c)) r = l.represent[c](t, c);
      else throw new YAMLException$2$1("!<" + l.tag + '> tag resolver accepts not "' + c + '" style');
      e5.dump = r;
    }
    return true;
  }
  return false;
}
function writeNode$1$1(e5, t, n, r, s, o) {
  e5.tag = null, e5.dump = n, detectType$1$1(e5, n, false) || detectType$1$1(e5, n, true);
  var a = _toString$3$1.call(e5.dump);
  r && (r = e5.flowLevel < 0 || e5.flowLevel > t);
  var l = a === "[object Object]" || a === "[object Array]", c, u;
  if (l && (c = e5.duplicates.indexOf(n), u = c !== -1), (e5.tag !== null && e5.tag !== "?" || u || e5.indent !== 2 && t > 0) && (s = false), u && e5.usedDuplicates[c]) e5.dump = "*ref_" + c;
  else {
    if (l && u && !e5.usedDuplicates[c] && (e5.usedDuplicates[c] = true), a === "[object Object]") r && Object.keys(e5.dump).length !== 0 ? (writeBlockMapping$1$1(e5, t, e5.dump, s), u && (e5.dump = "&ref_" + c + e5.dump)) : (writeFlowMapping$1$1(e5, t, e5.dump), u && (e5.dump = "&ref_" + c + " " + e5.dump));
    else if (a === "[object Array]") {
      var p = e5.noArrayIndent && t > 0 ? t - 1 : t;
      r && e5.dump.length !== 0 ? (writeBlockSequence$1$1(e5, p, e5.dump, s), u && (e5.dump = "&ref_" + c + e5.dump)) : (writeFlowSequence$1$1(e5, p, e5.dump), u && (e5.dump = "&ref_" + c + " " + e5.dump));
    } else if (a === "[object String]") e5.tag !== "?" && writeScalar$1$1(e5, e5.dump, t, o);
    else {
      if (e5.skipInvalid) return false;
      throw new YAMLException$2$1("unacceptable kind of an object to dump " + a);
    }
    e5.tag !== null && e5.tag !== "?" && (e5.dump = "!<" + e5.tag + "> " + e5.dump);
  }
  return true;
}
function getDuplicateReferences$1$1(e5, t) {
  var n = [], r = [], s, o;
  for (inspectNode$1$1(e5, n, r), s = 0, o = r.length; s < o; s += 1) t.duplicates.push(n[r[s]]);
  t.usedDuplicates = new Array(o);
}
function inspectNode$1$1(e5, t, n) {
  var r, s, o;
  if (e5 !== null && typeof e5 == "object") if (s = t.indexOf(e5), s !== -1) n.indexOf(s) === -1 && n.push(s);
  else if (t.push(e5), Array.isArray(e5)) for (s = 0, o = e5.length; s < o; s += 1) inspectNode$1$1(e5[s], t, n);
  else for (r = Object.keys(e5), s = 0, o = r.length; s < o; s += 1) inspectNode$1$1(e5[r[s]], t, n);
}
function dump$2$1(e5, t) {
  t = t || {};
  var n = new State$2$1(t);
  return n.noRefs || getDuplicateReferences$1$1(e5, n), writeNode$1$1(n, 0, e5, true, true) ? n.dump + `
` : "";
}
function safeDump$1$1(e5, t) {
  return dump$2$1(e5, common$1$1.extend({ schema: DEFAULT_SAFE_SCHEMA$2 }, t));
}
dumper$2$1.dump = dump$2$1;
dumper$2$1.safeDump = safeDump$1$1;
var loader$1$1 = loader$2$1, dumper$1$1 = dumper$2$1;
function deprecated$1(e5) {
  return function() {
    throw new Error("Function " + e5 + " is deprecated and cannot be used.");
  };
}
jsYaml$2$1.Type = type$1$1;
jsYaml$2$1.Schema = schema$1$2;
jsYaml$2$1.FAILSAFE_SCHEMA = failsafe$1$1;
jsYaml$2$1.JSON_SCHEMA = json$1$1;
jsYaml$2$1.CORE_SCHEMA = core$1$1;
jsYaml$2$1.DEFAULT_SAFE_SCHEMA = default_safe$1;
jsYaml$2$1.DEFAULT_FULL_SCHEMA = default_full$1;
jsYaml$2$1.load = loader$1$1.load;
jsYaml$2$1.loadAll = loader$1$1.loadAll;
jsYaml$2$1.safeLoad = loader$1$1.safeLoad;
jsYaml$2$1.safeLoadAll = loader$1$1.safeLoadAll;
jsYaml$2$1.dump = dumper$1$1.dump;
jsYaml$2$1.safeDump = dumper$1$1.safeDump;
jsYaml$2$1.YAMLException = exception$1$1;
jsYaml$2$1.MINIMAL_SCHEMA = failsafe$1$1;
jsYaml$2$1.SAFE_SCHEMA = default_safe$1;
jsYaml$2$1.DEFAULT_SCHEMA = default_full$1;
jsYaml$2$1.scan = deprecated$1("scan");
jsYaml$2$1.parse = deprecated$1("parse");
jsYaml$2$1.compose = deprecated$1("compose");
jsYaml$2$1.addConstructor = deprecated$1("addConstructor");
var yaml$1 = jsYaml$2$1, jsYaml$1$1 = yaml$1;
(function(module, exports) {
  const yaml = jsYaml$1$1, engines = module.exports;
  engines.yaml = { parse: yaml.safeLoad.bind(yaml), stringify: yaml.safeDump.bind(yaml) }, engines.json = { parse: JSON.parse.bind(JSON), stringify: function(e5, t) {
    const n = Object.assign({ replacer: null, space: 2 }, t);
    return JSON.stringify(e5, n.replacer, n.space);
  } }, engines.javascript = { parse: function parse(str, options, wrap) {
    try {
      return wrap !== false && (str = `(function() {
return ` + str.trim() + `;
}());`), eval(str) || {};
    } catch (e5) {
      if (wrap !== false && /(unexpected|identifier)/i.test(e5.message)) return parse(str, options, false);
      throw new SyntaxError(e5);
    }
  }, stringify: function() {
    throw new Error("stringifying JavaScript is not supported");
  } };
})(engines$2$1);
var enginesExports$1 = engines$2$1.exports, utils$3$1 = {};
/*!
* strip-bom-string <https://github.com/jonschlinkert/strip-bom-string>
*
* Copyright (c) 2015, 2017, Jon Schlinkert.
* Released under the MIT License.
*/
var stripBomString$1 = function(e5) {
  return typeof e5 == "string" && e5.charAt(0) === "\uFEFF" ? e5.slice(1) : e5;
};
(function(e5) {
  const t = stripBomString$1, n = kindOf$1;
  e5.define = function(r, s, o) {
    Reflect.defineProperty(r, s, { enumerable: false, configurable: true, writable: true, value: o });
  }, e5.isBuffer = function(r) {
    return n(r) === "buffer";
  }, e5.isObject = function(r) {
    return n(r) === "object";
  }, e5.toBuffer = function(r) {
    return typeof r == "string" ? Buffer.from(r) : r;
  }, e5.toString = function(r) {
    if (e5.isBuffer(r)) return t(String(r));
    if (typeof r != "string") throw new TypeError("expected input to be a string or buffer");
    return t(r);
  }, e5.arrayify = function(r) {
    return r ? Array.isArray(r) ? r : [r] : [];
  }, e5.startsWith = function(r, s, o) {
    return typeof o != "number" && (o = s.length), r.slice(0, o) === s;
  };
})(utils$3$1);
const engines$1$1 = enginesExports$1, utils$2$1 = utils$3$1;
var defaults$4$1 = function(e5) {
  const t = Object.assign({}, e5);
  return t.delimiters = utils$2$1.arrayify(t.delims || t.delimiters || "---"), t.delimiters.length === 1 && t.delimiters.push(t.delimiters[0]), t.language = (t.language || t.lang || "yaml").toLowerCase(), t.engines = Object.assign({}, engines$1$1, t.parsers, t.engines), t;
}, engine$1 = function(e5, t) {
  let n = t.engines[e5] || t.engines[aliase$1(e5)];
  if (typeof n > "u") throw new Error('gray-matter engine "' + e5 + '" is not registered');
  return typeof n == "function" && (n = { parse: n }), n;
};
function aliase$1(e5) {
  switch (e5.toLowerCase()) {
    case "js":
    case "javascript":
      return "javascript";
    case "coffee":
    case "coffeescript":
    case "cson":
      return "coffee";
    case "yaml":
    case "yml":
      return "yaml";
    default:
      return e5;
  }
}
const typeOf$1$1 = kindOf$1, getEngine$1$1 = engine$1, defaults$3$1 = defaults$4$1;
var stringify$2$1 = function(e5, t, n) {
  if (t == null && n == null) switch (typeOf$1$1(e5)) {
    case "object":
      t = e5.data, n = {};
      break;
    case "string":
      return e5;
    default:
      throw new TypeError("expected file to be a string or object");
  }
  const r = e5.content, s = defaults$3$1(n);
  if (t == null) {
    if (!s.data) return e5;
    t = s.data;
  }
  const o = e5.language || s.language, a = getEngine$1$1(o, s);
  if (typeof a.stringify != "function") throw new TypeError('expected "' + o + '.stringify" to be a function');
  t = Object.assign({}, e5.data, t);
  const l = s.delimiters[0], c = s.delimiters[1], u = a.stringify(t, n).trim();
  let p = "";
  return u !== "{}" && (p = newline$1(l) + newline$1(u) + newline$1(c)), typeof e5.excerpt == "string" && e5.excerpt !== "" && r.indexOf(e5.excerpt.trim()) === -1 && (p += newline$1(e5.excerpt) + newline$1(c)), p + newline$1(r);
};
function newline$1(e5) {
  return e5.slice(-1) !== `
` ? e5 + `
` : e5;
}
const defaults$2$1 = defaults$4$1;
var excerpt$1$1 = function(e5, t) {
  const n = defaults$2$1(t);
  if (e5.data == null && (e5.data = {}), typeof n.excerpt == "function") return n.excerpt(e5, n);
  const r = e5.data.excerpt_separator || n.excerpt_separator;
  if (r == null && (n.excerpt === false || n.excerpt == null)) return e5;
  const s = typeof n.excerpt == "string" ? n.excerpt : r || n.delimiters[0], o = e5.content.indexOf(s);
  return o !== -1 && (e5.excerpt = e5.content.slice(0, o)), e5;
};
const typeOf$3 = kindOf$1, stringify$1$1 = stringify$2$1, utils$1$1 = utils$3$1;
var toFile$1$1 = function(e5) {
  return typeOf$3(e5) !== "object" && (e5 = { content: e5 }), typeOf$3(e5.data) !== "object" && (e5.data = {}), e5.contents && e5.content == null && (e5.content = e5.contents), utils$1$1.define(e5, "orig", utils$1$1.toBuffer(e5.content)), utils$1$1.define(e5, "language", e5.language || ""), utils$1$1.define(e5, "matter", e5.matter || ""), utils$1$1.define(e5, "stringify", function(t, n) {
    return n && n.language && (e5.language = n.language), stringify$1$1(e5, t, n);
  }), e5.content = utils$1$1.toString(e5.content), e5.isEmpty = false, e5.excerpt = "", e5;
};
const getEngine$2 = engine$1, defaults$1$1 = defaults$4$1;
var parse$1$1 = function(e5, t, n) {
  const r = defaults$1$1(n), s = getEngine$2(e5, r);
  if (typeof s.parse != "function") throw new TypeError('expected "' + e5 + '.parse" to be a function');
  return s.parse(t, r);
};
const fs$3 = require$$0$2, sections$1 = sectionMatter$1, defaults$6 = defaults$4$1, stringify$c = stringify$2$1, excerpt$2 = excerpt$1$1, engines$3 = enginesExports$1, toFile$2 = toFile$1$1, parse$3 = parse$1$1, utils$4 = utils$3$1;
function matter$2(e5, t) {
  if (e5 === "") return { data: {}, content: e5, excerpt: "", orig: e5 };
  let n = toFile$2(e5);
  const r = matter$2.cache[n.content];
  if (!t) {
    if (r) return n = Object.assign({}, r), n.orig = r.orig, n;
    matter$2.cache[n.content] = n;
  }
  return parseMatter$1(n, t);
}
function parseMatter$1(e5, t) {
  const n = defaults$6(t), r = n.delimiters[0], s = `
` + n.delimiters[1];
  let o = e5.content;
  n.language && (e5.language = n.language);
  const a = r.length;
  if (!utils$4.startsWith(o, r, a)) return excerpt$2(e5, n), e5;
  if (o.charAt(a) === r.slice(-1)) return e5;
  o = o.slice(a);
  const l = o.length, c = matter$2.language(o, n);
  c.name && (e5.language = c.name, o = o.slice(c.raw.length));
  let u = o.indexOf(s);
  return u === -1 && (u = l), e5.matter = o.slice(0, u), e5.matter.replace(/^\s*#[^\n]+/gm, "").trim() === "" ? (e5.isEmpty = true, e5.empty = e5.content, e5.data = {}) : e5.data = parse$3(e5.language, e5.matter, n), u === l ? e5.content = "" : (e5.content = o.slice(u + s.length), e5.content[0] === "\r" && (e5.content = e5.content.slice(1)), e5.content[0] === `
` && (e5.content = e5.content.slice(1))), excerpt$2(e5, n), (n.sections === true || typeof n.section == "function") && sections$1(e5, n.section), e5;
}
matter$2.engines = engines$3;
matter$2.stringify = function(e5, t, n) {
  return typeof e5 == "string" && (e5 = matter$2(e5, n)), stringify$c(e5, t, n);
};
matter$2.read = function(e5, t) {
  const n = fs$3.readFileSync(e5, "utf8"), r = matter$2(n, t);
  return r.path = e5, r;
};
matter$2.test = function(e5, t) {
  return utils$4.startsWith(e5, defaults$6(t).delimiters[0]);
};
matter$2.language = function(e5, t) {
  const r = defaults$6(t).delimiters[0];
  matter$2.test(e5) && (e5 = e5.slice(r.length));
  const s = e5.slice(0, e5.search(/\r?\n/));
  return { raw: s, name: s ? s.trim() : "" };
};
matter$2.cache = {};
matter$2.clearCache = function() {
  matter$2.cache = {};
};
var grayMatter$1 = matter$2;
const matter$1$1 = Hm$1(grayMatter$1), markdownSerializer$1 = { format: "markdown", async serializeDocumentFileContents(e5, t) {
  const n = (e5 == null ? void 0 : e5.body) || "", r = { ...e5 };
  return delete r.body, matter$1$1.stringify(n, r);
}, async deserializeDocumentFileContents(e5, t) {
  const n = matter$1$1(e5);
  return { ...n.data, body: n.content };
} }, rawSerializer$1 = { format: "raw", async serializeDocumentFileContents(e5, t) {
  var _a4;
  return "" + ((_a4 = e5.body) != null ? _a4 : "");
}, async deserializeDocumentFileContents(e5, t) {
  return { body: e5 };
} };
/*! js-yaml 4.1.1 https://github.com/nodeca/js-yaml @license MIT */
function isNothing$2(e5) {
  return typeof e5 > "u" || e5 === null;
}
function isObject$3(e5) {
  return typeof e5 == "object" && e5 !== null;
}
function toArray$2(e5) {
  return Array.isArray(e5) ? e5 : isNothing$2(e5) ? [] : [e5];
}
function extend$3(e5, t) {
  var n, r, s, o;
  if (t) for (o = Object.keys(t), n = 0, r = o.length; n < r; n += 1) s = o[n], e5[s] = t[s];
  return e5;
}
function repeat$2(e5, t) {
  var n = "", r;
  for (r = 0; r < t; r += 1) n += e5;
  return n;
}
function isNegativeZero$2(e5) {
  return e5 === 0 && Number.NEGATIVE_INFINITY === 1 / e5;
}
var isNothing_1$1 = isNothing$2, isObject_1$1 = isObject$3, toArray_1$1 = toArray$2, repeat_1$1 = repeat$2, isNegativeZero_1$1 = isNegativeZero$2, extend_1$1 = extend$3, common$8 = { isNothing: isNothing_1$1, isObject: isObject_1$1, toArray: toArray_1$1, repeat: repeat_1$1, isNegativeZero: isNegativeZero_1$1, extend: extend_1$1 };
function formatError$1(e5, t) {
  var n = "", r = e5.reason || "(unknown reason)";
  return e5.mark ? (e5.mark.name && (n += 'in "' + e5.mark.name + '" '), n += "(" + (e5.mark.line + 1) + ":" + (e5.mark.column + 1) + ")", !t && e5.mark.snippet && (n += `

` + e5.mark.snippet), r + " " + n) : r;
}
function YAMLException$1$1(e5, t) {
  Error.call(this), this.name = "YAMLException", this.reason = e5, this.mark = t, this.message = formatError$1(this, false), Error.captureStackTrace ? Error.captureStackTrace(this, this.constructor) : this.stack = new Error().stack || "";
}
YAMLException$1$1.prototype = Object.create(Error.prototype);
YAMLException$1$1.prototype.constructor = YAMLException$1$1;
YAMLException$1$1.prototype.toString = function e(t) {
  return this.name + ": " + formatError$1(this, t);
};
var exception$2 = YAMLException$1$1;
function getLine$1(e5, t, n, r, s) {
  var o = "", a = "", l = Math.floor(s / 2) - 1;
  return r - t > l && (o = " ... ", t = r - l + o.length), n - r > l && (a = " ...", n = r + l - a.length), { str: o + e5.slice(t, n).replace(/\t/g, "\u2192") + a, pos: r - t + o.length };
}
function padStart$1(e5, t) {
  return common$8.repeat(" ", t - e5.length) + e5;
}
function makeSnippet$1(e5, t) {
  if (t = Object.create(t || null), !e5.buffer) return null;
  t.maxLength || (t.maxLength = 79), typeof t.indent != "number" && (t.indent = 1), typeof t.linesBefore != "number" && (t.linesBefore = 3), typeof t.linesAfter != "number" && (t.linesAfter = 2);
  for (var n = /\r?\n|\r|\0/g, r = [0], s = [], o, a = -1; o = n.exec(e5.buffer); ) s.push(o.index), r.push(o.index + o[0].length), e5.position <= o.index && a < 0 && (a = r.length - 2);
  a < 0 && (a = r.length - 1);
  var l = "", c, u, p = Math.min(e5.line + t.linesAfter, s.length).toString().length, f = t.maxLength - (t.indent + p + 3);
  for (c = 1; c <= t.linesBefore && !(a - c < 0); c++) u = getLine$1(e5.buffer, r[a - c], s[a - c], e5.position - (r[a] - r[a - c]), f), l = common$8.repeat(" ", t.indent) + padStart$1((e5.line - c + 1).toString(), p) + " | " + u.str + `
` + l;
  for (u = getLine$1(e5.buffer, r[a], s[a], e5.position, f), l += common$8.repeat(" ", t.indent) + padStart$1((e5.line + 1).toString(), p) + " | " + u.str + `
`, l += common$8.repeat("-", t.indent + p + 3 + u.pos) + `^
`, c = 1; c <= t.linesAfter && !(a + c >= s.length); c++) u = getLine$1(e5.buffer, r[a + c], s[a + c], e5.position - (r[a] - r[a + c]), f), l += common$8.repeat(" ", t.indent) + padStart$1((e5.line + c + 1).toString(), p) + " | " + u.str + `
`;
  return l.replace(/\n$/, "");
}
var snippet$1 = makeSnippet$1, TYPE_CONSTRUCTOR_OPTIONS$2 = ["kind", "multi", "resolve", "construct", "instanceOf", "predicate", "represent", "representName", "defaultStyle", "styleAliases"], YAML_NODE_KINDS$2 = ["scalar", "sequence", "mapping"];
function compileStyleAliases$2(e5) {
  var t = {};
  return e5 !== null && Object.keys(e5).forEach(function(n) {
    e5[n].forEach(function(r) {
      t[String(r)] = n;
    });
  }), t;
}
function Type$1$1(e5, t) {
  if (t = t || {}, Object.keys(t).forEach(function(n) {
    if (TYPE_CONSTRUCTOR_OPTIONS$2.indexOf(n) === -1) throw new exception$2('Unknown option "' + n + '" is met in definition of "' + e5 + '" YAML type.');
  }), this.options = t, this.tag = e5, this.kind = t.kind || null, this.resolve = t.resolve || function() {
    return true;
  }, this.construct = t.construct || function(n) {
    return n;
  }, this.instanceOf = t.instanceOf || null, this.predicate = t.predicate || null, this.represent = t.represent || null, this.representName = t.representName || null, this.defaultStyle = t.defaultStyle || null, this.multi = t.multi || false, this.styleAliases = compileStyleAliases$2(t.styleAliases || null), YAML_NODE_KINDS$2.indexOf(this.kind) === -1) throw new exception$2('Unknown kind "' + this.kind + '" is specified for "' + e5 + '" YAML type.');
}
var type$2 = Type$1$1;
function compileList$2(e5, t) {
  var n = [];
  return e5[t].forEach(function(r) {
    var s = n.length;
    n.forEach(function(o, a) {
      o.tag === r.tag && o.kind === r.kind && o.multi === r.multi && (s = a);
    }), n[s] = r;
  }), n;
}
function compileMap$2() {
  var e5 = { scalar: {}, sequence: {}, mapping: {}, fallback: {}, multi: { scalar: [], sequence: [], mapping: [], fallback: [] } }, t, n;
  function r(s) {
    s.multi ? (e5.multi[s.kind].push(s), e5.multi.fallback.push(s)) : e5[s.kind][s.tag] = e5.fallback[s.tag] = s;
  }
  for (t = 0, n = arguments.length; t < n; t += 1) arguments[t].forEach(r);
  return e5;
}
function Schema$1$1(e5) {
  return this.extend(e5);
}
Schema$1$1.prototype.extend = function e2(t) {
  var n = [], r = [];
  if (t instanceof type$2) r.push(t);
  else if (Array.isArray(t)) r = r.concat(t);
  else if (t && (Array.isArray(t.implicit) || Array.isArray(t.explicit))) t.implicit && (n = n.concat(t.implicit)), t.explicit && (r = r.concat(t.explicit));
  else throw new exception$2("Schema.extend argument should be a Type, [ Type ], or a schema definition ({ implicit: [...], explicit: [...] })");
  n.forEach(function(o) {
    if (!(o instanceof type$2)) throw new exception$2("Specified list of YAML types (or a single Type object) contains a non-Type object.");
    if (o.loadKind && o.loadKind !== "scalar") throw new exception$2("There is a non-scalar type in the implicit list of a schema. Implicit resolving of such types is not supported.");
    if (o.multi) throw new exception$2("There is a multi type in the implicit list of a schema. Multi tags can only be listed as explicit.");
  }), r.forEach(function(o) {
    if (!(o instanceof type$2)) throw new exception$2("Specified list of YAML types (or a single Type object) contains a non-Type object.");
  });
  var s = Object.create(Schema$1$1.prototype);
  return s.implicit = (this.implicit || []).concat(n), s.explicit = (this.explicit || []).concat(r), s.compiledImplicit = compileList$2(s, "implicit"), s.compiledExplicit = compileList$2(s, "explicit"), s.compiledTypeMap = compileMap$2(s.compiledImplicit, s.compiledExplicit), s;
};
var schema$9 = Schema$1$1, str$2 = new type$2("tag:yaml.org,2002:str", { kind: "scalar", construct: function(e5) {
  return e5 !== null ? e5 : "";
} }), seq$9 = new type$2("tag:yaml.org,2002:seq", { kind: "sequence", construct: function(e5) {
  return e5 !== null ? e5 : [];
} }), map$9 = new type$2("tag:yaml.org,2002:map", { kind: "mapping", construct: function(e5) {
  return e5 !== null ? e5 : {};
} }), failsafe$2 = new schema$9({ explicit: [str$2, seq$9, map$9] });
function resolveYamlNull$2(e5) {
  if (e5 === null) return true;
  var t = e5.length;
  return t === 1 && e5 === "~" || t === 4 && (e5 === "null" || e5 === "Null" || e5 === "NULL");
}
function constructYamlNull$2() {
  return null;
}
function isNull$2(e5) {
  return e5 === null;
}
var _null$6 = new type$2("tag:yaml.org,2002:null", { kind: "scalar", resolve: resolveYamlNull$2, construct: constructYamlNull$2, predicate: isNull$2, represent: { canonical: function() {
  return "~";
}, lowercase: function() {
  return "null";
}, uppercase: function() {
  return "NULL";
}, camelcase: function() {
  return "Null";
}, empty: function() {
  return "";
} }, defaultStyle: "lowercase" });
function resolveYamlBoolean$2(e5) {
  if (e5 === null) return false;
  var t = e5.length;
  return t === 4 && (e5 === "true" || e5 === "True" || e5 === "TRUE") || t === 5 && (e5 === "false" || e5 === "False" || e5 === "FALSE");
}
function constructYamlBoolean$2(e5) {
  return e5 === "true" || e5 === "True" || e5 === "TRUE";
}
function isBoolean$2(e5) {
  return Object.prototype.toString.call(e5) === "[object Boolean]";
}
var bool$7 = new type$2("tag:yaml.org,2002:bool", { kind: "scalar", resolve: resolveYamlBoolean$2, construct: constructYamlBoolean$2, predicate: isBoolean$2, represent: { lowercase: function(e5) {
  return e5 ? "true" : "false";
}, uppercase: function(e5) {
  return e5 ? "TRUE" : "FALSE";
}, camelcase: function(e5) {
  return e5 ? "True" : "False";
} }, defaultStyle: "lowercase" });
function isHexCode$2(e5) {
  return 48 <= e5 && e5 <= 57 || 65 <= e5 && e5 <= 70 || 97 <= e5 && e5 <= 102;
}
function isOctCode$2(e5) {
  return 48 <= e5 && e5 <= 55;
}
function isDecCode$2(e5) {
  return 48 <= e5 && e5 <= 57;
}
function resolveYamlInteger$2(e5) {
  if (e5 === null) return false;
  var t = e5.length, n = 0, r = false, s;
  if (!t) return false;
  if (s = e5[n], (s === "-" || s === "+") && (s = e5[++n]), s === "0") {
    if (n + 1 === t) return true;
    if (s = e5[++n], s === "b") {
      for (n++; n < t; n++) if (s = e5[n], s !== "_") {
        if (s !== "0" && s !== "1") return false;
        r = true;
      }
      return r && s !== "_";
    }
    if (s === "x") {
      for (n++; n < t; n++) if (s = e5[n], s !== "_") {
        if (!isHexCode$2(e5.charCodeAt(n))) return false;
        r = true;
      }
      return r && s !== "_";
    }
    if (s === "o") {
      for (n++; n < t; n++) if (s = e5[n], s !== "_") {
        if (!isOctCode$2(e5.charCodeAt(n))) return false;
        r = true;
      }
      return r && s !== "_";
    }
  }
  if (s === "_") return false;
  for (; n < t; n++) if (s = e5[n], s !== "_") {
    if (!isDecCode$2(e5.charCodeAt(n))) return false;
    r = true;
  }
  return !(!r || s === "_");
}
function constructYamlInteger$2(e5) {
  var t = e5, n = 1, r;
  if (t.indexOf("_") !== -1 && (t = t.replace(/_/g, "")), r = t[0], (r === "-" || r === "+") && (r === "-" && (n = -1), t = t.slice(1), r = t[0]), t === "0") return 0;
  if (r === "0") {
    if (t[1] === "b") return n * parseInt(t.slice(2), 2);
    if (t[1] === "x") return n * parseInt(t.slice(2), 16);
    if (t[1] === "o") return n * parseInt(t.slice(2), 8);
  }
  return n * parseInt(t, 10);
}
function isInteger$2(e5) {
  return Object.prototype.toString.call(e5) === "[object Number]" && e5 % 1 === 0 && !common$8.isNegativeZero(e5);
}
var int$9 = new type$2("tag:yaml.org,2002:int", { kind: "scalar", resolve: resolveYamlInteger$2, construct: constructYamlInteger$2, predicate: isInteger$2, represent: { binary: function(e5) {
  return e5 >= 0 ? "0b" + e5.toString(2) : "-0b" + e5.toString(2).slice(1);
}, octal: function(e5) {
  return e5 >= 0 ? "0o" + e5.toString(8) : "-0o" + e5.toString(8).slice(1);
}, decimal: function(e5) {
  return e5.toString(10);
}, hexadecimal: function(e5) {
  return e5 >= 0 ? "0x" + e5.toString(16).toUpperCase() : "-0x" + e5.toString(16).toUpperCase().slice(1);
} }, defaultStyle: "decimal", styleAliases: { binary: [2, "bin"], octal: [8, "oct"], decimal: [10, "dec"], hexadecimal: [16, "hex"] } }), YAML_FLOAT_PATTERN$2 = new RegExp("^(?:[-+]?(?:[0-9][0-9_]*)(?:\\.[0-9_]*)?(?:[eE][-+]?[0-9]+)?|\\.[0-9_]+(?:[eE][-+]?[0-9]+)?|[-+]?\\.(?:inf|Inf|INF)|\\.(?:nan|NaN|NAN))$");
function resolveYamlFloat$2(e5) {
  return !(e5 === null || !YAML_FLOAT_PATTERN$2.test(e5) || e5[e5.length - 1] === "_");
}
function constructYamlFloat$2(e5) {
  var t, n;
  return t = e5.replace(/_/g, "").toLowerCase(), n = t[0] === "-" ? -1 : 1, "+-".indexOf(t[0]) >= 0 && (t = t.slice(1)), t === ".inf" ? n === 1 ? Number.POSITIVE_INFINITY : Number.NEGATIVE_INFINITY : t === ".nan" ? NaN : n * parseFloat(t, 10);
}
var SCIENTIFIC_WITHOUT_DOT$2 = /^[-+]?[0-9]+e/;
function representYamlFloat$2(e5, t) {
  var n;
  if (isNaN(e5)) switch (t) {
    case "lowercase":
      return ".nan";
    case "uppercase":
      return ".NAN";
    case "camelcase":
      return ".NaN";
  }
  else if (Number.POSITIVE_INFINITY === e5) switch (t) {
    case "lowercase":
      return ".inf";
    case "uppercase":
      return ".INF";
    case "camelcase":
      return ".Inf";
  }
  else if (Number.NEGATIVE_INFINITY === e5) switch (t) {
    case "lowercase":
      return "-.inf";
    case "uppercase":
      return "-.INF";
    case "camelcase":
      return "-.Inf";
  }
  else if (common$8.isNegativeZero(e5)) return "-0.0";
  return n = e5.toString(10), SCIENTIFIC_WITHOUT_DOT$2.test(n) ? n.replace("e", ".e") : n;
}
function isFloat$2(e5) {
  return Object.prototype.toString.call(e5) === "[object Number]" && (e5 % 1 !== 0 || common$8.isNegativeZero(e5));
}
var float$9 = new type$2("tag:yaml.org,2002:float", { kind: "scalar", resolve: resolveYamlFloat$2, construct: constructYamlFloat$2, predicate: isFloat$2, represent: representYamlFloat$2, defaultStyle: "lowercase" }), json$5 = failsafe$2.extend({ implicit: [_null$6, bool$7, int$9, float$9] }), core$2 = json$5, YAML_DATE_REGEXP$2 = new RegExp("^([0-9][0-9][0-9][0-9])-([0-9][0-9])-([0-9][0-9])$"), YAML_TIMESTAMP_REGEXP$2 = new RegExp("^([0-9][0-9][0-9][0-9])-([0-9][0-9]?)-([0-9][0-9]?)(?:[Tt]|[ \\t]+)([0-9][0-9]?):([0-9][0-9]):([0-9][0-9])(?:\\.([0-9]*))?(?:[ \\t]*(Z|([-+])([0-9][0-9]?)(?::([0-9][0-9]))?))?$");
function resolveYamlTimestamp$2(e5) {
  return e5 === null ? false : YAML_DATE_REGEXP$2.exec(e5) !== null || YAML_TIMESTAMP_REGEXP$2.exec(e5) !== null;
}
function constructYamlTimestamp$2(e5) {
  var t, n, r, s, o, a, l, c = 0, u = null, p, f, h;
  if (t = YAML_DATE_REGEXP$2.exec(e5), t === null && (t = YAML_TIMESTAMP_REGEXP$2.exec(e5)), t === null) throw new Error("Date resolve error");
  if (n = +t[1], r = +t[2] - 1, s = +t[3], !t[4]) return new Date(Date.UTC(n, r, s));
  if (o = +t[4], a = +t[5], l = +t[6], t[7]) {
    for (c = t[7].slice(0, 3); c.length < 3; ) c += "0";
    c = +c;
  }
  return t[9] && (p = +t[10], f = +(t[11] || 0), u = (p * 60 + f) * 6e4, t[9] === "-" && (u = -u)), h = new Date(Date.UTC(n, r, s, o, a, l, c)), u && h.setTime(h.getTime() - u), h;
}
function representYamlTimestamp$2(e5) {
  return e5.toISOString();
}
var timestamp$6 = new type$2("tag:yaml.org,2002:timestamp", { kind: "scalar", resolve: resolveYamlTimestamp$2, construct: constructYamlTimestamp$2, instanceOf: Date, represent: representYamlTimestamp$2 });
function resolveYamlMerge$2(e5) {
  return e5 === "<<" || e5 === null;
}
var merge$7 = new type$2("tag:yaml.org,2002:merge", { kind: "scalar", resolve: resolveYamlMerge$2 }), BASE64_MAP$2 = `ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=
\r`;
function resolveYamlBinary$2(e5) {
  if (e5 === null) return false;
  var t, n, r = 0, s = e5.length, o = BASE64_MAP$2;
  for (n = 0; n < s; n++) if (t = o.indexOf(e5.charAt(n)), !(t > 64)) {
    if (t < 0) return false;
    r += 6;
  }
  return r % 8 === 0;
}
function constructYamlBinary$2(e5) {
  var t, n, r = e5.replace(/[\r\n=]/g, ""), s = r.length, o = BASE64_MAP$2, a = 0, l = [];
  for (t = 0; t < s; t++) t % 4 === 0 && t && (l.push(a >> 16 & 255), l.push(a >> 8 & 255), l.push(a & 255)), a = a << 6 | o.indexOf(r.charAt(t));
  return n = s % 4 * 6, n === 0 ? (l.push(a >> 16 & 255), l.push(a >> 8 & 255), l.push(a & 255)) : n === 18 ? (l.push(a >> 10 & 255), l.push(a >> 2 & 255)) : n === 12 && l.push(a >> 4 & 255), new Uint8Array(l);
}
function representYamlBinary$2(e5) {
  var t = "", n = 0, r, s, o = e5.length, a = BASE64_MAP$2;
  for (r = 0; r < o; r++) r % 3 === 0 && r && (t += a[n >> 18 & 63], t += a[n >> 12 & 63], t += a[n >> 6 & 63], t += a[n & 63]), n = (n << 8) + e5[r];
  return s = o % 3, s === 0 ? (t += a[n >> 18 & 63], t += a[n >> 12 & 63], t += a[n >> 6 & 63], t += a[n & 63]) : s === 2 ? (t += a[n >> 10 & 63], t += a[n >> 4 & 63], t += a[n << 2 & 63], t += a[64]) : s === 1 && (t += a[n >> 2 & 63], t += a[n << 4 & 63], t += a[64], t += a[64]), t;
}
function isBinary$2(e5) {
  return Object.prototype.toString.call(e5) === "[object Uint8Array]";
}
var binary$6 = new type$2("tag:yaml.org,2002:binary", { kind: "scalar", resolve: resolveYamlBinary$2, construct: constructYamlBinary$2, predicate: isBinary$2, represent: representYamlBinary$2 }), _hasOwnProperty$3$1 = Object.prototype.hasOwnProperty, _toString$2$1 = Object.prototype.toString;
function resolveYamlOmap$2(e5) {
  if (e5 === null) return true;
  var t = [], n, r, s, o, a, l = e5;
  for (n = 0, r = l.length; n < r; n += 1) {
    if (s = l[n], a = false, _toString$2$1.call(s) !== "[object Object]") return false;
    for (o in s) if (_hasOwnProperty$3$1.call(s, o)) if (!a) a = true;
    else return false;
    if (!a) return false;
    if (t.indexOf(o) === -1) t.push(o);
    else return false;
  }
  return true;
}
function constructYamlOmap$2(e5) {
  return e5 !== null ? e5 : [];
}
var omap$6 = new type$2("tag:yaml.org,2002:omap", { kind: "sequence", resolve: resolveYamlOmap$2, construct: constructYamlOmap$2 }), _toString$1$1 = Object.prototype.toString;
function resolveYamlPairs$2(e5) {
  if (e5 === null) return true;
  var t, n, r, s, o, a = e5;
  for (o = new Array(a.length), t = 0, n = a.length; t < n; t += 1) {
    if (r = a[t], _toString$1$1.call(r) !== "[object Object]" || (s = Object.keys(r), s.length !== 1)) return false;
    o[t] = [s[0], r[s[0]]];
  }
  return true;
}
function constructYamlPairs$2(e5) {
  if (e5 === null) return [];
  var t, n, r, s, o, a = e5;
  for (o = new Array(a.length), t = 0, n = a.length; t < n; t += 1) r = a[t], s = Object.keys(r), o[t] = [s[0], r[s[0]]];
  return o;
}
var pairs$7 = new type$2("tag:yaml.org,2002:pairs", { kind: "sequence", resolve: resolveYamlPairs$2, construct: constructYamlPairs$2 }), _hasOwnProperty$2$1 = Object.prototype.hasOwnProperty;
function resolveYamlSet$2(e5) {
  if (e5 === null) return true;
  var t, n = e5;
  for (t in n) if (_hasOwnProperty$2$1.call(n, t) && n[t] !== null) return false;
  return true;
}
function constructYamlSet$2(e5) {
  return e5 !== null ? e5 : {};
}
var set$6 = new type$2("tag:yaml.org,2002:set", { kind: "mapping", resolve: resolveYamlSet$2, construct: constructYamlSet$2 }), _default$1 = core$2.extend({ implicit: [timestamp$6, merge$7], explicit: [binary$6, omap$6, pairs$7, set$6] }), _hasOwnProperty$1$1 = Object.prototype.hasOwnProperty, CONTEXT_FLOW_IN$2 = 1, CONTEXT_FLOW_OUT$2 = 2, CONTEXT_BLOCK_IN$2 = 3, CONTEXT_BLOCK_OUT$2 = 4, CHOMPING_CLIP$2 = 1, CHOMPING_STRIP$2 = 2, CHOMPING_KEEP$2 = 3, PATTERN_NON_PRINTABLE$2 = /[\x00-\x08\x0B\x0C\x0E-\x1F\x7F-\x84\x86-\x9F\uFFFE\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF]/, PATTERN_NON_ASCII_LINE_BREAKS$2 = /[\x85\u2028\u2029]/, PATTERN_FLOW_INDICATORS$2 = /[,\[\]\{\}]/, PATTERN_TAG_HANDLE$2 = /^(?:!|!!|![a-z\-]+!)$/i, PATTERN_TAG_URI$2 = /^(?:!|[^,\[\]\{\}])(?:%[0-9a-f]{2}|[0-9a-z\-#;\/\?:@&=\+\$,_\.!~\*'\(\)\[\]])*$/i;
function _class$2(e5) {
  return Object.prototype.toString.call(e5);
}
function is_EOL$2(e5) {
  return e5 === 10 || e5 === 13;
}
function is_WHITE_SPACE$2(e5) {
  return e5 === 9 || e5 === 32;
}
function is_WS_OR_EOL$2(e5) {
  return e5 === 9 || e5 === 32 || e5 === 10 || e5 === 13;
}
function is_FLOW_INDICATOR$2(e5) {
  return e5 === 44 || e5 === 91 || e5 === 93 || e5 === 123 || e5 === 125;
}
function fromHexCode$2(e5) {
  var t;
  return 48 <= e5 && e5 <= 57 ? e5 - 48 : (t = e5 | 32, 97 <= t && t <= 102 ? t - 97 + 10 : -1);
}
function escapedHexLen$2(e5) {
  return e5 === 120 ? 2 : e5 === 117 ? 4 : e5 === 85 ? 8 : 0;
}
function fromDecimalCode$2(e5) {
  return 48 <= e5 && e5 <= 57 ? e5 - 48 : -1;
}
function simpleEscapeSequence$2(e5) {
  return e5 === 48 ? "\0" : e5 === 97 ? "\x07" : e5 === 98 ? "\b" : e5 === 116 || e5 === 9 ? "	" : e5 === 110 ? `
` : e5 === 118 ? "\v" : e5 === 102 ? "\f" : e5 === 114 ? "\r" : e5 === 101 ? "\x1B" : e5 === 32 ? " " : e5 === 34 ? '"' : e5 === 47 ? "/" : e5 === 92 ? "\\" : e5 === 78 ? "\x85" : e5 === 95 ? "\xA0" : e5 === 76 ? "\u2028" : e5 === 80 ? "\u2029" : "";
}
function charFromCodepoint$2(e5) {
  return e5 <= 65535 ? String.fromCharCode(e5) : String.fromCharCode((e5 - 65536 >> 10) + 55296, (e5 - 65536 & 1023) + 56320);
}
function setProperty$2(e5, t, n) {
  t === "__proto__" ? Object.defineProperty(e5, t, { configurable: true, enumerable: true, writable: true, value: n }) : e5[t] = n;
}
var simpleEscapeCheck$2 = new Array(256), simpleEscapeMap$2 = new Array(256);
for (var i$2 = 0; i$2 < 256; i$2++) simpleEscapeCheck$2[i$2] = simpleEscapeSequence$2(i$2) ? 1 : 0, simpleEscapeMap$2[i$2] = simpleEscapeSequence$2(i$2);
function State$1$1(e5, t) {
  this.input = e5, this.filename = t.filename || null, this.schema = t.schema || _default$1, this.onWarning = t.onWarning || null, this.legacy = t.legacy || false, this.json = t.json || false, this.listener = t.listener || null, this.implicitTypes = this.schema.compiledImplicit, this.typeMap = this.schema.compiledTypeMap, this.length = e5.length, this.position = 0, this.line = 0, this.lineStart = 0, this.lineIndent = 0, this.firstTabInLine = -1, this.documents = [];
}
function generateError$2(e5, t) {
  var n = { name: e5.filename, buffer: e5.input.slice(0, -1), position: e5.position, line: e5.line, column: e5.position - e5.lineStart };
  return n.snippet = snippet$1(n), new exception$2(t, n);
}
function throwError$2(e5, t) {
  throw generateError$2(e5, t);
}
function throwWarning$2(e5, t) {
  e5.onWarning && e5.onWarning.call(null, generateError$2(e5, t));
}
var directiveHandlers$2 = { YAML: function e3(t, n, r) {
  var s, o, a;
  t.version !== null && throwError$2(t, "duplication of %YAML directive"), r.length !== 1 && throwError$2(t, "YAML directive accepts exactly one argument"), s = /^([0-9]+)\.([0-9]+)$/.exec(r[0]), s === null && throwError$2(t, "ill-formed argument of the YAML directive"), o = parseInt(s[1], 10), a = parseInt(s[2], 10), o !== 1 && throwError$2(t, "unacceptable YAML version of the document"), t.version = r[0], t.checkLineBreaks = a < 2, a !== 1 && a !== 2 && throwWarning$2(t, "unsupported YAML version of the document");
}, TAG: function e4(t, n, r) {
  var s, o;
  r.length !== 2 && throwError$2(t, "TAG directive accepts exactly two arguments"), s = r[0], o = r[1], PATTERN_TAG_HANDLE$2.test(s) || throwError$2(t, "ill-formed tag handle (first argument) of the TAG directive"), _hasOwnProperty$1$1.call(t.tagMap, s) && throwError$2(t, 'there is a previously declared suffix for "' + s + '" tag handle'), PATTERN_TAG_URI$2.test(o) || throwError$2(t, "ill-formed tag prefix (second argument) of the TAG directive");
  try {
    o = decodeURIComponent(o);
  } catch {
    throwError$2(t, "tag prefix is malformed: " + o);
  }
  t.tagMap[s] = o;
} };
function captureSegment$2(e5, t, n, r) {
  var s, o, a, l;
  if (t < n) {
    if (l = e5.input.slice(t, n), r) for (s = 0, o = l.length; s < o; s += 1) a = l.charCodeAt(s), a === 9 || 32 <= a && a <= 1114111 || throwError$2(e5, "expected valid JSON character");
    else PATTERN_NON_PRINTABLE$2.test(l) && throwError$2(e5, "the stream contains non-printable characters");
    e5.result += l;
  }
}
function mergeMappings$2(e5, t, n, r) {
  var s, o, a, l;
  for (common$8.isObject(n) || throwError$2(e5, "cannot merge mappings; the provided source object is unacceptable"), s = Object.keys(n), a = 0, l = s.length; a < l; a += 1) o = s[a], _hasOwnProperty$1$1.call(t, o) || (setProperty$2(t, o, n[o]), r[o] = true);
}
function storeMappingPair$2(e5, t, n, r, s, o, a, l, c) {
  var u, p;
  if (Array.isArray(s)) for (s = Array.prototype.slice.call(s), u = 0, p = s.length; u < p; u += 1) Array.isArray(s[u]) && throwError$2(e5, "nested arrays are not supported inside keys"), typeof s == "object" && _class$2(s[u]) === "[object Object]" && (s[u] = "[object Object]");
  if (typeof s == "object" && _class$2(s) === "[object Object]" && (s = "[object Object]"), s = String(s), t === null && (t = {}), r === "tag:yaml.org,2002:merge") if (Array.isArray(o)) for (u = 0, p = o.length; u < p; u += 1) mergeMappings$2(e5, t, o[u], n);
  else mergeMappings$2(e5, t, o, n);
  else !e5.json && !_hasOwnProperty$1$1.call(n, s) && _hasOwnProperty$1$1.call(t, s) && (e5.line = a || e5.line, e5.lineStart = l || e5.lineStart, e5.position = c || e5.position, throwError$2(e5, "duplicated mapping key")), setProperty$2(t, s, o), delete n[s];
  return t;
}
function readLineBreak$2(e5) {
  var t;
  t = e5.input.charCodeAt(e5.position), t === 10 ? e5.position++ : t === 13 ? (e5.position++, e5.input.charCodeAt(e5.position) === 10 && e5.position++) : throwError$2(e5, "a line break is expected"), e5.line += 1, e5.lineStart = e5.position, e5.firstTabInLine = -1;
}
function skipSeparationSpace$2(e5, t, n) {
  for (var r = 0, s = e5.input.charCodeAt(e5.position); s !== 0; ) {
    for (; is_WHITE_SPACE$2(s); ) s === 9 && e5.firstTabInLine === -1 && (e5.firstTabInLine = e5.position), s = e5.input.charCodeAt(++e5.position);
    if (t && s === 35) do
      s = e5.input.charCodeAt(++e5.position);
    while (s !== 10 && s !== 13 && s !== 0);
    if (is_EOL$2(s)) for (readLineBreak$2(e5), s = e5.input.charCodeAt(e5.position), r++, e5.lineIndent = 0; s === 32; ) e5.lineIndent++, s = e5.input.charCodeAt(++e5.position);
    else break;
  }
  return n !== -1 && r !== 0 && e5.lineIndent < n && throwWarning$2(e5, "deficient indentation"), r;
}
function testDocumentSeparator$2(e5) {
  var t = e5.position, n;
  return n = e5.input.charCodeAt(t), !!((n === 45 || n === 46) && n === e5.input.charCodeAt(t + 1) && n === e5.input.charCodeAt(t + 2) && (t += 3, n = e5.input.charCodeAt(t), n === 0 || is_WS_OR_EOL$2(n)));
}
function writeFoldedLines$2(e5, t) {
  t === 1 ? e5.result += " " : t > 1 && (e5.result += common$8.repeat(`
`, t - 1));
}
function readPlainScalar$2(e5, t, n) {
  var r, s, o, a, l, c, u, p, f = e5.kind, h = e5.result, m;
  if (m = e5.input.charCodeAt(e5.position), is_WS_OR_EOL$2(m) || is_FLOW_INDICATOR$2(m) || m === 35 || m === 38 || m === 42 || m === 33 || m === 124 || m === 62 || m === 39 || m === 34 || m === 37 || m === 64 || m === 96 || (m === 63 || m === 45) && (s = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL$2(s) || n && is_FLOW_INDICATOR$2(s))) return false;
  for (e5.kind = "scalar", e5.result = "", o = a = e5.position, l = false; m !== 0; ) {
    if (m === 58) {
      if (s = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL$2(s) || n && is_FLOW_INDICATOR$2(s)) break;
    } else if (m === 35) {
      if (r = e5.input.charCodeAt(e5.position - 1), is_WS_OR_EOL$2(r)) break;
    } else {
      if (e5.position === e5.lineStart && testDocumentSeparator$2(e5) || n && is_FLOW_INDICATOR$2(m)) break;
      if (is_EOL$2(m)) if (c = e5.line, u = e5.lineStart, p = e5.lineIndent, skipSeparationSpace$2(e5, false, -1), e5.lineIndent >= t) {
        l = true, m = e5.input.charCodeAt(e5.position);
        continue;
      } else {
        e5.position = a, e5.line = c, e5.lineStart = u, e5.lineIndent = p;
        break;
      }
    }
    l && (captureSegment$2(e5, o, a, false), writeFoldedLines$2(e5, e5.line - c), o = a = e5.position, l = false), is_WHITE_SPACE$2(m) || (a = e5.position + 1), m = e5.input.charCodeAt(++e5.position);
  }
  return captureSegment$2(e5, o, a, false), e5.result ? true : (e5.kind = f, e5.result = h, false);
}
function readSingleQuotedScalar$2(e5, t) {
  var n, r, s;
  if (n = e5.input.charCodeAt(e5.position), n !== 39) return false;
  for (e5.kind = "scalar", e5.result = "", e5.position++, r = s = e5.position; (n = e5.input.charCodeAt(e5.position)) !== 0; ) if (n === 39) if (captureSegment$2(e5, r, e5.position, true), n = e5.input.charCodeAt(++e5.position), n === 39) r = e5.position, e5.position++, s = e5.position;
  else return true;
  else is_EOL$2(n) ? (captureSegment$2(e5, r, s, true), writeFoldedLines$2(e5, skipSeparationSpace$2(e5, false, t)), r = s = e5.position) : e5.position === e5.lineStart && testDocumentSeparator$2(e5) ? throwError$2(e5, "unexpected end of the document within a single quoted scalar") : (e5.position++, s = e5.position);
  throwError$2(e5, "unexpected end of the stream within a single quoted scalar");
}
function readDoubleQuotedScalar$2(e5, t) {
  var n, r, s, o, a, l;
  if (l = e5.input.charCodeAt(e5.position), l !== 34) return false;
  for (e5.kind = "scalar", e5.result = "", e5.position++, n = r = e5.position; (l = e5.input.charCodeAt(e5.position)) !== 0; ) {
    if (l === 34) return captureSegment$2(e5, n, e5.position, true), e5.position++, true;
    if (l === 92) {
      if (captureSegment$2(e5, n, e5.position, true), l = e5.input.charCodeAt(++e5.position), is_EOL$2(l)) skipSeparationSpace$2(e5, false, t);
      else if (l < 256 && simpleEscapeCheck$2[l]) e5.result += simpleEscapeMap$2[l], e5.position++;
      else if ((a = escapedHexLen$2(l)) > 0) {
        for (s = a, o = 0; s > 0; s--) l = e5.input.charCodeAt(++e5.position), (a = fromHexCode$2(l)) >= 0 ? o = (o << 4) + a : throwError$2(e5, "expected hexadecimal character");
        e5.result += charFromCodepoint$2(o), e5.position++;
      } else throwError$2(e5, "unknown escape sequence");
      n = r = e5.position;
    } else is_EOL$2(l) ? (captureSegment$2(e5, n, r, true), writeFoldedLines$2(e5, skipSeparationSpace$2(e5, false, t)), n = r = e5.position) : e5.position === e5.lineStart && testDocumentSeparator$2(e5) ? throwError$2(e5, "unexpected end of the document within a double quoted scalar") : (e5.position++, r = e5.position);
  }
  throwError$2(e5, "unexpected end of the stream within a double quoted scalar");
}
function readFlowCollection$2(e5, t) {
  var n = true, r, s, o, a = e5.tag, l, c = e5.anchor, u, p, f, h, m, g = /* @__PURE__ */ Object.create(null), d, y, S, $;
  if ($ = e5.input.charCodeAt(e5.position), $ === 91) p = 93, m = false, l = [];
  else if ($ === 123) p = 125, m = true, l = {};
  else return false;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = l), $ = e5.input.charCodeAt(++e5.position); $ !== 0; ) {
    if (skipSeparationSpace$2(e5, true, t), $ = e5.input.charCodeAt(e5.position), $ === p) return e5.position++, e5.tag = a, e5.anchor = c, e5.kind = m ? "mapping" : "sequence", e5.result = l, true;
    n ? $ === 44 && throwError$2(e5, "expected the node content, but found ','") : throwError$2(e5, "missed comma between flow collection entries"), y = d = S = null, f = h = false, $ === 63 && (u = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL$2(u) && (f = h = true, e5.position++, skipSeparationSpace$2(e5, true, t))), r = e5.line, s = e5.lineStart, o = e5.position, composeNode$5(e5, t, CONTEXT_FLOW_IN$2, false, true), y = e5.tag, d = e5.result, skipSeparationSpace$2(e5, true, t), $ = e5.input.charCodeAt(e5.position), (h || e5.line === r) && $ === 58 && (f = true, $ = e5.input.charCodeAt(++e5.position), skipSeparationSpace$2(e5, true, t), composeNode$5(e5, t, CONTEXT_FLOW_IN$2, false, true), S = e5.result), m ? storeMappingPair$2(e5, l, g, y, d, S, r, s, o) : f ? l.push(storeMappingPair$2(e5, null, g, y, d, S, r, s, o)) : l.push(d), skipSeparationSpace$2(e5, true, t), $ = e5.input.charCodeAt(e5.position), $ === 44 ? (n = true, $ = e5.input.charCodeAt(++e5.position)) : n = false;
  }
  throwError$2(e5, "unexpected end of the stream within a flow collection");
}
function readBlockScalar$2(e5, t) {
  var n, r, s = CHOMPING_CLIP$2, o = false, a = false, l = t, c = 0, u = false, p, f;
  if (f = e5.input.charCodeAt(e5.position), f === 124) r = false;
  else if (f === 62) r = true;
  else return false;
  for (e5.kind = "scalar", e5.result = ""; f !== 0; ) if (f = e5.input.charCodeAt(++e5.position), f === 43 || f === 45) CHOMPING_CLIP$2 === s ? s = f === 43 ? CHOMPING_KEEP$2 : CHOMPING_STRIP$2 : throwError$2(e5, "repeat of a chomping mode identifier");
  else if ((p = fromDecimalCode$2(f)) >= 0) p === 0 ? throwError$2(e5, "bad explicit indentation width of a block scalar; it cannot be less than one") : a ? throwError$2(e5, "repeat of an indentation width identifier") : (l = t + p - 1, a = true);
  else break;
  if (is_WHITE_SPACE$2(f)) {
    do
      f = e5.input.charCodeAt(++e5.position);
    while (is_WHITE_SPACE$2(f));
    if (f === 35) do
      f = e5.input.charCodeAt(++e5.position);
    while (!is_EOL$2(f) && f !== 0);
  }
  for (; f !== 0; ) {
    for (readLineBreak$2(e5), e5.lineIndent = 0, f = e5.input.charCodeAt(e5.position); (!a || e5.lineIndent < l) && f === 32; ) e5.lineIndent++, f = e5.input.charCodeAt(++e5.position);
    if (!a && e5.lineIndent > l && (l = e5.lineIndent), is_EOL$2(f)) {
      c++;
      continue;
    }
    if (e5.lineIndent < l) {
      s === CHOMPING_KEEP$2 ? e5.result += common$8.repeat(`
`, o ? 1 + c : c) : s === CHOMPING_CLIP$2 && o && (e5.result += `
`);
      break;
    }
    for (r ? is_WHITE_SPACE$2(f) ? (u = true, e5.result += common$8.repeat(`
`, o ? 1 + c : c)) : u ? (u = false, e5.result += common$8.repeat(`
`, c + 1)) : c === 0 ? o && (e5.result += " ") : e5.result += common$8.repeat(`
`, c) : e5.result += common$8.repeat(`
`, o ? 1 + c : c), o = true, a = true, c = 0, n = e5.position; !is_EOL$2(f) && f !== 0; ) f = e5.input.charCodeAt(++e5.position);
    captureSegment$2(e5, n, e5.position, false);
  }
  return true;
}
function readBlockSequence$2(e5, t) {
  var n, r = e5.tag, s = e5.anchor, o = [], a, l = false, c;
  if (e5.firstTabInLine !== -1) return false;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = o), c = e5.input.charCodeAt(e5.position); c !== 0 && (e5.firstTabInLine !== -1 && (e5.position = e5.firstTabInLine, throwError$2(e5, "tab characters must not be used in indentation")), !(c !== 45 || (a = e5.input.charCodeAt(e5.position + 1), !is_WS_OR_EOL$2(a)))); ) {
    if (l = true, e5.position++, skipSeparationSpace$2(e5, true, -1) && e5.lineIndent <= t) {
      o.push(null), c = e5.input.charCodeAt(e5.position);
      continue;
    }
    if (n = e5.line, composeNode$5(e5, t, CONTEXT_BLOCK_IN$2, false, true), o.push(e5.result), skipSeparationSpace$2(e5, true, -1), c = e5.input.charCodeAt(e5.position), (e5.line === n || e5.lineIndent > t) && c !== 0) throwError$2(e5, "bad indentation of a sequence entry");
    else if (e5.lineIndent < t) break;
  }
  return l ? (e5.tag = r, e5.anchor = s, e5.kind = "sequence", e5.result = o, true) : false;
}
function readBlockMapping$2(e5, t, n) {
  var r, s, o, a, l, c, u = e5.tag, p = e5.anchor, f = {}, h = /* @__PURE__ */ Object.create(null), m = null, g = null, d = null, y = false, S = false, $;
  if (e5.firstTabInLine !== -1) return false;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = f), $ = e5.input.charCodeAt(e5.position); $ !== 0; ) {
    if (!y && e5.firstTabInLine !== -1 && (e5.position = e5.firstTabInLine, throwError$2(e5, "tab characters must not be used in indentation")), r = e5.input.charCodeAt(e5.position + 1), o = e5.line, ($ === 63 || $ === 58) && is_WS_OR_EOL$2(r)) $ === 63 ? (y && (storeMappingPair$2(e5, f, h, m, g, null, a, l, c), m = g = d = null), S = true, y = true, s = true) : y ? (y = false, s = true) : throwError$2(e5, "incomplete explicit mapping pair; a key node is missed; or followed by a non-tabulated empty line"), e5.position += 1, $ = r;
    else {
      if (a = e5.line, l = e5.lineStart, c = e5.position, !composeNode$5(e5, n, CONTEXT_FLOW_OUT$2, false, true)) break;
      if (e5.line === o) {
        for ($ = e5.input.charCodeAt(e5.position); is_WHITE_SPACE$2($); ) $ = e5.input.charCodeAt(++e5.position);
        if ($ === 58) $ = e5.input.charCodeAt(++e5.position), is_WS_OR_EOL$2($) || throwError$2(e5, "a whitespace character is expected after the key-value separator within a block mapping"), y && (storeMappingPair$2(e5, f, h, m, g, null, a, l, c), m = g = d = null), S = true, y = false, s = false, m = e5.tag, g = e5.result;
        else if (S) throwError$2(e5, "can not read an implicit mapping pair; a colon is missed");
        else return e5.tag = u, e5.anchor = p, true;
      } else if (S) throwError$2(e5, "can not read a block mapping entry; a multiline key may not be an implicit key");
      else return e5.tag = u, e5.anchor = p, true;
    }
    if ((e5.line === o || e5.lineIndent > t) && (y && (a = e5.line, l = e5.lineStart, c = e5.position), composeNode$5(e5, t, CONTEXT_BLOCK_OUT$2, true, s) && (y ? g = e5.result : d = e5.result), y || (storeMappingPair$2(e5, f, h, m, g, d, a, l, c), m = g = d = null), skipSeparationSpace$2(e5, true, -1), $ = e5.input.charCodeAt(e5.position)), (e5.line === o || e5.lineIndent > t) && $ !== 0) throwError$2(e5, "bad indentation of a mapping entry");
    else if (e5.lineIndent < t) break;
  }
  return y && storeMappingPair$2(e5, f, h, m, g, null, a, l, c), S && (e5.tag = u, e5.anchor = p, e5.kind = "mapping", e5.result = f), S;
}
function readTagProperty$2(e5) {
  var t, n = false, r = false, s, o, a;
  if (a = e5.input.charCodeAt(e5.position), a !== 33) return false;
  if (e5.tag !== null && throwError$2(e5, "duplication of a tag property"), a = e5.input.charCodeAt(++e5.position), a === 60 ? (n = true, a = e5.input.charCodeAt(++e5.position)) : a === 33 ? (r = true, s = "!!", a = e5.input.charCodeAt(++e5.position)) : s = "!", t = e5.position, n) {
    do
      a = e5.input.charCodeAt(++e5.position);
    while (a !== 0 && a !== 62);
    e5.position < e5.length ? (o = e5.input.slice(t, e5.position), a = e5.input.charCodeAt(++e5.position)) : throwError$2(e5, "unexpected end of the stream within a verbatim tag");
  } else {
    for (; a !== 0 && !is_WS_OR_EOL$2(a); ) a === 33 && (r ? throwError$2(e5, "tag suffix cannot contain exclamation marks") : (s = e5.input.slice(t - 1, e5.position + 1), PATTERN_TAG_HANDLE$2.test(s) || throwError$2(e5, "named tag handle cannot contain such characters"), r = true, t = e5.position + 1)), a = e5.input.charCodeAt(++e5.position);
    o = e5.input.slice(t, e5.position), PATTERN_FLOW_INDICATORS$2.test(o) && throwError$2(e5, "tag suffix cannot contain flow indicator characters");
  }
  o && !PATTERN_TAG_URI$2.test(o) && throwError$2(e5, "tag name cannot contain such characters: " + o);
  try {
    o = decodeURIComponent(o);
  } catch {
    throwError$2(e5, "tag name is malformed: " + o);
  }
  return n ? e5.tag = o : _hasOwnProperty$1$1.call(e5.tagMap, s) ? e5.tag = e5.tagMap[s] + o : s === "!" ? e5.tag = "!" + o : s === "!!" ? e5.tag = "tag:yaml.org,2002:" + o : throwError$2(e5, 'undeclared tag handle "' + s + '"'), true;
}
function readAnchorProperty$2(e5) {
  var t, n;
  if (n = e5.input.charCodeAt(e5.position), n !== 38) return false;
  for (e5.anchor !== null && throwError$2(e5, "duplication of an anchor property"), n = e5.input.charCodeAt(++e5.position), t = e5.position; n !== 0 && !is_WS_OR_EOL$2(n) && !is_FLOW_INDICATOR$2(n); ) n = e5.input.charCodeAt(++e5.position);
  return e5.position === t && throwError$2(e5, "name of an anchor node must contain at least one character"), e5.anchor = e5.input.slice(t, e5.position), true;
}
function readAlias$2(e5) {
  var t, n, r;
  if (r = e5.input.charCodeAt(e5.position), r !== 42) return false;
  for (r = e5.input.charCodeAt(++e5.position), t = e5.position; r !== 0 && !is_WS_OR_EOL$2(r) && !is_FLOW_INDICATOR$2(r); ) r = e5.input.charCodeAt(++e5.position);
  return e5.position === t && throwError$2(e5, "name of an alias node must contain at least one character"), n = e5.input.slice(t, e5.position), _hasOwnProperty$1$1.call(e5.anchorMap, n) || throwError$2(e5, 'unidentified alias "' + n + '"'), e5.result = e5.anchorMap[n], skipSeparationSpace$2(e5, true, -1), true;
}
function composeNode$5(e5, t, n, r, s) {
  var o, a, l, c = 1, u = false, p = false, f, h, m, g, d, y;
  if (e5.listener !== null && e5.listener("open", e5), e5.tag = null, e5.anchor = null, e5.kind = null, e5.result = null, o = a = l = CONTEXT_BLOCK_OUT$2 === n || CONTEXT_BLOCK_IN$2 === n, r && skipSeparationSpace$2(e5, true, -1) && (u = true, e5.lineIndent > t ? c = 1 : e5.lineIndent === t ? c = 0 : e5.lineIndent < t && (c = -1)), c === 1) for (; readTagProperty$2(e5) || readAnchorProperty$2(e5); ) skipSeparationSpace$2(e5, true, -1) ? (u = true, l = o, e5.lineIndent > t ? c = 1 : e5.lineIndent === t ? c = 0 : e5.lineIndent < t && (c = -1)) : l = false;
  if (l && (l = u || s), (c === 1 || CONTEXT_BLOCK_OUT$2 === n) && (CONTEXT_FLOW_IN$2 === n || CONTEXT_FLOW_OUT$2 === n ? d = t : d = t + 1, y = e5.position - e5.lineStart, c === 1 ? l && (readBlockSequence$2(e5, y) || readBlockMapping$2(e5, y, d)) || readFlowCollection$2(e5, d) ? p = true : (a && readBlockScalar$2(e5, d) || readSingleQuotedScalar$2(e5, d) || readDoubleQuotedScalar$2(e5, d) ? p = true : readAlias$2(e5) ? (p = true, (e5.tag !== null || e5.anchor !== null) && throwError$2(e5, "alias node should not have any properties")) : readPlainScalar$2(e5, d, CONTEXT_FLOW_IN$2 === n) && (p = true, e5.tag === null && (e5.tag = "?")), e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result)) : c === 0 && (p = l && readBlockSequence$2(e5, y))), e5.tag === null) e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result);
  else if (e5.tag === "?") {
    for (e5.result !== null && e5.kind !== "scalar" && throwError$2(e5, 'unacceptable node kind for !<?> tag; it should be "scalar", not "' + e5.kind + '"'), f = 0, h = e5.implicitTypes.length; f < h; f += 1) if (g = e5.implicitTypes[f], g.resolve(e5.result)) {
      e5.result = g.construct(e5.result), e5.tag = g.tag, e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result);
      break;
    }
  } else if (e5.tag !== "!") {
    if (_hasOwnProperty$1$1.call(e5.typeMap[e5.kind || "fallback"], e5.tag)) g = e5.typeMap[e5.kind || "fallback"][e5.tag];
    else for (g = null, m = e5.typeMap.multi[e5.kind || "fallback"], f = 0, h = m.length; f < h; f += 1) if (e5.tag.slice(0, m[f].tag.length) === m[f].tag) {
      g = m[f];
      break;
    }
    g || throwError$2(e5, "unknown tag !<" + e5.tag + ">"), e5.result !== null && g.kind !== e5.kind && throwError$2(e5, "unacceptable node kind for !<" + e5.tag + '> tag; it should be "' + g.kind + '", not "' + e5.kind + '"'), g.resolve(e5.result, e5.tag) ? (e5.result = g.construct(e5.result, e5.tag), e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result)) : throwError$2(e5, "cannot resolve a node with !<" + e5.tag + "> explicit tag");
  }
  return e5.listener !== null && e5.listener("close", e5), e5.tag !== null || e5.anchor !== null || p;
}
function readDocument$2(e5) {
  var t = e5.position, n, r, s, o = false, a;
  for (e5.version = null, e5.checkLineBreaks = e5.legacy, e5.tagMap = /* @__PURE__ */ Object.create(null), e5.anchorMap = /* @__PURE__ */ Object.create(null); (a = e5.input.charCodeAt(e5.position)) !== 0 && (skipSeparationSpace$2(e5, true, -1), a = e5.input.charCodeAt(e5.position), !(e5.lineIndent > 0 || a !== 37)); ) {
    for (o = true, a = e5.input.charCodeAt(++e5.position), n = e5.position; a !== 0 && !is_WS_OR_EOL$2(a); ) a = e5.input.charCodeAt(++e5.position);
    for (r = e5.input.slice(n, e5.position), s = [], r.length < 1 && throwError$2(e5, "directive name must not be less than one character in length"); a !== 0; ) {
      for (; is_WHITE_SPACE$2(a); ) a = e5.input.charCodeAt(++e5.position);
      if (a === 35) {
        do
          a = e5.input.charCodeAt(++e5.position);
        while (a !== 0 && !is_EOL$2(a));
        break;
      }
      if (is_EOL$2(a)) break;
      for (n = e5.position; a !== 0 && !is_WS_OR_EOL$2(a); ) a = e5.input.charCodeAt(++e5.position);
      s.push(e5.input.slice(n, e5.position));
    }
    a !== 0 && readLineBreak$2(e5), _hasOwnProperty$1$1.call(directiveHandlers$2, r) ? directiveHandlers$2[r](e5, r, s) : throwWarning$2(e5, 'unknown document directive "' + r + '"');
  }
  if (skipSeparationSpace$2(e5, true, -1), e5.lineIndent === 0 && e5.input.charCodeAt(e5.position) === 45 && e5.input.charCodeAt(e5.position + 1) === 45 && e5.input.charCodeAt(e5.position + 2) === 45 ? (e5.position += 3, skipSeparationSpace$2(e5, true, -1)) : o && throwError$2(e5, "directives end mark is expected"), composeNode$5(e5, e5.lineIndent - 1, CONTEXT_BLOCK_OUT$2, false, true), skipSeparationSpace$2(e5, true, -1), e5.checkLineBreaks && PATTERN_NON_ASCII_LINE_BREAKS$2.test(e5.input.slice(t, e5.position)) && throwWarning$2(e5, "non-ASCII line breaks are interpreted as content"), e5.documents.push(e5.result), e5.position === e5.lineStart && testDocumentSeparator$2(e5)) {
    e5.input.charCodeAt(e5.position) === 46 && (e5.position += 3, skipSeparationSpace$2(e5, true, -1));
    return;
  }
  if (e5.position < e5.length - 1) throwError$2(e5, "end of the stream or a document separator is expected");
  else return;
}
function loadDocuments$2(e5, t) {
  e5 = String(e5), t = t || {}, e5.length !== 0 && (e5.charCodeAt(e5.length - 1) !== 10 && e5.charCodeAt(e5.length - 1) !== 13 && (e5 += `
`), e5.charCodeAt(0) === 65279 && (e5 = e5.slice(1)));
  var n = new State$1$1(e5, t), r = e5.indexOf("\0");
  for (r !== -1 && (n.position = r, throwError$2(n, "null byte is not allowed in input")), n.input += "\0"; n.input.charCodeAt(n.position) === 32; ) n.lineIndent += 1, n.position += 1;
  for (; n.position < n.length - 1; ) readDocument$2(n);
  return n.documents;
}
function loadAll$1$1(e5, t, n) {
  t !== null && typeof t == "object" && typeof n > "u" && (n = t, t = null);
  var r = loadDocuments$2(e5, n);
  if (typeof t != "function") return r;
  for (var s = 0, o = r.length; s < o; s += 1) t(r[s]);
}
function load$1$1(e5, t) {
  var n = loadDocuments$2(e5, t);
  if (n.length !== 0) {
    if (n.length === 1) return n[0];
    throw new exception$2("expected a single document in the stream, but found more");
  }
}
var loadAll_1$1 = loadAll$1$1, load_1$1 = load$1$1, loader$3 = { loadAll: loadAll_1$1, load: load_1$1 }, _toString$6 = Object.prototype.toString, _hasOwnProperty$8 = Object.prototype.hasOwnProperty, CHAR_BOM$1 = 65279, CHAR_TAB$2 = 9, CHAR_LINE_FEED$2 = 10, CHAR_CARRIAGE_RETURN$2 = 13, CHAR_SPACE$2 = 32, CHAR_EXCLAMATION$2 = 33, CHAR_DOUBLE_QUOTE$2 = 34, CHAR_SHARP$2 = 35, CHAR_PERCENT$2 = 37, CHAR_AMPERSAND$2 = 38, CHAR_SINGLE_QUOTE$2 = 39, CHAR_ASTERISK$2 = 42, CHAR_COMMA$2 = 44, CHAR_MINUS$2 = 45, CHAR_COLON$2 = 58, CHAR_EQUALS$2 = 61, CHAR_GREATER_THAN$2 = 62, CHAR_QUESTION$2 = 63, CHAR_COMMERCIAL_AT$2 = 64, CHAR_LEFT_SQUARE_BRACKET$2 = 91, CHAR_RIGHT_SQUARE_BRACKET$2 = 93, CHAR_GRAVE_ACCENT$2 = 96, CHAR_LEFT_CURLY_BRACKET$2 = 123, CHAR_VERTICAL_LINE$2 = 124, CHAR_RIGHT_CURLY_BRACKET$2 = 125, ESCAPE_SEQUENCES$2 = {};
ESCAPE_SEQUENCES$2[0] = "\\0";
ESCAPE_SEQUENCES$2[7] = "\\a";
ESCAPE_SEQUENCES$2[8] = "\\b";
ESCAPE_SEQUENCES$2[9] = "\\t";
ESCAPE_SEQUENCES$2[10] = "\\n";
ESCAPE_SEQUENCES$2[11] = "\\v";
ESCAPE_SEQUENCES$2[12] = "\\f";
ESCAPE_SEQUENCES$2[13] = "\\r";
ESCAPE_SEQUENCES$2[27] = "\\e";
ESCAPE_SEQUENCES$2[34] = '\\"';
ESCAPE_SEQUENCES$2[92] = "\\\\";
ESCAPE_SEQUENCES$2[133] = "\\N";
ESCAPE_SEQUENCES$2[160] = "\\_";
ESCAPE_SEQUENCES$2[8232] = "\\L";
ESCAPE_SEQUENCES$2[8233] = "\\P";
var DEPRECATED_BOOLEANS_SYNTAX$2 = ["y", "Y", "yes", "Yes", "YES", "on", "On", "ON", "n", "N", "no", "No", "NO", "off", "Off", "OFF"], DEPRECATED_BASE60_SYNTAX$1 = /^[-+]?[0-9_]+(?::[0-9_]+)+(?:\.[0-9_]*)?$/;
function compileStyleMap$2(e5, t) {
  var n, r, s, o, a, l, c;
  if (t === null) return {};
  for (n = {}, r = Object.keys(t), s = 0, o = r.length; s < o; s += 1) a = r[s], l = String(t[a]), a.slice(0, 2) === "!!" && (a = "tag:yaml.org,2002:" + a.slice(2)), c = e5.compiledTypeMap.fallback[a], c && _hasOwnProperty$8.call(c.styleAliases, l) && (l = c.styleAliases[l]), n[a] = l;
  return n;
}
function encodeHex$2(e5) {
  var t, n, r;
  if (t = e5.toString(16).toUpperCase(), e5 <= 255) n = "x", r = 2;
  else if (e5 <= 65535) n = "u", r = 4;
  else if (e5 <= 4294967295) n = "U", r = 8;
  else throw new exception$2("code point within a string may not be greater than 0xFFFFFFFF");
  return "\\" + n + common$8.repeat("0", r - t.length) + t;
}
var QUOTING_TYPE_SINGLE$1 = 1, QUOTING_TYPE_DOUBLE$1 = 2;
function State$4(e5) {
  this.schema = e5.schema || _default$1, this.indent = Math.max(1, e5.indent || 2), this.noArrayIndent = e5.noArrayIndent || false, this.skipInvalid = e5.skipInvalid || false, this.flowLevel = common$8.isNothing(e5.flowLevel) ? -1 : e5.flowLevel, this.styleMap = compileStyleMap$2(this.schema, e5.styles || null), this.sortKeys = e5.sortKeys || false, this.lineWidth = e5.lineWidth || 80, this.noRefs = e5.noRefs || false, this.noCompatMode = e5.noCompatMode || false, this.condenseFlow = e5.condenseFlow || false, this.quotingType = e5.quotingType === '"' ? QUOTING_TYPE_DOUBLE$1 : QUOTING_TYPE_SINGLE$1, this.forceQuotes = e5.forceQuotes || false, this.replacer = typeof e5.replacer == "function" ? e5.replacer : null, this.implicitTypes = this.schema.compiledImplicit, this.explicitTypes = this.schema.compiledExplicit, this.tag = null, this.result = "", this.duplicates = [], this.usedDuplicates = null;
}
function indentString$2(e5, t) {
  for (var n = common$8.repeat(" ", t), r = 0, s = -1, o = "", a, l = e5.length; r < l; ) s = e5.indexOf(`
`, r), s === -1 ? (a = e5.slice(r), r = l) : (a = e5.slice(r, s + 1), r = s + 1), a.length && a !== `
` && (o += n), o += a;
  return o;
}
function generateNextLine$2(e5, t) {
  return `
` + common$8.repeat(" ", e5.indent * t);
}
function testImplicitResolving$2(e5, t) {
  var n, r, s;
  for (n = 0, r = e5.implicitTypes.length; n < r; n += 1) if (s = e5.implicitTypes[n], s.resolve(t)) return true;
  return false;
}
function isWhitespace$2(e5) {
  return e5 === CHAR_SPACE$2 || e5 === CHAR_TAB$2;
}
function isPrintable$2(e5) {
  return 32 <= e5 && e5 <= 126 || 161 <= e5 && e5 <= 55295 && e5 !== 8232 && e5 !== 8233 || 57344 <= e5 && e5 <= 65533 && e5 !== CHAR_BOM$1 || 65536 <= e5 && e5 <= 1114111;
}
function isNsCharOrWhitespace$1(e5) {
  return isPrintable$2(e5) && e5 !== CHAR_BOM$1 && e5 !== CHAR_CARRIAGE_RETURN$2 && e5 !== CHAR_LINE_FEED$2;
}
function isPlainSafe$2(e5, t, n) {
  var r = isNsCharOrWhitespace$1(e5), s = r && !isWhitespace$2(e5);
  return (n ? r : r && e5 !== CHAR_COMMA$2 && e5 !== CHAR_LEFT_SQUARE_BRACKET$2 && e5 !== CHAR_RIGHT_SQUARE_BRACKET$2 && e5 !== CHAR_LEFT_CURLY_BRACKET$2 && e5 !== CHAR_RIGHT_CURLY_BRACKET$2) && e5 !== CHAR_SHARP$2 && !(t === CHAR_COLON$2 && !s) || isNsCharOrWhitespace$1(t) && !isWhitespace$2(t) && e5 === CHAR_SHARP$2 || t === CHAR_COLON$2 && s;
}
function isPlainSafeFirst$2(e5) {
  return isPrintable$2(e5) && e5 !== CHAR_BOM$1 && !isWhitespace$2(e5) && e5 !== CHAR_MINUS$2 && e5 !== CHAR_QUESTION$2 && e5 !== CHAR_COLON$2 && e5 !== CHAR_COMMA$2 && e5 !== CHAR_LEFT_SQUARE_BRACKET$2 && e5 !== CHAR_RIGHT_SQUARE_BRACKET$2 && e5 !== CHAR_LEFT_CURLY_BRACKET$2 && e5 !== CHAR_RIGHT_CURLY_BRACKET$2 && e5 !== CHAR_SHARP$2 && e5 !== CHAR_AMPERSAND$2 && e5 !== CHAR_ASTERISK$2 && e5 !== CHAR_EXCLAMATION$2 && e5 !== CHAR_VERTICAL_LINE$2 && e5 !== CHAR_EQUALS$2 && e5 !== CHAR_GREATER_THAN$2 && e5 !== CHAR_SINGLE_QUOTE$2 && e5 !== CHAR_DOUBLE_QUOTE$2 && e5 !== CHAR_PERCENT$2 && e5 !== CHAR_COMMERCIAL_AT$2 && e5 !== CHAR_GRAVE_ACCENT$2;
}
function isPlainSafeLast$1(e5) {
  return !isWhitespace$2(e5) && e5 !== CHAR_COLON$2;
}
function codePointAt$1(e5, t) {
  var n = e5.charCodeAt(t), r;
  return n >= 55296 && n <= 56319 && t + 1 < e5.length && (r = e5.charCodeAt(t + 1), r >= 56320 && r <= 57343) ? (n - 55296) * 1024 + r - 56320 + 65536 : n;
}
function needIndentIndicator$2(e5) {
  var t = /^\n* /;
  return t.test(e5);
}
var STYLE_PLAIN$2 = 1, STYLE_SINGLE$2 = 2, STYLE_LITERAL$2 = 3, STYLE_FOLDED$2 = 4, STYLE_DOUBLE$2 = 5;
function chooseScalarStyle$2(e5, t, n, r, s, o, a, l) {
  var c, u = 0, p = null, f = false, h = false, m = r !== -1, g = -1, d = isPlainSafeFirst$2(codePointAt$1(e5, 0)) && isPlainSafeLast$1(codePointAt$1(e5, e5.length - 1));
  if (t || a) for (c = 0; c < e5.length; u >= 65536 ? c += 2 : c++) {
    if (u = codePointAt$1(e5, c), !isPrintable$2(u)) return STYLE_DOUBLE$2;
    d = d && isPlainSafe$2(u, p, l), p = u;
  }
  else {
    for (c = 0; c < e5.length; u >= 65536 ? c += 2 : c++) {
      if (u = codePointAt$1(e5, c), u === CHAR_LINE_FEED$2) f = true, m && (h = h || c - g - 1 > r && e5[g + 1] !== " ", g = c);
      else if (!isPrintable$2(u)) return STYLE_DOUBLE$2;
      d = d && isPlainSafe$2(u, p, l), p = u;
    }
    h = h || m && c - g - 1 > r && e5[g + 1] !== " ";
  }
  return !f && !h ? d && !a && !s(e5) ? STYLE_PLAIN$2 : o === QUOTING_TYPE_DOUBLE$1 ? STYLE_DOUBLE$2 : STYLE_SINGLE$2 : n > 9 && needIndentIndicator$2(e5) ? STYLE_DOUBLE$2 : a ? o === QUOTING_TYPE_DOUBLE$1 ? STYLE_DOUBLE$2 : STYLE_SINGLE$2 : h ? STYLE_FOLDED$2 : STYLE_LITERAL$2;
}
function writeScalar$2(e5, t, n, r, s) {
  e5.dump = (function() {
    if (t.length === 0) return e5.quotingType === QUOTING_TYPE_DOUBLE$1 ? '""' : "''";
    if (!e5.noCompatMode && (DEPRECATED_BOOLEANS_SYNTAX$2.indexOf(t) !== -1 || DEPRECATED_BASE60_SYNTAX$1.test(t))) return e5.quotingType === QUOTING_TYPE_DOUBLE$1 ? '"' + t + '"' : "'" + t + "'";
    var o = e5.indent * Math.max(1, n), a = e5.lineWidth === -1 ? -1 : Math.max(Math.min(e5.lineWidth, 40), e5.lineWidth - o), l = r || e5.flowLevel > -1 && n >= e5.flowLevel;
    function c(u) {
      return testImplicitResolving$2(e5, u);
    }
    switch (chooseScalarStyle$2(t, l, e5.indent, a, c, e5.quotingType, e5.forceQuotes && !r, s)) {
      case STYLE_PLAIN$2:
        return t;
      case STYLE_SINGLE$2:
        return "'" + t.replace(/'/g, "''") + "'";
      case STYLE_LITERAL$2:
        return "|" + blockHeader$2(t, e5.indent) + dropEndingNewline$2(indentString$2(t, o));
      case STYLE_FOLDED$2:
        return ">" + blockHeader$2(t, e5.indent) + dropEndingNewline$2(indentString$2(foldString$2(t, a), o));
      case STYLE_DOUBLE$2:
        return '"' + escapeString$2(t) + '"';
      default:
        throw new exception$2("impossible error: invalid scalar style");
    }
  })();
}
function blockHeader$2(e5, t) {
  var n = needIndentIndicator$2(e5) ? String(t) : "", r = e5[e5.length - 1] === `
`, s = r && (e5[e5.length - 2] === `
` || e5 === `
`), o = s ? "+" : r ? "" : "-";
  return n + o + `
`;
}
function dropEndingNewline$2(e5) {
  return e5[e5.length - 1] === `
` ? e5.slice(0, -1) : e5;
}
function foldString$2(e5, t) {
  for (var n = /(\n+)([^\n]*)/g, r = (function() {
    var u = e5.indexOf(`
`);
    return u = u !== -1 ? u : e5.length, n.lastIndex = u, foldLine$2(e5.slice(0, u), t);
  })(), s = e5[0] === `
` || e5[0] === " ", o, a; a = n.exec(e5); ) {
    var l = a[1], c = a[2];
    o = c[0] === " ", r += l + (!s && !o && c !== "" ? `
` : "") + foldLine$2(c, t), s = o;
  }
  return r;
}
function foldLine$2(e5, t) {
  if (e5 === "" || e5[0] === " ") return e5;
  for (var n = / [^ ]/g, r, s = 0, o, a = 0, l = 0, c = ""; r = n.exec(e5); ) l = r.index, l - s > t && (o = a > s ? a : l, c += `
` + e5.slice(s, o), s = o + 1), a = l;
  return c += `
`, e5.length - s > t && a > s ? c += e5.slice(s, a) + `
` + e5.slice(a + 1) : c += e5.slice(s), c.slice(1);
}
function escapeString$2(e5) {
  for (var t = "", n = 0, r, s = 0; s < e5.length; n >= 65536 ? s += 2 : s++) n = codePointAt$1(e5, s), r = ESCAPE_SEQUENCES$2[n], !r && isPrintable$2(n) ? (t += e5[s], n >= 65536 && (t += e5[s + 1])) : t += r || encodeHex$2(n);
  return t;
}
function writeFlowSequence$2(e5, t, n) {
  var r = "", s = e5.tag, o, a, l;
  for (o = 0, a = n.length; o < a; o += 1) l = n[o], e5.replacer && (l = e5.replacer.call(n, String(o), l)), (writeNode$2(e5, t, l, false, false) || typeof l > "u" && writeNode$2(e5, t, null, false, false)) && (r !== "" && (r += "," + (e5.condenseFlow ? "" : " ")), r += e5.dump);
  e5.tag = s, e5.dump = "[" + r + "]";
}
function writeBlockSequence$2(e5, t, n, r) {
  var s = "", o = e5.tag, a, l, c;
  for (a = 0, l = n.length; a < l; a += 1) c = n[a], e5.replacer && (c = e5.replacer.call(n, String(a), c)), (writeNode$2(e5, t + 1, c, true, true, false, true) || typeof c > "u" && writeNode$2(e5, t + 1, null, true, true, false, true)) && ((!r || s !== "") && (s += generateNextLine$2(e5, t)), e5.dump && CHAR_LINE_FEED$2 === e5.dump.charCodeAt(0) ? s += "-" : s += "- ", s += e5.dump);
  e5.tag = o, e5.dump = s || "[]";
}
function writeFlowMapping$2(e5, t, n) {
  var r = "", s = e5.tag, o = Object.keys(n), a, l, c, u, p;
  for (a = 0, l = o.length; a < l; a += 1) p = "", r !== "" && (p += ", "), e5.condenseFlow && (p += '"'), c = o[a], u = n[c], e5.replacer && (u = e5.replacer.call(n, c, u)), writeNode$2(e5, t, c, false, false) && (e5.dump.length > 1024 && (p += "? "), p += e5.dump + (e5.condenseFlow ? '"' : "") + ":" + (e5.condenseFlow ? "" : " "), writeNode$2(e5, t, u, false, false) && (p += e5.dump, r += p));
  e5.tag = s, e5.dump = "{" + r + "}";
}
function writeBlockMapping$2(e5, t, n, r) {
  var s = "", o = e5.tag, a = Object.keys(n), l, c, u, p, f, h;
  if (e5.sortKeys === true) a.sort();
  else if (typeof e5.sortKeys == "function") a.sort(e5.sortKeys);
  else if (e5.sortKeys) throw new exception$2("sortKeys must be a boolean or a function");
  for (l = 0, c = a.length; l < c; l += 1) h = "", (!r || s !== "") && (h += generateNextLine$2(e5, t)), u = a[l], p = n[u], e5.replacer && (p = e5.replacer.call(n, u, p)), writeNode$2(e5, t + 1, u, true, true, true) && (f = e5.tag !== null && e5.tag !== "?" || e5.dump && e5.dump.length > 1024, f && (e5.dump && CHAR_LINE_FEED$2 === e5.dump.charCodeAt(0) ? h += "?" : h += "? "), h += e5.dump, f && (h += generateNextLine$2(e5, t)), writeNode$2(e5, t + 1, p, true, f) && (e5.dump && CHAR_LINE_FEED$2 === e5.dump.charCodeAt(0) ? h += ":" : h += ": ", h += e5.dump, s += h));
  e5.tag = o, e5.dump = s || "{}";
}
function detectType$2(e5, t, n) {
  var r, s, o, a, l, c;
  for (s = n ? e5.explicitTypes : e5.implicitTypes, o = 0, a = s.length; o < a; o += 1) if (l = s[o], (l.instanceOf || l.predicate) && (!l.instanceOf || typeof t == "object" && t instanceof l.instanceOf) && (!l.predicate || l.predicate(t))) {
    if (n ? l.multi && l.representName ? e5.tag = l.representName(t) : e5.tag = l.tag : e5.tag = "?", l.represent) {
      if (c = e5.styleMap[l.tag] || l.defaultStyle, _toString$6.call(l.represent) === "[object Function]") r = l.represent(t, c);
      else if (_hasOwnProperty$8.call(l.represent, c)) r = l.represent[c](t, c);
      else throw new exception$2("!<" + l.tag + '> tag resolver accepts not "' + c + '" style');
      e5.dump = r;
    }
    return true;
  }
  return false;
}
function writeNode$2(e5, t, n, r, s, o, a) {
  e5.tag = null, e5.dump = n, detectType$2(e5, n, false) || detectType$2(e5, n, true);
  var l = _toString$6.call(e5.dump), c = r, u;
  r && (r = e5.flowLevel < 0 || e5.flowLevel > t);
  var p = l === "[object Object]" || l === "[object Array]", f, h;
  if (p && (f = e5.duplicates.indexOf(n), h = f !== -1), (e5.tag !== null && e5.tag !== "?" || h || e5.indent !== 2 && t > 0) && (s = false), h && e5.usedDuplicates[f]) e5.dump = "*ref_" + f;
  else {
    if (p && h && !e5.usedDuplicates[f] && (e5.usedDuplicates[f] = true), l === "[object Object]") r && Object.keys(e5.dump).length !== 0 ? (writeBlockMapping$2(e5, t, e5.dump, s), h && (e5.dump = "&ref_" + f + e5.dump)) : (writeFlowMapping$2(e5, t, e5.dump), h && (e5.dump = "&ref_" + f + " " + e5.dump));
    else if (l === "[object Array]") r && e5.dump.length !== 0 ? (e5.noArrayIndent && !a && t > 0 ? writeBlockSequence$2(e5, t - 1, e5.dump, s) : writeBlockSequence$2(e5, t, e5.dump, s), h && (e5.dump = "&ref_" + f + e5.dump)) : (writeFlowSequence$2(e5, t, e5.dump), h && (e5.dump = "&ref_" + f + " " + e5.dump));
    else if (l === "[object String]") e5.tag !== "?" && writeScalar$2(e5, e5.dump, t, o, c);
    else {
      if (l === "[object Undefined]") return false;
      if (e5.skipInvalid) return false;
      throw new exception$2("unacceptable kind of an object to dump " + l);
    }
    e5.tag !== null && e5.tag !== "?" && (u = encodeURI(e5.tag[0] === "!" ? e5.tag.slice(1) : e5.tag).replace(/!/g, "%21"), e5.tag[0] === "!" ? u = "!" + u : u.slice(0, 18) === "tag:yaml.org,2002:" ? u = "!!" + u.slice(18) : u = "!<" + u + ">", e5.dump = u + " " + e5.dump);
  }
  return true;
}
function getDuplicateReferences$2(e5, t) {
  var n = [], r = [], s, o;
  for (inspectNode$2(e5, n, r), s = 0, o = r.length; s < o; s += 1) t.duplicates.push(n[r[s]]);
  t.usedDuplicates = new Array(o);
}
function inspectNode$2(e5, t, n) {
  var r, s, o;
  if (e5 !== null && typeof e5 == "object") if (s = t.indexOf(e5), s !== -1) n.indexOf(s) === -1 && n.push(s);
  else if (t.push(e5), Array.isArray(e5)) for (s = 0, o = e5.length; s < o; s += 1) inspectNode$2(e5[s], t, n);
  else for (r = Object.keys(e5), s = 0, o = r.length; s < o; s += 1) inspectNode$2(e5[r[s]], t, n);
}
function dump$1$1(e5, t) {
  t = t || {};
  var n = new State$4(t);
  n.noRefs || getDuplicateReferences$2(e5, n);
  var r = e5;
  return n.replacer && (r = n.replacer.call({ "": r }, "", r)), writeNode$2(n, 0, r, true, true) ? n.dump + `
` : "";
}
var dump_1$1 = dump$1$1, dumper$3 = { dump: dump_1$1 };
function renamed$1(e5, t) {
  return function() {
    throw new Error("Function yaml." + e5 + " is removed in js-yaml 4. Use yaml." + t + " instead, which is now safe by default.");
  };
}
var Type$k = type$2, Schema$b = schema$9, FAILSAFE_SCHEMA$1 = failsafe$2, JSON_SCHEMA$1 = json$5, CORE_SCHEMA$1 = core$2, DEFAULT_SCHEMA$1 = _default$1, load$3 = loader$3.load, loadAll$3 = loader$3.loadAll, dump$3 = dumper$3.dump, YAMLException$7 = exception$2, types$2 = { binary: binary$6, float: float$9, map: map$9, null: _null$6, pairs: pairs$7, set: set$6, timestamp: timestamp$6, bool: bool$7, int: int$9, merge: merge$7, omap: omap$6, seq: seq$9, str: str$2 }, safeLoad$2 = renamed$1("safeLoad", "load"), safeLoadAll$2 = renamed$1("safeLoadAll", "loadAll"), safeDump$2 = renamed$1("safeDump", "dump"), jsYaml$3 = { Type: Type$k, Schema: Schema$b, FAILSAFE_SCHEMA: FAILSAFE_SCHEMA$1, JSON_SCHEMA: JSON_SCHEMA$1, CORE_SCHEMA: CORE_SCHEMA$1, DEFAULT_SCHEMA: DEFAULT_SCHEMA$1, load: load$3, loadAll: loadAll$3, dump: dump$3, YAMLException: YAMLException$7, types: types$2, safeLoad: safeLoad$2, safeLoadAll: safeLoadAll$2, safeDump: safeDump$2 };
const yamlSerializer$1 = { format: "yaml", async serializeDocumentFileContents(e5, t) {
  return jsYaml$3.dump(e5, { noRefs: true });
}, async deserializeDocumentFileContents(e5, t) {
  return jsYaml$3.load(e5, { schema: jsYaml$3.JSON_SCHEMA });
} }, defaultDevUser$1 = { id: "dev", email: "dev@local.test", name: "Dev Editor" };
function resolveAuth$1(e5) {
  var _a4, _b;
  if (e5.mode === "dev") {
    const t = (_a4 = e5.devToken) != null ? _a4 : "dev-local-laika-token", n = (_b = e5.devUser) != null ? _b : defaultDevUser$1;
    return { authenticateAccessToken: async (r) => {
      if (r !== t) throw new qi$2("Unknown token (createCustomLaika is in dev mode)");
      return { ...n };
    } };
  }
  return { authenticateAccessToken: e5.authenticateAccessToken, authenticateApiToken: e5.authenticateApiToken };
}
function createCustomLaika$1(e5) {
  var _a4, _b, _c2, _d;
  const t = (_a4 = e5.basePath) != null ? _a4 : "/api/decap", n = (_b = e5.configKey) != null ? _b : "config", r = (_c2 = e5.logger) != null ? _c2 : console, s = (_d = e5.seedConfigOnFirstRequest) != null ? _d : true, o = new DecapContentBaseSettingsProvider$1({ storage: e5.storage, configKey: n }), a = new ContentBaseDocumentsRepository$1(e5.storage, o), l = new ContentBaseAssetsRepository$1(e5.storage, o), c = decapApi$1({ documents: a, storage: e5.storage, assets: l, basePath: t, logger: r, ...resolveAuth$1(e5.auth) });
  let u = false;
  return { fetch: async (f) => {
    var _a5, _b2;
    if (s && !u) {
      u = true;
      const h = `${n}.yml`;
      try {
        await runTask$1$1(e5.storage.getObject(h));
      } catch (m) {
        if (m instanceof Mi$2) try {
          await runTask$1$1(e5.storage.createObject({ key: h, content: stringify$3$1(e5.decapConfig) }));
        } catch (g) {
          (_a5 = r.warn) == null ? void 0 : _a5.call(r, "createCustomLaika: failed to seed config:", g);
        }
        else (_b2 = r.warn) == null ? void 0 : _b2.call(r, "createCustomLaika: failed to check config presence:", m);
      }
    }
    return c.fetch(f);
  }, authenticateRequest: c.authenticateRequest, storage: e5.storage, documents: a, assets: l, decapConfig: e5.decapConfig };
}
const safeText$1 = async (e5) => {
  try {
    return await e5.text();
  } catch {
    return "";
  }
}, bind$1 = (e5) => {
  if (e5 == null) return { type: "null" };
  if (typeof e5 == "string") return { type: "text", value: e5 };
  if (typeof e5 == "number") return Number.isInteger(e5) ? { type: "integer", value: String(e5) } : { type: "float", value: e5 };
  if (typeof e5 == "bigint") return { type: "integer", value: e5.toString() };
  if (typeof e5 == "boolean") return { type: "integer", value: e5 ? "1" : "0" };
  if (e5 instanceof Uint8Array) {
    let t = "";
    for (const n of e5) t += String.fromCharCode(n);
    return { type: "blob", base64: btoa(t) };
  }
  throw new Ge$2(`Cannot bind value of type ${typeof e5} to a libSQL argument`);
}, dec$1 = new TextDecoder(), unbind$1 = (e5) => {
  switch (e5.type) {
    case "null":
      return null;
    case "integer":
      return e5.value;
    case "float":
      return e5.value;
    case "text":
      return e5.value;
    case "blob": {
      const t = atob(e5.base64), n = new Uint8Array(t.length);
      for (let r = 0; r < t.length; r += 1) n[r] = t.charCodeAt(r);
      return dec$1.decode(n);
    }
  }
}, rowToObject$1 = (e5, t) => {
  const n = {};
  for (let r = 0; r < e5.length; r += 1) {
    const s = e5[r], o = t[r];
    s && o && (n[s.name] = unbind$1(o));
  }
  return n;
}, errorForResponse$1 = (e5, t, n) => {
  var _a4;
  let r = "";
  try {
    const s = JSON.parse(t), o = (_a4 = s.message) != null ? _a4 : s.error;
    o && (r = `: ${o}`);
  } catch {
  }
  switch (e5) {
    case 401:
      return we$5(new qi$2(`libSQL authentication failed for ${n}${r}`));
    case 403:
      return we$5(new Vi$2(`libSQL access denied for ${n}${r}`));
    case 404:
      return we$5(new Mi$2(`libSQL not found: ${n}`));
    case 429:
      return we$5(new Zi$2(`libSQL rate-limited request for ${n}`));
    default:
      return e5 >= 500 ? we$5(new qe$5(`libSQL returned HTTP ${e5} for ${n}`)) : we$5(new Ge$2(`libSQL returned HTTP ${e5} for ${n}${r}`));
  }
}, errorForStatement$1 = (e5, t) => {
  var _a4, _b;
  const n = (_a4 = e5.message) != null ? _a4 : "";
  return /UNIQUE constraint failed|SQLITE_CONSTRAINT/i.test(n) || ((_b = e5.code) == null ? void 0 : _b.startsWith("SQLITE_CONSTRAINT")) ? new Wi$2(`libSQL unique constraint failed for ${t}: ${n}`) : new Ge$2(`libSQL statement failed for ${t}: ${n}`);
};
let LibSqlDataSource$1 = class LibSqlDataSource {
  constructor(t) {
    __publicField$5(this, "fetchImpl");
    __publicField$5(this, "auth");
    __publicField$5(this, "baseUrl");
    var _a4, _b;
    if (this.fetchImpl = (_a4 = t.fetch) != null ? _a4 : globalThis.fetch, typeof this.fetchImpl != "function") throw new Ge$2("No `fetch` implementation available; pass one via LibSqlDataSourceOptions.fetch");
    this.auth = (_b = t.auth) != null ? _b : {}, this.baseUrl = t.url.replace(/\/+$/, "");
  }
  async execute(t, n = []) {
    const r = { sql: t, args: n.map(bind$1) }, s = await this.pipelineRequest([{ type: "execute", stmt: r }], `execute: ${t.slice(0, 60)}`);
    if (X$4(s)) return we$5(s.failure);
    const o = s.success.results[0];
    return o ? o.type === "error" ? we$5(errorForStatement$1(o.error, t.slice(0, 60))) : o.response.type !== "execute" ? we$5(new Ge$2("libSQL execute returned non-execute response")) : lt$2(o.response.result) : we$5(new Ge$2("libSQL execute returned no result"));
  }
  async batch(t) {
    if (t.length === 0) return lt$2([]);
    const n = t.map((l, c) => {
      var _a4;
      const u = { sql: l.sql, args: ((_a4 = l.args) != null ? _a4 : []).map(bind$1) };
      return c === 0 ? { stmt: u } : { stmt: u, condition: { type: "ok", step: c - 1 } };
    }), r = await this.pipelineRequest([{ type: "batch", batch: { steps: n } }], "batch");
    if (X$4(r)) return we$5(r.failure);
    const s = r.success.results[0];
    if (!s) return we$5(new Ge$2("libSQL batch returned no result"));
    if (s.type === "error") return we$5(errorForStatement$1(s.error, "batch"));
    if (s.response.type !== "batch") return we$5(new Ge$2("libSQL batch returned non-batch response"));
    const o = s.response.result.step_results, a = s.response.result.step_errors;
    for (let l = 0; l < a.length; l += 1) {
      const c = a[l];
      if (c) return we$5(errorForStatement$1(c, `batch step ${l}`));
    }
    return lt$2(o.filter((l) => l !== null));
  }
  async pipelineRequest(t, n) {
    const r = await this.accessToken();
    let s;
    try {
      s = await this.fetchImpl(`${this.baseUrl}/v2/pipeline`, { method: "POST", headers: { "Content-Type": "application/json", ...r ? { Authorization: `Bearer ${r}` } : {} }, body: JSON.stringify({ requests: t }) });
    } catch (a) {
      return we$5(new qe$5("libSQL unreachable", { cause: a }));
    }
    if (!s.ok) return errorForResponse$1(s.status, await safeText$1(s), n);
    const o = await s.json();
    return lt$2(o);
  }
  async accessToken() {
    return this.auth.tokenProvider ? await this.auth.tokenProvider() : this.auth.token;
  }
};
const DEFAULT_TABLE$1 = "laika_storage", DEFAULT_IGNORE_LIST$1 = ["**/.keep", "**/.DS_Store", "**/Thumbs.db", "**/desktop.ini", "**/.contentbase", "**/.laikacms"], TYPE_FILE$1 = "file", TYPE_FOLDER$1 = "folder", liftResult$1 = (e5) => K$2(Dm$1(() => e5), Nm$1), stripSlashes$1 = (e5) => e5.replace(/^\/+|\/+$/g, ""), splitPath$1 = (e5) => {
  const t = stripSlashes$1(e5), n = t.lastIndexOf("/");
  return n === -1 ? { parent: "", name: t } : { parent: t.slice(0, n), name: t.slice(n + 1) };
}, validateIdentifier$1 = (e5) => {
  if (!/^[A-Za-z_][A-Za-z0-9_]*$/.test(e5)) throw new zi$2(`Invalid SQL identifier: ${e5}`);
}, decodeRow$1 = (e5, t) => {
  var _a4, _b, _c2;
  const n = rowToObject$1(e5, t);
  return { Path: String((_a4 = n.Path) != null ? _a4 : ""), Parent: String((_b = n.Parent) != null ? _b : ""), Name: String((_c2 = n.Name) != null ? _c2 : ""), Type: n.Type === TYPE_FILE$1 ? TYPE_FILE$1 : TYPE_FOLDER$1, Extension: n.Extension === null || n.Extension === void 0 ? null : String(n.Extension), Content: n.Content === null || n.Content === void 0 ? null : String(n.Content) };
};
let LibSqlStorageRepository$1 = class LibSqlStorageRepository extends StorageRepository$1 {
  constructor(t) {
    super();
    __publicField$5(this, "dataSource");
    __publicField$5(this, "tableName");
    __publicField$5(this, "serializerRegistry");
    __publicField$5(this, "defaultFileExtension");
    __publicField$5(this, "availableExtensions");
    __publicField$5(this, "excludeFilter");
    __publicField$5(this, "determineExtension");
    const { dataSource: n, tableName: r = DEFAULT_TABLE$1, serializerRegistry: s, defaultFileExtension: o, ignoreList: a = DEFAULT_IGNORE_LIST$1, determineExtension: l = defaultDetermineExtension$1 } = t;
    validateIdentifier$1(r), this.tableName = r, this.dataSource = n, this.serializerRegistry = s, this.defaultFileExtension = o.startsWith(".") ? o.slice(1) : o, this.availableExtensions = Object.keys(s), this.determineExtension = l, this.excludeFilter = a.map((c) => makeRe$1(c, { dot: true, partial: true })).filter((c) => c !== false);
  }
  stripExtension(t) {
    for (const n of this.availableExtensions) if (t.endsWith(`.${n}`)) return t.slice(0, -(n.length + 1));
    return t;
  }
  resolveExtension(t, n) {
    const r = this.determineExtension(t, { metadata: n, defaultExtension: this.defaultFileExtension });
    return r && this.serializerRegistry[r] ? r : this.defaultFileExtension;
  }
  async serialize(t, n) {
    const r = t.startsWith(".") ? t.slice(1) : t, s = this.serializerRegistry[r];
    if (!s) throw new zi$2(`No serializer found for file extension: .${r}. Available formats: ${this.availableExtensions.join(", ")}`);
    return s.serializeDocumentFileContents(n, {});
  }
  async deserialize(t, n) {
    const r = t.startsWith(".") ? t.slice(1) : t, s = this.serializerRegistry[r];
    if (!s) throw new zi$2(`No serializer found for file extension: .${r}. Available formats: ${this.availableExtensions.join(", ")}`);
    return s.deserializeDocumentFileContents(n, {});
  }
  filePath(t, n) {
    return `${stripSlashes$1(this.stripExtension(t))}.${n}`;
  }
  async findFileRow(t) {
    const { parent: n, name: r } = splitPath$1(this.stripExtension(t)), s = await this.dataSource.execute(`SELECT Path, Parent, Name, Type, Extension, Content
       FROM ${this.tableName}
       WHERE Type = ? AND Parent = ? AND Name = ?
       LIMIT 1`, [TYPE_FILE$1, n, r]);
    if (X$4(s)) return null;
    const o = s.success.rows[0];
    return o ? decodeRow$1(s.success.cols, o) : null;
  }
  async hasFolder(t) {
    const n = stripSlashes$1(t);
    if (n === "") {
      const s = await this.dataSource.execute(`SELECT 1 FROM ${this.tableName} LIMIT 1`, []);
      return X$4(s) ? false : s.success.rows.length > 0;
    }
    const r = await this.dataSource.execute(`SELECT 1 FROM ${this.tableName}
       WHERE (Path = ? AND Type = ?)
          OR Parent = ?
       LIMIT 1`, [n, TYPE_FOLDER$1, n]);
    return X$4(r) ? false : r.success.rows.length > 0;
  }
  getObject(t) {
    return sc$1(() => ne$4({ self: this }, function* () {
      var _a4;
      const n = yield* Dm$1(() => this.findFileRow(t));
      if (!n) return yield* j$2(new Mi$2(`libSQL row not found: ${t}`));
      const r = (_a4 = n.Extension) != null ? _a4 : this.defaultFileExtension, s = yield* Dm$1(() => {
        var _a5;
        return this.deserialize(r, (_a5 = n.Content) != null ? _a5 : "");
      }), o = n.Parent === "" ? n.Name : `${n.Parent}/${n.Name}`, a = (/* @__PURE__ */ new Date()).toISOString();
      return { type: "object", key: o, createdAt: a, updatedAt: a, content: s, metadata: { extension: r, revisionId: n.Path } };
    }));
  }
  getFolder(t) {
    return sc$1(() => ne$4({ self: this }, function* () {
      if (!(yield* Dm$1(() => this.hasFolder(t)))) return yield* j$2(new Mi$2(`libSQL folder not found: ${t || "<root>"}`));
      const r = (/* @__PURE__ */ new Date()).toISOString();
      return { type: "folder", key: t, createdAt: r, updatedAt: r };
    }));
  }
  getAtom(t) {
    return sc$1(() => ne$4({ self: this }, function* () {
      if (yield* Dm$1(() => this.findFileRow(t))) return yield* us$2(this.getObject(t));
      const r = yield* di$2(us$2(this.getFolder(t)));
      return jt$2(r) ? r.success : yield* j$2(new zi$2(`Path not found: ${t}`));
    }));
  }
  createObject(t) {
    return sc$1(() => ne$4({ self: this }, function* () {
      var _a4;
      if (!t.content) return yield* j$2(new zi$2("Object content is required for creation"));
      const { parent: n, name: r } = splitPath$1(this.stripExtension(t.key)), s = yield* Dm$1(() => this.findFileRow(t.key));
      if (s) return yield* j$2(new Wi$2(`An object with key "${t.key}" already exists with extension .${(_a4 = s.Extension) != null ? _a4 : this.defaultFileExtension}`));
      const o = this.resolveExtension(t.key, t.metadata), a = yield* Dm$1(() => this.serialize(o, t.content)), l = this.filePath(t.key, o);
      return yield* liftResult$1(this.dataSource.execute(`INSERT INTO ${this.tableName} (Path, Parent, Name, Type, Extension, Content)
           VALUES (?, ?, ?, ?, ?, ?)`, [l, n, r, TYPE_FILE$1, o, a])), yield* us$2(this.getObject(t.key));
    }));
  }
  updateObject(t) {
    return sc$1(() => ne$4({ self: this }, function* () {
      var _a4;
      const n = yield* Dm$1(() => this.findFileRow(t.key));
      if (!n) return yield* j$2(new Mi$2(`libSQL row not found: ${t.key}`));
      if (t.content) {
        const r = (_a4 = n.Extension) != null ? _a4 : this.defaultFileExtension, s = yield* Dm$1(() => this.serialize(r, t.content));
        yield* liftResult$1(this.dataSource.execute(`UPDATE ${this.tableName} SET Content = ? WHERE Path = ?`, [s, n.Path]));
      }
      return yield* us$2(this.getObject(t.key));
    }));
  }
  createOrUpdateObject(t) {
    return sc$1(() => ne$4({ self: this }, function* () {
      var _a4, _b;
      const { parent: n, name: r } = splitPath$1(this.stripExtension(t.key)), o = (_b = (_a4 = yield* Dm$1(() => this.findFileRow(t.key))) == null ? void 0 : _a4.Extension) != null ? _b : this.resolveExtension(t.key, t.metadata), a = t.content ? yield* Dm$1(() => this.serialize(o, t.content)) : "", l = this.filePath(t.key, o);
      return yield* liftResult$1(this.dataSource.execute(`INSERT INTO ${this.tableName} (Path, Parent, Name, Type, Extension, Content)
           VALUES (?, ?, ?, ?, ?, ?)
           ON CONFLICT(Path) DO UPDATE SET Content = excluded.Content, Extension = excluded.Extension`, [l, n, r, TYPE_FILE$1, o, a])), yield* us$2(this.getObject(t.key));
    }));
  }
  createFolder(t) {
    return sc$1(() => ne$4({ self: this }, function* () {
      const n = stripSlashes$1(t.key);
      if (n === "") return yield* us$2(this.getFolder(""));
      const { parent: r, name: s } = splitPath$1(n);
      return yield* liftResult$1(this.dataSource.execute(`INSERT INTO ${this.tableName} (Path, Parent, Name, Type)
           VALUES (?, ?, ?, ?)
           ON CONFLICT(Path) DO NOTHING`, [n, r, s, TYPE_FOLDER$1])), yield* us$2(this.getFolder(n));
    }));
  }
  removeAtoms(t) {
    return ls$2((n) => ne$4({ self: this }, function* () {
      const r = t.map((c) => stripSlashes$1(c)).filter((c) => c !== ""), s = t.length - r.length;
      if (r.length === 0) {
        for (let c = 0; c < s; c++) yield* n.recoverableError(new zi$2("Refusing to delete empty key"));
        return { removed: 0, skipped: s };
      }
      const o = yield* Dm$1(async () => {
        const c = [];
        for (const u of r) {
          const p = await this.findFileRow(u);
          c.push({ key: u, resolved: p });
        }
        return c;
      }), a = o.filter((c) => c.resolved !== null), l = o.filter((c) => c.resolved === null);
      a.length > 0 && (yield* liftResult$1(this.dataSource.batch(a.map((c) => ({ sql: `DELETE FROM ${this.tableName} WHERE Path = ?`, args: [c.resolved.Path] })))));
      for (const c of a) yield* n.data(c.key);
      for (const c of l) yield* n.recoverableError(new Mi$2(`libSQL row not found: ${c.key}`));
      return { removed: a.length, skipped: s + l.length };
    }));
  }
  listAtomSummaries(t, n) {
    return ls$2((r) => ne$4({ self: this }, function* () {
      const s = yield* this.collectFilteredSummaries(t, n);
      return s.length > 0 && (yield* r.dataMany(s)), { total: s.length };
    }));
  }
  listAtoms(t, n) {
    return ls$2((r) => ne$4({ self: this }, function* () {
      const s = yield* this.collectFilteredSummaries(t, n);
      for (const o of s) if (o.type === "object-summary") {
        const a = yield* di$2(us$2(this.getObject(o.key)));
        X$4(a) ? yield* r.recoverableError(a.failure) : yield* r.data(a.success);
      } else {
        const a = yield* di$2(us$2(this.getFolder(o.key)));
        X$4(a) ? yield* r.recoverableError(a.failure) : yield* r.data(a.success);
      }
      return { total: s.length };
    }));
  }
  collectFilteredSummaries(t, n) {
    return ne$4({ self: this }, function* () {
      const r = stripSlashes$1(t), s = yield* liftResult$1(this.dataSource.execute(`SELECT Path, Parent, Name, Type, Extension, Content
         FROM ${this.tableName}
         WHERE Parent = ?`, [r])), c = [...s.rows.map((u) => decodeRow$1(s.cols, u)).map((u) => {
        const p = u.Parent === "" ? u.Name : `${u.Parent}/${u.Name}`;
        return u.Type === TYPE_FILE$1 ? { type: "object-summary", key: p } : { type: "folder-summary", key: p };
      }).filter((u) => this.excludeFilter.every((p) => !p.test(u.key)))].sort((u, p) => naturalCompare$1(u.key, p.key));
      return applyPagination$1(c, n.pagination);
    });
  }
  getCapabilities() {
    return Lh$1({ compatibilityDate: mm$1.make("2026-05-20"), fileExtensions: { supported: true, description: "Each object is one row in a libSQL/SQLite table; the extension is stored in the `Extension` column.", supportedExtensions: this.serializerRegistry }, pagination: { supported: true, description: "In-memory slicing over SELECT results; LIMIT/OFFSET pushdown not yet wired.", styles: { offset: true, page: true, cursor: false } } });
  }
};
function requireEnv$1(e5) {
  const t = process.env[e5];
  if (!t) throw new Error(`Missing required env var: ${e5}`);
  return t;
}
const dataSource$1 = new LibSqlDataSource$1({ url: requireEnv$1("LIBSQL_URL"), auth: { token: process.env.LIBSQL_AUTH_TOKEN } }), storage$1 = new LibSqlStorageRepository$1({ dataSource: dataSource$1, tableName: (_a3$1 = process.env.LIBSQL_TABLE) != null ? _a3$1 : "laika_storage", serializerRegistry: { md: markdownSerializer$1, yaml: yamlSerializer$1, yml: yamlSerializer$1, json: jsonSerializer$1, raw: rawSerializer$1 }, defaultFileExtension: "md" }), laika$1 = createCustomLaika$1({ storage: storage$1, decapConfig: { backend: { name: "laika", api_url: "/api/decap" }, media_folder: "public/uploads", public_folder: "/uploads", collections: e$1 }, basePath: "/api/decap", auth: { mode: "dev" } });

var __defProp$4 = Object.defineProperty;
var __defNormalProp$4 = (obj, key, value) => key in obj ? __defProp$4(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField$4 = (obj, key, value) => __defNormalProp$4(obj, key + "" , value);
function J$2(t = {}) {
  let e, s = false;
  const o = (n) => {
    if (e && e !== n) throw new Error("Context conflict");
  };
  let a;
  if (t.asyncContext) {
    const n = t.AsyncLocalStorage || globalThis.AsyncLocalStorage;
    n ? a = new n() : console.warn("[unctx] `AsyncLocalStorage` is not provided.");
  }
  const i = () => {
    if (a) {
      const n = a.getStore();
      if (n !== void 0) return n;
    }
    return e;
  };
  return { use: () => {
    const n = i();
    if (n === void 0) throw new Error("Context is not available");
    return n;
  }, tryUse: () => i(), set: (n, c) => {
    c || o(n), e = n, s = true;
  }, unset: () => {
    e = void 0, s = false;
  }, call: (n, c) => {
    o(n), e = n;
    try {
      return a ? a.run(n, c) : c();
    } finally {
      s || (e = void 0);
    }
  }, async callAsync(n, c) {
    e = n;
    const u = () => {
      e = n;
    }, g = () => e === n ? u : void 0;
    h$3.add(g);
    try {
      const q = a ? a.run(n, c) : c();
      return s || (e = void 0), await q;
    } finally {
      h$3.delete(g);
    }
  } };
}
function Q$3(t = {}) {
  const e = {};
  return { get(s, o = {}) {
    return e[s] || (e[s] = J$2({ ...t, ...o })), e[s];
  } };
}
const p$1 = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof global < "u" ? global : {}, y$2 = "__unctx__", V$3 = p$1[y$2] || (p$1[y$2] = Q$3()), X$3 = (t, e = {}) => V$3.get(t, e), m$2 = "__unctx_async_handlers__", h$3 = p$1[m$2] || (p$1[m$2] = /* @__PURE__ */ new Set());
function Y$3(t) {
  let e;
  const s = w$2(t), o = { duplex: "half", method: t.method, headers: t.headers };
  return t.node.req.body instanceof ArrayBuffer ? new Request(s, { ...o, body: t.node.req.body }) : new Request(s, { ...o, get body() {
    return e || (e = oe$3(t), e);
  } });
}
function Z$3(t) {
  var _a;
  return (_a = t.web) != null ? _a : t.web = { request: Y$3(t), url: w$2(t) }, t.web.request;
}
function ee$3() {
  return ue$3();
}
const x$3 = /* @__PURE__ */ Symbol("$HTTPEvent");
function te$2(t) {
  return typeof t == "object" && (t instanceof H3Event || (t == null ? void 0 : t[x$3]) instanceof H3Event || (t == null ? void 0 : t.__is_event__) === true);
}
function r$1(t) {
  return function(...e) {
    var _a;
    let s = e[0];
    if (te$2(s)) e[0] = s instanceof H3Event || s.__is_event__ ? s : s[x$3];
    else {
      if (!((_a = globalThis.app.config.server.experimental) == null ? void 0 : _a.asyncContext)) throw new Error("AsyncLocalStorage was not enabled. Use the `server.experimental.asyncContext: true` option in your app configuration to enable it. Or, pass the instance of HTTPEvent that you have as the first argument to the function.");
      if (s = ee$3(), !s) throw new Error("No HTTPEvent found in AsyncLocalStorage. Make sure you are using the function within the server runtime.");
      e.unshift(s);
    }
    return t(...e);
  };
}
const w$2 = r$1(getRequestURL), se$3 = r$1(getRequestIP), R$4 = r$1(setResponseStatus), v$3 = r$1(getResponseStatus), ne$3 = r$1(getResponseStatusText), l$1 = r$1(getResponseHeaders), b$3 = r$1(getResponseHeader), re$2 = r$1(setResponseHeader), C$4 = r$1(appendResponseHeader), _e$5 = r$1(parseCookies), xe$4 = r$1(getCookie), we$4 = r$1(setCookie), Ce$4 = r$1(setHeader), oe$3 = r$1(getRequestWebStream), ae$3 = r$1(removeResponseHeader), ce$4 = r$1(Z$3);
function ie$3() {
  var _a;
  return X$3("nitro-app", { asyncContext: !!((_a = globalThis.app.config.server.experimental) == null ? void 0 : _a.asyncContext), AsyncLocalStorage: AsyncLocalStorage });
}
function ue$3() {
  return ie$3().use().event;
}
const d$2 = "solidFetchEvent";
function le$4(t) {
  return { request: ce$4(t), response: fe$4(t), clientAddress: se$3(t), locals: {}, nativeEvent: t };
}
function pe$4(t) {
  return { ...t };
}
function He$4(t) {
  if (!t.context[d$2]) {
    const e = le$4(t);
    t.context[d$2] = e;
  }
  return t.context[d$2];
}
function qe$4(t, e) {
  for (const [s, o] of e.entries()) C$4(t, s, o);
}
let de$4 = class de {
  constructor(e) {
    __publicField$4(this, "event");
    this.event = e;
  }
  get(e) {
    const s = b$3(this.event, e);
    return Array.isArray(s) ? s.join(", ") : s || null;
  }
  has(e) {
    return this.get(e) !== null;
  }
  set(e, s) {
    return re$2(this.event, e, s);
  }
  delete(e) {
    return ae$3(this.event, e);
  }
  append(e, s) {
    C$4(this.event, e, s);
  }
  getSetCookie() {
    const e = b$3(this.event, "Set-Cookie");
    return Array.isArray(e) ? e : [e];
  }
  forEach(e) {
    return Object.entries(l$1(this.event)).forEach(([s, o]) => e(Array.isArray(o) ? o.join(", ") : o, s, this));
  }
  entries() {
    return Object.entries(l$1(this.event)).map(([e, s]) => [e, Array.isArray(s) ? s.join(", ") : s])[Symbol.iterator]();
  }
  keys() {
    return Object.keys(l$1(this.event))[Symbol.iterator]();
  }
  values() {
    return Object.values(l$1(this.event)).map((e) => Array.isArray(e) ? e.join(", ") : e)[Symbol.iterator]();
  }
  [Symbol.iterator]() {
    return this.entries()[Symbol.iterator]();
  }
};
function fe$4(t) {
  return { get status() {
    return v$3(t);
  }, set status(e) {
    R$4(t, e);
  }, get statusText() {
    return ne$3(t);
  }, set statusText(e) {
    R$4(t, v$3(t), e);
  }, headers: new de$4(t) };
}
function H$2(t, e, s) {
  if (typeof t != "function") throw new Error("Export from a 'use server' module must be a function");
  const o = "";
  return new Proxy(t, { get(a, i, n) {
    return i === "url" ? `${o}/_server?id=${encodeURIComponent(e)}&name=${encodeURIComponent(s)}` : i === "GET" ? n : a[i];
  }, apply(a, i, n) {
    const c = getRequestEvent();
    if (!c) throw new Error("Cannot call server function outside of a request");
    const u = pe$4(c);
    return u.locals.serverFunctionMeta = { id: e + "#" + s }, u.serverOnly = true, provideRequestEvent(u, () => t.apply(i, n));
  } });
}
const ge$4 = H$2(async () => {
  const { items: t } = await collectStream$1(laika$1.documents.listRecordSummaries({ pagination: { page: 1, perPage: 100 }, folder: "posts", depth: 1, type: "published" }));
  return t.filter((e) => e.type === "published-summary").sort((e, s) => e.updatedAt && s.updatedAt ? s.updatedAt.localeCompare(e.updatedAt) : s.key.localeCompare(e.key));
}, "src_lib_content_ts--getPosts_query", "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/lib/content.ts?tsr-directive-use-server="), Ae$4 = query(ge$4, "posts"), ye$4 = H$2(async (t) => runTask$1$1(laika$1.documents.getDocument(`posts/${t}`)), "src_lib_content_ts--getPost_query", "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/lib/content.ts?tsr-directive-use-server="), Ee$3 = query(ye$4, "post");

var __defProp$3 = Object.defineProperty;
var __defNormalProp$3 = (obj, key, value) => key in obj ? __defProp$3(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField$3 = (obj, key, value) => __defNormalProp$3(obj, typeof key !== "symbol" ? key + "" : key, value);
function kr$1(e, t) {
  const r = (e || "").split(";").filter((c) => typeof c == "string" && !!c.trim()), n = r.shift() || "", s = Ar$1(n), i = s.name;
  let o = s.value;
  try {
    o = (t == null ? void 0 : t.decode) === false ? o : ((t == null ? void 0 : t.decode) || decodeURIComponent)(o);
  } catch {
  }
  const u = { name: i, value: o };
  for (const c of r) {
    const l = c.split("="), p = (l.shift() || "").trimStart().toLowerCase(), d = l.join("=");
    switch (p) {
      case "expires": {
        u.expires = new Date(d);
        break;
      }
      case "max-age": {
        u.maxAge = Number.parseInt(d, 10);
        break;
      }
      case "secure": {
        u.secure = true;
        break;
      }
      case "httponly": {
        u.httpOnly = true;
        break;
      }
      case "samesite": {
        u.sameSite = d;
        break;
      }
      default:
        u[p] = d;
    }
  }
  return u;
}
function Ar$1(e) {
  let t = "", r = "";
  const n = e.split("=");
  return n.length > 1 ? (t = n.shift(), r = n.join("=")) : r = e, { name: t, value: r };
}
const ce$3 = "Invariant Violation", { setPrototypeOf: _r$2 = function(e, t) {
  return e.__proto__ = t, e;
} } = Object;
let ve$2 = class ve extends Error {
  constructor(t = ce$3) {
    super(typeof t == "number" ? `${ce$3}: ${t} (see https://github.com/apollographql/invariant-packages)` : t);
    __publicField$3(this, "framesToPop", 1);
    __publicField$3(this, "name", ce$3);
    _r$2(this, ve.prototype);
  }
};
function $r$1(e, t) {
  if (!e) throw new ve$2(t);
}
const M$2 = { NORMAL: 0, WILDCARD: 1, PLACEHOLDER: 2 };
function xr$1(e = {}) {
  const t = { options: e, rootNode: it$1(), staticRoutesMap: {} }, r = (n) => e.strictTrailingSlash ? n : n.replace(/\/$/, "") || "/";
  if (e.routes) for (const n in e.routes) Fe$1(t, r(n), e.routes[n]);
  return { ctx: t, lookup: (n) => zr$1(t, r(n)), insert: (n, s) => Fe$1(t, r(n), s), remove: (n) => Pr$1(t, r(n)) };
}
function zr$1(e, t) {
  const r = e.staticRoutesMap[t];
  if (r) return r.data;
  const n = t.split("/"), s = {};
  let i = false, o = null, u = e.rootNode, c = null;
  for (let l = 0; l < n.length; l++) {
    const p = n[l];
    u.wildcardChildNode !== null && (o = u.wildcardChildNode, c = n.slice(l).join("/"));
    const d = u.children.get(p);
    if (d === void 0) {
      if (u && u.placeholderChildren.length > 1) {
        const w = n.length - l;
        u = u.placeholderChildren.find((f) => f.maxDepth === w) || null;
      } else u = u.placeholderChildren[0] || null;
      if (!u) break;
      u.paramName && (s[u.paramName] = p), i = true;
    } else u = d;
  }
  return (u === null || u.data === null) && o !== null && (u = o, s[u.paramName || "_"] = c, i = true), u ? i ? { ...u.data, params: i ? s : void 0 } : u.data : null;
}
function Fe$1(e, t, r) {
  let n = true;
  const s = t.split("/");
  let i = e.rootNode, o = 0;
  const u = [i];
  for (const c of s) {
    let l;
    if (l = i.children.get(c)) i = l;
    else {
      const p = Or$1(c);
      l = it$1({ type: p, parent: i }), i.children.set(c, l), p === M$2.PLACEHOLDER ? (l.paramName = c === "*" ? `_${o++}` : c.slice(1), i.placeholderChildren.push(l), n = false) : p === M$2.WILDCARD && (i.wildcardChildNode = l, l.paramName = c.slice(3) || "_", n = false), u.push(l), i = l;
    }
  }
  for (const [c, l] of u.entries()) l.maxDepth = Math.max(u.length - c, l.maxDepth || 0);
  return i.data = r, n === true && (e.staticRoutesMap[t] = i), i;
}
function Pr$1(e, t) {
  let r = false;
  const n = t.split("/");
  let s = e.rootNode;
  for (const i of n) if (s = s.children.get(i), !s) return r;
  if (s.data) {
    const i = n.at(-1) || "";
    s.data = null, Object.keys(s.children).length === 0 && s.parent && (s.parent.children.delete(i), s.parent.wildcardChildNode = null, s.parent.placeholderChildren = []), r = true;
  }
  return r;
}
function it$1(e = {}) {
  return { type: e.type || M$2.NORMAL, maxDepth: 0, parent: e.parent || null, children: /* @__PURE__ */ new Map(), data: e.data || null, paramName: e.paramName || null, wildcardChildNode: null, placeholderChildren: [] };
}
function Or$1(e) {
  return e.startsWith("**") ? M$2.WILDCARD : e[0] === ":" || e === "*" ? M$2.PLACEHOLDER : M$2.NORMAL;
}
const Ir$1 = { preload: () => Ae$4() }, Nr$1 = { preload: ({ params: e }) => Ee$3(e.slug) }, ot$1 = [{ page: true, $component: { src: "src/routes/admin.tsx?pick=default&pick=$css", build: () => import('../build/admin.mjs'), import: () => import('../build/admin.mjs') }, path: "/admin", filePath: "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/routes/admin.tsx" }, { page: true, $component: { src: "src/routes/index.tsx?pick=default&pick=$css", build: () => import('../build/index.mjs'), import: () => import('../build/index.mjs') }, $$route: { require: () => ({ route: Ir$1 }), src: "src/routes/index.tsx?pick=route" }, path: "/", filePath: "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/routes/index.tsx" }, { page: true, $component: { src: "src/routes/blog/[slug].tsx?pick=default&pick=$css", build: () => import('../build/_slug_.mjs'), import: () => import('../build/_slug_.mjs') }, $$route: { require: () => ({ route: Nr$1 }), src: "src/routes/blog/[slug].tsx?pick=route" }, path: "/blog/:slug", filePath: "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/routes/blog/[slug].tsx" }, { page: false, $DELETE: { src: "src/routes/api/decap/[...path].ts?pick=DELETE", build: () => import('../build/_...path_.mjs'), import: () => import('../build/_...path_.mjs') }, $GET: { src: "src/routes/api/decap/[...path].ts?pick=GET", build: () => import('../build/_...path_2.mjs'), import: () => import('../build/_...path_2.mjs') }, $HEAD: { src: "src/routes/api/decap/[...path].ts?pick=HEAD", build: () => import('../build/_...path_3.mjs'), import: () => import('../build/_...path_3.mjs') }, $OPTIONS: { src: "src/routes/api/decap/[...path].ts?pick=OPTIONS", build: () => import('../build/_...path_4.mjs'), import: () => import('../build/_...path_4.mjs') }, $PATCH: { src: "src/routes/api/decap/[...path].ts?pick=PATCH", build: () => import('../build/_...path_5.mjs'), import: () => import('../build/_...path_5.mjs') }, $POST: { src: "src/routes/api/decap/[...path].ts?pick=POST", build: () => import('../build/_...path_6.mjs'), import: () => import('../build/_...path_6.mjs') }, $PUT: { src: "src/routes/api/decap/[...path].ts?pick=PUT", build: () => import('../build/_...path_7.mjs'), import: () => import('../build/_...path_7.mjs') }, path: "/api/decap/*path", filePath: "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/routes/api/decap/[...path].ts" }], Cr$1 = Lr$1(ot$1.filter((e) => e.page));
function Lr$1(e) {
  function t(r, n, s, i) {
    const o = Object.values(r).find((u) => s.startsWith(u.id + "/"));
    return o ? (t(o.children || (o.children = []), n, s.slice(o.id.length)), r) : (r.push({ ...n, id: s, path: s.replace(/\([^)/]+\)/g, "").replace(/\/+/g, "/") }), r);
  }
  return e.sort((r, n) => r.path.length - n.path.length).reduce((r, n) => t(r, n, n.path, n.path), []);
}
function Tr$1(e) {
  return e.$HEAD || e.$GET || e.$POST || e.$PUT || e.$PATCH || e.$DELETE;
}
xr$1({ routes: ot$1.reduce((e, t) => {
  if (!Tr$1(t)) return e;
  let r = t.path.replace(/\([^)/]+\)/g, "").replace(/\/+/g, "/").replace(/\*([^/]*)/g, (n, s) => `**:${s}`).split("/").map((n) => n.startsWith(":") || n.startsWith("*") ? n : encodeURIComponent(n)).join("/");
  if (/:[^/]*\?/g.test(r)) throw new Error(`Optional parameters are not supported in API routes: ${r}`);
  if (e[r]) throw new Error(`Duplicate API routes for "${r}" found at "${e[r].route.path}" and "${t.path}"`);
  return e[r] = { route: t }, e;
}, {}) });
var Ur$1 = " ";
const Dr$1 = { style: (e) => ssrElement("style", e.attrs, () => e.children, true), link: (e) => ssrElement("link", e.attrs, void 0, true), script: (e) => e.attrs.src ? ssrElement("script", mergeProps(() => e.attrs, { get id() {
  return e.key;
} }), () => ssr(Ur$1), true) : null, noscript: (e) => ssrElement("noscript", e.attrs, () => escape$2(e.children), true) };
function jr$1(e, t) {
  let { tag: r, attrs: { key: n, ...s } = { key: void 0 }, children: i } = e;
  return Dr$1[r]({ attrs: { ...s, nonce: t }, key: n, children: i });
}
function Mr$1(e, t, r, n = "default") {
  return lazy(async () => {
    var _a2;
    {
      const i = (await e.import())[n], u = (await ((_a2 = t.inputs) == null ? void 0 : _a2[e.src].assets())).filter((l) => l.tag === "style" || l.attrs.rel === "stylesheet");
      return { default: (l) => [...u.map((p) => jr$1(p)), createComponent(i, l)] };
    }
  });
}
function ut$1() {
  function e(r) {
    return { ...r, ...r.$$route ? r.$$route.require().route : void 0, info: { ...r.$$route ? r.$$route.require().route.info : {}, filesystem: true }, component: r.$component && Mr$1(r.$component, globalThis.MANIFEST.client, globalThis.MANIFEST.ssr), children: r.children ? r.children.map(e) : void 0 };
  }
  return Cr$1.map(e);
}
let Ue$1;
const go$1 = isServer ? () => getRequestEvent().routes : () => Ue$1 || (Ue$1 = ut$1());
function qr$1(e) {
  const t = xe$4(e.nativeEvent, "flash");
  if (t) try {
    let r = JSON.parse(t);
    if (!r || !r.result) return;
    const n = [...r.input.slice(0, -1), new Map(r.input[r.input.length - 1])], s = r.error ? new Error(r.result) : r.result;
    return { input: n, url: r.url, pending: false, result: r.thrown ? void 0 : s, error: r.thrown ? s : void 0 };
  } catch (r) {
    console.error(r);
  } finally {
    we$4(e.nativeEvent, "flash", "", { maxAge: 0 });
  }
}
async function Hr$1(e) {
  const t = globalThis.MANIFEST.client;
  return globalThis.MANIFEST.ssr, e.response.headers.set("Content-Type", "text/html"), Object.assign(e, { manifest: await t.json(), assets: [...await t.inputs[t.handler].assets()], router: { submission: qr$1(e) }, routes: ut$1(), complete: false, $islands: /* @__PURE__ */ new Set() });
}
const Br$1 = /* @__PURE__ */ new Set([301, 302, 303, 307, 308]);
function Vr$1(e) {
  return e.status && Br$1.has(e.status) ? e.status : 302;
}
const Wr$1 = { "src_lib_content_ts--getPosts_query": { functionName: "getPosts_query", importer: () => import('../build/content-D0eQygkN.mjs') }, "src_lib_content_ts--getPost_query": { functionName: "getPost_query", importer: () => import('../build/content-D0eQygkN.mjs') } };
var ct$1 = ((e) => (e[e.AggregateError = 1] = "AggregateError", e[e.ArrowFunction = 2] = "ArrowFunction", e[e.ErrorPrototypeStack = 4] = "ErrorPrototypeStack", e[e.ObjectAssign = 8] = "ObjectAssign", e[e.BigIntTypedArray = 16] = "BigIntTypedArray", e[e.RegExp = 32] = "RegExp", e))(ct$1 || {}), A$1 = Symbol.asyncIterator, lt$1 = Symbol.hasInstance, q$1 = Symbol.isConcatSpreadable, _$1 = Symbol.iterator, ft$1 = Symbol.match, pt$1 = Symbol.matchAll, dt$1 = Symbol.replace, ht$1 = Symbol.search, mt$1 = Symbol.species, gt$1 = Symbol.split, bt$1 = Symbol.toPrimitive, H$1 = Symbol.toStringTag, yt$1 = Symbol.unscopables, Gr$1 = { 0: "Symbol.asyncIterator", 1: "Symbol.hasInstance", 2: "Symbol.isConcatSpreadable", 3: "Symbol.iterator", 4: "Symbol.match", 5: "Symbol.matchAll", 6: "Symbol.replace", 7: "Symbol.search", 8: "Symbol.species", 9: "Symbol.split", 10: "Symbol.toPrimitive", 11: "Symbol.toStringTag", 12: "Symbol.unscopables" }, wt$1 = { [A$1]: 0, [lt$1]: 1, [q$1]: 2, [_$1]: 3, [ft$1]: 4, [pt$1]: 5, [dt$1]: 6, [ht$1]: 7, [mt$1]: 8, [gt$1]: 9, [bt$1]: 10, [H$1]: 11, [yt$1]: 12 }, Xr$1 = { 0: A$1, 1: lt$1, 2: q$1, 3: _$1, 4: ft$1, 5: pt$1, 6: dt$1, 7: ht$1, 8: mt$1, 9: gt$1, 10: bt$1, 11: H$1, 12: yt$1 }, Jr$1 = { 2: "!0", 3: "!1", 1: "void 0", 0: "null", 4: "-0", 5: "1/0", 6: "-1/0", 7: "0/0" }, a = void 0, Yr$1 = { 2: true, 3: false, 1: a, 0: null, 4: -0, 5: Number.POSITIVE_INFINITY, 6: Number.NEGATIVE_INFINITY, 7: Number.NaN }, vt$1 = { 0: "Error", 1: "EvalError", 2: "RangeError", 3: "ReferenceError", 4: "SyntaxError", 5: "TypeError", 6: "URIError" }, Qr$1 = { 0: Error, 1: EvalError, 2: RangeError, 3: ReferenceError, 4: SyntaxError, 5: TypeError, 6: URIError };
function m$1(e, t, r, n, s, i, o, u, c, l, p, d) {
  return { t: e, i: t, s: r, c: n, m: s, p: i, e: o, a: u, f: c, b: l, o: p, l: d };
}
function O$2(e) {
  return m$1(2, a, e, a, a, a, a, a, a, a, a, a);
}
var Et$1 = O$2(2), St$1 = O$2(3), Zr$1 = O$2(1), Kr$1 = O$2(0), en$1 = O$2(4), tn$1 = O$2(5), rn$1 = O$2(6), nn$1 = O$2(7);
function an$1(e) {
  switch (e) {
    case '"':
      return '\\"';
    case "\\":
      return "\\\\";
    case `
`:
      return "\\n";
    case "\r":
      return "\\r";
    case "\b":
      return "\\b";
    case "	":
      return "\\t";
    case "\f":
      return "\\f";
    case "<":
      return "\\x3C";
    case "\u2028":
      return "\\u2028";
    case "\u2029":
      return "\\u2029";
    default:
      return a;
  }
}
function R$3(e) {
  let t = "", r = 0, n;
  for (let s = 0, i = e.length; s < i; s++) n = an$1(e[s]), n && (t += e.slice(r, s) + n, r = s + 1);
  return r === 0 ? t = e : t += e.slice(r), t;
}
function sn$1(e) {
  switch (e) {
    case "\\\\":
      return "\\";
    case '\\"':
      return '"';
    case "\\n":
      return `
`;
    case "\\r":
      return "\r";
    case "\\b":
      return "\b";
    case "\\t":
      return "	";
    case "\\f":
      return "\f";
    case "\\x3C":
      return "<";
    case "\\u2028":
      return "\u2028";
    case "\\u2029":
      return "\u2029";
    default:
      return e;
  }
}
function I$2(e) {
  return e.replace(/(\\\\|\\"|\\n|\\r|\\b|\\t|\\f|\\u2028|\\u2029|\\x3C)/g, sn$1);
}
var G$1 = "__SEROVAL_REFS__", ae$2 = "$R", ne$2 = `self.${ae$2}`;
function on$1(e) {
  return e == null ? `${ne$2}=${ne$2}||[]` : `(${ne$2}=${ne$2}||{})["${R$3(e)}"]=[]`;
}
var Rt$1 = /* @__PURE__ */ new Map(), j$1 = /* @__PURE__ */ new Map();
function kt$1(e) {
  return Rt$1.has(e);
}
function un$1(e) {
  return j$1.has(e);
}
function cn$1(e) {
  if (kt$1(e)) return Rt$1.get(e);
  throw new Un$1(e);
}
function ln$1(e) {
  if (un$1(e)) return j$1.get(e);
  throw new Dn$1(e);
}
typeof globalThis < "u" ? Object.defineProperty(globalThis, G$1, { value: j$1, configurable: true, writable: false, enumerable: false }) : typeof self < "u" ? Object.defineProperty(self, G$1, { value: j$1, configurable: true, writable: false, enumerable: false }) : typeof global < "u" && Object.defineProperty(global, G$1, { value: j$1, configurable: true, writable: false, enumerable: false });
function Ee$2(e) {
  return e instanceof EvalError ? 1 : e instanceof RangeError ? 2 : e instanceof ReferenceError ? 3 : e instanceof SyntaxError ? 4 : e instanceof TypeError ? 5 : e instanceof URIError ? 6 : 0;
}
function fn$1(e) {
  let t = vt$1[Ee$2(e)];
  return e.name !== t ? { name: e.name } : e.constructor.name !== t ? { name: e.constructor.name } : {};
}
function At$1(e, t) {
  let r = fn$1(e), n = Object.getOwnPropertyNames(e);
  for (let s = 0, i = n.length, o; s < i; s++) o = n[s], o !== "name" && o !== "message" && (o === "stack" ? t & 4 && (r = r || {}, r[o] = e[o]) : (r = r || {}, r[o] = e[o]));
  return r;
}
function _t$2(e) {
  return Object.isFrozen(e) ? 3 : Object.isSealed(e) ? 2 : Object.isExtensible(e) ? 0 : 1;
}
function pn$1(e) {
  switch (e) {
    case Number.POSITIVE_INFINITY:
      return tn$1;
    case Number.NEGATIVE_INFINITY:
      return rn$1;
  }
  return e !== e ? nn$1 : Object.is(e, -0) ? en$1 : m$1(0, a, e, a, a, a, a, a, a, a, a, a);
}
function $t$1(e) {
  return m$1(1, a, R$3(e), a, a, a, a, a, a, a, a, a);
}
function dn$1(e) {
  return m$1(3, a, "" + e, a, a, a, a, a, a, a, a, a);
}
function hn$1(e) {
  return m$1(4, e, a, a, a, a, a, a, a, a, a, a);
}
function mn$1(e, t) {
  let r = t.valueOf();
  return m$1(5, e, r !== r ? "" : t.toISOString(), a, a, a, a, a, a, a, a, a);
}
function gn$1(e, t) {
  return m$1(6, e, a, R$3(t.source), t.flags, a, a, a, a, a, a, a);
}
function bn$1(e, t) {
  return m$1(17, e, wt$1[t], a, a, a, a, a, a, a, a, a);
}
function yn$1(e, t) {
  return m$1(18, e, R$3(cn$1(t)), a, a, a, a, a, a, a, a, a);
}
function xt$1(e, t, r) {
  return m$1(25, e, r, R$3(t), a, a, a, a, a, a, a, a);
}
function wn$1(e, t, r) {
  return m$1(9, e, a, a, a, a, a, r, a, a, _t$2(t), a);
}
function vn$1(e, t) {
  return m$1(21, e, a, a, a, a, a, a, t, a, a, a);
}
function En$1(e, t, r) {
  return m$1(15, e, a, t.constructor.name, a, a, a, a, r, t.byteOffset, a, t.length);
}
function Sn$1(e, t, r) {
  return m$1(16, e, a, t.constructor.name, a, a, a, a, r, t.byteOffset, a, t.byteLength);
}
function Rn$1(e, t, r) {
  return m$1(20, e, a, a, a, a, a, a, r, t.byteOffset, a, t.byteLength);
}
function kn$1(e, t, r) {
  return m$1(13, e, Ee$2(t), a, R$3(t.message), r, a, a, a, a, a, a);
}
function An$1(e, t, r) {
  return m$1(14, e, Ee$2(t), a, R$3(t.message), r, a, a, a, a, a, a);
}
function _n$2(e, t) {
  return m$1(7, e, a, a, a, a, a, t, a, a, a, a);
}
function $n$1(e, t) {
  return m$1(28, a, a, a, a, a, a, [e, t], a, a, a, a);
}
function xn$1(e, t) {
  return m$1(30, a, a, a, a, a, a, [e, t], a, a, a, a);
}
function zn$1(e, t, r) {
  return m$1(31, e, a, a, a, a, a, r, t, a, a, a);
}
function Pn$1(e, t) {
  return m$1(32, e, a, a, a, a, a, a, t, a, a, a);
}
function On$1(e, t) {
  return m$1(33, e, a, a, a, a, a, a, t, a, a, a);
}
function In$1(e, t) {
  return m$1(34, e, a, a, a, a, a, a, t, a, a, a);
}
function Nn$1(e, t, r, n) {
  return m$1(35, e, r, a, a, a, a, t, a, a, a, n);
}
var Cn$1 = { parsing: 1, serialization: 2, deserialization: 3 };
function Ln$1(e) {
  return `Seroval Error (step: ${Cn$1[e]})`;
}
var Tn$1 = (e, t) => Ln$1(e), zt$1 = class zt extends Error {
  constructor(t, r) {
    super(Tn$1(t)), this.cause = r;
  }
}, De$2 = class De extends zt$1 {
  constructor(e) {
    super("parsing", e);
  }
}, Fn$1 = class Fn extends zt$1 {
  constructor(e) {
    super("deserialization", e);
  }
};
function $$2(e) {
  return `Seroval Error (specific: ${e})`;
}
var se$2 = class se extends Error {
  constructor(t) {
    super($$2(1)), this.value = t;
  }
}, L$2 = class L extends Error {
  constructor(t) {
    super($$2(2));
  }
}, Pt$1 = class Pt extends Error {
  constructor(e) {
    super($$2(3));
  }
}, Z$2 = class Z extends Error {
  constructor(t) {
    super($$2(4));
  }
}, Un$1 = class Un extends Error {
  constructor(e) {
    super($$2(5)), this.value = e;
  }
}, Dn$1 = class Dn extends Error {
  constructor(e) {
    super($$2(6));
  }
}, jn$1 = class jn extends Error {
  constructor(e) {
    super($$2(7));
  }
}, x$2 = class x extends Error {
  constructor(t) {
    super($$2(8));
  }
}, Ot$1 = class Ot extends Error {
  constructor(e) {
    super($$2(9));
  }
}, Mn$1 = class Mn {
  constructor(e, t) {
    this.value = e, this.replacement = t;
  }
}, ie$2 = () => {
  let e = { p: 0, s: 0, f: 0 };
  return e.p = new Promise((t, r) => {
    e.s = t, e.f = r;
  }), e;
}, qn$1 = (e, t) => {
  e.s(t), e.p.s = 1, e.p.v = t;
}, Hn$1 = (e, t) => {
  e.f(t), e.p.s = 2, e.p.v = t;
}, Bn$1 = ie$2.toString(), Vn = qn$1.toString(), Wn$1 = Hn$1.toString(), It$1 = () => {
  let e = [], t = [], r = true, n = false, s = 0, i = (c, l, p) => {
    for (p = 0; p < s; p++) t[p] && t[p][l](c);
  }, o = (c, l, p, d) => {
    for (l = 0, p = e.length; l < p; l++) d = e[l], !r && l === p - 1 ? c[n ? "return" : "throw"](d) : c.next(d);
  }, u = (c, l) => (r && (l = s++, t[l] = c), o(c), () => {
    r && (t[l] = t[s], t[s--] = void 0);
  });
  return { __SEROVAL_STREAM__: true, on: (c) => u(c), next: (c) => {
    r && (e.push(c), i(c, "next"));
  }, throw: (c) => {
    r && (e.push(c), i(c, "throw"), r = false, n = false, t.length = 0);
  }, return: (c) => {
    r && (e.push(c), i(c, "return"), r = false, n = true, t.length = 0);
  } };
}, Gn$1 = It$1.toString(), Nt$1 = (e) => (t) => () => {
  let r = 0, n = { [e]: () => n, next: () => {
    if (r > t.d) return { done: true, value: void 0 };
    let s = r++, i = t.v[s];
    if (s === t.t) throw i;
    return { done: s === t.d, value: i };
  } };
  return n;
}, Xn$1 = Nt$1.toString(), Ct$1 = (e, t) => (r) => () => {
  let n = 0, s = -1, i = false, o = [], u = [], c = (p = 0, d = u.length) => {
    for (; p < d; p++) u[p].s({ done: true, value: void 0 });
  };
  r.on({ next: (p) => {
    let d = u.shift();
    d && d.s({ done: false, value: p }), o.push(p);
  }, throw: (p) => {
    let d = u.shift();
    d && d.f(p), c(), s = o.length, i = true, o.push(p);
  }, return: (p) => {
    let d = u.shift();
    d && d.s({ done: true, value: p }), c(), s = o.length, o.push(p);
  } });
  let l = { [e]: () => l, next: () => {
    if (s === -1) {
      let w = n++;
      if (w >= o.length) {
        let f = t();
        return u.push(f), f.p;
      }
      return { done: false, value: o[w] };
    }
    if (n > s) return { done: true, value: void 0 };
    let p = n++, d = o[p];
    if (p !== s) return { done: false, value: d };
    if (i) throw d;
    return { done: true, value: d };
  } };
  return l;
}, Jn$1 = Ct$1.toString(), Lt$1 = (e) => {
  let t = atob(e), r = t.length, n = new Uint8Array(r);
  for (let s = 0; s < r; s++) n[s] = t.charCodeAt(s);
  return n.buffer;
}, Yn$1 = Lt$1.toString();
function Qn$1(e) {
  return "__SEROVAL_SEQUENCE__" in e;
}
function Tt$1(e, t, r) {
  return { __SEROVAL_SEQUENCE__: true, v: e, t, d: r };
}
function Zn$1(e) {
  let t = [], r = -1, n = -1, s = e[_$1]();
  for (; ; ) try {
    let i = s.next();
    if (t.push(i.value), i.done) {
      n = t.length - 1;
      break;
    }
  } catch (i) {
    r = t.length, t.push(i);
  }
  return Tt$1(t, r, n);
}
var Kn$1 = Nt$1(_$1);
function ea$1(e) {
  return Kn$1(e);
}
var ta$1 = {}, ra$1 = {}, na$1 = { 0: {}, 1: {}, 2: {}, 3: {}, 4: {}, 5: {} }, aa$1 = { 0: "[]", 1: Bn$1, 2: Vn, 3: Wn$1, 4: Gn$1, 5: Yn$1 };
function sa$1(e) {
  return "__SEROVAL_STREAM__" in e;
}
function K$1() {
  return It$1();
}
function ia$1(e) {
  let t = K$1(), r = e[A$1]();
  async function n() {
    try {
      let s = await r.next();
      s.done ? t.return(s.value) : (t.next(s.value), await n());
    } catch (s) {
      t.throw(s);
    }
  }
  return n().catch(() => {
  }), t;
}
var oa$1 = Ct$1(A$1, ie$2);
function ua$1(e) {
  return oa$1(e);
}
function ca$1(e, t) {
  return { plugins: t.plugins, mode: e, marked: /* @__PURE__ */ new Set(), features: 63 ^ (t.disabledFeatures || 0), refs: t.refs || /* @__PURE__ */ new Map(), depthLimit: t.depthLimit || 1e3 };
}
function la$1(e, t) {
  e.marked.add(t);
}
function Ft$1(e, t) {
  let r = e.refs.size;
  return e.refs.set(t, r), r;
}
function oe$2(e, t) {
  let r = e.refs.get(t);
  return r != null ? (la$1(e, r), { type: 1, value: hn$1(r) }) : { type: 0, value: Ft$1(e, t) };
}
function Se$2(e, t) {
  let r = oe$2(e, t);
  return r.type === 1 ? r : kt$1(t) ? { type: 2, value: yn$1(r.value, t) } : r;
}
function N(e, t) {
  let r = Se$2(e, t);
  if (r.type !== 0) return r.value;
  if (t in wt$1) return bn$1(r.value, t);
  throw new se$2(t);
}
function F(e, t) {
  let r = oe$2(e, na$1[t]);
  return r.type === 1 ? r.value : m$1(26, r.value, t, a, a, a, a, a, a, a, a, a);
}
function fa$1(e) {
  let t = oe$2(e, ta$1);
  return t.type === 1 ? t.value : m$1(27, t.value, a, a, a, a, a, a, N(e, _$1), a, a, a);
}
function pa$1(e) {
  let t = oe$2(e, ra$1);
  return t.type === 1 ? t.value : m$1(29, t.value, a, a, a, a, a, [F(e, 1), N(e, A$1)], a, a, a, a);
}
function da$1(e, t, r, n) {
  return m$1(r ? 11 : 10, e, a, a, a, n, a, a, a, a, _t$2(t), a);
}
function ha$1(e, t, r, n) {
  return m$1(8, t, a, a, a, a, { k: r, v: n }, a, F(e, 0), a, a, a);
}
function ma$1(e, t, r) {
  return m$1(22, t, r, a, a, a, a, a, F(e, 1), a, a, a);
}
function ga$1(e, t, r) {
  let n = new Uint8Array(r), s = "";
  for (let i = 0, o = n.length; i < o; i++) s += String.fromCharCode(n[i]);
  return m$1(19, t, R$3(btoa(s)), a, a, a, a, a, F(e, 5), a, a, a);
}
var ba$1 = ((e) => (e[e.Vanilla = 1] = "Vanilla", e[e.Cross = 2] = "Cross", e))(ba$1 || {});
function Ut$1(e, t) {
  for (let r = 0, n = t.length; r < n; r++) {
    let s = t[r];
    e.has(s) || (e.add(s), s.extends && Ut$1(e, s.extends));
  }
}
function Re$2(e) {
  if (e) {
    let t = /* @__PURE__ */ new Set();
    return Ut$1(t, e), [...t];
  }
}
function ya$1(e) {
  switch (e) {
    case "Int8Array":
      return Int8Array;
    case "Int16Array":
      return Int16Array;
    case "Int32Array":
      return Int32Array;
    case "Uint8Array":
      return Uint8Array;
    case "Uint16Array":
      return Uint16Array;
    case "Uint32Array":
      return Uint32Array;
    case "Uint8ClampedArray":
      return Uint8ClampedArray;
    case "Float32Array":
      return Float32Array;
    case "Float64Array":
      return Float64Array;
    case "BigInt64Array":
      return BigInt64Array;
    case "BigUint64Array":
      return BigUint64Array;
    default:
      throw new jn$1(e);
  }
}
var wa$1 = 1e6, va$1 = 1e4, Ea$1 = 2e4;
function Dt$1(e, t) {
  switch (t) {
    case 3:
      return Object.freeze(e);
    case 1:
      return Object.preventExtensions(e);
    case 2:
      return Object.seal(e);
    default:
      return e;
  }
}
var Sa$1 = 1e3;
function Ra$1(e, t) {
  var r;
  let n = t.refs || /* @__PURE__ */ new Map();
  return "types" in n || Object.assign(n, { types: /* @__PURE__ */ new Map() }), { mode: e, plugins: t.plugins, refs: n, features: (r = t.features) != null ? r : 63 ^ (t.disabledFeatures || 0), depthLimit: t.depthLimit || Sa$1 };
}
function ka$1(e) {
  return { mode: 1, base: Ra$1(1, e), child: a, state: { marked: new Set(e.markedRefs) } };
}
var Aa = class {
  constructor(e, t) {
    this._p = e, this.depth = t;
  }
  deserialize(e) {
    return b$2(this._p, this.depth, e);
  }
};
function jt$1(e, t) {
  if (t < 0 || !Number.isFinite(t) || !Number.isInteger(t)) throw new x$2({ t: 4, i: t });
  if (e.refs.has(t)) throw new Error("Conflicted ref id: " + t);
}
function _a$2(e, t, r) {
  return jt$1(e.base, t), e.state.marked.has(t) && e.base.refs.set(t, r), r;
}
function $a$1(e, t, r) {
  return jt$1(e.base, t), e.base.refs.set(t, r), r;
}
function y$1(e, t, r) {
  return e.mode === 1 ? _a$2(e, t, r) : $a$1(e, t, r);
}
function ge$3(e, t, r) {
  if (Object.hasOwn(t, r)) return t[r];
  throw new x$2(e);
}
function xa$1(e, t) {
  return y$1(e, t.i, ln$1(I$2(t.s)));
}
function za$1(e, t, r) {
  let n = r.a, s = n.length, i = y$1(e, r.i, new Array(s));
  for (let o = 0, u; o < s; o++) u = n[o], u && (i[o] = b$2(e, t, u));
  return Dt$1(i, r.o), i;
}
function Pa$1(e) {
  switch (e) {
    case "constructor":
    case "__proto__":
    case "prototype":
    case "__defineGetter__":
    case "__defineSetter__":
    case "__lookupGetter__":
    case "__lookupSetter__":
      return false;
    default:
      return true;
  }
}
function Oa$1(e) {
  switch (e) {
    case A$1:
    case q$1:
    case H$1:
    case _$1:
      return true;
    default:
      return false;
  }
}
function je$1(e, t, r) {
  Pa$1(t) ? e[t] = r : Object.defineProperty(e, t, { value: r, configurable: true, enumerable: true, writable: true });
}
function Ia$1(e, t, r, n, s) {
  if (typeof n == "string") je$1(r, I$2(n), b$2(e, t, s));
  else {
    let i = b$2(e, t, n);
    switch (typeof i) {
      case "string":
        je$1(r, i, b$2(e, t, s));
        break;
      case "symbol":
        Oa$1(i) && (r[i] = b$2(e, t, s));
        break;
      default:
        throw new x$2(n);
    }
  }
}
function Mt$1(e, t, r) {
  e.base.refs.types.set(t, r);
}
function ee$2(e, t, r, n) {
  if (e.base.refs.types.get(r) !== n) throw new x$2(t);
}
function qt$1(e, t, r, n) {
  let s = r.k;
  if (s.length > 0) for (let i = 0, o = r.v, u = s.length; i < u; i++) Ia$1(e, t, n, s[i], o[i]);
  return n;
}
function Na$1(e, t, r) {
  let n = y$1(e, r.i, r.t === 10 ? {} : /* @__PURE__ */ Object.create(null));
  return qt$1(e, t, r.p, n), Dt$1(n, r.o), n;
}
function Ca$1(e, t) {
  return y$1(e, t.i, new Date(t.s));
}
function La$1(e, t) {
  if (e.base.features & 32) {
    let r = I$2(t.c);
    if (r.length > Ea$1) throw new x$2(t);
    return y$1(e, t.i, new RegExp(r, t.m));
  }
  throw new L$2(t);
}
function Ta$1(e, t, r) {
  let n = y$1(e, r.i, /* @__PURE__ */ new Set());
  for (let s = 0, i = r.a, o = i.length; s < o; s++) n.add(b$2(e, t, i[s]));
  return n;
}
function Fa$1(e, t, r) {
  let n = y$1(e, r.i, /* @__PURE__ */ new Map());
  for (let s = 0, i = r.e.k, o = r.e.v, u = i.length; s < u; s++) n.set(b$2(e, t, i[s]), b$2(e, t, o[s]));
  return n;
}
function Ua$1(e, t) {
  if (t.s.length > wa$1) throw new x$2(t);
  return y$1(e, t.i, Lt$1(I$2(t.s)));
}
function Da$1(e, t, r) {
  var n;
  let s = ya$1(r.c), i = b$2(e, t, r.f), o = (n = r.b) != null ? n : 0;
  if (o < 0 || o > i.byteLength) throw new x$2(r);
  return y$1(e, r.i, new s(i, o, r.l));
}
function ja$1(e, t, r) {
  var n;
  let s = b$2(e, t, r.f), i = (n = r.b) != null ? n : 0;
  if (i < 0 || i > s.byteLength) throw new x$2(r);
  return y$1(e, r.i, new DataView(s, i, r.l));
}
function Ht$1(e, t, r, n) {
  if (r.p) {
    let s = qt$1(e, t, r.p, {});
    Object.defineProperties(n, Object.getOwnPropertyDescriptors(s));
  }
  return n;
}
function Ma$1(e, t, r) {
  let n = y$1(e, r.i, new AggregateError([], I$2(r.m)));
  return Ht$1(e, t, r, n);
}
function qa$1(e, t, r) {
  let n = ge$3(r, Qr$1, r.s), s = y$1(e, r.i, new n(I$2(r.m)));
  return Ht$1(e, t, r, s);
}
function Ha$1(e, t, r) {
  let n = ie$2(), s = y$1(e, r.i, n.p), i = b$2(e, t, r.f);
  return r.s ? n.s(i) : n.f(i), s;
}
function Ba$1(e, t, r) {
  return y$1(e, r.i, Object(b$2(e, t, r.f)));
}
function Va$1(e, t, r) {
  let n = e.base.plugins;
  if (n) {
    let s = I$2(r.c);
    for (let i = 0, o = n.length; i < o; i++) {
      let u = n[i];
      if (u.tag === s) return y$1(e, r.i, u.deserialize(r.s, new Aa(e, t), { id: r.i }));
    }
  }
  throw new Pt$1(r.c);
}
function Wa$1(e, t) {
  let r = y$1(e, t.i, y$1(e, t.s, ie$2()).p);
  return Mt$1(e, t.s, 22), r;
}
function Ga$1(e, t, r) {
  let n = e.base.refs.get(r.i);
  if (n) return ee$2(e, r, r.i, 22), n.s(b$2(e, t, r.a[1])), a;
  throw new Z$2("Promise");
}
function Xa$1(e, t, r) {
  let n = e.base.refs.get(r.i);
  if (n) return ee$2(e, r, r.i, 22), n.f(b$2(e, t, r.a[1])), a;
  throw new Z$2("Promise");
}
function Ja$1(e, t, r) {
  b$2(e, t, r.a[0]);
  let n = b$2(e, t, r.a[1]);
  return ea$1(n);
}
function Ya$1(e, t, r) {
  b$2(e, t, r.a[0]);
  let n = b$2(e, t, r.a[1]);
  return ua$1(n);
}
function Qa$1(e, t, r) {
  let n = y$1(e, r.i, K$1());
  Mt$1(e, r.i, 31);
  let s = r.a, i = s.length;
  if (i) for (let o = 0; o < i; o++) b$2(e, t, s[o]);
  return n;
}
function Za$1(e, t, r) {
  let n = e.base.refs.get(r.i);
  if (n) return ee$2(e, r, r.i, 31), n.next(b$2(e, t, r.f)), a;
  throw new Z$2("Stream");
}
function Ka$1(e, t, r) {
  let n = e.base.refs.get(r.i);
  if (n) return ee$2(e, r, r.i, 31), n.throw(b$2(e, t, r.f)), a;
  throw new Z$2("Stream");
}
function es$1(e, t, r) {
  let n = e.base.refs.get(r.i);
  if (n) return ee$2(e, r, r.i, 31), n.return(b$2(e, t, r.f)), a;
  throw new Z$2("Stream");
}
function ts$1(e, t, r) {
  return b$2(e, t, r.f), a;
}
function rs$1(e, t, r) {
  return b$2(e, t, r.a[1]), a;
}
function ns$1(e, t, r) {
  let n = y$1(e, r.i, Tt$1([], r.s, r.l));
  for (let s = 0, i = r.a.length; s < i; s++) n.v[s] = b$2(e, t, r.a[s]);
  return n;
}
function b$2(e, t, r) {
  if (t > e.base.depthLimit) throw new Ot$1(e.base.depthLimit);
  switch (t += 1, r.t) {
    case 2:
      return ge$3(r, Yr$1, r.s);
    case 0:
      return Number(r.s);
    case 1:
      return I$2(String(r.s));
    case 3:
      if (String(r.s).length > va$1) throw new x$2(r);
      return BigInt(r.s);
    case 4:
      return e.base.refs.get(r.i);
    case 18:
      return xa$1(e, r);
    case 9:
      return za$1(e, t, r);
    case 10:
    case 11:
      return Na$1(e, t, r);
    case 5:
      return Ca$1(e, r);
    case 6:
      return La$1(e, r);
    case 7:
      return Ta$1(e, t, r);
    case 8:
      return Fa$1(e, t, r);
    case 19:
      return Ua$1(e, r);
    case 16:
    case 15:
      return Da$1(e, t, r);
    case 20:
      return ja$1(e, t, r);
    case 14:
      return Ma$1(e, t, r);
    case 13:
      return qa$1(e, t, r);
    case 12:
      return Ha$1(e, t, r);
    case 17:
      return ge$3(r, Xr$1, r.s);
    case 21:
      return Ba$1(e, t, r);
    case 25:
      return Va$1(e, t, r);
    case 22:
      return Wa$1(e, r);
    case 23:
      return Ga$1(e, t, r);
    case 24:
      return Xa$1(e, t, r);
    case 28:
      return Ja$1(e, t, r);
    case 30:
      return Ya$1(e, t, r);
    case 31:
      return Qa$1(e, t, r);
    case 32:
      return Za$1(e, t, r);
    case 33:
      return Ka$1(e, t, r);
    case 34:
      return es$1(e, t, r);
    case 27:
      return ts$1(e, t, r);
    case 29:
      return rs$1(e, t, r);
    case 35:
      return ns$1(e, t, r);
    default:
      throw new L$2(r);
  }
}
function as$1(e, t) {
  try {
    return b$2(e, 0, t);
  } catch (r) {
    throw new Fn$1(r);
  }
}
var ss$1 = () => T, is$1 = ss$1.toString(), Bt$1 = /=>/.test(is$1);
function Vt$1(e, t) {
  return Bt$1 ? (e.length === 1 ? e[0] : "(" + e.join(",") + ")") + "=>" + (t.startsWith("{") ? "(" + t + ")" : t) : "function(" + e.join(",") + "){return " + t + "}";
}
function os$1(e, t) {
  return Bt$1 ? (e.length === 1 ? e[0] : "(" + e.join(",") + ")") + "=>{" + t + "}" : "function(" + e.join(",") + "){" + t + "}";
}
var Wt$1 = "hjkmoquxzABCDEFGHIJKLNPQRTUVWXYZ$_", Me$2 = Wt$1.length, Gt$1 = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789$_", qe$3 = Gt$1.length;
function us$1(e) {
  let t = e % Me$2, r = Wt$1[t];
  for (e = (e - t) / Me$2; e > 0; ) t = e % qe$3, r += Gt$1[t], e = (e - t) / qe$3;
  return r;
}
var cs$1 = /^[$A-Z_][0-9A-Z_$]*$/i;
function Xt$1(e) {
  let t = e[0];
  return (t === "$" || t === "_" || t >= "A" && t <= "Z" || t >= "a" && t <= "z") && cs$1.test(e);
}
function X$2(e) {
  switch (e.t) {
    case 0:
      return e.s + "=" + e.v;
    case 2:
      return e.s + ".set(" + e.k + "," + e.v + ")";
    case 1:
      return e.s + ".add(" + e.v + ")";
    case 3:
      return e.s + ".delete(" + e.k + ")";
  }
}
function ls$1(e) {
  let t = [], r = e[0];
  for (let n = 1, s = e.length, i, o = r; n < s; n++) i = e[n], i.t === 0 && i.v === o.v ? r = { t: 0, s: i.s, k: a, v: X$2(r) } : i.t === 2 && i.s === o.s ? r = { t: 2, s: X$2(r), k: i.k, v: i.v } : i.t === 1 && i.s === o.s ? r = { t: 1, s: X$2(r), k: a, v: i.v } : i.t === 3 && i.s === o.s ? r = { t: 3, s: X$2(r), k: i.k, v: a } : (t.push(r), r = i), o = i;
  return t.push(r), t;
}
function Jt$1(e) {
  if (e.length) {
    let t = "", r = ls$1(e);
    for (let n = 0, s = r.length; n < s; n++) t += X$2(r[n]) + ",";
    return t;
  }
  return a;
}
var fs$2 = "Object.create(null)", ps$1 = "new Set", ds$1 = "new Map", hs$1 = "Promise.resolve", ms$1 = "Promise.reject", gs$1 = { 3: "Object.freeze", 2: "Object.seal", 1: "Object.preventExtensions", 0: a };
function bs$1(e, t) {
  return { mode: e, plugins: t.plugins, features: t.features, marked: new Set(t.markedRefs), stack: [], flags: [], assignments: [] };
}
function ys$1(e) {
  return { mode: 2, base: bs$1(2, e), state: e, child: a };
}
var ws$1 = class ws {
  constructor(e) {
    this._p = e;
  }
  serialize(e) {
    return h$2(this._p, e);
  }
};
function vs$1(e, t) {
  let r = e.valid.get(t);
  r == null && (r = e.valid.size, e.valid.set(t, r));
  let n = e.vars[r];
  return n == null && (n = us$1(r), e.vars[r] = n), n;
}
function Es$1(e) {
  return ae$2 + "[" + e + "]";
}
function g$1(e, t) {
  return e.mode === 1 ? vs$1(e.state, t) : Es$1(t);
}
function E(e, t) {
  e.marked.add(t);
}
function be$2(e, t) {
  return e.marked.has(t);
}
function ke$2(e, t, r) {
  t !== 0 && (E(e.base, r), e.base.flags.push({ type: t, value: g$1(e, r) }));
}
function Ss$1(e) {
  let t = "";
  for (let r = 0, n = e.flags, s = n.length; r < s; r++) {
    let i = n[r];
    t += gs$1[i.type] + "(" + i.value + "),";
  }
  return t;
}
function Rs$1(e) {
  let t = Jt$1(e.assignments), r = Ss$1(e);
  return t ? r ? t + r : t : r;
}
function Ae$3(e, t, r) {
  e.assignments.push({ t: 0, s: t, k: a, v: r });
}
function ks$1(e, t, r) {
  e.base.assignments.push({ t: 1, s: g$1(e, t), k: a, v: r });
}
function W$1(e, t, r, n) {
  e.base.assignments.push({ t: 2, s: g$1(e, t), k: r, v: n });
}
function He$3(e, t, r) {
  e.base.assignments.push({ t: 3, s: g$1(e, t), k: r, v: a });
}
function Y$2(e, t, r, n) {
  Ae$3(e.base, g$1(e, t) + "[" + r + "]", n);
}
function ye$3(e, t, r, n) {
  Ae$3(e.base, g$1(e, t) + "." + r, n);
}
function As$1(e, t, r, n) {
  Ae$3(e.base, g$1(e, t) + ".v[" + r + "]", n);
}
function k(e, t) {
  return t.t === 4 && e.stack.includes(t.i);
}
function V$2(e, t, r) {
  return e.mode === 1 && !be$2(e.base, t) ? r : g$1(e, t) + "=" + r;
}
function _s$2(e) {
  return G$1 + '.get("' + e.s + '")';
}
function Be(e, t, r, n) {
  return r ? k(e.base, r) ? (E(e.base, t), Y$2(e, t, n, g$1(e, r.i)), "") : h$2(e, r) : "";
}
function $s$1(e, t) {
  let r = t.i, n = t.a, s = n.length;
  if (s > 0) {
    e.base.stack.push(r);
    let i = Be(e, r, n[0], 0), o = i === "";
    for (let u = 1, c; u < s; u++) c = Be(e, r, n[u], u), i += "," + c, o = c === "";
    return e.base.stack.pop(), ke$2(e, t.o, t.i), "[" + i + (o ? ",]" : "]");
  }
  return "[]";
}
function Ve$1(e, t, r, n) {
  if (typeof r == "string") {
    let s = Number(r), i = s >= 0 && s.toString() === r || Xt$1(r);
    if (k(e.base, n)) {
      let o = g$1(e, n.i);
      return E(e.base, t.i), i && s !== s ? ye$3(e, t.i, r, o) : Y$2(e, t.i, i ? r : '"' + r + '"', o), "";
    }
    return (i ? r : '"' + r + '"') + ":" + h$2(e, n);
  }
  return "[" + h$2(e, r) + "]:" + h$2(e, n);
}
function Yt$1(e, t, r) {
  let n = r.k, s = n.length;
  if (s > 0) {
    let i = r.v;
    e.base.stack.push(t.i);
    let o = Ve$1(e, t, n[0], i[0]);
    for (let u = 1, c = o; u < s; u++) c = Ve$1(e, t, n[u], i[u]), o += (c && o && ",") + c;
    return e.base.stack.pop(), "{" + o + "}";
  }
  return "{}";
}
function xs$1(e, t) {
  return ke$2(e, t.o, t.i), Yt$1(e, t, t.p);
}
function zs$1(e, t, r, n) {
  let s = Yt$1(e, t, r);
  return s !== "{}" ? "Object.assign(" + n + "," + s + ")" : n;
}
function Ps$1(e, t, r, n, s) {
  let i = e.base, o = h$2(e, s), u = Number(n), c = u >= 0 && u.toString() === n || Xt$1(n);
  if (k(i, s)) c && u !== u ? ye$3(e, t.i, n, o) : Y$2(e, t.i, c ? n : '"' + n + '"', o);
  else {
    let l = i.assignments;
    i.assignments = r, c && u !== u ? ye$3(e, t.i, n, o) : Y$2(e, t.i, c ? n : '"' + n + '"', o), i.assignments = l;
  }
}
function Os$1(e, t, r, n, s) {
  if (typeof n == "string") Ps$1(e, t, r, n, s);
  else {
    let i = e.base, o = i.stack;
    i.stack = [];
    let u = h$2(e, s);
    i.stack = o;
    let c = i.assignments;
    i.assignments = r, Y$2(e, t.i, h$2(e, n), u), i.assignments = c;
  }
}
function Is$1(e, t, r) {
  let n = r.k, s = n.length;
  if (s > 0) {
    let i = [], o = r.v;
    e.base.stack.push(t.i);
    for (let u = 0; u < s; u++) Os$1(e, t, i, n[u], o[u]);
    return e.base.stack.pop(), Jt$1(i);
  }
  return a;
}
function _e$4(e, t, r) {
  if (t.p) {
    let n = e.base;
    if (n.features & 8) r = zs$1(e, t, t.p, r);
    else {
      E(n, t.i);
      let s = Is$1(e, t, t.p);
      if (s) return "(" + V$2(e, t.i, r) + "," + s + g$1(e, t.i) + ")";
    }
  }
  return r;
}
function Ns$1(e, t) {
  return ke$2(e, t.o, t.i), _e$4(e, t, fs$2);
}
function Cs$1(e) {
  return 'new Date("' + e.s + '")';
}
function Ls$1(e, t) {
  if (e.base.features & 32) return "/" + t.c + "/" + t.m;
  throw new L$2(t);
}
function We$1(e, t, r) {
  let n = e.base;
  return k(n, r) ? (E(n, t), ks$1(e, t, g$1(e, r.i)), "") : h$2(e, r);
}
function Ts$1(e, t) {
  let r = ps$1, n = t.a, s = n.length, i = t.i;
  if (s > 0) {
    e.base.stack.push(i);
    let o = We$1(e, i, n[0]);
    for (let u = 1, c = o; u < s; u++) c = We$1(e, i, n[u]), o += (c && o && ",") + c;
    e.base.stack.pop(), o && (r += "([" + o + "])");
  }
  return r;
}
function Ge$1(e, t, r, n, s) {
  let i = e.base;
  if (k(i, r)) {
    let o = g$1(e, r.i);
    if (E(i, t), k(i, n)) {
      let c = g$1(e, n.i);
      return W$1(e, t, o, c), "";
    }
    if (n.t !== 4 && n.i != null && be$2(i, n.i)) {
      let c = "(" + h$2(e, n) + ",[" + s + "," + s + "])";
      return W$1(e, t, o, g$1(e, n.i)), He$3(e, t, s), c;
    }
    let u = i.stack;
    return i.stack = [], W$1(e, t, o, h$2(e, n)), i.stack = u, "";
  }
  if (k(i, n)) {
    let o = g$1(e, n.i);
    if (E(i, t), r.t !== 4 && r.i != null && be$2(i, r.i)) {
      let c = "(" + h$2(e, r) + ",[" + s + "," + s + "])";
      return W$1(e, t, g$1(e, r.i), o), He$3(e, t, s), c;
    }
    let u = i.stack;
    return i.stack = [], W$1(e, t, h$2(e, r), o), i.stack = u, "";
  }
  return "[" + h$2(e, r) + "," + h$2(e, n) + "]";
}
function Fs$1(e, t) {
  let r = ds$1, n = t.e.k, s = n.length, i = t.i, o = t.f, u = g$1(e, o.i), c = e.base;
  if (s > 0) {
    let l = t.e.v;
    c.stack.push(i);
    let p = Ge$1(e, i, n[0], l[0], u);
    for (let d = 1, w = p; d < s; d++) w = Ge$1(e, i, n[d], l[d], u), p += (w && p && ",") + w;
    c.stack.pop(), p && (r += "([" + p + "])");
  }
  return o.t === 26 && (E(c, o.i), r = "(" + h$2(e, o) + "," + r + ")"), r;
}
function Us$1(e, t) {
  return U$1(e, t.f) + '("' + t.s + '")';
}
function Ds$1(e, t) {
  return "new " + t.c + "(" + h$2(e, t.f) + "," + t.b + "," + t.l + ")";
}
function js$1(e, t) {
  return "new DataView(" + h$2(e, t.f) + "," + t.b + "," + t.l + ")";
}
function Ms$1(e, t) {
  let r = t.i;
  e.base.stack.push(r);
  let n = _e$4(e, t, 'new AggregateError([],"' + t.m + '")');
  return e.base.stack.pop(), n;
}
function qs$1(e, t) {
  return _e$4(e, t, "new " + vt$1[t.s] + '("' + t.m + '")');
}
function Hs$1(e, t) {
  let r, n = t.f, s = t.i, i = t.s ? hs$1 : ms$1, o = e.base;
  if (k(o, n)) {
    let u = g$1(e, n.i);
    r = i + (t.s ? "().then(" + Vt$1([], u) + ")" : "().catch(" + os$1([], "throw " + u) + ")");
  } else {
    o.stack.push(s);
    let u = h$2(e, n);
    o.stack.pop(), r = i + "(" + u + ")";
  }
  return r;
}
function Bs$1(e, t) {
  return "Object(" + h$2(e, t.f) + ")";
}
function U$1(e, t) {
  let r = h$2(e, t);
  return t.t === 4 ? r : "(" + r + ")";
}
function Vs$1(e, t) {
  if (e.mode === 1) throw new L$2(t);
  return "(" + V$2(e, t.s, U$1(e, t.f) + "()") + ").p";
}
function Ws$1(e, t) {
  if (e.mode === 1) throw new L$2(t);
  return U$1(e, t.a[0]) + "(" + g$1(e, t.i) + "," + h$2(e, t.a[1]) + ")";
}
function Gs$1(e, t) {
  if (e.mode === 1) throw new L$2(t);
  return U$1(e, t.a[0]) + "(" + g$1(e, t.i) + "," + h$2(e, t.a[1]) + ")";
}
function Xs$1(e, t) {
  let r = e.base.plugins;
  if (r) for (let n = 0, s = r.length; n < s; n++) {
    let i = r[n];
    if (i.tag === t.c) return e.child == null && (e.child = new ws$1(e)), i.serialize(t.s, e.child, { id: t.i });
  }
  throw new Pt$1(t.c);
}
function Js$1(e, t) {
  let r = "", n = false;
  return t.f.t !== 4 && (E(e.base, t.f.i), r = "(" + h$2(e, t.f) + ",", n = true), r += V$2(e, t.i, "(" + Xn$1 + ")(" + g$1(e, t.f.i) + ")"), n && (r += ")"), r;
}
function Ys$1(e, t) {
  return U$1(e, t.a[0]) + "(" + h$2(e, t.a[1]) + ")";
}
function Qs$1(e, t) {
  let r = t.a[0], n = t.a[1], s = e.base, i = "";
  r.t !== 4 && (E(s, r.i), i += "(" + h$2(e, r)), n.t !== 4 && (E(s, n.i), i += (i ? "," : "(") + h$2(e, n)), i && (i += ",");
  let o = V$2(e, t.i, "(" + Jn$1 + ")(" + g$1(e, n.i) + "," + g$1(e, r.i) + ")");
  return i ? i + o + ")" : o;
}
function Zs$1(e, t) {
  return U$1(e, t.a[0]) + "(" + h$2(e, t.a[1]) + ")";
}
function Ks$1(e, t) {
  let r = V$2(e, t.i, U$1(e, t.f) + "()"), n = t.a.length;
  if (n) {
    let s = h$2(e, t.a[0]);
    for (let i = 1; i < n; i++) s += "," + h$2(e, t.a[i]);
    return "(" + r + "," + s + "," + g$1(e, t.i) + ")";
  }
  return r;
}
function ei$1(e, t) {
  return g$1(e, t.i) + ".next(" + h$2(e, t.f) + ")";
}
function ti$1(e, t) {
  return g$1(e, t.i) + ".throw(" + h$2(e, t.f) + ")";
}
function ri$1(e, t) {
  return g$1(e, t.i) + ".return(" + h$2(e, t.f) + ")";
}
function Xe$1(e, t, r, n) {
  let s = e.base;
  return k(s, n) ? (E(s, t), As$1(e, t, r, g$1(e, n.i)), "") : h$2(e, n);
}
function ni$1(e, t) {
  let r = t.a, n = r.length, s = t.i;
  if (n > 0) {
    e.base.stack.push(s);
    let i = Xe$1(e, s, 0, r[0]);
    for (let o = 1, u = i; o < n; o++) u = Xe$1(e, s, o, r[o]), i += (u && i && ",") + u;
    if (e.base.stack.pop(), i) return "{__SEROVAL_SEQUENCE__:!0,v:[" + i + "],t:" + t.s + ",d:" + t.l + "}";
  }
  return "{__SEROVAL_SEQUENCE__:!0,v:[],t:-1,d:0}";
}
function ai$1(e, t) {
  switch (t.t) {
    case 17:
      return Gr$1[t.s];
    case 18:
      return _s$2(t);
    case 9:
      return $s$1(e, t);
    case 10:
      return xs$1(e, t);
    case 11:
      return Ns$1(e, t);
    case 5:
      return Cs$1(t);
    case 6:
      return Ls$1(e, t);
    case 7:
      return Ts$1(e, t);
    case 8:
      return Fs$1(e, t);
    case 19:
      return Us$1(e, t);
    case 16:
    case 15:
      return Ds$1(e, t);
    case 20:
      return js$1(e, t);
    case 14:
      return Ms$1(e, t);
    case 13:
      return qs$1(e, t);
    case 12:
      return Hs$1(e, t);
    case 21:
      return Bs$1(e, t);
    case 22:
      return Vs$1(e, t);
    case 25:
      return Xs$1(e, t);
    case 26:
      return aa$1[t.s];
    case 35:
      return ni$1(e, t);
    default:
      throw new L$2(t);
  }
}
function h$2(e, t) {
  switch (t.t) {
    case 2:
      return Jr$1[t.s];
    case 0:
      return "" + t.s;
    case 1:
      return '"' + t.s + '"';
    case 3:
      return t.s + "n";
    case 4:
      return g$1(e, t.i);
    case 23:
      return Ws$1(e, t);
    case 24:
      return Gs$1(e, t);
    case 27:
      return Js$1(e, t);
    case 28:
      return Ys$1(e, t);
    case 29:
      return Qs$1(e, t);
    case 30:
      return Zs$1(e, t);
    case 31:
      return Ks$1(e, t);
    case 32:
      return ei$1(e, t);
    case 33:
      return ti$1(e, t);
    case 34:
      return ri$1(e, t);
    default:
      return V$2(e, t.i, ai$1(e, t));
  }
}
function si$1(e, t) {
  let r = h$2(e, t), n = t.i;
  if (n == null) return r;
  let s = Rs$1(e.base), i = g$1(e, n), o = e.state.scopeId, u = o == null ? "" : ae$2, c = s ? "(" + r + "," + s + i + ")" : r;
  if (u === "") return t.t === 10 && !s ? "(" + c + ")" : c;
  let l = o == null ? "()" : "(" + ae$2 + '["' + R$3(o) + '"])';
  return "(" + Vt$1([u], c) + ")" + l;
}
var ii$1 = class ii {
  constructor(e, t) {
    this._p = e, this.depth = t;
  }
  parse(e) {
    return v$2(this._p, this.depth, e);
  }
}, oi$1 = class oi {
  constructor(e, t) {
    this._p = e, this.depth = t;
  }
  parse(e) {
    return v$2(this._p, this.depth, e);
  }
  parseWithError(e) {
    return C$3(this._p, this.depth, e);
  }
  isAlive() {
    return this._p.state.alive;
  }
  pushPendingState() {
    Pe$2(this._p);
  }
  popPendingState() {
    Q$2(this._p);
  }
  onParse(e) {
    B$1(this._p, e);
  }
  onError(e) {
    xe$3(this._p, e);
  }
};
function ui$1(e) {
  return { alive: true, pending: 0, initial: true, buffer: [], onParse: e.onParse, onError: e.onError, onDone: e.onDone };
}
function Qt$1(e) {
  return { type: 2, base: ca$1(2, e), state: ui$1(e) };
}
function ci$1(e, t, r) {
  let n = [];
  for (let s = 0, i = r.length; s < i; s++) s in r ? n[s] = v$2(e, t, r[s]) : n[s] = 0;
  return n;
}
function li$1(e, t, r, n) {
  return wn$1(r, n, ci$1(e, t, n));
}
function $e$2(e, t, r) {
  let n = Object.entries(r), s = [], i = [];
  for (let o = 0, u = n.length; o < u; o++) s.push(R$3(n[o][0])), i.push(v$2(e, t, n[o][1]));
  return _$1 in r && (s.push(N(e.base, _$1)), i.push($n$1(fa$1(e.base), v$2(e, t, Zn$1(r))))), A$1 in r && (s.push(N(e.base, A$1)), i.push(xn$1(pa$1(e.base), v$2(e, t, e.type === 1 ? K$1() : ia$1(r))))), H$1 in r && (s.push(N(e.base, H$1)), i.push($t$1(r[H$1]))), q$1 in r && (s.push(N(e.base, q$1)), i.push(r[q$1] ? Et$1 : St$1)), { k: s, v: i };
}
function le$3(e, t, r, n, s) {
  return da$1(r, n, s, $e$2(e, t, n));
}
function fi$1(e, t, r, n) {
  return vn$1(r, v$2(e, t, n.valueOf()));
}
function pi$1(e, t, r, n) {
  return En$1(r, n, v$2(e, t, n.buffer));
}
function di$1(e, t, r, n) {
  return Sn$1(r, n, v$2(e, t, n.buffer));
}
function hi$1(e, t, r, n) {
  return Rn$1(r, n, v$2(e, t, n.buffer));
}
function Je$1(e, t, r, n) {
  let s = At$1(n, e.base.features);
  return kn$1(r, n, s ? $e$2(e, t, s) : a);
}
function mi$1(e, t, r, n) {
  let s = At$1(n, e.base.features);
  return An$1(r, n, s ? $e$2(e, t, s) : a);
}
function gi$1(e, t, r, n) {
  let s = [], i = [];
  for (let [o, u] of n.entries()) s.push(v$2(e, t, o)), i.push(v$2(e, t, u));
  return ha$1(e.base, r, s, i);
}
function bi$1(e, t, r, n) {
  let s = [];
  for (let i of n.keys()) s.push(v$2(e, t, i));
  return _n$2(r, s);
}
function yi$1(e, t, r, n) {
  let s = zn$1(r, F(e.base, 4), []);
  return e.type === 1 || (Pe$2(e), n.on({ next: (i) => {
    if (e.state.alive) {
      let o = C$3(e, t, i);
      o && B$1(e, Pn$1(r, o));
    }
  }, throw: (i) => {
    if (e.state.alive) {
      let o = C$3(e, t, i);
      o && B$1(e, On$1(r, o));
    }
    Q$2(e);
  }, return: (i) => {
    if (e.state.alive) {
      let o = C$3(e, t, i);
      o && B$1(e, In$1(r, o));
    }
    Q$2(e);
  } })), s;
}
function wi$1(e, t, r) {
  if (this.state.alive) {
    let n = C$3(this, t, r);
    n && B$1(this, m$1(23, e, a, a, a, a, a, [F(this.base, 2), n], a, a, a, a)), Q$2(this);
  }
}
function vi$1(e, t, r) {
  if (this.state.alive) {
    let n = C$3(this, t, r);
    n && B$1(this, m$1(24, e, a, a, a, a, a, [F(this.base, 3), n], a, a, a, a));
  }
  Q$2(this);
}
function Ei$1(e, t, r, n) {
  let s = Ft$1(e.base, {});
  return e.type === 2 && (Pe$2(e), n.then(wi$1.bind(e, s, t), vi$1.bind(e, s, t))), ma$1(e.base, r, s);
}
function Si$1(e, t, r, n, s) {
  for (let i = 0, o = s.length; i < o; i++) {
    let u = s[i];
    if (u.parse.sync && u.test(n)) return xt$1(r, u.tag, u.parse.sync(n, new ii$1(e, t), { id: r }));
  }
  return a;
}
function Ri$1(e, t, r, n, s) {
  for (let i = 0, o = s.length; i < o; i++) {
    let u = s[i];
    if (u.parse.stream && u.test(n)) return xt$1(r, u.tag, u.parse.stream(n, new oi$1(e, t), { id: r }));
  }
  return a;
}
function Zt$1(e, t, r, n) {
  let s = e.base.plugins;
  return s ? e.type === 1 ? Si$1(e, t, r, n, s) : Ri$1(e, t, r, n, s) : a;
}
function ki$1(e, t, r, n) {
  let s = [];
  for (let i = 0, o = n.v.length; i < o; i++) s[i] = v$2(e, t, n.v[i]);
  return Nn$1(r, s, n.t, n.d);
}
function Ai$1(e, t, r, n, s) {
  switch (s) {
    case Object:
      return le$3(e, t, r, n, false);
    case a:
      return le$3(e, t, r, n, true);
    case Date:
      return mn$1(r, n);
    case Error:
    case EvalError:
    case RangeError:
    case ReferenceError:
    case SyntaxError:
    case TypeError:
    case URIError:
      return Je$1(e, t, r, n);
    case Number:
    case Boolean:
    case String:
    case BigInt:
      return fi$1(e, t, r, n);
    case ArrayBuffer:
      return ga$1(e.base, r, n);
    case Int8Array:
    case Int16Array:
    case Int32Array:
    case Uint8Array:
    case Uint16Array:
    case Uint32Array:
    case Uint8ClampedArray:
    case Float32Array:
    case Float64Array:
      return pi$1(e, t, r, n);
    case DataView:
      return hi$1(e, t, r, n);
    case Map:
      return gi$1(e, t, r, n);
    case Set:
      return bi$1(e, t, r, n);
  }
  if (s === Promise || n instanceof Promise) return Ei$1(e, t, r, n);
  let i = e.base.features;
  if (i & 32 && s === RegExp) return gn$1(r, n);
  if (i & 16) switch (s) {
    case BigInt64Array:
    case BigUint64Array:
      return di$1(e, t, r, n);
  }
  if (i & 1 && typeof AggregateError < "u" && (s === AggregateError || n instanceof AggregateError)) return mi$1(e, t, r, n);
  if (n instanceof Error) return Je$1(e, t, r, n);
  if (_$1 in n || A$1 in n) return le$3(e, t, r, n, !!s);
  throw new se$2(n);
}
function _i$2(e, t, r, n) {
  if (Array.isArray(n)) return li$1(e, t, r, n);
  if (sa$1(n)) return yi$1(e, t, r, n);
  if (Qn$1(n)) return ki$1(e, t, r, n);
  let s = n.constructor;
  return s === Mn$1 ? v$2(e, t, n.replacement) : Zt$1(e, t, r, n) || Ai$1(e, t, r, n, s);
}
function $i$1(e, t, r) {
  let n = Se$2(e.base, r);
  if (n.type !== 0) return n.value;
  let s = Zt$1(e, t, n.value, r);
  if (s) return s;
  throw new se$2(r);
}
function v$2(e, t, r) {
  if (t >= e.base.depthLimit) throw new Ot$1(e.base.depthLimit);
  switch (typeof r) {
    case "boolean":
      return r ? Et$1 : St$1;
    case "undefined":
      return Zr$1;
    case "string":
      return $t$1(r);
    case "number":
      return pn$1(r);
    case "bigint":
      return dn$1(r);
    case "object": {
      if (r) {
        let n = Se$2(e.base, r);
        return n.type === 0 ? _i$2(e, t + 1, n.value, r) : n.value;
      }
      return Kr$1;
    }
    case "symbol":
      return N(e.base, r);
    case "function":
      return $i$1(e, t, r);
    default:
      throw new se$2(r);
  }
}
function B$1(e, t) {
  e.state.initial ? e.state.buffer.push(t) : ze$1(e, t, false);
}
function xe$3(e, t) {
  if (e.state.onError) e.state.onError(t);
  else throw t instanceof De$2 ? t : new De$2(t);
}
function Kt$1(e) {
  e.state.onDone && e.state.onDone();
}
function ze$1(e, t, r) {
  try {
    e.state.onParse(t, r);
  } catch (n) {
    xe$3(e, n);
  }
}
function Pe$2(e) {
  e.state.pending++;
}
function Q$2(e) {
  --e.state.pending <= 0 && Kt$1(e);
}
function C$3(e, t, r) {
  try {
    return v$2(e, t, r);
  } catch (n) {
    return xe$3(e, n), a;
  }
}
function er$1(e, t) {
  let r = C$3(e, 0, t);
  r && (ze$1(e, r, true), e.state.initial = false, xi$1(e, e.state), e.state.pending <= 0 && Oe$2(e));
}
function xi$1(e, t) {
  for (let r = 0, n = t.buffer.length; r < n; r++) ze$1(e, t.buffer[r], false);
}
function Oe$2(e) {
  e.state.alive && (Kt$1(e), e.state.alive = false);
}
function zi$1(e, t) {
  let r = Re$2(t.plugins), n = Qt$1({ plugins: r, refs: t.refs, disabledFeatures: t.disabledFeatures, onParse(s, i) {
    let o = ys$1({ plugins: r, features: n.base.features, scopeId: t.scopeId, markedRefs: n.base.marked }), u;
    try {
      u = si$1(o, s);
    } catch (c) {
      t.onError && t.onError(c);
      return;
    }
    t.onSerialize(u, i);
  }, onError: t.onError, onDone: t.onDone });
  return er$1(n, e), Oe$2.bind(null, n);
}
function Pi$1(e, t) {
  let r = Re$2(t.plugins), n = Qt$1({ plugins: r, refs: t.refs, disabledFeatures: t.disabledFeatures, depthLimit: t.depthLimit, onParse: t.onParse, onError: t.onError, onDone: t.onDone });
  return er$1(n, e), Oe$2.bind(null, n);
}
function Oi$1(e, t = {}) {
  var r;
  let n = Re$2(t.plugins), s = t.disabledFeatures || 0, i = (r = e.f) != null ? r : 63, o = ka$1({ plugins: n, markedRefs: e.m, features: i & ~s, disabledFeatures: s });
  return as$1(o, e.t);
}
var we$3 = (e) => {
  let t = new AbortController(), r = t.abort.bind(t);
  return e.then(r, r), t;
};
function Ii$1(e) {
  e(this.reason);
}
function Ni$1(e) {
  this.addEventListener("abort", Ii$1.bind(this, e), { once: true });
}
function Ye$1(e) {
  return new Promise(Ni$1.bind(e));
}
var J$1 = {}, Ci$1 = { tag: "seroval-plugins/web/AbortControllerFactoryPlugin", test(e) {
  return e === J$1;
}, parse: { sync() {
  return J$1;
}, async async() {
  return await Promise.resolve(J$1);
}, stream() {
  return J$1;
} }, serialize() {
  return we$3.toString();
}, deserialize() {
  return we$3;
} }, Li$1 = { tag: "seroval-plugins/web/AbortSignal", extends: [Ci$1], test(e) {
  return typeof AbortSignal > "u" ? false : e instanceof AbortSignal;
}, parse: { sync(e, t) {
  return e.aborted ? { reason: t.parse(e.reason) } : {};
}, async async(e, t) {
  if (e.aborted) return { reason: await t.parse(e.reason) };
  let r = await Ye$1(e);
  return { reason: await t.parse(r) };
}, stream(e, t) {
  if (e.aborted) return { reason: t.parse(e.reason) };
  let r = Ye$1(e);
  return { factory: t.parse(J$1), controller: t.parse(r) };
} }, serialize(e, t) {
  return e.reason ? "AbortSignal.abort(" + t.serialize(e.reason) + ")" : e.controller && e.factory ? "(" + t.serialize(e.factory) + ")(" + t.serialize(e.controller) + ").signal" : "(new AbortController).signal";
}, deserialize(e, t) {
  return e.reason ? AbortSignal.abort(t.deserialize(e.reason)) : e.controller ? we$3(t.deserialize(e.controller)).signal : new AbortController().signal;
} }, Ti$1 = Li$1;
function fe$3(e) {
  return { detail: e.detail, bubbles: e.bubbles, cancelable: e.cancelable, composed: e.composed };
}
var Fi$1 = { tag: "seroval-plugins/web/CustomEvent", test(e) {
  return typeof CustomEvent > "u" ? false : e instanceof CustomEvent;
}, parse: { sync(e, t) {
  return { type: t.parse(e.type), options: t.parse(fe$3(e)) };
}, async async(e, t) {
  return { type: await t.parse(e.type), options: await t.parse(fe$3(e)) };
}, stream(e, t) {
  return { type: t.parse(e.type), options: t.parse(fe$3(e)) };
} }, serialize(e, t) {
  return "new CustomEvent(" + t.serialize(e.type) + "," + t.serialize(e.options) + ")";
}, deserialize(e, t) {
  return new CustomEvent(t.deserialize(e.type), t.deserialize(e.options));
} }, Ui$1 = Fi$1, Di$1 = { tag: "seroval-plugins/web/DOMException", test(e) {
  return typeof DOMException > "u" ? false : e instanceof DOMException;
}, parse: { sync(e, t) {
  return { name: t.parse(e.name), message: t.parse(e.message) };
}, async async(e, t) {
  return { name: await t.parse(e.name), message: await t.parse(e.message) };
}, stream(e, t) {
  return { name: t.parse(e.name), message: t.parse(e.message) };
} }, serialize(e, t) {
  return "new DOMException(" + t.serialize(e.message) + "," + t.serialize(e.name) + ")";
}, deserialize(e, t) {
  return new DOMException(t.deserialize(e.message), t.deserialize(e.name));
} }, ji$1 = Di$1;
function pe$3(e) {
  return { bubbles: e.bubbles, cancelable: e.cancelable, composed: e.composed };
}
var Mi$1 = { tag: "seroval-plugins/web/Event", test(e) {
  return typeof Event > "u" ? false : e instanceof Event;
}, parse: { sync(e, t) {
  return { type: t.parse(e.type), options: t.parse(pe$3(e)) };
}, async async(e, t) {
  return { type: await t.parse(e.type), options: await t.parse(pe$3(e)) };
}, stream(e, t) {
  return { type: t.parse(e.type), options: t.parse(pe$3(e)) };
} }, serialize(e, t) {
  return "new Event(" + t.serialize(e.type) + "," + t.serialize(e.options) + ")";
}, deserialize(e, t) {
  return new Event(t.deserialize(e.type), t.deserialize(e.options));
} }, qi$1 = Mi$1, Hi$1 = { tag: "seroval-plugins/web/File", test(e) {
  return typeof File > "u" ? false : e instanceof File;
}, parse: { async async(e, t) {
  return { name: await t.parse(e.name), options: await t.parse({ type: e.type, lastModified: e.lastModified }), buffer: await t.parse(await e.arrayBuffer()) };
} }, serialize(e, t) {
  return "new File([" + t.serialize(e.buffer) + "]," + t.serialize(e.name) + "," + t.serialize(e.options) + ")";
}, deserialize(e, t) {
  return new File([t.deserialize(e.buffer)], t.deserialize(e.name), t.deserialize(e.options));
} }, Bi$1 = Hi$1;
function de$3(e) {
  let t = [];
  return e.forEach((r, n) => {
    t.push([n, r]);
  }), t;
}
var z$2 = {}, tr$1 = (e, t = new FormData(), r = 0, n = e.length, s) => {
  for (; r < n; r++) s = e[r], t.append(s[0], s[1]);
  return t;
}, Vi$1 = { tag: "seroval-plugins/web/FormDataFactory", test(e) {
  return e === z$2;
}, parse: { sync() {
  return z$2;
}, async async() {
  return await Promise.resolve(z$2);
}, stream() {
  return z$2;
} }, serialize() {
  return tr$1.toString();
}, deserialize() {
  return z$2;
} }, Wi$1 = { tag: "seroval-plugins/web/FormData", extends: [Bi$1, Vi$1], test(e) {
  return typeof FormData > "u" ? false : e instanceof FormData;
}, parse: { sync(e, t) {
  return { factory: t.parse(z$2), entries: t.parse(de$3(e)) };
}, async async(e, t) {
  return { factory: await t.parse(z$2), entries: await t.parse(de$3(e)) };
}, stream(e, t) {
  return { factory: t.parse(z$2), entries: t.parse(de$3(e)) };
} }, serialize(e, t) {
  return "(" + t.serialize(e.factory) + ")(" + t.serialize(e.entries) + ")";
}, deserialize(e, t) {
  return tr$1(t.deserialize(e.entries));
} }, Gi$1 = Wi$1;
function he$2(e) {
  let t = [];
  return e.forEach((r, n) => {
    t.push([n, r]);
  }), t;
}
var Xi$1 = { tag: "seroval-plugins/web/Headers", test(e) {
  return typeof Headers > "u" ? false : e instanceof Headers;
}, parse: { sync(e, t) {
  return { value: t.parse(he$2(e)) };
}, async async(e, t) {
  return { value: await t.parse(he$2(e)) };
}, stream(e, t) {
  return { value: t.parse(he$2(e)) };
} }, serialize(e, t) {
  return "new Headers(" + t.serialize(e.value) + ")";
}, deserialize(e, t) {
  return new Headers(t.deserialize(e.value));
} }, Ie$2 = Xi$1, P$2 = {}, rr$1 = (e) => new ReadableStream({ start: (t) => {
  e.on({ next: (r) => {
    try {
      t.enqueue(r);
    } catch {
    }
  }, throw: (r) => {
    t.error(r);
  }, return: () => {
    try {
      t.close();
    } catch {
    }
  } });
} }), Ji$1 = { tag: "seroval-plugins/web/ReadableStreamFactory", test(e) {
  return e === P$2;
}, parse: { sync() {
  return P$2;
}, async async() {
  return await Promise.resolve(P$2);
}, stream() {
  return P$2;
} }, serialize() {
  return rr$1.toString();
}, deserialize() {
  return P$2;
} };
function Qe$1(e) {
  let t = K$1(), r = e.getReader();
  async function n() {
    try {
      let s = await r.read();
      s.done ? t.return(s.value) : (t.next(s.value), await n());
    } catch (s) {
      t.throw(s);
    }
  }
  return n().catch(() => {
  }), t;
}
var Yi$1 = { tag: "seroval/plugins/web/ReadableStream", extends: [Ji$1], test(e) {
  return typeof ReadableStream > "u" ? false : e instanceof ReadableStream;
}, parse: { sync(e, t) {
  return { factory: t.parse(P$2), stream: t.parse(K$1()) };
}, async async(e, t) {
  return { factory: await t.parse(P$2), stream: await t.parse(Qe$1(e)) };
}, stream(e, t) {
  return { factory: t.parse(P$2), stream: t.parse(Qe$1(e)) };
} }, serialize(e, t) {
  return "(" + t.serialize(e.factory) + ")(" + t.serialize(e.stream) + ")";
}, deserialize(e, t) {
  let r = t.deserialize(e.stream);
  return rr$1(r);
} }, Ne$2 = Yi$1;
function Ze$2(e, t) {
  return { body: t, cache: e.cache, credentials: e.credentials, headers: e.headers, integrity: e.integrity, keepalive: e.keepalive, method: e.method, mode: e.mode, redirect: e.redirect, referrer: e.referrer, referrerPolicy: e.referrerPolicy };
}
var Qi$1 = { tag: "seroval-plugins/web/Request", extends: [Ne$2, Ie$2], test(e) {
  return typeof Request > "u" ? false : e instanceof Request;
}, parse: { async async(e, t) {
  return { url: await t.parse(e.url), options: await t.parse(Ze$2(e, e.body && !e.bodyUsed ? await e.clone().arrayBuffer() : null)) };
}, stream(e, t) {
  return { url: t.parse(e.url), options: t.parse(Ze$2(e, e.body && !e.bodyUsed ? e.clone().body : null)) };
} }, serialize(e, t) {
  return "new Request(" + t.serialize(e.url) + "," + t.serialize(e.options) + ")";
}, deserialize(e, t) {
  return new Request(t.deserialize(e.url), t.deserialize(e.options));
} }, Zi$1 = Qi$1;
function Ke$1(e) {
  return { headers: e.headers, status: e.status, statusText: e.statusText };
}
var Ki$1 = { tag: "seroval-plugins/web/Response", extends: [Ne$2, Ie$2], test(e) {
  return typeof Response > "u" ? false : e instanceof Response;
}, parse: { async async(e, t) {
  return { body: await t.parse(e.body && !e.bodyUsed ? await e.clone().arrayBuffer() : null), options: await t.parse(Ke$1(e)) };
}, stream(e, t) {
  return { body: t.parse(e.body && !e.bodyUsed ? e.clone().body : null), options: t.parse(Ke$1(e)) };
} }, serialize(e, t) {
  return "new Response(" + t.serialize(e.body) + "," + t.serialize(e.options) + ")";
}, deserialize(e, t) {
  return new Response(t.deserialize(e.body), t.deserialize(e.options));
} }, eo$1 = Ki$1, to$1 = { tag: "seroval-plugins/web/URL", test(e) {
  return typeof URL > "u" ? false : e instanceof URL;
}, parse: { sync(e, t) {
  return { value: t.parse(e.href) };
}, async async(e, t) {
  return { value: await t.parse(e.href) };
}, stream(e, t) {
  return { value: t.parse(e.href) };
} }, serialize(e, t) {
  return "new URL(" + t.serialize(e.value) + ")";
}, deserialize(e, t) {
  return new URL(t.deserialize(e.value));
} }, ro$1 = to$1, no$1 = { tag: "seroval-plugins/web/URLSearchParams", test(e) {
  return typeof URLSearchParams > "u" ? false : e instanceof URLSearchParams;
}, parse: { sync(e, t) {
  return { value: t.parse(e.toString()) };
}, async async(e, t) {
  return { value: await t.parse(e.toString()) };
}, stream(e, t) {
  return { value: t.parse(e.toString()) };
} }, serialize(e, t) {
  return "new URLSearchParams(" + t.serialize(e.value) + ")";
}, deserialize(e, t) {
  return new URLSearchParams(t.deserialize(e.value));
} }, ao$1 = no$1;
const Ce$3 = [Ti$1, Ui$1, ji$1, qi$1, Gi$1, Ie$2, Ne$2, Zi$1, eo$1, ao$1, ro$1], so$1 = 64, nr$1 = ct$1.RegExp;
function ar$1(e) {
  const t = new TextEncoder().encode(e), r = t.length, n = r.toString(16), s = "00000000".substring(0, 8 - n.length) + n, i = new TextEncoder().encode(`;0x${s};`), o = new Uint8Array(12 + r);
  return o.set(i), o.set(t, 12), o;
}
function et$1(e, t) {
  return new ReadableStream({ start(r) {
    zi$1(t, { scopeId: e, plugins: Ce$3, onSerialize(n, s) {
      r.enqueue(ar$1(s ? `(${on$1(e)},${n})` : n));
    }, onDone() {
      r.close();
    }, onError(n) {
      r.error(n);
    } });
  } });
}
function io$1(e) {
  return new ReadableStream({ start(t) {
    Pi$1(e, { disabledFeatures: nr$1, depthLimit: so$1, plugins: Ce$3, onParse(r) {
      t.enqueue(ar$1(JSON.stringify(r)));
    }, onDone() {
      t.close();
    }, onError(r) {
      t.error(r);
    } });
  } });
}
async function tt$1(e) {
  return Oi$1(JSON.parse(e), { plugins: Ce$3, disabledFeatures: nr$1 });
}
async function oo$1(e) {
  const t = He$4(e), r = t.request, n = r.headers.get("X-Server-Id"), s = r.headers.get("X-Server-Instance"), i = r.headers.has("X-Single-Flight"), o = new URL(r.url);
  let u, c;
  if (n) $r$1(typeof n == "string", "Invalid server function"), [u, c] = decodeURIComponent(n).split("#");
  else if (u = o.searchParams.get("id"), c = o.searchParams.get("name"), !u || !c) return new Response(null, { status: 404 });
  const l = Wr$1[u];
  let p;
  if (!l) return new Response(null, { status: 404 });
  p = await l.importer();
  const d = p[l.functionName];
  let w = [];
  if (!s || e.method === "GET") {
    const f = o.searchParams.get("args");
    if (f) {
      const S = await tt$1(f);
      for (const te of S) w.push(te);
    }
  }
  if (e.method === "POST") {
    const f = r.headers.get("content-type"), S = e.node.req, te = S instanceof ReadableStream, sr = S.body instanceof ReadableStream, ir = te && S.locked || sr && S.body.locked, or = te ? S : S.body, ue = ir ? r : new Request(r, { ...r, body: or });
    r.headers.get("x-serialized") ? w = await tt$1(await ue.text()) : (f == null ? void 0 : f.startsWith("multipart/form-data")) || (f == null ? void 0 : f.startsWith("application/x-www-form-urlencoded")) ? w.push(await ue.formData()) : (f == null ? void 0 : f.startsWith("application/json")) && (w = await ue.json());
  }
  try {
    let f = await provideRequestEvent(t, async () => (sharedConfig.context = { event: t }, t.locals.serverFunctionMeta = { id: u + "#" + c }, d(...w)));
    if (i && s && (f = await nt$1(t, f)), f instanceof Response) {
      if (f.headers && f.headers.has("X-Content-Raw")) return f;
      s && (f.headers && qe$4(e, f.headers), f.status && (f.status < 300 || f.status >= 400) && R$4(e, f.status), f.customBody ? f = await f.customBody() : f.body == null && (f = null));
    }
    if (!s) return rt$1(f, r, w);
    return Ce$4(e, "x-serialized", "true"), Ce$4(e, "content-type", "text/javascript"), et$1(s, f);
    return io$1(f);
  } catch (f) {
    if (f instanceof Response) i && s && (f = await nt$1(t, f)), f.headers && qe$4(e, f.headers), f.status && (!s || f.status < 300 || f.status >= 400) && R$4(e, f.status), f.customBody ? f = f.customBody() : f.body == null && (f = null), Ce$4(e, "X-Error", "true");
    else if (s) {
      const S = f instanceof Error ? f.message : typeof f == "string" ? f : "true";
      Ce$4(e, "X-Error", S.replace(/[\r\n]+/g, ""));
    } else f = rt$1(f, r, w, true);
    return s ? (Ce$4(e, "x-serialized", "true"), Ce$4(e, "content-type", "text/javascript"), et$1(s, f)) : f;
  }
}
function rt$1(e, t, r, n) {
  const s = new URL(t.url), i = e instanceof Error;
  let o = 302, u;
  return e instanceof Response ? (u = new Headers(e.headers), e.headers.has("Location") && (u.set("Location", new URL(e.headers.get("Location"), s.origin + "").toString()), o = Vr$1(e))) : u = new Headers({ Location: new URL(t.headers.get("referer")).toString() }), e && u.append("Set-Cookie", `flash=${encodeURIComponent(JSON.stringify({ url: s.pathname + s.search, result: i ? e.message : e, thrown: n, error: i, input: [...r.slice(0, -1), [...r[r.length - 1].entries()]] }))}; Secure; HttpOnly;`), new Response(null, { status: o, headers: u });
}
let me$2;
function uo$1(e) {
  var _a2;
  const t = new Headers(e.request.headers), r = _e$5(e.nativeEvent), n = e.response.headers.getSetCookie();
  t.delete("cookie");
  let s = false;
  return ((_a2 = e.nativeEvent.node) == null ? void 0 : _a2.req) && (s = true, e.nativeEvent.node.req.headers.cookie = ""), n.forEach((i) => {
    if (!i) return;
    const { maxAge: o, expires: u, name: c, value: l } = kr$1(i);
    if (o != null && o <= 0) {
      delete r[c];
      return;
    }
    if (u != null && u.getTime() <= Date.now()) {
      delete r[c];
      return;
    }
    r[c] = l;
  }), Object.entries(r).forEach(([i, o]) => {
    t.append("cookie", `${i}=${o}`), s && (e.nativeEvent.node.req.headers.cookie += `${i}=${o};`);
  }), t;
}
async function nt$1(e, t) {
  let r, n = new URL(e.request.headers.get("referer")).toString();
  t instanceof Response && (t.headers.has("X-Revalidate") && (r = t.headers.get("X-Revalidate").split(",")), t.headers.has("Location") && (n = new URL(t.headers.get("Location"), new URL(e.request.url).origin + "").toString()));
  const s = pe$4(e);
  return s.request = new Request(n, { headers: uo$1(e) }), await provideRequestEvent(s, async () => {
    await Hr$1(s), me$2 || (me$2 = (await import('../build/app-JEG4qAOq.mjs')).default), s.router.dataOnly = r || true, s.router.previousUrl = e.request.headers.get("referer");
    try {
      renderToString(() => {
        sharedConfig.context.event = s, me$2();
      });
    } catch (u) {
      console.log(u);
    }
    const i = s.router.data;
    if (!i) return t;
    let o = false;
    for (const u in i) i[u] === void 0 ? delete i[u] : o = true;
    return o && (t instanceof Response ? t.customBody && (i._$value = t.customBody()) : (i._$value = t, t = new Response(null, { status: 200 })), t.customBody = () => i, t.headers.set("X-Single-Flight", "true")), t;
  });
}
const So$1 = eventHandler(oo$1);

var __defProp$2 = Object.defineProperty;
var __defNormalProp$2 = (obj, key, value) => key in obj ? __defProp$2(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField$2 = (obj, key, value) => __defNormalProp$2(obj, typeof key !== "symbol" ? key + "" : key, value);
var _a2$1, _b, _c2, _d2, _e2, _f2, _g, _h2, _i2, _j, _k, _l2, _m2, _n2;
const ke$1 = (e, t) => {
  switch (t.length) {
    case 0:
      return e;
    case 1:
      return t[0](e);
    case 2:
      return t[1](t[0](e));
    case 3:
      return t[2](t[1](t[0](e)));
    case 4:
      return t[3](t[2](t[1](t[0](e))));
    case 5:
      return t[4](t[3](t[2](t[1](t[0](e)))));
    case 6:
      return t[5](t[4](t[3](t[2](t[1](t[0](e))))));
    case 7:
      return t[6](t[5](t[4](t[3](t[2](t[1](t[0](e)))))));
    case 8:
      return t[7](t[6](t[5](t[4](t[3](t[2](t[1](t[0](e))))))));
    case 9:
      return t[8](t[7](t[6](t[5](t[4](t[3](t[2](t[1](t[0](e)))))))));
    default: {
      let n = e;
      for (let r = 0, s = t.length; r < s; r++) n = t[r](n);
      return n;
    }
  }
}, fc = { pipe() {
  return ke$1(this, arguments);
} }, Er = (function() {
  function e() {
  }
  return e.prototype = fc, e;
})(), d$1 = function(e, t) {
  if (typeof e == "function") return function() {
    return e(arguments) ? t.apply(this, arguments) : (n) => t(n, ...arguments);
  };
  switch (e) {
    case 0:
    case 1:
      throw new RangeError(`Invalid arity ${e}`);
    case 2:
      return function(n, r) {
        return arguments.length >= 2 ? t(n, r) : function(s) {
          return t(s, n);
        };
      };
    case 3:
      return function(n, r, s) {
        return arguments.length >= 3 ? t(n, r, s) : function(o) {
          return t(o, n, r);
        };
      };
    default:
      return function() {
        if (arguments.length >= e) return t.apply(this, arguments);
        const n = arguments;
        return function(r) {
          return t(r, ...n);
        };
      };
  }
}, B = (e) => e, Nt = (e) => () => e, dc = Nt(true), Gs = Nt(void 0), qs = Gs;
function pc(e, t, n, r, s, o, i, u, c) {
  switch (arguments.length) {
    case 1:
      return e;
    case 2:
      return function() {
        return t(e.apply(this, arguments));
      };
    case 3:
      return function() {
        return n(t(e.apply(this, arguments)));
      };
    case 4:
      return function() {
        return r(n(t(e.apply(this, arguments))));
      };
    case 5:
      return function() {
        return s(r(n(t(e.apply(this, arguments)))));
      };
    case 6:
      return function() {
        return o(s(r(n(t(e.apply(this, arguments))))));
      };
    case 7:
      return function() {
        return i(o(s(r(n(t(e.apply(this, arguments)))))));
      };
    case 8:
      return function() {
        return u(i(o(s(r(n(t(e.apply(this, arguments))))))));
      };
    case 9:
      return function() {
        return c(u(i(o(s(r(n(t(e.apply(this, arguments)))))))));
      };
  }
}
function De$1(e) {
  const t = /* @__PURE__ */ new WeakMap();
  return (n) => {
    if (t.has(n)) return t.get(n);
    const r = e(n);
    return t.set(n, r), r;
  };
}
const Xn = (e) => {
  const t = new Set(Reflect.ownKeys(e));
  if (e.constructor === Object) return t;
  e instanceof Error && t.delete("stack");
  const n = Object.getPrototypeOf(e);
  let r = n;
  for (; r !== null && r !== Object.prototype; ) {
    const s = Reflect.ownKeys(r);
    for (let o = 0; o < s.length; o++) t.add(s[o]);
    r = Object.getPrototypeOf(r);
  }
  return t.has("constructor") && typeof e.constructor == "function" && n === e.constructor.prototype && t.delete("constructor"), t;
}, er = /* @__PURE__ */ new WeakSet();
function Hs(e) {
  return typeof e == "string";
}
function Zs(e) {
  return typeof e == "number";
}
function hc(e) {
  return typeof e == "boolean";
}
function mc(e) {
  return typeof e == "symbol";
}
function gc(e) {
  return Hs(e) || Zs(e) || mc(e);
}
function br(e) {
  return typeof e == "function";
}
function yc(e) {
  return e != null;
}
function Qs(e) {
  return true;
}
function Ec(e) {
  return typeof e == "object" && e !== null || br(e);
}
const P$1 = d$1(2, (e, t) => Ec(e) && t in e), ee$1 = "~effect/interfaces/Hash", D$1 = (e) => {
  switch (typeof e) {
    case "number":
      return hn(e);
    case "bigint":
      return R$2(e.toString(10));
    case "boolean":
      return R$2(String(e));
    case "symbol":
      return R$2(String(e));
    case "string":
      return R$2(e);
    case "undefined":
      return R$2("undefined");
    case "function":
    case "object": {
      if (e === null) return R$2("null");
      if (e instanceof Date) return R$2(e.toISOString());
      if (e instanceof RegExp) return R$2(e.toString());
      {
        if (er.has(e)) return tr(e);
        if (Kn.has(e)) return Kn.get(e);
        const t = wc(e, () => bc(e) ? e[ee$1]() : typeof e == "function" ? tr(e) : Array.isArray(e) || ArrayBuffer.isView(e) ? Xs(e) : e instanceof Map ? _c$1(e) : e instanceof Set ? xc(e) : Tc(e));
        return Kn.set(e, t), t;
      }
    }
    default:
      throw new Error(`BUG: unhandled typeof ${typeof e} - please report an issue at https://github.com/Effect-TS/effect/issues`);
  }
}, tr = (e) => (Jn.has(e) || Jn.set(e, hn(Math.floor(Math.random() * Number.MAX_SAFE_INTEGER))), Jn.get(e)), de$2 = d$1(2, (e, t) => e * 53 ^ t), pn = (e) => e & 3221225471 | e >>> 1 & 1073741824, bc = (e) => P$1(e, ee$1), hn = (e) => {
  if (e !== e) return R$2("NaN");
  if (e === 1 / 0) return R$2("Infinity");
  if (e === -1 / 0) return R$2("-Infinity");
  let t = e | 0;
  for (t !== e && (t ^= e * 4294967295); e > 4294967295; ) t ^= e /= 4294967295;
  return pn(t);
}, R$2 = (e) => {
  let t = 5381, n = e.length;
  for (; n; ) t = t * 33 ^ e.charCodeAt(--n);
  return pn(t);
}, Sc = (e, t) => {
  let n = 12289;
  for (const r of t) n ^= de$2(D$1(r), D$1(e[r]));
  return pn(n);
}, Tc = (e) => Sc(e, Xn(e)), Sr = (e, t) => (n) => {
  let r = e;
  for (const s of n) r ^= t(s);
  return pn(r);
}, Xs = Sr(6151, D$1), _c$1 = Sr(R$2("Map"), ([e, t]) => de$2(D$1(e), D$1(t))), xc = Sr(R$2("Set"), D$1), Jn = /* @__PURE__ */ new WeakMap(), Kn = /* @__PURE__ */ new WeakMap(), Bn = /* @__PURE__ */ new WeakSet();
function wc(e, t) {
  if (Bn.has(e)) return R$2("[Circular]");
  Bn.add(e);
  const n = t();
  return Bn.delete(e), n;
}
const te$1 = "~effect/interfaces/Equal";
function oe$1() {
  return arguments.length === 1 ? (e) => Pe$1(e, arguments[0]) : Pe$1(arguments[0], arguments[1]);
}
function Pe$1(e, t) {
  if (e === t) return true;
  if (e == null || t == null) return false;
  const n = typeof e;
  return n !== typeof t ? false : n === "number" && e !== e && t !== t ? true : n !== "object" && n !== "function" || er.has(e) || er.has(t) ? false : kc(e, t, Ac);
}
function Oc(e, t, n) {
  const r = Yn.has(e), s = Wn.has(t);
  if (r && s) return true;
  if (r || s) return false;
  Yn.add(e), Wn.add(t);
  const o = n();
  return Yn.delete(e), Wn.delete(t), o;
}
const Yn = /* @__PURE__ */ new WeakSet(), Wn = /* @__PURE__ */ new WeakSet();
function Ac(e, t) {
  if (D$1(e) !== D$1(t)) return false;
  if (e instanceof Date) return t instanceof Date ? e.toISOString() === t.toISOString() : false;
  if (e instanceof RegExp) return t instanceof RegExp ? e.toString() === t.toString() : false;
  const n = ps(e), r = ps(t);
  if (n !== r) return false;
  const s = n && r;
  return typeof e == "function" && !s ? false : Oc(e, t, () => s ? e[te$1](t) : Array.isArray(e) ? !Array.isArray(t) || e.length !== t.length ? false : Ic(e, t) : ArrayBuffer.isView(e) ? !ArrayBuffer.isView(t) || e.byteLength !== t.byteLength ? false : vc(e, t) : e instanceof Map ? !(t instanceof Map) || e.size !== t.size ? false : Dc(e, t) : e instanceof Set ? !(t instanceof Set) || e.size !== t.size ? false : Fc(e, t) : Cc(e, t));
}
function kc(e, t, n) {
  let r = Wt.get(e);
  if (!r) r = /* @__PURE__ */ new WeakMap(), Wt.set(e, r);
  else if (r.has(t)) return r.get(t);
  const s = n(e, t);
  r.set(t, s);
  let o = Wt.get(t);
  return o || (o = /* @__PURE__ */ new WeakMap(), Wt.set(t, o)), o.set(e, s), s;
}
const Wt = /* @__PURE__ */ new WeakMap();
function Ic(e, t) {
  for (let n = 0; n < e.length; n++) if (!Pe$1(e[n], t[n])) return false;
  return true;
}
function vc(e, t) {
  if (e.length !== t.length) return false;
  for (let n = 0; n < e.length; n++) if (e[n] !== t[n]) return false;
  return true;
}
function Cc(e, t) {
  const n = Xn(e), r = Xn(t);
  if (n.size !== r.size) return false;
  for (const s of n) if (!r.has(s) || !Pe$1(e[s], t[s])) return false;
  return true;
}
function $c(e, t) {
  return function(r, s) {
    for (const [o, i] of r) {
      let u = false;
      for (const [c, a] of s) if (e(o, c) && t(i, a)) {
        u = true;
        break;
      }
      if (!u) return false;
    }
    return true;
  };
}
const Dc = $c(Pe$1, Pe$1);
function Uc(e) {
  return function(n, r) {
    for (const s of n) {
      let o = false;
      for (const i of r) if (e(s, i)) {
        o = true;
        break;
      }
      if (!o) return false;
    }
    return true;
  };
}
const Fc = Uc(Pe$1), ps = (e) => P$1(e, te$1), Nc = () => oe$1, Tr = /* @__PURE__ */ Symbol.for("~effect/Redactable"), Rc = (e) => P$1(e, Tr);
function eo(e) {
  return Rc(e) ? to(e) : e;
}
function to(e) {
  var _a3, _b2;
  return e[Tr]((_b2 = (_a3 = globalThis[Tt]) == null ? void 0 : _a3.context) != null ? _b2 : Pc);
}
const Tt = "~effect/Fiber/currentFiber", Pc = { "~effect/Context": {}, mapUnsafe: /* @__PURE__ */ new Map(), pipe() {
  return ke$1(this, arguments);
} };
function L$1(e, t) {
  var _a3;
  const n = (_a3 = t == null ? void 0 : t.space) != null ? _a3 : 0, r = /* @__PURE__ */ new WeakSet(), s = n ? typeof n == "number" ? " ".repeat(n) : n : "", o = (a) => s.repeat(a), i = (a, l) => {
    const f = a == null ? void 0 : a.constructor;
    return f && f !== Object.prototype.constructor && f.name ? `${f.name}(${l})` : l;
  }, u = (a) => {
    try {
      return Reflect.ownKeys(a);
    } catch {
      return ["[ownKeys threw]"];
    }
  };
  function c(a, l = 0) {
    if (Array.isArray(a)) {
      if (r.has(a)) return hs;
      if (r.add(a), !s || a.length <= 1) return `[${a.map((p) => c(p, l)).join(",")}]`;
      const f = a.map((p) => c(p, l + 1)).join(`,
` + o(l + 1));
      return `[
${o(l + 1)}${f}
${o(l)}]`;
    }
    if (a instanceof Date) return jc(a);
    if (!(t == null ? void 0 : t.ignoreToString) && P$1(a, "toString") && typeof a.toString == "function" && a.toString !== Object.prototype.toString && a.toString !== Array.prototype.toString) {
      const f = Mc(a);
      return a instanceof Error && a.cause ? `${f} (cause: ${c(a.cause, l)})` : f;
    }
    if (typeof a == "string") return JSON.stringify(a);
    if (typeof a == "number" || a == null || typeof a == "boolean" || typeof a == "symbol") return String(a);
    if (typeof a == "bigint") return String(a) + "n";
    if (typeof a == "object" || typeof a == "function") {
      if (r.has(a)) return hs;
      if (r.add(a), Tr in a) return L$1(to(a));
      if (Symbol.iterator in a) return `${a.constructor.name}(${c(Array.from(a), l)})`;
      const f = u(a);
      if (!s || f.length <= 1) {
        const E = `{${f.map((m) => `${Xt(m)}:${c(a[m], l)}`).join(",")}}`;
        return i(a, E);
      }
      const p = `{
${f.map((E) => `${o(l + 1)}${Xt(E)}: ${c(a[E], l + 1)}`).join(`,
`)}
${o(l)}}`;
      return i(a, p);
    }
    return String(a);
  }
  return c(e, 0);
}
const hs = "[Circular]";
function Xt(e) {
  return typeof e == "string" ? JSON.stringify(e) : String(e);
}
function Lc(e) {
  return e.map((t) => `[${Xt(t)}]`).join("");
}
function jc(e) {
  try {
    return e.toISOString();
  } catch {
    return "Invalid Date";
  }
}
function Mc(e) {
  try {
    const t = e.toString();
    return typeof t == "string" ? t : String(t);
  } catch {
    return "[toString threw]";
  }
}
function no(e, t) {
  const n = [];
  return JSON.stringify(e, function(r, s) {
    const o = eo(s);
    if (typeof o != "object" || o === null) return o;
    for (; n.length > 0 && n[n.length - 1] !== this; ) n.pop();
    if (!n.includes(o)) return n.push(o), o;
  }, void 0 );
}
const mn = /* @__PURE__ */ Symbol.for("nodejs.util.inspect.custom"), gn = (e) => {
  try {
    if (P$1(e, "toJSON") && br(e.toJSON) && e.toJSON.length === 0) return e.toJSON();
    if (Array.isArray(e)) return e.map(gn);
  } catch {
    return "[toJSON threw]";
  }
  return eo(e);
};
class Rt {
  constructor(t) {
    __publicField$2(this, "called", false);
    __publicField$2(this, "self");
    this.self = t;
  }
  next(t) {
    return this.called ? { value: t, done: true } : (this.called = true, { value: this.self, done: false });
  }
  [Symbol.iterator]() {
    return new Rt(this.self);
  }
}
const Xe = "~effect/Utils/internal", ro = { [Xe]: (e) => e() }, zc = { [Xe]: (e) => {
  try {
    return e();
  } finally {
  }
} }, Vc = ((_a2$1 = ro[Xe](() => new Error().stack)) == null ? void 0 : _a2$1.includes(Xe)) === true, se$1 = Vc ? ro[Xe] : zc[Xe], Ue = "~effect/Effect", At = "~effect/Exit", Jc = { _A: B, _E: B, _R: B }, so = `${Ue}/identifier`, g = `${Ue}/args`, U = `${Ue}/evaluate`, Q$1 = `${Ue}/successCont`, ge$2 = `${Ue}/failureCont`, Le$1 = `${Ue}/ensureCont`, Ne$1 = /* @__PURE__ */ Symbol.for("effect/Effect/Yield"), Pt = { pipe() {
  return ke$1(this, arguments);
}, toJSON() {
  return { ...this };
}, toString() {
  return L$1(this.toJSON(), { ignoreToString: true, space: 2 });
}, [mn]() {
  return this.toJSON();
} }, Kc = { [Ue]: Jc, ...Pt, [Symbol.iterator]() {
  return new Rt(this);
}, toJSON() {
  return { _id: "Effect", op: this[so], ...g in this ? { args: this[g] } : void 0 };
} }, _r$1 = (e) => P$1(e, Ue), Bc = (e) => P$1(e, At), Gt = "~effect/Cause", Gn = "~effect/Cause/Reason", oo = (e) => P$1(e, Gt);
class it {
  constructor(t) {
    __publicField$2(this, _b);
    __publicField$2(this, "reasons");
    this[Gt] = Gt, this.reasons = t;
  }
  pipe() {
    return ke$1(this, arguments);
  }
  toJSON() {
    return { _id: "Cause", failures: this.reasons.map((t) => t.toJSON()) };
  }
  toString() {
    return `Cause(${L$1(this.reasons)})`;
  }
  [(_b = Gt, mn)]() {
    return this.toJSON();
  }
  [te$1](t) {
    return oo(t) && this.reasons.length === t.reasons.length && this.reasons.every((n, r) => oe$1(n, t.reasons[r]));
  }
  [ee$1]() {
    return Xs(this.reasons);
  }
}
const ms = /* @__PURE__ */ new WeakMap();
class xr {
  constructor(t, n, r) {
    __publicField$2(this, _c2);
    __publicField$2(this, "annotations");
    __publicField$2(this, "_tag");
    if (this[Gn] = Gn, this._tag = t, n !== yn && typeof r == "object" && r !== null && n.size > 0) {
      const s = ms.get(r);
      s && (n = new Map([...s, ...n])), ms.set(r, n);
    }
    this.annotations = n;
  }
  annotate(t, n) {
    if (t.mapUnsafe.size === 0) return this;
    const r = new Map(this.annotations);
    t.mapUnsafe.forEach((o, i) => {
      (n == null ? void 0 : n.overwrite) !== true && r.has(i) || r.set(i, o);
    });
    const s = Object.assign(Object.create(Object.getPrototypeOf(this)), this);
    return s.annotations = r, s;
  }
  pipe() {
    return ke$1(this, arguments);
  }
  toString() {
    return L$1(this);
  }
  [(_c2 = Gn, mn)]() {
    return this.toString();
  }
}
const yn = /* @__PURE__ */ new Map();
class Yc extends xr {
  constructor(t, n = yn) {
    super("Fail", n, t);
    __publicField$2(this, "error");
    this.error = t;
  }
  toString() {
    return `Fail(${L$1(this.error)})`;
  }
  toJSON() {
    return { _tag: "Fail", error: this.error };
  }
  [te$1](t) {
    return co(t) && oe$1(this.error, t.error) && oe$1(this.annotations, t.annotations);
  }
  [ee$1]() {
    return de$2(R$2(this._tag))(de$2(D$1(this.error))(D$1(this.annotations)));
  }
}
const nr = (e) => new it(e), io = (e) => new it([new Yc(e)]);
class Wc extends xr {
  constructor(t, n = yn) {
    super("Die", n, t);
    __publicField$2(this, "defect");
    this.defect = t;
  }
  toString() {
    return `Die(${L$1(this.defect)})`;
  }
  toJSON() {
    return { _tag: "Die", defect: this.defect };
  }
  [te$1](t) {
    return Gc(t) && oe$1(this.defect, t.defect) && oe$1(this.annotations, t.annotations);
  }
  [ee$1]() {
    return de$2(R$2(this._tag))(de$2(D$1(this.defect))(D$1(this.annotations)));
  }
}
const ao = (e) => new it([new Wc(e)]), rr = d$1((e) => oo(e[0]), (e, t, n) => t.mapUnsafe.size === 0 ? e : new it(e.reasons.map((r) => r.annotate(t, n)))), co = (e) => e._tag === "Fail", Gc = (e) => e._tag === "Die", uo = (e) => e._tag === "Interrupt";
function qc(e) {
  return kt("Effect.evaluate: Not implemented");
}
const ze = (e) => {
  var _a3;
  return { ...Kc, [so]: e.op, [U]: (_a3 = e[U]) != null ? _a3 : qc, [Q$1]: e[Q$1], [ge$2]: e[ge$2], [Le$1]: e[Le$1] };
}, he$1 = (e) => {
  const t = ze(e);
  return function() {
    const n = Object.create(t);
    return n[g] = e.single === false ? arguments : arguments[0], n;
  };
}, lo = (e) => {
  const t = { ...ze(e), [At]: At, _tag: e.op, get [e.prop]() {
    return this[g];
  }, toString() {
    return `${e.op}(${L$1(this[g])})`;
  }, toJSON() {
    return { _id: "Exit", _tag: e.op, [e.prop]: this[g] };
  }, [te$1](n) {
    return Bc(n) && n._tag === this._tag && oe$1(this[g], n[g]);
  }, [ee$1]() {
    return de$2(R$2(e.op), D$1(this[g]));
  } };
  return function(n) {
    const r = Object.create(t);
    return r[g] = n, r;
  };
}, I$1 = lo({ op: "Success", prop: "value", [U](e) {
  const t = e.getCont(Q$1);
  return t ? t[Q$1](this[g], e, this) : e.yieldWith(this);
} }), En = { key: "effect/Cause/StackTrace" }, Hc = { key: "effect/Cause/InterruptorStackTrace" }, ie$1 = lo({ op: "Failure", prop: "cause", [U](e) {
  let t = this[g], n = false;
  e.currentStackFrame && (t = rr(t, { mapUnsafe: /* @__PURE__ */ new Map([[En.key, e.currentStackFrame]]) }), n = true);
  let r = e.getCont(ge$2);
  for (; e.interruptible && e._interruptedCause && r; ) r = e.getCont(ge$2);
  return r ? r[ge$2](t, e, n ? void 0 : this) : e.yieldWith(n ? this : ie$1(t));
} }), Ve = (e) => ie$1(io(e)), kt = (e) => ie$1(ao(e)), Y$1 = he$1({ op: "WithFiber", [U](e) {
  return this[g](e);
} }), Zc = (function() {
  class e extends globalThis.Error {
  }
  const t = ze({ op: "YieldableError", [U]() {
    return Ve(this);
  } });
  return delete t.toString, Object.assign(e.prototype, t), e;
})(), Qc = /* @__PURE__ */ (function() {
  const e = /* @__PURE__ */ Symbol.for("effect/Data/Error/plainArgs");
  return class extends Zc {
    constructor(n) {
      super(n == null ? void 0 : n.message, (n == null ? void 0 : n.cause) ? { cause: n.cause } : void 0), n && (Object.assign(this, n), Object.defineProperty(this, e, { value: n, enumerable: false }));
    }
    toJSON() {
      return { ...this[e], ...this };
    }
  };
})(), wr = (e) => {
  class t extends Qc {
    constructor() {
      super(...arguments);
      __publicField$2(this, "_tag", e);
    }
  }
  return t.prototype.name = e, t;
}, It = "~effect/Cause/Done", Xc = (e) => P$1(e, It), fo = { [It]: It, _tag: "Done", value: void 0 }, Or = (e) => e === void 0 ? fo : { [It]: It, _tag: "Done", value: e }, eu = Ve(fo), tu = (e) => e === void 0 ? eu : Ve(Or(e)), nu = (e) => ze({ op: e.label, [U]: e.evaluate }), ru = (e) => (t, n) => t === n || e(t, n), po = "~effect/data/Option", ho = { [po]: { _A: (e) => e }, ...Pt, [Symbol.iterator]() {
  return new Rt(this);
} }, mo = Object.assign(Object.create(ho), { _tag: "Some", _op: "Some", [te$1](e) {
  return go(e) && Eo(e) && oe$1(this.value, e.value);
}, [ee$1]() {
  return de$2(D$1(this._tag))(D$1(this.value));
}, toString() {
  return `some(${L$1(this.value)})`;
}, toJSON() {
  return { _id: "Option", _tag: this._tag, value: gn(this.value) };
} });
Object.defineProperty(mo, "valueOrUndefined", { get() {
  return this.value;
} });
const su = D$1("None"), ou = Object.assign(Object.create(ho), { _tag: "None", _op: "None", valueOrUndefined: void 0, [te$1](e) {
  return go(e) && yo(e);
}, [ee$1]() {
  return su;
}, toString() {
  return "none()";
}, toJSON() {
  return { _id: "Option", _tag: this._tag };
} }), go = (e) => P$1(e, po), yo = (e) => e._tag === "None", Eo = (e) => e._tag === "Some", iu = Object.create(ou), au = (e) => {
  const t = Object.create(mo);
  return t.value = e, t;
}, bo = "~effect/data/Result", So = { [bo]: { _A: (e) => e, _E: (e) => e }, ...Pt, [Symbol.iterator]() {
  return new Rt(this);
} }, cu = Object.assign(Object.create(So), { _tag: "Success", _op: "Success", [te$1](e) {
  return Ar(e) && _o$1(e) && oe$1(this.success, e.success);
}, [ee$1]() {
  return de$2(D$1(this._tag))(D$1(this.success));
}, toString() {
  return `success(${L$1(this.success)})`;
}, toJSON() {
  return { _id: "Result", _tag: this._tag, value: gn(this.success) };
} }), uu = Object.assign(Object.create(So), { _tag: "Failure", _op: "Failure", [te$1](e) {
  return Ar(e) && To(e) && oe$1(this.failure, e.failure);
}, [ee$1]() {
  return de$2(D$1(this._tag))(D$1(this.failure));
}, toString() {
  return `failure(${L$1(this.failure)})`;
}, toJSON() {
  return { _id: "Result", _tag: this._tag, failure: gn(this.failure) };
} }), Ar = (e) => P$1(e, bo), To = (e) => e._tag === "Failure", _o$1 = (e) => e._tag === "Success", lu = (e) => {
  const t = Object.create(uu);
  return t.failure = e, t;
}, fu = (e) => {
  const t = Object.create(cu);
  return t.success = e, t;
};
function du(e) {
  return (t, n) => t === n ? 0 : e(t, n);
}
const pu = du((e, t) => globalThis.Number.isNaN(e) && globalThis.Number.isNaN(t) ? 0 : globalThis.Number.isNaN(e) ? -1 : globalThis.Number.isNaN(t) ? 1 : e < t ? -1 : 1), hu = (e) => d$1(2, (t, n) => e(t, n) !== -1), at = () => iu, C$2 = au, Lt = yo, gs = Eo, mu = d$1(2, (e, { onNone: t, onSome: n }) => Lt(e) ? t() : n(e.value)), gu = (e) => (...t) => {
  try {
    return C$2(e(...t));
  } catch {
    return at();
  }
}, xo = d$1(2, (e, t) => Lt(e) ? at() : C$2(t(e.value))), ys = "~effect/Context/Service", wo = function() {
  var _a3;
  const e = Error.stackTraceLimit;
  Error.stackTraceLimit = 2;
  const t = new Error();
  Error.stackTraceLimit = e;
  function n() {
  }
  const r = n;
  return Object.setPrototypeOf(r, yu), Object.defineProperty(r, "stack", { get() {
    return t.stack;
  } }), arguments.length > 0 ? (r.key = arguments[0], ((_a3 = arguments[1]) == null ? void 0 : _a3.defaultValue) && (r[sr] = sr, r.defaultValue = arguments[1].defaultValue), r) : function(s, o) {
    return r.key = s, (o == null ? void 0 : o.make) && (r.make = o.make), r;
  };
}, yu = { [ys]: ys, ...nu({ label: "Service", evaluate(e) {
  return I$1(qt(e.context, this));
} }), toJSON() {
  return { _id: "Service", key: this.key, stack: this.stack };
}, of(e) {
  return e;
}, context(e) {
  return Ao(this, e);
}, use(e) {
  return Y$1((t) => e(qt(t.context, this)));
}, useSync(e) {
  return Y$1((t) => I$1(e(qt(t.context, this))));
} }, sr = "~effect/Context/Reference", Oo = "~effect/Context", bn = (e) => {
  const t = Object.create(Eu);
  return t.mapUnsafe = e, t.mutable = false, t;
}, Eu = { ...Pt, [Oo]: { _Services: (e) => e }, toJSON() {
  return { _id: "Context", services: Array.from(this.mapUnsafe).map(([e, t]) => ({ key: e, value: t })) };
}, [te$1](e) {
  if (!bu(e) || this.mapUnsafe.size !== e.mapUnsafe.size) return false;
  for (const t of this.mapUnsafe.keys()) if (!e.mapUnsafe.has(t) || !oe$1(this.mapUnsafe.get(t), e.mapUnsafe.get(t))) return false;
  return true;
}, [ee$1]() {
  return hn(this.mapUnsafe.size);
} }, bu = (e) => P$1(e, Oo), ct = () => Su, Su = bn(/* @__PURE__ */ new Map()), Ao = (e, t) => bn(/* @__PURE__ */ new Map([[e.key, t]])), ko = d$1(3, (e, t, n) => Co(e, (r) => {
  r.set(t.key, n);
})), Tu = d$1(2, (e, t) => {
  if (!e.mapUnsafe.has(t.key)) {
    if (sr in t) return Io(t);
    throw xu(t);
  }
  return e.mapUnsafe.get(t.key);
}), qt = Tu, _u$1 = (e, t) => e.mapUnsafe.has(t.key) ? e.mapUnsafe.get(t.key) : Io(t), qn = "~effect/Context/defaultValue", Io = (e) => qn in e ? e[qn] : e[qn] = e.defaultValue(), xu = (e) => {
  const t = new Error(`Service not found${e.key ? `: ${String(e.key)}` : ""}`);
  if (e.stack) {
    const n = e.stack.split(`
`);
    if (n.length > 2) {
      const r = n[2].match(/at (.*)/);
      r && (t.message = t.message + ` (defined at ${r[1]})`);
    }
  }
  if (t.stack) {
    const n = t.stack.split(`
`);
    n.splice(1, 3), t.stack = n.join(`
`);
  }
  return t;
}, vo = d$1(2, (e, t) => e.mapUnsafe.size === 0 ? t : t.mapUnsafe.size === 0 ? e : Co(e, (n) => {
  t.mapUnsafe.forEach((r, s) => n.set(s, r));
})), Co = (e, t) => {
  if (e.mutable) return t(e.mapUnsafe), e;
  const n = new Map(e.mapUnsafe);
  return t(n), bn(n);
}, ut = wo, $o = (e) => e.length > 0, lt = fu, we$2 = lu, wu = Ar, X$1 = To, jt = _o$1, Ou = d$1(2, (e, { onFailure: t, onSuccess: n }) => X$1(e) ? t(e.failure) : n(e.success)), Au = d$1(2, (e, t) => {
  if (jt(e)) return e.success;
  throw t(e.failure);
}), vm = Au(B), Es = globalThis.Array, Oe$1 = (e) => Es.isArray(e) ? e : Es.from(e), Do = d$1(2, (e, t) => [...e, t]), ku = d$1(2, (e, t) => Oe$1(e).concat(Oe$1(t))), bs = $o, vt = $o;
function Iu(e, t) {
  return e < 0 || e >= t.length;
}
const vu = d$1(2, (e, t) => {
  const n = Math.floor(t);
  if (Iu(n, e)) throw new Error(`Index out of bounds: ${n}`);
  return e[n];
}), Cu = vu(0), $u = (e) => e.slice(1), Du = d$1(3, (e, t, n) => {
  const r = Oe$1(e), s = Oe$1(t);
  return vt(r) ? vt(s) ? Ru(n)(ku(r, s)) : r : s;
}), Uu = d$1(2, (e, t) => Du(e, t, Nc())), Fu = () => [], Nu = d$1(2, (e, t) => e.map(t)), Ru = d$1(2, (e, t) => {
  const n = Oe$1(e);
  if (vt(n)) {
    const r = [Cu(n)], s = $u(n);
    for (const o of s) r.every((i) => !t(o, i)) && r.push(o);
    return r;
  }
  return [];
}), Uo = d$1(2, (e, t) => (n) => {
  const r = e(n);
  if (X$1(r)) return we$2(n);
  const s = t(r.success);
  return X$1(s) ? we$2(n) : s;
}), Fo = ut("effect/Scheduler", { defaultValue: () => new kr() }), No = "setImmediate" in globalThis ? (e) => {
  const t = globalThis.setImmediate(e);
  return () => globalThis.clearImmediate(t);
} : (e) => {
  const t = setTimeout(e, 0);
  return () => clearTimeout(t);
};
class Pu {
  constructor() {
    __publicField$2(this, "buckets", []);
  }
  scheduleTask(t, n) {
    const r = this.buckets, s = r.length;
    let o, i = 0;
    for (; i < s && !(r[i][0] > n); i++) o = r[i];
    o && o[0] === n ? o[1].push(t) : i === s ? r.push([n, [t]]) : r.splice(i, 0, [n, [t]]);
  }
  drain() {
    const t = this.buckets;
    return this.buckets = [], t;
  }
}
class kr {
  constructor(t = "async", n = No) {
    __publicField$2(this, "executionMode");
    __publicField$2(this, "setImmediate");
    this.executionMode = t, this.setImmediate = n;
  }
  shouldYield(t) {
    return t.currentOpCount >= t.maxOpsBeforeYield;
  }
  makeDispatcher() {
    return new Lu(this.setImmediate);
  }
}
class Lu {
  constructor(t = No) {
    __publicField$2(this, "tasks", new Pu());
    __publicField$2(this, "running");
    __publicField$2(this, "setImmediate");
    __publicField$2(this, "afterScheduled", () => {
      this.running = void 0, this.runTasks();
    });
    this.setImmediate = t;
  }
  scheduleTask(t, n) {
    this.tasks.scheduleTask(t, n), this.running === void 0 && (this.running = this.setImmediate(this.afterScheduled));
  }
  runTasks() {
    const t = this.tasks.drain();
    for (let n = 0; n < t.length; n++) {
      const r = t[n][1];
      for (let s = 0; s < r.length; s++) r[s]();
    }
  }
  flush() {
    for (; this.tasks.buckets.length > 0; ) this.running !== void 0 && (this.running(), this.running = void 0), this.runTasks();
  }
}
const ju = ut("effect/Scheduler/MaxOpsBeforeYield", { defaultValue: () => 2048 }), Mu = ut("effect/Scheduler/PreventSchedulerYield", { defaultValue: () => false }), zu = "effect/Tracer/ParentSpan", Vu = "effect/Tracer", Ju = "effect/observability/Metric/FiberRuntimeMetricsKey", Ku = ut("effect/References/CurrentStackFrame", { defaultValue: Gs }), Bu = ut("effect/References/CurrentLogLevel", { defaultValue: () => "Info" }), Yu = ut("effect/References/MinimumLogLevel", { defaultValue: () => "Info" }), Wu = "dev";
class Gu extends xr {
  constructor(t, n = yn) {
    super("Interrupt", n, "Interrupted");
    __publicField$2(this, "fiberId");
    this.fiberId = t;
  }
  toString() {
    return `Interrupt(${this.fiberId})`;
  }
  toJSON() {
    return { _tag: "Interrupt", fiberId: this.fiberId };
  }
  [te$1](t) {
    return uo(t) && this.fiberId === t.fiberId && this.annotations === t.annotations;
  }
  [ee$1]() {
    return de$2(R$2(`${this._tag}:${this.fiberId}`))(tr(this.annotations));
  }
}
const Ro = (e) => new it([new Gu(e)]), Mt = (e) => {
  for (let t = 0; t < e.reasons.length; t++) {
    const n = e.reasons[t];
    if (n._tag === "Fail") return lt(n.error);
  }
  return we$2(e);
}, qu = (e) => e.reasons.some(uo), Hu = d$1(2, (e, t) => {
  if (e.reasons.length === 0) return t;
  if (t.reasons.length === 0) return e;
  const n = new it(Uu(e.reasons, t.reasons));
  return oe$1(e, n) ? e : n;
}), Zu = (e) => {
  const t = { Fail: [], Die: [], Interrupt: [] };
  for (let n = 0; n < e.reasons.length; n++) t[e.reasons[n]._tag].push(e.reasons[n]);
  return t;
}, Po = (e) => {
  const t = Zu(e);
  return t.Fail.length > 0 ? t.Fail[0].error : t.Die.length > 0 ? t.Die[0].defect : t.Interrupt.length > 0 ? new globalThis.Error("All fibers interrupted without error") : new globalThis.Error("Empty cause");
}, Qu = (e) => {
  const t = [], n = [];
  if (e.reasons.length === 0) return t;
  const r = Error.stackTraceLimit;
  Error.stackTraceLimit = 1;
  for (const s of e.reasons) {
    if (s._tag === "Interrupt") {
      n.push(s);
      continue;
    }
    t.push(or(s._tag === "Die" ? s.defect : s.error, s.annotations));
  }
  if (t.length === 0) {
    const s = new Error("The fiber was interrupted by:");
    s.name = "InterruptCause", s.stack = nl(s, n);
    const o = new globalThis.Error("All fibers interrupted without error", { cause: s });
    o.name = "InterruptError", o.stack = `${o.name}: ${o.message}`, t.push(or(o, n[0].annotations));
  }
  return Error.stackTraceLimit = r, t;
}, or = (e, t) => {
  const n = typeof e;
  let r;
  if (e && n === "object") {
    if (r = new globalThis.Error(Xu(e), { cause: e.cause ? or(e.cause) : void 0 }), typeof e.name == "string" && (r.name = e.name), typeof e.stack == "string") r.stack = tl(e.stack, r, t);
    else {
      const s = `${r.name}: ${r.message}`;
      r.stack = t ? Lo(s, t) : s;
    }
    for (const s of Object.keys(e)) s in r || (r[s] = e[s]);
  } else r = new globalThis.Error(e ? n === "string" ? e : no(e) : `Unknown error: ${e}`);
  return r;
}, Xu = (e) => {
  if (typeof e.message == "string") return e.message;
  if (typeof e.toString == "function" && e.toString !== Object.prototype.toString && e.toString !== Array.prototype.toString) try {
    return e.toString();
  } catch {
  }
  return no(e);
}, el = /\((.*)\)/g, tl = (e, t, n) => {
  const r = `${t.name}: ${t.message}`, s = (e.startsWith(r) ? e.slice(r.length) : e).split(`
`), o = [r];
  for (let i = 1; i < s.length && !/(?:Generator\.next|~effect\/Effect)/.test(s[i]); i++) o.push(s[i]);
  return n ? Lo(o.join(`
`), n) : o.join(`
`);
}, Lo = (e, t) => {
  const n = t == null ? void 0 : t.get(En.key);
  return n && (e = `${e}
${jo(n)}`), e;
}, nl = (e, t) => {
  const n = [`${e.name}: ${e.message}`];
  for (const r of t) {
    const s = r.fiberId !== void 0 ? `#${r.fiberId}` : "unknown", o = r.annotations.get(Hc.key);
    n.push(`    at fiber (${s})`), o && n.push(jo(o));
  }
  return n.join(`
`);
}, jo = (e) => {
  const t = [];
  let n = e, r = 0;
  for (; n && r < 10; ) {
    const s = n.stack();
    if (s) {
      const o = s.matchAll(el);
      let i = false;
      for (const [, u] of o) i = true, t.push(`    at ${n.name} (${u})`);
      i || t.push(`    at ${n.name} (${s.replace(/^at /, "")})`);
    } else t.push(`    at ${n.name}`);
    n = n.parent, r++;
  }
  return t.join(`
`);
}, rl = (e) => Qu(e).map((t) => t.cause ? `${t.stack} {
${Mo(t.cause, "  ")}
}` : t.stack).join(`
`), Mo = (e, t) => {
  const n = e.stack.split(`
`);
  let r = `${t}[cause]: ${n[0]}`;
  for (let s = 1, o = n.length; s < o; s++) r += `
${t}${n[s]}`;
  return e.cause && (r += ` {
${Mo(e.cause, `${t}  `)}
${t}}`), r;
}, ir = `~effect/Fiber/${Wu}`, sl = { _A: B, _E: B }, ol = { id: 0 }, en = () => globalThis[Tt];
_d2 = ir;
class zo {
  constructor(t, n = true) {
    __publicField$2(this, _d2);
    __publicField$2(this, "id");
    __publicField$2(this, "interruptible");
    __publicField$2(this, "currentOpCount");
    __publicField$2(this, "currentLoopCount");
    __publicField$2(this, "_stack");
    __publicField$2(this, "_observers");
    __publicField$2(this, "_exit");
    __publicField$2(this, "_currentExit");
    __publicField$2(this, "_children");
    __publicField$2(this, "_interruptedCause");
    __publicField$2(this, "_yielded");
    __publicField$2(this, "context");
    __publicField$2(this, "currentScheduler");
    __publicField$2(this, "currentTracerContext");
    __publicField$2(this, "currentSpan");
    __publicField$2(this, "currentLogLevel");
    __publicField$2(this, "minimumLogLevel");
    __publicField$2(this, "currentStackFrame");
    __publicField$2(this, "runtimeMetrics");
    __publicField$2(this, "maxOpsBeforeYield");
    __publicField$2(this, "currentPreventYield");
    __publicField$2(this, "_dispatcher");
    var _a3;
    this[ir] = sl, this.setContext(t), this.id = ++ol.id, this.currentOpCount = 0, this.currentLoopCount = 0, this.interruptible = n, this._stack = [], this._observers = [], this._exit = void 0, this._children = void 0, this._interruptedCause = void 0, this._yielded = void 0, (_a3 = this.runtimeMetrics) == null ? void 0 : _a3.recordFiberStart(this.context);
  }
  get currentDispatcher() {
    var _a3;
    return (_a3 = this._dispatcher) != null ? _a3 : this._dispatcher = this.currentScheduler.makeDispatcher();
  }
  getRef(t) {
    return _u$1(this.context, t);
  }
  addObserver(t) {
    return this._exit ? (t(this._exit), qs) : (this._observers.push(t), () => {
      const n = this._observers.indexOf(t);
      n >= 0 && this._observers.splice(n, 1);
    });
  }
  interruptUnsafe(t, n) {
    if (this._exit) return;
    let r = Ro(t);
    this.currentStackFrame && (r = rr(r, Ao(En, this.currentStackFrame))), n && (r = rr(r, n)), this._interruptedCause = this._interruptedCause ? Hu(this._interruptedCause, r) : r, this.interruptible && this.evaluate(Ce$2(this._interruptedCause));
  }
  pollUnsafe() {
    return this._exit;
  }
  evaluate(t) {
    var _a3;
    if (this._exit) return;
    if (this._yielded !== void 0) {
      const r = this._yielded;
      this._yielded = void 0, r();
    }
    const n = this.runLoop(t);
    if (n !== Ne$1) {
      this._exit = n, (_a3 = this.runtimeMetrics) == null ? void 0 : _a3.recordFiberEnd(this.context, this._exit);
      for (let r = 0; r < this._observers.length; r++) this._observers[r](n);
      this._observers.length = 0;
    }
  }
  runLoop(t) {
    const n = globalThis[Tt];
    globalThis[Tt] = this;
    let r = false, s = t;
    this.currentOpCount = 0;
    const o = ++this.currentLoopCount;
    try {
      for (; ; ) {
        if (this.currentOpCount++, !r && !this.currentPreventYield && this.currentScheduler.shouldYield(this)) {
          r = true;
          const i = s;
          s = W(Ko, () => i);
        }
        if (s = this.currentTracerContext ? this.currentTracerContext(s, this) : s[U](this), o !== this.currentLoopCount) return Ne$1;
        if (s === Ne$1) {
          const i = this._yielded;
          return At in i ? (this._yielded = void 0, i) : Ne$1;
        }
      }
    } catch (i) {
      return P$1(s, U) ? this.runLoop(kt(i)) : kt(`Fiber.runLoop: Not a valid effect: ${String(s)}`);
    } finally {
      globalThis[Tt] = n;
    }
  }
  getCont(t) {
    for (; ; ) {
      const n = this._stack.pop();
      if (!n) return;
      const r = n[Le$1] && n[Le$1](this);
      if (r) return r[t] = r, r;
      if (n[t]) return n;
    }
  }
  yieldWith(t) {
    return this._yielded = t, Ne$1;
  }
  children() {
    var _a3;
    return (_a3 = this._children) != null ? _a3 : this._children = /* @__PURE__ */ new Set();
  }
  pipe() {
    return ke$1(this, arguments);
  }
  setContext(t) {
    this.context = t;
    const n = this.getRef(Fo);
    n !== this.currentScheduler && (this.currentScheduler = n, this._dispatcher = void 0), this.currentSpan = t.mapUnsafe.get(zu), this.currentLogLevel = this.getRef(Bu), this.minimumLogLevel = this.getRef(Yu), this.currentStackFrame = t.mapUnsafe.get(Ku.key), this.maxOpsBeforeYield = this.getRef(ju), this.currentPreventYield = this.getRef(Mu), this.runtimeMetrics = t.mapUnsafe.get(Ju);
    const r = t.mapUnsafe.get(Vu);
    this.currentTracerContext = r ? r.context : void 0;
  }
  get currentSpanLocal() {
    var _a3;
    return ((_a3 = this.currentSpan) == null ? void 0 : _a3._tag) === "Span" ? this.currentSpan : void 0;
  }
}
const Sn = (e) => {
  if (!e.currentStackFrame) return;
  const t = /* @__PURE__ */ new Map();
  return t.set(En.key, e.currentStackFrame), bn(t);
}, il = (e) => {
  const t = e;
  return t._exit ? w$1(t._exit) : Fe((n) => t._exit ? n(w$1(t._exit)) : Z$1(e.addObserver((r) => n(w$1(r)))));
}, Vo = (e) => Fe((t) => {
  const n = e[Symbol.iterator](), r = [];
  let s;
  function o() {
    let i = n.next();
    for (; !i.done; ) {
      if (i.value._exit) {
        r.push(i.value._exit), i = n.next();
        continue;
      }
      s = i.value.addObserver((u) => {
        r.push(u), o();
      });
      return;
    }
    t(w$1(r));
  }
  return o(), Z$1(() => s == null ? void 0 : s());
}), al = (e) => {
  const t = e;
  return t._exit ? t._exit : Fe((n) => t._exit ? n(t._exit) : Z$1(e.addObserver(n)));
}, Jo = (e) => Y$1((t) => cl(e, t.id)), cl = d$1((e) => P$1(e[0], ir), (e, t, n) => Y$1((r) => {
  let s = Sn(r);
  return s = s && n ? vo(s, n) : s != null ? s : n, e.interruptUnsafe(t, s), Cr(il(e));
})), ul = (e) => Y$1((t) => {
  const n = Sn(t);
  for (const r of e) r.interruptUnsafe(t.id, n);
  return Cr(Vo(e));
}), w$1 = I$1, Ce$2 = ie$1, et = Ve, Z$1 = he$1({ op: "Sync", [U](e) {
  const t = this[g](), n = e.getCont(Q$1);
  return n ? n[Q$1](t, e) : e.yieldWith(I$1(t));
} }), M$1 = he$1({ op: "Suspend", [U](e) {
  return this[g]();
} }), ll = Ou({ onFailure: et, onSuccess: w$1 }), fl = he$1({ op: "Yield", [U](e) {
  var _a3;
  let t = false;
  return e.currentDispatcher.scheduleTask(() => {
    t || e.evaluate(ye$2);
  }, (_a3 = this[g]) != null ? _a3 : 0), e.yieldWith(() => {
    t = true;
  });
} }), Ko = fl(0), dl = (e) => w$1(C$2(e)), Bo = w$1(at()), Yo = (e) => kt(e), pl = (e) => M$1(() => et(se$1(e))), le$2 = w$1(void 0), hl = (e) => M$1(() => {
  try {
    return w$1(se$1(e.try));
  } catch (t) {
    return et(se$1(() => e.catch(t)));
  }
}), ml = (e) => Ir(function(t, n) {
  se$1(() => e(n)).then((r) => t(w$1(r)), (r) => t(Yo(r)));
}, e.length !== 0), gl = (e) => {
  const t = typeof e == "function" ? e : e.try, n = typeof e == "function" ? (r) => new bf(r, "An error occurred in Effect.tryPromise") : e.catch;
  return Ir(function(r, s) {
    try {
      se$1(() => t(s)).then((o) => r(w$1(o)), (o) => r(et(se$1(() => n(o)))));
    } catch (o) {
      r(et(se$1(() => n(o))));
    }
  }, eval.length !== 0);
}, yl = (e) => Y$1((t) => e(t.id)), Ir = he$1({ op: "Async", single: false, [U](e) {
  const t = se$1(() => this[g][0].bind(e.currentScheduler));
  let n = false, r = false;
  const s = this[g][1] ? new AbortController() : void 0, o = t((i) => {
    n || (n = true, r ? e.evaluate(i) : r = i);
  }, s == null ? void 0 : s.signal);
  return r !== false ? r : (r = true, e._yielded = () => {
    n = true;
  }, s === void 0 && o === void 0 || e._stack.push(El(() => (n = true, s == null ? void 0 : s.abort(), o != null ? o : ye$2))), Ne$1);
} }), El = he$1({ op: "AsyncFinalizer", [Le$1](e) {
  e.interruptible && (e.interruptible = false, e._stack.push(Rr));
}, [ge$2](e, t) {
  return qu(e) ? W(this[g](), () => Ce$2(e)) : Ce$2(e);
} }), Fe = (e) => Ir(e, e.length >= 2), bl = (...e) => M$1(() => Ct(e.length === 1 ? e[0]() : e[1].call(e[0].self))), Wo = (e, ...t) => {
  const n = t.length === 0 ? function() {
    return M$1(() => Ct(e.apply(this, arguments)));
  } : function() {
    let r = M$1(() => Ct(e.apply(this, arguments)));
    for (let s = 0; s < t.length; s++) r = t[s](r, ...arguments);
    return r;
  };
  return Go(e.length, n);
}, Go = (e, t) => Object.defineProperty(t, "length", { value: e, configurable: true }), Sl = (e, ...t) => Go(e.length, t.length === 0 ? function() {
  return Ss(() => e.apply(this, arguments));
} : function() {
  let n = Ss(() => e.apply(this, arguments));
  for (const r of t) n = r(n);
  return n;
}), Ss = (e) => {
  try {
    const t = e();
    let n;
    for (; ; ) {
      const r = t.next(n);
      if (r.done) return w$1(r.value);
      const s = r.value;
      if (s && s._tag === "Success") {
        n = s.value;
        continue;
      } else {
        if (s && s._tag === "Failure") return r.value;
        {
          let o = true;
          return M$1(() => o ? (o = false, W(r.value, (i) => Ct(t, i))) : M$1(() => Ct(e())));
        }
      }
    }
  } catch (t) {
    return Yo(t);
  }
}, Ct = he$1({ op: "Iterator", single: false, [Q$1](e, t) {
  const n = this[g][0];
  for (; ; ) {
    const r = n.next(e);
    if (r.done) return w$1(r.value);
    if (ae$1(r.value)) {
      if (r.value._tag === "Failure") return r.value;
    } else return t._stack.push(this), r.value;
    e = r.value.value;
  }
}, [U](e) {
  return this[Q$1](this[g][1], e);
} }), Tl = d$1(2, (e, t) => {
  const n = w$1(t);
  return W(e, (r) => n);
}), _l$1 = (e) => $r(e, C$2), vr = d$1(2, (e, t) => W(e, (n) => _r$1(t) ? t : se$1(() => t(n)))), Cr = (e) => W(e, (t) => ye$2), W = d$1(2, (e, t) => {
  const n = Object.create(xl);
  return n[g] = e, n[Q$1] = t.length !== 1 ? (r) => t(r) : t, n;
}), xl = ze({ op: "OnSuccess", [U](e) {
  return e._stack.push(this), this[g];
} }), ae$1 = (e) => At in e, wl = d$1(2, (e, t) => ae$1(e) ? e._tag === "Success" ? t(e.value) : e : W(e, t)), Ol = (e) => W(e, B), $r = d$1(2, (e, t) => W(e, (n) => w$1(se$1(() => t(n))))), Al = d$1(2, (e, t) => ae$1(e) ? $l(e, t) : $r(e, t)), kl = d$1(2, (e, t) => ae$1(e) ? Dl(e, t) : Vl(e, t)), Il = d$1(2, (e, t) => {
  if (ae$1(e)) {
    if (e._tag === "Success") return e;
    const n = Mt(e.cause);
    return X$1(n) ? e : t(n.success);
  }
  return Zo(e, t);
}), vl = (e) => ie$1(Ro(e)), Dr = (e) => e._tag === "Success", Cl = (e) => e._tag === "Failure" ? lt(e.cause) : we$2(e), ye$2 = I$1(void 0), $l = d$1(2, (e, t) => e._tag === "Success" ? I$1(t(e.value)) : e), Dl = d$1(2, (e, t) => {
  if (e._tag === "Success") return e;
  const n = Mt(e.cause);
  return X$1(n) ? e : Ve(t(n.success));
}), Ul = d$1(2, (e, t) => Dr(e) ? t : e), Fl = (e) => {
  const t = [];
  for (const n of e) n._tag === "Failure" && t.push(...n.cause.reasons);
  return t.length === 0 ? ye$2 : ie$1(nr(t));
}, Nl = (e) => Dr(e) ? C$2(e.value) : at(), Rl = d$1(2, (e, t) => Y$1((n) => {
  const r = n.context, s = t(r);
  return r === s ? e : (n.setContext(s), Dt(e, () => {
    n.setContext(r);
  }));
})), Pl = () => Ll, Ll = Y$1((e) => w$1(e.context)), jl = d$1(2, (e, t) => ae$1(e) ? e : Rl(e, vo(t))), Ml = d$1((e) => _r$1(e[0]), (e, t) => of({ while: dc, body: Nt((t == null ? void 0 : t.disableYield) ? e : W(e, (n) => Ko)), step: qs })), qo = d$1(2, (e, t) => {
  const n = Object.create(zl);
  return n[g] = e, n[ge$2] = t.length !== 1 ? (r) => t(r) : t, n;
}), zl = ze({ op: "OnFailure", [U](e) {
  return e._stack.push(this), this[g];
} }), Ho = d$1(3, (e, t, n) => qo(e, (r) => {
  const s = t(r);
  return X$1(s) ? Ce$2(s.failure) : se$1(() => n(s.success, r));
})), Zo = d$1(2, (e, t) => Ho(e, Mt, (n) => t(n))), Vl = d$1(2, (e, t) => Zo(e, (n) => pl(() => t(n)))), Jl = (e) => Bl(e, { onFailure: we$2, onSuccess: lt }), Qo = d$1(2, (e, t) => {
  const n = Object.create(Kl);
  return n[g] = e, n[Q$1] = t.onSuccess.length !== 1 ? (r) => t.onSuccess(r) : t.onSuccess, n[ge$2] = t.onFailure.length !== 1 ? (r) => t.onFailure(r) : t.onFailure, n;
}), Kl = ze({ op: "OnSuccessAndFailure", [U](e) {
  return e._stack.push(this), this[g];
} }), Xo = d$1(2, (e, t) => Qo(e, { onFailure: (n) => {
  const r = n.reasons.find(co);
  return r ? se$1(() => t.onFailure(r.error)) : Ce$2(n);
}, onSuccess: t.onSuccess })), ei = d$1(2, (e, t) => Xo(e, { onFailure: (n) => Z$1(() => t.onFailure(n)), onSuccess: (n) => Z$1(() => t.onSuccess(n)) })), Bl = d$1(2, (e, t) => {
  if (ae$1(e)) {
    if (e._tag === "Success") return I$1(t.onSuccess(e.value));
    const n = Mt(e.cause);
    return X$1(n) ? e : I$1(t.onFailure(n.success));
  }
  return ei(e, t);
}), Ur = (e) => ae$1(e) ? I$1(e) : Yl(e), Yl = he$1({ op: "Exit", [U](e) {
  return e._stack.push(this), this[g];
}, [Q$1](e, t, n) {
  return w$1(n != null ? n : I$1(e));
}, [ge$2](e, t, n) {
  return w$1(n != null ? n : ie$1(e));
} }), Ts = "~effect/Scope", _s$1 = "~effect/Scope/Closeable", ti = wo("effect/Scope"), ni = (e, t) => M$1(() => {
  var _a3;
  return (_a3 = ri(e, t)) != null ? _a3 : le$2;
}), ri = (e, t) => {
  if (e.state._tag === "Closed") return;
  const n = { _tag: "Closed", exit: t };
  if (e.state._tag === "Empty") {
    e.state = n;
    return;
  }
  const { finalizers: r } = e.state;
  if (e.state = n, r.size !== 0) return r.size === 1 ? r.values().next().value(t) : Wl(e, r, t);
}, Wl = Wo(function* (e, t, n) {
  let r = [];
  const s = [], o = Array.from(t.values()), i = en();
  for (let u = o.length - 1; u >= 0; u--) {
    const c = o[u];
    e.strategy === "sequential" ? r.push(yield* Ur(c(n))) : s.push(Pr(i, c(n), true, true, "inherit"));
  }
  return s.length > 0 && (r = yield* Vo(s)), yield* Fl(r);
}), Gl = (e, t) => {
  const n = Nr(t);
  if (e.state._tag === "Closed") return n.state = e.state, n;
  const r = {};
  return $t(e, r, (s) => ni(n, s)), $t(n, r, (s) => Z$1(() => Fr(e, r))), n;
}, ql = (e, t) => M$1(() => e.state._tag === "Closed" ? t(e.state.exit) : ($t(e, {}, t), le$2)), Hl = (e, t) => ql(e, Nt(t)), $t = (e, t, n) => {
  e.state._tag === "Empty" ? e.state = { _tag: "Open", finalizers: /* @__PURE__ */ new Map([[t, n]]) } : e.state._tag === "Open" && e.state.finalizers.set(t, n);
}, Fr = (e, t) => {
  e.state._tag === "Open" && e.state.finalizers.delete(t);
}, Nr = (e = "sequential") => ({ [_s$1]: _s$1, [Ts]: Ts, strategy: e, state: Zl }), Zl = { _tag: "Empty" }, Ql = (e) => Z$1(() => Nr(e)), Xl = (e) => Y$1((t) => {
  const n = t.context, r = Nr();
  return t.setContext(ko(t.context, ti, r)), Dt(e, (s) => (t.setContext(n), ri(r, s)));
}), Dt = he$1({ op: "OnExit", single: false, [U](e) {
  return e._stack.push(this), this[g][0];
}, [Le$1](e) {
  e.interruptible && this[g][2] !== true && (e._stack.push(Rr), e.interruptible = false);
}, [Q$1](e, t, n) {
  n != null ? n : n = I$1(e);
  const r = this[g][1](n);
  return r ? W(r, (s) => n) : n;
}, [ge$2](e, t, n) {
  n != null ? n : n = ie$1(e);
  const r = this[g][1](n);
  return r ? W(r, (s) => n) : n;
} }), ef = d$1(2, Dt), tf = d$1(3, (e, t, n) => ef(e, (r) => {
  const s = t(r);
  return X$1(s) ? le$2 : n(s.success, r);
})), nf = d$1(2, (e, t) => tf(e, Cl, t)), si = he$1({ op: "SetInterruptible", [Le$1](e) {
  if (e.interruptible = this[g], e._interruptedCause && e.interruptible) return () => Ce$2(e._interruptedCause);
} }), Rr = si(true), rf = si(false), sf = (e) => Y$1((t) => t.interruptible ? e : (t.interruptible = true, t._stack.push(rf), t._interruptedCause ? Ce$2(t._interruptedCause) : e)), xs = (e) => Y$1((t) => t.interruptible ? (t.interruptible = false, t._stack.push(Rr), e(sf)) : e(B)), of = he$1({ op: "While", [Q$1](e, t) {
  return this[g].step(e), this[g].while() ? (t._stack.push(this), this[g].body()) : ye$2;
}, [U](e) {
  return this[g].while() ? (e._stack.push(this), this[g].body()) : ye$2;
} }), af = (e) => {
  const t = e.onItem, n = e.step;
  return (r, s, o) => {
    var _a3, _b2, _c3;
    let i = (_a3 = o == null ? void 0 : o.start) != null ? _a3 : 0;
    const u = (_b2 = o == null ? void 0 : o.end) != null ? _b2 : s.length, c = (_c3 = o == null ? void 0 : o.concurrency) != null ? _c3 : 1;
    let a = false, l, f, p, E = false, m, y;
    const k = () => {
      let J = false;
      for (; !m && i < u; i++) {
        const _ = s[i], F = y != null ? y : t(r, _, i);
        if (ae$1(F)) {
          if (m = n(r, _, F, i), m) break;
        } else {
          if (c === 1) return W(Ur(F), (x) => {
            var _a4;
            return m = n(r, _, x, i), i++, (_a4 = m != null ? m : k()) != null ? _a4 : le$2;
          });
          if (l) {
            y = void 0;
            const x = Pr(l, F, true, true, "inherit");
            if (x._exit) {
              if (m = n(r, _, x._exit, i), m) break;
              continue;
            }
            f ? f.add(x) : f = /* @__PURE__ */ new Set([x]);
            const N = i;
            if (x.addObserver((Be) => {
              if (f.delete(x), m) {
                if (!E && Be._tag === "Failure") for (const q of Be.cause.reasons) q._tag !== "Interrupt" && (m._tag === "Failure" ? m.cause.reasons.push(q) : m = ie$1(nr([q])));
              } else {
                const q = n(r, _, Be, N);
                q && (m = q._tag === "Failure" ? ie$1(nr(q.cause.reasons.slice())) : q, k());
              }
              if (J) {
                const q = k();
                q && p(q);
              } else a && f.size === 0 && p(m != null ? m : le$2);
            }), f.size < c) continue;
            J = true, i++;
            return;
          } else return Fe((x) => {
            l = en(), y = F, p = x;
            const N = k();
            return N ? x(N) : M$1(() => (m = ye$2, E = true, f ? ul(f) : le$2));
          });
        }
      }
      if (a = true, m) {
        if (f && f.size > 0) {
          const _ = Sn(l);
          f.forEach((F) => F.interruptUnsafe(l.id, _));
          return;
        }
        if (p || m._tag === "Failure") return m;
      } else if (p) if (f) f.size === 0 && p(le$2);
      else return ye$2;
    };
    return k();
  };
}, Tn = () => af, Pr = (e, t, n = false, r = false, s = false) => {
  const o = s === "inherit" ? e.interruptible : !s, i = new zo(e.context, o);
  return n ? i.evaluate(t) : e.currentDispatcher.scheduleTask(() => i.evaluate(t), 0), !r && !i._exit && (e.children().add(i), i.addObserver(() => e._children.delete(i))), i;
}, cf = d$1((e) => _r$1(e[0]), (e, t, n) => Y$1((r) => {
  const s = Pr(r, e, n == null ? void 0 : n.startImmediately, true, n == null ? void 0 : n.uninterruptible);
  if (!s._exit) if (t.state._tag !== "Closed") {
    const o = {};
    $t(t, o, () => yl((u) => u === s.id ? le$2 : Jo(s))), s.addObserver(() => Fr(t, o));
  } else s.interruptUnsafe(r.id, Sn(r));
  return w$1(s);
})), _n$1 = (e) => (t, n) => {
  const r = new zo((n == null ? void 0 : n.scheduler) ? ko(e, Fo, n.scheduler) : e, (n == null ? void 0 : n.uninterruptible) !== true);
  if (r.evaluate(t), r._exit) return r;
  if (n == null ? void 0 : n.signal) if (n.signal.aborted) r.interruptUnsafe();
  else {
    const s = () => r.interruptUnsafe();
    n.signal.addEventListener("abort", s, { once: true }), r.addObserver(() => n.signal.removeEventListener("abort", s));
  }
  return (n == null ? void 0 : n.onFiberStart) && n.onFiberStart(r), r;
}, uf = d$1(2, (e, t) => {
  if (e._exit) return e;
  if (t.state._tag === "Closed") return e.interruptUnsafe(e.id), e;
  const n = {};
  return $t(t, n, () => Jo(e)), e.addObserver(() => Fr(t, n)), e;
}), lf = _n$1(ct()), oi = (e) => {
  const t = _n$1(e);
  return (n, r) => {
    const s = t(n, r);
    return new Promise((o) => {
      s.addObserver((i) => o(i));
    });
  };
}, ff = oi(ct()), df = (e) => {
  const t = oi(e);
  return (n, r) => t(n, r).then((s) => {
    if (s._tag === "Failure") throw Po(s.cause);
    return s.value;
  });
}, pf = df(ct()), ii = (e) => {
  const t = _n$1(e);
  return (n) => {
    var _a3, _b2;
    if (ae$1(n)) return n;
    const r = new kr("sync"), s = t(n, { scheduler: r });
    return (_a3 = s.currentDispatcher) == null ? void 0 : _a3.flush(), (_b2 = s._exit) != null ? _b2 : kt(new Ef(s));
  };
}, hf = ii(ct()), mf = (e) => {
  const t = ii(e);
  return (n) => {
    const r = t(n);
    if (r._tag === "Failure") throw Po(r.cause);
    return r.value;
  };
}, gf = mf(ct()), ws = "~effect/Cause/IllegalArgumentError";
let yf = class extends (_f2 = wr("IllegalArgumentError"), _e2 = ws, _f2) {
  constructor(t) {
    super({ message: t });
    __publicField$2(this, _e2, ws);
  }
};
const Os = "~effect/Cause/AsyncFiberError";
class Ef extends (_h2 = wr("AsyncFiberError"), _g = Os, _h2) {
  constructor(t) {
    super({ message: "An asynchronous Effect was executed with Effect.runSync", fiber: t });
    __publicField$2(this, _g, Os);
  }
}
const As = "~effect/Cause/UnknownError";
let bf = class extends (_j = wr("UnknownError"), _i2 = As, _j) {
  constructor(t, n) {
    super({ message: n, cause: t });
    __publicField$2(this, _i2, As);
  }
};
const Sf = typeof process == "object" && process !== null && typeof process.stdout == "object" && process.stdout !== null;
Sf && process.stdout.isTTY;
const Ht = I$1, Lr = ie$1, xn = Ve, ai = ye$2, wn = Dr, Tf = Nl, _f = ti, xf = Ql, ks = Hl, ci = Gl, jr = ni, wf = ao, On = Mt, Of = rl, Mr = Xc, ui = Or, zt = tu, Af = yf, ar = "~effect/time/DateTime", kf = { [ar]: ar, pipe() {
  return ke$1(this, arguments);
}, [mn]() {
  return this.toString();
}, toJSON() {
  return Jr(this).toJSON();
} }, If = { ...kf, _tag: "Utc", [ee$1]() {
  return hn(this.epochMilliseconds);
}, [te$1](e) {
  return zr(e) && e._tag === "Utc" && this.epochMilliseconds === e.epochMilliseconds;
}, toString() {
  return `DateTime.Utc(${Jr(this).toJSON()})`;
} }, zr = (e) => P$1(e, ar), vf = (e) => e._tag === "Utc", Cf = ru((e, t) => e.epochMilliseconds === t.epochMilliseconds), Vr = (e) => {
  const t = Object.create(If);
  return t.epochMilliseconds = e, Object.defineProperty(t, "partsUtc", { value: void 0, enumerable: false, writable: true }), t;
}, mt = (e) => {
  const t = e.getTime();
  if (Number.isNaN(t)) throw new Af("Invalid date");
  return Vr(t);
}, $f = (e) => {
  if (zr(e)) return e;
  if (e instanceof Date) return mt(e);
  if (typeof e == "object") {
    if ("epochMilliseconds" in e) return Vr(e.epochMilliseconds);
    const t = /* @__PURE__ */ new Date(0);
    return Nf(t, e), mt(t);
  } else if (typeof e == "string" && !Df(e)) return mt(/* @__PURE__ */ new Date(e + "Z"));
  return mt(new Date(e));
}, Df = (e) => /Z|GMT|[+-]\d{2}$|[+-]\d{2}:?\d{2}$|\]$/.test(e), Uf = gu($f), Ff = (e) => Vr(e.epochMilliseconds), Jr = (e) => new Date(e.epochMilliseconds), Nf = (e, t) => {
  if (t.year !== void 0 && e.setUTCFullYear(t.year), t.month !== void 0 && e.setUTCMonth(t.month - 1), t.day !== void 0 && e.setUTCDate(t.day), t.weekDay !== void 0) {
    const n = t.weekDay - e.getUTCDay();
    e.setUTCDate(e.getUTCDate() + n);
  }
  t.hour !== void 0 && e.setUTCHours(t.hour), t.minute !== void 0 && e.setUTCMinutes(t.minute), t.second !== void 0 && e.setUTCSeconds(t.second), t.millisecond !== void 0 && e.setUTCMilliseconds(t.millisecond);
}, Rf = (e) => Jr(e).toISOString(), li = d$1(2, (e, t) => Ho(e, An, (n) => t(n))), Is = (e) => e.reasons.some(Pf), Pf = (e) => e._tag === "Fail" && Mr(e.error), Lf = Uo(On, (e) => Mr(e) ? lt(e) : we$2(e)), An = Uo(On, (e) => Mr(e) ? lt(e.value) : we$2(e)), jf = (e) => {
  const t = Lf(e);
  return X$1(t) ? Lr(t.failure) : Ht(t.success.value);
}, Dm = ml, Um = gl, $$1 = w$1, kn = Bo, Mf = dl, fi = M$1, Kr = Z$1, Vt = le$2, ne$1 = bl, j = et, Jt = Ce$2, Fm = hl, Nm = ll, K = W, zf = Ol, Vf = vr, di = Jl, tn = Ur, Kt = $r, Jf = Tl, Ye = Cr, _t$1 = qo, Kf = ei, Bf = Qo, pi = Xo, hi = Pl, gt = jl, mi = Xl, vs = nf, Hn = Ml, xt = cf, Yf = lf, Wf = _n$1, xe$2 = pf, Gf = ff, gi = gf, qf = hf, Br = Wo, Yr = Al, In = kl, We = wl, Hf = Il, cr = Sl, yi = "Data", Ei = "RecoverableError", bi = "Progress", tt = (e) => ({ _tag: yi, value: e }), Si = (e) => ({ _tag: Ei, error: e }), Ti = (e) => ({ _tag: bi, progress: e }), Bt = (e) => e._tag === yi, vn = (e) => e._tag === Ei, _i$1 = (e) => e._tag === bi, Zf = Object.freeze(Object.defineProperty({ __proto__: null, data: tt, isData: Bt, isProgress: _i$1, isRecoverableError: vn, progress: Ti, recoverableError: Si }, Symbol.toStringTag, { value: "Module" })), Qf = al, Xf = uf, ed = /* @__PURE__ */ Symbol.for("effect/MutableList/Empty"), td = () => ({ head: void 0, tail: void 0, length: 0 }), Cs = () => ({ array: [], mutable: true, offset: 0, next: void 0 }), He$2 = (e, t) => {
  e.tail ? e.tail.mutable || (e.tail.next = Cs(), e.tail = e.tail.next) : e.head = e.tail = Cs(), e.tail.array.push(t), e.length++;
}, nd = (e, t) => rd(e, Oe$1(t), !Array.isArray(t)), rd = (e, t, n = false) => {
  if (t.length === 0) return 0;
  const r = { array: t, mutable: n, offset: 0, next: void 0 };
  return e.head ? e.tail = e.tail.next = r : e.head = e.tail = r, e.length += t.length, t.length;
}, wt = (e) => {
  e.head = e.tail = void 0, e.length = 0;
}, xi = (e, t) => {
  var _a3;
  if (t <= 0 || !e.head) return [];
  if (t = Math.min(t, e.length), t === e.length && ((_a3 = e.head) == null ? void 0 : _a3.offset) === 0 && !e.head.next) {
    const o = e.head.array;
    return wt(e), o;
  }
  const n = new Array(t);
  let r = 0, s = e.head;
  for (; s; ) {
    for (; s.offset < s.array.length; ) if (n[r++] = s.array[s.offset], s.mutable && (s.array[s.offset] = void 0), s.offset++, r === t) return e.head = s, e.length -= t, e.length === 0 && wt(e), n;
    s = s.next;
  }
  return wt(e), n;
}, nn = (e) => {
  if (!e.head) return ed;
  const t = e.head.array[e.head.offset];
  return e.head.mutable && (e.head.array[e.head.offset] = void 0), e.head.offset++, e.length--, e.head.offset === e.head.array.length && (e.head.next ? e.head = e.head.next : wt(e)), t;
}, sd = "~effect/Queue", od = "~effect/Queue/Enqueue", id = "~effect/Queue/Dequeue", Zn = { _A: B, _E: B }, ad = { [sd]: Zn, [od]: Zn, [id]: Zn, ...Pt, toJSON() {
  return { _id: "effect/Queue", state: this.state._tag, size: dd(this) };
} }, cd = (e) => Y$1((t) => {
  var _a3, _b2;
  const n = Object.create(ad);
  return n.dispatcher = t.currentDispatcher, n.capacity = (_a3 = e == null ? void 0 : e.capacity) != null ? _a3 : Number.POSITIVE_INFINITY, n.strategy = (_b2 = e == null ? void 0 : e.strategy) != null ? _b2 : "suspend", n.messages = td(), n.scheduleRunning = false, n.state = { _tag: "Open", takers: /* @__PURE__ */ new Set(), offers: /* @__PURE__ */ new Set(), awaiters: /* @__PURE__ */ new Set() }, w$1(n);
}), rn = (e) => cd({ capacity: e }), _e$3 = (e, t) => M$1(() => {
  if (e.state._tag !== "Open") return on;
  if (e.messages.length >= e.capacity) switch (e.strategy) {
    case "dropping":
      return on;
    case "suspend":
      return e.capacity <= 0 && e.state.takers.size > 0 ? (He$2(e.messages, t), ki(e), Zt) : gd(e, t);
    case "sliding":
      return nn(e.messages), He$2(e.messages, t), Zt;
  }
  return He$2(e.messages, t), lr(e), Zt;
}), ud = (e, t) => M$1(() => {
  if (e.state._tag !== "Open") return w$1(Oe$1(t));
  const n = ld(e, t);
  return n.length === 0 ? I$1([]) : e.strategy === "dropping" ? w$1(n) : yd(e, n);
}), ld = (e, t) => {
  if (e.state._tag !== "Open") return Oe$1(t);
  if (e.capacity === Number.POSITIVE_INFINITY || e.strategy === "sliding") return nd(e.messages, t), e.strategy === "sliding" && xi(e.messages, e.messages.length - e.capacity), lr(e), [];
  const n = e.capacity <= 0 ? e.state.takers.size : e.capacity - e.messages.length;
  if (n === 0) return Oe$1(t);
  const r = [];
  let s = 0;
  for (const o of t) s < n ? He$2(e.messages, o) : r.push(o), s++;
  return lr(e), r;
}, sn = (e, t) => yt(e, io(t)), yt = d$1(2, (e, t) => Z$1(() => wi(e, t))), wi = (e, t) => {
  if (e.state._tag !== "Open") return false;
  const n = ie$1(t), r = Ul(n, pd);
  return e.state.offers.size === 0 && e.messages.length === 0 ? (Wr(e, r), true) : (e.state = { ...e.state, _tag: "Closing", exit: r }, true);
}, $s = (e) => Z$1(() => {
  if (e.state._tag === "Done") return true;
  wt(e.messages);
  const t = e.state.offers;
  if (Wr(e, e.state._tag === "Open" ? hd : e.state.exit), t.size > 0) {
    for (const n of t) n._tag === "Single" ? n.resume(on) : n.resume(I$1(n.remaining.slice(n.offset)));
    t.clear();
  }
  return true;
}), Oi = (e) => Ai(e, 1, Number.POSITIVE_INFINITY), Ai = (e, t, n) => M$1(() => {
  var _a3;
  return (_a3 = md(e, t, n)) != null ? _a3 : vr(Ii(e), Ai(e, 1, n));
}), ur = (e) => M$1(() => {
  var _a3;
  return (_a3 = fd(e)) != null ? _a3 : vr(Ii(e), ur(e));
}), fd = (e) => {
  if (e.state._tag === "Done") return e.state.exit;
  if (e.messages.length > 0) {
    const t = nn(e.messages);
    return Ze$1(e), I$1(t);
  } else if (e.capacity <= 0 && e.state.offers.size > 0) {
    e.capacity = 1, Ze$1(e), e.capacity = 0;
    const t = nn(e.messages);
    return Ze$1(e), I$1(t);
  }
}, dd = (e) => e.state._tag === "Done" ? 0 : e.messages.length, on = I$1(false), Zt = I$1(true), pd = Ve(Or()), hd = vl(), ki = (e) => {
  if (e.scheduleRunning = false, !(e.state._tag === "Done" || e.state.takers.size === 0)) {
    for (const t of e.state.takers) if (e.state.takers.delete(t), t(ye$2), e.messages.length === 0) break;
  }
}, lr = (e) => {
  e.scheduleRunning || e.state._tag === "Done" || e.state.takers.size === 0 || (e.scheduleRunning = true, e.dispatcher.scheduleTask(() => ki(e), 0));
}, md = (e, t, n) => {
  if (e.state._tag === "Done") return e.state.exit;
  if (n <= 0 || t <= 0) return I$1([]);
  if (e.capacity <= 0 && e.state.offers.size > 0) {
    e.capacity = 1, Ze$1(e), e.capacity = 0;
    const r = [nn(e.messages)];
    return Ze$1(e), I$1(r);
  }
  if (t = Math.min(t, e.capacity || 1), t <= e.messages.length) {
    const r = xi(e.messages, n);
    return Ze$1(e), I$1(r);
  }
}, gd = (e, t) => Fe((n) => {
  if (e.state._tag !== "Open") return n(on);
  const r = { _tag: "Single", message: t, resume: n };
  return e.state.offers.add(r), Z$1(() => {
    e.state._tag === "Open" && e.state.offers.delete(r);
  });
}), yd = (e, t) => Fe((n) => {
  if (e.state._tag !== "Open") return n(I$1(t));
  const r = { _tag: "Array", remaining: t, offset: 0, resume: n };
  return e.state.offers.add(r), Z$1(() => {
    e.state._tag === "Open" && e.state.offers.delete(r);
  });
}), Ze$1 = (e) => {
  if (e.state._tag === "Done") return Is(e.state.exit.cause);
  if (e.state.offers.size === 0) return e.state._tag === "Closing" && e.messages.length === 0 ? (Wr(e, e.state.exit), Is(e.state.exit.cause)) : false;
  let t = e.capacity - e.messages.length;
  for (const n of e.state.offers) {
    if (t === 0) break;
    if (n._tag === "Single") He$2(e.messages, n.message), t--, n.resume(Zt), e.state.offers.delete(n);
    else {
      for (; n.offset < n.remaining.length; n.offset++) {
        if (t === 0) return false;
        He$2(e.messages, n.remaining[n.offset]), t--;
      }
      n.resume(I$1([])), e.state.offers.delete(n);
    }
  }
  return false;
}, Ii = (e) => Fe((t) => e.state._tag === "Done" ? t(e.state.exit) : (e.state.takers.add(t), Z$1(() => {
  e.state._tag !== "Done" && e.state.takers.delete(t);
}))), Wr = (e, t) => {
  if (e.state._tag === "Done") return;
  const n = e.state;
  e.state = { _tag: "Done", exit: t };
  for (const r of n.takers) r(t);
  n.takers.clear();
  for (const r of n.awaiters) r(t);
  n.awaiters.clear();
}, vi = (e) => new Ed(e);
class Ed {
  constructor(t) {
    __publicField$2(this, "waiters", /* @__PURE__ */ new Set());
    __publicField$2(this, "taken", 0);
    __publicField$2(this, "permits");
    __publicField$2(this, "withPermit", this.withPermits(1));
    this.permits = t;
  }
  get free() {
    return this.permits - this.taken;
  }
  take(t) {
    const n = M$1(() => this.free < t ? Fe((r) => {
      if (this.free >= t) return r(n);
      const s = () => {
        this.free < t || (this.waiters.delete(s), r(n));
      };
      return this.waiters.add(s), Z$1(() => {
        this.waiters.delete(s);
      });
    }) : (this.taken += t, w$1(t)));
    return n;
  }
  updateTakenUnsafe(t, n) {
    return this.taken = n(this.taken), this.waiters.size > 0 && t.currentDispatcher.scheduleTask(() => {
      const r = this.waiters.values();
      let s = r.next();
      for (; s.done === false && this.free > 0; ) s.value(), s = r.next();
    }, 0), this.free;
  }
  updateTaken(t) {
    return Y$1((n) => w$1(this.updateTakenUnsafe(n, t)));
  }
  resize(t) {
    return Y$1((n) => (this.permits = t, this.free < 0 || this.updateTakenUnsafe(n, (r) => r), le$2));
  }
  release(t) {
    return this.updateTaken((n) => n - t);
  }
  get releaseAll() {
    return this.updateTaken((t) => 0);
  }
  withPermits(t) {
    return (n) => xs((r) => W(r(this.take(t)), (s) => Dt(r(n), () => {
      this.updateTakenUnsafe(en(), (o) => o - s);
    }, true)));
  }
  withPermitsIfAvailable(t) {
    return (n) => xs((r) => this.free < t ? Bo : (this.taken += t, Dt(r(_l$1(n)), () => {
      this.updateTakenUnsafe(en(), (s) => s - t);
    }, true)));
  }
}
const Ci = "~effect/Channel", $i = (e) => P$1(e, Ci), bd = { [Ci]: { _Env: B, _InErr: B, _InElem: B, _OutErr: B, _OutElem: B }, pipe() {
  return ke$1(this, arguments);
} }, Je = (e) => {
  const t = Object.create(bd);
  return t.transform = (n, r) => _t$1(e(n, r), (s) => $$1(Jt(s))), t;
}, Gr = (e, t) => Je((n, r) => K(je(e)(n, r), (s) => t(s, r))), qr = (e) => Je((t, n) => e), Sd = (e) => Je(Br(function* (t, n) {
  const r = ci(n), s = (i) => jr(r, jf(i)), o = yield* vs(e(t, n, r), s);
  return vs(o, s);
})), je = (e) => e.transform, Di = (e) => Td($$1(e)), Cn = (e) => qr($$1(zt(e))), Ui = (e) => qr($$1(j(e))), Td = (e) => qr(Kr(() => {
  let t = false;
  return fi(() => t ? zt() : (t = true, e));
})), Fi = d$1(2, (e, t) => Gr(e, (n) => Kr(() => {
  let r = 0;
  return Kt(n, (s) => t(s, r++));
}))), Ni = d$1(2, (e, t) => _d(e, (n) => $$1(t(n)))), _d = d$1(2, (e, t) => Gr(e, (n) => $$1(li(n, (r) => K(t(r), zt))))), xd = (e) => e === void 0 || e !== "unbounded" && e <= 1, wd = d$1((e) => $i(e[0]), (e, t, n) => xd(n == null ? void 0 : n.concurrency) ? Od(e, t) : Ad(e, t, n)), Od = (e, t) => Je((n, r) => {
  let s = 0;
  return Kt(je(e)(n, r), K((o) => t(o, s++)));
}), Ad = (e, t, n) => Sd(Br(function* (r, s, o) {
  let i = 0;
  const u = yield* je(e)(r, s), c = n.concurrency === "unbounded" ? Number.MAX_SAFE_INTEGER : n.concurrency, a = yield* rn(0);
  yield* ks(o, $s(a));
  const l = Wf(yield* hi()), f = Xf(o);
  if (n.unordered) {
    const p = vi(c), E = Nt(p.release(1)), m = Bf({ onFailure: (y) => K(yt(a, y), E), onSuccess: (y) => K(_e$3(a, y), E) });
    yield* p.take(1).pipe(K(() => u), K((y) => (f(l(m(t(y, i++)))), Vt)), Hn({ disableYield: true }), _t$1((y) => p.withPermits(c - 1)(yt(a, y))), xt(o));
  } else {
    const p = yield* rn(c - 2);
    yield* ks(o, $s(a)), yield* ur(p).pipe(zf, K((y) => _e$3(a, y)), Hn({ disableYield: true }), _t$1((y) => yt(a, y)), xt(o));
    let E;
    const m = (y) => {
      y._tag !== "Success" && (E = y.cause, wi(a, y.cause));
    };
    yield* u.pipe(K((y) => {
      if (E) return Jt(E);
      const k = l(t(y, i++));
      return f(k), k.addObserver(m), _e$3(p, Qf(k));
    }), Hn({ disableYield: true }), _t$1((y) => _e$3(p, Lr(y)).pipe(Vf(yt(p, y)))), xt(o));
  }
  return ur(a);
})), $n = d$1((e) => $i(e[0]), (e, t, n) => wd(e, (r) => Jf(t(r), r), n)), kd = d$1(2, (e, t) => Je((n, r) => Kr(() => {
  let s;
  const o = ci(r), i = K(je(e)(n, o), (u) => (s = li(u, (c) => jr(o, ai).pipe(K(() => je(t(c))(n, r)), K((a) => (s = a, a)))), s));
  return fi(() => s != null ? s : i);
}))), Ri = d$1(2, (e, t) => kd(e, (n) => t)), Id = d$1(2, (e, t) => Gr(e, (n) => $$1(K(n, function r(s) {
  const o = [];
  for (let i = 0; i < s.length; i++) t(s[i]) && o.push(s[i]);
  return vt(o) ? $$1(o) : K(n, r);
})))), Pi = Br(function* (e) {
  const t = vi(1), n = yield* hi(), r = qt(n, _f);
  return (yield* je(e)(zt(), r)).pipe(gt(n), t.withPermits(1));
}, _t$1((e) => $$1(Jt(e)))), vd = (e, t) => je(e)(zt(), t);
let b$1 = class b extends Error {
  constructor(t, n) {
    super(t, n);
    __publicField$2(this, "jsonApiSource");
    __publicField$2(this, "translation");
    __publicField$2(this, "status");
    __publicField$2(this, "code");
    __publicField$2(this, "title");
    this.status = new.target.STATUS, this.jsonApiSource = n == null ? void 0 : n.jsonApiSource, this.translation = n == null ? void 0 : n.translation, this.code = new.target.CODE, this.title = new.target.TITLE, Object.setPrototypeOf(this, new.target.prototype);
  }
  toResult() {
    return we$2(this);
  }
};
__publicField$2(b$1, "TITLE");
__publicField$2(b$1, "STATUS");
__publicField$2(b$1, "CODE");
const S$1 = { NOT_IMPLEMENTED: "not_implemented", ILLEGAL_STATE: "illegal_state", NOT_FOUND: "not_found", BAD_REQUEST: "bad_request", FORBIDDEN: "forbidden", AUTHENTICATION_ERROR: "unauthenticated", AUTHORIZATION_ERROR: "unauthorized", DIR_INSTEAD_OF_FILE: "dir_instead_of_file", INVALID_DATA: "invalid_data", INTERNAL_ERROR: "internal_error", FILE_INSTEAD_OF_DIR: "file_instead_of_dir", VERSIONING_MISMATCH: "version_mismatch", VALIDATION_ERROR: "validation_error", ENTRY_ALREADY_EXISTS: "entry_already_exists", CONFLICT: "conflict", TOO_MANY_REQUESTS: "too_many_requests", SERVICE_UNAVAILABLE: "service_unavailable", GATEWAY_TIMEOUT: "gateway_timeout", UNKNOWN_ERROR: "unknown_error", AUTHORIZER_FAILURE: "authorizer_failure", UNSUPPORTED_FILE_TYPE: "unsupported_file_type", DANGEROUS_FILE_TYPE: "dangerous_file_type", CORRUPTED_FILE: "corrupted_file", EMBEDDED_CONTENT: "embedded_content", FILE_TOO_LARGE: "file_too_large" }, T$1 = { NOT_IMPLEMENTED: 501, ILLEGAL_STATE: 500, NOT_FOUND: 404, BAD_REQUEST: 400, FORBIDDEN: 403, AUTHORIZATION_ERROR: 401, AUTHENTICATION_ERROR: 401, DIR_INSTEAD_OF_FILE: 403, FILE_INSTEAD_OF_DIR: 403, INVALID_DATA: 400, INTERNAL_ERROR: 500, VERSIONING_MISMATCH: 409, VALIDATION_ERROR: 400, ENTRY_ALREADY_EXISTS: 409, CONFLICT: 409, TOO_MANY_REQUESTS: 429, SERVICE_UNAVAILABLE: 503, GATEWAY_TIMEOUT: 504, UNKNOWN_ERROR: 500, AUTHORIZER_FAILURE: 500, UNSUPPORTED_FILE_TYPE: 415, DANGEROUS_FILE_TYPE: 415, CORRUPTED_FILE: 422, EMBEDDED_CONTENT: 422, FILE_TOO_LARGE: 413 };
class Li extends b$1 {
}
__publicField$2(Li, "TITLE", "Not Implemented");
__publicField$2(Li, "CODE", S$1.NOT_IMPLEMENTED);
__publicField$2(Li, "STATUS", T$1.NOT_IMPLEMENTED);
class ji extends b$1 {
}
__publicField$2(ji, "TITLE", "Illegal State");
__publicField$2(ji, "CODE", S$1.ILLEGAL_STATE);
__publicField$2(ji, "STATUS", T$1.ILLEGAL_STATE);
class Mi extends b$1 {
}
__publicField$2(Mi, "TITLE", "Not Found");
__publicField$2(Mi, "CODE", S$1.NOT_FOUND);
__publicField$2(Mi, "STATUS", T$1.NOT_FOUND);
class zi extends b$1 {
}
__publicField$2(zi, "TITLE", "Bad Request");
__publicField$2(zi, "CODE", S$1.BAD_REQUEST);
__publicField$2(zi, "STATUS", T$1.BAD_REQUEST);
class Vi extends b$1 {
}
__publicField$2(Vi, "TITLE", "Forbidden");
__publicField$2(Vi, "CODE", S$1.FORBIDDEN);
__publicField$2(Vi, "STATUS", T$1.FORBIDDEN);
class Ji extends b$1 {
}
__publicField$2(Ji, "TITLE", "Directory Instead of File");
__publicField$2(Ji, "CODE", S$1.DIR_INSTEAD_OF_FILE);
__publicField$2(Ji, "STATUS", T$1.DIR_INSTEAD_OF_FILE);
class Ki extends b$1 {
}
__publicField$2(Ki, "TITLE", "File Instead of Directory");
__publicField$2(Ki, "CODE", S$1.FILE_INSTEAD_OF_DIR);
__publicField$2(Ki, "STATUS", T$1.FILE_INSTEAD_OF_DIR);
class Bi extends b$1 {
}
__publicField$2(Bi, "TITLE", "Invalid Data");
__publicField$2(Bi, "CODE", S$1.INVALID_DATA);
__publicField$2(Bi, "STATUS", T$1.INVALID_DATA);
class Ge extends b$1 {
}
__publicField$2(Ge, "TITLE", "Internal Error");
__publicField$2(Ge, "CODE", S$1.INTERNAL_ERROR);
__publicField$2(Ge, "STATUS", T$1.INTERNAL_ERROR);
class Yi extends b$1 {
}
__publicField$2(Yi, "TITLE", "Version Mismatch");
__publicField$2(Yi, "CODE", S$1.VERSIONING_MISMATCH);
__publicField$2(Yi, "STATUS", T$1.VERSIONING_MISMATCH);
class Hr extends b$1 {
}
__publicField$2(Hr, "TITLE", "Validation Error");
__publicField$2(Hr, "CODE", S$1.VALIDATION_ERROR);
__publicField$2(Hr, "STATUS", T$1.VALIDATION_ERROR);
class Wi extends b$1 {
}
__publicField$2(Wi, "TITLE", "Entry Already Exists");
__publicField$2(Wi, "CODE", S$1.ENTRY_ALREADY_EXISTS);
__publicField$2(Wi, "STATUS", T$1.ENTRY_ALREADY_EXISTS);
class Gi extends b$1 {
}
__publicField$2(Gi, "TITLE", "Authorization Error");
__publicField$2(Gi, "CODE", S$1.AUTHORIZATION_ERROR);
__publicField$2(Gi, "STATUS", T$1.AUTHORIZATION_ERROR);
class qi extends b$1 {
}
__publicField$2(qi, "TITLE", "Authentication Error");
__publicField$2(qi, "CODE", S$1.AUTHENTICATION_ERROR);
__publicField$2(qi, "STATUS", T$1.AUTHENTICATION_ERROR);
class Hi extends b$1 {
}
__publicField$2(Hi, "TITLE", "Conflict");
__publicField$2(Hi, "CODE", S$1.CONFLICT);
__publicField$2(Hi, "STATUS", T$1.CONFLICT);
class Zi extends b$1 {
}
__publicField$2(Zi, "TITLE", "Too Many Requests");
__publicField$2(Zi, "CODE", S$1.TOO_MANY_REQUESTS);
__publicField$2(Zi, "STATUS", T$1.TOO_MANY_REQUESTS);
let qe$2 = class qe extends b$1 {
};
__publicField$2(qe$2, "TITLE", "Service Unavailable");
__publicField$2(qe$2, "CODE", S$1.SERVICE_UNAVAILABLE);
__publicField$2(qe$2, "STATUS", T$1.SERVICE_UNAVAILABLE);
class Qi extends b$1 {
}
__publicField$2(Qi, "TITLE", "Gateway Timeout");
__publicField$2(Qi, "CODE", S$1.GATEWAY_TIMEOUT);
__publicField$2(Qi, "STATUS", T$1.GATEWAY_TIMEOUT);
class nt extends b$1 {
}
__publicField$2(nt, "TITLE", "Unknown Error");
__publicField$2(nt, "CODE", S$1.UNKNOWN_ERROR);
__publicField$2(nt, "STATUS", T$1.UNKNOWN_ERROR);
class Xi extends b$1 {
}
__publicField$2(Xi, "TITLE", "Authorizer Failure");
__publicField$2(Xi, "CODE", S$1.AUTHORIZER_FAILURE);
__publicField$2(Xi, "STATUS", T$1.INTERNAL_ERROR);
class ea extends b$1 {
}
__publicField$2(ea, "TITLE", "Unsupported File Type");
__publicField$2(ea, "CODE", S$1.UNSUPPORTED_FILE_TYPE);
__publicField$2(ea, "STATUS", T$1.UNSUPPORTED_FILE_TYPE);
class ta extends b$1 {
}
__publicField$2(ta, "TITLE", "Dangerous File Type");
__publicField$2(ta, "CODE", S$1.DANGEROUS_FILE_TYPE);
__publicField$2(ta, "STATUS", T$1.DANGEROUS_FILE_TYPE);
class na extends b$1 {
}
__publicField$2(na, "TITLE", "Corrupted File");
__publicField$2(na, "CODE", S$1.CORRUPTED_FILE);
__publicField$2(na, "STATUS", T$1.CORRUPTED_FILE);
class ra extends b$1 {
}
__publicField$2(ra, "TITLE", "Embedded Content");
__publicField$2(ra, "CODE", S$1.EMBEDDED_CONTENT);
__publicField$2(ra, "STATUS", T$1.EMBEDDED_CONTENT);
class sa extends b$1 {
}
__publicField$2(sa, "TITLE", "File Too Large");
__publicField$2(sa, "CODE", S$1.FILE_TOO_LARGE);
__publicField$2(sa, "STATUS", T$1.FILE_TOO_LARGE);
const Cd = Object.freeze(Object.defineProperty({ __proto__: null, AuthenticationError: qi, AuthorizationError: Gi, AuthorizerFailureError: Xi, BadRequestError: zi, ConflictError: Hi, CorruptedFileError: na, DangerousFileTypeError: ta, DirInsteadOfFile: Ji, EmbeddedContentError: ra, EntryAlreadyExistsError: Wi, FileInsteadOfDir: Ki, FileTooLargeError: sa, ForbiddenError: Vi, GatewayTimeoutError: Qi, IllegalStateException: ji, InternalError: Ge, InvalidData: Bi, LaikaError: b$1, NotFoundError: Mi, NotImplementedError: Li, ServiceUnavailableError: qe$2, TooManyRequestsError: Zi, UnknownError: nt, UnsupportedFileTypeError: ea, ValidationError: Hr, VersionMismatchError: Yi, errorCode: S$1, errorStatus: T$1 }, Symbol.toStringTag, { value: "Module" })), $d = Object.fromEntries(Object.entries(S$1).map(([e, t]) => [t, e])), Dd = Object.fromEntries(Object.entries(S$1).map(([e, t]) => [t, T$1[e]])), Ud = { ...Cd, LaikaError: void 0 }, oa = Object.fromEntries(Object.entries(Ud).filter(([e, t]) => e !== "LaikaError" && t && "CODE" in t)), Fd = Object.fromEntries(Object.entries(oa).map(([e, t]) => [t.CODE, t])), Nd = Fd, Rd = zr, Pd = vf, Ld = Cf, jd = mt, Md = Uf, zd = Ff, Vd = Rf;
function ia(e) {
  return e.checks ? e.checks[e.checks.length - 1].annotations : e.annotations;
}
function aa(e) {
  return (t) => {
    var _a3;
    return (_a3 = ia(t)) == null ? void 0 : _a3[e];
  };
}
const Jd = aa("identifier"), Kd = aa("brands"), Zr = De$1((e) => {
  const t = Jd(e);
  return typeof t == "string" ? t : e.getExpected(Zr);
});
function Et(e, t, n) {
  return t === "__proto__" ? Object.defineProperty(e, t, { value: n, writable: true, enumerable: true, configurable: true }) : e[t] = n, e;
}
const Bd = (e) => e.replace(/[/\\^$*+?.()|[\]{}]/g, "\\$&"), fr = "~effect/SchemaIssue/Issue";
function Yd(e) {
  return P$1(e, fr);
}
let Se$1 = (_k = fr, class {
  constructor() {
    __publicField$2(this, _k, fr);
  }
  toString() {
    return ep(this);
  }
}), Wd = class extends Se$1 {
  constructor(t, n, r) {
    super();
    __publicField$2(this, "_tag", "Filter");
    __publicField$2(this, "actual");
    __publicField$2(this, "filter");
    __publicField$2(this, "issue");
    this.actual = t, this.filter = n, this.issue = r;
  }
};
class Gd extends Se$1 {
  constructor(t, n, r) {
    super();
    __publicField$2(this, "_tag", "Encoding");
    __publicField$2(this, "ast");
    __publicField$2(this, "actual");
    __publicField$2(this, "issue");
    this.ast = t, this.actual = n, this.issue = r;
  }
}
class ft extends Se$1 {
  constructor(t, n) {
    super();
    __publicField$2(this, "_tag", "Pointer");
    __publicField$2(this, "path");
    __publicField$2(this, "issue");
    this.path = t, this.issue = n;
  }
}
class ca extends Se$1 {
  constructor(t) {
    super();
    __publicField$2(this, "_tag", "MissingKey");
    __publicField$2(this, "annotations");
    this.annotations = t;
  }
}
class ua extends Se$1 {
  constructor(t, n) {
    super();
    __publicField$2(this, "_tag", "UnexpectedKey");
    __publicField$2(this, "ast");
    __publicField$2(this, "actual");
    this.ast = t, this.actual = n;
  }
}
let fe$2 = class fe extends Se$1 {
  constructor(t, n, r) {
    super();
    __publicField$2(this, "_tag", "Composite");
    __publicField$2(this, "ast");
    __publicField$2(this, "actual");
    __publicField$2(this, "issues");
    this.ast = t, this.actual = n, this.issues = r;
  }
};
class Yt extends Se$1 {
  constructor(t, n) {
    super();
    __publicField$2(this, "_tag", "InvalidType");
    __publicField$2(this, "ast");
    __publicField$2(this, "actual");
    this.ast = t, this.actual = n;
  }
}
class Ut extends Se$1 {
  constructor(t, n) {
    super();
    __publicField$2(this, "_tag", "InvalidValue");
    __publicField$2(this, "actual");
    __publicField$2(this, "annotations");
    this.actual = t, this.annotations = n;
  }
}
class Ds extends Se$1 {
  constructor(t, n, r) {
    super();
    __publicField$2(this, "_tag", "AnyOf");
    __publicField$2(this, "ast");
    __publicField$2(this, "actual");
    __publicField$2(this, "issues");
    this.ast = t, this.actual = n, this.issues = r;
  }
}
class qd extends Se$1 {
  constructor(t, n, r) {
    super();
    __publicField$2(this, "_tag", "OneOf");
    __publicField$2(this, "ast");
    __publicField$2(this, "actual");
    __publicField$2(this, "successes");
    this.ast = t, this.actual = n, this.successes = r;
  }
}
function dr(e, t) {
  if (Yd(t)) return t;
  if (typeof t == "string") return new Ut(C$2(e), { message: t });
  const n = typeof t.issue == "string" ? new Ut(C$2(e), { message: t.issue }) : t.issue;
  return new ft(t.path, n);
}
function Hd(e, t) {
  if (t !== void 0) return typeof t == "boolean" ? t ? void 0 : new Ut(C$2(e)) : dr(e, t);
}
function Zd(e, t, n) {
  return Array.isArray(n) ? vt(n) ? n.length === 1 ? dr(e, n[0]) : new fe$2(t, C$2(e), Nu(n, (r) => dr(e, r))) : void 0 : Hd(e, n);
}
const la = (e) => {
  const t = Ft(e);
  if (t !== void 0) return t;
  switch (e._tag) {
    case "InvalidType":
      return pr(Zr(e.ast), Us(e.actual));
    case "InvalidValue":
      return `Invalid data ${Us(e.actual)}`;
    case "MissingKey":
      return "Missing key";
    case "UnexpectedKey":
      return `Unexpected key with value ${L$1(e.actual)}`;
    case "Forbidden":
      return "Forbidden operation";
    case "OneOf":
      return `Expected exactly one member to match the input ${L$1(e.actual)}`;
  }
}, fa = (e) => {
  var _a3;
  return (_a3 = Ft(e.issue)) != null ? _a3 : Ft(e);
};
function Qd(e) {
  return (t) => ({ issues: Re$1(t, [], la, fa) });
}
function pr(e, t) {
  return `Expected ${e}, got ${t}`;
}
function Re$1(e, t, n, r) {
  switch (e._tag) {
    case "Filter": {
      const s = r(e);
      if (s !== void 0) return [{ path: t, message: s }];
      switch (e.issue._tag) {
        case "InvalidValue":
          return [{ path: t, message: pr(da(e.filter), L$1(e.actual)) }];
        default:
          return Re$1(e.issue, t, n, r);
      }
    }
    case "Encoding":
      return Re$1(e.issue, t, n, r);
    case "Pointer":
      return Re$1(e.issue, [...t, ...e.path], n, r);
    case "Composite":
      return e.issues.flatMap((s) => Re$1(s, t, n, r));
    case "AnyOf": {
      const s = Ft(e);
      if (e.issues.length === 0) {
        if (s !== void 0) return [{ path: t, message: s }];
        const o = pr(Zr(e.ast), L$1(e.actual));
        return [{ path: t, message: o }];
      }
      return e.issues.flatMap((o) => Re$1(o, t, n, r));
    }
    default:
      return [{ path: t, message: n(e) }];
  }
}
function da(e) {
  var _a3;
  const t = (_a3 = e.annotations) == null ? void 0 : _a3.expected;
  if (typeof t == "string") return t;
  switch (e._tag) {
    case "Filter":
      return "<filter>";
    case "FilterGroup":
      return e.checks.map((n) => da(n)).join(" & ");
  }
}
function Xd() {
  return (e) => Re$1(e, [], la, fa).map(tp).join(`
`);
}
const ep = Xd();
function tp(e) {
  let t = e.message;
  if (e.path && e.path.length > 0) {
    const n = Lc(e.path);
    t += `
  at ${n}`;
  }
  return t;
}
function Ft(e) {
  switch (e._tag) {
    case "InvalidType":
    case "OneOf":
    case "Composite":
    case "AnyOf":
      return ht(e.ast.annotations);
    case "InvalidValue":
    case "Forbidden":
      return ht(e.annotations);
    case "MissingKey":
      return ht(e.annotations, "messageMissingKey");
    case "UnexpectedKey":
      return ht(e.ast.annotations, "messageUnexpectedKey");
    case "Filter":
      return ht(e.filter.annotations);
    case "Encoding":
      return Ft(e.issue);
  }
}
function ht(e, t = "message") {
  const n = e == null ? void 0 : e[t];
  if (typeof n == "string") return n;
}
function Us(e) {
  return Lt(e) ? "no value provided" : L$1(e.value);
}
class rt extends Er {
  constructor(t) {
    super();
    __publicField$2(this, "run");
    this.run = t;
  }
  map(t) {
    return new rt((n, r) => this.run(n, r).pipe(Yr(xo(t))));
  }
  compose(t) {
    return Fs(this) ? t : Fs(t) ? this : new rt((n, r) => this.run(n, r).pipe(We((s) => t.run(s, r))));
  }
}
const pa = new rt($$1);
function Fs(e) {
  return e.run === pa.run;
}
function Ns() {
  return pa;
}
function np(e) {
  return new rt((t, n) => Lt(t) ? kn : e(t.value, n));
}
function Ae$2(e) {
  return rp(xo(e));
}
function Rs(e) {
  return np((t, n) => e(t, n).pipe(Yr(C$2)));
}
function rp(e) {
  return new rt((t) => $$1(e(t)));
}
function sp() {
  return Ae$2(globalThis.String);
}
function ha() {
  return Ae$2(globalThis.Number);
}
const hr = "~effect/SchemaTransformation/Transformation";
_l2 = hr;
const _Ee = class _Ee {
  constructor(t, n) {
    __publicField$2(this, _l2, hr);
    __publicField$2(this, "_tag", "Transformation");
    __publicField$2(this, "decode");
    __publicField$2(this, "encode");
    this.decode = t, this.encode = n;
  }
  flip() {
    return new _Ee(this.encode, this.decode);
  }
  compose(t) {
    return new _Ee(this.decode.compose(t.decode), t.encode.compose(this.encode));
  }
};
let Ee$1 = _Ee;
function op(e) {
  return P$1(e, hr);
}
const ma = (e) => op(e) ? e : new Ee$1(e.decode, e.encode);
function ip(e) {
  return new Ee$1(Rs(e.decode), Rs(e.encode));
}
const ap = new Ee$1(Ns(), Ns());
function cp() {
  return ap;
}
const Qr = new Ee$1(ha(), sp()), ga = ip({ decode: (e) => mu(Md(e), { onNone: () => j(new Ut(C$2(e), { message: `Invalid UTC DateTime string: ${e}` })), onSome: (t) => $$1(zd(t)) }), encode: (e) => $$1(Vd(e)) });
function Ke(e) {
  return (t) => t._tag === e;
}
const up = Ke("Declaration"), lp = Ke("Never"), an = Ke("Literal"), fp = Ke("UniqueSymbol"), dp = Ke("Arrays"), pp = Ke("Objects"), hp = Ke("Union");
let pe$2 = class pe {
  constructor(t, n) {
    __publicField$2(this, "to");
    __publicField$2(this, "transformation");
    this.to = t, this.transformation = n;
  }
};
const mp = {};
class cn {
  constructor(t, n, r = void 0, s = void 0) {
    __publicField$2(this, "isOptional");
    __publicField$2(this, "isMutable");
    __publicField$2(this, "defaultValue");
    __publicField$2(this, "annotations");
    this.isOptional = t, this.isMutable = n, this.defaultValue = r, this.annotations = s;
  }
}
const Ps = "~effect/Schema";
_m2 = Ps;
let ce$2 = class ce {
  constructor(t = void 0, n = void 0, r = void 0, s = void 0) {
    __publicField$2(this, _m2, Ps);
    __publicField$2(this, "annotations");
    __publicField$2(this, "checks");
    __publicField$2(this, "encoding");
    __publicField$2(this, "context");
    this.annotations = t, this.checks = n, this.encoding = r, this.context = s;
  }
  toString() {
    return `<${this._tag}>`;
  }
};
class Xr extends ce$2 {
  constructor(t, n, r, s, o, i) {
    super(r, s, o, i);
    __publicField$2(this, "_tag", "Declaration");
    __publicField$2(this, "typeParameters");
    __publicField$2(this, "run");
    this.typeParameters = t, this.run = n;
  }
  getParser() {
    const t = this.run(this.typeParameters);
    return (n, r) => Lt(n) ? kn : Yr(t(n.value, this, r), C$2);
  }
  recur(t) {
    const n = st(this.typeParameters, t);
    return n === this.typeParameters ? this : new Xr(n, this.run, this.annotations, this.checks, void 0, this.context);
  }
  getExpected() {
    var _a3;
    const t = (_a3 = this.annotations) == null ? void 0 : _a3.expected;
    return typeof t == "string" ? t : "<Declaration>";
  }
}
class gp extends ce$2 {
  constructor() {
    super(...arguments);
    __publicField$2(this, "_tag", "Null");
  }
  getParser() {
    return ss(this, null);
  }
  getExpected() {
    return "null";
  }
}
const yp = new gp();
let Ep = class extends ce$2 {
  constructor() {
    super(...arguments);
    __publicField$2(this, "_tag", "Undefined");
  }
  getParser() {
    return ss(this, void 0);
  }
  toCodecJson() {
    return H(this, [bp]);
  }
  getExpected() {
    return "undefined";
  }
};
const bp = new pe$2(yp, new Ee$1(Ae$2(() => {
}), Ae$2(() => null))), Sp = new Ep();
let Tp = class extends ce$2 {
  constructor() {
    super(...arguments);
    __publicField$2(this, "_tag", "Any");
  }
  getParser() {
    return dt(this, Qs);
  }
  getExpected() {
    return "any";
  }
};
const _p = new Tp();
let xp = class extends ce$2 {
  constructor() {
    super(...arguments);
    __publicField$2(this, "_tag", "Unknown");
  }
  getParser() {
    return dt(this, Qs);
  }
  getExpected() {
    return "unknown";
  }
};
const wp = new xp();
let Ot = class extends ce$2 {
  constructor(t, n, r, s, o) {
    super(n, r, s, o);
    __publicField$2(this, "_tag", "Literal");
    __publicField$2(this, "literal");
    if (typeof t == "number" && !globalThis.Number.isFinite(t)) throw new Error(`A numeric literal must be finite, got ${L$1(t)}`);
    this.literal = t;
  }
  getParser() {
    return ss(this, this.literal);
  }
  toCodecJson() {
    return typeof this.literal == "bigint" ? Ls(this) : this;
  }
  toCodecStringTree() {
    return typeof this.literal == "string" ? this : Ls(this);
  }
  getExpected() {
    return typeof this.literal == "string" ? JSON.stringify(this.literal) : globalThis.String(this.literal);
  }
};
function Ls(e) {
  const t = globalThis.String(e.literal);
  return H(e, [new pe$2(new Ot(t), new Ee$1(Ae$2(() => e.literal), Ae$2(() => t)))]);
}
let Op = class extends ce$2 {
  constructor() {
    super(...arguments);
    __publicField$2(this, "_tag", "String");
  }
  getParser() {
    return dt(this, Hs);
  }
  getExpected() {
    return "string";
  }
};
const ya = new Op();
let Ap = class extends ce$2 {
  constructor() {
    super(...arguments);
    __publicField$2(this, "_tag", "Number");
  }
  getParser() {
    return dt(this, Zs);
  }
  toCodecJson() {
    return this.checks && (bt(this.checks, "isFinite") || bt(this.checks, "isInt")) ? this : H(this, [Rp]);
  }
  toCodecStringTree() {
    return this.checks && (bt(this.checks, "isFinite") || bt(this.checks, "isInt")) ? H(this, [Zp]) : H(this, [Qp]);
  }
  getExpected() {
    return "number";
  }
};
function bt(e, t) {
  return e.some((n) => {
    var _a3, _b2;
    switch (n._tag) {
      case "Filter":
        return ((_b2 = (_a3 = n.annotations) == null ? void 0 : _a3.meta) == null ? void 0 : _b2._tag) === t;
      case "FilterGroup":
        return bt(n.checks, t);
    }
  });
}
const Ea = new Ap();
let kp = class extends ce$2 {
  constructor() {
    super(...arguments);
    __publicField$2(this, "_tag", "Boolean");
  }
  getParser() {
    return dt(this, hc);
  }
  getExpected() {
    return "boolean";
  }
};
const Ip = new kp();
class es extends ce$2 {
  constructor(t, n, r, s, o, i, u) {
    super(s, o, i, u);
    __publicField$2(this, "_tag", "Arrays");
    __publicField$2(this, "isMutable");
    __publicField$2(this, "elements");
    __publicField$2(this, "rest");
    this.isMutable = t, this.elements = n, this.rest = r;
    const c = n.findIndex(ve$1);
    if (c !== -1 && (n.slice(c + 1).some((a) => !ve$1(a)) || r.length > 1)) throw new Error("A required element cannot follow an optional element. ts(1257)");
    if (r.length > 1 && r.slice(1).some(ve$1)) throw new Error("An optional element cannot follow a rest element. ts(1266)");
  }
  getParser(t) {
    const n = this, r = n.elements.map((l) => ({ ast: l, parser: t(l) })), s = n.rest.map((l) => ({ ast: l, parser: t(l) })), o = r.length, [i, ...u] = s, c = u.length;
    function a(l, f) {
      return f < o ? r[f] : f >= l ? u[f - l] : i;
    }
    return cr(function* (l, f) {
      if (l._tag === "None") return l;
      const p = l.value;
      if (!Array.isArray(p)) return yield* j(new Yt(n, l));
      const E = p.length, m = { ast: n, getParser: a, oinput: l, len: E, tailThreshold: Cp(E, o, c), output: new globalThis.Array(E), issues: void 0, options: f }, y = ts(f == null ? void 0 : f.concurrency), k = vp(m, p, { concurrency: y == null ? void 0 : y.concurrency, end: n.rest.length === 0 ? o : Math.max(E, o + c) });
      if (k && (yield* k), n.rest.length === 0 && E > o) for (let J = o; J <= E - 1; J++) {
        const _ = new ft([J], new ua(n, p[J]));
        if (f.errors === "all") m.issues ? m.issues.push(_) : m.issues = [_];
        else return yield* j(new fe$2(n, l, [_]));
      }
      return m.issues ? yield* j(new fe$2(n, l, m.issues)) : C$2(m.output);
    });
  }
  recur(t) {
    const n = st(this.elements, t), r = st(this.rest, t);
    return n === this.elements && r === this.rest ? this : new es(this.isMutable, n, r, this.annotations, this.checks, void 0, this.context);
  }
  getExpected() {
    return "array";
  }
}
const vp = Tn()({ onItem(e, t, n) {
  const r = n < e.len ? C$2(t) : at();
  return e.getParser(e.tailThreshold, n).parser(r, e.options);
}, step(e, t, n, r) {
  var _a3;
  if (n._tag === "Failure") return un(e, e.ast, r, n);
  if (n.value._tag === "Some") e.output[r] = n.value.value;
  else {
    const s = e.getParser(e.tailThreshold, r);
    if (ve$1(s.ast)) return;
    const o = new ft([r], new ca((_a3 = s.ast.context) == null ? void 0 : _a3.annotations));
    if (e.options.errors === "all") e.issues ? e.issues.push(o) : e.issues = [o];
    else return xn(new fe$2(e.ast, e.oinput, [o]));
  }
} });
function Cp(e, t, n) {
  return Math.max(t, e - n);
}
const ts = (e) => (e = e === "unbounded" ? 1 / 0 : e != null ? e : 1, e > 1 ? { concurrency: e } : void 0), un = (e, t, n, r) => {
  const s = On(r.cause);
  if (X$1(s)) return r;
  const o = new ft([n], s.success);
  if (e.options.errors === "all") e.issues ? e.issues.push(o) : e.issues = [o];
  else return xn(new fe$2(t, e.oinput, [o]));
}, ns = "[+-]?\\d*\\.?\\d+(?:[Ee][+-]?\\d+)?", $p = new globalThis.RegExp(`(?:${ns}|Infinity|-Infinity|NaN)`);
function ba(e, t) {
  const n = Nn(t);
  switch (n._tag) {
    case "String":
      return Object.keys(e);
    case "TemplateLiteral": {
      const r = Yp(n);
      return Object.keys(e).filter((s) => r.test(s));
    }
    case "Symbol":
      return Object.getOwnPropertySymbols(e);
    case "Number":
      return Object.keys(e).filter((r) => $p.test(r));
    case "Union":
      return [...new Set(n.types.flatMap((r) => ba(e, r)))];
    default:
      return [];
  }
}
class rs {
  constructor(t, n) {
    __publicField$2(this, "name");
    __publicField$2(this, "type");
    this.name = t, this.type = n;
  }
}
class Sa {
  constructor(t, n, r) {
    __publicField$2(this, "parameter");
    __publicField$2(this, "type");
    __publicField$2(this, "merge");
    if (this.parameter = t, this.type = n, this.merge = r, ve$1(n) && !Ra(n)) throw new Error("Cannot use `Schema.optionalKey` with index signatures, use `Schema.optional` instead.");
  }
}
class Dn extends ce$2 {
  constructor(t, n, r, s, o, i) {
    super(r, s, o, i);
    __publicField$2(this, "_tag", "Objects");
    __publicField$2(this, "propertySignatures");
    __publicField$2(this, "indexSignatures");
    this.propertySignatures = t, this.indexSignatures = n;
    const u = t.map((c) => c.name).filter((c, a, l) => l.indexOf(c) !== a);
    if (u.length > 0) throw new Error(`Duplicate identifiers: ${JSON.stringify(u)}. ts(2300)`);
  }
  getParser(t) {
    const n = this, r = [], s = /* @__PURE__ */ new Set(), o = [];
    for (const c of n.propertySignatures) r.push(c.name), s.add(c.name), o.push({ ps: c, parser: t(c.type), name: c.name, type: c.type });
    const i = n.indexSignatures.length;
    if (n.propertySignatures.length === 0 && n.indexSignatures.length === 0) return dt(n, yc);
    const u = i > 0 ? Tn()({ onItem: cr(function* (c, [a, l]) {
      const p = t(ja(l.parameter))(C$2(a), c.options), E = ae$1(p) ? p : yield* tn(p);
      if (E._tag === "Failure") {
        const _ = un(c, n, a, E);
        _ && (yield* _);
        return;
      }
      const m = C$2(c.input[a]), k = t(l.type)(m, c.options), J = ae$1(k) ? k : yield* tn(k);
      if (J._tag === "Failure") {
        const _ = un(c, n, a, J);
        _ && (yield* _);
        return;
      } else if (E.value._tag === "Some" && J.value._tag === "Some") {
        const _ = E.value.value, F = J.value.value;
        if (l.merge && l.merge.decode && Object.hasOwn(c.out, _)) {
          const [x, N] = l.merge.decode.combine([_, c.out[_]], [_, F]);
          Et(c.out, x, N);
        } else Et(c.out, _, F);
      }
    }), step: (c, a, l) => l._tag === "Failure" ? l : void 0 }) : void 0;
    return cr(function* (c, a) {
      if (c._tag === "None") return c;
      const l = c.value;
      if (!(typeof l == "object" && l !== null && !Array.isArray(l))) return yield* j(new Yt(n, c));
      const f = {}, p = { ast: n, oinput: c, input: l, out: f, issues: void 0, options: a }, E = a.errors === "all", m = a.onExcessProperty === "error", y = a.onExcessProperty === "preserve";
      let k;
      if (n.indexSignatures.length === 0 && (m || y)) {
        k = Reflect.ownKeys(l);
        for (let F = 0; F < k.length; F++) {
          const x = k[F];
          if (!s.has(x)) if (m) {
            const N = new ft([x], new ua(n, l[x]));
            if (E) {
              p.issues ? p.issues.push(N) : p.issues = [N];
              continue;
            } else return yield* j(new fe$2(n, c, [N]));
          } else Et(f, x, l[x]);
        }
      }
      const J = ts(a == null ? void 0 : a.concurrency), _ = Dp(p, o, J);
      if (_ && (yield* _), u) {
        const F = Fu();
        for (let N = 0; N < i; N++) {
          const Be = n.indexSignatures[N], q = ba(l, Be.parameter);
          for (let Vn = 0; Vn < q.length; Vn++) {
            const lc = q[Vn];
            F.push([lc, Be]);
          }
        }
        const x = u(p, F, J);
        x && (yield* x);
      }
      if (p.issues) return yield* j(new fe$2(n, c, p.issues));
      if (a.propertyOrder === "original") {
        const F = (k != null ? k : Reflect.ownKeys(l)).concat(r), x = {};
        for (const N of F) Object.hasOwn(f, N) && Et(x, N, f[N]);
        return C$2(x);
      }
      return C$2(f);
    });
  }
  rebuild(t, n) {
    const r = st(this.propertySignatures, (o) => {
      const i = t(o.type);
      return i === o.type ? o : new rs(o.name, i);
    }), s = st(this.indexSignatures, (o) => {
      var _a3;
      const i = t(o.parameter), u = t(o.type), c = n ? (_a3 = o.merge) == null ? void 0 : _a3.flip() : o.merge;
      return i === o.parameter && u === o.type && c === o.merge ? o : new Sa(i, u, c);
    });
    return r === this.propertySignatures && s === this.indexSignatures ? this : new Dn(r, s, this.annotations, this.checks, void 0, this.context);
  }
  flip(t) {
    return this.rebuild(t, true);
  }
  recur(t) {
    return this.rebuild(t, false);
  }
  getExpected() {
    return this.propertySignatures.length === 0 && this.indexSignatures.length === 0 ? "object | array" : "object";
  }
}
const Dp = Tn()({ onItem(e, t) {
  const n = Object.hasOwn(e.input, t.name) ? C$2(e.input[t.name]) : at();
  return t.parser(n, e.options);
}, step(e, t, n) {
  var _a3;
  if (n._tag === "Failure") return un(e, e.ast, t.name, n);
  if (n.value._tag === "Some") Et(e.out, t.name, n.value.value);
  else if (!ve$1(t.type)) {
    const r = new ft([t.name], new ca((_a3 = t.type.context) == null ? void 0 : _a3.annotations));
    if (e.options.errors === "all") {
      e.issues ? e.issues.push(r) : e.issues = [r];
      return;
    } else return xn(new fe$2(e.ast, e.oinput, [r]));
  }
} });
function Ta(e, t, n) {
  return new Dn(Reflect.ownKeys(e).map((r) => new rs(r, e[r].ast)), [], n, t);
}
function _a$1(e) {
  return e.ast;
}
function xa(e, t, n) {
  return new Un(e.map(_a$1), t, void 0, n);
}
function wa(e) {
  switch (e._tag) {
    case "Null":
      return ["null"];
    case "Undefined":
    case "Void":
      return ["undefined"];
    case "String":
    case "TemplateLiteral":
      return ["string"];
    case "Number":
      return ["number"];
    case "Boolean":
      return ["boolean"];
    case "Symbol":
    case "UniqueSymbol":
      return ["symbol"];
    case "BigInt":
      return ["bigint"];
    case "Arrays":
      return ["array"];
    case "ObjectKeyword":
      return ["object", "array", "function"];
    case "Objects":
      return e.propertySignatures.length || e.indexSignatures.length ? ["object"] : ["object", "array"];
    case "Enum":
      return Array.from(new Set(e.enums.map(([, t]) => typeof t)));
    case "Literal":
      return [typeof e.literal];
    case "Union":
      return Array.from(new Set(e.types.flatMap(wa)));
    default:
      return ["null", "undefined", "string", "number", "boolean", "symbol", "bigint", "object", "array", "function"];
  }
}
function Oa(e) {
  var _a3;
  switch (e._tag) {
    default:
      return [];
    case "Declaration": {
      const t = (_a3 = e.annotations) == null ? void 0 : _a3["~sentinels"];
      return Array.isArray(t) ? t : [];
    }
    case "Objects":
      return e.propertySignatures.flatMap((t) => {
        const n = t.type;
        if (!ve$1(n)) {
          if (an(n)) return [{ key: t.name, literal: n.literal }];
          if (fp(n)) return [{ key: t.name, literal: n.symbol }];
        }
        return [];
      });
    case "Arrays":
      return e.elements.flatMap((t, n) => an(t) && !ve$1(t) ? [{ key: n, literal: t.literal }] : []);
    case "Suspend":
      return Oa(e.thunk());
  }
}
const js = /* @__PURE__ */ new WeakMap();
function Up(e) {
  var _a3, _b2, _c3, _d3, _e3, _f3, _g2;
  let t = js.get(e);
  if (t) return t;
  t = {};
  for (const n of e) {
    const r = Nn(n);
    if (lp(r)) continue;
    const s = wa(r), o = Oa(r);
    (_a3 = t.byType) != null ? _a3 : t.byType = {};
    for (const i of s) ((_c3 = (_b2 = t.byType)[i]) != null ? _c3 : _b2[i] = []).push(n);
    if (o.length > 0) {
      (_d3 = t.bySentinel) != null ? _d3 : t.bySentinel = /* @__PURE__ */ new Map();
      for (const { key: i, literal: u } of o) {
        let c = t.bySentinel.get(i);
        c || t.bySentinel.set(i, c = /* @__PURE__ */ new Map());
        let a = c.get(u);
        a || c.set(u, a = []), a.push(n);
      }
    } else {
      (_e3 = t.otherwise) != null ? _e3 : t.otherwise = {};
      for (const i of s) ((_g2 = (_f3 = t.otherwise)[i]) != null ? _g2 : _f3[i] = []).push(n);
    }
  }
  return js.set(e, t), t;
}
function Ms(e) {
  return (t) => {
    const n = Nn(t);
    return n._tag === "Literal" ? n.literal === e : n._tag === "UniqueSymbol" ? n.symbol === e : true;
  };
}
function Fp(e, t) {
  var _a3, _b2, _c3, _d3;
  const n = Up(t), r = e === null ? "null" : Array.isArray(e) ? "array" : typeof e;
  if (n.bySentinel) {
    const s = (_b2 = (_a3 = n.otherwise) == null ? void 0 : _a3[r]) != null ? _b2 : [];
    if (r === "object" || r === "array") {
      for (const [o, i] of n.bySentinel) if (Object.hasOwn(e, o)) {
        const u = i.get(e[o]);
        if (u) return [...u, ...s].filter(Ms(e));
      }
    }
    return s;
  }
  return ((_d3 = (_c3 = n.byType) == null ? void 0 : _c3[r]) != null ? _d3 : []).filter(Ms(e));
}
let Un = class Aa extends ce$2 {
  constructor(t, n, r, s, o, i) {
    super(r, s, o, i);
    __publicField$2(this, "_tag", "Union");
    __publicField$2(this, "types");
    __publicField$2(this, "mode");
    this.types = t, this.mode = n;
  }
  getParser(t) {
    const n = this;
    return (r, s) => {
      var _a3;
      if (r._tag === "None") return $$1(r);
      const o = r.value, i = Fp(o, n.types), u = { ast: n, recur: t, oinput: r, input: o, out: void 0, successes: [], issues: void 0, options: s }, c = ts(s == null ? void 0 : s.concurrency), a = Np(u, i, c);
      return a ? K(a, (l) => {
        var _a4;
        return u.out ? $$1(u.out) : j(new Ds(n, o, (_a4 = u.issues) != null ? _a4 : []));
      }) : u.out ? $$1(u.out) : j(new Ds(n, o, (_a3 = u.issues) != null ? _a3 : []));
    };
  }
  recur(t) {
    const n = st(this.types, t);
    return n === this.types ? this : new Aa(n, this.mode, this.annotations, this.checks, void 0, this.context);
  }
  getExpected(t) {
    var _a3;
    const n = (_a3 = this.annotations) == null ? void 0 : _a3.expected;
    if (typeof n == "string") return n;
    if (this.types.length === 0) return "never";
    const r = this.types.map((s) => {
      const o = Nn(s);
      switch (o._tag) {
        case "Arrays": {
          const i = o.elements.filter(an);
          if (i.length > 0) return `${zs(o.isMutable)}[ ${i.map((u) => {
            var _a4;
            return t(u) + Vs((_a4 = u.context) == null ? void 0 : _a4.isOptional);
          }).join(", ")}, ... ]`;
          break;
        }
        case "Objects": {
          const i = o.propertySignatures.filter((u) => an(u.type));
          if (i.length > 0) return `{ ${i.map((u) => {
            var _a4, _b2;
            return `${zs((_a4 = u.type.context) == null ? void 0 : _a4.isMutable)}${Xt(u.name)}${Vs((_b2 = u.type.context) == null ? void 0 : _b2.isOptional)}: ${t(u.type)}`;
          }).join(", ")}, ... }`;
          break;
        }
      }
      return t(o);
    });
    return Array.from(new Set(r)).join(" | ");
  }
};
const Np = Tn()({ onItem(e, t) {
  return e.recur(t)(e.oinput, e.options);
}, step(e, t, n) {
  if (n._tag === "Failure") {
    const r = On(n.cause);
    if (X$1(r)) return n;
    e.issues ? e.issues.push(r.success) : e.issues = [r.success];
  } else {
    if (e.out && e.ast.mode === "oneOf") return e.successes.push(t), xn(new qd(e.ast, e.input, e.successes));
    if (e.out = n.value, e.successes.push(t), e.ast.mode === "anyOf") return ai;
  }
} }), ka = new Un([new Ot("Infinity"), new Ot("-Infinity"), new Ot("NaN")], "anyOf"), Rp = new pe$2(new Un([Ea, ka], "anyOf"), new Ee$1(ha(), Ae$2((e) => globalThis.Number.isFinite(e) ? e : globalThis.String(e))));
function zs(e) {
  return e ? "" : "readonly ";
}
function Vs(e) {
  return e ? "?" : "";
}
class ln extends Er {
  constructor(t, n = void 0, r = false) {
    super();
    __publicField$2(this, "_tag", "Filter");
    __publicField$2(this, "run");
    __publicField$2(this, "annotations");
    __publicField$2(this, "aborted");
    this.run = t, this.annotations = n, this.aborted = r;
  }
  annotate(t) {
    return new ln(this.run, { ...this.annotations, ...t }, this.aborted);
  }
  abort() {
    return new ln(this.run, this.annotations, true);
  }
  and(t, n) {
    return new fn([this, t], n);
  }
}
class fn extends Er {
  constructor(t, n = void 0) {
    super();
    __publicField$2(this, "_tag", "FilterGroup");
    __publicField$2(this, "checks");
    __publicField$2(this, "annotations");
    this.checks = t, this.annotations = n;
  }
  annotate(t) {
    return new fn(this.checks, { ...this.annotations, ...t });
  }
  and(t, n) {
    return new fn([this, t], n);
  }
}
function Ia(e, t, n = false) {
  return new ln((r, s, o) => Zd(r, s, e(r, s, o)), t, n);
}
function Pp(e, t) {
  const n = e.source;
  return Ia((r) => e.test(r), { expected: `a string matching the RegExp ${n}`, meta: { _tag: "isPattern", regExp: e }, toArbitraryConstraint: { string: { patterns: [e.source] } }, ...t });
}
function Fn(e, t) {
  const n = Object.getOwnPropertyDescriptors(e);
  return t(n), Object.create(Object.getPrototypeOf(e), n);
}
function H(e, t) {
  return e.encoding === t ? e : Fn(e, (n) => {
    n.encoding.value = t;
  });
}
function va(e, t) {
  return e.context === t ? e : Fn(e, (n) => {
    n.context.value = t;
  });
}
function Ca(e, t) {
  if (e.checks) {
    const n = e.checks[e.checks.length - 1];
    return $a(e, Do(e.checks.slice(0, -1), n.annotate(t)));
  }
  return Fn(e, (n) => {
    n.annotations.value = { ...n.annotations.value, ...t };
  });
}
function $a(e, t) {
  return e.checks === t ? e : Fn(e, (n) => {
    n.checks.value = t;
  });
}
function Da(e, t) {
  return $a(e, e.checks ? [...e.checks, ...t] : t);
}
function Ua(e, t) {
  const n = e, r = n[n.length - 1], s = t(r.to);
  return s !== r.to ? Do(e.slice(0, e.length - 1), new pe$2(s, r.transformation)) : e;
}
function Lp(e) {
  return (t) => t.encoding ? H(t, Ua(t.encoding, e)) : t;
}
function jp(e, t, n) {
  const r = new pe$2(e, t);
  return H(n, n.encoding ? [...n.encoding, r] : [r]);
}
function Mp(e, t) {
  const n = Kd(e), r = n ? [...n, t] : [t];
  return Ca(e, { brands: r });
}
function st(e, t) {
  let n = false;
  const r = new Array(e.length);
  for (let s = 0; s < e.length; s++) {
    const o = e[s], i = t(o);
    i !== o && (n = true), r[s] = i;
  }
  return n ? r : e;
}
function zp(e, t) {
  const n = e.context ? new cn(e.context.isOptional, e.context.isMutable, e.context.defaultValue, { ...e.context.annotations, ...t }) : new cn(false, false, void 0, t);
  return va(e, n);
}
const Vp = Lp(Fa);
function Fa(e) {
  const t = e.context ? e.context.isOptional === false ? new cn(true, e.context.isMutable, e.context.defaultValue, e.context.annotations) : e.context : new cn(true, false);
  return Vp(va(e, t));
}
function Jp(e, t, n) {
  return jp(e, n, t);
}
function Na(e) {
  switch (e._tag) {
    case "Literal":
      return { literals: gc(e.literal) ? [e.literal] : [], parameters: [] };
    case "UniqueSymbol":
      return { literals: [e.symbol], parameters: [] };
    case "String":
    case "Number":
    case "Symbol":
    case "TemplateLiteral":
      return { literals: [], parameters: [e] };
    case "Union": {
      const t = { literals: [], parameters: [] };
      for (let n = 0; n < e.types.length; n++) {
        const r = Na(e.types[n]);
        t.literals = t.literals.concat(r.literals), t.parameters = t.parameters.concat(r.parameters);
      }
      return t;
    }
  }
  return { literals: [], parameters: [] };
}
function Kp(e, t, n) {
  const { literals: r, parameters: s } = Na(e);
  return new Dn(r.map((o) => new rs(o, t)), s.map((o) => new Sa(o, t, n)));
}
function ve$1(e) {
  var _a3, _b2;
  return (_b2 = (_a3 = e.context) == null ? void 0 : _a3.isOptional) != null ? _b2 : false;
}
const dn = De$1((e) => {
  var _a3, _b2;
  if (e.encoding) return dn(H(e, void 0));
  const t = e;
  return (_b2 = (_a3 = t.recur) == null ? void 0 : _a3.call(t, dn)) != null ? _b2 : t;
}), Nn = De$1((e) => dn(Qe(e)));
function Bp(e, t) {
  const n = t, r = n.length, s = n[r - 1], o = [new pe$2(Qe(H(e, void 0)), n[0].transformation.flip())];
  for (let u = 1; u < r; u++) o.unshift(new pe$2(Qe(n[u - 1].to), n[u].transformation.flip()));
  const i = Qe(s.to);
  return i.encoding ? H(i, [...i.encoding, ...o]) : H(i, o);
}
const Qe = De$1((e) => {
  var _a3, _b2, _c3, _d3;
  if (e.encoding) return Bp(e, e.encoding);
  const t = e;
  return (_d3 = (_c3 = (_a3 = t.flip) == null ? void 0 : _a3.call(t, Qe)) != null ? _c3 : (_b2 = t.recur) == null ? void 0 : _b2.call(t, Qe)) != null ? _d3 : t;
});
function Ra(e) {
  switch (e._tag) {
    case "Undefined":
      return true;
    case "Union":
      return e.types.some(Ra);
    default:
      return false;
  }
}
function Pa(e, t) {
  return e.encodedParts.map((n) => Wp(n, La(n), t)).join("");
}
const Yp = De$1((e) => new globalThis.RegExp(`^${Pa(e, true)}$`));
function La(e) {
  switch (e._tag) {
    case "Literal":
      return Bd(globalThis.String(e.literal));
    case "String":
      return qp;
    case "Number":
      return ns;
    case "BigInt":
      return Xp;
    case "TemplateLiteral":
      return Pa(e, false);
    case "Union":
      return e.types.map(La).join("|");
  }
}
function Wp(e, t, n) {
  if (hp(e)) {
    if (!n) return `(?:${t})`;
  } else if (!n) return t;
  return `(${t})`;
}
function ss(e, t) {
  const n = Mf(t);
  return (r) => r._tag === "None" ? kn : r.value === t ? n : j(new Yt(e, r));
}
function dt(e, t) {
  return (n) => n._tag === "None" ? kn : t(n.value) ? $$1(n) : j(new Yt(e, n));
}
function Gp(e) {
  function t(n) {
    return n.encoding ? H(n, Ua(n.encoding, t)) : e(n);
  }
  return De$1(t);
}
const ja = Gp((e) => {
  switch (e._tag) {
    default:
      return e;
    case "Number":
      return e.toCodecStringTree();
    case "Union":
      return e.recur(ja);
  }
}), qp = "[\\s\\S]*?", Js = new globalThis.RegExp(`^${ns}$`);
function Hp(e) {
  return Pp(Js, { expected: "a string representing a finite number", meta: { _tag: "isStringFinite", regExp: Js }, ...e });
}
const Ma = Da(ya, [Hp()]), Zp = new pe$2(Ma, Qr), Qp = new pe$2(new Un([Ma, ka], "anyOf"), Qr), Xp = "-?\\d+";
function mr(e, t, n, r, s) {
  for (let o = 0; o < e.length; o++) {
    const i = e[o];
    if (i._tag === "FilterGroup") mr(i.checks, t, n, r, s);
    else {
      const u = i.run(t, r, s);
      if (u && (n.push(new Wd(t, i, u)), i.aborted || (s == null ? void 0 : s.errors) !== "all")) return;
    }
  }
}
const eh = "~effect/Schema/Class", th = "~structural", Rn = (e) => e, St = De$1((e) => {
  var _a3;
  switch (e._tag) {
    case "Declaration": {
      const t = (_a3 = e.annotations) == null ? void 0 : _a3[eh];
      if (br(t)) {
        const n = t(e.typeParameters), r = St(n.to);
        return H(e, r === n.to ? [n] : [new pe$2(r, n.transformation)]);
      }
      return e;
    }
    case "Objects":
    case "Arrays":
      return e.recur((t) => {
        var _a4;
        const n = (_a4 = t.context) == null ? void 0 : _a4.defaultValue;
        return n ? H(St(t), n) : St(t);
      });
    case "Suspend":
      return e.recur(St);
    default:
      return e;
  }
});
function os(e) {
  const t = St(dn(e.ast)), n = Va(t);
  return (r, s) => n(r, (s == null ? void 0 : s.disableChecks) ? (s == null ? void 0 : s.parseOptions) ? { ...s.parseOptions, disableChecks: true } : { disableChecks: true } : s == null ? void 0 : s.parseOptions);
}
function nh(e) {
  const t = os(e);
  return (n, r) => Tf(qf(t(n, r)));
}
function rh(e) {
  const t = os(e);
  return (n, r) => gi(In(t(n, r), (s) => new Error(s.toString(), { cause: s })));
}
function za(e) {
  return Va(e.ast);
}
function sh(e) {
  return oh(za(e));
}
function Va(e) {
  const t = Qt(e);
  return (n, r) => We(t(C$2(n), r != null ? r : mp), (s) => s._tag === "None" ? j(new Ut(s)) : $$1(s.value));
}
function oh(e) {
  return (t, n) => gi(In(e(t, n), (r) => new Error(r.toString(), { cause: r })));
}
const Qt = De$1((e) => {
  var _a3;
  let t;
  const n = (_a3 = ia(e)) == null ? void 0 : _a3.parseOptions;
  if (!e.context && !e.encoding && !e.checks) return (s, o) => (t != null ? t : t = e.getParser(Qt), n && (o = { ...o, ...n }), t(s, o));
  const r = dp(e) || pp(e) || up(e) && e.typeParameters.length > 0;
  return (s, o) => {
    n && (o = { ...o, ...n });
    const i = e.encoding;
    let u;
    if (i) {
      const a = i, l = a.length;
      for (let f = l - 1; f >= 0; f--) {
        const p = a[f], E = p.to, m = Qt(E);
        if (u = u ? We(u, (y) => m(y, o)) : m(s, o), p.transformation._tag === "Transformation") {
          const y = p.transformation.decode;
          u = We(u, (k) => y.run(k, o));
        } else u = p.transformation.decode(u, o);
      }
      u = In(u, (f) => new Gd(e, s, f));
    }
    t != null ? t : t = e.getParser(Qt);
    let c = u ? We(u, (a) => t(a, o)) : t(s, o);
    if (e.checks && !(o == null ? void 0 : o.disableChecks)) {
      const a = e.checks;
      (o == null ? void 0 : o.errors) === "all" && r && gs(s) && (c = Hf(c, (l) => {
        const f = [];
        mr(a.filter((E) => {
          var _a4;
          return (_a4 = E.annotations) == null ? void 0 : _a4[th];
        }), s.value, f, e, o);
        const p = bs(f) ? l._tag === "Composite" && l.ast === e ? new fe$2(e, l.actual, [...l.issues, ...f]) : new fe$2(e, s, [l, ...f]) : l;
        return j(p);
      })), c = We(c, (l) => {
        if (gs(l)) {
          const f = l.value, p = [];
          if (mr(a, f, p, e, o), bs(p)) return j(new fe$2(e, l, p));
        }
        return $$1(l);
      });
    }
    return c;
  };
}), Ks = "~effect/Schema/Schema", ih = { [Ks]: Ks, pipe() {
  return ke$1(this, arguments);
}, annotate(e) {
  return this.rebuild(Ca(this.ast, e));
}, annotateKey(e) {
  return this.rebuild(zp(this.ast, e));
}, check(...e) {
  return this.rebuild(Da(this.ast, e));
} };
function Ja(e, t) {
  const n = Object.create(ih);
  return t && Object.assign(n, t), n.ast = e, n.rebuild = (r) => Ja(r, t), n.makeEffect = pc(os(n), In((r) => new ah(r))), n.make = rh(n), n.makeOption = nh(n), n;
}
const Bs = "~effect/Schema/SchemaError";
_n2 = Bs;
class ah {
  constructor(t) {
    __publicField$2(this, _n2, Bs);
    __publicField$2(this, "_tag", "SchemaError");
    __publicField$2(this, "name", "SchemaError");
    __publicField$2(this, "issue");
    this.issue = t;
  }
  get message() {
    return this.issue.toString();
  }
  toString() {
    return `SchemaError(${this.message})`;
  }
}
function ch() {
  return (e, t, n) => z$1(new Xr(e.map(_a$1), (r) => t(r.map((s) => z$1(s))), n));
}
function uh(e, t) {
  return ch()([], () => (n, r) => e(n) ? $$1(n) : j(new Yt(r, C$2(n))), t);
}
function Ys(e) {
  return wn(e) ? e.value : { issues: [{ message: Of(e.cause) }] };
}
function re$1(e, t) {
  const n = za(e), r = { errors: "all", ...void 0  }, s = Qd(), o = (i) => {
    var _a3;
    const u = new kr(), c = Yf(Kf(n(i, r), { onFailure: s, onSuccess: (l) => ({ value: l }) }), { scheduler: u });
    (_a3 = c.currentDispatcher) == null ? void 0 : _a3.flush();
    const a = c.pollUnsafe();
    return a ? Ys(a) : new Promise((l) => {
      c.addObserver((f) => {
        l(Ys(f));
      });
    });
  };
  if ("~standard" in e) {
    const i = e;
    return "validate" in i["~standard"] || Object.assign(i["~standard"], { validate: o }), i;
  } else return Object.assign(e, { "~standard": { version: 1, vendor: "effect", validate: o } });
}
const pt = sh, z$1 = Ja, lh = Rn((e) => z$1(Fa(e.ast), { schema: e })), V$1 = Rn((e) => lh(ph(e)));
function O$1(e) {
  const t = z$1(new Ot(e), { literal: e, transform(n) {
    return t.pipe(as(O$1(n), { decode: Ae$2(() => n), encode: Ae$2(() => e) }));
  } });
  return t;
}
const Bm = z$1(_p), is = z$1(wp), fh = z$1(Sp), h$1 = z$1(ya), Me$1 = z$1(Ea), Qn = z$1(Ip);
function Ka(e, t) {
  return z$1(e, { fields: t, mapFields(n, r) {
    const s = n(this.fields);
    return Ka(Ta(s, (r == null ? void 0 : r.unsafePreserveChecks) ? this.ast.checks : void 0), s);
  } });
}
function A(e) {
  return Ka(Ta(e, void 0), e);
}
function Pn(e, t, n) {
  return z$1(Kp(e.ast, t.ast, void 0), { key: e, value: t });
}
const dh = Rn((e) => z$1(new es(false, [], [e.ast]), { schema: e }));
function Ba(e, t) {
  return z$1(e, { members: t, mapMembers(n, r) {
    const s = n(this.members);
    return Ba(xa(s, this.ast.mode, (r == null ? void 0 : r.unsafePreserveChecks) ? this.ast.checks : void 0), s);
  } });
}
function be$1(e, t) {
  return Ba(xa(e, "anyOf", void 0), e);
}
const ph = Rn((e) => be$1([e, fh]));
function $e$1(...e) {
  return (t) => t.check(...e);
}
function Ln(e) {
  return (t) => z$1(Mp(t.ast, e), { schema: t, identifier: e });
}
function as(e, t) {
  return (n) => z$1(Jp(n.ast, e.ast, t ? ma(t) : cp()), { from: n, to: e });
}
function hh() {
  return (e, t) => new pe$2(e.ast, ma(t));
}
const cs = Ia;
function mh(e) {
  var _a3;
  const t = hu(e.order), n = (_a3 = e.formatter) != null ? _a3 : L$1;
  return (r, s) => {
    var _a4;
    return cs((o) => t(o, r), { expected: `a value greater than or equal to ${n(r)}`, ...(_a4 = e.annotate) == null ? void 0 : _a4.call(e, r), ...s });
  };
}
const ot = mh({ order: pu, annotate: (e) => ({ meta: { _tag: "isGreaterThanOrEqualTo", minimum: e }, toArbitraryConstraint: { number: { min: e } } }) }), Ym = h$1.annotate({ expected: "a string that will be decoded as a number" }).pipe(as(Me$1, Qr)), gh = uh((e) => Rd(e) && Pd(e), { typeConstructor: { _tag: "effect/DateTime.Utc" }, generation: { runtime: "Schema.DateTimeUtc", Type: "DateTime.Utc", importDeclaration: 'import * as DateTime from "effect/DateTime"' }, expected: "DateTime.Utc", toCodecJson: () => hh()(h$1, ga), toArbitrary: () => (e, t) => {
  var _a3;
  return e.date({ noInvalidDate: true, ...(_a3 = t == null ? void 0 : t.constraints) == null ? void 0 : _a3.date }).map((n) => jd(n));
}, toFormatter: () => (e) => e.toString(), toEquivalence: () => Ld }), yh = h$1.annotate({ expected: "a string that will be decoded as a DateTime.Utc" }).pipe(as(gh, ga)), Eh = yh, Ya = [".html", ".htm", ".xhtml", ".md", ".markdown", ".txt", ".log", ".csv", ".tsv", ".json", ".yaml", ".yml", ".xml", ".toml", ".js", ".ts", ".jsx", ".tsx", ".css", ".scss", ".sass", ".less", ".styl", ".php", ".py", ".rb", ".java", ".c", ".cpp", ".h", ".hpp", ".cs", ".fs", ".fsx", ".fsi", ".go", ".rs", ".swift", ".kt", ".clj", ".cljs", ".cljc", ".edn", ".lua", ".pl", ".r", ".sh", ".bash", ".zsh", ".fish", ".awk", ".ps1", ".bat", ".cmd", ".vbs", ".mp3", ".wav", ".ogg", ".flac", ".aac", ".jpg", ".jpeg", ".png", ".gif", ".bmp", ".webp", ".svg", ".tiff", ".mp4", ".mov", ".avi", ".mkv", ".webm", ".flv", ".wmv", ".zip", ".rar", ".7z", ".pdf", ".exe", ".bin", ".iso"], bh = new Set(Ya), Sh = cs((e) => bh.has(e) ? void 0 : `Expected one of: ${Ya.join(", ")}`), Th = h$1.pipe($e$1(Sh)), Wa = ["text/html", "application/xhtml+xml", "text/markdown", "text/plain", "text/csv", "text/tab-separated-values", "application/json", "application/x-yaml", "application/xml", "application/toml", "application/javascript", "application/typescript", "text/jsx", "text/tsx", "text/css", "text/x-scss", "text/x-sass", "text/x-clojure", "application/edn", "text/x-lua", "text/x-perl", "text/x-r", "application/x-sh", "application/x-fish", "application/x-awk", "application/x-powershell", "application/x-msdos-program", "text/vbscript", "text/x-python", "text/x-ruby", "text/x-java-source", "text/x-c", "text/x-c++src", "text/x-c++hdr", "text/x-csharp", "text/x-go", "text/rust", "text/x-swift", "text/x-kotlin", "application/x-httpd-php", "audio/mpeg", "audio/wav", "audio/ogg", "audio/flac", "audio/aac", "image/jpeg", "image/png", "image/gif", "image/bmp", "image/webp", "image/svg+xml", "image/tiff", "video/mp4", "video/quicktime", "video/x-msvideo", "video/x-matroska", "video/webm", "video/x-flv", "video/x-ms-wmv", "application/zip", "application/vnd.rar", "application/x-7z-compressed", "application/pdf", "application/vnd.microsoft.portable-executable", "application/octet-stream", "application/x-iso9660-image"], _h = new Set(Wa), xh = cs((e) => _h.has(e) ? void 0 : `Expected one of: ${Wa.join(", ")}`), wh = h$1.pipe($e$1(xh)), Oh = (e) => e in gr ? gr[e] : "application/octet-stream", gr = { ".html": "text/html", ".htm": "text/html", ".xhtml": "application/xhtml+xml", ".md": "text/markdown", ".markdown": "text/markdown", ".txt": "text/plain", ".log": "text/plain", ".csv": "text/csv", ".tsv": "text/tab-separated-values", ".json": "application/json", ".yaml": "application/x-yaml", ".yml": "application/x-yaml", ".xml": "application/xml", ".toml": "application/toml", ".js": "application/javascript", ".ts": "application/typescript", ".jsx": "text/jsx", ".tsx": "text/tsx", ".css": "text/css", ".scss": "text/x-scss", ".sass": "text/x-sass", ".less": "text/css", ".styl": "text/plain", ".php": "application/x-httpd-php", ".py": "text/x-python", ".rb": "text/x-ruby", ".java": "text/x-java-source", ".c": "text/x-c", ".cpp": "text/x-c++src", ".h": "text/x-c", ".hpp": "text/x-c++hdr", ".cs": "text/x-csharp", ".fs": "text/plain", ".fsx": "text/plain", ".fsi": "text/plain", ".go": "text/x-go", ".rs": "text/rust", ".swift": "text/x-swift", ".kt": "text/x-kotlin", ".clj": "text/x-clojure", ".cljs": "text/x-clojure", ".cljc": "text/x-clojure", ".edn": "application/edn", ".lua": "text/x-lua", ".pl": "text/x-perl", ".r": "text/x-r", ".sh": "application/x-sh", ".bash": "application/x-sh", ".zsh": "application/x-sh", ".fish": "application/x-fish", ".awk": "application/x-awk", ".ps1": "application/x-powershell", ".bat": "application/x-msdos-program", ".cmd": "application/x-msdos-program", ".vbs": "text/vbscript", ".mp3": "audio/mpeg", ".wav": "audio/wav", ".ogg": "audio/ogg", ".flac": "audio/flac", ".aac": "audio/aac", ".jpg": "image/jpeg", ".jpeg": "image/jpeg", ".png": "image/png", ".gif": "image/gif", ".bmp": "image/bmp", ".webp": "image/webp", ".svg": "image/svg+xml", ".tiff": "image/tiff", ".mp4": "video/mp4", ".mov": "video/quicktime", ".avi": "video/x-msvideo", ".mkv": "video/x-matroska", ".webm": "video/webm", ".flv": "video/x-flv", ".wmv": "video/x-ms-wmv", ".zip": "application/zip", ".rar": "application/vnd.rar", ".7z": "application/x-7z-compressed", ".pdf": "application/pdf", ".exe": "application/vnd.microsoft.portable-executable", ".bin": "application/octet-stream", ".iso": "application/x-iso9660-image" }, Ga = re$1(A({ page: Me$1.pipe($e$1(ot(1))), perPage: V$1(Me$1.pipe($e$1(ot(1)))) })), qa = re$1(A({ before: V$1(h$1), perPage: V$1(Me$1.pipe($e$1(ot(1)))) })), Ha = re$1(A({ after: V$1(h$1), perPage: V$1(Me$1.pipe($e$1(ot(1)))) })), Za = re$1(A({ offset: Me$1.pipe($e$1(ot(0))), limit: V$1(Me$1.pipe($e$1(ot(1)))) })), Ah = be$1([Ga, qa, Ha, Za]), ue$2 = { Owner: "owner", Admin: "admin", Editor: "editor", Author: "author", Guest: "guest" }, kh = { [ue$2.Owner]: "Is the owner of the project. Can manage all settings and content.", [ue$2.Admin]: "Full access to all project settings and content. Can manage users and their roles.", [ue$2.Editor]: "Can create, edit, and publish all types of content. Cannot modify project settings or manage users.", [ue$2.Author]: "Can create and edit their own content, but cannot publish without approval from an Editor or Admin.", [ue$2.Guest]: "Read-only access to published content. Cannot create, edit, or publish any content." };
var v$1;
(function(e) {
  e.ManageSettings = "manage_settings", e.ManageUsers = "manage_users", e.CreateDocument = "create_document", e.EditDocument = "edit_document", e.PublishDocument = "publish_document", e.DeleteDocument = "delete_document", e.ViewDocument = "view_document", e.ManageCollections = "manage_collections", e.ViewCollection = "view_collection";
})(v$1 || (v$1 = {}));
const Ih = { [v$1.ManageSettings]: "Allows users to modify and manage system or application settings, including configurations that affect the entire platform.", [v$1.ManageUsers]: "Grants the ability to manage user accounts, including creating, editing, and deleting users, as well as assigning roles and permissions.", [v$1.CreateDocument]: "Allows users to create new documents in the system.", [v$1.EditDocument]: "Grants the ability to edit or modify existing documents.", [v$1.PublishDocument]: "Enables users to publish documents, making them publicly available or accessible to others.", [v$1.DeleteDocument]: "Provides the ability to permanently delete documents from the system.", [v$1.ViewDocument]: "Grants permission to view documents, but without the ability to edit, publish, or delete them.", [v$1.ManageCollections]: "Allows users to create, edit, or delete collections of documents, as well as configure how collections are organized.", [v$1.ViewCollection]: "Grants read-only access to view collections of documents, but without the ability to modify them." }, Qa = [v$1.ViewDocument, v$1.ViewCollection], Xa = [...Qa, v$1.CreateDocument, v$1.EditDocument, v$1.PublishDocument], ec = [...Xa, v$1.DeleteDocument, v$1.ManageCollections], tc = [...ec, v$1.ManageSettings, v$1.ManageUsers], vh = [...tc], Ch = { [ue$2.Owner]: vh, [ue$2.Admin]: tc, [ue$2.Editor]: ec, [ue$2.Author]: Xa, [ue$2.Guest]: Qa }, $h = (e) => {
  let t = null;
  return () => (t || (t = e()), t);
}, Dh = (e) => {
  let t = null;
  return async () => (t || (t = await e()), t);
}, Uh = { toArray: async (e) => {
  const t = [];
  for await (const n of e) t.push(n);
  return t;
}, first: async (e) => {
  for await (const t of e) return t;
}, accumulateFirst: async (e) => {
  const t = [];
  for await (const n of e) {
    if (jt(n)) return lt(n.success);
    X$1(n) && t.push(n.failure);
  }
  return we$2(t);
} }, Fh = (e) => {
  let t;
  return (n) => {
    if ((t == null ? void 0 : t[0]) === n) return t[1];
    const r = e(n);
    return t = [n, r], r;
  };
}, Ie$1 = { isAbsolute: (e) => typeof e == "string" && /^[a-zA-Z][a-zA-Z\d+\-.]*:/.test(e), join: (e, t) => (e = Ie$1.normalize(e), t = Ie$1.normalize(t), e ? t ? Ie$1.isAbsolute(t) ? t : e + t : e : t != null ? t : ""), normalize: (e) => (e && e.endsWith("/") && (e = e.slice(0, -1)), Ie$1.isAbsolute(e) || e && !e.startsWith("/") && (e = "/" + e), e), combine: (...e) => e.reduce((t, n) => Ie$1.join(t, n), "") || "" }, nc = { pathToSegments: (e) => e.split("/").map((n) => n.trim()).filter((n) => n.length > 0), toSegments: (e) => nc.pathToSegments(e), combine: (...e) => e.map((n) => n.trim()).filter((n) => n.length > 0).join("/") }, Nh = { url: (e, ...t) => e.reduce((r, s, o) => {
  if (o === t.length) return Ie$1.join(r, s);
  const i = String(t[o]);
  return Ie$1.combine(r, s, i);
}, "") }, yr = { AuthorizationHeaderBearerRegex: /^Bearer\s+([A-Za-z0-9\-._~+/]+={0,2})$/, AuthorizationHeaderApiKeyRegex: /^ApiKey\s+([A-Za-z0-9\-._~+/]+={0,2})$/, ExtractAuthorizationBearerToken: (e) => {
  if (!e) return;
  const t = e.match(yr.AuthorizationHeaderBearerRegex);
  return t ? t[1] : void 0;
}, ExtractAuthorizationApiKey: (e) => {
  if (!e) return;
  const t = e.match(yr.AuthorizationHeaderApiKeyRegex);
  return t ? t[1] : void 0;
} }, jn = "RecoverableError", rc = "Progress", Rh = (e) => ({ _tag: jn, error: e }), Ph = (e) => ({ _tag: rc, progress: e }), Lh = (e) => G(Cn(e)), jh = (e) => G(Ui(e)), Mh = (e) => sc((t) => ne$1(function* () {
  const { value: n, recoverableErrors: r } = yield* e;
  if (r) for (const s of r) yield* t.recoverableError(s);
  return n;
})), sc = (e, t) => {
  var _a3;
  const n = (_a3 = t == null ? void 0 : t.capacity) != null ? _a3 : 16;
  return G(Je((r, s) => ne$1(function* () {
    const o = yield* rn(n), u = e({ recoverableError: (c) => Ye(_e$3(o, Rh(c))), progress: (c) => Ye(_e$3(o, Ph(c))) }).pipe(pi({ onSuccess: (c) => sn(o, ui(c)), onFailure: (c) => sn(o, c) }));
    return yield* xt(u, s), Oi(o);
  })));
}, oc = (e) => {
  const t = [], n = [];
  return Kt(Mn(e, (r) => {
    for (const s of r) s._tag === jn ? t.push(s.error) : n.push(s.progress);
    return Vt;
  }), (r) => ({ recoverableErrors: t, progress: n, value: r }));
}, us = (e) => Mn(e, () => Vt), zh = (e, t) => Mn(e, (n) => ne$1(function* () {
  for (const r of n) r._tag === jn ? yield* t.recoverableError(r.error) : yield* t.progress(r.progress);
})), Vh = (e) => xe$2(us(e)), Jh = (e) => xe$2(oc(e)), Kh = (e) => xe$2(di(us(e))), Mn = (e, t) => mi(ne$1(function* () {
  const n = yield* Pi(e);
  for (; ; ) {
    const r = yield* tn(n);
    if (wn(r)) {
      yield* t(r.value);
      continue;
    }
    const s = An(r.cause);
    return jt(s) ? s.success : yield* Jt(s.failure);
  }
})), Bh = (e, t) => G(Ni(e, t)), Yh = (e, t) => G($n(e, (n) => ne$1(function* () {
  for (const r of n) r._tag === jn && (yield* t(r.error));
}))), Wh = (e, t) => G($n(e, (n) => ne$1(function* () {
  for (const r of n) r._tag === rc && (yield* t(r.progress));
}))), Gh = Object.freeze(Object.defineProperty({ __proto__: null, drainWithValue: Mn, fail: jh, fromEffect: Mh, make: sc, map: Bh, runCollect: oc, runPromise: Vh, runPromiseCollect: Jh, runPromiseResult: Kh, runValue: us, runValueForwarding: zh, succeed: Lh, tapProgress: Wh, tapRecoverableErrors: Yh }, Symbol.toStringTag, { value: "Module" })), qh = Object.freeze(Object.defineProperty({ __proto__: null, AsyncGenerator: Uh, AuthenticationError: qi, AuthorizationError: Gi, AuthorizerFailureError: Xi, BadRequestError: zi, ConflictError: Hi, CorruptedFileError: na, DangerousFileTypeError: ta, DirInsteadOfFile: Ji, EmbeddedContentError: ra, EntryAlreadyExistsError: Wi, ErrorClasses: oa, ErrorCodeToClassMap: Nd, ErrorCodeToKeyMap: $d, ErrorCodeToStatusMap: Dd, ExtNameSchema: Th, FileInsteadOfDir: Ki, FileTooLargeError: sa, ForbiddenError: Vi, GatewayTimeoutError: Qi, Header: yr, IllegalStateException: ji, InternalError: Ge, InvalidData: Bi, IsoDateWithFallbackSchema: Eh, LaikaElement: Zf, LaikaError: b$1, LaikaStream: pm, LaikaTask: Gh, MimeTypeSchema: wh, NotFoundError: Mi, NotImplementedError: Li, PaginationAfterSchema: Ha, PaginationBeforeSchema: qa, PaginationOffsetSchema: Za, PaginationPageBasedSchema: Ga, PaginationSchema: Ah, Paths: nc, get Permission() {
  return v$1;
}, Role: ue$2, RoleDescriptions: kh, ServiceUnavailableError: qe$2, TemplateLiteral: Nh, TooManyRequestsError: Zi, UnknownError: nt, UnsupportedFileTypeError: ea, Url: Ie$1, ValidationError: Hr, VersionMismatchError: Yi, errorCode: S$1, errorStatus: T$1, extNameToMimeType: Oh, lazy: $h, lazyAsync: Dh, memoize: Fh, mimeTypeMapper: gr, permissionDescriptions: Ih, rolePermissions: Ch }, Symbol.toStringTag, { value: "Module" })), G = (e) => Hh(e), Hh = (e) => (Object.defineProperty(e, Symbol.asyncIterator, { value: () => {
  const t = ct();
  let n, r, s = false;
  const o = async () => n || (n = await xe$2(gt(xf(), t)), n), i = async () => {
    if (r) return r;
    const a = await o();
    return r = await xe$2(gt(vd(e, a), t)), r;
  }, u = async (a) => {
    if (!n) return;
    const l = n;
    n = void 0;
    try {
      await xe$2(gt(jr(l, a), t));
    } catch {
    }
  }, c = { async next() {
    if (s) return { value: void 0, done: true };
    let a;
    try {
      const p = await i();
      a = await Gf(gt(p, t));
    } catch (p) {
      throw s = true, await u(Ht(void 0)), p instanceof b$1 ? p : new nt(Ws(p), { cause: p });
    }
    if (wn(a)) return { value: a.value, done: false };
    const l = An(a.cause);
    if (jt(l)) return s = true, await u(Ht(void 0)), { value: l.success, done: true };
    s = true, await u(a);
    const f = Zh(l.failure);
    throw f instanceof b$1 ? f : new nt(Ws(f), { cause: f });
  }, async return(a) {
    return s || (s = true, await u(Ht(void 0))), { value: a, done: true };
  }, async throw(a) {
    throw s || (s = true, await u(Lr(wf(a)))), a;
  } };
  return c[Symbol.asyncIterator] = () => c, c;
}, enumerable: false, configurable: false, writable: false }), e), Ws = (e) => e instanceof Error ? e.message : typeof e == "string" ? e : "LaikaStream iteration failed", Zh = (e) => {
  for (const t of e.reasons) if (t._tag === "Fail") return t.error;
  for (const t of e.reasons) if (t._tag === "Die") return t.defect;
  return e;
}, ic = (e) => G(Cn(e)), Qh = (e, t) => G(Ri(Di([tt(e)]), Cn(t))), Xh = (e, t) => e.length === 0 ? ic(t) : G(Ri(Di(e.map(tt)), Cn(t))), em = (e) => G(Ui(e)), tm = (e) => ls((t) => ne$1(function* () {
  const { value: n, done: r, recoverableErrors: s } = yield* e;
  if (s) for (const o of s) yield* t.recoverableError(o);
  return yield* t.data(n), r;
})), ls = (e, t) => {
  var _a3;
  const n = (_a3 = t == null ? void 0 : t.capacity) != null ? _a3 : 16;
  return G(Je((r, s) => ne$1(function* () {
    const o = yield* rn(n), u = e({ data: (c) => Ye(_e$3(o, tt(c))), recoverableError: (c) => Ye(_e$3(o, Si(c))), progress: (c) => Ye(_e$3(o, Ti(c))), dataMany: (c) => Ye(ud(o, c.map(tt))) }).pipe(pi({ onSuccess: (c) => sn(o, ui(c)), onFailure: (c) => sn(o, c) }));
    return yield* xt(u, s), Oi(o);
  })));
}, nm = (e, t) => ls((n) => ne$1(function* () {
  let r = e, s = 0, o;
  for (; ; ) {
    yield* n.progress({ stage: "paginate", current: s, message: r === void 0 ? "first page" : `cursor: ${r}` });
    const i = yield* t(r);
    if (i.items.length > 0 && (yield* n.dataMany(i.items)), o = i.done, s += 1, i.nextCursor === void 0) break;
    r = i.nextCursor;
  }
  return o;
})), zn = (e, t) => mi(ne$1(function* () {
  const n = yield* Pi(e);
  for (; ; ) {
    const r = yield* tn(n);
    if (wn(r)) {
      yield* t(r.value);
      continue;
    }
    const s = An(r.cause);
    return jt(s) ? s.success : yield* Jt(s.failure);
  }
})), fs$1 = (e) => {
  const t = [], n = [], r = [];
  return Kt(zn(e, (s) => {
    for (const o of s) Bt(o) ? t.push(o.value) : vn(o) ? n.push(o.error) : r.push(o.progress);
    return Vt;
  }), (s) => ({ data: t, recoverableErrors: n, progress: r, done: s }));
}, ac = (e) => zn(e, () => Vt), rm = (e, t) => {
  const n = [];
  return Kt(zn(e, (r) => ne$1(function* () {
    for (const s of r) Bt(s) ? n.push(s.value) : vn(s) ? yield* t.recoverableError(s.error) : yield* t.progress(s.progress);
  })), (r) => ({ data: n, done: r }));
}, sm = (e) => xe$2(ac(e)), om = (e) => xe$2(fs$1(e)), im = (e) => xe$2(di(fs$1(e))), am = (e, t) => $n(e, (n) => ne$1(function* () {
  for (const r of n) vn(r) && (yield* t(r.error));
})), cm = (e, t) => $n(e, (n) => ne$1(function* () {
  for (const r of n) _i$1(r) && (yield* t(r.progress));
})), um = (e, t) => G(Fi(e, (n) => {
  const r = new Array(n.length);
  for (let s = 0; s < n.length; s++) {
    const o = n[s];
    r[s] = Bt(o) ? tt(t(o.value)) : o;
  }
  return r;
})), lm = (e, t) => G(Fi(e, (n) => {
  const r = new Array(n.length);
  for (let s = 0; s < n.length; s++) r[s] = t(n[s]);
  return r;
})), fm = (e, t) => G(Id(e, (n) => Bt(n) ? t(n.value) : true)), dm = (e, t) => G(Ni(e, t)), pm = Object.freeze(Object.defineProperty({ __proto__: null, drainWithDone: zn, empty: ic, fail: em, filter: fm, fromEffect: tm, make: ls, mapData: um, mapDone: dm, mapElements: lm, paginate: nm, runCollect: fs$1, runCollectForwarding: rm, runDone: ac, runPromise: sm, runPromiseCollect: om, runPromiseResult: im, succeed: Qh, succeedMany: Xh, tapProgress: cm, tapRecoverableErrors: am }, Symbol.toStringTag, { value: "Module" })), hm = h$1.pipe(Ln("StorageFormat")), mm = h$1.pipe(Ln("StorageRepositoryCompatibilityDate"));
dh(h$1);
const cc = A({ supported: O$1(false), description: h$1 }), gm = A({ supported: O$1(true), description: h$1, supportedExtensions: Pn(h$1, A({ format: hm })) }), ym = A({ supported: O$1(true), description: h$1, styles: A({ offset: Qn, page: Qn, cursor: Qn }) }), ds = be$1([cc, ym]);
re$1(A({ compatibilityDate: mm, fileExtensions: be$1([cc, gm]), pagination: ds }));
const Em = h$1.pipe(Ln("AssetsRepositoryCompatibilityDate"));
re$1(A({ compatibilityDate: Em, pagination: ds }));
class Wm {
}
const bm = h$1.pipe(Ln("DocumentsRepositoryCompatibilityDate"));
re$1(A({ compatibilityDate: bm, pagination: ds }));
class Gm {
}
var qm = typeof globalThis < "u" ? globalThis : typeof global < "u" ? global : typeof self < "u" ? self : {};
function Hm(e) {
  return e && e.__esModule && Object.prototype.hasOwnProperty.call(e, "default") ? e.default : e;
}
function me$1(e, t, n) {
  const { [n]: r, ...s } = e;
  return { type: t, id: r, attributes: s };
}
function Te(e, t, n) {
  return { [n]: e.id, ...e.attributes };
}
const uc = (e) => {
  console.error("errorToJsonApiMapper():", e);
  const t = typeof e == "string" ? new nt() : e;
  return typeof e == "object" && e !== null && "_tag" in e && e._tag === "ParseError" ? { errors: e.issues.map((r) => ({ status: "400", title: "Validation Error", code: "validation_error", detail: r.message, source: r.path.length > 0 ? { pointer: "/" + r.path.join("/") } : void 0 })), status: Hr.STATUS } : e instanceof b$1 ? { errors: [{ title: e.title, code: e.code, status: "" + e.status, detail: e.message }], status: e.status } : wu(e) && X$1(e) ? uc(e.failure) : typeof t == "object" && t !== null && ("name" in t && t.name === "NetworkingError" || "name" in t && t.name === "TimeoutError") ? { errors: [{ status: "" + qe$2.STATUS, code: qe$2.CODE, title: qe$2.TITLE, detail: "Cannot connect to a required service." }], status: qe$2.STATUS } : { errors: [{ status: "" + Ge.STATUS, code: Ge.CODE, title: Ge.TITLE, detail: "Internal Server Error" }], status: Ge.STATUS };
}, Zm = (e) => {
  if (e.length !== 0) return e.map((t) => uc(t).errors[0]);
}, Sm = (e) => {
  const t = { ...qh, LaikaError: void 0 }, n = Object.values(t).find((r) => typeof r == "function" && "CODE" in r && r.CODE === e);
  return !n || n === b$1 ? nt : n;
}, Tm = (e) => {
  const t = Sm(e.code);
  return new t(e.detail);
}, Qm = (e) => {
  if (!e || typeof e != "object") return [];
  const t = e.warnings;
  return Array.isArray(t) ? t.filter((n) => !!n && typeof n == "object" && typeof n.code == "string").map(Tm) : [];
}, Xm = (e) => e instanceof b$1, eg = (e) => me$1(e, "published", "key"), tg = (e) => Te(e, "published", "key"), ng = (e) => me$1(e, "published", "key"), rg = (e) => Te(e, "published", "key"), sg = (e) => me$1(e, "published", "key"), og = (e) => me$1(e, "published-summary", "key"), ig = (e) => Te(e, "published-summary", "key"), ag = (e) => me$1(e, "unpublished", "key"), cg = (e) => Te(e, "unpublished", "key"), ug = (e) => me$1(e, "unpublished", "key"), lg = (e) => Te(e, "unpublished", "key"), fg = (e) => me$1(e, "unpublished", "key"), dg = (e) => Te(e, "unpublished", "key"), pg = (e) => me$1(e, "unpublished-summary", "key"), hg = (e) => Te(e, "unpublished-summary", "key"), mg = (e) => me$1(e, "revision", "key"), gg = (e) => Te(e, "revision", "key"), yg = (e) => me$1(e, "revision", "key"), Eg = (e) => Te(e, "revision", "key"), bg = (e) => me$1(e, "revision-summary", "key"), Sg = (e) => Te(e, "revision-summary", "key"), _m = re$1(A({ type: O$1("published"), id: h$1, attributes: A({ type: O$1("published"), status: O$1("published"), content: Pn(h$1, is), createdAt: V$1(h$1), updatedAt: V$1(h$1) }) })), xm = re$1(A({ type: be$1([O$1("published"), O$1("published-summary")]), id: h$1, attributes: A({ type: be$1([O$1("published-summary"), O$1("published")]), status: O$1("published"), createdAt: V$1(h$1), updatedAt: V$1(h$1) }) })), wm = re$1(A({ type: O$1("unpublished"), id: h$1, attributes: A({ type: O$1("unpublished"), status: h$1, content: Pn(h$1, is), createdAt: V$1(h$1), updatedAt: V$1(h$1) }) })), Om = re$1(A({ type: be$1([O$1("unpublished"), O$1("unpublished-summary")]), id: h$1, attributes: A({ type: be$1([O$1("unpublished-summary"), O$1("unpublished")]), status: h$1, createdAt: V$1(h$1), updatedAt: V$1(h$1) }) })), Am = re$1(A({ type: O$1("revision"), id: h$1, attributes: A({ type: O$1("revision"), revision: h$1, content: Pn(h$1, is), createdAt: h$1, updatedAt: V$1(h$1) }) })), km = re$1(A({ type: be$1([O$1("revision"), O$1("revision-summary")]), id: h$1, attributes: A({ type: be$1([O$1("revision-summary"), O$1("revision")]), revision: h$1, createdAt: h$1, updatedAt: V$1(h$1) }) }));
pt(_m);
pt(xm);
pt(wm);
pt(Om);
pt(Am);
pt(km);

const e = [{ name: "posts", label: "Blog Posts", folder: "posts", create: true, slug: "{{slug}}", sortable_fields: ["title", "date"], fields: [{ label: "Title", name: "title", widget: "string" }, { label: "Date", name: "date", widget: "datetime" }, { label: "Description", name: "description", widget: "string", required: false }, { label: "Body", name: "body", widget: "markdown" }] }];

var __defProp$1 = Object.defineProperty;
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __defNormalProp$1 = (obj, key, value) => key in obj ? __defProp$1(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField$1 = (obj, key, value) => __defNormalProp$1(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), member.set(obj, value), value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);
var _n, _i, _r, _e$2, _t, _a2, _c, _l, _s, _o, _u, _AST_instances, y_fn, _AST_static, f_fn, $_fn, h_fn, p_fn, S_fn, m_fn, E_fn, A_fn, b_fn, d_fn, g_fn, w_fn, _Minimatch_instances, n_fn, i_fn, r_fn, _a3;
const runTask$1 = (e5, t) => {
  const { onProgress: n } = {};
  return n ? xe$2(Mn(e5, (r) => {
    for (const s of r) n(s);
    return Vt;
  })) : Vh(e5);
}, collectStream = async (e5, t) => {
  const { onProgress: n } = {};
  if (!n) {
    const o = [], a = await xe$2(zn(e5, (l) => {
      for (const c of l) Bt(c) && o.push(c.value);
      return Vt;
    }));
    return { items: o, done: a };
  }
  const r = [], s = await xe$2(zn(e5, (o) => {
    for (const a of o) Bt(a) ? r.push(a.value) : n(a);
    return Vt;
  }));
  return { items: r, done: s };
}, defaultDetermineExtension = (e5, { metadata: t, defaultExtension: n }) => {
  var _a4;
  return (_a4 = t == null ? void 0 : t.extension) != null ? _a4 : n;
};
class StorageRepository {
}
const pathToSegments = (e5) => e5.split("/").map((n) => n.trim()).filter((n) => n.length > 0), pathCombine = (...e5) => e5.map((n) => n.trim()).filter((n) => n.length > 0).join("/"), basename = (e5) => {
  if (typeof e5 != "string" || e5.length === 0) return "";
  let t = e5.length - 1;
  for (; t > 0 && (e5[t] === "/" || e5[t] === "\\"); ) t--;
  if (t === 0 && (e5[0] === "/" || e5[0] === "\\")) return e5[0];
  let n = t;
  for (; n >= 0 && e5[n] !== "/" && e5[n] !== "\\"; ) n--;
  return e5.slice(n + 1, t + 1);
}, naturalCollator = new Intl.Collator(void 0, { numeric: true, sensitivity: "base" }), naturalCompare = (e5, t) => naturalCollator.compare(e5, t), applyPagination = (e5, t) => {
  var _a4, _b;
  if (!t) return [...e5];
  if ("offset" in t) {
    const n = (_a4 = t.limit) != null ? _a4 : e5.length;
    return e5.slice(t.offset, t.offset + n);
  }
  if ("page" in t) {
    const n = (_b = t.perPage) != null ? _b : e5.length, r = Math.max(0, (t.page - 1) * n);
    return e5.slice(r, r + n);
  }
  return [...e5];
}, liftPromiseResult$1 = (e5) => K(Dm(() => e5), Nm);
function bytesToBase64(e5) {
  let t = "";
  for (let n = 0; n < e5.length; n++) t += String.fromCharCode(e5[n]);
  return btoa(t);
}
async function consumeBinary(e5) {
  if (e5 instanceof Uint8Array) return e5;
  if (e5 instanceof ArrayBuffer) return new Uint8Array(e5);
  const t = e5.getReader(), n = [];
  let r = 0;
  for (; ; ) {
    const { done: a, value: l } = await t.read();
    if (a) break;
    n.push(l), r += l.byteLength;
  }
  const s = new Uint8Array(r);
  let o = 0;
  for (const a of n) s.set(a, o), o += a.byteLength;
  return s;
}
class ContentBaseAssetsRepository extends Wm {
  constructor(t, n) {
    super();
    __publicField$1(this, "storageRepository");
    __publicField$1(this, "settingsProvider");
    this.storageRepository = t, this.settingsProvider = n;
  }
  getCapabilities() {
    return sc((t) => ne$1({ self: this }, function* () {
      const n = yield* zh(this.storageRepository.getCapabilities(), t);
      return { compatibilityDate: Em.make("2026-05-11"), pagination: n.pagination };
    }));
  }
  parseKey(t) {
    const n = pathToSegments(t);
    if (n.length === 0) return { collection: "", remainder: "" };
    const [r, ...s] = n;
    return { collection: r, remainder: s.length > 0 ? pathCombine(...s) : "" };
  }
  async resolveCollection(t) {
    var _a4;
    const n = await this.settingsProvider.getMediaCollectionSettings(t);
    if (X$1(n)) return we$2(n.failure);
    const r = (_a4 = n.success.directory) != null ? _a4 : t;
    return lt({ directory: r, settings: n.success });
  }
  async getAssetPath(t) {
    const { collection: n, remainder: r } = this.parseKey(t);
    if (!n) return we$2(new zi(`Asset key '${t}' is missing a collection prefix`));
    const s = await this.resolveCollection(n);
    if (X$1(s)) return we$2(s.failure);
    const { directory: o, settings: a } = s.success, l = r ? pathCombine(o, r) : o;
    return lt({ physical: l, directory: o, collection: n, settings: a });
  }
  extractKeyFromPath(t, n, r) {
    const s = pathToSegments(t), o = pathToSegments(n);
    let a = s;
    return o.length <= s.length && o.every((l, c) => l === s[c]) && (a = s.slice(o.length)), a.length > 0 ? pathCombine(r, ...a) : r;
  }
  renderUrlTemplate(t, n) {
    const r = pathToSegments(n), s = r.length > 0 ? r[r.length - 1] : n;
    return t.replace(/\{key\}/g, n).replace(/\{filename\}/g, s).replace(/\{path\}/g, n);
  }
  toAssetContent(t) {
    const n = { size: typeof t.size == "number" ? t.size : 0, contentType: typeof t.mimeType == "string" ? t.mimeType : "application/octet-stream" };
    return t.customMetadata && typeof t.customMetadata == "object" && (n.customMetadata = t.customMetadata), n;
  }
  atomToResource(t, n, r) {
    const s = this.extractKeyFromPath(t.key, n, r);
    return t.type === "folder" ? { ...t, key: s } : { type: "asset", key: s, createdAt: t.createdAt, updatedAt: t.updatedAt, content: this.toAssetContent(t.content) };
  }
  summaryToResource(t, n, r) {
    const s = this.extractKeyFromPath(t.key, n, r);
    return t.type === "folder-summary" ? { type: "folder", key: s, createdAt: t.createdAt, updatedAt: t.updatedAt } : { type: "asset", key: s, createdAt: t.createdAt, updatedAt: t.updatedAt, content: {} };
  }
  getResource(t, n) {
    return sc((r) => ne$1({ self: this }, function* () {
      const s = yield* liftPromiseResult$1(this.getAssetPath(t)), o = yield* zh(this.storageRepository.getAtom(s.physical), r);
      return [this.atomToResource(o, s.directory, s.collection)];
    }));
  }
  listResources(t, n) {
    return ls((r) => ne$1({ self: this }, function* () {
      const { collection: s, remainder: o } = this.parseKey(t);
      if (!s) return yield* j(new zi(`folderKey '${t}' is missing a collection prefix`));
      const a = yield* liftPromiseResult$1(this.resolveCollection(s)), l = o ? pathCombine(a.directory, o) : a.directory, c = yield* Kt(rm(this.storageRepository.listAtomSummaries(l, { pagination: n.pagination, depth: n.depth }), r), (p) => p.data);
      let u = 0;
      for (const p of c) yield* r.data(this.summaryToResource(p, a.directory, s)), u += 1;
      return { total: u };
    }));
  }
  getAsset(t, n) {
    return sc((r) => ne$1({ self: this }, function* () {
      const s = yield* liftPromiseResult$1(this.getAssetPath(t)), o = yield* zh(this.storageRepository.getObject(s.physical), r);
      return { type: "asset", key: t, createdAt: o.createdAt, updatedAt: o.updatedAt, content: this.toAssetContent(o.content) };
    }));
  }
  createAsset(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* liftPromiseResult$1(this.getAssetPath(t.key));
      if (r.settings.accept && r.settings.accept.length > 0 && !r.settings.accept.includes(t.mimeType)) return yield* j(new zi(`MIME type '${t.mimeType}' is not allowed in collection '${r.settings.key}'. Allowed: ${r.settings.accept.join(", ")}`));
      const s = yield* Dm(() => consumeBinary(t.content)), o = { data: bytesToBase64(s), mimeType: t.mimeType, size: s.byteLength };
      t.filename && (o.filename = t.filename), t.customMetadata && (o.customMetadata = t.customMetadata), t.cacheControl && (o.cacheControl = t.cacheControl);
      const a = yield* zh(this.storageRepository.createOrUpdateObject({ type: "object", key: r.physical, content: o }), n);
      return { type: "asset", key: t.key, createdAt: a.createdAt, updatedAt: a.updatedAt, content: this.toAssetContent(o) };
    }));
  }
  updateAsset(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* liftPromiseResult$1(this.getAssetPath(t.key)), o = { ...(yield* zh(this.storageRepository.getObject(r.physical), n)).content };
      t.mimeType && (o.mimeType = t.mimeType), t.cacheControl && (o.cacheControl = t.cacheControl), t.customMetadata && (o.customMetadata = t.customMetadata);
      const a = yield* zh(this.storageRepository.updateObject({ key: r.physical, content: o }), n);
      return { type: "asset", key: t.key, createdAt: a.createdAt, updatedAt: a.updatedAt, content: this.toAssetContent(o) };
    }));
  }
  deleteAsset(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* liftPromiseResult$1(this.getAssetPath(t));
      yield* Kt(rm(this.storageRepository.removeAtoms([r.physical]), n), (s) => s.data);
    }));
  }
  deleteAssets(t) {
    return ls((n) => ne$1({ self: this }, function* () {
      var _a4;
      const r = /* @__PURE__ */ new Map(), s = [];
      for (const l of t) {
        const c = yield* di(liftPromiseResult$1(this.getAssetPath(l)));
        if (X$1(c)) {
          yield* n.recoverableError(c.failure);
          continue;
        }
        s.push(c.success.physical), r.set(c.success.physical, l);
      }
      if (s.length === 0) return { removed: 0, skipped: t.length };
      const o = yield* Kt(rm(this.storageRepository.removeAtoms(s), n), (l) => l.data);
      let a = 0;
      for (const l of o) yield* n.data((_a4 = r.get(l)) != null ? _a4 : l), a += 1;
      return { removed: a, skipped: t.length - a };
    }));
  }
  getVariations(t) {
    return ls((n) => ne$1(function* () {
      for (const r of t) yield* n.data({ key: r.key, variations: {} });
      return { total: t.length };
    }));
  }
  getUrls(t) {
    return ls((n) => ne$1({ self: this }, function* () {
      let r = 0;
      for (const s of t) {
        const o = yield* di(liftPromiseResult$1(this.getAssetPath(s.key)));
        if (X$1(o)) {
          yield* n.recoverableError(o.failure);
          continue;
        }
        const a = o.success.settings.url ? this.renderUrlTemplate(o.success.settings.url, s.key) : s.key;
        yield* n.data({ key: s.key, url: a }), r += 1;
      }
      return { total: r };
    }));
  }
  getMetadata(t) {
    return ls((n) => ne$1({ self: this }, function* () {
      let r = 0;
      for (const s of t) {
        const o = yield* di(liftPromiseResult$1(this.getAssetPath(s.key)));
        if (X$1(o)) {
          yield* n.recoverableError(o.failure);
          continue;
        }
        const a = yield* di(zh(this.storageRepository.getObject(o.success.physical), n));
        if (X$1(a)) {
          yield* n.recoverableError(a.failure);
          continue;
        }
        const l = a.success.content, c = { kind: "binary", size: typeof l.size == "number" ? l.size : 0, mimeType: typeof l.mimeType == "string" ? l.mimeType : "application/octet-stream", ...typeof l.filename == "string" ? { filename: l.filename } : {} };
        yield* n.data({ key: s.key, metadata: c }), r += 1;
      }
      return { total: r };
    }));
  }
  getFolder(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* liftPromiseResult$1(this.getAssetPath(t));
      return { ...yield* zh(this.storageRepository.getFolder(r.physical), n), key: t };
    }));
  }
  createFolder(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* liftPromiseResult$1(this.getAssetPath(t.key));
      return { ...yield* zh(this.storageRepository.createFolder({ type: "folder", key: r.physical }), n), key: t.key };
    }));
  }
  deleteFolder(t, n) {
    return sc((r) => ne$1({ self: this }, function* () {
      const s = yield* liftPromiseResult$1(this.getAssetPath(t));
      yield* Kt(rm(this.storageRepository.removeAtoms([s.physical]), r), (o) => o.data);
    }));
  }
}
class ContentBaseSettingsProvider {
}
function failAs(e5) {
  return we$2(e5);
}
async function firstResult$2(e5) {
  return xe$2(di(us(e5)));
}
const startCase = (e5) => e5.charAt(0).toUpperCase() + e5.slice(1), readOnly = (e5) => we$2(new Bi(`${e5} is not supported by DecapContentBaseSettingsProvider \u2014 the Decap config is the source of truth; edit it directly.`)), isFolderCollection = (e5) => typeof e5.folder == "string";
class DecapContentBaseSettingsProvider extends ContentBaseSettingsProvider {
  constructor(t) {
    super();
    __publicField$1(this, "storage");
    __publicField$1(this, "configKey");
    __publicField$1(this, "getSettings", async () => {
      const t = await this.readDecapConfig();
      return X$1(t) ? failAs(t.failure) : lt(this.buildSettings(t.success));
    });
    this.storage = t.storage, this.configKey = t.configKey, firstResult$2(this.storage.getCapabilities()).then(vm).then((n) => {
      n.fileExtensions.supported || console.warn("Underlying storage for DecapContentBaseSettingsProvider does not advertise file-extension support. The Decap config must be served from a path the storage can deserialize.");
    }).catch((n) => {
      console.warn("DecapContentBaseSettingsProvider: getCapabilities probe failed", n);
    });
  }
  async readDecapConfig() {
    const t = await firstResult$2(this.storage.getObject(this.configKey));
    if (X$1(t)) return failAs(t.failure);
    const n = t.success.content;
    return n === null || typeof n != "object" ? we$2(new Bi(`Decap config at '${this.configKey}' did not deserialize to an object`)) : lt(n);
  }
  translateCollection(t) {
    var _a4;
    if (!isFolderCollection(t)) return null;
    const r = t.publish !== false ? { draft: { directory: "draft", name: "Draft" }, pending_review: { directory: "pending_review", name: "Pending Review" }, pending_publish: { directory: "pending_publish", name: "Pending Publish" } } : void 0;
    return { type: "document", key: t.name, name: (_a4 = t.label) != null ? _a4 : startCase(t.name), directory: t.folder, recursive: !!t.nested, ...r ? { unpublishedStatuses: r } : {}, revisionDirectory: `.contentbase/revisions/${t.name}` };
  }
  buildSettings(t) {
    var _a4;
    const n = {};
    for (const r of (_a4 = t.collections) != null ? _a4 : []) {
      const s = this.translateCollection(r);
      s && (n[s.key] = s);
    }
    return { collections: n };
  }
  async getDocumentCollectionSettings(t) {
    var _a4;
    const n = await this.getSettings();
    if (X$1(n)) return failAs(n.failure);
    const r = (_a4 = n.success.collections) == null ? void 0 : _a4[t];
    return r ? r.type !== "document" ? we$2(new Bi(`Settings for document collection '${t}' are of type '${r.type}' not 'document'.`)) : lt(r) : lt(defaultDocumentCollectionSettings(t));
  }
  async getMediaCollectionSettings(t) {
    var _a4;
    const n = await this.readDecapConfig(), r = defaultMediaCollectionSettings(t);
    if (X$1(n)) return lt(r);
    const s = ((_a4 = n.success.public_folder) != null ? _a4 : "").replace(/\/+$/, "");
    return s ? lt({ ...r, url: `${s}/{filename}` }) : lt(r);
  }
  async getCollectionSchema(t) {
    var _a4;
    const n = await this.readDecapConfig();
    if (X$1(n)) return failAs(n.failure);
    const r = ((_a4 = n.success.collections) != null ? _a4 : []).find((s) => s.name === t);
    return r ? isFolderCollection(r) ? lt(decapFieldsToJsonSchema(r.fields)) : we$2(new Bi(`Decap collection '${t}' is a 'files' collection \u2014 its schema isn't a single JSONSchema7.`)) : we$2(new Mi(`No Decap collection '${t}' in config`));
  }
  async putSettings(t) {
    return readOnly("putSettings");
  }
  async putDocumentCollectionSettings(t, n) {
    return readOnly("putDocumentCollectionSettings");
  }
  async putMediaCollectionSettings(t, n) {
    return readOnly("putMediaCollectionSettings");
  }
  async putCollectionSchema(t, n) {
    return readOnly("putCollectionSchema");
  }
}
function decapFieldsToJsonSchema(e5) {
  const t = {}, n = [];
  for (const r of e5) t[r.name] = decapFieldToJsonSchema(r), r.required !== false && n.push(r.name);
  return { type: "object", properties: t, ...n.length > 0 ? { required: n } : {}, additionalProperties: true };
}
function decapFieldToJsonSchema(e5) {
  var _a4, _b, _c2;
  switch ((_a4 = e5.widget) != null ? _a4 : "string") {
    case "string":
    case "text":
    case "markdown":
    case "code":
    case "color":
    case "hidden":
      return { type: "string" };
    case "boolean":
      return { type: "boolean" };
    case "number":
      return e5.value_type === "int" ? { type: "integer" } : { type: "number" };
    case "date":
      return { type: "string", format: "date" };
    case "datetime":
      return { type: "string", format: "date-time" };
    case "object":
      return decapFieldsToJsonSchema((_b = e5.fields) != null ? _b : []);
    case "list": {
      let n;
      e5.fields ? n = decapFieldsToJsonSchema(e5.fields) : e5.field ? n = decapFieldToJsonSchema(e5.field) : n = { type: "string" };
      const r = { type: "array", items: n };
      return typeof e5.min == "number" && (r.minItems = e5.min), typeof e5.max == "number" && (r.maxItems = e5.max), r;
    }
    case "select": {
      const r = ((_c2 = e5.options) != null ? _c2 : []).map((a) => typeof a == "string" || typeof a == "number" ? a : a.value), o = r.every((a) => typeof a == "string") ? { type: "string", enum: r } : { type: ["string", "number"], enum: r };
      return e5.multiple ? { type: "array", items: o } : o;
    }
    case "image":
    case "file":
    case "relation":
      return { type: "string" };
    default:
      return {};
  }
}
const defaultDocumentCollectionSettings = (e5) => ({ type: "document", key: e5, name: startCase(e5), directory: e5, recursive: true, revisionDirectory: `.contentbase/revisions/${e5}` }), defaultMediaCollectionSettings = (e5) => ({ type: "media", key: e5, name: startCase(e5), directory: e5, recursive: true }), liftPromiseResult = (e5) => K(Dm(() => e5), Nm), collectStreamData = (e5, t) => Kt(rm(e5, t), (n) => n.data);
class ContentBaseDocumentsRepository extends Gm {
  constructor(t, n) {
    super();
    __publicField$1(this, "storageRepository");
    __publicField$1(this, "settingsProvider");
    this.storageRepository = t, this.settingsProvider = n;
  }
  getCapabilities() {
    return sc((t) => ne$1({ self: this }, function* () {
      const n = yield* zh(this.storageRepository.getCapabilities(), t);
      return { compatibilityDate: bm.make("2026-05-11"), pagination: n.pagination };
    }));
  }
  parseKey(t) {
    const n = pathToSegments(t);
    if (n.length === 0) return { collection: "", remainder: "" };
    const [r, ...s] = n;
    return { collection: r, remainder: s.length > 0 ? pathCombine(...s) : "" };
  }
  async getDocumentPath(t) {
    var _a4;
    const { collection: n, remainder: r } = this.parseKey(t);
    if (!n) return we$2(new zi(`Document key '${t}' is missing a collection prefix`));
    const s = await this.settingsProvider.getDocumentCollectionSettings(n);
    if (X$1(s)) return we$2(s.failure);
    const o = (_a4 = s.success.directory) != null ? _a4 : n;
    return lt(r ? pathCombine(o, r) : o);
  }
  async getUnpublishedPath(t, n) {
    const { collection: r, remainder: s } = this.parseKey(t);
    if (!r) return we$2(new zi(`Document key '${t}' is missing a collection prefix`));
    const o = await this.settingsProvider.getDocumentCollectionSettings(r);
    if (X$1(o)) return we$2(o.failure);
    const a = o.success.unpublishedStatuses || {}, l = a[n];
    if (!l) return we$2(new zi(`Unknown unpublished status '${n}' for collection '${r}'. Available statuses: ${Object.keys(a).join(", ")}`));
    const c = `.contentbase/${r}/${l.directory}`;
    return lt(s ? pathCombine(c, s) : c);
  }
  async getRevisionPath(t, n) {
    const { collection: r, remainder: s } = this.parseKey(t);
    if (!r) return we$2(new zi(`Document key '${t}' is missing a collection prefix`));
    const o = await this.settingsProvider.getDocumentCollectionSettings(r);
    if (X$1(o)) return we$2(o.failure);
    const a = o.success.revisionDirectory || `.contentbase/${r}/revisions`, l = s ? pathCombine(a, s) : a;
    return lt(n ? pathCombine(l, n) : l);
  }
  extractKeyFromPath(t, n, r) {
    const s = t.startsWith(n) ? t.substring(n.length) : t, o = pathToSegments(s);
    return o.length > 0 ? pathCombine(r, ...o) : r;
  }
  getDocument(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      var _a4;
      const r = yield* liftPromiseResult(this.getDocumentPath(t)), s = yield* zh(this.storageRepository.getObject(r), n);
      return { ...s, key: t, type: "published", language: (_a4 = s.content.language) != null ? _a4 : "und", status: "published" };
    }));
  }
  createDocument(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* liftPromiseResult(this.getDocumentPath(t.key)), s = yield* zh(this.storageRepository.createObject({ type: "object", key: r, content: t.content }), n), o = (/* @__PURE__ */ new Date()).toISOString();
      return { ...s, key: t.key, type: "published", status: "published", language: t.language, createdAt: o, updatedAt: o };
    }));
  }
  updateDocument(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      var _a4;
      const r = yield* liftPromiseResult(this.getDocumentPath(t.key)), s = yield* zh(this.getDocument(t.key), n), o = (_a4 = t.content) != null ? _a4 : s.content;
      return yield* zh(this.storageRepository.updateObject({ key: r, content: o }), n), { ...s, content: o, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    }));
  }
  deleteDocument(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* liftPromiseResult(this.getDocumentPath(t));
      yield* collectStreamData(this.storageRepository.removeAtoms([r]), n);
    }));
  }
  getUnpublished(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      var _a4;
      const { collection: r, remainder: s } = this.parseKey(t);
      if (!r) return yield* j(new zi(`Document key '${t}' is missing a collection prefix`));
      const a = (yield* liftPromiseResult(this.settingsProvider.getDocumentCollectionSettings(r))).unpublishedStatuses || {};
      for (const [l, c] of Object.entries(a)) {
        const u = `.contentbase/${r}/${c.directory}`, p = s ? pathCombine(u, s) : u, f = yield* di(zh(this.storageRepository.getObject(p), n));
        if (jt(f)) return { ...f.success, key: t, type: "unpublished", language: (_a4 = f.success.content.language) != null ? _a4 : "und", status: l };
      }
      return yield* j(new Mi(`Unpublished document '${t}' not found in collection '${r}'`));
    }));
  }
  createUnpublished(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* liftPromiseResult(this.getUnpublishedPath(t.key, t.status)), s = yield* zh(this.storageRepository.createObject({ type: "object", key: r, content: t.content }), n), o = (/* @__PURE__ */ new Date()).toISOString();
      return { ...s, key: t.key, type: "unpublished", status: t.status, language: t.language, createdAt: o, updatedAt: o };
    }));
  }
  updateUnpublished(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* zh(this.getUnpublished(t.key), n), s = t.content || r.content;
      if (t.status && t.status !== r.status) return yield* zh(this.updateUnpublishedStatus(t.key, t.status), n);
      const o = yield* liftPromiseResult(this.getUnpublishedPath(t.key, r.status));
      return yield* zh(this.storageRepository.updateObject({ key: o, content: s }), n), { ...r, content: s, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    }));
  }
  updateUnpublishedStatus(t, n) {
    return sc((r) => ne$1({ self: this }, function* () {
      const s = yield* zh(this.getUnpublished(t), r), o = yield* liftPromiseResult(this.getUnpublishedPath(t, s.status)), a = yield* liftPromiseResult(this.getUnpublishedPath(t, n));
      return yield* zh(this.storageRepository.createObject({ type: "object", key: a, content: s.content }), r), yield* collectStreamData(this.storageRepository.removeAtoms([o]), r), { ...s, status: n, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    }));
  }
  deleteUnpublished(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* zh(this.getUnpublished(t), n), s = yield* liftPromiseResult(this.getUnpublishedPath(t, r.status));
      yield* collectStreamData(this.storageRepository.removeAtoms([s]), n);
    }));
  }
  unpublish(t, n) {
    return sc((r) => ne$1({ self: this }, function* () {
      const s = yield* zh(this.getDocument(t), r), o = yield* liftPromiseResult(this.getDocumentPath(t)), a = yield* liftPromiseResult(this.getUnpublishedPath(t, n));
      return yield* zh(this.storageRepository.createObject({ type: "object", key: a, content: s.content }), r), yield* collectStreamData(this.storageRepository.removeAtoms([o]), r), { key: t, type: "unpublished", status: n, language: s.language, content: s.content, createdAt: s.createdAt, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    }));
  }
  publish(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* zh(this.getUnpublished(t), n), s = yield* liftPromiseResult(this.getUnpublishedPath(t, r.status)), o = yield* liftPromiseResult(this.getDocumentPath(t));
      return yield* zh(this.storageRepository.createObject({ type: "object", key: o, content: r.content }), n), yield* collectStreamData(this.storageRepository.removeAtoms([s]), n), { key: t, type: "published", status: "published", language: r.language, content: r.content, createdAt: r.createdAt, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    }));
  }
  listRecords(t) {
    return this.listRecordsInternal(t, "full");
  }
  listRecordSummaries(t) {
    return this.listRecordsInternal(t, "summary");
  }
  listRecordsInternal(t, n) {
    return ls((r) => ne$1({ self: this }, function* () {
      var _a4;
      if (!t.folder) return yield* j(new zi("listRecords requires `folder` (the collection name) to identify which collection to list"));
      const { collection: s, remainder: o } = this.parseKey(t.folder);
      if (!s) return yield* j(new zi(`folder '${t.folder}' is missing a collection prefix`));
      const a = yield* liftPromiseResult(this.settingsProvider.getDocumentCollectionSettings(s));
      let l = 0;
      if (t.type === "published" || t.type === void 0) {
        const c = (_a4 = a.directory) != null ? _a4 : s, u = o ? pathCombine(c, o) : c, p = { pagination: t.pagination, depth: t.depth };
        if (n === "full") {
          const f = yield* collectStreamData(this.storageRepository.listAtoms(u, p), r);
          for (const h of f) {
            if (h.type !== "object") continue;
            const m = this.extractKeyFromPath(h.key, c, s);
            yield* r.data({ ...h, key: m, type: "published", status: "published" }), l += 1;
          }
        } else {
          const f = yield* collectStreamData(this.storageRepository.listAtomSummaries(u, p), r);
          for (const h of f) {
            if (h.type !== "object-summary") continue;
            const m = this.extractKeyFromPath(h.key, c, s);
            yield* r.data({ ...h, key: m, type: "published-summary", status: "published" }), l += 1;
          }
        }
      }
      if (t.type === "unpublished" || t.type === void 0) {
        const c = a.unpublishedStatuses || {}, u = t.statuses || Object.keys(c);
        for (const p of u) {
          const f = c[p];
          if (!f) continue;
          const h = `.contentbase/${s}/${f.directory}`, m = o ? pathCombine(h, o) : h, g = { pagination: t.pagination, depth: t.depth };
          if (n === "full") {
            const d = yield* di(collectStreamData(this.storageRepository.listAtoms(m, g), r));
            if (X$1(d)) {
              d.failure.code !== Mi.CODE && (yield* r.recoverableError(d.failure));
              continue;
            }
            for (const y of d.success) {
              if (y.type !== "object") continue;
              const S = this.extractKeyFromPath(y.key, h, s);
              yield* r.data({ ...y, key: S, type: "unpublished", status: p }), l += 1;
            }
          } else {
            const d = yield* di(collectStreamData(this.storageRepository.listAtomSummaries(m, g), r));
            if (X$1(d)) {
              d.failure.code !== Mi.CODE && (yield* r.recoverableError(d.failure));
              continue;
            }
            for (const y of d.success) {
              if (y.type !== "object-summary") continue;
              const S = this.extractKeyFromPath(y.key, h, s);
              yield* r.data({ ...y, key: S, type: "unpublished-summary", status: p }), l += 1;
            }
          }
        }
      }
      return { total: l };
    }));
  }
  getRevision(t, n) {
    return sc((r) => ne$1({ self: this }, function* () {
      var _a4;
      const s = yield* liftPromiseResult(this.getRevisionPath(t, n)), o = yield* zh(this.storageRepository.getObject(s), r);
      return o.createdAt ? { ...o, createdAt: o.createdAt, language: (_a4 = o.content.language) != null ? _a4 : "und", revision: n, type: "revision", key: t } : yield* j(new Bi("Revision is missing createdAt date"));
    }));
  }
  createRevision(t) {
    return sc((n) => ne$1({ self: this }, function* () {
      const r = yield* liftPromiseResult(this.getRevisionPath(t.key, t.revision)), s = yield* zh(this.storageRepository.createObject({ type: "object", key: r, content: t.content }), n), o = (/* @__PURE__ */ new Date()).toISOString();
      return { ...s, key: t.key, revision: t.revision, language: t.language, type: "revision", createdAt: o, updatedAt: o };
    }));
  }
  listRevisions(t, n) {
    return ls((r) => ne$1({ self: this }, function* () {
      var _a4;
      const s = yield* liftPromiseResult(this.getRevisionPath(t)), o = yield* collectStreamData(this.storageRepository.listAtoms(s, { pagination: n.pagination, depth: 1 }), r);
      let a = 0;
      for (const l of o) {
        if (l.type !== "object") continue;
        const c = basename(l.key);
        yield* r.data({ ...l, type: "revision-summary", revision: c, language: (_a4 = l.content.language) != null ? _a4 : "und", key: t }), a += 1;
      }
      return { total: a };
    }));
  }
}
var composer$1 = {}, directives$2 = {}, identity$y = {};
const ALIAS = /* @__PURE__ */ Symbol.for("yaml.alias"), DOC = /* @__PURE__ */ Symbol.for("yaml.document"), MAP = /* @__PURE__ */ Symbol.for("yaml.map"), PAIR = /* @__PURE__ */ Symbol.for("yaml.pair"), SCALAR$1 = /* @__PURE__ */ Symbol.for("yaml.scalar"), SEQ = /* @__PURE__ */ Symbol.for("yaml.seq"), NODE_TYPE = /* @__PURE__ */ Symbol.for("yaml.node.type"), isAlias = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE] === ALIAS, isDocument = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE] === DOC, isMap = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE] === MAP, isPair = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE] === PAIR, isScalar$1 = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE] === SCALAR$1, isSeq = (e5) => !!e5 && typeof e5 == "object" && e5[NODE_TYPE] === SEQ;
function isCollection$1(e5) {
  if (e5 && typeof e5 == "object") switch (e5[NODE_TYPE]) {
    case MAP:
    case SEQ:
      return true;
  }
  return false;
}
function isNode(e5) {
  if (e5 && typeof e5 == "object") switch (e5[NODE_TYPE]) {
    case ALIAS:
    case MAP:
    case SCALAR$1:
    case SEQ:
      return true;
  }
  return false;
}
const hasAnchor = (e5) => (isScalar$1(e5) || isCollection$1(e5)) && !!e5.anchor;
identity$y.ALIAS = ALIAS;
identity$y.DOC = DOC;
identity$y.MAP = MAP;
identity$y.NODE_TYPE = NODE_TYPE;
identity$y.PAIR = PAIR;
identity$y.SCALAR = SCALAR$1;
identity$y.SEQ = SEQ;
identity$y.hasAnchor = hasAnchor;
identity$y.isAlias = isAlias;
identity$y.isCollection = isCollection$1;
identity$y.isDocument = isDocument;
identity$y.isMap = isMap;
identity$y.isNode = isNode;
identity$y.isPair = isPair;
identity$y.isScalar = isScalar$1;
identity$y.isSeq = isSeq;
var visit$6 = {}, identity$x = identity$y;
const BREAK$1 = /* @__PURE__ */ Symbol("break visit"), SKIP$1 = /* @__PURE__ */ Symbol("skip children"), REMOVE$1 = /* @__PURE__ */ Symbol("remove node");
function visit$5(e5, t) {
  const n = initVisitor(t);
  identity$x.isDocument(e5) ? visit_(null, e5.contents, n, Object.freeze([e5])) === REMOVE$1 && (e5.contents = null) : visit_(null, e5, n, Object.freeze([]));
}
visit$5.BREAK = BREAK$1;
visit$5.SKIP = SKIP$1;
visit$5.REMOVE = REMOVE$1;
function visit_(e5, t, n, r) {
  const s = callVisitor(e5, t, n, r);
  if (identity$x.isNode(s) || identity$x.isPair(s)) return replaceNode(e5, r, s), visit_(e5, s, n, r);
  if (typeof s != "symbol") {
    if (identity$x.isCollection(t)) {
      r = Object.freeze(r.concat(t));
      for (let o = 0; o < t.items.length; ++o) {
        const a = visit_(o, t.items[o], n, r);
        if (typeof a == "number") o = a - 1;
        else {
          if (a === BREAK$1) return BREAK$1;
          a === REMOVE$1 && (t.items.splice(o, 1), o -= 1);
        }
      }
    } else if (identity$x.isPair(t)) {
      r = Object.freeze(r.concat(t));
      const o = visit_("key", t.key, n, r);
      if (o === BREAK$1) return BREAK$1;
      o === REMOVE$1 && (t.key = null);
      const a = visit_("value", t.value, n, r);
      if (a === BREAK$1) return BREAK$1;
      a === REMOVE$1 && (t.value = null);
    }
  }
  return s;
}
async function visitAsync(e5, t) {
  const n = initVisitor(t);
  identity$x.isDocument(e5) ? await visitAsync_(null, e5.contents, n, Object.freeze([e5])) === REMOVE$1 && (e5.contents = null) : await visitAsync_(null, e5, n, Object.freeze([]));
}
visitAsync.BREAK = BREAK$1;
visitAsync.SKIP = SKIP$1;
visitAsync.REMOVE = REMOVE$1;
async function visitAsync_(e5, t, n, r) {
  const s = await callVisitor(e5, t, n, r);
  if (identity$x.isNode(s) || identity$x.isPair(s)) return replaceNode(e5, r, s), visitAsync_(e5, s, n, r);
  if (typeof s != "symbol") {
    if (identity$x.isCollection(t)) {
      r = Object.freeze(r.concat(t));
      for (let o = 0; o < t.items.length; ++o) {
        const a = await visitAsync_(o, t.items[o], n, r);
        if (typeof a == "number") o = a - 1;
        else {
          if (a === BREAK$1) return BREAK$1;
          a === REMOVE$1 && (t.items.splice(o, 1), o -= 1);
        }
      }
    } else if (identity$x.isPair(t)) {
      r = Object.freeze(r.concat(t));
      const o = await visitAsync_("key", t.key, n, r);
      if (o === BREAK$1) return BREAK$1;
      o === REMOVE$1 && (t.key = null);
      const a = await visitAsync_("value", t.value, n, r);
      if (a === BREAK$1) return BREAK$1;
      a === REMOVE$1 && (t.value = null);
    }
  }
  return s;
}
function initVisitor(e5) {
  return typeof e5 == "object" && (e5.Collection || e5.Node || e5.Value) ? Object.assign({ Alias: e5.Node, Map: e5.Node, Scalar: e5.Node, Seq: e5.Node }, e5.Value && { Map: e5.Value, Scalar: e5.Value, Seq: e5.Value }, e5.Collection && { Map: e5.Collection, Seq: e5.Collection }, e5) : e5;
}
function callVisitor(e5, t, n, r) {
  var _a4, _b, _c2, _d, _e2;
  if (typeof n == "function") return n(e5, t, r);
  if (identity$x.isMap(t)) return (_a4 = n.Map) == null ? void 0 : _a4.call(n, e5, t, r);
  if (identity$x.isSeq(t)) return (_b = n.Seq) == null ? void 0 : _b.call(n, e5, t, r);
  if (identity$x.isPair(t)) return (_c2 = n.Pair) == null ? void 0 : _c2.call(n, e5, t, r);
  if (identity$x.isScalar(t)) return (_d = n.Scalar) == null ? void 0 : _d.call(n, e5, t, r);
  if (identity$x.isAlias(t)) return (_e2 = n.Alias) == null ? void 0 : _e2.call(n, e5, t, r);
}
function replaceNode(e5, t, n) {
  const r = t[t.length - 1];
  if (identity$x.isCollection(r)) r.items[e5] = n;
  else if (identity$x.isPair(r)) e5 === "key" ? r.key = n : r.value = n;
  else if (identity$x.isDocument(r)) r.contents = n;
  else {
    const s = identity$x.isAlias(r) ? "alias" : "scalar";
    throw new Error(`Cannot replace node with ${s} parent`);
  }
}
visit$6.visit = visit$5;
visit$6.visitAsync = visitAsync;
var identity$w = identity$y, visit$4 = visit$6;
const escapeChars = { "!": "%21", ",": "%2C", "[": "%5B", "]": "%5D", "{": "%7B", "}": "%7D" }, escapeTagName = (e5) => e5.replace(/[!,[\]{}]/g, (t) => escapeChars[t]);
class Directives {
  constructor(t, n) {
    this.docStart = null, this.docEnd = false, this.yaml = Object.assign({}, Directives.defaultYaml, t), this.tags = Object.assign({}, Directives.defaultTags, n);
  }
  clone() {
    const t = new Directives(this.yaml, this.tags);
    return t.docStart = this.docStart, t;
  }
  atDocument() {
    const t = new Directives(this.yaml, this.tags);
    switch (this.yaml.version) {
      case "1.1":
        this.atNextDocument = true;
        break;
      case "1.2":
        this.atNextDocument = false, this.yaml = { explicit: Directives.defaultYaml.explicit, version: "1.2" }, this.tags = Object.assign({}, Directives.defaultTags);
        break;
    }
    return t;
  }
  add(t, n) {
    this.atNextDocument && (this.yaml = { explicit: Directives.defaultYaml.explicit, version: "1.1" }, this.tags = Object.assign({}, Directives.defaultTags), this.atNextDocument = false);
    const r = t.trim().split(/[ \t]+/), s = r.shift();
    switch (s) {
      case "%TAG": {
        if (r.length !== 2 && (n(0, "%TAG directive should contain exactly two parts"), r.length < 2)) return false;
        const [o, a] = r;
        return this.tags[o] = a, true;
      }
      case "%YAML": {
        if (this.yaml.explicit = true, r.length !== 1) return n(0, "%YAML directive should contain exactly one part"), false;
        const [o] = r;
        if (o === "1.1" || o === "1.2") return this.yaml.version = o, true;
        {
          const a = /^\d+\.\d+$/.test(o);
          return n(6, `Unsupported YAML version ${o}`, a), false;
        }
      }
      default:
        return n(0, `Unknown directive ${s}`, true), false;
    }
  }
  tagName(t, n) {
    if (t === "!") return "!";
    if (t[0] !== "!") return n(`Not a valid tag: ${t}`), null;
    if (t[1] === "<") {
      const a = t.slice(2, -1);
      return a === "!" || a === "!!" ? (n(`Verbatim tags aren't resolved, so ${t} is invalid.`), null) : (t[t.length - 1] !== ">" && n("Verbatim tags must end with a >"), a);
    }
    const [, r, s] = t.match(/^(.*!)([^!]*)$/s);
    s || n(`The ${t} tag has no suffix`);
    const o = this.tags[r];
    if (o) try {
      return o + decodeURIComponent(s);
    } catch (a) {
      return n(String(a)), null;
    }
    return r === "!" ? t : (n(`Could not resolve tag: ${t}`), null);
  }
  tagString(t) {
    for (const [n, r] of Object.entries(this.tags)) if (t.startsWith(r)) return n + escapeTagName(t.substring(r.length));
    return t[0] === "!" ? t : `!<${t}>`;
  }
  toString(t) {
    const n = this.yaml.explicit ? [`%YAML ${this.yaml.version || "1.2"}`] : [], r = Object.entries(this.tags);
    let s;
    if (t && r.length > 0 && identity$w.isNode(t.contents)) {
      const o = {};
      visit$4.visit(t.contents, (a, l) => {
        identity$w.isNode(l) && l.tag && (o[l.tag] = true);
      }), s = Object.keys(o);
    } else s = [];
    for (const [o, a] of r) o === "!!" && a === "tag:yaml.org,2002:" || (!t || s.some((l) => l.startsWith(a))) && n.push(`%TAG ${o} ${a}`);
    return n.join(`
`);
  }
}
Directives.defaultYaml = { explicit: false, version: "1.2" };
Directives.defaultTags = { "!!": "tag:yaml.org,2002:" };
directives$2.Directives = Directives;
var Document$4 = {}, Alias$4 = {}, anchors$3 = {}, identity$v = identity$y, visit$3 = visit$6;
function anchorIsValid(e5) {
  if (/[\x00-\x19\s,[\]{}]/.test(e5)) {
    const n = `Anchor must not contain whitespace or control characters: ${JSON.stringify(e5)}`;
    throw new Error(n);
  }
  return true;
}
function anchorNames(e5) {
  const t = /* @__PURE__ */ new Set();
  return visit$3.visit(e5, { Value(n, r) {
    r.anchor && t.add(r.anchor);
  } }), t;
}
function findNewAnchor(e5, t) {
  for (let n = 1; ; ++n) {
    const r = `${e5}${n}`;
    if (!t.has(r)) return r;
  }
}
function createNodeAnchors(e5, t) {
  const n = [], r = /* @__PURE__ */ new Map();
  let s = null;
  return { onAnchor: (o) => {
    n.push(o), s != null ? s : s = anchorNames(e5);
    const a = findNewAnchor(t, s);
    return s.add(a), a;
  }, setAnchors: () => {
    for (const o of n) {
      const a = r.get(o);
      if (typeof a == "object" && a.anchor && (identity$v.isScalar(a.node) || identity$v.isCollection(a.node))) a.node.anchor = a.anchor;
      else {
        const l = new Error("Failed to resolve repeated object (this should not happen)");
        throw l.source = o, l;
      }
    }
  }, sourceObjects: r };
}
anchors$3.anchorIsValid = anchorIsValid;
anchors$3.anchorNames = anchorNames;
anchors$3.createNodeAnchors = createNodeAnchors;
anchors$3.findNewAnchor = findNewAnchor;
var Node$3 = {}, applyReviver$3 = {};
function applyReviver$2(e5, t, n, r) {
  if (r && typeof r == "object") if (Array.isArray(r)) for (let s = 0, o = r.length; s < o; ++s) {
    const a = r[s], l = applyReviver$2(e5, r, String(s), a);
    l === void 0 ? delete r[s] : l !== a && (r[s] = l);
  }
  else if (r instanceof Map) for (const s of Array.from(r.keys())) {
    const o = r.get(s), a = applyReviver$2(e5, r, s, o);
    a === void 0 ? r.delete(s) : a !== o && r.set(s, a);
  }
  else if (r instanceof Set) for (const s of Array.from(r)) {
    const o = applyReviver$2(e5, r, s, s);
    o === void 0 ? r.delete(s) : o !== s && (r.delete(s), r.add(o));
  }
  else for (const [s, o] of Object.entries(r)) {
    const a = applyReviver$2(e5, r, s, o);
    a === void 0 ? delete r[s] : a !== o && (r[s] = a);
  }
  return e5.call(t, n, r);
}
applyReviver$3.applyReviver = applyReviver$2;
var toJS$8 = {}, identity$u = identity$y;
function toJS$7(e5, t, n) {
  if (Array.isArray(e5)) return e5.map((r, s) => toJS$7(r, String(s), n));
  if (e5 && typeof e5.toJSON == "function") {
    if (!n || !identity$u.hasAnchor(e5)) return e5.toJSON(t, n);
    const r = { aliasCount: 0, count: 1, res: void 0 };
    n.anchors.set(e5, r), n.onCreate = (o) => {
      r.res = o, delete n.onCreate;
    };
    const s = e5.toJSON(t, n);
    return n.onCreate && n.onCreate(s), s;
  }
  return typeof e5 == "bigint" && !(n == null ? void 0 : n.keep) ? Number(e5) : e5;
}
toJS$8.toJS = toJS$7;
var applyReviver$1 = applyReviver$3, identity$t = identity$y, toJS$6 = toJS$8;
class NodeBase {
  constructor(t) {
    Object.defineProperty(this, identity$t.NODE_TYPE, { value: t });
  }
  clone() {
    const t = Object.create(Object.getPrototypeOf(this), Object.getOwnPropertyDescriptors(this));
    return this.range && (t.range = this.range.slice()), t;
  }
  toJS(t, { mapAsMap: n, maxAliasCount: r, onAnchor: s, reviver: o } = {}) {
    if (!identity$t.isDocument(t)) throw new TypeError("A document argument is required");
    const a = { anchors: /* @__PURE__ */ new Map(), doc: t, keep: true, mapAsMap: n === true, mapKeyWarned: false, maxAliasCount: typeof r == "number" ? r : 100 }, l = toJS$6.toJS(this, "", a);
    if (typeof s == "function") for (const { count: c, res: u } of a.anchors.values()) s(u, c);
    return typeof o == "function" ? applyReviver$1.applyReviver(o, { "": l }, "", l) : l;
  }
}
Node$3.NodeBase = NodeBase;
var anchors$2 = anchors$3, visit$2 = visit$6, identity$s = identity$y, Node$2 = Node$3, toJS$5 = toJS$8;
let Alias$3 = class extends Node$2.NodeBase {
  constructor(t) {
    super(identity$s.ALIAS), this.source = t, Object.defineProperty(this, "tag", { set() {
      throw new Error("Alias nodes cannot have tags");
    } });
  }
  resolve(t, n) {
    if ((n == null ? void 0 : n.maxAliasCount) === 0) throw new ReferenceError("Alias resolution is disabled");
    let r;
    (n == null ? void 0 : n.aliasResolveCache) ? r = n.aliasResolveCache : (r = [], visit$2.visit(t, { Node: (o, a) => {
      (identity$s.isAlias(a) || identity$s.hasAnchor(a)) && r.push(a);
    } }), n && (n.aliasResolveCache = r));
    let s;
    for (const o of r) {
      if (o === this) break;
      o.anchor === this.source && (s = o);
    }
    return s;
  }
  toJSON(t, n) {
    if (!n) return { source: this.source };
    const { anchors: r, doc: s, maxAliasCount: o } = n, a = this.resolve(s, n);
    if (!a) {
      const c = `Unresolved alias (the anchor must be set before the alias): ${this.source}`;
      throw new ReferenceError(c);
    }
    let l = r.get(a);
    if (l || (toJS$5.toJS(a, null, n), l = r.get(a)), (l == null ? void 0 : l.res) === void 0) {
      const c = "This should not happen: Alias anchor was not resolved?";
      throw new ReferenceError(c);
    }
    if (o >= 0 && (l.count += 1, l.aliasCount === 0 && (l.aliasCount = getAliasCount(s, a, r)), l.count * l.aliasCount > o)) {
      const c = "Excessive alias count indicates a resource exhaustion attack";
      throw new ReferenceError(c);
    }
    return l.res;
  }
  toString(t, n, r) {
    const s = `*${this.source}`;
    if (t) {
      if (anchors$2.anchorIsValid(this.source), t.options.verifyAliasOrder && !t.anchors.has(this.source)) {
        const o = `Unresolved alias (the anchor must be set before the alias): ${this.source}`;
        throw new Error(o);
      }
      if (t.implicitKey) return `${s} `;
    }
    return s;
  }
};
function getAliasCount(e5, t, n) {
  if (identity$s.isAlias(t)) {
    const r = t.resolve(e5), s = n && r && n.get(r);
    return s ? s.count * s.aliasCount : 0;
  } else if (identity$s.isCollection(t)) {
    let r = 0;
    for (const s of t.items) {
      const o = getAliasCount(e5, s, n);
      o > r && (r = o);
    }
    return r;
  } else if (identity$s.isPair(t)) {
    const r = getAliasCount(e5, t.key, n), s = getAliasCount(e5, t.value, n);
    return Math.max(r, s);
  }
  return 1;
}
Alias$4.Alias = Alias$3;
var Collection$4 = {}, createNode$5 = {}, Scalar$k = {}, identity$r = identity$y, Node$1 = Node$3, toJS$4 = toJS$8;
const isScalarValue = (e5) => !e5 || typeof e5 != "function" && typeof e5 != "object";
let Scalar$j = class extends Node$1.NodeBase {
  constructor(t) {
    super(identity$r.SCALAR), this.value = t;
  }
  toJSON(t, n) {
    return (n == null ? void 0 : n.keep) ? this.value : toJS$4.toJS(this.value, t, n);
  }
  toString() {
    return String(this.value);
  }
};
Scalar$j.BLOCK_FOLDED = "BLOCK_FOLDED";
Scalar$j.BLOCK_LITERAL = "BLOCK_LITERAL";
Scalar$j.PLAIN = "PLAIN";
Scalar$j.QUOTE_DOUBLE = "QUOTE_DOUBLE";
Scalar$j.QUOTE_SINGLE = "QUOTE_SINGLE";
Scalar$k.Scalar = Scalar$j;
Scalar$k.isScalarValue = isScalarValue;
var Alias$2 = Alias$4, identity$q = identity$y, Scalar$i = Scalar$k;
const defaultTagPrefix = "tag:yaml.org,2002:";
function findTagObject(e5, t, n) {
  var _a4;
  if (t) {
    const r = n.filter((o) => o.tag === t), s = (_a4 = r.find((o) => !o.format)) != null ? _a4 : r[0];
    if (!s) throw new Error(`Tag ${t} not found`);
    return s;
  }
  return n.find((r) => {
    var _a5;
    return ((_a5 = r.identify) == null ? void 0 : _a5.call(r, e5)) && !r.format;
  });
}
function createNode$4(e5, t, n) {
  var _a4, _b, _c2, _d;
  if (identity$q.isDocument(e5) && (e5 = e5.contents), identity$q.isNode(e5)) return e5;
  if (identity$q.isPair(e5)) {
    const f = (_b = (_a4 = n.schema[identity$q.MAP]).createNode) == null ? void 0 : _b.call(_a4, n.schema, null, n);
    return f.items.push(e5), f;
  }
  (e5 instanceof String || e5 instanceof Number || e5 instanceof Boolean || typeof BigInt < "u" && e5 instanceof BigInt) && (e5 = e5.valueOf());
  const { aliasDuplicateObjects: r, onAnchor: s, onTagObj: o, schema: a, sourceObjects: l } = n;
  let c;
  if (r && e5 && typeof e5 == "object") {
    if (c = l.get(e5), c) return (_c2 = c.anchor) != null ? _c2 : c.anchor = s(e5), new Alias$2.Alias(c.anchor);
    c = { anchor: null, node: null }, l.set(e5, c);
  }
  (t == null ? void 0 : t.startsWith("!!")) && (t = defaultTagPrefix + t.slice(2));
  let u = findTagObject(e5, t, a.tags);
  if (!u) {
    if (e5 && typeof e5.toJSON == "function" && (e5 = e5.toJSON()), !e5 || typeof e5 != "object") {
      const f = new Scalar$i.Scalar(e5);
      return c && (c.node = f), f;
    }
    u = e5 instanceof Map ? a[identity$q.MAP] : Symbol.iterator in Object(e5) ? a[identity$q.SEQ] : a[identity$q.MAP];
  }
  o && (o(u), delete n.onTagObj);
  const p = (u == null ? void 0 : u.createNode) ? u.createNode(n.schema, e5, n) : typeof ((_d = u == null ? void 0 : u.nodeClass) == null ? void 0 : _d.from) == "function" ? u.nodeClass.from(n.schema, e5, n) : new Scalar$i.Scalar(e5);
  return t ? p.tag = t : u.default || (p.tag = u.tag), c && (c.node = p), p;
}
createNode$5.createNode = createNode$4;
var createNode$3 = createNode$5, identity$p = identity$y, Node = Node$3;
function collectionFromPath(e5, t, n) {
  let r = n;
  for (let s = t.length - 1; s >= 0; --s) {
    const o = t[s];
    if (typeof o == "number" && Number.isInteger(o) && o >= 0) {
      const a = [];
      a[o] = r, r = a;
    } else r = /* @__PURE__ */ new Map([[o, r]]);
  }
  return createNode$3.createNode(r, void 0, { aliasDuplicateObjects: false, keepUndefined: false, onAnchor: () => {
    throw new Error("This should not happen, please report a bug.");
  }, schema: e5, sourceObjects: /* @__PURE__ */ new Map() });
}
const isEmptyPath = (e5) => e5 == null || typeof e5 == "object" && !!e5[Symbol.iterator]().next().done;
let Collection$3 = class extends Node.NodeBase {
  constructor(t, n) {
    super(t), Object.defineProperty(this, "schema", { value: n, configurable: true, enumerable: false, writable: true });
  }
  clone(t) {
    const n = Object.create(Object.getPrototypeOf(this), Object.getOwnPropertyDescriptors(this));
    return t && (n.schema = t), n.items = n.items.map((r) => identity$p.isNode(r) || identity$p.isPair(r) ? r.clone(t) : r), this.range && (n.range = this.range.slice()), n;
  }
  addIn(t, n) {
    if (isEmptyPath(t)) this.add(n);
    else {
      const [r, ...s] = t, o = this.get(r, true);
      if (identity$p.isCollection(o)) o.addIn(s, n);
      else if (o === void 0 && this.schema) this.set(r, collectionFromPath(this.schema, s, n));
      else throw new Error(`Expected YAML collection at ${r}. Remaining path: ${s}`);
    }
  }
  deleteIn(t) {
    const [n, ...r] = t;
    if (r.length === 0) return this.delete(n);
    const s = this.get(n, true);
    if (identity$p.isCollection(s)) return s.deleteIn(r);
    throw new Error(`Expected YAML collection at ${n}. Remaining path: ${r}`);
  }
  getIn(t, n) {
    const [r, ...s] = t, o = this.get(r, true);
    return s.length === 0 ? !n && identity$p.isScalar(o) ? o.value : o : identity$p.isCollection(o) ? o.getIn(s, n) : void 0;
  }
  hasAllNullValues(t) {
    return this.items.every((n) => {
      if (!identity$p.isPair(n)) return false;
      const r = n.value;
      return r == null || t && identity$p.isScalar(r) && r.value == null && !r.commentBefore && !r.comment && !r.tag;
    });
  }
  hasIn(t) {
    const [n, ...r] = t;
    if (r.length === 0) return this.has(n);
    const s = this.get(n, true);
    return identity$p.isCollection(s) ? s.hasIn(r) : false;
  }
  setIn(t, n) {
    const [r, ...s] = t;
    if (s.length === 0) this.set(r, n);
    else {
      const o = this.get(r, true);
      if (identity$p.isCollection(o)) o.setIn(s, n);
      else if (o === void 0 && this.schema) this.set(r, collectionFromPath(this.schema, s, n));
      else throw new Error(`Expected YAML collection at ${r}. Remaining path: ${s}`);
    }
  }
};
Collection$4.Collection = Collection$3;
Collection$4.collectionFromPath = collectionFromPath;
Collection$4.isEmptyPath = isEmptyPath;
var Pair$8 = {}, stringifyPair$2 = {}, stringify$b = {}, stringifyComment$5 = {};
const stringifyComment$4 = (e5) => e5.replace(/^(?!$)(?: $)?/gm, "#");
function indentComment(e5, t) {
  return /^\n+$/.test(e5) ? e5.substring(1) : t ? e5.replace(/^(?! *$)/gm, t) : e5;
}
const lineComment = (e5, t, n) => e5.endsWith(`
`) ? indentComment(n, t) : n.includes(`
`) ? `
` + indentComment(n, t) : (e5.endsWith(" ") ? "" : " ") + n;
stringifyComment$5.indentComment = indentComment;
stringifyComment$5.lineComment = lineComment;
stringifyComment$5.stringifyComment = stringifyComment$4;
var stringifyString$5 = {}, foldFlowLines$2 = {};
const FOLD_FLOW = "flow", FOLD_BLOCK = "block", FOLD_QUOTED = "quoted";
function foldFlowLines$1(e5, t, n = "flow", { indentAtStart: r, lineWidth: s = 80, minContentWidth: o = 20, onFold: a, onOverflow: l } = {}) {
  if (!s || s < 0) return e5;
  s < o && (o = 0);
  const c = Math.max(1 + o, 1 + s - t.length);
  if (e5.length <= c) return e5;
  const u = [], p = {};
  let f = s - t.length;
  typeof r == "number" && (r > s - Math.max(2, o) ? u.push(0) : f = s - r);
  let h, m, g = false, d = -1, y = -1, S = -1;
  n === FOLD_BLOCK && (d = consumeMoreIndentedLines(e5, d, t.length), d !== -1 && (f = d + c));
  for (let E; E = e5[d += 1]; ) {
    if (n === FOLD_QUOTED && E === "\\") {
      switch (y = d, e5[d + 1]) {
        case "x":
          d += 3;
          break;
        case "u":
          d += 5;
          break;
        case "U":
          d += 9;
          break;
        default:
          d += 1;
      }
      S = d;
    }
    if (E === `
`) n === FOLD_BLOCK && (d = consumeMoreIndentedLines(e5, d, t.length)), f = d + t.length + c, h = void 0;
    else {
      if (E === " " && m && m !== " " && m !== `
` && m !== "	") {
        const b = e5[d + 1];
        b && b !== " " && b !== `
` && b !== "	" && (h = d);
      }
      if (d >= f) if (h) u.push(h), f = h + c, h = void 0;
      else if (n === FOLD_QUOTED) {
        for (; m === " " || m === "	"; ) m = E, E = e5[d += 1], g = true;
        const b = d > S + 1 ? d - 2 : y - 1;
        if (p[b]) return e5;
        u.push(b), p[b] = true, f = b + c, h = void 0;
      } else g = true;
    }
    m = E;
  }
  if (g && l && l(), u.length === 0) return e5;
  a && a();
  let $ = e5.slice(0, u[0]);
  for (let E = 0; E < u.length; ++E) {
    const b = u[E], A = u[E + 1] || e5.length;
    b === 0 ? $ = `
${t}${e5.slice(0, A)}` : (n === FOLD_QUOTED && p[b] && ($ += `${e5[b]}\\`), $ += `
${t}${e5.slice(b + 1, A)}`);
  }
  return $;
}
function consumeMoreIndentedLines(e5, t, n) {
  let r = t, s = t + 1, o = e5[s];
  for (; o === " " || o === "	"; ) if (t < s + n) o = e5[++t];
  else {
    do
      o = e5[++t];
    while (o && o !== `
`);
    r = t, s = t + 1, o = e5[s];
  }
  return r;
}
foldFlowLines$2.FOLD_BLOCK = FOLD_BLOCK;
foldFlowLines$2.FOLD_FLOW = FOLD_FLOW;
foldFlowLines$2.FOLD_QUOTED = FOLD_QUOTED;
foldFlowLines$2.foldFlowLines = foldFlowLines$1;
var Scalar$h = Scalar$k, foldFlowLines = foldFlowLines$2;
const getFoldOptions = (e5, t) => ({ indentAtStart: t ? e5.indent.length : e5.indentAtStart, lineWidth: e5.options.lineWidth, minContentWidth: e5.options.minContentWidth }), containsDocumentMarker = (e5) => /^(%|---|\.\.\.)/m.test(e5);
function lineLengthOverLimit(e5, t, n) {
  if (!t || t < 0) return false;
  const r = t - n, s = e5.length;
  if (s <= r) return false;
  for (let o = 0, a = 0; o < s; ++o) if (e5[o] === `
`) {
    if (o - a > r) return true;
    if (a = o + 1, s - a <= r) return false;
  }
  return true;
}
function doubleQuotedString(e5, t) {
  const n = JSON.stringify(e5);
  if (t.options.doubleQuotedAsJSON) return n;
  const { implicitKey: r } = t, s = t.options.doubleQuotedMinMultiLineLength, o = t.indent || (containsDocumentMarker(e5) ? "  " : "");
  let a = "", l = 0;
  for (let c = 0, u = n[c]; u; u = n[++c]) if (u === " " && n[c + 1] === "\\" && n[c + 2] === "n" && (a += n.slice(l, c) + "\\ ", c += 1, l = c, u = "\\"), u === "\\") switch (n[c + 1]) {
    case "u":
      {
        a += n.slice(l, c);
        const p = n.substr(c + 2, 4);
        switch (p) {
          case "0000":
            a += "\\0";
            break;
          case "0007":
            a += "\\a";
            break;
          case "000b":
            a += "\\v";
            break;
          case "001b":
            a += "\\e";
            break;
          case "0085":
            a += "\\N";
            break;
          case "00a0":
            a += "\\_";
            break;
          case "2028":
            a += "\\L";
            break;
          case "2029":
            a += "\\P";
            break;
          default:
            p.substr(0, 2) === "00" ? a += "\\x" + p.substr(2) : a += n.substr(c, 6);
        }
        c += 5, l = c + 1;
      }
      break;
    case "n":
      if (r || n[c + 2] === '"' || n.length < s) c += 1;
      else {
        for (a += n.slice(l, c) + `

`; n[c + 2] === "\\" && n[c + 3] === "n" && n[c + 4] !== '"'; ) a += `
`, c += 2;
        a += o, n[c + 2] === " " && (a += "\\"), c += 1, l = c + 1;
      }
      break;
    default:
      c += 1;
  }
  return a = l ? a + n.slice(l) : n, r ? a : foldFlowLines.foldFlowLines(a, o, foldFlowLines.FOLD_QUOTED, getFoldOptions(t, false));
}
function singleQuotedString(e5, t) {
  if (t.options.singleQuote === false || t.implicitKey && e5.includes(`
`) || /[ \t]\n|\n[ \t]/.test(e5)) return doubleQuotedString(e5, t);
  const n = t.indent || (containsDocumentMarker(e5) ? "  " : ""), r = "'" + e5.replace(/'/g, "''").replace(/\n+/g, `$&
${n}`) + "'";
  return t.implicitKey ? r : foldFlowLines.foldFlowLines(r, n, foldFlowLines.FOLD_FLOW, getFoldOptions(t, false));
}
function quotedString(e5, t) {
  const { singleQuote: n } = t.options;
  let r;
  if (n === false) r = doubleQuotedString;
  else {
    const s = e5.includes('"'), o = e5.includes("'");
    s && !o ? r = singleQuotedString : o && !s ? r = doubleQuotedString : r = n ? singleQuotedString : doubleQuotedString;
  }
  return r(e5, t);
}
let blockEndNewlines;
try {
  blockEndNewlines = new RegExp(`(^|(?<!
))
+(?!
|$)`, "g");
} catch {
  blockEndNewlines = /\n+(?!\n|$)/g;
}
function blockString({ comment: e5, type: t, value: n }, r, s, o) {
  const { blockQuote: a, commentString: l, lineWidth: c } = r.options;
  if (!a || /\n[\t ]+$/.test(n)) return quotedString(n, r);
  const u = r.indent || (r.forceBlockIndent || containsDocumentMarker(n) ? "  " : ""), p = a === "literal" ? true : a === "folded" || t === Scalar$h.Scalar.BLOCK_FOLDED ? false : t === Scalar$h.Scalar.BLOCK_LITERAL ? true : !lineLengthOverLimit(n, c, u.length);
  if (!n) return p ? `|
` : `>
`;
  let f, h;
  for (h = n.length; h > 0; --h) {
    const A = n[h - 1];
    if (A !== `
` && A !== "	" && A !== " ") break;
  }
  let m = n.substring(h);
  const g = m.indexOf(`
`);
  g === -1 ? f = "-" : n === m || g !== m.length - 1 ? (f = "+", o && o()) : f = "", m && (n = n.slice(0, -m.length), m[m.length - 1] === `
` && (m = m.slice(0, -1)), m = m.replace(blockEndNewlines, `$&${u}`));
  let d = false, y, S = -1;
  for (y = 0; y < n.length; ++y) {
    const A = n[y];
    if (A === " ") d = true;
    else if (A === `
`) S = y;
    else break;
  }
  let $ = n.substring(0, S < y ? S + 1 : y);
  $ && (n = n.substring($.length), $ = $.replace(/\n+/g, `$&${u}`));
  let b = (d ? u ? "2" : "1" : "") + f;
  if (e5 && (b += " " + l(e5.replace(/ ?[\r\n]+/g, " ")), s && s()), !p) {
    const A = n.replace(/\n+/g, `
$&`).replace(/(?:^|\n)([\t ].*)(?:([\n\t ]*)\n(?![\n\t ]))?/g, "$1$2").replace(/\n+/g, `$&${u}`);
    let w = false;
    const v = getFoldOptions(r, true);
    a !== "folded" && t !== Scalar$h.Scalar.BLOCK_FOLDED && (v.onOverflow = () => {
      w = true;
    });
    const C = foldFlowLines.foldFlowLines(`${$}${A}${m}`, u, foldFlowLines.FOLD_BLOCK, v);
    if (!w) return `>${b}
${u}${C}`;
  }
  return n = n.replace(/\n+/g, `$&${u}`), `|${b}
${u}${$}${n}${m}`;
}
function plainString(e5, t, n, r) {
  const { type: s, value: o } = e5, { actualString: a, implicitKey: l, indent: c, indentStep: u, inFlow: p } = t;
  if (l && o.includes(`
`) || p && /[[\]{},]/.test(o)) return quotedString(o, t);
  if (/^[\n\t ,[\]{}#&*!|>'"%@`]|^[?-]$|^[?-][ \t]|[\n:][ \t]|[ \t]\n|[\n\t ]#|[\n\t :]$/.test(o)) return l || p || !o.includes(`
`) ? quotedString(o, t) : blockString(e5, t, n, r);
  if (!l && !p && s !== Scalar$h.Scalar.PLAIN && o.includes(`
`)) return blockString(e5, t, n, r);
  if (containsDocumentMarker(o)) {
    if (c === "") return t.forceBlockIndent = true, blockString(e5, t, n, r);
    if (l && c === u) return quotedString(o, t);
  }
  const f = o.replace(/\n+/g, `$&
${c}`);
  if (a) {
    const h = (d) => {
      var _a4;
      return d.default && d.tag !== "tag:yaml.org,2002:str" && ((_a4 = d.test) == null ? void 0 : _a4.test(f));
    }, { compat: m, tags: g } = t.doc.schema;
    if (g.some(h) || (m == null ? void 0 : m.some(h))) return quotedString(o, t);
  }
  return l ? f : foldFlowLines.foldFlowLines(f, c, foldFlowLines.FOLD_FLOW, getFoldOptions(t, false));
}
function stringifyString$4(e5, t, n, r) {
  const { implicitKey: s, inFlow: o } = t, a = typeof e5.value == "string" ? e5 : Object.assign({}, e5, { value: String(e5.value) });
  let { type: l } = e5;
  l !== Scalar$h.Scalar.QUOTE_DOUBLE && /[\x00-\x08\x0b-\x1f\x7f-\x9f\u{D800}-\u{DFFF}]/u.test(a.value) && (l = Scalar$h.Scalar.QUOTE_DOUBLE);
  const c = (p) => {
    switch (p) {
      case Scalar$h.Scalar.BLOCK_FOLDED:
      case Scalar$h.Scalar.BLOCK_LITERAL:
        return s || o ? quotedString(a.value, t) : blockString(a, t, n, r);
      case Scalar$h.Scalar.QUOTE_DOUBLE:
        return doubleQuotedString(a.value, t);
      case Scalar$h.Scalar.QUOTE_SINGLE:
        return singleQuotedString(a.value, t);
      case Scalar$h.Scalar.PLAIN:
        return plainString(a, t, n, r);
      default:
        return null;
    }
  };
  let u = c(l);
  if (u === null) {
    const { defaultKeyType: p, defaultStringType: f } = t.options, h = s && p || f;
    if (u = c(h), u === null) throw new Error(`Unsupported default string type ${h}`);
  }
  return u;
}
stringifyString$5.stringifyString = stringifyString$4;
var anchors$1 = anchors$3, identity$o = identity$y, stringifyComment$3 = stringifyComment$5, stringifyString$3 = stringifyString$5;
function createStringifyContext(e5, t) {
  const n = Object.assign({ blockQuote: true, commentString: stringifyComment$3.stringifyComment, defaultKeyType: null, defaultStringType: "PLAIN", directives: null, doubleQuotedAsJSON: false, doubleQuotedMinMultiLineLength: 40, falseStr: "false", flowCollectionPadding: true, indentSeq: true, lineWidth: 80, minContentWidth: 20, nullStr: "null", simpleKeys: false, singleQuote: null, trailingComma: false, trueStr: "true", verifyAliasOrder: true }, e5.schema.toStringOptions, t);
  let r;
  switch (n.collectionStyle) {
    case "block":
      r = false;
      break;
    case "flow":
      r = true;
      break;
    default:
      r = null;
  }
  return { anchors: /* @__PURE__ */ new Set(), doc: e5, flowCollectionPadding: n.flowCollectionPadding ? " " : "", indent: "", indentStep: typeof n.indent == "number" ? " ".repeat(n.indent) : "  ", inFlow: r, options: n };
}
function getTagObject(e5, t) {
  var _a4, _b, _c2, _d;
  if (t.tag) {
    const s = e5.filter((o) => o.tag === t.tag);
    if (s.length > 0) return (_a4 = s.find((o) => o.format === t.format)) != null ? _a4 : s[0];
  }
  let n, r;
  if (identity$o.isScalar(t)) {
    r = t.value;
    let s = e5.filter((o) => {
      var _a5;
      return (_a5 = o.identify) == null ? void 0 : _a5.call(o, r);
    });
    if (s.length > 1) {
      const o = s.filter((a) => a.test);
      o.length > 0 && (s = o);
    }
    n = (_b = s.find((o) => o.format === t.format)) != null ? _b : s.find((o) => !o.format);
  } else r = t, n = e5.find((s) => s.nodeClass && r instanceof s.nodeClass);
  if (!n) {
    const s = (_d = (_c2 = r == null ? void 0 : r.constructor) == null ? void 0 : _c2.name) != null ? _d : r === null ? "null" : typeof r;
    throw new Error(`Tag not resolved for ${s} value`);
  }
  return n;
}
function stringifyProps(e5, t, { anchors: n, doc: r }) {
  var _a4;
  if (!r.directives) return "";
  const s = [], o = (identity$o.isScalar(e5) || identity$o.isCollection(e5)) && e5.anchor;
  o && anchors$1.anchorIsValid(o) && (n.add(o), s.push(`&${o}`));
  const a = (_a4 = e5.tag) != null ? _a4 : t.default ? null : t.tag;
  return a && s.push(r.directives.tagString(a)), s.join(" ");
}
function stringify$a(e5, t, n, r) {
  var _a4, _b;
  if (identity$o.isPair(e5)) return e5.toString(t, n, r);
  if (identity$o.isAlias(e5)) {
    if (t.doc.directives) return e5.toString(t);
    if ((_a4 = t.resolvedAliases) == null ? void 0 : _a4.has(e5)) throw new TypeError("Cannot stringify circular structure without alias nodes");
    t.resolvedAliases ? t.resolvedAliases.add(e5) : t.resolvedAliases = /* @__PURE__ */ new Set([e5]), e5 = e5.resolve(t.doc);
  }
  let s;
  const o = identity$o.isNode(e5) ? e5 : t.doc.createNode(e5, { onTagObj: (c) => s = c });
  s != null ? s : s = getTagObject(t.doc.schema.tags, o);
  const a = stringifyProps(o, s, t);
  a.length > 0 && (t.indentAtStart = ((_b = t.indentAtStart) != null ? _b : 0) + a.length + 1);
  const l = typeof s.stringify == "function" ? s.stringify(o, t, n, r) : identity$o.isScalar(o) ? stringifyString$3.stringifyString(o, t, n, r) : o.toString(t, n, r);
  return a ? identity$o.isScalar(o) || l[0] === "{" || l[0] === "[" ? `${a} ${l}` : `${a}
${t.indent}${l}` : l;
}
stringify$b.createStringifyContext = createStringifyContext;
stringify$b.stringify = stringify$a;
var identity$n = identity$y, Scalar$g = Scalar$k, stringify$9 = stringify$b, stringifyComment$2 = stringifyComment$5;
function stringifyPair$1({ key: e5, value: t }, n, r, s) {
  var _a4, _b;
  const { allNullValues: o, doc: a, indent: l, indentStep: c, options: { commentString: u, indentSeq: p, simpleKeys: f } } = n;
  let h = identity$n.isNode(e5) && e5.comment || null;
  if (f) {
    if (h) throw new Error("With simple keys, key nodes cannot have comments");
    if (identity$n.isCollection(e5) || !identity$n.isNode(e5) && typeof e5 == "object") {
      const v = "With simple keys, collection cannot be used as a key value";
      throw new Error(v);
    }
  }
  let m = !f && (!e5 || h && t == null && !n.inFlow || identity$n.isCollection(e5) || (identity$n.isScalar(e5) ? e5.type === Scalar$g.Scalar.BLOCK_FOLDED || e5.type === Scalar$g.Scalar.BLOCK_LITERAL : typeof e5 == "object"));
  n = Object.assign({}, n, { allNullValues: false, implicitKey: !m && (f || !o), indent: l + c });
  let g = false, d = false, y = stringify$9.stringify(e5, n, () => g = true, () => d = true);
  if (!m && !n.inFlow && y.length > 1024) {
    if (f) throw new Error("With simple keys, single line scalar must not span more than 1024 characters");
    m = true;
  }
  if (n.inFlow) {
    if (o || t == null) return g && r && r(), y === "" ? "?" : m ? `? ${y}` : y;
  } else if (o && !f || t == null && m) return y = `? ${y}`, h && !g ? y += stringifyComment$2.lineComment(y, n.indent, u(h)) : d && s && s(), y;
  g && (h = null), m ? (h && (y += stringifyComment$2.lineComment(y, n.indent, u(h))), y = `? ${y}
${l}:`) : (y = `${y}:`, h && (y += stringifyComment$2.lineComment(y, n.indent, u(h))));
  let S, $, E;
  identity$n.isNode(t) ? (S = !!t.spaceBefore, $ = t.commentBefore, E = t.comment) : (S = false, $ = null, E = null, t && typeof t == "object" && (t = a.createNode(t))), n.implicitKey = false, !m && !h && identity$n.isScalar(t) && (n.indentAtStart = y.length + 1), d = false, !p && c.length >= 2 && !n.inFlow && !m && identity$n.isSeq(t) && !t.flow && !t.tag && !t.anchor && (n.indent = n.indent.substring(2));
  let b = false;
  const A = stringify$9.stringify(t, n, () => b = true, () => d = true);
  let w = " ";
  if (h || S || $) {
    if (w = S ? `
` : "", $) {
      const v = u($);
      w += `
${stringifyComment$2.indentComment(v, n.indent)}`;
    }
    A === "" && !n.inFlow ? w === `
` && E && (w = `

`) : w += `
${n.indent}`;
  } else if (!m && identity$n.isCollection(t)) {
    const v = A[0], C = A.indexOf(`
`), T = C !== -1, O = (_b = (_a4 = n.inFlow) != null ? _a4 : t.flow) != null ? _b : t.items.length === 0;
    if (T || !O) {
      let L = false;
      if (T && (v === "&" || v === "!")) {
        let _ = A.indexOf(" ");
        v === "&" && _ !== -1 && _ < C && A[_ + 1] === "!" && (_ = A.indexOf(" ", _ + 1)), (_ === -1 || C < _) && (L = true);
      }
      L || (w = `
${n.indent}`);
    }
  } else (A === "" || A[0] === `
`) && (w = "");
  return y += w + A, n.inFlow ? b && r && r() : E && !b ? y += stringifyComment$2.lineComment(y, n.indent, u(E)) : d && s && s(), y;
}
stringifyPair$2.stringifyPair = stringifyPair$1;
var addPairToJSMap$3 = {}, log$2 = {}, node_process$2 = require$$0;
function debug$1(e5, ...t) {
  e5 === "debug" && console.log(...t);
}
function warn(e5, t) {
  (e5 === "debug" || e5 === "warn") && (typeof node_process$2.emitWarning == "function" ? node_process$2.emitWarning(t) : console.warn(t));
}
log$2.debug = debug$1;
log$2.warn = warn;
var merge$6 = {}, identity$m = identity$y, Scalar$f = Scalar$k;
const MERGE_KEY = "<<", merge$5 = { identify: (e5) => e5 === MERGE_KEY || typeof e5 == "symbol" && e5.description === MERGE_KEY, default: "key", tag: "tag:yaml.org,2002:merge", test: /^<<$/, resolve: () => Object.assign(new Scalar$f.Scalar(Symbol(MERGE_KEY)), { addToJSMap: addMergeToJSMap }), stringify: () => MERGE_KEY }, isMergeKey = (e5, t) => (merge$5.identify(t) || identity$m.isScalar(t) && (!t.type || t.type === Scalar$f.Scalar.PLAIN) && merge$5.identify(t.value)) && (e5 == null ? void 0 : e5.doc.schema.tags.some((n) => n.tag === merge$5.tag && n.default));
function addMergeToJSMap(e5, t, n) {
  const r = resolveAliasValue(e5, n);
  if (identity$m.isSeq(r)) for (const s of r.items) mergeValue(e5, t, s);
  else if (Array.isArray(r)) for (const s of r) mergeValue(e5, t, s);
  else mergeValue(e5, t, r);
}
function mergeValue(e5, t, n) {
  const r = resolveAliasValue(e5, n);
  if (!identity$m.isMap(r)) throw new Error("Merge sources must be maps or map aliases");
  const s = r.toJSON(null, e5, Map);
  for (const [o, a] of s) t instanceof Map ? t.has(o) || t.set(o, a) : t instanceof Set ? t.add(o) : Object.prototype.hasOwnProperty.call(t, o) || Object.defineProperty(t, o, { value: a, writable: true, enumerable: true, configurable: true });
  return t;
}
function resolveAliasValue(e5, t) {
  return e5 && identity$m.isAlias(t) ? t.resolve(e5.doc, e5) : t;
}
merge$6.addMergeToJSMap = addMergeToJSMap;
merge$6.isMergeKey = isMergeKey;
merge$6.merge = merge$5;
var log$1 = log$2, merge$4 = merge$6, stringify$8 = stringify$b, identity$l = identity$y, toJS$3 = toJS$8;
function addPairToJSMap$2(e5, t, { key: n, value: r }) {
  if (identity$l.isNode(n) && n.addToJSMap) n.addToJSMap(e5, t, r);
  else if (merge$4.isMergeKey(e5, n)) merge$4.addMergeToJSMap(e5, t, r);
  else {
    const s = toJS$3.toJS(n, "", e5);
    if (t instanceof Map) t.set(s, toJS$3.toJS(r, s, e5));
    else if (t instanceof Set) t.add(s);
    else {
      const o = stringifyKey(n, s, e5), a = toJS$3.toJS(r, o, e5);
      o in t ? Object.defineProperty(t, o, { value: a, writable: true, enumerable: true, configurable: true }) : t[o] = a;
    }
  }
  return t;
}
function stringifyKey(e5, t, n) {
  if (t === null) return "";
  if (typeof t != "object") return String(t);
  if (identity$l.isNode(e5) && (n == null ? void 0 : n.doc)) {
    const r = stringify$8.createStringifyContext(n.doc, {});
    r.anchors = /* @__PURE__ */ new Set();
    for (const o of n.anchors.keys()) r.anchors.add(o.anchor);
    r.inFlow = true, r.inStringifyKey = true;
    const s = e5.toString(r);
    if (!n.mapKeyWarned) {
      let o = JSON.stringify(s);
      o.length > 40 && (o = o.substring(0, 36) + '..."'), log$1.warn(n.doc.options.logLevel, `Keys with collection values will be stringified due to JS Object restrictions: ${o}. Set mapAsMap: true to use object keys.`), n.mapKeyWarned = true;
    }
    return s;
  }
  return JSON.stringify(t);
}
addPairToJSMap$3.addPairToJSMap = addPairToJSMap$2;
var createNode$2 = createNode$5, stringifyPair = stringifyPair$2, addPairToJSMap$1 = addPairToJSMap$3, identity$k = identity$y;
function createPair(e5, t, n) {
  const r = createNode$2.createNode(e5, void 0, n), s = createNode$2.createNode(t, void 0, n);
  return new Pair$7(r, s);
}
let Pair$7 = class P {
  constructor(t, n = null) {
    Object.defineProperty(this, identity$k.NODE_TYPE, { value: identity$k.PAIR }), this.key = t, this.value = n;
  }
  clone(t) {
    let { key: n, value: r } = this;
    return identity$k.isNode(n) && (n = n.clone(t)), identity$k.isNode(r) && (r = r.clone(t)), new P(n, r);
  }
  toJSON(t, n) {
    const r = (n == null ? void 0 : n.mapAsMap) ? /* @__PURE__ */ new Map() : {};
    return addPairToJSMap$1.addPairToJSMap(n, r, this);
  }
  toString(t, n, r) {
    return (t == null ? void 0 : t.doc) ? stringifyPair.stringifyPair(this, t, n, r) : JSON.stringify(this);
  }
};
Pair$8.Pair = Pair$7;
Pair$8.createPair = createPair;
var Schema$a = {}, map$8 = {}, YAMLMap$8 = {}, stringifyCollection$3 = {}, identity$j = identity$y, stringify$7 = stringify$b, stringifyComment$1 = stringifyComment$5;
function stringifyCollection$2(e5, t, n) {
  var _a4;
  return (((_a4 = t.inFlow) != null ? _a4 : e5.flow) ? stringifyFlowCollection : stringifyBlockCollection)(e5, t, n);
}
function stringifyBlockCollection({ comment: e5, items: t }, n, { blockItemPrefix: r, flowChars: s, itemIndent: o, onChompKeep: a, onComment: l }) {
  const { indent: c, options: { commentString: u } } = n, p = Object.assign({}, n, { indent: o, type: null });
  let f = false;
  const h = [];
  for (let g = 0; g < t.length; ++g) {
    const d = t[g];
    let y = null;
    if (identity$j.isNode(d)) !f && d.spaceBefore && h.push(""), addCommentBefore(n, h, d.commentBefore, f), d.comment && (y = d.comment);
    else if (identity$j.isPair(d)) {
      const $ = identity$j.isNode(d.key) ? d.key : null;
      $ && (!f && $.spaceBefore && h.push(""), addCommentBefore(n, h, $.commentBefore, f));
    }
    f = false;
    let S = stringify$7.stringify(d, p, () => y = null, () => f = true);
    y && (S += stringifyComment$1.lineComment(S, o, u(y))), f && y && (f = false), h.push(r + S);
  }
  let m;
  if (h.length === 0) m = s.start + s.end;
  else {
    m = h[0];
    for (let g = 1; g < h.length; ++g) {
      const d = h[g];
      m += d ? `
${c}${d}` : `
`;
    }
  }
  return e5 ? (m += `
` + stringifyComment$1.indentComment(u(e5), c), l && l()) : f && a && a(), m;
}
function stringifyFlowCollection({ items: e5 }, t, { flowChars: n, itemIndent: r }) {
  const { indent: s, indentStep: o, flowCollectionPadding: a, options: { commentString: l } } = t;
  r += o;
  const c = Object.assign({}, t, { indent: r, inFlow: true, type: null });
  let u = false, p = 0;
  const f = [];
  for (let g = 0; g < e5.length; ++g) {
    const d = e5[g];
    let y = null;
    if (identity$j.isNode(d)) d.spaceBefore && f.push(""), addCommentBefore(t, f, d.commentBefore, false), d.comment && (y = d.comment);
    else if (identity$j.isPair(d)) {
      const $ = identity$j.isNode(d.key) ? d.key : null;
      $ && ($.spaceBefore && f.push(""), addCommentBefore(t, f, $.commentBefore, false), $.comment && (u = true));
      const E = identity$j.isNode(d.value) ? d.value : null;
      E ? (E.comment && (y = E.comment), E.commentBefore && (u = true)) : d.value == null && ($ == null ? void 0 : $.comment) && (y = $.comment);
    }
    y && (u = true);
    let S = stringify$7.stringify(d, c, () => y = null);
    u || (u = f.length > p || S.includes(`
`)), g < e5.length - 1 ? S += "," : t.options.trailingComma && (t.options.lineWidth > 0 && (u || (u = f.reduce(($, E) => $ + E.length + 2, 2) + (S.length + 2) > t.options.lineWidth)), u && (S += ",")), y && (S += stringifyComment$1.lineComment(S, r, l(y))), f.push(S), p = f.length;
  }
  const { start: h, end: m } = n;
  if (f.length === 0) return h + m;
  if (!u) {
    const g = f.reduce((d, y) => d + y.length + 2, 2);
    u = t.options.lineWidth > 0 && g > t.options.lineWidth;
  }
  if (u) {
    let g = h;
    for (const d of f) g += d ? `
${o}${s}${d}` : `
`;
    return `${g}
${s}${m}`;
  } else return `${h}${a}${f.join(" ")}${a}${m}`;
}
function addCommentBefore({ indent: e5, options: { commentString: t } }, n, r, s) {
  if (r && s && (r = r.replace(/^\n+/, "")), r) {
    const o = stringifyComment$1.indentComment(t(r), e5);
    n.push(o.trimStart());
  }
}
stringifyCollection$3.stringifyCollection = stringifyCollection$2;
var stringifyCollection$1 = stringifyCollection$3, addPairToJSMap = addPairToJSMap$3, Collection$2 = Collection$4, identity$i = identity$y, Pair$6 = Pair$8, Scalar$e = Scalar$k;
function findPair(e5, t) {
  const n = identity$i.isScalar(t) ? t.value : t;
  for (const r of e5) if (identity$i.isPair(r) && (r.key === t || r.key === n || identity$i.isScalar(r.key) && r.key.value === n)) return r;
}
let YAMLMap$7 = class extends Collection$2.Collection {
  static get tagName() {
    return "tag:yaml.org,2002:map";
  }
  constructor(t) {
    super(identity$i.MAP, t), this.items = [];
  }
  static from(t, n, r) {
    const { keepUndefined: s, replacer: o } = r, a = new this(t), l = (c, u) => {
      if (typeof o == "function") u = o.call(n, c, u);
      else if (Array.isArray(o) && !o.includes(c)) return;
      (u !== void 0 || s) && a.items.push(Pair$6.createPair(c, u, r));
    };
    if (n instanceof Map) for (const [c, u] of n) l(c, u);
    else if (n && typeof n == "object") for (const c of Object.keys(n)) l(c, n[c]);
    return typeof t.sortMapEntries == "function" && a.items.sort(t.sortMapEntries), a;
  }
  add(t, n) {
    var _a4;
    let r;
    identity$i.isPair(t) ? r = t : !t || typeof t != "object" || !("key" in t) ? r = new Pair$6.Pair(t, t == null ? void 0 : t.value) : r = new Pair$6.Pair(t.key, t.value);
    const s = findPair(this.items, r.key), o = (_a4 = this.schema) == null ? void 0 : _a4.sortMapEntries;
    if (s) {
      if (!n) throw new Error(`Key ${r.key} already set`);
      identity$i.isScalar(s.value) && Scalar$e.isScalarValue(r.value) ? s.value.value = r.value : s.value = r.value;
    } else if (o) {
      const a = this.items.findIndex((l) => o(r, l) < 0);
      a === -1 ? this.items.push(r) : this.items.splice(a, 0, r);
    } else this.items.push(r);
  }
  delete(t) {
    const n = findPair(this.items, t);
    return n ? this.items.splice(this.items.indexOf(n), 1).length > 0 : false;
  }
  get(t, n) {
    var _a4, _b;
    const s = (_a4 = findPair(this.items, t)) == null ? void 0 : _a4.value;
    return (_b = !n && identity$i.isScalar(s) ? s.value : s) != null ? _b : void 0;
  }
  has(t) {
    return !!findPair(this.items, t);
  }
  set(t, n) {
    this.add(new Pair$6.Pair(t, n), true);
  }
  toJSON(t, n, r) {
    const s = r ? new r() : (n == null ? void 0 : n.mapAsMap) ? /* @__PURE__ */ new Map() : {};
    (n == null ? void 0 : n.onCreate) && n.onCreate(s);
    for (const o of this.items) addPairToJSMap.addPairToJSMap(n, s, o);
    return s;
  }
  toString(t, n, r) {
    if (!t) return JSON.stringify(this);
    for (const s of this.items) if (!identity$i.isPair(s)) throw new Error(`Map items must all be pairs; found ${JSON.stringify(s)} instead`);
    return !t.allNullValues && this.hasAllNullValues(false) && (t = Object.assign({}, t, { allNullValues: true })), stringifyCollection$1.stringifyCollection(this, t, { blockItemPrefix: "", flowChars: { start: "{", end: "}" }, itemIndent: t.indent || "", onChompKeep: r, onComment: n });
  }
};
YAMLMap$8.YAMLMap = YAMLMap$7;
YAMLMap$8.findPair = findPair;
var identity$h = identity$y, YAMLMap$6 = YAMLMap$8;
const map$7 = { collection: "map", default: true, nodeClass: YAMLMap$6.YAMLMap, tag: "tag:yaml.org,2002:map", resolve(e5, t) {
  return identity$h.isMap(e5) || t("Expected a mapping for this tag"), e5;
}, createNode: (e5, t, n) => YAMLMap$6.YAMLMap.from(e5, t, n) };
map$8.map = map$7;
var seq$8 = {}, YAMLSeq$7 = {}, createNode$1 = createNode$5, stringifyCollection = stringifyCollection$3, Collection$1 = Collection$4, identity$g = identity$y, Scalar$d = Scalar$k, toJS$2 = toJS$8;
let YAMLSeq$6 = class extends Collection$1.Collection {
  static get tagName() {
    return "tag:yaml.org,2002:seq";
  }
  constructor(t) {
    super(identity$g.SEQ, t), this.items = [];
  }
  add(t) {
    this.items.push(t);
  }
  delete(t) {
    const n = asItemIndex(t);
    return typeof n != "number" ? false : this.items.splice(n, 1).length > 0;
  }
  get(t, n) {
    const r = asItemIndex(t);
    if (typeof r != "number") return;
    const s = this.items[r];
    return !n && identity$g.isScalar(s) ? s.value : s;
  }
  has(t) {
    const n = asItemIndex(t);
    return typeof n == "number" && n < this.items.length;
  }
  set(t, n) {
    const r = asItemIndex(t);
    if (typeof r != "number") throw new Error(`Expected a valid index, not ${t}.`);
    const s = this.items[r];
    identity$g.isScalar(s) && Scalar$d.isScalarValue(n) ? s.value = n : this.items[r] = n;
  }
  toJSON(t, n) {
    const r = [];
    (n == null ? void 0 : n.onCreate) && n.onCreate(r);
    let s = 0;
    for (const o of this.items) r.push(toJS$2.toJS(o, String(s++), n));
    return r;
  }
  toString(t, n, r) {
    return t ? stringifyCollection.stringifyCollection(this, t, { blockItemPrefix: "- ", flowChars: { start: "[", end: "]" }, itemIndent: (t.indent || "") + "  ", onChompKeep: r, onComment: n }) : JSON.stringify(this);
  }
  static from(t, n, r) {
    const { replacer: s } = r, o = new this(t);
    if (n && Symbol.iterator in Object(n)) {
      let a = 0;
      for (let l of n) {
        if (typeof s == "function") {
          const c = n instanceof Set ? l : String(a++);
          l = s.call(n, c, l);
        }
        o.items.push(createNode$1.createNode(l, void 0, r));
      }
    }
    return o;
  }
};
function asItemIndex(e5) {
  let t = identity$g.isScalar(e5) ? e5.value : e5;
  return t && typeof t == "string" && (t = Number(t)), typeof t == "number" && Number.isInteger(t) && t >= 0 ? t : null;
}
YAMLSeq$7.YAMLSeq = YAMLSeq$6;
var identity$f = identity$y, YAMLSeq$5 = YAMLSeq$7;
const seq$7 = { collection: "seq", default: true, nodeClass: YAMLSeq$5.YAMLSeq, tag: "tag:yaml.org,2002:seq", resolve(e5, t) {
  return identity$f.isSeq(e5) || t("Expected a sequence for this tag"), e5;
}, createNode: (e5, t, n) => YAMLSeq$5.YAMLSeq.from(e5, t, n) };
seq$8.seq = seq$7;
var string$5 = {}, stringifyString$2 = stringifyString$5;
const string$4 = { identify: (e5) => typeof e5 == "string", default: true, tag: "tag:yaml.org,2002:str", resolve: (e5) => e5, stringify(e5, t, n, r) {
  return t = Object.assign({ actualString: true }, t), stringifyString$2.stringifyString(e5, t, n, r);
} };
string$5.string = string$4;
var tags$1 = {}, _null$5 = {}, Scalar$c = Scalar$k;
const nullTag = { identify: (e5) => e5 == null, createNode: () => new Scalar$c.Scalar(null), default: true, tag: "tag:yaml.org,2002:null", test: /^(?:~|[Nn]ull|NULL)?$/, resolve: () => new Scalar$c.Scalar(null), stringify: ({ source: e5 }, t) => typeof e5 == "string" && nullTag.test.test(e5) ? e5 : t.options.nullStr };
_null$5.nullTag = nullTag;
var bool$6 = {}, Scalar$b = Scalar$k;
const boolTag = { identify: (e5) => typeof e5 == "boolean", default: true, tag: "tag:yaml.org,2002:bool", test: /^(?:[Tt]rue|TRUE|[Ff]alse|FALSE)$/, resolve: (e5) => new Scalar$b.Scalar(e5[0] === "t" || e5[0] === "T"), stringify({ source: e5, value: t }, n) {
  if (e5 && boolTag.test.test(e5)) {
    const r = e5[0] === "t" || e5[0] === "T";
    if (t === r) return e5;
  }
  return t ? n.options.trueStr : n.options.falseStr;
} };
bool$6.boolTag = boolTag;
var float$8 = {}, stringifyNumber$6 = {};
function stringifyNumber$5({ format: e5, minFractionDigits: t, tag: n, value: r }) {
  if (typeof r == "bigint") return String(r);
  const s = typeof r == "number" ? r : Number(r);
  if (!isFinite(s)) return isNaN(s) ? ".nan" : s < 0 ? "-.inf" : ".inf";
  let o = Object.is(r, -0) ? "-0" : JSON.stringify(r);
  if (!e5 && t && (!n || n === "tag:yaml.org,2002:float") && /^-?\d/.test(o) && !o.includes("e")) {
    let a = o.indexOf(".");
    a < 0 && (a = o.length, o += ".");
    let l = t - (o.length - a - 1);
    for (; l-- > 0; ) o += "0";
  }
  return o;
}
stringifyNumber$6.stringifyNumber = stringifyNumber$5;
var Scalar$a = Scalar$k, stringifyNumber$4 = stringifyNumber$6;
const floatNaN$1 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", test: /^(?:[-+]?\.(?:inf|Inf|INF)|\.nan|\.NaN|\.NAN)$/, resolve: (e5) => e5.slice(-3).toLowerCase() === "nan" ? NaN : e5[0] === "-" ? Number.NEGATIVE_INFINITY : Number.POSITIVE_INFINITY, stringify: stringifyNumber$4.stringifyNumber }, floatExp$1 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", format: "EXP", test: /^[-+]?(?:\.[0-9]+|[0-9]+(?:\.[0-9]*)?)[eE][-+]?[0-9]+$/, resolve: (e5) => parseFloat(e5), stringify(e5) {
  const t = Number(e5.value);
  return isFinite(t) ? t.toExponential() : stringifyNumber$4.stringifyNumber(e5);
} }, float$7 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", test: /^[-+]?(?:\.[0-9]+|[0-9]+\.[0-9]*)$/, resolve(e5) {
  const t = new Scalar$a.Scalar(parseFloat(e5)), n = e5.indexOf(".");
  return n !== -1 && e5[e5.length - 1] === "0" && (t.minFractionDigits = e5.length - n - 1), t;
}, stringify: stringifyNumber$4.stringifyNumber };
float$8.float = float$7;
float$8.floatExp = floatExp$1;
float$8.floatNaN = floatNaN$1;
var int$8 = {}, stringifyNumber$3 = stringifyNumber$6;
const intIdentify$2 = (e5) => typeof e5 == "bigint" || Number.isInteger(e5), intResolve$1 = (e5, t, n, { intAsBigInt: r }) => r ? BigInt(e5) : parseInt(e5.substring(t), n);
function intStringify$1(e5, t, n) {
  const { value: r } = e5;
  return intIdentify$2(r) && r >= 0 ? n + r.toString(t) : stringifyNumber$3.stringifyNumber(e5);
}
const intOct$1 = { identify: (e5) => intIdentify$2(e5) && e5 >= 0, default: true, tag: "tag:yaml.org,2002:int", format: "OCT", test: /^0o[0-7]+$/, resolve: (e5, t, n) => intResolve$1(e5, 2, 8, n), stringify: (e5) => intStringify$1(e5, 8, "0o") }, int$7 = { identify: intIdentify$2, default: true, tag: "tag:yaml.org,2002:int", test: /^[-+]?[0-9]+$/, resolve: (e5, t, n) => intResolve$1(e5, 0, 10, n), stringify: stringifyNumber$3.stringifyNumber }, intHex$1 = { identify: (e5) => intIdentify$2(e5) && e5 >= 0, default: true, tag: "tag:yaml.org,2002:int", format: "HEX", test: /^0x[0-9a-fA-F]+$/, resolve: (e5, t, n) => intResolve$1(e5, 2, 16, n), stringify: (e5) => intStringify$1(e5, 16, "0x") };
int$8.int = int$7;
int$8.intHex = intHex$1;
int$8.intOct = intOct$1;
var schema$8 = {}, map$6 = map$8, _null$4 = _null$5, seq$6 = seq$8, string$3 = string$5, bool$5 = bool$6, float$6 = float$8, int$6 = int$8;
const schema$7 = [map$6.map, seq$6.seq, string$3.string, _null$4.nullTag, bool$5.boolTag, int$6.intOct, int$6.int, int$6.intHex, float$6.floatNaN, float$6.floatExp, float$6.float];
schema$8.schema = schema$7;
var schema$6 = {}, Scalar$9 = Scalar$k, map$5 = map$8, seq$5 = seq$8;
function intIdentify$1(e5) {
  return typeof e5 == "bigint" || Number.isInteger(e5);
}
const stringifyJSON = ({ value: e5 }) => JSON.stringify(e5), jsonScalars = [{ identify: (e5) => typeof e5 == "string", default: true, tag: "tag:yaml.org,2002:str", resolve: (e5) => e5, stringify: stringifyJSON }, { identify: (e5) => e5 == null, createNode: () => new Scalar$9.Scalar(null), default: true, tag: "tag:yaml.org,2002:null", test: /^null$/, resolve: () => null, stringify: stringifyJSON }, { identify: (e5) => typeof e5 == "boolean", default: true, tag: "tag:yaml.org,2002:bool", test: /^true$|^false$/, resolve: (e5) => e5 === "true", stringify: stringifyJSON }, { identify: intIdentify$1, default: true, tag: "tag:yaml.org,2002:int", test: /^-?(?:0|[1-9][0-9]*)$/, resolve: (e5, t, { intAsBigInt: n }) => n ? BigInt(e5) : parseInt(e5, 10), stringify: ({ value: e5 }) => intIdentify$1(e5) ? e5.toString() : JSON.stringify(e5) }, { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", test: /^-?(?:0|[1-9][0-9]*)(?:\.[0-9]*)?(?:[eE][-+]?[0-9]+)?$/, resolve: (e5) => parseFloat(e5), stringify: stringifyJSON }], jsonError = { default: true, tag: "", test: /^/, resolve(e5, t) {
  return t(`Unresolved plain scalar ${JSON.stringify(e5)}`), e5;
} }, schema$5 = [map$5.map, seq$5.seq].concat(jsonScalars, jsonError);
schema$6.schema = schema$5;
var binary$5 = {}, node_buffer = require$$0$1, Scalar$8 = Scalar$k, stringifyString$1 = stringifyString$5;
const binary$4 = { identify: (e5) => e5 instanceof Uint8Array, default: false, tag: "tag:yaml.org,2002:binary", resolve(e5, t) {
  if (typeof node_buffer.Buffer == "function") return node_buffer.Buffer.from(e5, "base64");
  if (typeof atob == "function") {
    const n = atob(e5.replace(/[\n\r]/g, "")), r = new Uint8Array(n.length);
    for (let s = 0; s < n.length; ++s) r[s] = n.charCodeAt(s);
    return r;
  } else return t("This environment does not support reading binary tags; either Buffer or atob is required"), e5;
}, stringify({ comment: e5, type: t, value: n }, r, s, o) {
  if (!n) return "";
  const a = n;
  let l;
  if (typeof node_buffer.Buffer == "function") l = a instanceof node_buffer.Buffer ? a.toString("base64") : node_buffer.Buffer.from(a.buffer).toString("base64");
  else if (typeof btoa == "function") {
    let c = "";
    for (let u = 0; u < a.length; ++u) c += String.fromCharCode(a[u]);
    l = btoa(c);
  } else throw new Error("This environment does not support writing binary tags; either Buffer or btoa is required");
  if (t != null ? t : t = Scalar$8.Scalar.BLOCK_LITERAL, t !== Scalar$8.Scalar.QUOTE_DOUBLE) {
    const c = Math.max(r.options.lineWidth - r.indent.length, r.options.minContentWidth), u = Math.ceil(l.length / c), p = new Array(u);
    for (let f = 0, h = 0; f < u; ++f, h += c) p[f] = l.substr(h, c);
    l = p.join(t === Scalar$8.Scalar.BLOCK_LITERAL ? `
` : " ");
  }
  return stringifyString$1.stringifyString({ comment: e5, type: t, value: l }, r, s, o);
} };
binary$5.binary = binary$4;
var omap$5 = {}, pairs$6 = {}, identity$e = identity$y, Pair$5 = Pair$8, Scalar$7 = Scalar$k, YAMLSeq$4 = YAMLSeq$7;
function resolvePairs(e5, t) {
  var _a4;
  if (identity$e.isSeq(e5)) for (let n = 0; n < e5.items.length; ++n) {
    let r = e5.items[n];
    if (!identity$e.isPair(r)) {
      if (identity$e.isMap(r)) {
        r.items.length > 1 && t("Each pair must have its own sequence indicator");
        const s = r.items[0] || new Pair$5.Pair(new Scalar$7.Scalar(null));
        if (r.commentBefore && (s.key.commentBefore = s.key.commentBefore ? `${r.commentBefore}
${s.key.commentBefore}` : r.commentBefore), r.comment) {
          const o = (_a4 = s.value) != null ? _a4 : s.key;
          o.comment = o.comment ? `${r.comment}
${o.comment}` : r.comment;
        }
        r = s;
      }
      e5.items[n] = identity$e.isPair(r) ? r : new Pair$5.Pair(r);
    }
  }
  else t("Expected a sequence for this tag");
  return e5;
}
function createPairs(e5, t, n) {
  const { replacer: r } = n, s = new YAMLSeq$4.YAMLSeq(e5);
  s.tag = "tag:yaml.org,2002:pairs";
  let o = 0;
  if (t && Symbol.iterator in Object(t)) for (let a of t) {
    typeof r == "function" && (a = r.call(t, String(o++), a));
    let l, c;
    if (Array.isArray(a)) if (a.length === 2) l = a[0], c = a[1];
    else throw new TypeError(`Expected [key, value] tuple: ${a}`);
    else if (a && a instanceof Object) {
      const u = Object.keys(a);
      if (u.length === 1) l = u[0], c = a[l];
      else throw new TypeError(`Expected tuple with one key, not ${u.length} keys`);
    } else l = a;
    s.items.push(Pair$5.createPair(l, c, n));
  }
  return s;
}
const pairs$5 = { collection: "seq", default: false, tag: "tag:yaml.org,2002:pairs", resolve: resolvePairs, createNode: createPairs };
pairs$6.createPairs = createPairs;
pairs$6.pairs = pairs$5;
pairs$6.resolvePairs = resolvePairs;
var identity$d = identity$y, toJS$1 = toJS$8, YAMLMap$5 = YAMLMap$8, YAMLSeq$3 = YAMLSeq$7, pairs$4 = pairs$6;
class YAMLOMap extends YAMLSeq$3.YAMLSeq {
  constructor() {
    super(), this.add = YAMLMap$5.YAMLMap.prototype.add.bind(this), this.delete = YAMLMap$5.YAMLMap.prototype.delete.bind(this), this.get = YAMLMap$5.YAMLMap.prototype.get.bind(this), this.has = YAMLMap$5.YAMLMap.prototype.has.bind(this), this.set = YAMLMap$5.YAMLMap.prototype.set.bind(this), this.tag = YAMLOMap.tag;
  }
  toJSON(t, n) {
    if (!n) return super.toJSON(t);
    const r = /* @__PURE__ */ new Map();
    (n == null ? void 0 : n.onCreate) && n.onCreate(r);
    for (const s of this.items) {
      let o, a;
      if (identity$d.isPair(s) ? (o = toJS$1.toJS(s.key, "", n), a = toJS$1.toJS(s.value, o, n)) : o = toJS$1.toJS(s, "", n), r.has(o)) throw new Error("Ordered maps must not include duplicate keys");
      r.set(o, a);
    }
    return r;
  }
  static from(t, n, r) {
    const s = pairs$4.createPairs(t, n, r), o = new this();
    return o.items = s.items, o;
  }
}
YAMLOMap.tag = "tag:yaml.org,2002:omap";
const omap$4 = { collection: "seq", identify: (e5) => e5 instanceof Map, nodeClass: YAMLOMap, default: false, tag: "tag:yaml.org,2002:omap", resolve(e5, t) {
  const n = pairs$4.resolvePairs(e5, t), r = [];
  for (const { key: s } of n.items) identity$d.isScalar(s) && (r.includes(s.value) ? t(`Ordered maps must not include duplicate keys: ${s.value}`) : r.push(s.value));
  return Object.assign(new YAMLOMap(), n);
}, createNode: (e5, t, n) => YAMLOMap.from(e5, t, n) };
omap$5.YAMLOMap = YAMLOMap;
omap$5.omap = omap$4;
var schema$4 = {}, bool$4 = {}, Scalar$6 = Scalar$k;
function boolStringify({ value: e5, source: t }, n) {
  return t && (e5 ? trueTag : falseTag).test.test(t) ? t : e5 ? n.options.trueStr : n.options.falseStr;
}
const trueTag = { identify: (e5) => e5 === true, default: true, tag: "tag:yaml.org,2002:bool", test: /^(?:Y|y|[Yy]es|YES|[Tt]rue|TRUE|[Oo]n|ON)$/, resolve: () => new Scalar$6.Scalar(true), stringify: boolStringify }, falseTag = { identify: (e5) => e5 === false, default: true, tag: "tag:yaml.org,2002:bool", test: /^(?:N|n|[Nn]o|NO|[Ff]alse|FALSE|[Oo]ff|OFF)$/, resolve: () => new Scalar$6.Scalar(false), stringify: boolStringify };
bool$4.falseTag = falseTag;
bool$4.trueTag = trueTag;
var float$5 = {}, Scalar$5 = Scalar$k, stringifyNumber$2 = stringifyNumber$6;
const floatNaN = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", test: /^(?:[-+]?\.(?:inf|Inf|INF)|\.nan|\.NaN|\.NAN)$/, resolve: (e5) => e5.slice(-3).toLowerCase() === "nan" ? NaN : e5[0] === "-" ? Number.NEGATIVE_INFINITY : Number.POSITIVE_INFINITY, stringify: stringifyNumber$2.stringifyNumber }, floatExp = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", format: "EXP", test: /^[-+]?(?:[0-9][0-9_]*)?(?:\.[0-9_]*)?[eE][-+]?[0-9]+$/, resolve: (e5) => parseFloat(e5.replace(/_/g, "")), stringify(e5) {
  const t = Number(e5.value);
  return isFinite(t) ? t.toExponential() : stringifyNumber$2.stringifyNumber(e5);
} }, float$4 = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", test: /^[-+]?(?:[0-9][0-9_]*)?\.[0-9_]*$/, resolve(e5) {
  const t = new Scalar$5.Scalar(parseFloat(e5.replace(/_/g, ""))), n = e5.indexOf(".");
  if (n !== -1) {
    const r = e5.substring(n + 1).replace(/_/g, "");
    r[r.length - 1] === "0" && (t.minFractionDigits = r.length);
  }
  return t;
}, stringify: stringifyNumber$2.stringifyNumber };
float$5.float = float$4;
float$5.floatExp = floatExp;
float$5.floatNaN = floatNaN;
var int$5 = {}, stringifyNumber$1 = stringifyNumber$6;
const intIdentify = (e5) => typeof e5 == "bigint" || Number.isInteger(e5);
function intResolve(e5, t, n, { intAsBigInt: r }) {
  const s = e5[0];
  if ((s === "-" || s === "+") && (t += 1), e5 = e5.substring(t).replace(/_/g, ""), r) {
    switch (n) {
      case 2:
        e5 = `0b${e5}`;
        break;
      case 8:
        e5 = `0o${e5}`;
        break;
      case 16:
        e5 = `0x${e5}`;
        break;
    }
    const a = BigInt(e5);
    return s === "-" ? BigInt(-1) * a : a;
  }
  const o = parseInt(e5, n);
  return s === "-" ? -1 * o : o;
}
function intStringify(e5, t, n) {
  const { value: r } = e5;
  if (intIdentify(r)) {
    const s = r.toString(t);
    return r < 0 ? "-" + n + s.substr(1) : n + s;
  }
  return stringifyNumber$1.stringifyNumber(e5);
}
const intBin = { identify: intIdentify, default: true, tag: "tag:yaml.org,2002:int", format: "BIN", test: /^[-+]?0b[0-1_]+$/, resolve: (e5, t, n) => intResolve(e5, 2, 2, n), stringify: (e5) => intStringify(e5, 2, "0b") }, intOct = { identify: intIdentify, default: true, tag: "tag:yaml.org,2002:int", format: "OCT", test: /^[-+]?0[0-7_]+$/, resolve: (e5, t, n) => intResolve(e5, 1, 8, n), stringify: (e5) => intStringify(e5, 8, "0") }, int$4 = { identify: intIdentify, default: true, tag: "tag:yaml.org,2002:int", test: /^[-+]?[0-9][0-9_]*$/, resolve: (e5, t, n) => intResolve(e5, 0, 10, n), stringify: stringifyNumber$1.stringifyNumber }, intHex = { identify: intIdentify, default: true, tag: "tag:yaml.org,2002:int", format: "HEX", test: /^[-+]?0x[0-9a-fA-F_]+$/, resolve: (e5, t, n) => intResolve(e5, 2, 16, n), stringify: (e5) => intStringify(e5, 16, "0x") };
int$5.int = int$4;
int$5.intBin = intBin;
int$5.intHex = intHex;
int$5.intOct = intOct;
var set$5 = {}, identity$c = identity$y, Pair$4 = Pair$8, YAMLMap$4 = YAMLMap$8;
class YAMLSet extends YAMLMap$4.YAMLMap {
  constructor(t) {
    super(t), this.tag = YAMLSet.tag;
  }
  add(t) {
    let n;
    identity$c.isPair(t) ? n = t : t && typeof t == "object" && "key" in t && "value" in t && t.value === null ? n = new Pair$4.Pair(t.key, null) : n = new Pair$4.Pair(t, null), YAMLMap$4.findPair(this.items, n.key) || this.items.push(n);
  }
  get(t, n) {
    const r = YAMLMap$4.findPair(this.items, t);
    return !n && identity$c.isPair(r) ? identity$c.isScalar(r.key) ? r.key.value : r.key : r;
  }
  set(t, n) {
    if (typeof n != "boolean") throw new Error(`Expected boolean value for set(key, value) in a YAML set, not ${typeof n}`);
    const r = YAMLMap$4.findPair(this.items, t);
    r && !n ? this.items.splice(this.items.indexOf(r), 1) : !r && n && this.items.push(new Pair$4.Pair(t));
  }
  toJSON(t, n) {
    return super.toJSON(t, n, Set);
  }
  toString(t, n, r) {
    if (!t) return JSON.stringify(this);
    if (this.hasAllNullValues(true)) return super.toString(Object.assign({}, t, { allNullValues: true }), n, r);
    throw new Error("Set items must all have null values");
  }
  static from(t, n, r) {
    const { replacer: s } = r, o = new this(t);
    if (n && Symbol.iterator in Object(n)) for (let a of n) typeof s == "function" && (a = s.call(n, a, a)), o.items.push(Pair$4.createPair(a, null, r));
    return o;
  }
}
YAMLSet.tag = "tag:yaml.org,2002:set";
const set$4 = { collection: "map", identify: (e5) => e5 instanceof Set, nodeClass: YAMLSet, default: false, tag: "tag:yaml.org,2002:set", createNode: (e5, t, n) => YAMLSet.from(e5, t, n), resolve(e5, t) {
  if (identity$c.isMap(e5)) {
    if (e5.hasAllNullValues(true)) return Object.assign(new YAMLSet(), e5);
    t("Set items must all have null values");
  } else t("Expected a mapping for this tag");
  return e5;
} };
set$5.YAMLSet = YAMLSet;
set$5.set = set$4;
var timestamp$5 = {}, stringifyNumber = stringifyNumber$6;
function parseSexagesimal(e5, t) {
  const n = e5[0], r = n === "-" || n === "+" ? e5.substring(1) : e5, s = (a) => t ? BigInt(a) : Number(a), o = r.replace(/_/g, "").split(":").reduce((a, l) => a * s(60) + s(l), s(0));
  return n === "-" ? s(-1) * o : o;
}
function stringifySexagesimal(e5) {
  let { value: t } = e5, n = (a) => a;
  if (typeof t == "bigint") n = (a) => BigInt(a);
  else if (isNaN(t) || !isFinite(t)) return stringifyNumber.stringifyNumber(e5);
  let r = "";
  t < 0 && (r = "-", t *= n(-1));
  const s = n(60), o = [t % s];
  return t < 60 ? o.unshift(0) : (t = (t - o[0]) / s, o.unshift(t % s), t >= 60 && (t = (t - o[0]) / s, o.unshift(t))), r + o.map((a) => String(a).padStart(2, "0")).join(":").replace(/000000\d*$/, "");
}
const intTime = { identify: (e5) => typeof e5 == "bigint" || Number.isInteger(e5), default: true, tag: "tag:yaml.org,2002:int", format: "TIME", test: /^[-+]?[0-9][0-9_]*(?::[0-5]?[0-9])+$/, resolve: (e5, t, { intAsBigInt: n }) => parseSexagesimal(e5, n), stringify: stringifySexagesimal }, floatTime = { identify: (e5) => typeof e5 == "number", default: true, tag: "tag:yaml.org,2002:float", format: "TIME", test: /^[-+]?[0-9][0-9_]*(?::[0-5]?[0-9])+\.[0-9_]*$/, resolve: (e5) => parseSexagesimal(e5, false), stringify: stringifySexagesimal }, timestamp$4 = { identify: (e5) => e5 instanceof Date, default: true, tag: "tag:yaml.org,2002:timestamp", test: RegExp("^([0-9]{4})-([0-9]{1,2})-([0-9]{1,2})(?:(?:t|T|[ \\t]+)([0-9]{1,2}):([0-9]{1,2}):([0-9]{1,2}(\\.[0-9]+)?)(?:[ \\t]*(Z|[-+][012]?[0-9](?::[0-9]{2})?))?)?$"), resolve(e5) {
  const t = e5.match(timestamp$4.test);
  if (!t) throw new Error("!!timestamp expects a date, starting with yyyy-mm-dd");
  const [, n, r, s, o, a, l] = t.map(Number), c = t[7] ? Number((t[7] + "00").substr(1, 3)) : 0;
  let u = Date.UTC(n, r - 1, s, o || 0, a || 0, l || 0, c);
  const p = t[8];
  if (p && p !== "Z") {
    let f = parseSexagesimal(p, false);
    Math.abs(f) < 30 && (f *= 60), u -= 6e4 * f;
  }
  return new Date(u);
}, stringify: ({ value: e5 }) => {
  var _a4;
  return (_a4 = e5 == null ? void 0 : e5.toISOString().replace(/(T00:00:00)?\.000Z$/, "")) != null ? _a4 : "";
} };
timestamp$5.floatTime = floatTime;
timestamp$5.intTime = intTime;
timestamp$5.timestamp = timestamp$4;
var map$4 = map$8, _null$3 = _null$5, seq$4 = seq$8, string$2 = string$5, binary$3 = binary$5, bool$3 = bool$4, float$3 = float$5, int$3 = int$5, merge$3 = merge$6, omap$3 = omap$5, pairs$3 = pairs$6, set$3 = set$5, timestamp$3 = timestamp$5;
const schema$3 = [map$4.map, seq$4.seq, string$2.string, _null$3.nullTag, bool$3.trueTag, bool$3.falseTag, int$3.intBin, int$3.intOct, int$3.int, int$3.intHex, float$3.floatNaN, float$3.floatExp, float$3.float, binary$3.binary, merge$3.merge, omap$3.omap, pairs$3.pairs, set$3.set, timestamp$3.intTime, timestamp$3.floatTime, timestamp$3.timestamp];
schema$4.schema = schema$3;
var map$3 = map$8, _null$2 = _null$5, seq$3 = seq$8, string$1 = string$5, bool$2 = bool$6, float$2 = float$8, int$2 = int$8, schema$2 = schema$8, schema$1$1 = schema$6, binary$2 = binary$5, merge$2 = merge$6, omap$2 = omap$5, pairs$2 = pairs$6, schema$2$1 = schema$4, set$2 = set$5, timestamp$2 = timestamp$5;
const schemas = /* @__PURE__ */ new Map([["core", schema$2.schema], ["failsafe", [map$3.map, seq$3.seq, string$1.string]], ["json", schema$1$1.schema], ["yaml11", schema$2$1.schema], ["yaml-1.1", schema$2$1.schema]]), tagsByName = { binary: binary$2.binary, bool: bool$2.boolTag, float: float$2.float, floatExp: float$2.floatExp, floatNaN: float$2.floatNaN, floatTime: timestamp$2.floatTime, int: int$2.int, intHex: int$2.intHex, intOct: int$2.intOct, intTime: timestamp$2.intTime, map: map$3.map, merge: merge$2.merge, null: _null$2.nullTag, omap: omap$2.omap, pairs: pairs$2.pairs, seq: seq$3.seq, set: set$2.set, timestamp: timestamp$2.timestamp }, coreKnownTags = { "tag:yaml.org,2002:binary": binary$2.binary, "tag:yaml.org,2002:merge": merge$2.merge, "tag:yaml.org,2002:omap": omap$2.omap, "tag:yaml.org,2002:pairs": pairs$2.pairs, "tag:yaml.org,2002:set": set$2.set, "tag:yaml.org,2002:timestamp": timestamp$2.timestamp };
function getTags(e5, t, n) {
  const r = schemas.get(t);
  if (r && !e5) return n && !r.includes(merge$2.merge) ? r.concat(merge$2.merge) : r.slice();
  let s = r;
  if (!s) if (Array.isArray(e5)) s = [];
  else {
    const o = Array.from(schemas.keys()).filter((a) => a !== "yaml11").map((a) => JSON.stringify(a)).join(", ");
    throw new Error(`Unknown schema "${t}"; use one of ${o} or define customTags array`);
  }
  if (Array.isArray(e5)) for (const o of e5) s = s.concat(o);
  else typeof e5 == "function" && (s = e5(s.slice()));
  return n && (s = s.concat(merge$2.merge)), s.reduce((o, a) => {
    const l = typeof a == "string" ? tagsByName[a] : a;
    if (!l) {
      const c = JSON.stringify(a), u = Object.keys(tagsByName).map((p) => JSON.stringify(p)).join(", ");
      throw new Error(`Unknown custom tag ${c}; use one of ${u}`);
    }
    return o.includes(l) || o.push(l), o;
  }, []);
}
tags$1.coreKnownTags = coreKnownTags;
tags$1.getTags = getTags;
var identity$b = identity$y, map$2 = map$8, seq$2 = seq$8, string = string$5, tags = tags$1;
const sortMapEntriesByKey = (e5, t) => e5.key < t.key ? -1 : e5.key > t.key ? 1 : 0;
let Schema$9 = class I {
  constructor({ compat: t, customTags: n, merge: r, resolveKnownTags: s, schema: o, sortMapEntries: a, toStringDefaults: l }) {
    this.compat = Array.isArray(t) ? tags.getTags(t, "compat") : t ? tags.getTags(null, t) : null, this.name = typeof o == "string" && o || "core", this.knownTags = s ? tags.coreKnownTags : {}, this.tags = tags.getTags(n, this.name, r), this.toStringOptions = l != null ? l : null, Object.defineProperty(this, identity$b.MAP, { value: map$2.map }), Object.defineProperty(this, identity$b.SCALAR, { value: string.string }), Object.defineProperty(this, identity$b.SEQ, { value: seq$2.seq }), this.sortMapEntries = typeof a == "function" ? a : a === true ? sortMapEntriesByKey : null;
  }
  clone() {
    const t = Object.create(I.prototype, Object.getOwnPropertyDescriptors(this));
    return t.tags = this.tags.slice(), t;
  }
};
Schema$a.Schema = Schema$9;
var stringifyDocument$2 = {}, identity$a = identity$y, stringify$6 = stringify$b, stringifyComment = stringifyComment$5;
function stringifyDocument$1(e5, t) {
  var _a4;
  const n = [];
  let r = t.directives === true;
  if (t.directives !== false && e5.directives) {
    const c = e5.directives.toString(e5);
    c ? (n.push(c), r = true) : e5.directives.docStart && (r = true);
  }
  r && n.push("---");
  const s = stringify$6.createStringifyContext(e5, t), { commentString: o } = s.options;
  if (e5.commentBefore) {
    n.length !== 1 && n.unshift("");
    const c = o(e5.commentBefore);
    n.unshift(stringifyComment.indentComment(c, ""));
  }
  let a = false, l = null;
  if (e5.contents) {
    if (identity$a.isNode(e5.contents)) {
      if (e5.contents.spaceBefore && r && n.push(""), e5.contents.commentBefore) {
        const p = o(e5.contents.commentBefore);
        n.push(stringifyComment.indentComment(p, ""));
      }
      s.forceBlockIndent = !!e5.comment, l = e5.contents.comment;
    }
    const c = l ? void 0 : () => a = true;
    let u = stringify$6.stringify(e5.contents, s, () => l = null, c);
    l && (u += stringifyComment.lineComment(u, "", o(l))), (u[0] === "|" || u[0] === ">") && n[n.length - 1] === "---" ? n[n.length - 1] = `--- ${u}` : n.push(u);
  } else n.push(stringify$6.stringify(e5.contents, s));
  if ((_a4 = e5.directives) == null ? void 0 : _a4.docEnd) if (e5.comment) {
    const c = o(e5.comment);
    c.includes(`
`) ? (n.push("..."), n.push(stringifyComment.indentComment(c, ""))) : n.push(`... ${c}`);
  } else n.push("...");
  else {
    let c = e5.comment;
    c && a && (c = c.replace(/^\n+/, "")), c && ((!a || l) && n[n.length - 1] !== "" && n.push(""), n.push(stringifyComment.indentComment(o(c), "")));
  }
  return n.join(`
`) + `
`;
}
stringifyDocument$2.stringifyDocument = stringifyDocument$1;
var Alias$1 = Alias$4, Collection = Collection$4, identity$9 = identity$y, Pair$3 = Pair$8, toJS = toJS$8, Schema$8 = Schema$a, stringifyDocument = stringifyDocument$2, anchors = anchors$3, applyReviver = applyReviver$3, createNode = createNode$5, directives$1 = directives$2;
let Document$3 = class F {
  constructor(t, n, r) {
    this.commentBefore = null, this.comment = null, this.errors = [], this.warnings = [], Object.defineProperty(this, identity$9.NODE_TYPE, { value: identity$9.DOC });
    let s = null;
    typeof n == "function" || Array.isArray(n) ? s = n : r === void 0 && n && (r = n, n = void 0);
    const o = Object.assign({ intAsBigInt: false, keepSourceTokens: false, logLevel: "warn", prettyErrors: true, strict: true, stringKeys: false, uniqueKeys: true, version: "1.2" }, r);
    this.options = o;
    let { version: a } = o;
    (r == null ? void 0 : r._directives) ? (this.directives = r._directives.atDocument(), this.directives.yaml.explicit && (a = this.directives.yaml.version)) : this.directives = new directives$1.Directives({ version: a }), this.setSchema(a, r), this.contents = t === void 0 ? null : this.createNode(t, s, r);
  }
  clone() {
    const t = Object.create(F.prototype, { [identity$9.NODE_TYPE]: { value: identity$9.DOC } });
    return t.commentBefore = this.commentBefore, t.comment = this.comment, t.errors = this.errors.slice(), t.warnings = this.warnings.slice(), t.options = Object.assign({}, this.options), this.directives && (t.directives = this.directives.clone()), t.schema = this.schema.clone(), t.contents = identity$9.isNode(this.contents) ? this.contents.clone(t.schema) : this.contents, this.range && (t.range = this.range.slice()), t;
  }
  add(t) {
    assertCollection(this.contents) && this.contents.add(t);
  }
  addIn(t, n) {
    assertCollection(this.contents) && this.contents.addIn(t, n);
  }
  createAlias(t, n) {
    if (!t.anchor) {
      const r = anchors.anchorNames(this);
      t.anchor = !n || r.has(n) ? anchors.findNewAnchor(n || "a", r) : n;
    }
    return new Alias$1.Alias(t.anchor);
  }
  createNode(t, n, r) {
    let s;
    if (typeof n == "function") t = n.call({ "": t }, "", t), s = n;
    else if (Array.isArray(n)) {
      const y = ($) => typeof $ == "number" || $ instanceof String || $ instanceof Number, S = n.filter(y).map(String);
      S.length > 0 && (n = n.concat(S)), s = n;
    } else r === void 0 && n && (r = n, n = void 0);
    const { aliasDuplicateObjects: o, anchorPrefix: a, flow: l, keepUndefined: c, onTagObj: u, tag: p } = r != null ? r : {}, { onAnchor: f, setAnchors: h, sourceObjects: m } = anchors.createNodeAnchors(this, a || "a"), g = { aliasDuplicateObjects: o != null ? o : true, keepUndefined: c != null ? c : false, onAnchor: f, onTagObj: u, replacer: s, schema: this.schema, sourceObjects: m }, d = createNode.createNode(t, p, g);
    return l && identity$9.isCollection(d) && (d.flow = true), h(), d;
  }
  createPair(t, n, r = {}) {
    const s = this.createNode(t, null, r), o = this.createNode(n, null, r);
    return new Pair$3.Pair(s, o);
  }
  delete(t) {
    return assertCollection(this.contents) ? this.contents.delete(t) : false;
  }
  deleteIn(t) {
    return Collection.isEmptyPath(t) ? this.contents == null ? false : (this.contents = null, true) : assertCollection(this.contents) ? this.contents.deleteIn(t) : false;
  }
  get(t, n) {
    return identity$9.isCollection(this.contents) ? this.contents.get(t, n) : void 0;
  }
  getIn(t, n) {
    return Collection.isEmptyPath(t) ? !n && identity$9.isScalar(this.contents) ? this.contents.value : this.contents : identity$9.isCollection(this.contents) ? this.contents.getIn(t, n) : void 0;
  }
  has(t) {
    return identity$9.isCollection(this.contents) ? this.contents.has(t) : false;
  }
  hasIn(t) {
    return Collection.isEmptyPath(t) ? this.contents !== void 0 : identity$9.isCollection(this.contents) ? this.contents.hasIn(t) : false;
  }
  set(t, n) {
    this.contents == null ? this.contents = Collection.collectionFromPath(this.schema, [t], n) : assertCollection(this.contents) && this.contents.set(t, n);
  }
  setIn(t, n) {
    Collection.isEmptyPath(t) ? this.contents = n : this.contents == null ? this.contents = Collection.collectionFromPath(this.schema, Array.from(t), n) : assertCollection(this.contents) && this.contents.setIn(t, n);
  }
  setSchema(t, n = {}) {
    typeof t == "number" && (t = String(t));
    let r;
    switch (t) {
      case "1.1":
        this.directives ? this.directives.yaml.version = "1.1" : this.directives = new directives$1.Directives({ version: "1.1" }), r = { resolveKnownTags: false, schema: "yaml-1.1" };
        break;
      case "1.2":
      case "next":
        this.directives ? this.directives.yaml.version = t : this.directives = new directives$1.Directives({ version: t }), r = { resolveKnownTags: true, schema: "core" };
        break;
      case null:
        this.directives && delete this.directives, r = null;
        break;
      default: {
        const s = JSON.stringify(t);
        throw new Error(`Expected '1.1', '1.2' or null as first argument, but found: ${s}`);
      }
    }
    if (n.schema instanceof Object) this.schema = n.schema;
    else if (r) this.schema = new Schema$8.Schema(Object.assign(r, n));
    else throw new Error("With a null YAML version, the { schema: Schema } option is required");
  }
  toJS({ json: t, jsonArg: n, mapAsMap: r, maxAliasCount: s, onAnchor: o, reviver: a } = {}) {
    const l = { anchors: /* @__PURE__ */ new Map(), doc: this, keep: !t, mapAsMap: r === true, mapKeyWarned: false, maxAliasCount: typeof s == "number" ? s : 100 }, c = toJS.toJS(this.contents, n != null ? n : "", l);
    if (typeof o == "function") for (const { count: u, res: p } of l.anchors.values()) o(p, u);
    return typeof a == "function" ? applyReviver.applyReviver(a, { "": c }, "", c) : c;
  }
  toJSON(t, n) {
    return this.toJS({ json: true, jsonArg: t, mapAsMap: false, onAnchor: n });
  }
  toString(t = {}) {
    if (this.errors.length > 0) throw new Error("Document with errors cannot be stringified");
    if ("indent" in t && (!Number.isInteger(t.indent) || Number(t.indent) <= 0)) {
      const n = JSON.stringify(t.indent);
      throw new Error(`"indent" option must be a positive integer, not ${n}`);
    }
    return stringifyDocument.stringifyDocument(this, t);
  }
};
function assertCollection(e5) {
  if (identity$9.isCollection(e5)) return true;
  throw new Error("Expected a YAML collection as document contents");
}
Document$4.Document = Document$3;
var errors$4 = {};
class YAMLError extends Error {
  constructor(t, n, r, s) {
    super(), this.name = t, this.code = r, this.message = s, this.pos = n;
  }
}
class YAMLParseError extends YAMLError {
  constructor(t, n, r) {
    super("YAMLParseError", t, n, r);
  }
}
class YAMLWarning extends YAMLError {
  constructor(t, n, r) {
    super("YAMLWarning", t, n, r);
  }
}
const prettifyError = (e5, t) => (n) => {
  if (n.pos[0] === -1) return;
  n.linePos = n.pos.map((l) => t.linePos(l));
  const { line: r, col: s } = n.linePos[0];
  n.message += ` at line ${r}, column ${s}`;
  let o = s - 1, a = e5.substring(t.lineStarts[r - 1], t.lineStarts[r]).replace(/[\n\r]+$/, "");
  if (o >= 60 && a.length > 80) {
    const l = Math.min(o - 39, a.length - 79);
    a = "\u2026" + a.substring(l), o -= l - 1;
  }
  if (a.length > 80 && (a = a.substring(0, 79) + "\u2026"), r > 1 && /^ *$/.test(a.substring(0, o))) {
    let l = e5.substring(t.lineStarts[r - 2], t.lineStarts[r - 1]);
    l.length > 80 && (l = l.substring(0, 79) + `\u2026
`), a = l + a;
  }
  if (/[^ ]/.test(a)) {
    let l = 1;
    const c = n.linePos[1];
    (c == null ? void 0 : c.line) === r && c.col > s && (l = Math.max(1, Math.min(c.col - s, 80 - o)));
    const u = " ".repeat(o) + "^".repeat(l);
    n.message += `:

${a}
${u}
`;
  }
};
errors$4.YAMLError = YAMLError;
errors$4.YAMLParseError = YAMLParseError;
errors$4.YAMLWarning = YAMLWarning;
errors$4.prettifyError = prettifyError;
var composeDoc$2 = {}, composeNode$4 = {}, composeCollection$2 = {}, resolveBlockMap$2 = {}, resolveProps$5 = {};
function resolveProps$4(e5, { flow: t, indicator: n, next: r, offset: s, onError: o, parentIndent: a, startOnNewline: l }) {
  let c = false, u = l, p = l, f = "", h = "", m = false, g = false, d = null, y = null, S = null, $ = null, E = null, b = null, A = null;
  for (const C of e5) switch (g && (C.type !== "space" && C.type !== "newline" && C.type !== "comma" && o(C.offset, "MISSING_CHAR", "Tags and anchors must be separated from the next token by white space"), g = false), d && (u && C.type !== "comment" && C.type !== "newline" && o(d, "TAB_AS_INDENT", "Tabs are not allowed as indentation"), d = null), C.type) {
    case "space":
      !t && (n !== "doc-start" || (r == null ? void 0 : r.type) !== "flow-collection") && C.source.includes("	") && (d = C), p = true;
      break;
    case "comment": {
      p || o(C, "MISSING_CHAR", "Comments must be separated from other tokens by white space characters");
      const T = C.source.substring(1) || " ";
      f ? f += h + T : f = T, h = "", u = false;
      break;
    }
    case "newline":
      u ? f ? f += C.source : (!b || n !== "seq-item-ind") && (c = true) : h += C.source, u = true, m = true, (y || S) && ($ = C), p = true;
      break;
    case "anchor":
      y && o(C, "MULTIPLE_ANCHORS", "A node can have at most one anchor"), C.source.endsWith(":") && o(C.offset + C.source.length - 1, "BAD_ALIAS", "Anchor ending in : is ambiguous", true), y = C, A != null ? A : A = C.offset, u = false, p = false, g = true;
      break;
    case "tag": {
      S && o(C, "MULTIPLE_TAGS", "A node can have at most one tag"), S = C, A != null ? A : A = C.offset, u = false, p = false, g = true;
      break;
    }
    case n:
      (y || S) && o(C, "BAD_PROP_ORDER", `Anchors and tags must be after the ${C.source} indicator`), b && o(C, "UNEXPECTED_TOKEN", `Unexpected ${C.source} in ${t != null ? t : "collection"}`), b = C, u = n === "seq-item-ind" || n === "explicit-key-ind", p = false;
      break;
    case "comma":
      if (t) {
        E && o(C, "UNEXPECTED_TOKEN", `Unexpected , in ${t}`), E = C, u = false, p = false;
        break;
      }
    default:
      o(C, "UNEXPECTED_TOKEN", `Unexpected ${C.type} token`), u = false, p = false;
  }
  const w = e5[e5.length - 1], v = w ? w.offset + w.source.length : s;
  return g && r && r.type !== "space" && r.type !== "newline" && r.type !== "comma" && (r.type !== "scalar" || r.source !== "") && o(r.offset, "MISSING_CHAR", "Tags and anchors must be separated from the next token by white space"), d && (u && d.indent <= a || (r == null ? void 0 : r.type) === "block-map" || (r == null ? void 0 : r.type) === "block-seq") && o(d, "TAB_AS_INDENT", "Tabs are not allowed as indentation"), { comma: E, found: b, spaceBefore: c, comment: f, hasNewline: m, anchor: y, tag: S, newlineAfterProp: $, end: v, start: A != null ? A : v };
}
resolveProps$5.resolveProps = resolveProps$4;
var utilContainsNewline$3 = {};
function containsNewline(e5) {
  if (!e5) return null;
  switch (e5.type) {
    case "alias":
    case "scalar":
    case "double-quoted-scalar":
    case "single-quoted-scalar":
      if (e5.source.includes(`
`)) return true;
      if (e5.end) {
        for (const t of e5.end) if (t.type === "newline") return true;
      }
      return false;
    case "flow-collection":
      for (const t of e5.items) {
        for (const n of t.start) if (n.type === "newline") return true;
        if (t.sep) {
          for (const n of t.sep) if (n.type === "newline") return true;
        }
        if (containsNewline(t.key) || containsNewline(t.value)) return true;
      }
      return false;
    default:
      return true;
  }
}
utilContainsNewline$3.containsNewline = containsNewline;
var utilFlowIndentCheck$2 = {}, utilContainsNewline$2 = utilContainsNewline$3;
function flowIndentCheck(e5, t, n) {
  if ((t == null ? void 0 : t.type) === "flow-collection") {
    const r = t.end[0];
    r.indent === e5 && (r.source === "]" || r.source === "}") && utilContainsNewline$2.containsNewline(t) && n(r, "BAD_INDENT", "Flow end indicator should be more indented than parent", true);
  }
}
utilFlowIndentCheck$2.flowIndentCheck = flowIndentCheck;
var utilMapIncludes$2 = {}, identity$8 = identity$y;
function mapIncludes(e5, t, n) {
  const { uniqueKeys: r } = e5.options;
  if (r === false) return false;
  const s = typeof r == "function" ? r : (o, a) => o === a || identity$8.isScalar(o) && identity$8.isScalar(a) && o.value === a.value;
  return t.some((o) => s(o.key, n));
}
utilMapIncludes$2.mapIncludes = mapIncludes;
var Pair$2 = Pair$8, YAMLMap$3 = YAMLMap$8, resolveProps$3 = resolveProps$5, utilContainsNewline$1 = utilContainsNewline$3, utilFlowIndentCheck$1 = utilFlowIndentCheck$2, utilMapIncludes$1 = utilMapIncludes$2;
const startColMsg = "All mapping items must start at the same column";
function resolveBlockMap$1({ composeNode: e5, composeEmptyNode: t }, n, r, s, o) {
  var _a4, _b;
  const a = (_a4 = o == null ? void 0 : o.nodeClass) != null ? _a4 : YAMLMap$3.YAMLMap, l = new a(n.schema);
  n.atRoot && (n.atRoot = false);
  let c = r.offset, u = null;
  for (const p of r.items) {
    const { start: f, key: h, sep: m, value: g } = p, d = resolveProps$3.resolveProps(f, { indicator: "explicit-key-ind", next: h != null ? h : m == null ? void 0 : m[0], offset: c, onError: s, parentIndent: r.indent, startOnNewline: true }), y = !d.found;
    if (y) {
      if (h && (h.type === "block-seq" ? s(c, "BLOCK_AS_IMPLICIT_KEY", "A block sequence may not be used as an implicit map key") : "indent" in h && h.indent !== r.indent && s(c, "BAD_INDENT", startColMsg)), !d.anchor && !d.tag && !m) {
        u = d.end, d.comment && (l.comment ? l.comment += `
` + d.comment : l.comment = d.comment);
        continue;
      }
      (d.newlineAfterProp || utilContainsNewline$1.containsNewline(h)) && s(h != null ? h : f[f.length - 1], "MULTILINE_IMPLICIT_KEY", "Implicit keys need to be on a single line");
    } else ((_b = d.found) == null ? void 0 : _b.indent) !== r.indent && s(c, "BAD_INDENT", startColMsg);
    n.atKey = true;
    const S = d.end, $ = h ? e5(n, h, d, s) : t(n, S, f, null, d, s);
    n.schema.compat && utilFlowIndentCheck$1.flowIndentCheck(r.indent, h, s), n.atKey = false, utilMapIncludes$1.mapIncludes(n, l.items, $) && s(S, "DUPLICATE_KEY", "Map keys must be unique");
    const E = resolveProps$3.resolveProps(m != null ? m : [], { indicator: "map-value-ind", next: g, offset: $.range[2], onError: s, parentIndent: r.indent, startOnNewline: !h || h.type === "block-scalar" });
    if (c = E.end, E.found) {
      y && ((g == null ? void 0 : g.type) === "block-map" && !E.hasNewline && s(c, "BLOCK_AS_IMPLICIT_KEY", "Nested mappings are not allowed in compact mappings"), n.options.strict && d.start < E.found.offset - 1024 && s($.range, "KEY_OVER_1024_CHARS", "The : indicator must be at most 1024 chars after the start of an implicit block mapping key"));
      const b = g ? e5(n, g, E, s) : t(n, c, m, null, E, s);
      n.schema.compat && utilFlowIndentCheck$1.flowIndentCheck(r.indent, g, s), c = b.range[2];
      const A = new Pair$2.Pair($, b);
      n.options.keepSourceTokens && (A.srcToken = p), l.items.push(A);
    } else {
      y && s($.range, "MISSING_CHAR", "Implicit map keys need to be followed by map values"), E.comment && ($.comment ? $.comment += `
` + E.comment : $.comment = E.comment);
      const b = new Pair$2.Pair($);
      n.options.keepSourceTokens && (b.srcToken = p), l.items.push(b);
    }
  }
  return u && u < c && s(u, "IMPOSSIBLE", "Map comment with trailing content"), l.range = [r.offset, c, u != null ? u : c], l;
}
resolveBlockMap$2.resolveBlockMap = resolveBlockMap$1;
var resolveBlockSeq$2 = {}, YAMLSeq$2 = YAMLSeq$7, resolveProps$2 = resolveProps$5, utilFlowIndentCheck = utilFlowIndentCheck$2;
function resolveBlockSeq$1({ composeNode: e5, composeEmptyNode: t }, n, r, s, o) {
  var _a4;
  const a = (_a4 = o == null ? void 0 : o.nodeClass) != null ? _a4 : YAMLSeq$2.YAMLSeq, l = new a(n.schema);
  n.atRoot && (n.atRoot = false), n.atKey && (n.atKey = false);
  let c = r.offset, u = null;
  for (const { start: p, value: f } of r.items) {
    const h = resolveProps$2.resolveProps(p, { indicator: "seq-item-ind", next: f, offset: c, onError: s, parentIndent: r.indent, startOnNewline: true });
    if (!h.found) if (h.anchor || h.tag || f) (f == null ? void 0 : f.type) === "block-seq" ? s(h.end, "BAD_INDENT", "All sequence items must start at the same column") : s(c, "MISSING_CHAR", "Sequence item without - indicator");
    else {
      u = h.end, h.comment && (l.comment = h.comment);
      continue;
    }
    const m = f ? e5(n, f, h, s) : t(n, h.end, p, null, h, s);
    n.schema.compat && utilFlowIndentCheck.flowIndentCheck(r.indent, f, s), c = m.range[2], l.items.push(m);
  }
  return l.range = [r.offset, c, u != null ? u : c], l;
}
resolveBlockSeq$2.resolveBlockSeq = resolveBlockSeq$1;
var resolveFlowCollection$2 = {}, resolveEnd$6 = {};
function resolveEnd$5(e5, t, n, r) {
  let s = "";
  if (e5) {
    let o = false, a = "";
    for (const l of e5) {
      const { source: c, type: u } = l;
      switch (u) {
        case "space":
          o = true;
          break;
        case "comment": {
          n && !o && r(l, "MISSING_CHAR", "Comments must be separated from other tokens by white space characters");
          const p = c.substring(1) || " ";
          s ? s += a + p : s = p, a = "";
          break;
        }
        case "newline":
          s && (a += c), o = true;
          break;
        default:
          r(l, "UNEXPECTED_TOKEN", `Unexpected ${u} at node end`);
      }
      t += c.length;
    }
  }
  return { comment: s, offset: t };
}
resolveEnd$6.resolveEnd = resolveEnd$5;
var identity$7 = identity$y, Pair$1 = Pair$8, YAMLMap$2 = YAMLMap$8, YAMLSeq$1 = YAMLSeq$7, resolveEnd$4 = resolveEnd$6, resolveProps$1 = resolveProps$5, utilContainsNewline = utilContainsNewline$3, utilMapIncludes = utilMapIncludes$2;
const blockMsg = "Block collections are not allowed within flow collections", isBlock = (e5) => e5 && (e5.type === "block-map" || e5.type === "block-seq");
function resolveFlowCollection$1({ composeNode: e5, composeEmptyNode: t }, n, r, s, o) {
  var _a4, _b, _c2;
  const a = r.start.source === "{", l = a ? "flow map" : "flow sequence", c = (_a4 = o == null ? void 0 : o.nodeClass) != null ? _a4 : a ? YAMLMap$2.YAMLMap : YAMLSeq$1.YAMLSeq, u = new c(n.schema);
  u.flow = true;
  const p = n.atRoot;
  p && (n.atRoot = false), n.atKey && (n.atKey = false);
  let f = r.offset + r.start.source.length;
  for (let y = 0; y < r.items.length; ++y) {
    const S = r.items[y], { start: $, key: E, sep: b, value: A } = S, w = resolveProps$1.resolveProps($, { flow: l, indicator: "explicit-key-ind", next: E != null ? E : b == null ? void 0 : b[0], offset: f, onError: s, parentIndent: r.indent, startOnNewline: false });
    if (!w.found) {
      if (!w.anchor && !w.tag && !b && !A) {
        y === 0 && w.comma ? s(w.comma, "UNEXPECTED_TOKEN", `Unexpected , in ${l}`) : y < r.items.length - 1 && s(w.start, "UNEXPECTED_TOKEN", `Unexpected empty item in ${l}`), w.comment && (u.comment ? u.comment += `
` + w.comment : u.comment = w.comment), f = w.end;
        continue;
      }
      !a && n.options.strict && utilContainsNewline.containsNewline(E) && s(E, "MULTILINE_IMPLICIT_KEY", "Implicit keys of flow sequence pairs need to be on a single line");
    }
    if (y === 0) w.comma && s(w.comma, "UNEXPECTED_TOKEN", `Unexpected , in ${l}`);
    else if (w.comma || s(w.start, "MISSING_CHAR", `Missing , between ${l} items`), w.comment) {
      let v = "";
      e: for (const C of $) switch (C.type) {
        case "comma":
        case "space":
          break;
        case "comment":
          v = C.source.substring(1);
          break e;
        default:
          break e;
      }
      if (v) {
        let C = u.items[u.items.length - 1];
        identity$7.isPair(C) && (C = (_b = C.value) != null ? _b : C.key), C.comment ? C.comment += `
` + v : C.comment = v, w.comment = w.comment.substring(v.length + 1);
      }
    }
    if (!a && !b && !w.found) {
      const v = A ? e5(n, A, w, s) : t(n, w.end, b, null, w, s);
      u.items.push(v), f = v.range[2], isBlock(A) && s(v.range, "BLOCK_IN_FLOW", blockMsg);
    } else {
      n.atKey = true;
      const v = w.end, C = E ? e5(n, E, w, s) : t(n, v, $, null, w, s);
      isBlock(E) && s(C.range, "BLOCK_IN_FLOW", blockMsg), n.atKey = false;
      const T = resolveProps$1.resolveProps(b != null ? b : [], { flow: l, indicator: "map-value-ind", next: A, offset: C.range[2], onError: s, parentIndent: r.indent, startOnNewline: false });
      if (T.found) {
        if (!a && !w.found && n.options.strict) {
          if (b) for (const _ of b) {
            if (_ === T.found) break;
            if (_.type === "newline") {
              s(_, "MULTILINE_IMPLICIT_KEY", "Implicit keys of flow sequence pairs need to be on a single line");
              break;
            }
          }
          w.start < T.found.offset - 1024 && s(T.found, "KEY_OVER_1024_CHARS", "The : indicator must be at most 1024 chars after the start of an implicit flow sequence key");
        }
      } else A && ("source" in A && ((_c2 = A.source) == null ? void 0 : _c2[0]) === ":" ? s(A, "MISSING_CHAR", `Missing space after : in ${l}`) : s(T.start, "MISSING_CHAR", `Missing , or : between ${l} items`));
      const O = A ? e5(n, A, T, s) : T.found ? t(n, T.end, b, null, T, s) : null;
      O ? isBlock(A) && s(O.range, "BLOCK_IN_FLOW", blockMsg) : T.comment && (C.comment ? C.comment += `
` + T.comment : C.comment = T.comment);
      const L = new Pair$1.Pair(C, O);
      if (n.options.keepSourceTokens && (L.srcToken = S), a) {
        const _ = u;
        utilMapIncludes.mapIncludes(n, _.items, C) && s(v, "DUPLICATE_KEY", "Map keys must be unique"), _.items.push(L);
      } else {
        const _ = new YAMLMap$2.YAMLMap(n.schema);
        _.flow = true, _.items.push(L);
        const N = (O != null ? O : C).range;
        _.range = [C.range[0], N[1], N[2]], u.items.push(_);
      }
      f = O ? O.range[2] : T.end;
    }
  }
  const h = a ? "}" : "]", [m, ...g] = r.end;
  let d = f;
  if ((m == null ? void 0 : m.source) === h) d = m.offset + m.source.length;
  else {
    const y = l[0].toUpperCase() + l.substring(1), S = p ? `${y} must end with a ${h}` : `${y} in block collection must be sufficiently indented and end with a ${h}`;
    s(f, p ? "MISSING_CHAR" : "BAD_INDENT", S), m && m.source.length !== 1 && g.unshift(m);
  }
  if (g.length > 0) {
    const y = resolveEnd$4.resolveEnd(g, d, n.options.strict, s);
    y.comment && (u.comment ? u.comment += `
` + y.comment : u.comment = y.comment), u.range = [r.offset, d, y.offset];
  } else u.range = [r.offset, d, d];
  return u;
}
resolveFlowCollection$2.resolveFlowCollection = resolveFlowCollection$1;
var identity$6 = identity$y, Scalar$4 = Scalar$k, YAMLMap$1 = YAMLMap$8, YAMLSeq = YAMLSeq$7, resolveBlockMap = resolveBlockMap$2, resolveBlockSeq = resolveBlockSeq$2, resolveFlowCollection = resolveFlowCollection$2;
function resolveCollection(e5, t, n, r, s, o) {
  const a = n.type === "block-map" ? resolveBlockMap.resolveBlockMap(e5, t, n, r, o) : n.type === "block-seq" ? resolveBlockSeq.resolveBlockSeq(e5, t, n, r, o) : resolveFlowCollection.resolveFlowCollection(e5, t, n, r, o), l = a.constructor;
  return s === "!" || s === l.tagName ? (a.tag = l.tagName, a) : (s && (a.tag = s), a);
}
function composeCollection$1(e5, t, n, r, s) {
  var _a4, _b, _c2;
  const o = r.tag, a = o ? t.directives.tagName(o.source, (h) => s(o, "TAG_RESOLVE_FAILED", h)) : null;
  if (n.type === "block-seq") {
    const { anchor: h, newlineAfterProp: m } = r, g = h && o ? h.offset > o.offset ? h : o : h != null ? h : o;
    g && (!m || m.offset < g.offset) && s(g, "MISSING_CHAR", "Missing newline after block sequence props");
  }
  const l = n.type === "block-map" ? "map" : n.type === "block-seq" ? "seq" : n.start.source === "{" ? "map" : "seq";
  if (!o || !a || a === "!" || a === YAMLMap$1.YAMLMap.tagName && l === "map" || a === YAMLSeq.YAMLSeq.tagName && l === "seq") return resolveCollection(e5, t, n, s, a);
  let c = t.schema.tags.find((h) => h.tag === a && h.collection === l);
  if (!c) {
    const h = t.schema.knownTags[a];
    if ((h == null ? void 0 : h.collection) === l) t.schema.tags.push(Object.assign({}, h, { default: false })), c = h;
    else return h ? s(o, "BAD_COLLECTION_TYPE", `${h.tag} used for ${l} collection, but expects ${(_a4 = h.collection) != null ? _a4 : "scalar"}`, true) : s(o, "TAG_RESOLVE_FAILED", `Unresolved tag: ${a}`, true), resolveCollection(e5, t, n, s, a);
  }
  const u = resolveCollection(e5, t, n, s, a, c), p = (_c2 = (_b = c.resolve) == null ? void 0 : _b.call(c, u, (h) => s(o, "TAG_RESOLVE_FAILED", h), t.options)) != null ? _c2 : u, f = identity$6.isNode(p) ? p : new Scalar$4.Scalar(p);
  return f.range = u.range, f.tag = a, (c == null ? void 0 : c.format) && (f.format = c.format), f;
}
composeCollection$2.composeCollection = composeCollection$1;
var composeScalar$2 = {}, resolveBlockScalar$3 = {}, Scalar$3 = Scalar$k;
function resolveBlockScalar$2(e5, t, n) {
  const r = t.offset, s = parseBlockScalarHeader(t, e5.options.strict, n);
  if (!s) return { value: "", type: null, comment: "", range: [r, r, r] };
  const o = s.mode === ">" ? Scalar$3.Scalar.BLOCK_FOLDED : Scalar$3.Scalar.BLOCK_LITERAL, a = t.source ? splitLines(t.source) : [];
  let l = a.length;
  for (let d = a.length - 1; d >= 0; --d) {
    const y = a[d][1];
    if (y === "" || y === "\r") l = d;
    else break;
  }
  if (l === 0) {
    const d = s.chomp === "+" && a.length > 0 ? `
`.repeat(Math.max(1, a.length - 1)) : "";
    let y = r + s.length;
    return t.source && (y += t.source.length), { value: d, type: o, comment: s.comment, range: [r, y, y] };
  }
  let c = t.indent + s.indent, u = t.offset + s.length, p = 0;
  for (let d = 0; d < l; ++d) {
    const [y, S] = a[d];
    if (S === "" || S === "\r") s.indent === 0 && y.length > c && (c = y.length);
    else {
      y.length < c && n(u + y.length, "MISSING_CHAR", "Block scalars with more-indented leading empty lines must use an explicit indentation indicator"), s.indent === 0 && (c = y.length), p = d, c === 0 && !e5.atRoot && n(u, "BAD_INDENT", "Block scalar values in collections must be indented");
      break;
    }
    u += y.length + S.length + 1;
  }
  for (let d = a.length - 1; d >= l; --d) a[d][0].length > c && (l = d + 1);
  let f = "", h = "", m = false;
  for (let d = 0; d < p; ++d) f += a[d][0].slice(c) + `
`;
  for (let d = p; d < l; ++d) {
    let [y, S] = a[d];
    u += y.length + S.length + 1;
    const $ = S[S.length - 1] === "\r";
    if ($ && (S = S.slice(0, -1)), S && y.length < c) {
      const b = `Block scalar lines must not be less indented than their ${s.indent ? "explicit indentation indicator" : "first line"}`;
      n(u - S.length - ($ ? 2 : 1), "BAD_INDENT", b), y = "";
    }
    o === Scalar$3.Scalar.BLOCK_LITERAL ? (f += h + y.slice(c) + S, h = `
`) : y.length > c || S[0] === "	" ? (h === " " ? h = `
` : !m && h === `
` && (h = `

`), f += h + y.slice(c) + S, h = `
`, m = true) : S === "" ? h === `
` ? f += `
` : h = `
` : (f += h + S, h = " ", m = false);
  }
  switch (s.chomp) {
    case "-":
      break;
    case "+":
      for (let d = l; d < a.length; ++d) f += `
` + a[d][0].slice(c);
      f[f.length - 1] !== `
` && (f += `
`);
      break;
    default:
      f += `
`;
  }
  const g = r + s.length + t.source.length;
  return { value: f, type: o, comment: s.comment, range: [r, g, g] };
}
function parseBlockScalarHeader({ offset: e5, props: t }, n, r) {
  if (t[0].type !== "block-scalar-header") return r(t[0], "IMPOSSIBLE", "Block scalar header not found"), null;
  const { source: s } = t[0], o = s[0];
  let a = 0, l = "", c = -1;
  for (let h = 1; h < s.length; ++h) {
    const m = s[h];
    if (!l && (m === "-" || m === "+")) l = m;
    else {
      const g = Number(m);
      !a && g ? a = g : c === -1 && (c = e5 + h);
    }
  }
  c !== -1 && r(c, "UNEXPECTED_TOKEN", `Block scalar header includes extra characters: ${s}`);
  let u = false, p = "", f = s.length;
  for (let h = 1; h < t.length; ++h) {
    const m = t[h];
    switch (m.type) {
      case "space":
        u = true;
      case "newline":
        f += m.source.length;
        break;
      case "comment":
        n && !u && r(m, "MISSING_CHAR", "Comments must be separated from other tokens by white space characters"), f += m.source.length, p = m.source.substring(1);
        break;
      case "error":
        r(m, "UNEXPECTED_TOKEN", m.message), f += m.source.length;
        break;
      default: {
        const g = `Unexpected token in block scalar header: ${m.type}`;
        r(m, "UNEXPECTED_TOKEN", g);
        const d = m.source;
        d && typeof d == "string" && (f += d.length);
      }
    }
  }
  return { mode: o, indent: a, chomp: l, comment: p, length: f };
}
function splitLines(e5) {
  const t = e5.split(/\n( *)/), n = t[0], r = n.match(/^( *)/), o = [(r == null ? void 0 : r[1]) ? [r[1], n.slice(r[1].length)] : ["", n]];
  for (let a = 1; a < t.length; a += 2) o.push([t[a], t[a + 1]]);
  return o;
}
resolveBlockScalar$3.resolveBlockScalar = resolveBlockScalar$2;
var resolveFlowScalar$3 = {}, Scalar$2 = Scalar$k, resolveEnd$3 = resolveEnd$6;
function resolveFlowScalar$2(e5, t, n) {
  const { offset: r, type: s, source: o, end: a } = e5;
  let l, c;
  const u = (h, m, g) => n(r + h, m, g);
  switch (s) {
    case "scalar":
      l = Scalar$2.Scalar.PLAIN, c = plainValue(o, u);
      break;
    case "single-quoted-scalar":
      l = Scalar$2.Scalar.QUOTE_SINGLE, c = singleQuotedValue(o, u);
      break;
    case "double-quoted-scalar":
      l = Scalar$2.Scalar.QUOTE_DOUBLE, c = doubleQuotedValue(o, u);
      break;
    default:
      return n(e5, "UNEXPECTED_TOKEN", `Expected a flow scalar value, but found: ${s}`), { value: "", type: null, comment: "", range: [r, r + o.length, r + o.length] };
  }
  const p = r + o.length, f = resolveEnd$3.resolveEnd(a, p, t, n);
  return { value: c, type: l, comment: f.comment, range: [r, p, f.offset] };
}
function plainValue(e5, t) {
  let n = "";
  switch (e5[0]) {
    case "	":
      n = "a tab character";
      break;
    case ",":
      n = "flow indicator character ,";
      break;
    case "%":
      n = "directive indicator character %";
      break;
    case "|":
    case ">": {
      n = `block scalar indicator ${e5[0]}`;
      break;
    }
    case "@":
    case "`": {
      n = `reserved character ${e5[0]}`;
      break;
    }
  }
  return n && t(0, "BAD_SCALAR_START", `Plain value cannot start with ${n}`), foldLines(e5);
}
function singleQuotedValue(e5, t) {
  return (e5[e5.length - 1] !== "'" || e5.length === 1) && t(e5.length, "MISSING_CHAR", "Missing closing 'quote"), foldLines(e5.slice(1, -1)).replace(/''/g, "'");
}
function foldLines(e5) {
  var _a4;
  let t, n;
  try {
    t = new RegExp(`(.*?)(?<![ 	])[ 	]*\r?
`, "sy"), n = new RegExp(`[ 	]*(.*?)(?:(?<![ 	])[ 	]*)?\r?
`, "sy");
  } catch {
    t = /(.*?)[ \t]*\r?\n/sy, n = /[ \t]*(.*?)[ \t]*\r?\n/sy;
  }
  let r = t.exec(e5);
  if (!r) return e5;
  let s = r[1], o = " ", a = t.lastIndex;
  for (n.lastIndex = a; r = n.exec(e5); ) r[1] === "" ? o === `
` ? s += o : o = `
` : (s += o + r[1], o = " "), a = n.lastIndex;
  const l = /[ \t]*(.*)/sy;
  return l.lastIndex = a, r = l.exec(e5), s + o + ((_a4 = r == null ? void 0 : r[1]) != null ? _a4 : "");
}
function doubleQuotedValue(e5, t) {
  let n = "";
  for (let r = 1; r < e5.length - 1; ++r) {
    const s = e5[r];
    if (!(s === "\r" && e5[r + 1] === `
`)) if (s === `
`) {
      const { fold: o, offset: a } = foldNewline(e5, r);
      n += o, r = a;
    } else if (s === "\\") {
      let o = e5[++r];
      const a = escapeCodes[o];
      if (a) n += a;
      else if (o === `
`) for (o = e5[r + 1]; o === " " || o === "	"; ) o = e5[++r + 1];
      else if (o === "\r" && e5[r + 1] === `
`) for (o = e5[++r + 1]; o === " " || o === "	"; ) o = e5[++r + 1];
      else if (o === "x" || o === "u" || o === "U") {
        const l = o === "x" ? 2 : o === "u" ? 4 : 8;
        n += parseCharCode(e5, r + 1, l, t), r += l;
      } else {
        const l = e5.substr(r - 1, 2);
        t(r - 1, "BAD_DQ_ESCAPE", `Invalid escape sequence ${l}`), n += l;
      }
    } else if (s === " " || s === "	") {
      const o = r;
      let a = e5[r + 1];
      for (; a === " " || a === "	"; ) a = e5[++r + 1];
      a !== `
` && !(a === "\r" && e5[r + 2] === `
`) && (n += r > o ? e5.slice(o, r + 1) : s);
    } else n += s;
  }
  return (e5[e5.length - 1] !== '"' || e5.length === 1) && t(e5.length, "MISSING_CHAR", 'Missing closing "quote'), n;
}
function foldNewline(e5, t) {
  let n = "", r = e5[t + 1];
  for (; (r === " " || r === "	" || r === `
` || r === "\r") && !(r === "\r" && e5[t + 2] !== `
`); ) r === `
` && (n += `
`), t += 1, r = e5[t + 1];
  return n || (n = " "), { fold: n, offset: t };
}
const escapeCodes = { 0: "\0", a: "\x07", b: "\b", e: "\x1B", f: "\f", n: `
`, r: "\r", t: "	", v: "\v", N: "\x85", _: "\xA0", L: "\u2028", P: "\u2029", " ": " ", '"': '"', "/": "/", "\\": "\\", "	": "	" };
function parseCharCode(e5, t, n, r) {
  const s = e5.substr(t, n), a = s.length === n && /^[0-9a-fA-F]+$/.test(s) ? parseInt(s, 16) : NaN;
  try {
    return String.fromCodePoint(a);
  } catch {
    const l = e5.substr(t - 2, n + 2);
    return r(t - 2, "BAD_DQ_ESCAPE", `Invalid escape sequence ${l}`), l;
  }
}
resolveFlowScalar$3.resolveFlowScalar = resolveFlowScalar$2;
var identity$5 = identity$y, Scalar$1 = Scalar$k, resolveBlockScalar$1 = resolveBlockScalar$3, resolveFlowScalar$1 = resolveFlowScalar$3;
function composeScalar$1(e5, t, n, r) {
  const { value: s, type: o, comment: a, range: l } = t.type === "block-scalar" ? resolveBlockScalar$1.resolveBlockScalar(e5, t, r) : resolveFlowScalar$1.resolveFlowScalar(t, e5.options.strict, r), c = n ? e5.directives.tagName(n.source, (f) => r(n, "TAG_RESOLVE_FAILED", f)) : null;
  let u;
  e5.options.stringKeys && e5.atKey ? u = e5.schema[identity$5.SCALAR] : c ? u = findScalarTagByName(e5.schema, s, c, n, r) : t.type === "scalar" ? u = findScalarTagByTest(e5, s, t, r) : u = e5.schema[identity$5.SCALAR];
  let p;
  try {
    const f = u.resolve(s, (h) => r(n != null ? n : t, "TAG_RESOLVE_FAILED", h), e5.options);
    p = identity$5.isScalar(f) ? f : new Scalar$1.Scalar(f);
  } catch (f) {
    const h = f instanceof Error ? f.message : String(f);
    r(n != null ? n : t, "TAG_RESOLVE_FAILED", h), p = new Scalar$1.Scalar(s);
  }
  return p.range = l, p.source = s, o && (p.type = o), c && (p.tag = c), u.format && (p.format = u.format), a && (p.comment = a), p;
}
function findScalarTagByName(e5, t, n, r, s) {
  var _a4;
  if (n === "!") return e5[identity$5.SCALAR];
  const o = [];
  for (const l of e5.tags) if (!l.collection && l.tag === n) if (l.default && l.test) o.push(l);
  else return l;
  for (const l of o) if ((_a4 = l.test) == null ? void 0 : _a4.test(t)) return l;
  const a = e5.knownTags[n];
  return a && !a.collection ? (e5.tags.push(Object.assign({}, a, { default: false, test: void 0 })), a) : (s(r, "TAG_RESOLVE_FAILED", `Unresolved tag: ${n}`, n !== "tag:yaml.org,2002:str"), e5[identity$5.SCALAR]);
}
function findScalarTagByTest({ atKey: e5, directives: t, schema: n }, r, s, o) {
  var _a4;
  const a = n.tags.find((l) => {
    var _a5;
    return (l.default === true || e5 && l.default === "key") && ((_a5 = l.test) == null ? void 0 : _a5.test(r));
  }) || n[identity$5.SCALAR];
  if (n.compat) {
    const l = (_a4 = n.compat.find((c) => {
      var _a5;
      return c.default && ((_a5 = c.test) == null ? void 0 : _a5.test(r));
    })) != null ? _a4 : n[identity$5.SCALAR];
    if (a.tag !== l.tag) {
      const c = t.tagString(a.tag), u = t.tagString(l.tag), p = `Value may be parsed as either ${c} or ${u}`;
      o(s, "TAG_RESOLVE_FAILED", p, true);
    }
  }
  return a;
}
composeScalar$2.composeScalar = composeScalar$1;
var utilEmptyScalarPosition$1 = {};
function emptyScalarPosition(e5, t, n) {
  if (t) {
    n != null ? n : n = t.length;
    for (let r = n - 1; r >= 0; --r) {
      let s = t[r];
      switch (s.type) {
        case "space":
        case "comment":
        case "newline":
          e5 -= s.source.length;
          continue;
      }
      for (s = t[++r]; (s == null ? void 0 : s.type) === "space"; ) e5 += s.source.length, s = t[++r];
      break;
    }
  }
  return e5;
}
utilEmptyScalarPosition$1.emptyScalarPosition = emptyScalarPosition;
var Alias = Alias$4, identity$4 = identity$y, composeCollection = composeCollection$2, composeScalar = composeScalar$2, resolveEnd$2 = resolveEnd$6, utilEmptyScalarPosition = utilEmptyScalarPosition$1;
const CN = { composeNode: composeNode$3, composeEmptyNode };
function composeNode$3(e5, t, n, r) {
  const s = e5.atKey, { spaceBefore: o, comment: a, anchor: l, tag: c } = n;
  let u, p = true;
  switch (t.type) {
    case "alias":
      u = composeAlias(e5, t, r), (l || c) && r(t, "ALIAS_PROPS", "An alias node must not specify any properties");
      break;
    case "scalar":
    case "single-quoted-scalar":
    case "double-quoted-scalar":
    case "block-scalar":
      u = composeScalar.composeScalar(e5, t, c, r), l && (u.anchor = l.source.substring(1));
      break;
    case "block-map":
    case "block-seq":
    case "flow-collection":
      try {
        u = composeCollection.composeCollection(CN, e5, t, n, r), l && (u.anchor = l.source.substring(1));
      } catch (f) {
        const h = f instanceof Error ? f.message : String(f);
        r(t, "RESOURCE_EXHAUSTION", h);
      }
      break;
    default: {
      const f = t.type === "error" ? t.message : `Unsupported token (type: ${t.type})`;
      r(t, "UNEXPECTED_TOKEN", f), p = false;
    }
  }
  return u != null ? u : u = composeEmptyNode(e5, t.offset, void 0, null, n, r), l && u.anchor === "" && r(l, "BAD_ALIAS", "Anchor cannot be an empty string"), s && e5.options.stringKeys && (!identity$4.isScalar(u) || typeof u.value != "string" || u.tag && u.tag !== "tag:yaml.org,2002:str") && r(c != null ? c : t, "NON_STRING_KEY", "With stringKeys, all keys must be strings"), o && (u.spaceBefore = true), a && (t.type === "scalar" && t.source === "" ? u.comment = a : u.commentBefore = a), e5.options.keepSourceTokens && p && (u.srcToken = t), u;
}
function composeEmptyNode(e5, t, n, r, { spaceBefore: s, comment: o, anchor: a, tag: l, end: c }, u) {
  const p = { type: "scalar", offset: utilEmptyScalarPosition.emptyScalarPosition(t, n, r), indent: -1, source: "" }, f = composeScalar.composeScalar(e5, p, l, u);
  return a && (f.anchor = a.source.substring(1), f.anchor === "" && u(a, "BAD_ALIAS", "Anchor cannot be an empty string")), s && (f.spaceBefore = true), o && (f.comment = o, f.range[2] = c), f;
}
function composeAlias({ options: e5 }, { offset: t, source: n, end: r }, s) {
  const o = new Alias.Alias(n.substring(1));
  o.source === "" && s(t, "BAD_ALIAS", "Alias cannot be an empty string"), o.source.endsWith(":") && s(t + n.length - 1, "BAD_ALIAS", "Alias ending in : is ambiguous", true);
  const a = t + n.length, l = resolveEnd$2.resolveEnd(r, a, e5.strict, s);
  return o.range = [t, a, l.offset], l.comment && (o.comment = l.comment), o;
}
composeNode$4.composeEmptyNode = composeEmptyNode;
composeNode$4.composeNode = composeNode$3;
var Document$2 = Document$4, composeNode$2 = composeNode$4, resolveEnd$1 = resolveEnd$6, resolveProps = resolveProps$5;
function composeDoc$1(e5, t, { offset: n, start: r, value: s, end: o }, a) {
  const l = Object.assign({ _directives: t }, e5), c = new Document$2.Document(void 0, l), u = { atKey: false, atRoot: true, directives: c.directives, options: c.options, schema: c.schema }, p = resolveProps.resolveProps(r, { indicator: "doc-start", next: s != null ? s : o == null ? void 0 : o[0], offset: n, onError: a, parentIndent: 0, startOnNewline: true });
  p.found && (c.directives.docStart = true, s && (s.type === "block-map" || s.type === "block-seq") && !p.hasNewline && a(p.end, "MISSING_CHAR", "Block collection cannot start on same line with directives-end marker")), c.contents = s ? composeNode$2.composeNode(u, s, p, a) : composeNode$2.composeEmptyNode(u, p.end, r, null, p, a);
  const f = c.contents.range[2], h = resolveEnd$1.resolveEnd(o, f, false, a);
  return h.comment && (c.comment = h.comment), c.range = [n, f, h.offset], c;
}
composeDoc$2.composeDoc = composeDoc$1;
var node_process$1 = require$$0, directives = directives$2, Document$1 = Document$4, errors$3 = errors$4, identity$3 = identity$y, composeDoc = composeDoc$2, resolveEnd = resolveEnd$6;
function getErrorPos(e5) {
  if (typeof e5 == "number") return [e5, e5 + 1];
  if (Array.isArray(e5)) return e5.length === 2 ? e5 : [e5[0], e5[1]];
  const { offset: t, source: n } = e5;
  return [t, t + (typeof n == "string" ? n.length : 1)];
}
function parsePrelude(e5) {
  var _a4;
  let t = "", n = false, r = false;
  for (let s = 0; s < e5.length; ++s) {
    const o = e5[s];
    switch (o[0]) {
      case "#":
        t += (t === "" ? "" : r ? `

` : `
`) + (o.substring(1) || " "), n = true, r = false;
        break;
      case "%":
        ((_a4 = e5[s + 1]) == null ? void 0 : _a4[0]) !== "#" && (s += 1), n = false;
        break;
      default:
        n || (r = true), n = false;
    }
  }
  return { comment: t, afterEmptyLine: r };
}
class Composer {
  constructor(t = {}) {
    this.doc = null, this.atDirectives = false, this.prelude = [], this.errors = [], this.warnings = [], this.onError = (n, r, s, o) => {
      const a = getErrorPos(n);
      o ? this.warnings.push(new errors$3.YAMLWarning(a, r, s)) : this.errors.push(new errors$3.YAMLParseError(a, r, s));
    }, this.directives = new directives.Directives({ version: t.version || "1.2" }), this.options = t;
  }
  decorate(t, n) {
    const { comment: r, afterEmptyLine: s } = parsePrelude(this.prelude);
    if (r) {
      const o = t.contents;
      if (n) t.comment = t.comment ? `${t.comment}
${r}` : r;
      else if (s || t.directives.docStart || !o) t.commentBefore = r;
      else if (identity$3.isCollection(o) && !o.flow && o.items.length > 0) {
        let a = o.items[0];
        identity$3.isPair(a) && (a = a.key);
        const l = a.commentBefore;
        a.commentBefore = l ? `${r}
${l}` : r;
      } else {
        const a = o.commentBefore;
        o.commentBefore = a ? `${r}
${a}` : r;
      }
    }
    n ? (Array.prototype.push.apply(t.errors, this.errors), Array.prototype.push.apply(t.warnings, this.warnings)) : (t.errors = this.errors, t.warnings = this.warnings), this.prelude = [], this.errors = [], this.warnings = [];
  }
  streamInfo() {
    return { comment: parsePrelude(this.prelude).comment, directives: this.directives, errors: this.errors, warnings: this.warnings };
  }
  *compose(t, n = false, r = -1) {
    for (const s of t) yield* this.next(s);
    yield* this.end(n, r);
  }
  *next(t) {
    switch (node_process$1.env.LOG_STREAM && console.dir(t, { depth: null }), t.type) {
      case "directive":
        this.directives.add(t.source, (n, r, s) => {
          const o = getErrorPos(t);
          o[0] += n, this.onError(o, "BAD_DIRECTIVE", r, s);
        }), this.prelude.push(t.source), this.atDirectives = true;
        break;
      case "document": {
        const n = composeDoc.composeDoc(this.options, this.directives, t, this.onError);
        this.atDirectives && !n.directives.docStart && this.onError(t, "MISSING_CHAR", "Missing directives-end/doc-start indicator line"), this.decorate(n, false), this.doc && (yield this.doc), this.doc = n, this.atDirectives = false;
        break;
      }
      case "byte-order-mark":
      case "space":
        break;
      case "comment":
      case "newline":
        this.prelude.push(t.source);
        break;
      case "error": {
        const n = t.source ? `${t.message}: ${JSON.stringify(t.source)}` : t.message, r = new errors$3.YAMLParseError(getErrorPos(t), "UNEXPECTED_TOKEN", n);
        this.atDirectives || !this.doc ? this.errors.push(r) : this.doc.errors.push(r);
        break;
      }
      case "doc-end": {
        if (!this.doc) {
          const r = "Unexpected doc-end without preceding document";
          this.errors.push(new errors$3.YAMLParseError(getErrorPos(t), "UNEXPECTED_TOKEN", r));
          break;
        }
        this.doc.directives.docEnd = true;
        const n = resolveEnd.resolveEnd(t.end, t.offset + t.source.length, this.doc.options.strict, this.onError);
        if (this.decorate(this.doc, true), n.comment) {
          const r = this.doc.comment;
          this.doc.comment = r ? `${r}
${n.comment}` : n.comment;
        }
        this.doc.range[2] = n.offset;
        break;
      }
      default:
        this.errors.push(new errors$3.YAMLParseError(getErrorPos(t), "UNEXPECTED_TOKEN", `Unsupported token ${t.type}`));
    }
  }
  *end(t = false, n = -1) {
    if (this.doc) this.decorate(this.doc, true), yield this.doc, this.doc = null;
    else if (t) {
      const r = Object.assign({ _directives: this.directives }, this.options), s = new Document$1.Document(void 0, r);
      this.atDirectives && this.onError(n, "MISSING_CHAR", "Missing directives-end indicator line"), s.range = [0, n, n], this.decorate(s, false), yield s;
    }
  }
}
composer$1.Composer = Composer;
var cst$2 = {}, cstScalar$1 = {}, resolveBlockScalar = resolveBlockScalar$3, resolveFlowScalar = resolveFlowScalar$3, errors$2 = errors$4, stringifyString = stringifyString$5;
function resolveAsScalar(e5, t = true, n) {
  if (e5) {
    const r = (s, o, a) => {
      const l = typeof s == "number" ? s : Array.isArray(s) ? s[0] : s.offset;
      if (n) n(l, o, a);
      else throw new errors$2.YAMLParseError([l, l + 1], o, a);
    };
    switch (e5.type) {
      case "scalar":
      case "single-quoted-scalar":
      case "double-quoted-scalar":
        return resolveFlowScalar.resolveFlowScalar(e5, t, r);
      case "block-scalar":
        return resolveBlockScalar.resolveBlockScalar({ options: { strict: t } }, e5, r);
    }
  }
  return null;
}
function createScalarToken(e5, t) {
  var _a4;
  const { implicitKey: n = false, indent: r, inFlow: s = false, offset: o = -1, type: a = "PLAIN" } = t, l = stringifyString.stringifyString({ type: a, value: e5 }, { implicitKey: n, indent: r > 0 ? " ".repeat(r) : "", inFlow: s, options: { blockQuote: true, lineWidth: -1 } }), c = (_a4 = t.end) != null ? _a4 : [{ type: "newline", offset: -1, indent: r, source: `
` }];
  switch (l[0]) {
    case "|":
    case ">": {
      const u = l.indexOf(`
`), p = l.substring(0, u), f = l.substring(u + 1) + `
`, h = [{ type: "block-scalar-header", offset: o, indent: r, source: p }];
      return addEndtoBlockProps(h, c) || h.push({ type: "newline", offset: -1, indent: r, source: `
` }), { type: "block-scalar", offset: o, indent: r, props: h, source: f };
    }
    case '"':
      return { type: "double-quoted-scalar", offset: o, indent: r, source: l, end: c };
    case "'":
      return { type: "single-quoted-scalar", offset: o, indent: r, source: l, end: c };
    default:
      return { type: "scalar", offset: o, indent: r, source: l, end: c };
  }
}
function setScalarValue(e5, t, n = {}) {
  let { afterKey: r = false, implicitKey: s = false, inFlow: o = false, type: a } = n, l = "indent" in e5 ? e5.indent : null;
  if (r && typeof l == "number" && (l += 2), !a) switch (e5.type) {
    case "single-quoted-scalar":
      a = "QUOTE_SINGLE";
      break;
    case "double-quoted-scalar":
      a = "QUOTE_DOUBLE";
      break;
    case "block-scalar": {
      const u = e5.props[0];
      if (u.type !== "block-scalar-header") throw new Error("Invalid block scalar header");
      a = u.source[0] === ">" ? "BLOCK_FOLDED" : "BLOCK_LITERAL";
      break;
    }
    default:
      a = "PLAIN";
  }
  const c = stringifyString.stringifyString({ type: a, value: t }, { implicitKey: s || l === null, indent: l !== null && l > 0 ? " ".repeat(l) : "", inFlow: o, options: { blockQuote: true, lineWidth: -1 } });
  switch (c[0]) {
    case "|":
    case ">":
      setBlockScalarValue(e5, c);
      break;
    case '"':
      setFlowScalarValue(e5, c, "double-quoted-scalar");
      break;
    case "'":
      setFlowScalarValue(e5, c, "single-quoted-scalar");
      break;
    default:
      setFlowScalarValue(e5, c, "scalar");
  }
}
function setBlockScalarValue(e5, t) {
  const n = t.indexOf(`
`), r = t.substring(0, n), s = t.substring(n + 1) + `
`;
  if (e5.type === "block-scalar") {
    const o = e5.props[0];
    if (o.type !== "block-scalar-header") throw new Error("Invalid block scalar header");
    o.source = r, e5.source = s;
  } else {
    const { offset: o } = e5, a = "indent" in e5 ? e5.indent : -1, l = [{ type: "block-scalar-header", offset: o, indent: a, source: r }];
    addEndtoBlockProps(l, "end" in e5 ? e5.end : void 0) || l.push({ type: "newline", offset: -1, indent: a, source: `
` });
    for (const c of Object.keys(e5)) c !== "type" && c !== "offset" && delete e5[c];
    Object.assign(e5, { type: "block-scalar", indent: a, props: l, source: s });
  }
}
function addEndtoBlockProps(e5, t) {
  if (t) for (const n of t) switch (n.type) {
    case "space":
    case "comment":
      e5.push(n);
      break;
    case "newline":
      return e5.push(n), true;
  }
  return false;
}
function setFlowScalarValue(e5, t, n) {
  switch (e5.type) {
    case "scalar":
    case "double-quoted-scalar":
    case "single-quoted-scalar":
      e5.type = n, e5.source = t;
      break;
    case "block-scalar": {
      const r = e5.props.slice(1);
      let s = t.length;
      e5.props[0].type === "block-scalar-header" && (s -= e5.props[0].source.length);
      for (const o of r) o.offset += s;
      delete e5.props, Object.assign(e5, { type: n, source: t, end: r });
      break;
    }
    case "block-map":
    case "block-seq": {
      const s = { type: "newline", offset: e5.offset + t.length, indent: e5.indent, source: `
` };
      delete e5.items, Object.assign(e5, { type: n, source: t, end: [s] });
      break;
    }
    default: {
      const r = "indent" in e5 ? e5.indent : -1, s = "end" in e5 && Array.isArray(e5.end) ? e5.end.filter((o) => o.type === "space" || o.type === "comment" || o.type === "newline") : [];
      for (const o of Object.keys(e5)) o !== "type" && o !== "offset" && delete e5[o];
      Object.assign(e5, { type: n, indent: r, source: t, end: s });
    }
  }
}
cstScalar$1.createScalarToken = createScalarToken;
cstScalar$1.resolveAsScalar = resolveAsScalar;
cstScalar$1.setScalarValue = setScalarValue;
var cstStringify$1 = {};
const stringify$5 = (e5) => "type" in e5 ? stringifyToken(e5) : stringifyItem(e5);
function stringifyToken(e5) {
  switch (e5.type) {
    case "block-scalar": {
      let t = "";
      for (const n of e5.props) t += stringifyToken(n);
      return t + e5.source;
    }
    case "block-map":
    case "block-seq": {
      let t = "";
      for (const n of e5.items) t += stringifyItem(n);
      return t;
    }
    case "flow-collection": {
      let t = e5.start.source;
      for (const n of e5.items) t += stringifyItem(n);
      for (const n of e5.end) t += n.source;
      return t;
    }
    case "document": {
      let t = stringifyItem(e5);
      if (e5.end) for (const n of e5.end) t += n.source;
      return t;
    }
    default: {
      let t = e5.source;
      if ("end" in e5 && e5.end) for (const n of e5.end) t += n.source;
      return t;
    }
  }
}
function stringifyItem({ start: e5, key: t, sep: n, value: r }) {
  let s = "";
  for (const o of e5) s += o.source;
  if (t && (s += stringifyToken(t)), n) for (const o of n) s += o.source;
  return r && (s += stringifyToken(r)), s;
}
cstStringify$1.stringify = stringify$5;
var cstVisit$1 = {};
const BREAK = /* @__PURE__ */ Symbol("break visit"), SKIP = /* @__PURE__ */ Symbol("skip children"), REMOVE = /* @__PURE__ */ Symbol("remove item");
function visit$1(e5, t) {
  "type" in e5 && e5.type === "document" && (e5 = { start: e5.start, value: e5.value }), _visit(Object.freeze([]), e5, t);
}
visit$1.BREAK = BREAK;
visit$1.SKIP = SKIP;
visit$1.REMOVE = REMOVE;
visit$1.itemAtPath = (e5, t) => {
  let n = e5;
  for (const [r, s] of t) {
    const o = n == null ? void 0 : n[r];
    if (o && "items" in o) n = o.items[s];
    else return;
  }
  return n;
};
visit$1.parentCollection = (e5, t) => {
  const n = visit$1.itemAtPath(e5, t.slice(0, -1)), r = t[t.length - 1][0], s = n == null ? void 0 : n[r];
  if (s && "items" in s) return s;
  throw new Error("Parent collection not found");
};
function _visit(e5, t, n) {
  let r = n(t, e5);
  if (typeof r == "symbol") return r;
  for (const s of ["key", "value"]) {
    const o = t[s];
    if (o && "items" in o) {
      for (let a = 0; a < o.items.length; ++a) {
        const l = _visit(Object.freeze(e5.concat([[s, a]])), o.items[a], n);
        if (typeof l == "number") a = l - 1;
        else {
          if (l === BREAK) return BREAK;
          l === REMOVE && (o.items.splice(a, 1), a -= 1);
        }
      }
      typeof r == "function" && s === "key" && (r = r(t, e5));
    }
  }
  return typeof r == "function" ? r(t, e5) : r;
}
cstVisit$1.visit = visit$1;
var cstScalar = cstScalar$1, cstStringify = cstStringify$1, cstVisit = cstVisit$1;
const BOM = "\uFEFF", DOCUMENT = "", FLOW_END = "", SCALAR = "", isCollection = (e5) => !!e5 && "items" in e5, isScalar = (e5) => !!e5 && (e5.type === "scalar" || e5.type === "single-quoted-scalar" || e5.type === "double-quoted-scalar" || e5.type === "block-scalar");
function prettyToken(e5) {
  switch (e5) {
    case BOM:
      return "<BOM>";
    case DOCUMENT:
      return "<DOC>";
    case FLOW_END:
      return "<FLOW_END>";
    case SCALAR:
      return "<SCALAR>";
    default:
      return JSON.stringify(e5);
  }
}
function tokenType(e5) {
  switch (e5) {
    case BOM:
      return "byte-order-mark";
    case DOCUMENT:
      return "doc-mode";
    case FLOW_END:
      return "flow-error-end";
    case SCALAR:
      return "scalar";
    case "---":
      return "doc-start";
    case "...":
      return "doc-end";
    case "":
    case `
`:
    case `\r
`:
      return "newline";
    case "-":
      return "seq-item-ind";
    case "?":
      return "explicit-key-ind";
    case ":":
      return "map-value-ind";
    case "{":
      return "flow-map-start";
    case "}":
      return "flow-map-end";
    case "[":
      return "flow-seq-start";
    case "]":
      return "flow-seq-end";
    case ",":
      return "comma";
  }
  switch (e5[0]) {
    case " ":
    case "	":
      return "space";
    case "#":
      return "comment";
    case "%":
      return "directive-line";
    case "*":
      return "alias";
    case "&":
      return "anchor";
    case "!":
      return "tag";
    case "'":
      return "single-quoted-scalar";
    case '"':
      return "double-quoted-scalar";
    case "|":
    case ">":
      return "block-scalar-header";
  }
  return null;
}
cst$2.createScalarToken = cstScalar.createScalarToken;
cst$2.resolveAsScalar = cstScalar.resolveAsScalar;
cst$2.setScalarValue = cstScalar.setScalarValue;
cst$2.stringify = cstStringify.stringify;
cst$2.visit = cstVisit.visit;
cst$2.BOM = BOM;
cst$2.DOCUMENT = DOCUMENT;
cst$2.FLOW_END = FLOW_END;
cst$2.SCALAR = SCALAR;
cst$2.isCollection = isCollection;
cst$2.isScalar = isScalar;
cst$2.prettyToken = prettyToken;
cst$2.tokenType = tokenType;
var lexer$1 = {}, cst$1 = cst$2;
function isEmpty(e5) {
  switch (e5) {
    case void 0:
    case " ":
    case `
`:
    case "\r":
    case "	":
      return true;
    default:
      return false;
  }
}
const hexDigits = new Set("0123456789ABCDEFabcdef"), tagChars = new Set("0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-#;/?:@&=+$_.!~*'()"), flowIndicatorChars = new Set(",[]{}"), invalidAnchorChars = new Set(` ,[]{}
\r	`), isNotAnchorChar = (e5) => !e5 || invalidAnchorChars.has(e5);
class Lexer {
  constructor() {
    this.atEnd = false, this.blockScalarIndent = -1, this.blockScalarKeep = false, this.buffer = "", this.flowKey = false, this.flowLevel = 0, this.indentNext = 0, this.indentValue = 0, this.lineEndPos = null, this.next = null, this.pos = 0;
  }
  *lex(t, n = false) {
    var _a4;
    if (t) {
      if (typeof t != "string") throw TypeError("source is not a string");
      this.buffer = this.buffer ? this.buffer + t : t, this.lineEndPos = null;
    }
    this.atEnd = !n;
    let r = (_a4 = this.next) != null ? _a4 : "stream";
    for (; r && (n || this.hasChars(1)); ) r = yield* this.parseNext(r);
  }
  atLineEnd() {
    let t = this.pos, n = this.buffer[t];
    for (; n === " " || n === "	"; ) n = this.buffer[++t];
    return !n || n === "#" || n === `
` ? true : n === "\r" ? this.buffer[t + 1] === `
` : false;
  }
  charAt(t) {
    return this.buffer[this.pos + t];
  }
  continueScalar(t) {
    let n = this.buffer[t];
    if (this.indentNext > 0) {
      let r = 0;
      for (; n === " "; ) n = this.buffer[++r + t];
      if (n === "\r") {
        const s = this.buffer[r + t + 1];
        if (s === `
` || !s && !this.atEnd) return t + r + 1;
      }
      return n === `
` || r >= this.indentNext || !n && !this.atEnd ? t + r : -1;
    }
    if (n === "-" || n === ".") {
      const r = this.buffer.substr(t, 3);
      if ((r === "---" || r === "...") && isEmpty(this.buffer[t + 3])) return -1;
    }
    return t;
  }
  getLine() {
    let t = this.lineEndPos;
    return (typeof t != "number" || t !== -1 && t < this.pos) && (t = this.buffer.indexOf(`
`, this.pos), this.lineEndPos = t), t === -1 ? this.atEnd ? this.buffer.substring(this.pos) : null : (this.buffer[t - 1] === "\r" && (t -= 1), this.buffer.substring(this.pos, t));
  }
  hasChars(t) {
    return this.pos + t <= this.buffer.length;
  }
  setNext(t) {
    return this.buffer = this.buffer.substring(this.pos), this.pos = 0, this.lineEndPos = null, this.next = t, null;
  }
  peek(t) {
    return this.buffer.substr(this.pos, t);
  }
  *parseNext(t) {
    switch (t) {
      case "stream":
        return yield* this.parseStream();
      case "line-start":
        return yield* this.parseLineStart();
      case "block-start":
        return yield* this.parseBlockStart();
      case "doc":
        return yield* this.parseDocument();
      case "flow":
        return yield* this.parseFlowCollection();
      case "quoted-scalar":
        return yield* this.parseQuotedScalar();
      case "block-scalar":
        return yield* this.parseBlockScalar();
      case "plain-scalar":
        return yield* this.parsePlainScalar();
    }
  }
  *parseStream() {
    let t = this.getLine();
    if (t === null) return this.setNext("stream");
    if (t[0] === cst$1.BOM && (yield* this.pushCount(1), t = t.substring(1)), t[0] === "%") {
      let n = t.length, r = t.indexOf("#");
      for (; r !== -1; ) {
        const o = t[r - 1];
        if (o === " " || o === "	") {
          n = r - 1;
          break;
        } else r = t.indexOf("#", r + 1);
      }
      for (; ; ) {
        const o = t[n - 1];
        if (o === " " || o === "	") n -= 1;
        else break;
      }
      const s = (yield* this.pushCount(n)) + (yield* this.pushSpaces(true));
      return yield* this.pushCount(t.length - s), this.pushNewline(), "stream";
    }
    if (this.atLineEnd()) {
      const n = yield* this.pushSpaces(true);
      return yield* this.pushCount(t.length - n), yield* this.pushNewline(), "stream";
    }
    return yield cst$1.DOCUMENT, yield* this.parseLineStart();
  }
  *parseLineStart() {
    const t = this.charAt(0);
    if (!t && !this.atEnd) return this.setNext("line-start");
    if (t === "-" || t === ".") {
      if (!this.atEnd && !this.hasChars(4)) return this.setNext("line-start");
      const n = this.peek(3);
      if ((n === "---" || n === "...") && isEmpty(this.charAt(3))) return yield* this.pushCount(3), this.indentValue = 0, this.indentNext = 0, n === "---" ? "doc" : "stream";
    }
    return this.indentValue = yield* this.pushSpaces(false), this.indentNext > this.indentValue && !isEmpty(this.charAt(1)) && (this.indentNext = this.indentValue), yield* this.parseBlockStart();
  }
  *parseBlockStart() {
    const [t, n] = this.peek(2);
    if (!n && !this.atEnd) return this.setNext("block-start");
    if ((t === "-" || t === "?" || t === ":") && isEmpty(n)) {
      const r = (yield* this.pushCount(1)) + (yield* this.pushSpaces(true));
      return this.indentNext = this.indentValue + 1, this.indentValue += r, yield* this.parseBlockStart();
    }
    return "doc";
  }
  *parseDocument() {
    yield* this.pushSpaces(true);
    const t = this.getLine();
    if (t === null) return this.setNext("doc");
    let n = yield* this.pushIndicators();
    switch (t[n]) {
      case "#":
        yield* this.pushCount(t.length - n);
      case void 0:
        return yield* this.pushNewline(), yield* this.parseLineStart();
      case "{":
      case "[":
        return yield* this.pushCount(1), this.flowKey = false, this.flowLevel = 1, "flow";
      case "}":
      case "]":
        return yield* this.pushCount(1), "doc";
      case "*":
        return yield* this.pushUntil(isNotAnchorChar), "doc";
      case '"':
      case "'":
        return yield* this.parseQuotedScalar();
      case "|":
      case ">":
        return n += yield* this.parseBlockScalarHeader(), n += yield* this.pushSpaces(true), yield* this.pushCount(t.length - n), yield* this.pushNewline(), yield* this.parseBlockScalar();
      default:
        return yield* this.parsePlainScalar();
    }
  }
  *parseFlowCollection() {
    let t, n, r = -1;
    do
      t = yield* this.pushNewline(), t > 0 ? (n = yield* this.pushSpaces(false), this.indentValue = r = n) : n = 0, n += yield* this.pushSpaces(true);
    while (t + n > 0);
    const s = this.getLine();
    if (s === null) return this.setNext("flow");
    if ((r !== -1 && r < this.indentNext && s[0] !== "#" || r === 0 && (s.startsWith("---") || s.startsWith("...")) && isEmpty(s[3])) && !(r === this.indentNext - 1 && this.flowLevel === 1 && (s[0] === "]" || s[0] === "}"))) return this.flowLevel = 0, yield cst$1.FLOW_END, yield* this.parseLineStart();
    let o = 0;
    for (; s[o] === ","; ) o += yield* this.pushCount(1), o += yield* this.pushSpaces(true), this.flowKey = false;
    switch (o += yield* this.pushIndicators(), s[o]) {
      case void 0:
        return "flow";
      case "#":
        return yield* this.pushCount(s.length - o), "flow";
      case "{":
      case "[":
        return yield* this.pushCount(1), this.flowKey = false, this.flowLevel += 1, "flow";
      case "}":
      case "]":
        return yield* this.pushCount(1), this.flowKey = true, this.flowLevel -= 1, this.flowLevel ? "flow" : "doc";
      case "*":
        return yield* this.pushUntil(isNotAnchorChar), "flow";
      case '"':
      case "'":
        return this.flowKey = true, yield* this.parseQuotedScalar();
      case ":": {
        const a = this.charAt(1);
        if (this.flowKey || isEmpty(a) || a === ",") return this.flowKey = false, yield* this.pushCount(1), yield* this.pushSpaces(true), "flow";
      }
      default:
        return this.flowKey = false, yield* this.parsePlainScalar();
    }
  }
  *parseQuotedScalar() {
    const t = this.charAt(0);
    let n = this.buffer.indexOf(t, this.pos + 1);
    if (t === "'") for (; n !== -1 && this.buffer[n + 1] === "'"; ) n = this.buffer.indexOf("'", n + 2);
    else for (; n !== -1; ) {
      let o = 0;
      for (; this.buffer[n - 1 - o] === "\\"; ) o += 1;
      if (o % 2 === 0) break;
      n = this.buffer.indexOf('"', n + 1);
    }
    const r = this.buffer.substring(0, n);
    let s = r.indexOf(`
`, this.pos);
    if (s !== -1) {
      for (; s !== -1; ) {
        const o = this.continueScalar(s + 1);
        if (o === -1) break;
        s = r.indexOf(`
`, o);
      }
      s !== -1 && (n = s - (r[s - 1] === "\r" ? 2 : 1));
    }
    if (n === -1) {
      if (!this.atEnd) return this.setNext("quoted-scalar");
      n = this.buffer.length;
    }
    return yield* this.pushToIndex(n + 1, false), this.flowLevel ? "flow" : "doc";
  }
  *parseBlockScalarHeader() {
    this.blockScalarIndent = -1, this.blockScalarKeep = false;
    let t = this.pos;
    for (; ; ) {
      const n = this.buffer[++t];
      if (n === "+") this.blockScalarKeep = true;
      else if (n > "0" && n <= "9") this.blockScalarIndent = Number(n) - 1;
      else if (n !== "-") break;
    }
    return yield* this.pushUntil((n) => isEmpty(n) || n === "#");
  }
  *parseBlockScalar() {
    let t = this.pos - 1, n = 0, r;
    e: for (let o = this.pos; r = this.buffer[o]; ++o) switch (r) {
      case " ":
        n += 1;
        break;
      case `
`:
        t = o, n = 0;
        break;
      case "\r": {
        const a = this.buffer[o + 1];
        if (!a && !this.atEnd) return this.setNext("block-scalar");
        if (a === `
`) break;
      }
      default:
        break e;
    }
    if (!r && !this.atEnd) return this.setNext("block-scalar");
    if (n >= this.indentNext) {
      this.blockScalarIndent === -1 ? this.indentNext = n : this.indentNext = this.blockScalarIndent + (this.indentNext === 0 ? 1 : this.indentNext);
      do {
        const o = this.continueScalar(t + 1);
        if (o === -1) break;
        t = this.buffer.indexOf(`
`, o);
      } while (t !== -1);
      if (t === -1) {
        if (!this.atEnd) return this.setNext("block-scalar");
        t = this.buffer.length;
      }
    }
    let s = t + 1;
    for (r = this.buffer[s]; r === " "; ) r = this.buffer[++s];
    if (r === "	") {
      for (; r === "	" || r === " " || r === "\r" || r === `
`; ) r = this.buffer[++s];
      t = s - 1;
    } else if (!this.blockScalarKeep) do {
      let o = t - 1, a = this.buffer[o];
      a === "\r" && (a = this.buffer[--o]);
      const l = o;
      for (; a === " "; ) a = this.buffer[--o];
      if (a === `
` && o >= this.pos && o + 1 + n > l) t = o;
      else break;
    } while (true);
    return yield cst$1.SCALAR, yield* this.pushToIndex(t + 1, true), yield* this.parseLineStart();
  }
  *parsePlainScalar() {
    const t = this.flowLevel > 0;
    let n = this.pos - 1, r = this.pos - 1, s;
    for (; s = this.buffer[++r]; ) if (s === ":") {
      const o = this.buffer[r + 1];
      if (isEmpty(o) || t && flowIndicatorChars.has(o)) break;
      n = r;
    } else if (isEmpty(s)) {
      let o = this.buffer[r + 1];
      if (s === "\r" && (o === `
` ? (r += 1, s = `
`, o = this.buffer[r + 1]) : n = r), o === "#" || t && flowIndicatorChars.has(o)) break;
      if (s === `
`) {
        const a = this.continueScalar(r + 1);
        if (a === -1) break;
        r = Math.max(r, a - 2);
      }
    } else {
      if (t && flowIndicatorChars.has(s)) break;
      n = r;
    }
    return !s && !this.atEnd ? this.setNext("plain-scalar") : (yield cst$1.SCALAR, yield* this.pushToIndex(n + 1, true), t ? "flow" : "doc");
  }
  *pushCount(t) {
    return t > 0 ? (yield this.buffer.substr(this.pos, t), this.pos += t, t) : 0;
  }
  *pushToIndex(t, n) {
    const r = this.buffer.slice(this.pos, t);
    return r ? (yield r, this.pos += r.length, r.length) : (n && (yield ""), 0);
  }
  *pushIndicators() {
    switch (this.charAt(0)) {
      case "!":
        return (yield* this.pushTag()) + (yield* this.pushSpaces(true)) + (yield* this.pushIndicators());
      case "&":
        return (yield* this.pushUntil(isNotAnchorChar)) + (yield* this.pushSpaces(true)) + (yield* this.pushIndicators());
      case "-":
      case "?":
      case ":": {
        const t = this.flowLevel > 0, n = this.charAt(1);
        if (isEmpty(n) || t && flowIndicatorChars.has(n)) return t ? this.flowKey && (this.flowKey = false) : this.indentNext = this.indentValue + 1, (yield* this.pushCount(1)) + (yield* this.pushSpaces(true)) + (yield* this.pushIndicators());
      }
    }
    return 0;
  }
  *pushTag() {
    if (this.charAt(1) === "<") {
      let t = this.pos + 2, n = this.buffer[t];
      for (; !isEmpty(n) && n !== ">"; ) n = this.buffer[++t];
      return yield* this.pushToIndex(n === ">" ? t + 1 : t, false);
    } else {
      let t = this.pos + 1, n = this.buffer[t];
      for (; n; ) if (tagChars.has(n)) n = this.buffer[++t];
      else if (n === "%" && hexDigits.has(this.buffer[t + 1]) && hexDigits.has(this.buffer[t + 2])) n = this.buffer[t += 3];
      else break;
      return yield* this.pushToIndex(t, false);
    }
  }
  *pushNewline() {
    const t = this.buffer[this.pos];
    return t === `
` ? yield* this.pushCount(1) : t === "\r" && this.charAt(1) === `
` ? yield* this.pushCount(2) : 0;
  }
  *pushSpaces(t) {
    let n = this.pos - 1, r;
    do
      r = this.buffer[++n];
    while (r === " " || t && r === "	");
    const s = n - this.pos;
    return s > 0 && (yield this.buffer.substr(this.pos, s), this.pos = n), s;
  }
  *pushUntil(t) {
    let n = this.pos, r = this.buffer[n];
    for (; !t(r); ) r = this.buffer[++n];
    return yield* this.pushToIndex(n, false);
  }
}
lexer$1.Lexer = Lexer;
var lineCounter$1 = {};
class LineCounter {
  constructor() {
    this.lineStarts = [], this.addNewLine = (t) => this.lineStarts.push(t), this.linePos = (t) => {
      let n = 0, r = this.lineStarts.length;
      for (; n < r; ) {
        const o = n + r >> 1;
        this.lineStarts[o] < t ? n = o + 1 : r = o;
      }
      if (this.lineStarts[n] === t) return { line: n + 1, col: 1 };
      if (n === 0) return { line: 0, col: t };
      const s = this.lineStarts[n - 1];
      return { line: n, col: t - s + 1 };
    };
  }
}
lineCounter$1.LineCounter = LineCounter;
var parser$1 = {}, node_process = require$$0, cst = cst$2, lexer = lexer$1;
function includesToken(e5, t) {
  for (let n = 0; n < e5.length; ++n) if (e5[n].type === t) return true;
  return false;
}
function findNonEmptyIndex(e5) {
  for (let t = 0; t < e5.length; ++t) switch (e5[t].type) {
    case "space":
    case "comment":
    case "newline":
      break;
    default:
      return t;
  }
  return -1;
}
function isFlowToken(e5) {
  switch (e5 == null ? void 0 : e5.type) {
    case "alias":
    case "scalar":
    case "single-quoted-scalar":
    case "double-quoted-scalar":
    case "flow-collection":
      return true;
    default:
      return false;
  }
}
function getPrevProps(e5) {
  var _a4;
  switch (e5.type) {
    case "document":
      return e5.start;
    case "block-map": {
      const t = e5.items[e5.items.length - 1];
      return (_a4 = t.sep) != null ? _a4 : t.start;
    }
    case "block-seq":
      return e5.items[e5.items.length - 1].start;
    default:
      return [];
  }
}
function getFirstKeyStartProps(e5) {
  var _a4;
  if (e5.length === 0) return [];
  let t = e5.length;
  e: for (; --t >= 0; ) switch (e5[t].type) {
    case "doc-start":
    case "explicit-key-ind":
    case "map-value-ind":
    case "seq-item-ind":
    case "newline":
      break e;
  }
  for (; ((_a4 = e5[++t]) == null ? void 0 : _a4.type) === "space"; ) ;
  return e5.splice(t, e5.length);
}
function fixFlowSeqItems(e5) {
  if (e5.start.type === "flow-seq-start") for (const t of e5.items) t.sep && !t.value && !includesToken(t.start, "explicit-key-ind") && !includesToken(t.sep, "map-value-ind") && (t.key && (t.value = t.key), delete t.key, isFlowToken(t.value) ? t.value.end ? Array.prototype.push.apply(t.value.end, t.sep) : t.value.end = t.sep : Array.prototype.push.apply(t.start, t.sep), delete t.sep);
}
class Parser {
  constructor(t) {
    this.atNewLine = true, this.atScalar = false, this.indent = 0, this.offset = 0, this.onKeyLine = false, this.stack = [], this.source = "", this.type = "", this.lexer = new lexer.Lexer(), this.onNewLine = t;
  }
  *parse(t, n = false) {
    this.onNewLine && this.offset === 0 && this.onNewLine(0);
    for (const r of this.lexer.lex(t, n)) yield* this.next(r);
    n || (yield* this.end());
  }
  *next(t) {
    if (this.source = t, node_process.env.LOG_TOKENS && console.log("|", cst.prettyToken(t)), this.atScalar) {
      this.atScalar = false, yield* this.step(), this.offset += t.length;
      return;
    }
    const n = cst.tokenType(t);
    if (n) if (n === "scalar") this.atNewLine = false, this.atScalar = true, this.type = "scalar";
    else {
      switch (this.type = n, yield* this.step(), n) {
        case "newline":
          this.atNewLine = true, this.indent = 0, this.onNewLine && this.onNewLine(this.offset + t.length);
          break;
        case "space":
          this.atNewLine && t[0] === " " && (this.indent += t.length);
          break;
        case "explicit-key-ind":
        case "map-value-ind":
        case "seq-item-ind":
          this.atNewLine && (this.indent += t.length);
          break;
        case "doc-mode":
        case "flow-error-end":
          return;
        default:
          this.atNewLine = false;
      }
      this.offset += t.length;
    }
    else {
      const r = `Not a YAML token: ${t}`;
      yield* this.pop({ type: "error", offset: this.offset, message: r, source: t }), this.offset += t.length;
    }
  }
  *end() {
    for (; this.stack.length > 0; ) yield* this.pop();
  }
  get sourceToken() {
    return { type: this.type, offset: this.offset, indent: this.indent, source: this.source };
  }
  *step() {
    const t = this.peek(1);
    if (this.type === "doc-end" && (t == null ? void 0 : t.type) !== "doc-end") {
      for (; this.stack.length > 0; ) yield* this.pop();
      this.stack.push({ type: "doc-end", offset: this.offset, source: this.source });
      return;
    }
    if (!t) return yield* this.stream();
    switch (t.type) {
      case "document":
        return yield* this.document(t);
      case "alias":
      case "scalar":
      case "single-quoted-scalar":
      case "double-quoted-scalar":
        return yield* this.scalar(t);
      case "block-scalar":
        return yield* this.blockScalar(t);
      case "block-map":
        return yield* this.blockMap(t);
      case "block-seq":
        return yield* this.blockSequence(t);
      case "flow-collection":
        return yield* this.flowCollection(t);
      case "doc-end":
        return yield* this.documentEnd(t);
    }
    yield* this.pop();
  }
  peek(t) {
    return this.stack[this.stack.length - t];
  }
  *pop(t) {
    const n = t != null ? t : this.stack.pop();
    if (!n) yield { type: "error", offset: this.offset, source: "", message: "Tried to pop an empty stack" };
    else if (this.stack.length === 0) yield n;
    else {
      const r = this.peek(1);
      switch (n.type === "block-scalar" ? n.indent = "indent" in r ? r.indent : 0 : n.type === "flow-collection" && r.type === "document" && (n.indent = 0), n.type === "flow-collection" && fixFlowSeqItems(n), r.type) {
        case "document":
          r.value = n;
          break;
        case "block-scalar":
          r.props.push(n);
          break;
        case "block-map": {
          const s = r.items[r.items.length - 1];
          if (s.value) {
            r.items.push({ start: [], key: n, sep: [] }), this.onKeyLine = true;
            return;
          } else if (s.sep) s.value = n;
          else {
            Object.assign(s, { key: n, sep: [] }), this.onKeyLine = !s.explicitKey;
            return;
          }
          break;
        }
        case "block-seq": {
          const s = r.items[r.items.length - 1];
          s.value ? r.items.push({ start: [], value: n }) : s.value = n;
          break;
        }
        case "flow-collection": {
          const s = r.items[r.items.length - 1];
          !s || s.value ? r.items.push({ start: [], key: n, sep: [] }) : s.sep ? s.value = n : Object.assign(s, { key: n, sep: [] });
          return;
        }
        default:
          yield* this.pop(), yield* this.pop(n);
      }
      if ((r.type === "document" || r.type === "block-map" || r.type === "block-seq") && (n.type === "block-map" || n.type === "block-seq")) {
        const s = n.items[n.items.length - 1];
        s && !s.sep && !s.value && s.start.length > 0 && findNonEmptyIndex(s.start) === -1 && (n.indent === 0 || s.start.every((o) => o.type !== "comment" || o.indent < n.indent)) && (r.type === "document" ? r.end = s.start : r.items.push({ start: s.start }), n.items.splice(-1, 1));
      }
    }
  }
  *stream() {
    switch (this.type) {
      case "directive-line":
        yield { type: "directive", offset: this.offset, source: this.source };
        return;
      case "byte-order-mark":
      case "space":
      case "comment":
      case "newline":
        yield this.sourceToken;
        return;
      case "doc-mode":
      case "doc-start": {
        const t = { type: "document", offset: this.offset, start: [] };
        this.type === "doc-start" && t.start.push(this.sourceToken), this.stack.push(t);
        return;
      }
    }
    yield { type: "error", offset: this.offset, message: `Unexpected ${this.type} token in YAML stream`, source: this.source };
  }
  *document(t) {
    if (t.value) return yield* this.lineEnd(t);
    switch (this.type) {
      case "doc-start": {
        findNonEmptyIndex(t.start) !== -1 ? (yield* this.pop(), yield* this.step()) : t.start.push(this.sourceToken);
        return;
      }
      case "anchor":
      case "tag":
      case "space":
      case "comment":
      case "newline":
        t.start.push(this.sourceToken);
        return;
    }
    const n = this.startBlockValue(t);
    n ? this.stack.push(n) : yield { type: "error", offset: this.offset, message: `Unexpected ${this.type} token in YAML document`, source: this.source };
  }
  *scalar(t) {
    if (this.type === "map-value-ind") {
      const n = getPrevProps(this.peek(2)), r = getFirstKeyStartProps(n);
      let s;
      t.end ? (s = t.end, s.push(this.sourceToken), delete t.end) : s = [this.sourceToken];
      const o = { type: "block-map", offset: t.offset, indent: t.indent, items: [{ start: r, key: t, sep: s }] };
      this.onKeyLine = true, this.stack[this.stack.length - 1] = o;
    } else yield* this.lineEnd(t);
  }
  *blockScalar(t) {
    switch (this.type) {
      case "space":
      case "comment":
      case "newline":
        t.props.push(this.sourceToken);
        return;
      case "scalar":
        if (t.source = this.source, this.atNewLine = true, this.indent = 0, this.onNewLine) {
          let n = this.source.indexOf(`
`) + 1;
          for (; n !== 0; ) this.onNewLine(this.offset + n), n = this.source.indexOf(`
`, n) + 1;
        }
        yield* this.pop();
        break;
      default:
        yield* this.pop(), yield* this.step();
    }
  }
  *blockMap(t) {
    var _a4, _b, _c2;
    const n = t.items[t.items.length - 1];
    switch (this.type) {
      case "newline":
        if (this.onKeyLine = false, n.value) {
          const r = "end" in n.value ? n.value.end : void 0;
          ((_a4 = Array.isArray(r) ? r[r.length - 1] : void 0) == null ? void 0 : _a4.type) === "comment" ? r == null ? void 0 : r.push(this.sourceToken) : t.items.push({ start: [this.sourceToken] });
        } else n.sep ? n.sep.push(this.sourceToken) : n.start.push(this.sourceToken);
        return;
      case "space":
      case "comment":
        if (n.value) t.items.push({ start: [this.sourceToken] });
        else if (n.sep) n.sep.push(this.sourceToken);
        else {
          if (this.atIndentedComment(n.start, t.indent)) {
            const s = (_c2 = (_b = t.items[t.items.length - 2]) == null ? void 0 : _b.value) == null ? void 0 : _c2.end;
            if (Array.isArray(s)) {
              Array.prototype.push.apply(s, n.start), s.push(this.sourceToken), t.items.pop();
              return;
            }
          }
          n.start.push(this.sourceToken);
        }
        return;
    }
    if (this.indent >= t.indent) {
      const r = !this.onKeyLine && this.indent === t.indent, s = r && (n.sep || n.explicitKey) && this.type !== "seq-item-ind";
      let o = [];
      if (s && n.sep && !n.value) {
        const a = [];
        for (let l = 0; l < n.sep.length; ++l) {
          const c = n.sep[l];
          switch (c.type) {
            case "newline":
              a.push(l);
              break;
            case "space":
              break;
            case "comment":
              c.indent > t.indent && (a.length = 0);
              break;
            default:
              a.length = 0;
          }
        }
        a.length >= 2 && (o = n.sep.splice(a[1]));
      }
      switch (this.type) {
        case "anchor":
        case "tag":
          s || n.value ? (o.push(this.sourceToken), t.items.push({ start: o }), this.onKeyLine = true) : n.sep ? n.sep.push(this.sourceToken) : n.start.push(this.sourceToken);
          return;
        case "explicit-key-ind":
          !n.sep && !n.explicitKey ? (n.start.push(this.sourceToken), n.explicitKey = true) : s || n.value ? (o.push(this.sourceToken), t.items.push({ start: o, explicitKey: true })) : this.stack.push({ type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: [this.sourceToken], explicitKey: true }] }), this.onKeyLine = true;
          return;
        case "map-value-ind":
          if (n.explicitKey) if (n.sep) if (n.value) t.items.push({ start: [], key: null, sep: [this.sourceToken] });
          else if (includesToken(n.sep, "map-value-ind")) this.stack.push({ type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: o, key: null, sep: [this.sourceToken] }] });
          else if (isFlowToken(n.key) && !includesToken(n.sep, "newline")) {
            const a = getFirstKeyStartProps(n.start), l = n.key, c = n.sep;
            c.push(this.sourceToken), delete n.key, delete n.sep, this.stack.push({ type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: a, key: l, sep: c }] });
          } else o.length > 0 ? n.sep = n.sep.concat(o, this.sourceToken) : n.sep.push(this.sourceToken);
          else if (includesToken(n.start, "newline")) Object.assign(n, { key: null, sep: [this.sourceToken] });
          else {
            const a = getFirstKeyStartProps(n.start);
            this.stack.push({ type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: a, key: null, sep: [this.sourceToken] }] });
          }
          else n.sep ? n.value || s ? t.items.push({ start: o, key: null, sep: [this.sourceToken] }) : includesToken(n.sep, "map-value-ind") ? this.stack.push({ type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: [], key: null, sep: [this.sourceToken] }] }) : n.sep.push(this.sourceToken) : Object.assign(n, { key: null, sep: [this.sourceToken] });
          this.onKeyLine = true;
          return;
        case "alias":
        case "scalar":
        case "single-quoted-scalar":
        case "double-quoted-scalar": {
          const a = this.flowScalar(this.type);
          s || n.value ? (t.items.push({ start: o, key: a, sep: [] }), this.onKeyLine = true) : n.sep ? this.stack.push(a) : (Object.assign(n, { key: a, sep: [] }), this.onKeyLine = true);
          return;
        }
        default: {
          const a = this.startBlockValue(t);
          if (a) {
            if (a.type === "block-seq") {
              if (!n.explicitKey && n.sep && !includesToken(n.sep, "newline")) {
                yield* this.pop({ type: "error", offset: this.offset, message: "Unexpected block-seq-ind on same line with key", source: this.source });
                return;
              }
            } else r && t.items.push({ start: o });
            this.stack.push(a);
            return;
          }
        }
      }
    }
    yield* this.pop(), yield* this.step();
  }
  *blockSequence(t) {
    var _a4, _b, _c2;
    const n = t.items[t.items.length - 1];
    switch (this.type) {
      case "newline":
        if (n.value) {
          const r = "end" in n.value ? n.value.end : void 0;
          ((_a4 = Array.isArray(r) ? r[r.length - 1] : void 0) == null ? void 0 : _a4.type) === "comment" ? r == null ? void 0 : r.push(this.sourceToken) : t.items.push({ start: [this.sourceToken] });
        } else n.start.push(this.sourceToken);
        return;
      case "space":
      case "comment":
        if (n.value) t.items.push({ start: [this.sourceToken] });
        else {
          if (this.atIndentedComment(n.start, t.indent)) {
            const s = (_c2 = (_b = t.items[t.items.length - 2]) == null ? void 0 : _b.value) == null ? void 0 : _c2.end;
            if (Array.isArray(s)) {
              Array.prototype.push.apply(s, n.start), s.push(this.sourceToken), t.items.pop();
              return;
            }
          }
          n.start.push(this.sourceToken);
        }
        return;
      case "anchor":
      case "tag":
        if (n.value || this.indent <= t.indent) break;
        n.start.push(this.sourceToken);
        return;
      case "seq-item-ind":
        if (this.indent !== t.indent) break;
        n.value || includesToken(n.start, "seq-item-ind") ? t.items.push({ start: [this.sourceToken] }) : n.start.push(this.sourceToken);
        return;
    }
    if (this.indent > t.indent) {
      const r = this.startBlockValue(t);
      if (r) {
        this.stack.push(r);
        return;
      }
    }
    yield* this.pop(), yield* this.step();
  }
  *flowCollection(t) {
    const n = t.items[t.items.length - 1];
    if (this.type === "flow-error-end") {
      let r;
      do
        yield* this.pop(), r = this.peek(1);
      while ((r == null ? void 0 : r.type) === "flow-collection");
    } else if (t.end.length === 0) {
      switch (this.type) {
        case "comma":
        case "explicit-key-ind":
          !n || n.sep ? t.items.push({ start: [this.sourceToken] }) : n.start.push(this.sourceToken);
          return;
        case "map-value-ind":
          !n || n.value ? t.items.push({ start: [], key: null, sep: [this.sourceToken] }) : n.sep ? n.sep.push(this.sourceToken) : Object.assign(n, { key: null, sep: [this.sourceToken] });
          return;
        case "space":
        case "comment":
        case "newline":
        case "anchor":
        case "tag":
          !n || n.value ? t.items.push({ start: [this.sourceToken] }) : n.sep ? n.sep.push(this.sourceToken) : n.start.push(this.sourceToken);
          return;
        case "alias":
        case "scalar":
        case "single-quoted-scalar":
        case "double-quoted-scalar": {
          const s = this.flowScalar(this.type);
          !n || n.value ? t.items.push({ start: [], key: s, sep: [] }) : n.sep ? this.stack.push(s) : Object.assign(n, { key: s, sep: [] });
          return;
        }
        case "flow-map-end":
        case "flow-seq-end":
          t.end.push(this.sourceToken);
          return;
      }
      const r = this.startBlockValue(t);
      r ? this.stack.push(r) : (yield* this.pop(), yield* this.step());
    } else {
      const r = this.peek(2);
      if (r.type === "block-map" && (this.type === "map-value-ind" && r.indent === t.indent || this.type === "newline" && !r.items[r.items.length - 1].sep)) yield* this.pop(), yield* this.step();
      else if (this.type === "map-value-ind" && r.type !== "flow-collection") {
        const s = getPrevProps(r), o = getFirstKeyStartProps(s);
        fixFlowSeqItems(t);
        const a = t.end.splice(1, t.end.length);
        a.push(this.sourceToken);
        const l = { type: "block-map", offset: t.offset, indent: t.indent, items: [{ start: o, key: t, sep: a }] };
        this.onKeyLine = true, this.stack[this.stack.length - 1] = l;
      } else yield* this.lineEnd(t);
    }
  }
  flowScalar(t) {
    if (this.onNewLine) {
      let n = this.source.indexOf(`
`) + 1;
      for (; n !== 0; ) this.onNewLine(this.offset + n), n = this.source.indexOf(`
`, n) + 1;
    }
    return { type: t, offset: this.offset, indent: this.indent, source: this.source };
  }
  startBlockValue(t) {
    switch (this.type) {
      case "alias":
      case "scalar":
      case "single-quoted-scalar":
      case "double-quoted-scalar":
        return this.flowScalar(this.type);
      case "block-scalar-header":
        return { type: "block-scalar", offset: this.offset, indent: this.indent, props: [this.sourceToken], source: "" };
      case "flow-map-start":
      case "flow-seq-start":
        return { type: "flow-collection", offset: this.offset, indent: this.indent, start: this.sourceToken, items: [], end: [] };
      case "seq-item-ind":
        return { type: "block-seq", offset: this.offset, indent: this.indent, items: [{ start: [this.sourceToken] }] };
      case "explicit-key-ind": {
        this.onKeyLine = true;
        const n = getPrevProps(t), r = getFirstKeyStartProps(n);
        return r.push(this.sourceToken), { type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: r, explicitKey: true }] };
      }
      case "map-value-ind": {
        this.onKeyLine = true;
        const n = getPrevProps(t), r = getFirstKeyStartProps(n);
        return { type: "block-map", offset: this.offset, indent: this.indent, items: [{ start: r, key: null, sep: [this.sourceToken] }] };
      }
    }
    return null;
  }
  atIndentedComment(t, n) {
    return this.type !== "comment" || this.indent <= n ? false : t.every((r) => r.type === "newline" || r.type === "space");
  }
  *documentEnd(t) {
    this.type !== "doc-mode" && (t.end ? t.end.push(this.sourceToken) : t.end = [this.sourceToken], this.type === "newline" && (yield* this.pop()));
  }
  *lineEnd(t) {
    switch (this.type) {
      case "comma":
      case "doc-start":
      case "doc-end":
      case "flow-seq-end":
      case "flow-map-end":
      case "map-value-ind":
        yield* this.pop(), yield* this.step();
        break;
      case "newline":
        this.onKeyLine = false;
      case "space":
      case "comment":
      default:
        t.end ? t.end.push(this.sourceToken) : t.end = [this.sourceToken], this.type === "newline" && (yield* this.pop());
    }
  }
}
parser$1.Parser = Parser;
var publicApi$1 = {}, composer = composer$1, Document = Document$4, errors$1 = errors$4, log = log$2, identity$2 = identity$y, lineCounter = lineCounter$1, parser = parser$1;
function parseOptions(e5) {
  const t = e5.prettyErrors !== false;
  return { lineCounter: e5.lineCounter || t && new lineCounter.LineCounter() || null, prettyErrors: t };
}
function parseAllDocuments(e5, t = {}) {
  const { lineCounter: n, prettyErrors: r } = parseOptions(t), s = new parser.Parser(n == null ? void 0 : n.addNewLine), o = new composer.Composer(t), a = Array.from(o.compose(s.parse(e5)));
  if (r && n) for (const l of a) l.errors.forEach(errors$1.prettifyError(e5, n)), l.warnings.forEach(errors$1.prettifyError(e5, n));
  return a.length > 0 ? a : Object.assign([], { empty: true }, o.streamInfo());
}
function parseDocument(e5, t = {}) {
  const { lineCounter: n, prettyErrors: r } = parseOptions(t), s = new parser.Parser(n == null ? void 0 : n.addNewLine), o = new composer.Composer(t);
  let a = null;
  for (const l of o.compose(s.parse(e5), true, e5.length)) if (!a) a = l;
  else if (a.options.logLevel !== "silent") {
    a.errors.push(new errors$1.YAMLParseError(l.range.slice(0, 2), "MULTIPLE_DOCS", "Source contains multiple documents; please use YAML.parseAllDocuments()"));
    break;
  }
  return r && n && (a.errors.forEach(errors$1.prettifyError(e5, n)), a.warnings.forEach(errors$1.prettifyError(e5, n))), a;
}
function parse$2(e5, t, n) {
  let r;
  typeof t == "function" ? r = t : n === void 0 && t && typeof t == "object" && (n = t);
  const s = parseDocument(e5, n);
  if (!s) return null;
  if (s.warnings.forEach((o) => log.warn(s.options.logLevel, o)), s.errors.length > 0) {
    if (s.options.logLevel !== "silent") throw s.errors[0];
    s.errors = [];
  }
  return s.toJS(Object.assign({ reviver: r }, n));
}
function stringify$4(e5, t, n) {
  var _a4;
  let r = null;
  if (typeof t == "function" || Array.isArray(t) ? r = t : n === void 0 && t && (n = t), typeof n == "string" && (n = n.length), typeof n == "number") {
    const s = Math.round(n);
    n = s < 1 ? void 0 : s > 8 ? { indent: 8 } : { indent: s };
  }
  if (e5 === void 0) {
    const { keepUndefined: s } = (_a4 = n != null ? n : t) != null ? _a4 : {};
    if (!s) return;
  }
  return identity$2.isDocument(e5) && !r ? e5.toString(n) : new Document.Document(e5, r, n).toString(n);
}
publicApi$1.parse = parse$2;
publicApi$1.parseAllDocuments = parseAllDocuments;
publicApi$1.parseDocument = parseDocument;
publicApi$1.stringify = stringify$4;
var errors = errors$4, identity$1 = identity$y, Pair = Pair$8, Scalar = Scalar$k, YAMLMap = YAMLMap$8, publicApi = publicApi$1, visit = visit$6;
errors.YAMLError;
errors.YAMLParseError;
errors.YAMLWarning;
identity$1.isAlias;
identity$1.isCollection;
identity$1.isDocument;
identity$1.isMap;
identity$1.isNode;
identity$1.isPair;
identity$1.isScalar;
identity$1.isSeq;
Pair.Pair;
Scalar.Scalar;
YAMLMap.YAMLMap;
var stringify$3 = publicApi.stringify;
visit.visit;
visit.visitAsync;
function assetToJsonApi(e5, t) {
  const { key: n, ...r } = e5, s = t == null ? void 0 : t.advertiseRelationships, o = (s == null ? void 0 : s.urls) || (s == null ? void 0 : s.variations) ? { ...s.urls ? { urls: { data: { type: "asset-url", id: n } } } : {}, ...s.variations ? { variations: { data: { type: "asset-variation", id: n } } } : {} } : void 0, a = { type: "asset", id: n, attributes: r };
  return (t == null ? void 0 : t.metadata) && (a.meta = t.metadata), o && (a.relationships = o), a;
}
function folderToJsonApi$1(e5) {
  const { key: t, ...n } = e5;
  return { type: "folder", id: t, attributes: n };
}
function resourceToJsonApi(e5, t) {
  return e5.type === "asset" ? assetToJsonApi(e5, t) : folderToJsonApi$1(e5);
}
function assetVariationsToJsonApi(e5) {
  const { key: t, ...n } = e5;
  return { type: "asset-variation", id: t, attributes: n };
}
function assetUrlToJsonApi(e5) {
  const { key: t, ...n } = e5;
  return { type: "asset-url", id: t, attributes: n };
}
function parseIncludeQuery(e5) {
  if (!e5) return { urls: false, variations: false };
  const t = e5.split(",").map((n) => n.trim().toLowerCase());
  return { urls: t.includes("urls"), variations: t.includes("variations") };
}
function parseMetaQuery(e5) {
  if (!e5) return { metadata: false };
  const t = e5.trim().toLowerCase();
  return { metadata: t === "true" || t === "1" || t === "yes" };
}
function parsePaginationQuery$1(e5) {
  return { limit: e5["page[limit]"] ? parseInt(e5["page[limit]"], 10) : void 0, cursor: e5["page[cursor]"], direction: e5["page[direction]"] === "backward" ? "backward" : "forward" };
}
function buildPaginationLinks$1(e5, t, n, r, s) {
  const o = new URL(e5), a = { self: e5, first: null, last: null, prev: null, next: null }, l = new URL(o);
  if (l.searchParams.delete("page[cursor]"), t.limit && l.searchParams.set("page[limit]", String(t.limit)), a.first = l.toString(), n && r) {
    const c = new URL(o);
    c.searchParams.set("page[cursor]", r), t.limit && c.searchParams.set("page[limit]", String(t.limit)), a.next = c.toString();
  }
  if (s) {
    const c = new URL(o);
    c.searchParams.set("page[cursor]", s), c.searchParams.set("page[direction]", "backward"), t.limit && c.searchParams.set("page[limit]", String(t.limit)), a.prev = c.toString();
  }
  return a;
}
const isAtLeast1 = cs((e5) => e5 >= 1 ? void 0 : "Must be at least 1"), isAtLeast0 = cs((e5) => e5 >= 0 ? void 0 : "Must be at least 0"), PaginationPageBasedSchema = re$1(A({ page: Me$1.pipe($e$1(isAtLeast1)), perPage: V$1(Me$1.pipe($e$1(isAtLeast1))) })), PaginationBeforeSchema = re$1(A({ before: V$1(h$1), perPage: V$1(Me$1.pipe($e$1(isAtLeast1))) })), PaginationAfterSchema = re$1(A({ after: V$1(h$1), perPage: V$1(Me$1.pipe($e$1(isAtLeast1))) })), PaginationOffsetSchema = re$1(A({ offset: Me$1.pipe($e$1(isAtLeast0)), limit: V$1(Me$1.pipe($e$1(isAtLeast1))) }));
re$1(be$1([PaginationPageBasedSchema, PaginationBeforeSchema, PaginationAfterSchema, PaginationOffsetSchema]));
function isPageBased(e5) {
  return "page" in e5 && typeof e5.page == "number";
}
function isOffsetBased(e5) {
  return "offset" in e5 && typeof e5.offset == "number";
}
function isAfterBased(e5) {
  return "after" in e5;
}
function isBeforeBased(e5) {
  return "before" in e5 && !("after" in e5);
}
function buildPaginationLinks(e5, t, n, r, s, o) {
  const a = { self: e5 };
  if (isAfterBased(t)) {
    const l = t.perPage;
    t.after && s && (a.prev = `${e5}?page[before]=${encodeURIComponent(s)}`, l && (a.prev += `&page[size]=${l}`)), n && o && (a.next = `${e5}?page[after]=${encodeURIComponent(o)}`, l && (a.next += `&page[size]=${l}`));
  } else if (isBeforeBased(t)) {
    const l = t.perPage;
    n && s && (a.prev = `${e5}?page[before]=${encodeURIComponent(s)}`, l && (a.prev += `&page[size]=${l}`)), t.before && o && (a.next = `${e5}?page[after]=${encodeURIComponent(o)}`, l && (a.next += `&page[size]=${l}`));
  } else if (isPageBased(t)) {
    const l = t.page, c = t.perPage || 10;
    a.first = `${e5}?page[number]=1&page[size]=${c}`, l > 1 && (a.prev = `${e5}?page[number]=${l - 1}&page[size]=${c}`), n && (a.next = `${e5}?page[number]=${l + 1}&page[size]=${c}`);
  } else if (isOffsetBased(t)) {
    const l = t.offset, c = t.limit || 10;
    if (a.first = `${e5}?page[offset]=0&page[limit]=${c}`, l > 0) {
      const u = Math.max(0, l - c);
      a.prev = `${e5}?page[offset]=${u}&page[limit]=${c}`;
    }
    n && (a.next = `${e5}?page[offset]=${l + c}&page[limit]=${c}`);
  }
  return a;
}
function parsePaginationQuery(e5) {
  const t = e5["page[after]"], n = e5["page[before]"], r = e5["page[number]"], s = e5["page[size]"], o = e5["page[offset]"], a = e5["page[limit]"];
  return t ? { after: Array.isArray(t) ? t[0] : t, perPage: s ? parseInt(Array.isArray(s) ? s[0] : s) : void 0 } : n ? { before: Array.isArray(n) ? n[0] : n, perPage: s ? parseInt(Array.isArray(s) ? s[0] : s) : void 0 } : r ? { page: parseInt(Array.isArray(r) ? r[0] : r), perPage: s ? parseInt(Array.isArray(s) ? s[0] : s) : void 0 } : o !== void 0 ? { offset: parseInt(Array.isArray(o) ? o[0] : o), limit: a ? parseInt(Array.isArray(a) ? a[0] : a) : void 0 } : { after: void 0, perPage: 10 };
}
const toLaikaError$2 = (e5) => e5 instanceof b$1 ? e5 : e5 instanceof Error ? new Ge(e5.message, { cause: e5 }) : new Ge(String(e5)), firstResultWithMetadata$1 = async (e5) => {
  try {
    const t = await xe$2(di(oc(e5)));
    return X$1(t) ? we$2(t.failure) : lt({ value: t.success.value, recoverableErrors: t.success.recoverableErrors });
  } catch (t) {
    return we$2(toLaikaError$2(t));
  }
}, firstResult$1 = async (e5) => {
  try {
    return await xe$2(di(us(e5)));
  } catch (t) {
    return we$2(toLaikaError$2(t));
  }
}, runStream$1 = async (e5) => {
  try {
    const t = await xe$2(di(fs$1(e5)));
    return X$1(t) ? we$2(t.failure) : lt(t.success.data);
  } catch (t) {
    return we$2(toLaikaError$2(t));
  }
}, runStreamWithDone$1 = async (e5) => {
  try {
    const t = await xe$2(di(fs$1(e5)));
    return X$1(t) ? we$2(t.failure) : lt({ data: t.success.data, done: t.success.done, recoverableErrors: t.success.recoverableErrors });
  } catch (t) {
    return we$2(toLaikaError$2(t));
  }
}, json$4 = (e5, t = 200, n = {}) => new Response(JSON.stringify(e5), { status: t, headers: { "Content-Type": "application/vnd.api+json", "Cache-Control": "no-store", ...n } });
function respondError$2(e5, t = 400) {
  return json$4({ errors: [{ status: String(t), code: e5.code, detail: e5.message }] }, t);
}
function respondValidationError(e5, t = 400) {
  return json$4({ errors: e5.map((n) => ({ status: String(t), code: "validation_error", detail: n.message })) }, t);
}
const assetsSelfPathFor = (e5, t, n) => {
  const r = encodeURIComponent(n);
  switch (t) {
    case "asset":
    case "folder":
      return `${e5}/resources/${r}`;
    case "asset-url":
    case "asset-variation":
      return;
    case "assets-capabilities":
      return `${e5}/capabilities`;
    default:
      return;
  }
}, withAssetsSelfLink = (e5, t) => {
  var _a4;
  const n = assetsSelfPathFor(t, e5.type, e5.id);
  return n ? { ...e5, links: { ...(_a4 = e5.links) != null ? _a4 : {}, self: n } } : e5;
};
function respondResource$1(e5, t, n = "", r) {
  const s = { data: withAssetsSelfLink(e5, n) };
  t && t.length > 0 && (s.included = t);
  const o = r ? Zm(r) : void 0;
  return o && (s.meta = { warnings: o }), json$4(s);
}
function respondCollection$1(e5, t, n, r, s = "") {
  const o = { data: e5.map((a) => withAssetsSelfLink(a, s)) };
  return t && t.length > 0 && (o.included = t), n && (o.links = n), r && (o.meta = r), json$4(o);
}
const JsonApiAssetCreateSchema = re$1(A({ type: O$1("asset"), id: h$1, attributes: A({ mimeType: V$1(h$1), filename: V$1(h$1), cacheControl: V$1(h$1), customMetadata: V$1(Pn(h$1, h$1)), content: V$1(h$1) }) })), JsonApiAssetUpdateSchema = re$1(A({ type: O$1("asset"), id: h$1, attributes: A({ mimeType: V$1(h$1), cacheControl: V$1(h$1), customMetadata: V$1(Pn(h$1, h$1)) }) })), JsonApiFolderCreateSchema$2 = re$1(A({ type: O$1("folder"), id: h$1, attributes: A({ type: V$1(O$1("folder")) }) }));
function buildAssetsApi(e5) {
  const { repository: t, basePath: n = "/api/assets" } = e5, r = pt(JsonApiAssetCreateSchema), s = pt(JsonApiAssetUpdateSchema), o = pt(JsonApiFolderCreateSchema$2);
  return { async fetch(a) {
    var _a4;
    const l = new URL(a.url), c = l.pathname, u = a.method.toUpperCase(), p = {};
    l.searchParams.forEach((g, d) => {
      p[d] = g;
    });
    const f = parseIncludeQuery(p.include), m = { metadata: parseMetaQuery(p.meta).metadata, urls: f.urls, variations: f.variations };
    if (c === `${n}/capabilities` && u === "GET") {
      const g = await firstResult$1(t.getCapabilities());
      return X$1(g) ? respondError$2(g.failure, T$1.INTERNAL_ERROR) : respondResource$1({ type: "assets-capabilities", id: "self", attributes: g.success, links: { self: `${n}/capabilities` } });
    }
    if (c === `${n}/resources` && u === "GET") {
      const g = p.folder || p["filter[prefix]"] || "", d = parsePaginationQuery$1(p), y = p["filter[depth]"] || p.depth, S = y ? Math.max(1, parseInt(y, 10) || 1) : 1, $ = { offset: 0, limit: d.limit || 100 }, E = [];
      let b = false, A;
      const w = await runStreamWithDone$1(t.listResources(g, { pagination: $, depth: S, hints: m }));
      if (X$1(w)) return respondError$2(w.failure, T$1.BAD_REQUEST);
      const v = w.success.data, C = w.success.done, T = v.filter((k) => k.type === "asset"), O = /* @__PURE__ */ new Map();
      if (m.metadata) {
        const k = await runStream$1(t.getMetadata(T));
        if (jt(k)) for (const R of k.success) O.set(R.key, R.metadata);
      }
      if (m.urls) {
        const k = await runStream$1(t.getUrls(T));
        if (jt(k)) for (const R of k.success) E.push(assetUrlToJsonApi(R));
      }
      if (m.variations) {
        const k = await runStream$1(t.getVariations(T));
        if (jt(k)) for (const R of k.success) E.push(assetVariationsToJsonApi(R));
      }
      const L = v.map((k) => resourceToJsonApi(k, { metadata: k.type === "asset" ? O.get(k.key) : void 0, advertiseRelationships: { urls: m.urls, variations: m.variations } }));
      v.length >= $.limit && (b = true, A = (_a4 = v[v.length - 1]) == null ? void 0 : _a4.key);
      const _ = buildPaginationLinks$1(l.toString(), d, b, A, d.cursor), N = Zm(w.success.recoverableErrors), x = typeof C.total == "number" ? { total: C.total } : void 0, M = x || N ? { ...x ? { page: x } : {}, ...N ? { warnings: N } : {} } : void 0;
      return respondCollection$1(L, E, _, M, n);
    }
    if (c.startsWith(`${n}/resources/`) && u === "GET") {
      const g = decodeURIComponent(c.slice(`${n}/resources/`.length)), d = await firstResultWithMetadata$1(t.getResource(g, { hints: m }));
      if (X$1(d)) return respondError$2(d.failure, T$1.NOT_FOUND);
      const y = d.success.value[0];
      if (!y) return respondError$2(new zi("Resource not found"), T$1.NOT_FOUND);
      const S = [];
      let $;
      if (y.type === "asset") {
        if (m.metadata) {
          const b = await runStream$1(t.getMetadata([y]));
          jt(b) && b.success[0] && ($ = b.success[0].metadata);
        }
        if (m.urls) {
          const b = await runStream$1(t.getUrls([y]));
          jt(b) && b.success[0] && S.push(assetUrlToJsonApi(b.success[0]));
        }
        if (m.variations) {
          const b = await runStream$1(t.getVariations([y]));
          jt(b) && b.success[0] && S.push(assetVariationsToJsonApi(b.success[0]));
        }
      }
      const E = resourceToJsonApi(y, { metadata: $, advertiseRelationships: { urls: m.urls, variations: m.variations } });
      return respondResource$1(E, S, n, d.success.recoverableErrors);
    }
    if (c === `${n}/resources` && u === "POST") {
      const g = a.headers.get("Content-Type") || "";
      if (g.includes("multipart/form-data")) {
        const d = await a.formData(), y = d.get("file"), S = d.get("metadata"), $ = d.get("key"), E = d.get("mimeType"), b = d.get("filename"), A = d.get("cacheControl"), w = d.get("customMetadata");
        if (!y) return respondError$2(new zi("Missing file in multipart form data"), T$1.BAD_REQUEST);
        let v;
        if (S) try {
          v = JSON.parse(S);
        } catch {
          return respondError$2(new zi("Invalid metadata JSON"), T$1.BAD_REQUEST);
        }
        let C = v == null ? void 0 : v.customMetadata;
        if (w) try {
          C = JSON.parse(w);
        } catch {
        }
        const T = $ || (v == null ? void 0 : v.key) || y.name, O = E || (v == null ? void 0 : v.mimeType) || y.type || "application/octet-stream", L = b || (v == null ? void 0 : v.filename) || y.name, _ = A || (v == null ? void 0 : v.cacheControl), N = await y.arrayBuffer(), x = await firstResultWithMetadata$1(t.createAsset({ key: T, mimeType: O, content: N, filename: L, customMetadata: C, cacheControl: _ }));
        return X$1(x) ? respondError$2(x.failure, T$1.BAD_REQUEST) : respondResource$1(assetToJsonApi(x.success.value), void 0, n, x.success.recoverableErrors);
      }
      if (g.includes("application/vnd.api+json") || g.includes("application/json")) {
        const y = (await a.json()).data;
        if (y.type === "folder") {
          let S;
          try {
            S = o(y);
          } catch {
            return respondValidationError([{ message: "Invalid folder create data" }], T$1.BAD_REQUEST);
          }
          const $ = { key: S.id, type: "folder" }, E = await firstResultWithMetadata$1(t.createFolder($));
          return X$1(E) ? respondError$2(E.failure, T$1.BAD_REQUEST) : respondResource$1(folderToJsonApi$1(E.success.value), void 0, n, E.success.recoverableErrors);
        }
        if (y.type === "asset") {
          let S;
          try {
            S = r(y);
          } catch {
            return respondValidationError([{ message: "Invalid asset create data" }], T$1.BAD_REQUEST);
          }
          const $ = S.attributes.content;
          if (!$) return respondError$2(new zi("Missing content in asset creation. Use multipart/form-data for binary uploads."), T$1.BAD_REQUEST);
          const E = atob($), b = new Uint8Array(E.length);
          for (let v = 0; v < E.length; v++) b[v] = E.charCodeAt(v);
          const A = { key: S.id, mimeType: S.attributes.mimeType || "application/octet-stream", filename: S.attributes.filename, cacheControl: S.attributes.cacheControl, customMetadata: S.attributes.customMetadata, content: b.buffer }, w = await firstResultWithMetadata$1(t.createAsset(A));
          return X$1(w) ? respondError$2(w.failure, T$1.BAD_REQUEST) : respondResource$1(assetToJsonApi(w.success.value), void 0, n, w.success.recoverableErrors);
        }
        return respondError$2(new zi('Invalid resource type. Must be "asset" or "folder".'), T$1.BAD_REQUEST);
      }
      return respondError$2(new zi("Unsupported Content-Type. Use multipart/form-data or application/vnd.api+json."), T$1.BAD_REQUEST);
    }
    if (c.startsWith(`${n}/resources/`) && u === "PATCH") {
      const g = decodeURIComponent(c.slice(`${n}/resources/`.length)), y = { ...(await a.json()).data, id: g };
      let S;
      try {
        S = s(y);
      } catch {
        return respondValidationError([{ message: "Invalid asset update data" }], T$1.BAD_REQUEST);
      }
      const $ = { key: S.id, mimeType: S.attributes.mimeType, cacheControl: S.attributes.cacheControl, customMetadata: S.attributes.customMetadata }, E = await firstResultWithMetadata$1(t.updateAsset($));
      return X$1(E) ? respondError$2(E.failure, T$1.BAD_REQUEST) : respondResource$1(assetToJsonApi(E.success.value), void 0, n, E.success.recoverableErrors);
    }
    if (c.startsWith(`${n}/resources/`) && u === "DELETE") {
      const g = decodeURIComponent(c.slice(`${n}/resources/`.length)), d = p.recursive === "true", y = await firstResult$1(t.getResource(g));
      if (X$1(y)) return respondError$2(y.failure, T$1.NOT_FOUND);
      const S = y.success[0];
      if (!S) return respondError$2(new zi("Resource not found"), T$1.NOT_FOUND);
      const E = S.type === "folder" ? t.deleteFolder(g, d) : t.deleteAsset(g), b = await firstResultWithMetadata$1(E);
      if (X$1(b)) return respondError$2(b.failure, T$1.BAD_REQUEST);
      const A = Zm(b.success.recoverableErrors);
      return A ? json$4({ meta: { deleted: true, warnings: A } }) : new Response(null, { status: 204 });
    }
    return json$4({ errors: [{ status: "404", code: "not_found", detail: `Route not found: ${u} ${c}` }] }, 404);
  } };
}
const TIMING_CONSTANTS = { DEFAULT_JITTER_MS: 50 };
async function addTimingJitter(e5 = TIMING_CONSTANTS.DEFAULT_JITTER_MS) {
  const t = crypto.getRandomValues(new Uint8Array(1))[0] % e5;
  await new Promise((n) => setTimeout(n, t));
}
const json$3 = (e5, t = 200) => new Response(JSON.stringify(e5), { status: t, headers: { "Content-Type": "application/vnd.api+json", "Cache-Control": "no-store" } });
function respondError$1(e5, t = 400) {
  if (jt(e5)) throw new Error("respondError called with success result");
  return json$3(uc(e5), t);
}
const docsSelfPathFor = (e5, t) => {
  const n = encodeURIComponent(t);
  switch (e5) {
    case "published":
    case "published-summary":
      return `/published/${n}`;
    case "unpublished":
    case "unpublished-summary":
      return `/unpublished/${n}`;
    case "revision":
    case "revision-summary":
      return `/revisions/${n}`;
    case "documents-capabilities":
      return "/capabilities";
    default:
      return;
  }
}, withDocsSelfLink = (e5, t) => {
  var _a4;
  const n = docsSelfPathFor(e5.type, e5.id);
  return n ? { ...e5, links: { ...(_a4 = e5.links) != null ? _a4 : {}, self: `${t}${n}` } } : e5;
};
function respondResource(e5, t, n, r) {
  var _a4;
  if (X$1(e5)) {
    const o = e5.failure.code === Mi.CODE || ((_a4 = e5.failure.message) == null ? void 0 : _a4.toLowerCase().includes("not found"));
    return respondError$1(e5, o ? 404 : 400);
  }
  const s = r ? Zm(r) : void 0;
  return json$3({ data: withDocsSelfLink(t(e5.success), n), ...s ? { meta: { warnings: s } } : {} });
}
async function respondResourceWithWarnings(e5, t, n) {
  const r = await firstResultWithMetadata(e5);
  return X$1(r) ? respondResource(we$2(r.failure), t, n) : respondResource(lt(r.success.value), t, n, r.success.recoverableErrors);
}
function respondVoid(e5, t) {
  if (X$1(e5)) return respondError$1(e5);
  const n = t ? Zm(t) : void 0;
  return json$3({ meta: { deleted: true, ...n ? { warnings: n } : {} } });
}
async function respondVoidWithWarnings(e5) {
  const t = await firstResultWithMetadata(e5);
  return X$1(t) ? respondVoid(we$2(t.failure)) : respondVoid(lt(void 0), t.success.recoverableErrors);
}
async function respondCollection(e5, t, n, r, s, o, a) {
  var _a4;
  const l = new URL(e5.url), c = Object.fromEntries(l.searchParams.entries()), u = parsePaginationQuery(c), p = t[t.length - 1];
  (_a4 = p == null ? void 0 : p.id) != null ? _a4 : p == null ? void 0 : p.key;
  const f = "limit" in u ? u.limit : "perPage" in u ? u.perPage : void 0, h = typeof f == "number" ? t.length === f : false, m = buildPaginationLinks(r, u, h), g = a ? Zm(a) : void 0, d = typeof (o == null ? void 0 : o.total) == "number" ? { total: o.total } : void 0, y = d || g ? { ...d ? { page: d } : {}, ...g ? { warnings: g } : {} } : void 0, S = { data: t.map(($) => withDocsSelfLink(n($), s)), links: m, ...y ? { meta: y } : {} };
  return json$3(S);
}
function toLaikaError$1(e5) {
  return e5 instanceof b$1 ? e5 : e5 instanceof Error ? new Ge(e5.message, { cause: e5 }) : new Ge(String(e5));
}
async function firstResult(e5) {
  try {
    return await xe$2(di(us(e5)));
  } catch (t) {
    return we$2(toLaikaError$1(t));
  }
}
async function firstResultWithMetadata(e5) {
  try {
    const t = await xe$2(di(oc(e5)));
    return X$1(t) ? we$2(t.failure) : lt({ value: t.success.value, recoverableErrors: t.success.recoverableErrors });
  } catch (t) {
    return we$2(toLaikaError$1(t));
  }
}
async function runStreamWithDone(e5) {
  try {
    const t = await xe$2(di(fs$1(e5)));
    return X$1(t) ? we$2(t.failure) : lt({ data: t.success.data, done: t.success.done, recoverableErrors: t.success.recoverableErrors });
  } catch (t) {
    return we$2(toLaikaError$1(t));
  }
}
const RecordsQuerySchema = re$1(A({ "filter[type]": V$1(be$1([O$1("published"), O$1("unpublished"), O$1("all")])), "filter[folder]": V$1(h$1), "filter[depth]": V$1(Ym.pipe($e$1(cs((e5) => e5 >= 1 ? void 0 : "Depth must be at least 1")))) })), DocumentCreateBodySchema = re$1(A({ data: A({ type: O$1("published"), id: V$1(h$1), attributes: Pn(h$1, Bm) }) })), UnpublishedCreateBodySchema = re$1(A({ data: A({ type: O$1("unpublished"), id: V$1(h$1), attributes: Pn(h$1, Bm) }) })), UnpublishedUpdateBodySchema = re$1(A({ data: A({ type: O$1("unpublished"), id: h$1, attributes: Pn(h$1, Bm) }) })), UnpublishBodySchema = re$1(A({ data: A({ type: O$1("unpublished"), attributes: A({ status: h$1 }) }) })), RevisionCreateBodySchema = re$1(A({ data: A({ type: O$1("revision"), id: V$1(h$1), attributes: Pn(h$1, Bm) }) })), RefSchema = re$1(A({ id: h$1, type: be$1([O$1("document"), O$1("unpublished"), O$1("revision")]) })), AddUnpublishedOpSchema = re$1(A({ op: O$1("add"), data: A({ type: O$1("unpublished"), id: V$1(h$1), attributes: Pn(h$1, Bm) }) })), AddDocumentOpSchema = re$1(A({ op: O$1("add"), data: A({ type: O$1("published"), id: V$1(h$1), attributes: Pn(h$1, Bm) }) })), StateTransitionOpSchema = re$1(A({ op: O$1("update"), href: be$1([O$1("/publish"), O$1("/unpublish")]), ref: RefSchema, data: V$1(A({ type: O$1("unpublished"), attributes: A({ status: h$1 }) })) })), UpdateUnpublishedOpSchema = re$1(A({ op: O$1("update"), data: A({ type: O$1("unpublished"), id: h$1, attributes: Pn(h$1, Bm) }) })), RemoveOpSchema = re$1(A({ op: O$1("remove"), ref: RefSchema })), AtomicOperationSchema$1 = be$1([AddUnpublishedOpSchema, AddDocumentOpSchema, StateTransitionOpSchema, UpdateUnpublishedOpSchema, RemoveOpSchema]), OperationsSchema = re$1(A({ "atomic:operations": dh(AtomicOperationSchema$1) })), decodeRecordsQuery = pt(RecordsQuerySchema), decodeDocumentCreateBody = pt(DocumentCreateBodySchema), decodeUnpublishedCreateBody = pt(UnpublishedCreateBodySchema), decodeUnpublishedUpdateBody = pt(UnpublishedUpdateBodySchema), decodeUnpublishBody = pt(UnpublishBodySchema), decodeRevisionCreateBody = pt(RevisionCreateBodySchema), decodeOperations = pt(OperationsSchema);
function buildJsonApi$1(e5) {
  const { repo: t, basePath: n = "" } = e5;
  return { async fetch(r) {
    var _a4, _b, _c2, _d;
    const s = new URL(r.url);
    let o = s.pathname.substring(n.length);
    if (o.startsWith("/") && (o = o.substring(1)), o.endsWith("/") && (o = o.slice(0, -1)), o === "" && r.method === "GET") return json$3({ data: { type: "api-info", id: "documents", attributes: { name: "Documents API", version: "1.0.0", endpoints: [{ path: "/capabilities", methods: ["GET"], description: "Underlying documents repository capabilities" }, { path: "/records", methods: ["GET"], description: "List full records (published + unpublished view per key)" }, { path: "/record-summaries", methods: ["GET"], description: "List record summaries (lightweight listing)" }, { path: "/published/{key}", methods: ["GET", "POST", "PATCH", "DELETE"], description: "Read, create, update, or remove a published document" }, { path: "/published/{key}/unpublish", methods: ["POST"], description: "State transition: move a published document to unpublished" }, { path: "/unpublished/{key}", methods: ["GET", "POST", "PATCH", "DELETE"], description: "Read, create, update, or remove an unpublished draft" }, { path: "/unpublished/{key}/publish", methods: ["POST"], description: "State transition: publish an unpublished draft" }, { path: "/revisions", methods: ["POST"], description: "Create a revision for a document" }, { path: "/revisions/{key}", methods: ["GET"], description: "List revisions for a document" }, { path: "/revisions/{key}/{revisionId}", methods: ["GET"], description: "Read a specific revision of a document" }, { path: "/operations", methods: ["POST"], description: "Atomic operations (add/update/remove + publish/unpublish transitions)" }] } } });
    const a = o.split("/"), l = a[0], c = a[1] ? decodeURIComponent(a[1]) : void 0, u = a[2], p = Object.fromEntries(s.searchParams.entries()), f = async () => {
      var _a5, _b2, _c3;
      const g = decodeRecordsQuery(p), d = g["filter[type]"] === "all" ? void 0 : (_a5 = g["filter[type]"]) != null ? _a5 : "published", y = (_b2 = g["filter[folder]"]) != null ? _b2 : "", S = (_c3 = g["filter[depth]"]) != null ? _c3 : 1, $ = await runStreamWithDone(t.listRecords({ pagination: parsePaginationQuery(p), folder: y, type: d, depth: S }));
      if (X$1($)) return respondError$1($);
      const b = $.success.data.map((A) => {
        switch (A.type) {
          case "published":
            return eg(A);
          case "unpublished":
            return ag(A);
          default:
            throw new Error(`Unknown entry type: ${A.type}`);
        }
      });
      return respondCollection(r, b, (A) => A, r.url, n, { total: $.success.done.total }, $.success.recoverableErrors);
    }, h = async () => {
      var _a5, _b2, _c3;
      const g = decodeRecordsQuery(p), d = g["filter[type]"] === "all" ? void 0 : (_a5 = g["filter[type]"]) != null ? _a5 : "published", y = (_b2 = g["filter[folder]"]) != null ? _b2 : "", S = (_c3 = g["filter[depth]"]) != null ? _c3 : 1, $ = await runStreamWithDone(t.listRecordSummaries({ pagination: parsePaginationQuery(p), folder: y, type: d, depth: S }));
      if (X$1($)) return respondError$1($);
      const b = $.success.data.map((A) => {
        switch (A.type) {
          case "published":
            return og({ ...A, type: "published-summary" });
          case "unpublished":
            return pg({ ...A, type: "unpublished-summary" });
          default:
            throw new Error(`Unknown entry type: ${A.type}`);
        }
      });
      return respondCollection(r, b, (A) => A, r.url, n, { total: $.success.done.total }, $.success.recoverableErrors);
    };
    if (l === "capabilities" && r.method === "GET") {
      const g = await firstResult(t.getCapabilities());
      return X$1(g) ? respondError$1(g) : json$3({ data: { type: "documents-capabilities", id: "self", attributes: g.success, links: { self: `${n}/capabilities` } } });
    }
    if (l === "records" && r.method === "GET") return f();
    if (l === "record-summaries" && r.method === "GET") return h();
    if (l === "published" && r.method === "GET" && c) return respondResourceWithWarnings(t.getDocument(c), eg, n);
    if (l === "published" && u === "unpublish" && r.method === "POST" && c) {
      const g = await r.json(), { data: d } = decodeUnpublishBody(g);
      return respondResourceWithWarnings(t.unpublish(c, d.attributes.status), ag, n);
    }
    if (l === "published" && r.method === "POST") {
      const g = await r.json(), { data: d } = decodeDocumentCreateBody(g), y = rg({ id: (_a4 = d.id) != null ? _a4 : "", attributes: d.attributes });
      return respondResourceWithWarnings(t.createDocument(y), eg, n);
    }
    if (l === "published" && r.method === "PATCH" && c) {
      const g = await r.json(), { data: d } = decodeDocumentCreateBody(g), y = { key: c, ...d.attributes };
      return respondResourceWithWarnings(t.updateDocument(y), eg, n);
    }
    if (l === "published" && r.method === "DELETE" && c) return respondVoidWithWarnings(t.deleteDocument(c));
    if (l === "unpublished" && r.method === "GET" && c) return respondResourceWithWarnings(t.getUnpublished(c), ag, n);
    if (l === "unpublished" && u === "publish" && r.method === "POST" && c) return respondResourceWithWarnings(t.publish(c), eg, n);
    if (l === "unpublished" && r.method === "POST") {
      const g = await r.json(), { data: d } = decodeUnpublishedCreateBody(g), y = lg({ id: (_b = d.id) != null ? _b : "", attributes: d.attributes });
      return respondResourceWithWarnings(t.createUnpublished(y), ag, n);
    }
    if (l === "unpublished" && r.method === "PATCH" && c) {
      const g = await r.json(), { data: d } = decodeUnpublishedUpdateBody(g), y = dg({ id: d.id, attributes: d.attributes });
      return respondResourceWithWarnings(t.updateUnpublished({ ...y, key: c }), ag, n);
    }
    if (l === "unpublished" && r.method === "DELETE" && c) return respondVoidWithWarnings(t.deleteUnpublished(c));
    if (l === "revisions" && r.method === "POST") {
      const g = await r.json(), { data: d } = decodeRevisionCreateBody(g), y = Eg({ id: (_c2 = d.id) != null ? _c2 : "", attributes: d.attributes });
      return respondResourceWithWarnings(t.createRevision(y), mg, n);
    }
    if (l === "revisions" && r.method === "GET" && c && !u) {
      const g = await runStreamWithDone(t.listRevisions(c, { pagination: parsePaginationQuery(p) }));
      return X$1(g) ? respondError$1(g) : respondCollection(r, g.success.data, bg, r.url, n, { total: g.success.done.total }, g.success.recoverableErrors);
    }
    if (l === "revisions" && r.method === "GET" && c && u) return respondResourceWithWarnings(t.getRevision(c, u), mg, n);
    if (l === "operations" && r.method === "POST") {
      const g = await r.json(), y = decodeOperations(g)["atomic:operations"].map(async (E) => {
        var _a5, _b2, _c3;
        let b, A = null;
        if (E.op === "add") {
          if ("data" in E && E.data.type === "unpublished") {
            const w = E, v = lg({ id: (_a5 = w.data.id) != null ? _a5 : "", attributes: w.data.attributes });
            b = await firstResultWithMetadata(t.createUnpublished(v)), A = ag;
          } else if ("data" in E && E.data.type === "published") {
            const w = E, v = rg({ id: (_b2 = w.data.id) != null ? _b2 : "", attributes: w.data.attributes });
            b = await firstResultWithMetadata(t.createDocument(v)), A = eg;
          } else b = we$2(new zi(`Cannot add type: ${(_c3 = E.data) == null ? void 0 : _c3.type}`));
          return { op: b, operation: E, transformer: A };
        }
        if (E.op === "update") {
          if ("href" in E && "ref" in E) {
            const { href: w, ref: v, data: C } = E;
            switch (w) {
              case "/publish":
                v.type === "unpublished" ? (b = await firstResultWithMetadata(t.publish(v.id)), A = eg) : b = we$2(new zi(`Cannot publish ${v.type}`));
                break;
              case "/unpublish":
                if (v.type === "document") {
                  if (!C) {
                    b = we$2(new zi("Missing data for unpublish operation"));
                    break;
                  }
                  b = await firstResultWithMetadata(t.unpublish(v.id, C.attributes.status)), A = ag;
                } else b = we$2(new zi(`Cannot unpublish ${v.type}`));
                break;
              default:
                b = we$2(new zi(`Unknown action: ${w}`));
            }
          } else if ("data" in E) {
            const w = E, v = dg({ id: w.data.id, attributes: w.data.attributes });
            b = await firstResultWithMetadata(t.updateUnpublished(v)), A = ag;
          } else b = we$2(new zi("Invalid update operation"));
          return { op: b, operation: E, transformer: A };
        }
        if (E.op === "remove") {
          const { ref: w } = E;
          return w.type === "document" ? (b = await firstResultWithMetadata(t.deleteDocument(w.id)), A = null) : w.type === "unpublished" ? (b = await firstResultWithMetadata(t.deleteUnpublished(w.id)), A = null) : b = we$2(new zi(`Cannot remove ${w.type}`)), { op: b, operation: E, transformer: A };
        }
        return { op: we$2(new zi(`Unsupported operation: ${E.op}`)), operation: E, transformer: null };
      }), $ = (await Promise.allSettled(y)).map((E) => {
        if (E.status === "rejected") return { errors: [{ status: "500", title: "Operation Failed", detail: E.reason.message }] };
        const { op: b, operation: A, transformer: w } = E.value;
        if (X$1(b)) return { errors: [{ status: "400", title: "Operation Failed", detail: b.failure.message }] };
        if (A.op === "remove") {
          const v = A, C = b.success, T = C ? Zm(C.recoverableErrors) : void 0;
          return { meta: T ? { deleted: true, ref: v.ref, warnings: T } : { deleted: true, ref: v.ref } };
        }
        if (w) {
          const v = b.success, C = Zm(v.recoverableErrors), T = w(v.value);
          return C ? { data: T, meta: { warnings: C } } : { data: T };
        }
        return { data: null };
      });
      return json$3({ "atomic:results": $ });
    }
    (_d = e5.logger) == null ? void 0 : _d.debug("Documents endpoint not found:", o);
    const m = new Mi("Endpoint not found");
    return respondError$1(we$2(m), 404);
  } };
}
function storageObjectToJsonApi(e5) {
  const { key: t, metadata: n, ...r } = e5;
  return { type: "object", id: t, attributes: r, ...n ? { meta: n } : {} };
}
function folderToJsonApi(e5) {
  const { key: t, ...n } = e5;
  return { type: "folder", id: t, attributes: n };
}
function storageObjectSummaryToJsonApi(e5) {
  const { key: t, ...n } = e5;
  return { type: "object-summary", id: t, attributes: n };
}
function folderSummaryToJsonApi(e5) {
  const { key: t, ...n } = e5;
  return { type: "folder-summary", id: t, attributes: n };
}
function atomToJsonApi(e5) {
  return e5.type === "object" ? storageObjectToJsonApi(e5) : folderToJsonApi(e5);
}
function atomSummaryToJsonApi(e5) {
  return e5.type === "object-summary" ? storageObjectSummaryToJsonApi(e5) : folderSummaryToJsonApi(e5);
}
function withSelfLink(e5, t) {
  var _a4;
  const n = selfPathFor(e5.type, e5.id);
  return n ? { ...e5, links: { ...(_a4 = e5.links) != null ? _a4 : {}, self: `${t}${n}` } } : e5;
}
const selfPathFor = (e5, t) => {
  const n = encodeURIComponent(t);
  switch (e5) {
    case "object":
    case "object-summary":
      return `/objects/${n}`;
    case "folder":
    case "folder-summary":
      return `/folders/${n}`;
    case "storage-capabilities":
      return "/capabilities";
    default:
      return;
  }
}, JsonApiStorageObjectSchema = re$1(A({ type: O$1("object"), id: h$1, attributes: Pn(h$1, is), meta: V$1(Pn(h$1, is)) })), JsonApiStorageObjectCreateSchema$1 = re$1(A({ type: O$1("object"), id: h$1, attributes: Pn(h$1, is), meta: V$1(Pn(h$1, is)) })), JsonApiStorageObjectUpdateSchema$1 = re$1(A({ type: O$1("object"), id: h$1, attributes: Pn(h$1, is), meta: V$1(Pn(h$1, is)) })), JsonApiFolderSchema = re$1(A({ type: O$1("folder"), id: h$1, attributes: Pn(h$1, is) })), JsonApiFolderCreateSchema$1 = re$1(A({ type: O$1("folder"), id: h$1, attributes: Pn(h$1, is) })), JsonApiStorageObjectSummarySchema = re$1(A({ type: O$1("object-summary"), id: h$1, attributes: Pn(h$1, is) })), JsonApiFolderSummarySchema = re$1(A({ type: O$1("folder-summary"), id: h$1, attributes: Pn(h$1, is) })), JsonApiAtomSchema = be$1([JsonApiStorageObjectSchema, JsonApiFolderSchema]), JsonApiAtomSummarySchema = be$1([JsonApiStorageObjectSummarySchema, JsonApiFolderSummarySchema]);
pt(JsonApiStorageObjectSchema);
pt(JsonApiStorageObjectCreateSchema$1);
pt(JsonApiStorageObjectUpdateSchema$1);
pt(JsonApiFolderSchema);
pt(JsonApiFolderCreateSchema$1);
pt(JsonApiAtomSchema);
pt(JsonApiAtomSummarySchema);
const json$2 = (e5, t = 200) => new Response(JSON.stringify(e5), { status: t, headers: { "Content-Type": "application/vnd.api+json", "Cache-Control": "no-store" } }), safeDecode = (e5) => {
  try {
    return decodeURIComponent(e5);
  } catch {
    return e5;
  }
};
function respondError(e5, t = 400) {
  if (jt(e5)) throw new Ge("respondError called with success result");
  return json$2(uc(e5), t);
}
function respondResourceWithConverter(e5, t, n, r) {
  if (X$1(e5)) return respondError(e5);
  const s = r ? Zm(r) : void 0;
  return json$2({ data: withSelfLink(t(e5.success), n), ...s ? { meta: { warnings: s } } : {} });
}
function respondCollectionWithConverter(e5, t, n, r, s, o, a) {
  const l = new URL(e5.url), c = Object.fromEntries(l.searchParams.entries()), u = parsePaginationQuery(c);
  let p, f;
  if (t.length > 0) {
    const E = t[0], b = t[t.length - 1];
    p = E.id || E.key, f = b.id || b.key;
  }
  const h = "limit" in u ? u.limit : "perPage" in u ? u.perPage : void 0, m = (o == null ? void 0 : o.pagination) !== void 0 || (typeof (o == null ? void 0 : o.total) == "number" && typeof h == "number" ? t.length === h : false), g = buildPaginationLinks(r, u, m, f, p, f), d = a ? Zm(a) : void 0, y = typeof (o == null ? void 0 : o.total) == "number" ? { total: o.total } : void 0, S = y || d ? { ...y ? { page: y } : {}, ...d ? { warnings: d } : {} } : void 0, $ = { data: t.map((E) => withSelfLink(n(E), s)), links: g, ...S ? { meta: S } : {} };
  return json$2($);
}
const toLaikaError = (e5) => e5 instanceof b$1 ? e5 : e5 instanceof Error ? new Ge(e5.message, { cause: e5 }) : new Ge(String(e5)), runTask = async (e5) => {
  try {
    return await xe$2(di(us(e5)));
  } catch (t) {
    return we$2(toLaikaError(t));
  }
}, runTaskWithMetadata = async (e5) => {
  try {
    const t = await xe$2(di(oc(e5)));
    return X$1(t) ? we$2(t.failure) : lt({ value: t.success.value, recoverableErrors: t.success.recoverableErrors });
  } catch (t) {
    return we$2(toLaikaError(t));
  }
}, runStream = async (e5) => {
  try {
    return await xe$2(di(fs$1(e5)));
  } catch (t) {
    return we$2(toLaikaError(t));
  }
}, JsonApiStorageObjectCreateSchema = re$1(A({ type: O$1("object"), id: h$1, attributes: A({ type: V$1(O$1("object")), content: V$1(Pn(h$1, Bm)) }), meta: V$1(Pn(h$1, Bm)) })), JsonApiStorageObjectUpdateSchema = re$1(A({ type: O$1("object"), id: h$1, attributes: A({ type: V$1(O$1("object")), content: V$1(Pn(h$1, Bm)) }), meta: V$1(Pn(h$1, Bm)) })), JsonApiFolderCreateSchema = re$1(A({ type: O$1("folder"), id: h$1, attributes: A({ type: V$1(O$1("folder")) }) })), StorageObjectCreateBodySchema = re$1(A({ data: JsonApiStorageObjectCreateSchema })), StorageObjectUpdateBodySchema = re$1(A({ data: JsonApiStorageObjectUpdateSchema })), RemoveOperationSchema = re$1(A({ op: O$1("remove"), ref: A({ type: be$1([O$1("object"), O$1("folder"), O$1("atom")]), id: h$1 }) })), AddOperationSchema = re$1(A({ op: O$1("add"), data: be$1([JsonApiStorageObjectCreateSchema, JsonApiFolderCreateSchema]) })), UpdateOperationSchema = re$1(A({ op: O$1("update"), data: JsonApiStorageObjectUpdateSchema })), AtomicOperationSchema = be$1([RemoveOperationSchema, AddOperationSchema, UpdateOperationSchema]), AtomicOperationsRequestSchema = re$1(A({ "atomic:operations": dh(AtomicOperationSchema) }));
function buildJsonApi(e5) {
  const { repo: t, basePath: n = "" } = e5, r = pt(StorageObjectCreateBodySchema), s = pt(StorageObjectUpdateBodySchema), o = pt(AtomicOperationsRequestSchema);
  return { async fetch(a) {
    var _a4, _b, _c2, _d, _e2;
    let c = new URL(a.url).pathname.substring(n.length);
    if (c.startsWith("/") && (c = c.substring(1)), c.endsWith("/") && (c = c.slice(0, -1)), c === "" && a.method === "GET") return json$2({ data: { type: "api-info", id: "storage", attributes: { name: "Storage API", version: "1.0.0", endpoints: [{ path: "/capabilities", methods: ["GET"], description: "Underlying storage repository capabilities" }, { path: "/atoms/{key}", methods: ["GET"], description: "List atoms in a folder" }, { path: "/atom-summaries/{key}", methods: ["GET"], description: "List atom summaries (lightweight listing) in a folder" }, { path: "/objects/{key}", methods: ["GET", "POST", "PATCH"], description: "Read, create, or update storage objects" }, { path: "/folders/{key}", methods: ["GET"], description: "Read a folder" }, { path: "/operations", methods: ["POST"], description: "Atomic operations (add, update, remove)" }] } } });
    const [u, p] = c.split("/"), f = p === void 0 ? void 0 : safeDecode(p), h = async () => {
      const g = { depth: 1, pagination: { perPage: 10 } }, d = await runStream(t.listAtoms(f, g));
      if (X$1(d)) {
        const y = d.failure.code;
        return respondError(d, Dd[y] || 400);
      }
      return respondCollectionWithConverter(a, d.success.data, atomToJsonApi, a.url, n, d.success.done, d.success.recoverableErrors);
    }, m = async () => {
      const g = { depth: 1, pagination: { perPage: 10 } }, d = await runStream(t.listAtomSummaries(f, g));
      if (X$1(d)) {
        const y = d.failure.code;
        return respondError(d, Dd[y] || 400);
      }
      return respondCollectionWithConverter(a, d.success.data, atomSummaryToJsonApi, a.url, n, d.success.done, d.success.recoverableErrors);
    };
    if (u === "capabilities" && a.method === "GET") {
      const g = await runTask(t.getCapabilities());
      if (X$1(g)) {
        const d = (_a4 = Dd[g.failure.code]) != null ? _a4 : 500;
        return respondError(g, d);
      }
      return json$2({ data: { type: "storage-capabilities", id: "self", attributes: g.success, links: { self: `${n}/capabilities` } } });
    }
    if (u === "atoms" && a.method === "GET") return h();
    if (u === "atom-summaries" && a.method === "GET") return m();
    if (u === "objects" && a.method === "GET") {
      if (!f) return respondError(we$2(new Bi("Missing object key")), 400);
      const g = await runTaskWithMetadata(t.getObject(f));
      if (X$1(g)) {
        const d = (_b = Dd[g.failure.code]) != null ? _b : 400;
        return respondError(g, d);
      }
      return respondResourceWithConverter(lt(g.success.value), storageObjectToJsonApi, n, g.success.recoverableErrors);
    } else if (u === "folders" && a.method === "GET") {
      if (!f) return respondError(we$2(new Bi("Missing folder key")), 400);
      const g = await runTaskWithMetadata(t.getFolder(f));
      if (X$1(g)) {
        const d = (_c2 = Dd[g.failure.code]) != null ? _c2 : 400;
        return respondError(g, d);
      }
      return respondResourceWithConverter(lt(g.success.value), folderToJsonApi, n, g.success.recoverableErrors);
    } else if (u === "objects" && a.method === "POST") {
      let g;
      try {
        const S = await a.json();
        g = r(S);
      } catch {
        return respondError(we$2(new Bi("Invalid request body")), 400);
      }
      const d = { key: g.data.id, type: "object", content: g.data.attributes.content || {}, ...g.data.meta ? { metadata: g.data.meta } : {} }, y = await runTaskWithMetadata(t.createObject(d));
      return X$1(y) ? respondError(y) : respondResourceWithConverter(lt(y.success.value), storageObjectToJsonApi, n, y.success.recoverableErrors);
    }
    if (c.startsWith("objects") && a.method === "PATCH") {
      const [, g] = c.split("/"), d = g === void 0 ? void 0 : safeDecode(g);
      let y;
      try {
        const E = await a.json();
        y = s(E);
      } catch {
        return respondError(we$2(new Bi("Invalid request body")), 400);
      }
      if (y.data.id !== d) return respondError(we$2(new Bi("Key in URL does not match key in body")), Dd[Bi.CODE]);
      const S = { key: y.data.id, type: "object", content: y.data.attributes.content, ...y.data.meta ? { metadata: y.data.meta } : {} }, $ = await runTaskWithMetadata(t.updateObject(S));
      return X$1($) ? respondError($) : respondResourceWithConverter(lt($.success.value), storageObjectToJsonApi, n, $.success.recoverableErrors);
    } else if (c === "operations" && a.method === "POST") {
      let g;
      try {
        const A = await a.json();
        g = o(A);
      } catch {
        return respondError(we$2(new Bi("Invalid atomic operations request")), 400);
      }
      const d = [], y = (A) => new Promise((w, v) => d.push([A, w, v])), S = g["atomic:operations"].map((A) => {
        switch (A.op) {
          case "add":
            if (A.data.type === "object") {
              const w = { key: A.data.id, type: "object", content: A.data.attributes.content || {} };
              return runTaskWithMetadata(t.createObject(w)).then((v) => ({ op: v, operation: A }));
            } else if (A.data.type === "folder") {
              const w = { key: A.data.id, type: "folder" };
              return runTaskWithMetadata(t.createFolder(w)).then((v) => ({ op: v, operation: A }));
            }
            return Promise.resolve({ op: we$2(new Bi("Unsupported add type")), operation: A });
          case "update":
            if (A.data.type === "object") {
              const w = { key: A.data.id, type: "object", content: A.data.attributes.content };
              return runTaskWithMetadata(t.updateObject(w)).then((v) => ({ op: v, operation: A }));
            }
            return Promise.resolve({ op: we$2(new Bi("Unsupported update type")), operation: A });
          case "remove":
            return y(A.ref.id).then((w) => ({ op: w, operation: A }));
        }
      }), $ = await runStream(t.removeAtoms(d.map(([A]) => A)));
      if (X$1($)) return respondError($);
      for (const A of $.success.data) {
        const w = d.find(([v]) => v === A);
        if (w) {
          const [, v] = w;
          v(lt({ type: "atom", key: A }));
        }
      }
      for (const [A, w] of d) $.success.data.includes(A) || w(we$2((_d = $.success.recoverableErrors[0]) != null ? _d : new Mi(`Failed to remove ${A}`)));
      const E = await Promise.allSettled(S), b = [];
      for (const A of E) {
        if (A.status === "rejected") continue;
        const w = A.value, v = w.op;
        if (jt(v)) {
          if (w.operation.op === "add" || w.operation.op === "update") {
            const C = v.success, T = C.value, O = Zm(C.recoverableErrors);
            if (typeof T == "object" && T !== null && "type" in T) {
              const _ = T.type === "object" ? storageObjectToJsonApi(T) : folderToJsonApi(T);
              b.push(O ? { data: _, meta: { warnings: O } } : { data: _ });
            }
          } else if (w.operation.op === "remove") {
            const C = v.success;
            b.push({ meta: { deleted: true, ref: { type: C.type, id: C.key } } });
          }
        }
      }
      return json$2({ "atomic:results": b });
    } else return (_e2 = e5.logger) == null ? void 0 : _e2.debug("storage endpoint not found:", c), respondError(we$2(new Mi("Storage endpoint not found")), 404);
  } };
}
const SECURITY_DEFAULTS = { MAX_TOKEN_LENGTH: 2048 };
function validateTokenInput(e5) {
  return !e5 || typeof e5 != "string" || e5.length > SECURITY_DEFAULTS.MAX_TOKEN_LENGTH ? null : e5;
}
const SECURITY_HEADERS = { "Content-Type": "application/vnd.api+json", "X-Content-Type-Options": "nosniff", "Cache-Control": "no-store, no-cache, must-revalidate", Pragma: "no-cache" }, decapApi = (e5) => {
  const { documents: t, storage: n, assets: r, authenticateAccessToken: s, authenticateApiToken: o, basePath: a } = e5, l = Ie$1.normalize(a != null ? a : ""), c = Nh.url`${l}/health`, u = Nh.url`${l}/storage`, p = Nh.url`${l}/documents`, f = Nh.url`${l}/assets`, h = Nh.url`${l}/session`, m = async (g) => {
    var _a4, _b, _c2;
    const d = g.headers.get("Authorization") || void 0, y = g.headers.get("X-API-Key") || void 0, S = d ? yr.ExtractAuthorizationApiKey(d) : void 0;
    new URL(g.url).searchParams.has("api_key") && ((_a4 = e5.logger) == null ? void 0 : _a4.warn("Rejecting api_key supplied via URL query string. Use the X-API-Key header or Authorization: ApiKey <key>."));
    try {
      const $ = y || S, E = $ ? validateTokenInput($) : null;
      if ($ && !E) throw new qi("Invalid API key format");
      if (E) {
        if (!o) throw (_b = e5.logger) == null ? void 0 : _b.error("API key authentication not configured"), new qi("API key authentication not configured");
        return await o(E);
      } else {
        const b = yr.ExtractAuthorizationBearerToken(d), A = validateTokenInput(b);
        if (!A) throw new qi("Invalid or missing authentication token");
        return await s(A);
      }
    } catch ($) {
      (_c2 = e5.logger) == null ? void 0 : _c2.error("Authentication failed:", $);
      const E = Xm($) ? $ : new qi("Authentication failed");
      return await addTimingJitter(), new Response(JSON.stringify(uc(E)), { status: 401, headers: SECURITY_HEADERS });
    }
  };
  return { authenticateRequest: m, async fetch(g) {
    var _a4, _b, _c2;
    const d = new URL(g.url), y = Ie$1.normalize(d.pathname);
    if (y === c) return (_a4 = e5.logger) == null ? void 0 : _a4.debug("Health check endpoint"), new Response(JSON.stringify({ status: "ok", timestamp: (/* @__PURE__ */ new Date()).toISOString() }), { status: 200, headers: { ...SECURITY_HEADERS, "Content-Type": "application/json" } });
    const S = await m(g);
    if (S instanceof Response) return S;
    const $ = S;
    if (y.startsWith(h)) {
      (_b = e5.logger) == null ? void 0 : _b.debug("Session endpoint for user:", $.id);
      const { passwordHash: E, ...b } = $;
      return new Response(JSON.stringify({ data: { type: "session", id: $.id || $.email, attributes: { ...b } } }), { status: 200, headers: SECURITY_HEADERS });
    } else return y.startsWith(u) ? buildJsonApi({ repo: n, basePath: `${l}/storage`, logger: e5.logger }).fetch(g) : y.startsWith(p) ? buildJsonApi$1({ repo: t, basePath: `${l}/documents`, logger: e5.logger }).fetch(g) : r && y.startsWith(f) ? buildAssetsApi({ repository: r, basePath: `${l}/assets` }).fetch(g) : ((_c2 = e5.logger) == null ? void 0 : _c2.debug("Endpoint not found:", y), new Response(JSON.stringify(uc(new Mi("Endpoint not found"))), { status: 404, headers: SECURITY_HEADERS }));
  } };
}, balanced = (e5, t, n) => {
  const r = e5 instanceof RegExp ? maybeMatch(e5, n) : e5, s = t instanceof RegExp ? maybeMatch(t, n) : t, o = r !== null && s != null && range(r, s, n);
  return o && { start: o[0], end: o[1], pre: n.slice(0, o[0]), body: n.slice(o[0] + r.length, o[1]), post: n.slice(o[1] + s.length) };
}, maybeMatch = (e5, t) => {
  const n = t.match(e5);
  return n ? n[0] : null;
}, range = (e5, t, n) => {
  let r, s, o, a, l, c = n.indexOf(e5), u = n.indexOf(t, c + 1), p = c;
  if (c >= 0 && u > 0) {
    if (e5 === t) return [c, u];
    for (r = [], o = n.length; p >= 0 && !l; ) {
      if (p === c) r.push(p), c = n.indexOf(e5, p + 1);
      else if (r.length === 1) {
        const f = r.pop();
        f !== void 0 && (l = [f, u]);
      } else s = r.pop(), s !== void 0 && s < o && (o = s, a = u), u = n.indexOf(t, p + 1);
      p = c < u && c >= 0 ? c : u;
    }
    r.length && a !== void 0 && (l = [o, a]);
  }
  return l;
}, escSlash = "\0SLASH" + Math.random() + "\0", escOpen = "\0OPEN" + Math.random() + "\0", escClose = "\0CLOSE" + Math.random() + "\0", escComma = "\0COMMA" + Math.random() + "\0", escPeriod = "\0PERIOD" + Math.random() + "\0", escSlashPattern = new RegExp(escSlash, "g"), escOpenPattern = new RegExp(escOpen, "g"), escClosePattern = new RegExp(escClose, "g"), escCommaPattern = new RegExp(escComma, "g"), escPeriodPattern = new RegExp(escPeriod, "g"), slashPattern = /\\\\/g, openPattern = /\\{/g, closePattern = /\\}/g, commaPattern = /\\,/g, periodPattern = /\\\./g, EXPANSION_MAX = 1e5;
function numeric(e5) {
  return isNaN(e5) ? e5.charCodeAt(0) : parseInt(e5, 10);
}
function escapeBraces(e5) {
  return e5.replace(slashPattern, escSlash).replace(openPattern, escOpen).replace(closePattern, escClose).replace(commaPattern, escComma).replace(periodPattern, escPeriod);
}
function unescapeBraces(e5) {
  return e5.replace(escSlashPattern, "\\").replace(escOpenPattern, "{").replace(escClosePattern, "}").replace(escCommaPattern, ",").replace(escPeriodPattern, ".");
}
function parseCommaParts(e5) {
  if (!e5) return [""];
  const t = [], n = balanced("{", "}", e5);
  if (!n) return e5.split(",");
  const { pre: r, body: s, post: o } = n, a = r.split(",");
  a[a.length - 1] += "{" + s + "}";
  const l = parseCommaParts(o);
  return o.length && (a[a.length - 1] += l.shift(), a.push.apply(a, l)), t.push.apply(t, a), t;
}
function expand(e5, t = {}) {
  if (!e5) return [];
  const { max: n = EXPANSION_MAX } = t;
  return e5.slice(0, 2) === "{}" && (e5 = "\\{\\}" + e5.slice(2)), expand_(escapeBraces(e5), n, true).map(unescapeBraces);
}
function embrace(e5) {
  return "{" + e5 + "}";
}
function isPadded(e5) {
  return /^-?0\d/.test(e5);
}
function lte(e5, t) {
  return e5 <= t;
}
function gte(e5, t) {
  return e5 >= t;
}
function expand_(e5, t, n) {
  const r = [], s = balanced("{", "}", e5);
  if (!s) return [e5];
  const o = s.pre, a = s.post.length ? expand_(s.post, t, false) : [""];
  if (/\$$/.test(s.pre)) for (let l = 0; l < a.length && l < t; l++) {
    const c = o + "{" + s.body + "}" + a[l];
    r.push(c);
  }
  else {
    const l = /^-?\d+\.\.-?\d+(?:\.\.-?\d+)?$/.test(s.body), c = /^[a-zA-Z]\.\.[a-zA-Z](?:\.\.-?\d+)?$/.test(s.body), u = l || c, p = s.body.indexOf(",") >= 0;
    if (!u && !p) return s.post.match(/,(?!,).*\}/) ? (e5 = s.pre + "{" + s.body + escClose + s.post, expand_(e5, t, true)) : [e5];
    let f;
    if (u) f = s.body.split(/\.\./);
    else if (f = parseCommaParts(s.body), f.length === 1 && f[0] !== void 0 && (f = expand_(f[0], t, false).map(embrace), f.length === 1)) return a.map((m) => s.pre + f[0] + m);
    let h;
    if (u && f[0] !== void 0 && f[1] !== void 0) {
      const m = numeric(f[0]), g = numeric(f[1]), d = Math.max(f[0].length, f[1].length);
      let y = f.length === 3 && f[2] !== void 0 ? Math.max(Math.abs(numeric(f[2])), 1) : 1, S = lte;
      g < m && (y *= -1, S = gte);
      const E = f.some(isPadded);
      h = [];
      for (let b = m; S(b, g) && h.length < t; b += y) {
        let A;
        if (c) A = String.fromCharCode(b), A === "\\" && (A = "");
        else if (A = String(b), E) {
          const w = d - A.length;
          if (w > 0) {
            const v = new Array(w + 1).join("0");
            b < 0 ? A = "-" + v + A.slice(1) : A = v + A;
          }
        }
        h.push(A);
      }
    } else {
      h = [];
      for (let m = 0; m < f.length; m++) h.push.apply(h, expand_(f[m], t, false));
    }
    for (let m = 0; m < h.length; m++) for (let g = 0; g < a.length && r.length < t; g++) {
      const d = o + h[m] + a[g];
      (!n || u || d) && r.push(d);
    }
  }
  return r;
}
const MAX_PATTERN_LENGTH = 1024 * 64, assertValidPattern = (e5) => {
  if (typeof e5 != "string") throw new TypeError("invalid pattern");
  if (e5.length > MAX_PATTERN_LENGTH) throw new TypeError("pattern is too long");
}, posixClasses = { "[:alnum:]": ["\\p{L}\\p{Nl}\\p{Nd}", true], "[:alpha:]": ["\\p{L}\\p{Nl}", true], "[:ascii:]": ["\\x00-\\x7f", false], "[:blank:]": ["\\p{Zs}\\t", true], "[:cntrl:]": ["\\p{Cc}", true], "[:digit:]": ["\\p{Nd}", true], "[:graph:]": ["\\p{Z}\\p{C}", true, true], "[:lower:]": ["\\p{Ll}", true], "[:print:]": ["\\p{C}", true], "[:punct:]": ["\\p{P}", true], "[:space:]": ["\\p{Z}\\t\\r\\n\\v\\f", true], "[:upper:]": ["\\p{Lu}", true], "[:word:]": ["\\p{L}\\p{Nl}\\p{Nd}\\p{Pc}", true], "[:xdigit:]": ["A-Fa-f0-9", false] }, braceEscape = (e5) => e5.replace(/[[\]\\-]/g, "\\$&"), regexpEscape = (e5) => e5.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), rangesToString = (e5) => e5.join(""), parseClass = (e5, t) => {
  const n = t;
  if (e5.charAt(n) !== "[") throw new Error("not in a brace expression");
  const r = [], s = [];
  let o = n + 1, a = false, l = false, c = false, u = false, p = n, f = "";
  e: for (; o < e5.length; ) {
    const d = e5.charAt(o);
    if ((d === "!" || d === "^") && o === n + 1) {
      u = true, o++;
      continue;
    }
    if (d === "]" && a && !c) {
      p = o + 1;
      break;
    }
    if (a = true, d === "\\" && !c) {
      c = true, o++;
      continue;
    }
    if (d === "[" && !c) {
      for (const [y, [S, $, E]] of Object.entries(posixClasses)) if (e5.startsWith(y, o)) {
        if (f) return ["$.", false, e5.length - n, true];
        o += y.length, E ? s.push(S) : r.push(S), l = l || $;
        continue e;
      }
    }
    if (c = false, f) {
      d > f ? r.push(braceEscape(f) + "-" + braceEscape(d)) : d === f && r.push(braceEscape(d)), f = "", o++;
      continue;
    }
    if (e5.startsWith("-]", o + 1)) {
      r.push(braceEscape(d + "-")), o += 2;
      continue;
    }
    if (e5.startsWith("-", o + 1)) {
      f = d, o += 2;
      continue;
    }
    r.push(braceEscape(d)), o++;
  }
  if (p < o) return ["", false, 0, false];
  if (!r.length && !s.length) return ["$.", false, e5.length - n, true];
  if (s.length === 0 && r.length === 1 && /^\\?.$/.test(r[0]) && !u) {
    const d = r[0].length === 2 ? r[0].slice(-1) : r[0];
    return [regexpEscape(d), false, p - n, false];
  }
  const h = "[" + (u ? "^" : "") + rangesToString(r) + "]", m = "[" + (u ? "" : "^") + rangesToString(s) + "]";
  return [r.length && s.length ? "(" + h + "|" + m + ")" : r.length ? h : m, l, p - n, true];
}, unescape = (e5, { windowsPathsNoEscape: t = false, magicalBraces: n = true } = {}) => n ? t ? e5.replace(/\[([^/\\])\]/g, "$1") : e5.replace(/((?!\\).|^)\[([^/\\])\]/g, "$1$2").replace(/\\([^/])/g, "$1") : t ? e5.replace(/\[([^/\\{}])\]/g, "$1") : e5.replace(/((?!\\).|^)\[([^/\\{}])\]/g, "$1$2").replace(/\\([^/{}])/g, "$1");
var _a;
const types$1 = /* @__PURE__ */ new Set(["!", "?", "+", "*", "@"]), isExtglobType = (e5) => types$1.has(e5), isExtglobAST = (e5) => isExtglobType(e5.type), adoptionMap = /* @__PURE__ */ new Map([["!", ["@"]], ["?", ["?", "@"]], ["@", ["@"]], ["*", ["*", "+", "?", "@"]], ["+", ["+", "@"]]]), adoptionWithSpaceMap = /* @__PURE__ */ new Map([["!", ["?"]], ["@", ["?"]], ["+", ["?", "*"]]]), adoptionAnyMap = /* @__PURE__ */ new Map([["!", ["?", "@"]], ["?", ["?", "@"]], ["@", ["?", "@"]], ["*", ["*", "+", "?", "@"]], ["+", ["+", "@", "?", "*"]]]), usurpMap = /* @__PURE__ */ new Map([["!", /* @__PURE__ */ new Map([["!", "@"]])], ["?", /* @__PURE__ */ new Map([["*", "*"], ["+", "*"]])], ["@", /* @__PURE__ */ new Map([["!", "!"], ["?", "?"], ["@", "@"], ["*", "*"], ["+", "+"]])], ["+", /* @__PURE__ */ new Map([["?", "*"], ["*", "*"]])]]), startNoTraversal = "(?!(?:^|/)\\.\\.?(?:$|/))", startNoDot = "(?!\\.)", addPatternStart = /* @__PURE__ */ new Set(["[", "."]), justDots = /* @__PURE__ */ new Set(["..", "."]), reSpecials = new Set("().*{}+?[]^$\\!"), regExpEscape$1 = (e5) => e5.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), qmark$1 = "[^/]", star$1 = qmark$1 + "*?", starNoEmpty = qmark$1 + "+?";
let ID = 0;
class AST {
  constructor(t, n, r = {}) {
    __privateAdd(this, _AST_instances);
    __publicField$1(this, "type");
    __privateAdd(this, _n);
    __privateAdd(this, _i);
    __privateAdd(this, _r, false);
    __privateAdd(this, _e$2, []);
    __privateAdd(this, _t);
    __privateAdd(this, _a2);
    __privateAdd(this, _c);
    __privateAdd(this, _l, false);
    __privateAdd(this, _s);
    __privateAdd(this, _o);
    __privateAdd(this, _u, false);
    __publicField$1(this, "id", ++ID);
    this.type = t, t && __privateSet(this, _i, true), __privateSet(this, _t, n), __privateSet(this, _n, __privateGet(this, _t) ? __privateGet(__privateGet(this, _t), _n) : this), __privateSet(this, _s, __privateGet(this, _n) === this ? r : __privateGet(__privateGet(this, _n), _s)), __privateSet(this, _c, __privateGet(this, _n) === this ? [] : __privateGet(__privateGet(this, _n), _c)), t === "!" && !__privateGet(__privateGet(this, _n), _l) && __privateGet(this, _c).push(this), __privateSet(this, _a2, __privateGet(this, _t) ? __privateGet(__privateGet(this, _t), _e$2).length : 0);
  }
  get depth() {
    var _a4, _b;
    return ((_b = (_a4 = __privateGet(this, _t)) == null ? void 0 : _a4.depth) != null ? _b : -1) + 1;
  }
  [/* @__PURE__ */ Symbol.for("nodejs.util.inspect.custom")]() {
    var _a4;
    return { "@@type": "AST", id: this.id, type: this.type, root: __privateGet(this, _n).id, parent: (_a4 = __privateGet(this, _t)) == null ? void 0 : _a4.id, depth: this.depth, partsLength: __privateGet(this, _e$2).length, parts: __privateGet(this, _e$2) };
  }
  get hasMagic() {
    if (__privateGet(this, _i) !== void 0) return __privateGet(this, _i);
    for (const t of __privateGet(this, _e$2)) if (typeof t != "string" && (t.type || t.hasMagic)) return __privateSet(this, _i, true);
    return __privateGet(this, _i);
  }
  toString() {
    return __privateGet(this, _o) !== void 0 ? __privateGet(this, _o) : this.type ? __privateSet(this, _o, this.type + "(" + __privateGet(this, _e$2).map((t) => String(t)).join("|") + ")") : __privateSet(this, _o, __privateGet(this, _e$2).map((t) => String(t)).join(""));
  }
  push(...t) {
    for (const n of t) if (n !== "") {
      if (typeof n != "string" && !(n instanceof _a && __privateGet(n, _t) === this)) throw new Error("invalid part: " + n);
      __privateGet(this, _e$2).push(n);
    }
  }
  toJSON() {
    var _a4;
    const t = this.type === null ? __privateGet(this, _e$2).slice().map((n) => typeof n == "string" ? n : n.toJSON()) : [this.type, ...__privateGet(this, _e$2).map((n) => n.toJSON())];
    return this.isStart() && !this.type && t.unshift([]), this.isEnd() && (this === __privateGet(this, _n) || __privateGet(__privateGet(this, _n), _l) && ((_a4 = __privateGet(this, _t)) == null ? void 0 : _a4.type) === "!") && t.push({}), t;
  }
  isStart() {
    var _a4;
    if (__privateGet(this, _n) === this) return true;
    if (!((_a4 = __privateGet(this, _t)) == null ? void 0 : _a4.isStart())) return false;
    if (__privateGet(this, _a2) === 0) return true;
    const t = __privateGet(this, _t);
    for (let n = 0; n < __privateGet(this, _a2); n++) {
      const r = __privateGet(t, _e$2)[n];
      if (!(r instanceof _a && r.type === "!")) return false;
    }
    return true;
  }
  isEnd() {
    var _a4, _b, _c2;
    if (__privateGet(this, _n) === this || ((_a4 = __privateGet(this, _t)) == null ? void 0 : _a4.type) === "!") return true;
    if (!((_b = __privateGet(this, _t)) == null ? void 0 : _b.isEnd())) return false;
    if (!this.type) return (_c2 = __privateGet(this, _t)) == null ? void 0 : _c2.isEnd();
    const t = __privateGet(this, _t) ? __privateGet(__privateGet(this, _t), _e$2).length : 0;
    return __privateGet(this, _a2) === t - 1;
  }
  copyIn(t) {
    typeof t == "string" ? this.push(t) : this.push(t.clone(this));
  }
  clone(t) {
    const n = new _a(this.type, t);
    for (const r of __privateGet(this, _e$2)) n.copyIn(r);
    return n;
  }
  static fromGlob(t, n = {}) {
    var _a4;
    const r = new _a(null, void 0, n);
    return __privateMethod(_a4 = _a, _AST_static, f_fn).call(_a4, t, r, 0, n, 0), r;
  }
  toMMPattern() {
    if (this !== __privateGet(this, _n)) return __privateGet(this, _n).toMMPattern();
    const t = this.toString(), [n, r, s, o] = this.toRegExpSource();
    if (!(s || __privateGet(this, _i) || __privateGet(this, _s).nocase && !__privateGet(this, _s).nocaseMagicOnly && t.toUpperCase() !== t.toLowerCase())) return r;
    const l = (__privateGet(this, _s).nocase ? "i" : "") + (o ? "u" : "");
    return Object.assign(new RegExp(`^${n}$`, l), { _src: n, _glob: t });
  }
  get options() {
    return __privateGet(this, _s);
  }
  toRegExpSource(t) {
    var _a4;
    const n = t != null ? t : !!__privateGet(this, _s).dot;
    if (__privateGet(this, _n) === this && (__privateMethod(this, _AST_instances, d_fn).call(this), __privateMethod(this, _AST_instances, y_fn).call(this)), !isExtglobAST(this)) {
      const c = this.isStart() && this.isEnd() && !__privateGet(this, _e$2).some((m) => typeof m != "string"), u = __privateGet(this, _e$2).map((m) => {
        var _a5;
        const [g, d, y, S] = typeof m == "string" ? __privateMethod(_a5 = _a, _AST_static, w_fn).call(_a5, m, __privateGet(this, _i), c) : m.toRegExpSource(t);
        return __privateSet(this, _i, __privateGet(this, _i) || y), __privateSet(this, _r, __privateGet(this, _r) || S), g;
      }).join("");
      let p = "";
      if (this.isStart() && typeof __privateGet(this, _e$2)[0] == "string" && !(__privateGet(this, _e$2).length === 1 && justDots.has(__privateGet(this, _e$2)[0]))) {
        const g = addPatternStart, d = n && g.has(u.charAt(0)) || u.startsWith("\\.") && g.has(u.charAt(2)) || u.startsWith("\\.\\.") && g.has(u.charAt(4)), y = !n && !t && g.has(u.charAt(0));
        p = d ? startNoTraversal : y ? startNoDot : "";
      }
      let f = "";
      return this.isEnd() && __privateGet(__privateGet(this, _n), _l) && ((_a4 = __privateGet(this, _t)) == null ? void 0 : _a4.type) === "!" && (f = "(?:$|\\/)"), [p + u + f, unescape(u), __privateSet(this, _i, !!__privateGet(this, _i)), __privateGet(this, _r)];
    }
    const r = this.type === "*" || this.type === "+", s = this.type === "!" ? "(?:(?!(?:" : "(?:";
    let o = __privateMethod(this, _AST_instances, g_fn).call(this, n);
    if (this.isStart() && this.isEnd() && !o && this.type !== "!") {
      const c = this.toString(), u = this;
      return __privateSet(u, _e$2, [c]), u.type = null, __privateSet(u, _i, void 0), [c, unescape(this.toString()), false, false];
    }
    let a = !r || t || n || !startNoDot ? "" : __privateMethod(this, _AST_instances, g_fn).call(this, true);
    a === o && (a = ""), a && (o = `(?:${o})(?:${a})*?`);
    let l = "";
    if (this.type === "!" && __privateGet(this, _u)) l = (this.isStart() && !n ? startNoDot : "") + starNoEmpty;
    else {
      const c = this.type === "!" ? "))" + (this.isStart() && !n && !t ? startNoDot : "") + star$1 + ")" : this.type === "@" ? ")" : this.type === "?" ? ")?" : this.type === "+" && a ? ")" : this.type === "*" && a ? ")?" : `)${this.type}`;
      l = s + o + c;
    }
    return [l, unescape(o), __privateSet(this, _i, !!__privateGet(this, _i)), __privateGet(this, _r)];
  }
}
_n = new WeakMap();
_i = new WeakMap();
_r = new WeakMap();
_e$2 = new WeakMap();
_t = new WeakMap();
_a2 = new WeakMap();
_c = new WeakMap();
_l = new WeakMap();
_s = new WeakMap();
_o = new WeakMap();
_u = new WeakMap();
_AST_instances = new WeakSet();
y_fn = function() {
  if (this !== __privateGet(this, _n)) throw new Error("should only call on root");
  if (__privateGet(this, _l)) return this;
  this.toString(), __privateSet(this, _l, true);
  let t;
  for (; t = __privateGet(this, _c).pop(); ) {
    if (t.type !== "!") continue;
    let n = t, r = __privateGet(n, _t);
    for (; r; ) {
      for (let s = __privateGet(n, _a2) + 1; !r.type && s < __privateGet(r, _e$2).length; s++) for (const o of __privateGet(t, _e$2)) {
        if (typeof o == "string") throw new Error("string part in extglob AST??");
        o.copyIn(__privateGet(r, _e$2)[s]);
      }
      n = r, r = __privateGet(n, _t);
    }
  }
  return this;
};
_AST_static = new WeakSet();
f_fn = function(t, n, r, s, o) {
  var _a4, _b, _c2, _d, _e2;
  const a = (_a4 = s.maxExtglobRecursion) != null ? _a4 : 2;
  let l = false, c = false, u = -1, p = false;
  if (n.type === null) {
    let d = r, y = "";
    for (; d < t.length; ) {
      const S = t.charAt(d++);
      if (l || S === "\\") {
        l = !l, y += S;
        continue;
      }
      if (c) {
        d === u + 1 ? (S === "^" || S === "!") && (p = true) : S === "]" && !(d === u + 2 && p) && (c = false), y += S;
        continue;
      } else if (S === "[") {
        c = true, u = d, p = false, y += S;
        continue;
      }
      if (!s.noext && isExtglobType(S) && t.charAt(d) === "(" && o <= a) {
        n.push(y), y = "";
        const E = new _a(S, n);
        d = __privateMethod(_b = _a, _AST_static, f_fn).call(_b, t, E, d, s, o + 1), n.push(E);
        continue;
      }
      y += S;
    }
    return n.push(y), d;
  }
  let f = r + 1, h = new _a(null, n);
  const m = [];
  let g = "";
  for (; f < t.length; ) {
    const d = t.charAt(f++);
    if (l || d === "\\") {
      l = !l, g += d;
      continue;
    }
    if (c) {
      f === u + 1 ? (d === "^" || d === "!") && (p = true) : d === "]" && !(f === u + 2 && p) && (c = false), g += d;
      continue;
    } else if (d === "[") {
      c = true, u = f, p = false, g += d;
      continue;
    }
    if (!s.noext && isExtglobType(d) && t.charAt(f) === "(" && (o <= a || n && __privateMethod(_c2 = n, _AST_instances, p_fn).call(_c2, d))) {
      const S = n && __privateMethod(_d = n, _AST_instances, p_fn).call(_d, d) ? 0 : 1;
      h.push(g), g = "";
      const $ = new _a(d, h);
      h.push($), f = __privateMethod(_e2 = _a, _AST_static, f_fn).call(_e2, t, $, f, s, o + S);
      continue;
    }
    if (d === "|") {
      h.push(g), g = "", m.push(h), h = new _a(null, n);
      continue;
    }
    if (d === ")") return g === "" && __privateGet(n, _e$2).length === 0 && __privateSet(n, _u, true), h.push(g), g = "", n.push(...m, h), f;
    g += d;
  }
  return n.type = null, __privateSet(n, _i, void 0), __privateSet(n, _e$2, [t.substring(r - 1)]), f;
};
$_fn = function(t) {
  return __privateMethod(this, _AST_instances, h_fn).call(this, t, adoptionWithSpaceMap);
};
h_fn = function(t, n = adoptionMap) {
  if (!t || typeof t != "object" || t.type !== null || __privateGet(t, _e$2).length !== 1 || this.type === null) return false;
  const r = __privateGet(t, _e$2)[0];
  return !r || typeof r != "object" || r.type === null ? false : __privateMethod(this, _AST_instances, p_fn).call(this, r.type, n);
};
p_fn = function(t, n = adoptionAnyMap) {
  var _a4;
  return !!((_a4 = n.get(this.type)) == null ? void 0 : _a4.includes(t));
};
S_fn = function(t, n) {
  const r = __privateGet(t, _e$2)[0], s = new _a(null, r, this.options);
  __privateGet(s, _e$2).push(""), r.push(s), __privateMethod(this, _AST_instances, m_fn).call(this, t, n);
};
m_fn = function(t, n) {
  const r = __privateGet(t, _e$2)[0];
  __privateGet(this, _e$2).splice(n, 1, ...__privateGet(r, _e$2));
  for (const s of __privateGet(r, _e$2)) typeof s == "object" && __privateSet(s, _t, this);
  __privateSet(this, _o, void 0);
};
E_fn = function(t) {
  var _a4;
  return !!((_a4 = usurpMap.get(this.type)) == null ? void 0 : _a4.has(t));
};
A_fn = function(t) {
  if (!t || typeof t != "object" || t.type !== null || __privateGet(t, _e$2).length !== 1 || this.type === null || __privateGet(this, _e$2).length !== 1) return false;
  const n = __privateGet(t, _e$2)[0];
  return !n || typeof n != "object" || n.type === null ? false : __privateMethod(this, _AST_instances, E_fn).call(this, n.type);
};
b_fn = function(t) {
  const n = usurpMap.get(this.type), r = __privateGet(t, _e$2)[0], s = n == null ? void 0 : n.get(r.type);
  if (!s) return false;
  __privateSet(this, _e$2, __privateGet(r, _e$2));
  for (const o of __privateGet(this, _e$2)) typeof o == "object" && __privateSet(o, _t, this);
  this.type = s, __privateSet(this, _o, void 0), __privateSet(this, _u, false);
};
d_fn = function() {
  var _a4, _b;
  if (isExtglobAST(this)) {
    let t = 0, n = false;
    do {
      n = true;
      for (let r = 0; r < __privateGet(this, _e$2).length; r++) {
        const s = __privateGet(this, _e$2)[r];
        typeof s == "object" && (__privateMethod(_a4 = s, _AST_instances, d_fn).call(_a4), __privateMethod(this, _AST_instances, h_fn).call(this, s) ? (n = false, __privateMethod(this, _AST_instances, m_fn).call(this, s, r)) : __privateMethod(this, _AST_instances, $_fn).call(this, s) ? (n = false, __privateMethod(this, _AST_instances, S_fn).call(this, s, r)) : __privateMethod(this, _AST_instances, A_fn).call(this, s) && (n = false, __privateMethod(this, _AST_instances, b_fn).call(this, s)));
      }
    } while (!n && ++t < 10);
  } else for (const t of __privateGet(this, _e$2)) typeof t == "object" && __privateMethod(_b = t, _AST_instances, d_fn).call(_b);
  __privateSet(this, _o, void 0);
};
g_fn = function(t) {
  return __privateGet(this, _e$2).map((n) => {
    if (typeof n == "string") throw new Error("string type in extglob ast??");
    const [r, s, o, a] = n.toRegExpSource(t);
    return __privateSet(this, _r, __privateGet(this, _r) || a), r;
  }).filter((n) => !(this.isStart() && this.isEnd()) || !!n).join("|");
};
w_fn = function(t, n, r = false) {
  let s = false, o = "", a = false, l = false;
  for (let c = 0; c < t.length; c++) {
    const u = t.charAt(c);
    if (s) {
      s = false, o += (reSpecials.has(u) ? "\\" : "") + u;
      continue;
    }
    if (u === "*") {
      if (l) continue;
      l = true, o += r && /^[*]+$/.test(t) ? starNoEmpty : star$1, n = true;
      continue;
    } else l = false;
    if (u === "\\") {
      c === t.length - 1 ? o += "\\\\" : s = true;
      continue;
    }
    if (u === "[") {
      const [p, f, h, m] = parseClass(t, c);
      if (h) {
        o += p, a = a || f, c += h - 1, n = n || m;
        continue;
      }
    }
    if (u === "?") {
      o += qmark$1, n = true;
      continue;
    }
    o += regExpEscape$1(u);
  }
  return [o, unescape(t), !!n, a];
};
__privateAdd(AST, _AST_static);
_a = AST;
const escape = (e5, { windowsPathsNoEscape: t = false, magicalBraces: n = false } = {}) => n ? t ? e5.replace(/[?*()[\]{}]/g, "[$&]") : e5.replace(/[?*()[\]\\{}]/g, "\\$&") : t ? e5.replace(/[?*()[\]]/g, "[$&]") : e5.replace(/[?*()[\]\\]/g, "\\$&"), minimatch = (e5, t, n = {}) => (assertValidPattern(t), !n.nocomment && t.charAt(0) === "#" ? false : new Minimatch(t, n).match(e5)), starDotExtRE = /^\*+([^+@!?*[(]*)$/, starDotExtTest = (e5) => (t) => !t.startsWith(".") && t.endsWith(e5), starDotExtTestDot = (e5) => (t) => t.endsWith(e5), starDotExtTestNocase = (e5) => (e5 = e5.toLowerCase(), (t) => !t.startsWith(".") && t.toLowerCase().endsWith(e5)), starDotExtTestNocaseDot = (e5) => (e5 = e5.toLowerCase(), (t) => t.toLowerCase().endsWith(e5)), starDotStarRE = /^\*+\.\*+$/, starDotStarTest = (e5) => !e5.startsWith(".") && e5.includes("."), starDotStarTestDot = (e5) => e5 !== "." && e5 !== ".." && e5.includes("."), dotStarRE = /^\.\*+$/, dotStarTest = (e5) => e5 !== "." && e5 !== ".." && e5.startsWith("."), starRE = /^\*+$/, starTest = (e5) => e5.length !== 0 && !e5.startsWith("."), starTestDot = (e5) => e5.length !== 0 && e5 !== "." && e5 !== "..", qmarksRE = /^\?+([^+@!?*[(]*)?$/, qmarksTestNocase = ([e5, t = ""]) => {
  const n = qmarksTestNoExt([e5]);
  return t ? (t = t.toLowerCase(), (r) => n(r) && r.toLowerCase().endsWith(t)) : n;
}, qmarksTestNocaseDot = ([e5, t = ""]) => {
  const n = qmarksTestNoExtDot([e5]);
  return t ? (t = t.toLowerCase(), (r) => n(r) && r.toLowerCase().endsWith(t)) : n;
}, qmarksTestDot = ([e5, t = ""]) => {
  const n = qmarksTestNoExtDot([e5]);
  return t ? (r) => n(r) && r.endsWith(t) : n;
}, qmarksTest = ([e5, t = ""]) => {
  const n = qmarksTestNoExt([e5]);
  return t ? (r) => n(r) && r.endsWith(t) : n;
}, qmarksTestNoExt = ([e5]) => {
  const t = e5.length;
  return (n) => n.length === t && !n.startsWith(".");
}, qmarksTestNoExtDot = ([e5]) => {
  const t = e5.length;
  return (n) => n.length === t && n !== "." && n !== "..";
}, defaultPlatform = typeof process == "object" && process ? typeof process.env == "object" && process.env && process.env.__MINIMATCH_TESTING_PLATFORM__ || process.platform : "posix", path$1 = { win32: { sep: "\\" }, posix: { sep: "/" } }, sep = defaultPlatform === "win32" ? path$1.win32.sep : path$1.posix.sep;
minimatch.sep = sep;
const GLOBSTAR = /* @__PURE__ */ Symbol("globstar **");
minimatch.GLOBSTAR = GLOBSTAR;
const qmark = "[^/]", star = qmark + "*?", twoStarDot = "(?:(?!(?:\\/|^)(?:\\.{1,2})($|\\/)).)*?", twoStarNoDot = "(?:(?!(?:\\/|^)\\.).)*?", filter = (e5, t = {}) => (n) => minimatch(n, e5, t);
minimatch.filter = filter;
const ext = (e5, t = {}) => Object.assign({}, e5, t), defaults$5 = (e5) => {
  if (!e5 || typeof e5 != "object" || !Object.keys(e5).length) return minimatch;
  const t = minimatch;
  return Object.assign((r, s, o = {}) => t(r, s, ext(e5, o)), { Minimatch: class extends t.Minimatch {
    constructor(s, o = {}) {
      super(s, ext(e5, o));
    }
    static defaults(s) {
      return t.defaults(ext(e5, s)).Minimatch;
    }
  }, AST: class extends t.AST {
    constructor(s, o, a = {}) {
      super(s, o, ext(e5, a));
    }
    static fromGlob(s, o = {}) {
      return t.AST.fromGlob(s, ext(e5, o));
    }
  }, unescape: (r, s = {}) => t.unescape(r, ext(e5, s)), escape: (r, s = {}) => t.escape(r, ext(e5, s)), filter: (r, s = {}) => t.filter(r, ext(e5, s)), defaults: (r) => t.defaults(ext(e5, r)), makeRe: (r, s = {}) => t.makeRe(r, ext(e5, s)), braceExpand: (r, s = {}) => t.braceExpand(r, ext(e5, s)), match: (r, s, o = {}) => t.match(r, s, ext(e5, o)), sep: t.sep, GLOBSTAR });
};
minimatch.defaults = defaults$5;
const braceExpand = (e5, t = {}) => (assertValidPattern(e5), t.nobrace || !/\{(?:(?!\{).)*\}/.test(e5) ? [e5] : expand(e5, { max: t.braceExpandMax }));
minimatch.braceExpand = braceExpand;
const makeRe = (e5, t = {}) => new Minimatch(e5, t).makeRe();
minimatch.makeRe = makeRe;
const match = (e5, t, n = {}) => {
  const r = new Minimatch(t, n);
  return e5 = e5.filter((s) => r.match(s)), r.options.nonull && !e5.length && e5.push(t), e5;
};
minimatch.match = match;
const globMagic = /[?*]|[+@!]\(.*?\)|\[|\]/, regExpEscape = (e5) => e5.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&");
class Minimatch {
  constructor(t, n = {}) {
    __privateAdd(this, _Minimatch_instances);
    __publicField$1(this, "options");
    __publicField$1(this, "set");
    __publicField$1(this, "pattern");
    __publicField$1(this, "windowsPathsNoEscape");
    __publicField$1(this, "nonegate");
    __publicField$1(this, "negate");
    __publicField$1(this, "comment");
    __publicField$1(this, "empty");
    __publicField$1(this, "preserveMultipleSlashes");
    __publicField$1(this, "partial");
    __publicField$1(this, "globSet");
    __publicField$1(this, "globParts");
    __publicField$1(this, "nocase");
    __publicField$1(this, "isWindows");
    __publicField$1(this, "platform");
    __publicField$1(this, "windowsNoMagicRoot");
    __publicField$1(this, "maxGlobstarRecursion");
    __publicField$1(this, "regexp");
    var _a4;
    assertValidPattern(t), n = n || {}, this.options = n, this.maxGlobstarRecursion = (_a4 = n.maxGlobstarRecursion) != null ? _a4 : 200, this.pattern = t, this.platform = n.platform || defaultPlatform, this.isWindows = this.platform === "win32";
    const r = "allowWindowsEscape";
    this.windowsPathsNoEscape = !!n.windowsPathsNoEscape || n[r] === false, this.windowsPathsNoEscape && (this.pattern = this.pattern.replace(/\\/g, "/")), this.preserveMultipleSlashes = !!n.preserveMultipleSlashes, this.regexp = null, this.negate = false, this.nonegate = !!n.nonegate, this.comment = false, this.empty = false, this.partial = !!n.partial, this.nocase = !!this.options.nocase, this.windowsNoMagicRoot = n.windowsNoMagicRoot !== void 0 ? n.windowsNoMagicRoot : !!(this.isWindows && this.nocase), this.globSet = [], this.globParts = [], this.set = [], this.make();
  }
  hasMagic() {
    if (this.options.magicalBraces && this.set.length > 1) return true;
    for (const t of this.set) for (const n of t) if (typeof n != "string") return true;
    return false;
  }
  debug(...t) {
  }
  make() {
    const t = this.pattern, n = this.options;
    if (!n.nocomment && t.charAt(0) === "#") {
      this.comment = true;
      return;
    }
    if (!t) {
      this.empty = true;
      return;
    }
    this.parseNegate(), this.globSet = [...new Set(this.braceExpand())], n.debug && (this.debug = (...o) => console.error(...o)), this.debug(this.pattern, this.globSet);
    const r = this.globSet.map((o) => this.slashSplit(o));
    this.globParts = this.preprocess(r), this.debug(this.pattern, this.globParts);
    let s = this.globParts.map((o, a, l) => {
      if (this.isWindows && this.windowsNoMagicRoot) {
        const c = o[0] === "" && o[1] === "" && (o[2] === "?" || !globMagic.test(o[2])) && !globMagic.test(o[3]), u = /^[a-z]:/i.test(o[0]);
        if (c) return [...o.slice(0, 4), ...o.slice(4).map((p) => this.parse(p))];
        if (u) return [o[0], ...o.slice(1).map((p) => this.parse(p))];
      }
      return o.map((c) => this.parse(c));
    });
    if (this.debug(this.pattern, s), this.set = s.filter((o) => o.indexOf(false) === -1), this.isWindows) for (let o = 0; o < this.set.length; o++) {
      const a = this.set[o];
      a[0] === "" && a[1] === "" && this.globParts[o][2] === "?" && typeof a[3] == "string" && /^[a-z]:$/i.test(a[3]) && (a[2] = "?");
    }
    this.debug(this.pattern, this.set);
  }
  preprocess(t) {
    if (this.options.noglobstar) for (const r of t) for (let s = 0; s < r.length; s++) r[s] === "**" && (r[s] = "*");
    const { optimizationLevel: n = 1 } = this.options;
    return n >= 2 ? (t = this.firstPhasePreProcess(t), t = this.secondPhasePreProcess(t)) : n >= 1 ? t = this.levelOneOptimize(t) : t = this.adjascentGlobstarOptimize(t), t;
  }
  adjascentGlobstarOptimize(t) {
    return t.map((n) => {
      let r = -1;
      for (; (r = n.indexOf("**", r + 1)) !== -1; ) {
        let s = r;
        for (; n[s + 1] === "**"; ) s++;
        s !== r && n.splice(r, s - r);
      }
      return n;
    });
  }
  levelOneOptimize(t) {
    return t.map((n) => (n = n.reduce((r, s) => {
      const o = r[r.length - 1];
      return s === "**" && o === "**" ? r : s === ".." && o && o !== ".." && o !== "." && o !== "**" ? (r.pop(), r) : (r.push(s), r);
    }, []), n.length === 0 ? [""] : n));
  }
  levelTwoFileOptimize(t) {
    Array.isArray(t) || (t = this.slashSplit(t));
    let n = false;
    do {
      if (n = false, !this.preserveMultipleSlashes) {
        for (let s = 1; s < t.length - 1; s++) {
          const o = t[s];
          s === 1 && o === "" && t[0] === "" || (o === "." || o === "") && (n = true, t.splice(s, 1), s--);
        }
        t[0] === "." && t.length === 2 && (t[1] === "." || t[1] === "") && (n = true, t.pop());
      }
      let r = 0;
      for (; (r = t.indexOf("..", r + 1)) !== -1; ) {
        const s = t[r - 1];
        s && s !== "." && s !== ".." && s !== "**" && !(this.isWindows && /^[a-z]:$/i.test(s)) && (n = true, t.splice(r - 1, 2), r -= 2);
      }
    } while (n);
    return t.length === 0 ? [""] : t;
  }
  firstPhasePreProcess(t) {
    let n = false;
    do {
      n = false;
      for (let r of t) {
        let s = -1;
        for (; (s = r.indexOf("**", s + 1)) !== -1; ) {
          let a = s;
          for (; r[a + 1] === "**"; ) a++;
          a > s && r.splice(s + 1, a - s);
          let l = r[s + 1];
          const c = r[s + 2], u = r[s + 3];
          if (l !== ".." || !c || c === "." || c === ".." || !u || u === "." || u === "..") continue;
          n = true, r.splice(s, 1);
          const p = r.slice(0);
          p[s] = "**", t.push(p), s--;
        }
        if (!this.preserveMultipleSlashes) {
          for (let a = 1; a < r.length - 1; a++) {
            const l = r[a];
            a === 1 && l === "" && r[0] === "" || (l === "." || l === "") && (n = true, r.splice(a, 1), a--);
          }
          r[0] === "." && r.length === 2 && (r[1] === "." || r[1] === "") && (n = true, r.pop());
        }
        let o = 0;
        for (; (o = r.indexOf("..", o + 1)) !== -1; ) {
          const a = r[o - 1];
          if (a && a !== "." && a !== ".." && a !== "**") {
            n = true;
            const c = o === 1 && r[o + 1] === "**" ? ["."] : [];
            r.splice(o - 1, 2, ...c), r.length === 0 && r.push(""), o -= 2;
          }
        }
      }
    } while (n);
    return t;
  }
  secondPhasePreProcess(t) {
    for (let n = 0; n < t.length - 1; n++) for (let r = n + 1; r < t.length; r++) {
      const s = this.partsMatch(t[n], t[r], !this.preserveMultipleSlashes);
      if (s) {
        t[n] = [], t[r] = s;
        break;
      }
    }
    return t.filter((n) => n.length);
  }
  partsMatch(t, n, r = false) {
    let s = 0, o = 0, a = [], l = "";
    for (; s < t.length && o < n.length; ) if (t[s] === n[o]) a.push(l === "b" ? n[o] : t[s]), s++, o++;
    else if (r && t[s] === "**" && n[o] === t[s + 1]) a.push(t[s]), s++;
    else if (r && n[o] === "**" && t[s] === n[o + 1]) a.push(n[o]), o++;
    else if (t[s] === "*" && n[o] && (this.options.dot || !n[o].startsWith(".")) && n[o] !== "**") {
      if (l === "b") return false;
      l = "a", a.push(t[s]), s++, o++;
    } else if (n[o] === "*" && t[s] && (this.options.dot || !t[s].startsWith(".")) && t[s] !== "**") {
      if (l === "a") return false;
      l = "b", a.push(n[o]), s++, o++;
    } else return false;
    return t.length === n.length && a;
  }
  parseNegate() {
    if (this.nonegate) return;
    const t = this.pattern;
    let n = false, r = 0;
    for (let s = 0; s < t.length && t.charAt(s) === "!"; s++) n = !n, r++;
    r && (this.pattern = t.slice(r)), this.negate = n;
  }
  matchOne(t, n, r = false) {
    let s = 0, o = 0;
    if (this.isWindows) {
      const l = typeof t[0] == "string" && /^[a-z]:$/i.test(t[0]), c = !l && t[0] === "" && t[1] === "" && t[2] === "?" && /^[a-z]:$/i.test(t[3]), u = typeof n[0] == "string" && /^[a-z]:$/i.test(n[0]), p = !u && n[0] === "" && n[1] === "" && n[2] === "?" && typeof n[3] == "string" && /^[a-z]:$/i.test(n[3]), f = c ? 3 : l ? 0 : void 0, h = p ? 3 : u ? 0 : void 0;
      if (typeof f == "number" && typeof h == "number") {
        const [m, g] = [t[f], n[h]];
        m.toLowerCase() === g.toLowerCase() && (n[h] = m, o = h, s = f);
      }
    }
    const { optimizationLevel: a = 1 } = this.options;
    return a >= 2 && (t = this.levelTwoFileOptimize(t)), n.includes(GLOBSTAR) ? __privateMethod(this, _Minimatch_instances, n_fn).call(this, t, n, r, s, o) : __privateMethod(this, _Minimatch_instances, r_fn).call(this, t, n, r, s, o);
  }
  braceExpand() {
    return braceExpand(this.pattern, this.options);
  }
  parse(t) {
    assertValidPattern(t);
    const n = this.options;
    if (t === "**") return GLOBSTAR;
    if (t === "") return "";
    let r, s = null;
    (r = t.match(starRE)) ? s = n.dot ? starTestDot : starTest : (r = t.match(starDotExtRE)) ? s = (n.nocase ? n.dot ? starDotExtTestNocaseDot : starDotExtTestNocase : n.dot ? starDotExtTestDot : starDotExtTest)(r[1]) : (r = t.match(qmarksRE)) ? s = (n.nocase ? n.dot ? qmarksTestNocaseDot : qmarksTestNocase : n.dot ? qmarksTestDot : qmarksTest)(r) : (r = t.match(starDotStarRE)) ? s = n.dot ? starDotStarTestDot : starDotStarTest : (r = t.match(dotStarRE)) && (s = dotStarTest);
    const o = AST.fromGlob(t, this.options).toMMPattern();
    return s && typeof o == "object" && Reflect.defineProperty(o, "test", { value: s }), o;
  }
  makeRe() {
    if (this.regexp || this.regexp === false) return this.regexp;
    const t = this.set;
    if (!t.length) return this.regexp = false, this.regexp;
    const n = this.options, r = n.noglobstar ? star : n.dot ? twoStarDot : twoStarNoDot, s = new Set(n.nocase ? ["i"] : []);
    let o = t.map((c) => {
      const u = c.map((f) => {
        if (f instanceof RegExp) for (const h of f.flags.split("")) s.add(h);
        return typeof f == "string" ? regExpEscape(f) : f === GLOBSTAR ? GLOBSTAR : f._src;
      });
      u.forEach((f, h) => {
        const m = u[h + 1], g = u[h - 1];
        f !== GLOBSTAR || g === GLOBSTAR || (g === void 0 ? m !== void 0 && m !== GLOBSTAR ? u[h + 1] = "(?:\\/|" + r + "\\/)?" + m : u[h] = r : m === void 0 ? u[h - 1] = g + "(?:\\/|\\/" + r + ")?" : m !== GLOBSTAR && (u[h - 1] = g + "(?:\\/|\\/" + r + "\\/)" + m, u[h + 1] = GLOBSTAR));
      });
      const p = u.filter((f) => f !== GLOBSTAR);
      if (this.partial && p.length >= 1) {
        const f = [];
        for (let h = 1; h <= p.length; h++) f.push(p.slice(0, h).join("/"));
        return "(?:" + f.join("|") + ")";
      }
      return p.join("/");
    }).join("|");
    const [a, l] = t.length > 1 ? ["(?:", ")"] : ["", ""];
    o = "^" + a + o + l + "$", this.partial && (o = "^(?:\\/|" + a + o.slice(1, -1) + l + ")$"), this.negate && (o = "^(?!" + o + ").+$");
    try {
      this.regexp = new RegExp(o, [...s].join(""));
    } catch {
      this.regexp = false;
    }
    return this.regexp;
  }
  slashSplit(t) {
    return this.preserveMultipleSlashes ? t.split("/") : this.isWindows && /^\/\/[^/]+/.test(t) ? ["", ...t.split(/\/+/)] : t.split(/\/+/);
  }
  match(t, n = this.partial) {
    if (this.debug("match", t, this.pattern), this.comment) return false;
    if (this.empty) return t === "";
    if (t === "/" && n) return true;
    const r = this.options;
    this.isWindows && (t = t.split("\\").join("/"));
    const s = this.slashSplit(t);
    this.debug(this.pattern, "split", s);
    const o = this.set;
    this.debug(this.pattern, "set", o);
    let a = s[s.length - 1];
    if (!a) for (let l = s.length - 2; !a && l >= 0; l--) a = s[l];
    for (const l of o) {
      let c = s;
      if (r.matchBase && l.length === 1 && (c = [a]), this.matchOne(c, l, n)) return r.flipNegate ? true : !this.negate;
    }
    return r.flipNegate ? false : this.negate;
  }
  static defaults(t) {
    return minimatch.defaults(t).Minimatch;
  }
}
_Minimatch_instances = new WeakSet();
n_fn = function(t, n, r, s, o) {
  const a = n.indexOf(GLOBSTAR, o), l = n.lastIndexOf(GLOBSTAR), [c, u, p] = r ? [n.slice(o, a), n.slice(a + 1), []] : [n.slice(o, a), n.slice(a + 1, l), n.slice(l + 1)];
  if (c.length) {
    const $ = t.slice(s, s + c.length);
    if (!__privateMethod(this, _Minimatch_instances, r_fn).call(this, $, c, r, 0, 0)) return false;
    s += c.length, o += c.length;
  }
  let f = 0;
  if (p.length) {
    if (p.length + s > t.length) return false;
    let $ = t.length - p.length;
    if (__privateMethod(this, _Minimatch_instances, r_fn).call(this, t, p, r, $, 0)) f = p.length;
    else {
      if (t[t.length - 1] !== "" || s + p.length === t.length || ($--, !__privateMethod(this, _Minimatch_instances, r_fn).call(this, t, p, r, $, 0))) return false;
      f = p.length + 1;
    }
  }
  if (!u.length) {
    let $ = !!f;
    for (let E = s; E < t.length - f; E++) {
      const b = String(t[E]);
      if ($ = true, b === "." || b === ".." || !this.options.dot && b.startsWith(".")) return false;
    }
    return r || $;
  }
  const h = [[[], 0]];
  let m = h[0], g = 0;
  const d = [0];
  for (const $ of u) $ === GLOBSTAR ? (d.push(g), m = [[], 0], h.push(m)) : (m[0].push($), g++);
  let y = h.length - 1;
  const S = t.length - f;
  for (const $ of h) $[1] = S - (d[y--] + $[0].length);
  return !!__privateMethod(this, _Minimatch_instances, i_fn).call(this, t, h, s, 0, r, 0, !!f);
};
i_fn = function(t, n, r, s, o, a, l) {
  const c = n[s];
  if (!c) {
    for (let f = r; f < t.length; f++) {
      l = true;
      const h = t[f];
      if (h === "." || h === ".." || !this.options.dot && h.startsWith(".")) return false;
    }
    return l;
  }
  const [u, p] = c;
  for (; r <= p; ) {
    if (__privateMethod(this, _Minimatch_instances, r_fn).call(this, t.slice(0, r + u.length), u, o, r, 0) && a < this.maxGlobstarRecursion) {
      const m = __privateMethod(this, _Minimatch_instances, i_fn).call(this, t, n, r + u.length, s + 1, o, a + 1, l);
      if (m !== false) return m;
    }
    const h = t[r];
    if (h === "." || h === ".." || !this.options.dot && h.startsWith(".")) return false;
    r++;
  }
  return o || null;
};
r_fn = function(t, n, r, s, o) {
  let a, l, c, u;
  for (a = s, l = o, u = t.length, c = n.length; a < u && l < c; a++, l++) {
    this.debug("matchOne loop");
    let p = n[l], f = t[a];
    if (this.debug(n, p, f), p === false || p === GLOBSTAR) return false;
    let h;
    if (typeof p == "string" ? (h = f === p, this.debug("string match", p, f, h)) : (h = p.test(f), this.debug("pattern match", p, f, h)), !h) return false;
  }
  if (a === u && l === c) return true;
  if (a === u) return r;
  if (l === c) return a === u - 1 && t[a] === "";
  throw new Error("wtf?");
};
minimatch.AST = AST;
minimatch.Minimatch = Minimatch;
minimatch.escape = escape;
minimatch.unescape = unescape;
const jsonSerializer = { format: "json", async serializeDocumentFileContents(e5, t) {
  return JSON.stringify(e5, null, 2);
}, async deserializeDocumentFileContents(e5, t) {
  return JSON.parse(e5);
} };
var toString = Object.prototype.toString, kindOf = function(t) {
  if (t === void 0) return "undefined";
  if (t === null) return "null";
  var n = typeof t;
  if (n === "boolean") return "boolean";
  if (n === "string") return "string";
  if (n === "number") return "number";
  if (n === "symbol") return "symbol";
  if (n === "function") return isGeneratorFn(t) ? "generatorfunction" : "function";
  if (isArray(t)) return "array";
  if (isBuffer$1(t)) return "buffer";
  if (isArguments(t)) return "arguments";
  if (isDate(t)) return "date";
  if (isError(t)) return "error";
  if (isRegexp(t)) return "regexp";
  switch (ctorName(t)) {
    case "Symbol":
      return "symbol";
    case "Promise":
      return "promise";
    case "WeakMap":
      return "weakmap";
    case "WeakSet":
      return "weakset";
    case "Map":
      return "map";
    case "Set":
      return "set";
    case "Int8Array":
      return "int8array";
    case "Uint8Array":
      return "uint8array";
    case "Uint8ClampedArray":
      return "uint8clampedarray";
    case "Int16Array":
      return "int16array";
    case "Uint16Array":
      return "uint16array";
    case "Int32Array":
      return "int32array";
    case "Uint32Array":
      return "uint32array";
    case "Float32Array":
      return "float32array";
    case "Float64Array":
      return "float64array";
  }
  if (isGeneratorObj(t)) return "generator";
  switch (n = toString.call(t), n) {
    case "[object Object]":
      return "object";
    case "[object Map Iterator]":
      return "mapiterator";
    case "[object Set Iterator]":
      return "setiterator";
    case "[object String Iterator]":
      return "stringiterator";
    case "[object Array Iterator]":
      return "arrayiterator";
  }
  return n.slice(8, -1).toLowerCase().replace(/\s/g, "");
};
function ctorName(e5) {
  return typeof e5.constructor == "function" ? e5.constructor.name : null;
}
function isArray(e5) {
  return Array.isArray ? Array.isArray(e5) : e5 instanceof Array;
}
function isError(e5) {
  return e5 instanceof Error || typeof e5.message == "string" && e5.constructor && typeof e5.constructor.stackTraceLimit == "number";
}
function isDate(e5) {
  return e5 instanceof Date ? true : typeof e5.toDateString == "function" && typeof e5.getDate == "function" && typeof e5.setDate == "function";
}
function isRegexp(e5) {
  return e5 instanceof RegExp ? true : typeof e5.flags == "string" && typeof e5.ignoreCase == "boolean" && typeof e5.multiline == "boolean" && typeof e5.global == "boolean";
}
function isGeneratorFn(e5, t) {
  return ctorName(e5) === "GeneratorFunction";
}
function isGeneratorObj(e5) {
  return typeof e5.throw == "function" && typeof e5.return == "function" && typeof e5.next == "function";
}
function isArguments(e5) {
  try {
    if (typeof e5.length == "number" && typeof e5.callee == "function") return true;
  } catch (t) {
    if (t.message.indexOf("callee") !== -1) return true;
  }
  return false;
}
function isBuffer$1(e5) {
  return e5.constructor && typeof e5.constructor.isBuffer == "function" ? e5.constructor.isBuffer(e5) : false;
}
/*!
* is-extendable <https://github.com/jonschlinkert/is-extendable>
*
* Copyright (c) 2015, Jon Schlinkert.
* Licensed under the MIT License.
*/
var isExtendable = function(t) {
  return typeof t < "u" && t !== null && (typeof t == "object" || typeof t == "function");
}, isObject$2 = isExtendable, extendShallow = function(t) {
  isObject$2(t) || (t = {});
  for (var n = arguments.length, r = 1; r < n; r++) {
    var s = arguments[r];
    isObject$2(s) && assign(t, s);
  }
  return t;
};
function assign(e5, t) {
  for (var n in t) hasOwn(t, n) && (e5[n] = t[n]);
}
function hasOwn(e5, t) {
  return Object.prototype.hasOwnProperty.call(e5, t);
}
var typeOf$2 = kindOf, extend$2 = extendShallow, sectionMatter = function(e5, t) {
  typeof t == "function" && (t = { parse: t });
  var n = toObject(e5), r = { section_delimiter: "---", parse: identity }, s = extend$2({}, r, t), o = s.section_delimiter, a = n.content.split(/\r?\n/), l = null, c = createSection(), u = [], p = [];
  function f(S) {
    n.content = S, l = [], u = [];
  }
  function h(S) {
    p.length && (c.key = getKey(p[0], o), c.content = S, s.parse(c, l), l.push(c), c = createSection(), u = [], p = []);
  }
  for (var m = 0; m < a.length; m++) {
    var g = a[m], d = p.length, y = g.trim();
    if (isDelimiter(y, o)) {
      if (y.length === 3 && m !== 0) {
        if (d === 0 || d === 2) {
          u.push(g);
          continue;
        }
        p.push(y), c.data = u.join(`
`), u = [];
        continue;
      }
      l === null && f(u.join(`
`)), d === 2 && h(u.join(`
`)), p.push(y);
      continue;
    }
    u.push(g);
  }
  return l === null ? f(u.join(`
`)) : h(u.join(`
`)), n.sections = l, n;
};
function isDelimiter(e5, t) {
  return !(e5.slice(0, t.length) !== t || e5.charAt(t.length + 1) === t.slice(-1));
}
function toObject(e5) {
  if (typeOf$2(e5) !== "object" && (e5 = { content: e5 }), typeof e5.content != "string" && !isBuffer(e5.content)) throw new TypeError("expected a buffer or string");
  return e5.content = e5.content.toString(), e5.sections = [], e5;
}
function getKey(e5, t) {
  return e5 ? e5.slice(t.length).trim() : "";
}
function createSection() {
  return { key: "", data: "", content: "" };
}
function identity(e5) {
  return e5;
}
function isBuffer(e5) {
  return e5 && e5.constructor && typeof e5.constructor.isBuffer == "function" ? e5.constructor.isBuffer(e5) : false;
}
var engines$2 = { exports: {} }, jsYaml$2 = {}, loader$2 = {}, common$7 = {};
function isNothing$1(e5) {
  return typeof e5 > "u" || e5 === null;
}
function isObject$1(e5) {
  return typeof e5 == "object" && e5 !== null;
}
function toArray$1(e5) {
  return Array.isArray(e5) ? e5 : isNothing$1(e5) ? [] : [e5];
}
function extend$1(e5, t) {
  var n, r, s, o;
  if (t) for (o = Object.keys(t), n = 0, r = o.length; n < r; n += 1) s = o[n], e5[s] = t[s];
  return e5;
}
function repeat$1(e5, t) {
  var n = "", r;
  for (r = 0; r < t; r += 1) n += e5;
  return n;
}
function isNegativeZero$1(e5) {
  return e5 === 0 && Number.NEGATIVE_INFINITY === 1 / e5;
}
common$7.isNothing = isNothing$1;
common$7.isObject = isObject$1;
common$7.toArray = toArray$1;
common$7.repeat = repeat$1;
common$7.isNegativeZero = isNegativeZero$1;
common$7.extend = extend$1;
function YAMLException$6(e5, t) {
  Error.call(this), this.name = "YAMLException", this.reason = e5, this.mark = t, this.message = (this.reason || "(unknown reason)") + (this.mark ? " " + this.mark.toString() : ""), Error.captureStackTrace ? Error.captureStackTrace(this, this.constructor) : this.stack = new Error().stack || "";
}
YAMLException$6.prototype = Object.create(Error.prototype);
YAMLException$6.prototype.constructor = YAMLException$6;
YAMLException$6.prototype.toString = function(t) {
  var n = this.name + ": ";
  return n += this.reason || "(unknown reason)", !t && this.mark && (n += " " + this.mark.toString()), n;
};
var exception$1 = YAMLException$6, common$6 = common$7;
function Mark$1(e5, t, n, r, s) {
  this.name = e5, this.buffer = t, this.position = n, this.line = r, this.column = s;
}
Mark$1.prototype.getSnippet = function(t, n) {
  var r, s, o, a, l;
  if (!this.buffer) return null;
  for (t = t || 4, n = n || 75, r = "", s = this.position; s > 0 && `\0\r
\x85\u2028\u2029`.indexOf(this.buffer.charAt(s - 1)) === -1; ) if (s -= 1, this.position - s > n / 2 - 1) {
    r = " ... ", s += 5;
    break;
  }
  for (o = "", a = this.position; a < this.buffer.length && `\0\r
\x85\u2028\u2029`.indexOf(this.buffer.charAt(a)) === -1; ) if (a += 1, a - this.position > n / 2 - 1) {
    o = " ... ", a -= 5;
    break;
  }
  return l = this.buffer.slice(s, a), common$6.repeat(" ", t) + r + l + o + `
` + common$6.repeat(" ", t + this.position - s + r.length) + "^";
};
Mark$1.prototype.toString = function(t) {
  var n, r = "";
  return this.name && (r += 'in "' + this.name + '" '), r += "at line " + (this.line + 1) + ", column " + (this.column + 1), t || (n = this.getSnippet(), n && (r += `:
` + n)), r;
};
var mark = Mark$1, YAMLException$5 = exception$1, TYPE_CONSTRUCTOR_OPTIONS$1 = ["kind", "resolve", "construct", "instanceOf", "predicate", "represent", "defaultStyle", "styleAliases"], YAML_NODE_KINDS$1 = ["scalar", "sequence", "mapping"];
function compileStyleAliases$1(e5) {
  var t = {};
  return e5 !== null && Object.keys(e5).forEach(function(n) {
    e5[n].forEach(function(r) {
      t[String(r)] = n;
    });
  }), t;
}
function Type$j(e5, t) {
  if (t = t || {}, Object.keys(t).forEach(function(n) {
    if (TYPE_CONSTRUCTOR_OPTIONS$1.indexOf(n) === -1) throw new YAMLException$5('Unknown option "' + n + '" is met in definition of "' + e5 + '" YAML type.');
  }), this.tag = e5, this.kind = t.kind || null, this.resolve = t.resolve || function() {
    return true;
  }, this.construct = t.construct || function(n) {
    return n;
  }, this.instanceOf = t.instanceOf || null, this.predicate = t.predicate || null, this.represent = t.represent || null, this.defaultStyle = t.defaultStyle || null, this.styleAliases = compileStyleAliases$1(t.styleAliases || null), YAML_NODE_KINDS$1.indexOf(this.kind) === -1) throw new YAMLException$5('Unknown kind "' + this.kind + '" is specified for "' + e5 + '" YAML type.');
}
var type$1 = Type$j, common$5 = common$7, YAMLException$4 = exception$1, Type$i = type$1;
function compileList$1(e5, t, n) {
  var r = [];
  return e5.include.forEach(function(s) {
    n = compileList$1(s, t, n);
  }), e5[t].forEach(function(s) {
    n.forEach(function(o, a) {
      o.tag === s.tag && o.kind === s.kind && r.push(a);
    }), n.push(s);
  }), n.filter(function(s, o) {
    return r.indexOf(o) === -1;
  });
}
function compileMap$1() {
  var e5 = { scalar: {}, sequence: {}, mapping: {}, fallback: {} }, t, n;
  function r(s) {
    e5[s.kind][s.tag] = e5.fallback[s.tag] = s;
  }
  for (t = 0, n = arguments.length; t < n; t += 1) arguments[t].forEach(r);
  return e5;
}
function Schema$7(e5) {
  this.include = e5.include || [], this.implicit = e5.implicit || [], this.explicit = e5.explicit || [], this.implicit.forEach(function(t) {
    if (t.loadKind && t.loadKind !== "scalar") throw new YAMLException$4("There is a non-scalar type in the implicit list of a schema. Implicit resolving of such types is not supported.");
  }), this.compiledImplicit = compileList$1(this, "implicit", []), this.compiledExplicit = compileList$1(this, "explicit", []), this.compiledTypeMap = compileMap$1(this.compiledImplicit, this.compiledExplicit);
}
Schema$7.DEFAULT = null;
Schema$7.create = function() {
  var t, n;
  switch (arguments.length) {
    case 1:
      t = Schema$7.DEFAULT, n = arguments[0];
      break;
    case 2:
      t = arguments[0], n = arguments[1];
      break;
    default:
      throw new YAMLException$4("Wrong number of arguments for Schema.create function");
  }
  if (t = common$5.toArray(t), n = common$5.toArray(n), !t.every(function(r) {
    return r instanceof Schema$7;
  })) throw new YAMLException$4("Specified list of super schemas (or a single Schema object) contains a non-Schema object.");
  if (!n.every(function(r) {
    return r instanceof Type$i;
  })) throw new YAMLException$4("Specified list of YAML types (or a single Type object) contains a non-Type object.");
  return new Schema$7({ include: t, explicit: n });
};
var schema$1 = Schema$7, Type$h = type$1, str$1 = new Type$h("tag:yaml.org,2002:str", { kind: "scalar", construct: function(e5) {
  return e5 !== null ? e5 : "";
} }), Type$g = type$1, seq$1 = new Type$g("tag:yaml.org,2002:seq", { kind: "sequence", construct: function(e5) {
  return e5 !== null ? e5 : [];
} }), Type$f = type$1, map$1 = new Type$f("tag:yaml.org,2002:map", { kind: "mapping", construct: function(e5) {
  return e5 !== null ? e5 : {};
} }), Schema$6 = schema$1, failsafe$1 = new Schema$6({ explicit: [str$1, seq$1, map$1] }), Type$e = type$1;
function resolveYamlNull$1(e5) {
  if (e5 === null) return true;
  var t = e5.length;
  return t === 1 && e5 === "~" || t === 4 && (e5 === "null" || e5 === "Null" || e5 === "NULL");
}
function constructYamlNull$1() {
  return null;
}
function isNull$1(e5) {
  return e5 === null;
}
var _null$1 = new Type$e("tag:yaml.org,2002:null", { kind: "scalar", resolve: resolveYamlNull$1, construct: constructYamlNull$1, predicate: isNull$1, represent: { canonical: function() {
  return "~";
}, lowercase: function() {
  return "null";
}, uppercase: function() {
  return "NULL";
}, camelcase: function() {
  return "Null";
} }, defaultStyle: "lowercase" }), Type$d = type$1;
function resolveYamlBoolean$1(e5) {
  if (e5 === null) return false;
  var t = e5.length;
  return t === 4 && (e5 === "true" || e5 === "True" || e5 === "TRUE") || t === 5 && (e5 === "false" || e5 === "False" || e5 === "FALSE");
}
function constructYamlBoolean$1(e5) {
  return e5 === "true" || e5 === "True" || e5 === "TRUE";
}
function isBoolean$1(e5) {
  return Object.prototype.toString.call(e5) === "[object Boolean]";
}
var bool$1 = new Type$d("tag:yaml.org,2002:bool", { kind: "scalar", resolve: resolveYamlBoolean$1, construct: constructYamlBoolean$1, predicate: isBoolean$1, represent: { lowercase: function(e5) {
  return e5 ? "true" : "false";
}, uppercase: function(e5) {
  return e5 ? "TRUE" : "FALSE";
}, camelcase: function(e5) {
  return e5 ? "True" : "False";
} }, defaultStyle: "lowercase" }), common$4 = common$7, Type$c = type$1;
function isHexCode$1(e5) {
  return 48 <= e5 && e5 <= 57 || 65 <= e5 && e5 <= 70 || 97 <= e5 && e5 <= 102;
}
function isOctCode$1(e5) {
  return 48 <= e5 && e5 <= 55;
}
function isDecCode$1(e5) {
  return 48 <= e5 && e5 <= 57;
}
function resolveYamlInteger$1(e5) {
  if (e5 === null) return false;
  var t = e5.length, n = 0, r = false, s;
  if (!t) return false;
  if (s = e5[n], (s === "-" || s === "+") && (s = e5[++n]), s === "0") {
    if (n + 1 === t) return true;
    if (s = e5[++n], s === "b") {
      for (n++; n < t; n++) if (s = e5[n], s !== "_") {
        if (s !== "0" && s !== "1") return false;
        r = true;
      }
      return r && s !== "_";
    }
    if (s === "x") {
      for (n++; n < t; n++) if (s = e5[n], s !== "_") {
        if (!isHexCode$1(e5.charCodeAt(n))) return false;
        r = true;
      }
      return r && s !== "_";
    }
    for (; n < t; n++) if (s = e5[n], s !== "_") {
      if (!isOctCode$1(e5.charCodeAt(n))) return false;
      r = true;
    }
    return r && s !== "_";
  }
  if (s === "_") return false;
  for (; n < t; n++) if (s = e5[n], s !== "_") {
    if (s === ":") break;
    if (!isDecCode$1(e5.charCodeAt(n))) return false;
    r = true;
  }
  return !r || s === "_" ? false : s !== ":" ? true : /^(:[0-5]?[0-9])+$/.test(e5.slice(n));
}
function constructYamlInteger$1(e5) {
  var t = e5, n = 1, r, s, o = [];
  return t.indexOf("_") !== -1 && (t = t.replace(/_/g, "")), r = t[0], (r === "-" || r === "+") && (r === "-" && (n = -1), t = t.slice(1), r = t[0]), t === "0" ? 0 : r === "0" ? t[1] === "b" ? n * parseInt(t.slice(2), 2) : t[1] === "x" ? n * parseInt(t, 16) : n * parseInt(t, 8) : t.indexOf(":") !== -1 ? (t.split(":").forEach(function(a) {
    o.unshift(parseInt(a, 10));
  }), t = 0, s = 1, o.forEach(function(a) {
    t += a * s, s *= 60;
  }), n * t) : n * parseInt(t, 10);
}
function isInteger$1(e5) {
  return Object.prototype.toString.call(e5) === "[object Number]" && e5 % 1 === 0 && !common$4.isNegativeZero(e5);
}
var int$1 = new Type$c("tag:yaml.org,2002:int", { kind: "scalar", resolve: resolveYamlInteger$1, construct: constructYamlInteger$1, predicate: isInteger$1, represent: { binary: function(e5) {
  return e5 >= 0 ? "0b" + e5.toString(2) : "-0b" + e5.toString(2).slice(1);
}, octal: function(e5) {
  return e5 >= 0 ? "0" + e5.toString(8) : "-0" + e5.toString(8).slice(1);
}, decimal: function(e5) {
  return e5.toString(10);
}, hexadecimal: function(e5) {
  return e5 >= 0 ? "0x" + e5.toString(16).toUpperCase() : "-0x" + e5.toString(16).toUpperCase().slice(1);
} }, defaultStyle: "decimal", styleAliases: { binary: [2, "bin"], octal: [8, "oct"], decimal: [10, "dec"], hexadecimal: [16, "hex"] } }), common$3 = common$7, Type$b = type$1, YAML_FLOAT_PATTERN$1 = new RegExp("^(?:[-+]?(?:0|[1-9][0-9_]*)(?:\\.[0-9_]*)?(?:[eE][-+]?[0-9]+)?|\\.[0-9_]+(?:[eE][-+]?[0-9]+)?|[-+]?[0-9][0-9_]*(?::[0-5]?[0-9])+\\.[0-9_]*|[-+]?\\.(?:inf|Inf|INF)|\\.(?:nan|NaN|NAN))$");
function resolveYamlFloat$1(e5) {
  return !(e5 === null || !YAML_FLOAT_PATTERN$1.test(e5) || e5[e5.length - 1] === "_");
}
function constructYamlFloat$1(e5) {
  var t, n, r, s;
  return t = e5.replace(/_/g, "").toLowerCase(), n = t[0] === "-" ? -1 : 1, s = [], "+-".indexOf(t[0]) >= 0 && (t = t.slice(1)), t === ".inf" ? n === 1 ? Number.POSITIVE_INFINITY : Number.NEGATIVE_INFINITY : t === ".nan" ? NaN : t.indexOf(":") >= 0 ? (t.split(":").forEach(function(o) {
    s.unshift(parseFloat(o, 10));
  }), t = 0, r = 1, s.forEach(function(o) {
    t += o * r, r *= 60;
  }), n * t) : n * parseFloat(t, 10);
}
var SCIENTIFIC_WITHOUT_DOT$1 = /^[-+]?[0-9]+e/;
function representYamlFloat$1(e5, t) {
  var n;
  if (isNaN(e5)) switch (t) {
    case "lowercase":
      return ".nan";
    case "uppercase":
      return ".NAN";
    case "camelcase":
      return ".NaN";
  }
  else if (Number.POSITIVE_INFINITY === e5) switch (t) {
    case "lowercase":
      return ".inf";
    case "uppercase":
      return ".INF";
    case "camelcase":
      return ".Inf";
  }
  else if (Number.NEGATIVE_INFINITY === e5) switch (t) {
    case "lowercase":
      return "-.inf";
    case "uppercase":
      return "-.INF";
    case "camelcase":
      return "-.Inf";
  }
  else if (common$3.isNegativeZero(e5)) return "-0.0";
  return n = e5.toString(10), SCIENTIFIC_WITHOUT_DOT$1.test(n) ? n.replace("e", ".e") : n;
}
function isFloat$1(e5) {
  return Object.prototype.toString.call(e5) === "[object Number]" && (e5 % 1 !== 0 || common$3.isNegativeZero(e5));
}
var float$1 = new Type$b("tag:yaml.org,2002:float", { kind: "scalar", resolve: resolveYamlFloat$1, construct: constructYamlFloat$1, predicate: isFloat$1, represent: representYamlFloat$1, defaultStyle: "lowercase" }), Schema$5 = schema$1, json$1 = new Schema$5({ include: [failsafe$1], implicit: [_null$1, bool$1, int$1, float$1] }), Schema$4 = schema$1, core$1 = new Schema$4({ include: [json$1] }), Type$a = type$1, YAML_DATE_REGEXP$1 = new RegExp("^([0-9][0-9][0-9][0-9])-([0-9][0-9])-([0-9][0-9])$"), YAML_TIMESTAMP_REGEXP$1 = new RegExp("^([0-9][0-9][0-9][0-9])-([0-9][0-9]?)-([0-9][0-9]?)(?:[Tt]|[ \\t]+)([0-9][0-9]?):([0-9][0-9]):([0-9][0-9])(?:\\.([0-9]*))?(?:[ \\t]*(Z|([-+])([0-9][0-9]?)(?::([0-9][0-9]))?))?$");
function resolveYamlTimestamp$1(e5) {
  return e5 === null ? false : YAML_DATE_REGEXP$1.exec(e5) !== null || YAML_TIMESTAMP_REGEXP$1.exec(e5) !== null;
}
function constructYamlTimestamp$1(e5) {
  var t, n, r, s, o, a, l, c = 0, u = null, p, f, h;
  if (t = YAML_DATE_REGEXP$1.exec(e5), t === null && (t = YAML_TIMESTAMP_REGEXP$1.exec(e5)), t === null) throw new Error("Date resolve error");
  if (n = +t[1], r = +t[2] - 1, s = +t[3], !t[4]) return new Date(Date.UTC(n, r, s));
  if (o = +t[4], a = +t[5], l = +t[6], t[7]) {
    for (c = t[7].slice(0, 3); c.length < 3; ) c += "0";
    c = +c;
  }
  return t[9] && (p = +t[10], f = +(t[11] || 0), u = (p * 60 + f) * 6e4, t[9] === "-" && (u = -u)), h = new Date(Date.UTC(n, r, s, o, a, l, c)), u && h.setTime(h.getTime() - u), h;
}
function representYamlTimestamp$1(e5) {
  return e5.toISOString();
}
var timestamp$1 = new Type$a("tag:yaml.org,2002:timestamp", { kind: "scalar", resolve: resolveYamlTimestamp$1, construct: constructYamlTimestamp$1, instanceOf: Date, represent: representYamlTimestamp$1 }), Type$9 = type$1;
function resolveYamlMerge$1(e5) {
  return e5 === "<<" || e5 === null;
}
var merge$1 = new Type$9("tag:yaml.org,2002:merge", { kind: "scalar", resolve: resolveYamlMerge$1 });
function commonjsRequire(e5) {
  throw new Error('Could not dynamically require "' + e5 + '". Please configure the dynamicRequireTargets or/and ignoreDynamicRequires option of @rollup/plugin-commonjs appropriately for this require call to work.');
}
var NodeBuffer;
try {
  var _require$1 = commonjsRequire;
  NodeBuffer = _require$1("buffer").Buffer;
} catch {
}
var Type$8 = type$1, BASE64_MAP$1 = `ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=
\r`;
function resolveYamlBinary$1(e5) {
  if (e5 === null) return false;
  var t, n, r = 0, s = e5.length, o = BASE64_MAP$1;
  for (n = 0; n < s; n++) if (t = o.indexOf(e5.charAt(n)), !(t > 64)) {
    if (t < 0) return false;
    r += 6;
  }
  return r % 8 === 0;
}
function constructYamlBinary$1(e5) {
  var t, n, r = e5.replace(/[\r\n=]/g, ""), s = r.length, o = BASE64_MAP$1, a = 0, l = [];
  for (t = 0; t < s; t++) t % 4 === 0 && t && (l.push(a >> 16 & 255), l.push(a >> 8 & 255), l.push(a & 255)), a = a << 6 | o.indexOf(r.charAt(t));
  return n = s % 4 * 6, n === 0 ? (l.push(a >> 16 & 255), l.push(a >> 8 & 255), l.push(a & 255)) : n === 18 ? (l.push(a >> 10 & 255), l.push(a >> 2 & 255)) : n === 12 && l.push(a >> 4 & 255), NodeBuffer ? NodeBuffer.from ? NodeBuffer.from(l) : new NodeBuffer(l) : l;
}
function representYamlBinary$1(e5) {
  var t = "", n = 0, r, s, o = e5.length, a = BASE64_MAP$1;
  for (r = 0; r < o; r++) r % 3 === 0 && r && (t += a[n >> 18 & 63], t += a[n >> 12 & 63], t += a[n >> 6 & 63], t += a[n & 63]), n = (n << 8) + e5[r];
  return s = o % 3, s === 0 ? (t += a[n >> 18 & 63], t += a[n >> 12 & 63], t += a[n >> 6 & 63], t += a[n & 63]) : s === 2 ? (t += a[n >> 10 & 63], t += a[n >> 4 & 63], t += a[n << 2 & 63], t += a[64]) : s === 1 && (t += a[n >> 2 & 63], t += a[n << 4 & 63], t += a[64], t += a[64]), t;
}
function isBinary$1(e5) {
  return NodeBuffer && NodeBuffer.isBuffer(e5);
}
var binary$1 = new Type$8("tag:yaml.org,2002:binary", { kind: "scalar", resolve: resolveYamlBinary$1, construct: constructYamlBinary$1, predicate: isBinary$1, represent: representYamlBinary$1 }), Type$7 = type$1, _hasOwnProperty$7 = Object.prototype.hasOwnProperty, _toString$5 = Object.prototype.toString;
function resolveYamlOmap$1(e5) {
  if (e5 === null) return true;
  var t = [], n, r, s, o, a, l = e5;
  for (n = 0, r = l.length; n < r; n += 1) {
    if (s = l[n], a = false, _toString$5.call(s) !== "[object Object]") return false;
    for (o in s) if (_hasOwnProperty$7.call(s, o)) if (!a) a = true;
    else return false;
    if (!a) return false;
    if (t.indexOf(o) === -1) t.push(o);
    else return false;
  }
  return true;
}
function constructYamlOmap$1(e5) {
  return e5 !== null ? e5 : [];
}
var omap$1 = new Type$7("tag:yaml.org,2002:omap", { kind: "sequence", resolve: resolveYamlOmap$1, construct: constructYamlOmap$1 }), Type$6 = type$1, _toString$4 = Object.prototype.toString;
function resolveYamlPairs$1(e5) {
  if (e5 === null) return true;
  var t, n, r, s, o, a = e5;
  for (o = new Array(a.length), t = 0, n = a.length; t < n; t += 1) {
    if (r = a[t], _toString$4.call(r) !== "[object Object]" || (s = Object.keys(r), s.length !== 1)) return false;
    o[t] = [s[0], r[s[0]]];
  }
  return true;
}
function constructYamlPairs$1(e5) {
  if (e5 === null) return [];
  var t, n, r, s, o, a = e5;
  for (o = new Array(a.length), t = 0, n = a.length; t < n; t += 1) r = a[t], s = Object.keys(r), o[t] = [s[0], r[s[0]]];
  return o;
}
var pairs$1 = new Type$6("tag:yaml.org,2002:pairs", { kind: "sequence", resolve: resolveYamlPairs$1, construct: constructYamlPairs$1 }), Type$5 = type$1, _hasOwnProperty$6 = Object.prototype.hasOwnProperty;
function resolveYamlSet$1(e5) {
  if (e5 === null) return true;
  var t, n = e5;
  for (t in n) if (_hasOwnProperty$6.call(n, t) && n[t] !== null) return false;
  return true;
}
function constructYamlSet$1(e5) {
  return e5 !== null ? e5 : {};
}
var set$1 = new Type$5("tag:yaml.org,2002:set", { kind: "mapping", resolve: resolveYamlSet$1, construct: constructYamlSet$1 }), Schema$3 = schema$1, default_safe = new Schema$3({ include: [core$1], implicit: [timestamp$1, merge$1], explicit: [binary$1, omap$1, pairs$1, set$1] }), Type$4 = type$1;
function resolveJavascriptUndefined() {
  return true;
}
function constructJavascriptUndefined() {
}
function representJavascriptUndefined() {
  return "";
}
function isUndefined(e5) {
  return typeof e5 > "u";
}
var _undefined = new Type$4("tag:yaml.org,2002:js/undefined", { kind: "scalar", resolve: resolveJavascriptUndefined, construct: constructJavascriptUndefined, predicate: isUndefined, represent: representJavascriptUndefined }), Type$3 = type$1;
function resolveJavascriptRegExp(e5) {
  if (e5 === null || e5.length === 0) return false;
  var t = e5, n = /\/([gim]*)$/.exec(e5), r = "";
  return !(t[0] === "/" && (n && (r = n[1]), r.length > 3 || t[t.length - r.length - 1] !== "/"));
}
function constructJavascriptRegExp(e5) {
  var t = e5, n = /\/([gim]*)$/.exec(e5), r = "";
  return t[0] === "/" && (n && (r = n[1]), t = t.slice(1, t.length - r.length - 1)), new RegExp(t, r);
}
function representJavascriptRegExp(e5) {
  var t = "/" + e5.source + "/";
  return e5.global && (t += "g"), e5.multiline && (t += "m"), e5.ignoreCase && (t += "i"), t;
}
function isRegExp(e5) {
  return Object.prototype.toString.call(e5) === "[object RegExp]";
}
var regexp = new Type$3("tag:yaml.org,2002:js/regexp", { kind: "scalar", resolve: resolveJavascriptRegExp, construct: constructJavascriptRegExp, predicate: isRegExp, represent: representJavascriptRegExp }), esprima;
try {
  var _require = commonjsRequire;
  esprima = _require("esprima");
} catch {
}
var Type$2 = type$1;
function resolveJavascriptFunction(e5) {
  if (e5 === null) return false;
  try {
    var t = "(" + e5 + ")", n = esprima.parse(t, { range: true });
    return !(n.type !== "Program" || n.body.length !== 1 || n.body[0].type !== "ExpressionStatement" || n.body[0].expression.type !== "ArrowFunctionExpression" && n.body[0].expression.type !== "FunctionExpression");
  } catch {
    return false;
  }
}
function constructJavascriptFunction(e5) {
  var t = "(" + e5 + ")", n = esprima.parse(t, { range: true }), r = [], s;
  if (n.type !== "Program" || n.body.length !== 1 || n.body[0].type !== "ExpressionStatement" || n.body[0].expression.type !== "ArrowFunctionExpression" && n.body[0].expression.type !== "FunctionExpression") throw new Error("Failed to resolve function");
  return n.body[0].expression.params.forEach(function(o) {
    r.push(o.name);
  }), s = n.body[0].expression.body.range, n.body[0].expression.body.type === "BlockStatement" ? new Function(r, t.slice(s[0] + 1, s[1] - 1)) : new Function(r, "return " + t.slice(s[0], s[1]));
}
function representJavascriptFunction(e5) {
  return e5.toString();
}
function isFunction(e5) {
  return Object.prototype.toString.call(e5) === "[object Function]";
}
var _function = new Type$2("tag:yaml.org,2002:js/function", { kind: "scalar", resolve: resolveJavascriptFunction, construct: constructJavascriptFunction, predicate: isFunction, represent: representJavascriptFunction }), Schema$2 = schema$1, default_full = Schema$2.DEFAULT = new Schema$2({ include: [default_safe], explicit: [_undefined, regexp, _function] }), common$2 = common$7, YAMLException$3 = exception$1, Mark = mark, DEFAULT_SAFE_SCHEMA$1 = default_safe, DEFAULT_FULL_SCHEMA$1 = default_full, _hasOwnProperty$5 = Object.prototype.hasOwnProperty, CONTEXT_FLOW_IN$1 = 1, CONTEXT_FLOW_OUT$1 = 2, CONTEXT_BLOCK_IN$1 = 3, CONTEXT_BLOCK_OUT$1 = 4, CHOMPING_CLIP$1 = 1, CHOMPING_STRIP$1 = 2, CHOMPING_KEEP$1 = 3, PATTERN_NON_PRINTABLE$1 = /[\x00-\x08\x0B\x0C\x0E-\x1F\x7F-\x84\x86-\x9F\uFFFE\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF]/, PATTERN_NON_ASCII_LINE_BREAKS$1 = /[\x85\u2028\u2029]/, PATTERN_FLOW_INDICATORS$1 = /[,\[\]\{\}]/, PATTERN_TAG_HANDLE$1 = /^(?:!|!!|![a-z\-]+!)$/i, PATTERN_TAG_URI$1 = /^(?:!|[^,\[\]\{\}])(?:%[0-9a-f]{2}|[0-9a-z\-#;\/\?:@&=\+\$,_\.!~\*'\(\)\[\]])*$/i;
function _class$1(e5) {
  return Object.prototype.toString.call(e5);
}
function is_EOL$1(e5) {
  return e5 === 10 || e5 === 13;
}
function is_WHITE_SPACE$1(e5) {
  return e5 === 9 || e5 === 32;
}
function is_WS_OR_EOL$1(e5) {
  return e5 === 9 || e5 === 32 || e5 === 10 || e5 === 13;
}
function is_FLOW_INDICATOR$1(e5) {
  return e5 === 44 || e5 === 91 || e5 === 93 || e5 === 123 || e5 === 125;
}
function fromHexCode$1(e5) {
  var t;
  return 48 <= e5 && e5 <= 57 ? e5 - 48 : (t = e5 | 32, 97 <= t && t <= 102 ? t - 97 + 10 : -1);
}
function escapedHexLen$1(e5) {
  return e5 === 120 ? 2 : e5 === 117 ? 4 : e5 === 85 ? 8 : 0;
}
function fromDecimalCode$1(e5) {
  return 48 <= e5 && e5 <= 57 ? e5 - 48 : -1;
}
function simpleEscapeSequence$1(e5) {
  return e5 === 48 ? "\0" : e5 === 97 ? "\x07" : e5 === 98 ? "\b" : e5 === 116 || e5 === 9 ? "	" : e5 === 110 ? `
` : e5 === 118 ? "\v" : e5 === 102 ? "\f" : e5 === 114 ? "\r" : e5 === 101 ? "\x1B" : e5 === 32 ? " " : e5 === 34 ? '"' : e5 === 47 ? "/" : e5 === 92 ? "\\" : e5 === 78 ? "\x85" : e5 === 95 ? "\xA0" : e5 === 76 ? "\u2028" : e5 === 80 ? "\u2029" : "";
}
function charFromCodepoint$1(e5) {
  return e5 <= 65535 ? String.fromCharCode(e5) : String.fromCharCode((e5 - 65536 >> 10) + 55296, (e5 - 65536 & 1023) + 56320);
}
function setProperty$1(e5, t, n) {
  t === "__proto__" ? Object.defineProperty(e5, t, { configurable: true, enumerable: true, writable: true, value: n }) : e5[t] = n;
}
var simpleEscapeCheck$1 = new Array(256), simpleEscapeMap$1 = new Array(256);
for (var i$1 = 0; i$1 < 256; i$1++) simpleEscapeCheck$1[i$1] = simpleEscapeSequence$1(i$1) ? 1 : 0, simpleEscapeMap$1[i$1] = simpleEscapeSequence$1(i$1);
function State$3(e5, t) {
  this.input = e5, this.filename = t.filename || null, this.schema = t.schema || DEFAULT_FULL_SCHEMA$1, this.onWarning = t.onWarning || null, this.legacy = t.legacy || false, this.json = t.json || false, this.listener = t.listener || null, this.implicitTypes = this.schema.compiledImplicit, this.typeMap = this.schema.compiledTypeMap, this.length = e5.length, this.position = 0, this.line = 0, this.lineStart = 0, this.lineIndent = 0, this.documents = [];
}
function generateError$1(e5, t) {
  return new YAMLException$3(t, new Mark(e5.filename, e5.input, e5.position, e5.line, e5.position - e5.lineStart));
}
function throwError$1(e5, t) {
  throw generateError$1(e5, t);
}
function throwWarning$1(e5, t) {
  e5.onWarning && e5.onWarning.call(null, generateError$1(e5, t));
}
var directiveHandlers$1 = { YAML: function(t, n, r) {
  var s, o, a;
  t.version !== null && throwError$1(t, "duplication of %YAML directive"), r.length !== 1 && throwError$1(t, "YAML directive accepts exactly one argument"), s = /^([0-9]+)\.([0-9]+)$/.exec(r[0]), s === null && throwError$1(t, "ill-formed argument of the YAML directive"), o = parseInt(s[1], 10), a = parseInt(s[2], 10), o !== 1 && throwError$1(t, "unacceptable YAML version of the document"), t.version = r[0], t.checkLineBreaks = a < 2, a !== 1 && a !== 2 && throwWarning$1(t, "unsupported YAML version of the document");
}, TAG: function(t, n, r) {
  var s, o;
  r.length !== 2 && throwError$1(t, "TAG directive accepts exactly two arguments"), s = r[0], o = r[1], PATTERN_TAG_HANDLE$1.test(s) || throwError$1(t, "ill-formed tag handle (first argument) of the TAG directive"), _hasOwnProperty$5.call(t.tagMap, s) && throwError$1(t, 'there is a previously declared suffix for "' + s + '" tag handle'), PATTERN_TAG_URI$1.test(o) || throwError$1(t, "ill-formed tag prefix (second argument) of the TAG directive"), t.tagMap[s] = o;
} };
function captureSegment$1(e5, t, n, r) {
  var s, o, a, l;
  if (t < n) {
    if (l = e5.input.slice(t, n), r) for (s = 0, o = l.length; s < o; s += 1) a = l.charCodeAt(s), a === 9 || 32 <= a && a <= 1114111 || throwError$1(e5, "expected valid JSON character");
    else PATTERN_NON_PRINTABLE$1.test(l) && throwError$1(e5, "the stream contains non-printable characters");
    e5.result += l;
  }
}
function mergeMappings$1(e5, t, n, r) {
  var s, o, a, l;
  for (common$2.isObject(n) || throwError$1(e5, "cannot merge mappings; the provided source object is unacceptable"), s = Object.keys(n), a = 0, l = s.length; a < l; a += 1) o = s[a], _hasOwnProperty$5.call(t, o) || (setProperty$1(t, o, n[o]), r[o] = true);
}
function storeMappingPair$1(e5, t, n, r, s, o, a, l) {
  var c, u;
  if (Array.isArray(s)) for (s = Array.prototype.slice.call(s), c = 0, u = s.length; c < u; c += 1) Array.isArray(s[c]) && throwError$1(e5, "nested arrays are not supported inside keys"), typeof s == "object" && _class$1(s[c]) === "[object Object]" && (s[c] = "[object Object]");
  if (typeof s == "object" && _class$1(s) === "[object Object]" && (s = "[object Object]"), s = String(s), t === null && (t = {}), r === "tag:yaml.org,2002:merge") if (Array.isArray(o)) for (c = 0, u = o.length; c < u; c += 1) mergeMappings$1(e5, t, o[c], n);
  else mergeMappings$1(e5, t, o, n);
  else !e5.json && !_hasOwnProperty$5.call(n, s) && _hasOwnProperty$5.call(t, s) && (e5.line = a || e5.line, e5.position = l || e5.position, throwError$1(e5, "duplicated mapping key")), setProperty$1(t, s, o), delete n[s];
  return t;
}
function readLineBreak$1(e5) {
  var t;
  t = e5.input.charCodeAt(e5.position), t === 10 ? e5.position++ : t === 13 ? (e5.position++, e5.input.charCodeAt(e5.position) === 10 && e5.position++) : throwError$1(e5, "a line break is expected"), e5.line += 1, e5.lineStart = e5.position;
}
function skipSeparationSpace$1(e5, t, n) {
  for (var r = 0, s = e5.input.charCodeAt(e5.position); s !== 0; ) {
    for (; is_WHITE_SPACE$1(s); ) s = e5.input.charCodeAt(++e5.position);
    if (t && s === 35) do
      s = e5.input.charCodeAt(++e5.position);
    while (s !== 10 && s !== 13 && s !== 0);
    if (is_EOL$1(s)) for (readLineBreak$1(e5), s = e5.input.charCodeAt(e5.position), r++, e5.lineIndent = 0; s === 32; ) e5.lineIndent++, s = e5.input.charCodeAt(++e5.position);
    else break;
  }
  return n !== -1 && r !== 0 && e5.lineIndent < n && throwWarning$1(e5, "deficient indentation"), r;
}
function testDocumentSeparator$1(e5) {
  var t = e5.position, n;
  return n = e5.input.charCodeAt(t), !!((n === 45 || n === 46) && n === e5.input.charCodeAt(t + 1) && n === e5.input.charCodeAt(t + 2) && (t += 3, n = e5.input.charCodeAt(t), n === 0 || is_WS_OR_EOL$1(n)));
}
function writeFoldedLines$1(e5, t) {
  t === 1 ? e5.result += " " : t > 1 && (e5.result += common$2.repeat(`
`, t - 1));
}
function readPlainScalar$1(e5, t, n) {
  var r, s, o, a, l, c, u, p, f = e5.kind, h = e5.result, m;
  if (m = e5.input.charCodeAt(e5.position), is_WS_OR_EOL$1(m) || is_FLOW_INDICATOR$1(m) || m === 35 || m === 38 || m === 42 || m === 33 || m === 124 || m === 62 || m === 39 || m === 34 || m === 37 || m === 64 || m === 96 || (m === 63 || m === 45) && (s = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL$1(s) || n && is_FLOW_INDICATOR$1(s))) return false;
  for (e5.kind = "scalar", e5.result = "", o = a = e5.position, l = false; m !== 0; ) {
    if (m === 58) {
      if (s = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL$1(s) || n && is_FLOW_INDICATOR$1(s)) break;
    } else if (m === 35) {
      if (r = e5.input.charCodeAt(e5.position - 1), is_WS_OR_EOL$1(r)) break;
    } else {
      if (e5.position === e5.lineStart && testDocumentSeparator$1(e5) || n && is_FLOW_INDICATOR$1(m)) break;
      if (is_EOL$1(m)) if (c = e5.line, u = e5.lineStart, p = e5.lineIndent, skipSeparationSpace$1(e5, false, -1), e5.lineIndent >= t) {
        l = true, m = e5.input.charCodeAt(e5.position);
        continue;
      } else {
        e5.position = a, e5.line = c, e5.lineStart = u, e5.lineIndent = p;
        break;
      }
    }
    l && (captureSegment$1(e5, o, a, false), writeFoldedLines$1(e5, e5.line - c), o = a = e5.position, l = false), is_WHITE_SPACE$1(m) || (a = e5.position + 1), m = e5.input.charCodeAt(++e5.position);
  }
  return captureSegment$1(e5, o, a, false), e5.result ? true : (e5.kind = f, e5.result = h, false);
}
function readSingleQuotedScalar$1(e5, t) {
  var n, r, s;
  if (n = e5.input.charCodeAt(e5.position), n !== 39) return false;
  for (e5.kind = "scalar", e5.result = "", e5.position++, r = s = e5.position; (n = e5.input.charCodeAt(e5.position)) !== 0; ) if (n === 39) if (captureSegment$1(e5, r, e5.position, true), n = e5.input.charCodeAt(++e5.position), n === 39) r = e5.position, e5.position++, s = e5.position;
  else return true;
  else is_EOL$1(n) ? (captureSegment$1(e5, r, s, true), writeFoldedLines$1(e5, skipSeparationSpace$1(e5, false, t)), r = s = e5.position) : e5.position === e5.lineStart && testDocumentSeparator$1(e5) ? throwError$1(e5, "unexpected end of the document within a single quoted scalar") : (e5.position++, s = e5.position);
  throwError$1(e5, "unexpected end of the stream within a single quoted scalar");
}
function readDoubleQuotedScalar$1(e5, t) {
  var n, r, s, o, a, l;
  if (l = e5.input.charCodeAt(e5.position), l !== 34) return false;
  for (e5.kind = "scalar", e5.result = "", e5.position++, n = r = e5.position; (l = e5.input.charCodeAt(e5.position)) !== 0; ) {
    if (l === 34) return captureSegment$1(e5, n, e5.position, true), e5.position++, true;
    if (l === 92) {
      if (captureSegment$1(e5, n, e5.position, true), l = e5.input.charCodeAt(++e5.position), is_EOL$1(l)) skipSeparationSpace$1(e5, false, t);
      else if (l < 256 && simpleEscapeCheck$1[l]) e5.result += simpleEscapeMap$1[l], e5.position++;
      else if ((a = escapedHexLen$1(l)) > 0) {
        for (s = a, o = 0; s > 0; s--) l = e5.input.charCodeAt(++e5.position), (a = fromHexCode$1(l)) >= 0 ? o = (o << 4) + a : throwError$1(e5, "expected hexadecimal character");
        e5.result += charFromCodepoint$1(o), e5.position++;
      } else throwError$1(e5, "unknown escape sequence");
      n = r = e5.position;
    } else is_EOL$1(l) ? (captureSegment$1(e5, n, r, true), writeFoldedLines$1(e5, skipSeparationSpace$1(e5, false, t)), n = r = e5.position) : e5.position === e5.lineStart && testDocumentSeparator$1(e5) ? throwError$1(e5, "unexpected end of the document within a double quoted scalar") : (e5.position++, r = e5.position);
  }
  throwError$1(e5, "unexpected end of the stream within a double quoted scalar");
}
function readFlowCollection$1(e5, t) {
  var n = true, r, s = e5.tag, o, a = e5.anchor, l, c, u, p, f, h = {}, m, g, d, y;
  if (y = e5.input.charCodeAt(e5.position), y === 91) c = 93, f = false, o = [];
  else if (y === 123) c = 125, f = true, o = {};
  else return false;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = o), y = e5.input.charCodeAt(++e5.position); y !== 0; ) {
    if (skipSeparationSpace$1(e5, true, t), y = e5.input.charCodeAt(e5.position), y === c) return e5.position++, e5.tag = s, e5.anchor = a, e5.kind = f ? "mapping" : "sequence", e5.result = o, true;
    n || throwError$1(e5, "missed comma between flow collection entries"), g = m = d = null, u = p = false, y === 63 && (l = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL$1(l) && (u = p = true, e5.position++, skipSeparationSpace$1(e5, true, t))), r = e5.line, composeNode$1(e5, t, CONTEXT_FLOW_IN$1, false, true), g = e5.tag, m = e5.result, skipSeparationSpace$1(e5, true, t), y = e5.input.charCodeAt(e5.position), (p || e5.line === r) && y === 58 && (u = true, y = e5.input.charCodeAt(++e5.position), skipSeparationSpace$1(e5, true, t), composeNode$1(e5, t, CONTEXT_FLOW_IN$1, false, true), d = e5.result), f ? storeMappingPair$1(e5, o, h, g, m, d) : u ? o.push(storeMappingPair$1(e5, null, h, g, m, d)) : o.push(m), skipSeparationSpace$1(e5, true, t), y = e5.input.charCodeAt(e5.position), y === 44 ? (n = true, y = e5.input.charCodeAt(++e5.position)) : n = false;
  }
  throwError$1(e5, "unexpected end of the stream within a flow collection");
}
function readBlockScalar$1(e5, t) {
  var n, r, s = CHOMPING_CLIP$1, o = false, a = false, l = t, c = 0, u = false, p, f;
  if (f = e5.input.charCodeAt(e5.position), f === 124) r = false;
  else if (f === 62) r = true;
  else return false;
  for (e5.kind = "scalar", e5.result = ""; f !== 0; ) if (f = e5.input.charCodeAt(++e5.position), f === 43 || f === 45) CHOMPING_CLIP$1 === s ? s = f === 43 ? CHOMPING_KEEP$1 : CHOMPING_STRIP$1 : throwError$1(e5, "repeat of a chomping mode identifier");
  else if ((p = fromDecimalCode$1(f)) >= 0) p === 0 ? throwError$1(e5, "bad explicit indentation width of a block scalar; it cannot be less than one") : a ? throwError$1(e5, "repeat of an indentation width identifier") : (l = t + p - 1, a = true);
  else break;
  if (is_WHITE_SPACE$1(f)) {
    do
      f = e5.input.charCodeAt(++e5.position);
    while (is_WHITE_SPACE$1(f));
    if (f === 35) do
      f = e5.input.charCodeAt(++e5.position);
    while (!is_EOL$1(f) && f !== 0);
  }
  for (; f !== 0; ) {
    for (readLineBreak$1(e5), e5.lineIndent = 0, f = e5.input.charCodeAt(e5.position); (!a || e5.lineIndent < l) && f === 32; ) e5.lineIndent++, f = e5.input.charCodeAt(++e5.position);
    if (!a && e5.lineIndent > l && (l = e5.lineIndent), is_EOL$1(f)) {
      c++;
      continue;
    }
    if (e5.lineIndent < l) {
      s === CHOMPING_KEEP$1 ? e5.result += common$2.repeat(`
`, o ? 1 + c : c) : s === CHOMPING_CLIP$1 && o && (e5.result += `
`);
      break;
    }
    for (r ? is_WHITE_SPACE$1(f) ? (u = true, e5.result += common$2.repeat(`
`, o ? 1 + c : c)) : u ? (u = false, e5.result += common$2.repeat(`
`, c + 1)) : c === 0 ? o && (e5.result += " ") : e5.result += common$2.repeat(`
`, c) : e5.result += common$2.repeat(`
`, o ? 1 + c : c), o = true, a = true, c = 0, n = e5.position; !is_EOL$1(f) && f !== 0; ) f = e5.input.charCodeAt(++e5.position);
    captureSegment$1(e5, n, e5.position, false);
  }
  return true;
}
function readBlockSequence$1(e5, t) {
  var n, r = e5.tag, s = e5.anchor, o = [], a, l = false, c;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = o), c = e5.input.charCodeAt(e5.position); c !== 0 && !(c !== 45 || (a = e5.input.charCodeAt(e5.position + 1), !is_WS_OR_EOL$1(a))); ) {
    if (l = true, e5.position++, skipSeparationSpace$1(e5, true, -1) && e5.lineIndent <= t) {
      o.push(null), c = e5.input.charCodeAt(e5.position);
      continue;
    }
    if (n = e5.line, composeNode$1(e5, t, CONTEXT_BLOCK_IN$1, false, true), o.push(e5.result), skipSeparationSpace$1(e5, true, -1), c = e5.input.charCodeAt(e5.position), (e5.line === n || e5.lineIndent > t) && c !== 0) throwError$1(e5, "bad indentation of a sequence entry");
    else if (e5.lineIndent < t) break;
  }
  return l ? (e5.tag = r, e5.anchor = s, e5.kind = "sequence", e5.result = o, true) : false;
}
function readBlockMapping$1(e5, t, n) {
  var r, s, o, a, l = e5.tag, c = e5.anchor, u = {}, p = {}, f = null, h = null, m = null, g = false, d = false, y;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = u), y = e5.input.charCodeAt(e5.position); y !== 0; ) {
    if (r = e5.input.charCodeAt(e5.position + 1), o = e5.line, a = e5.position, (y === 63 || y === 58) && is_WS_OR_EOL$1(r)) y === 63 ? (g && (storeMappingPair$1(e5, u, p, f, h, null), f = h = m = null), d = true, g = true, s = true) : g ? (g = false, s = true) : throwError$1(e5, "incomplete explicit mapping pair; a key node is missed; or followed by a non-tabulated empty line"), e5.position += 1, y = r;
    else if (composeNode$1(e5, n, CONTEXT_FLOW_OUT$1, false, true)) if (e5.line === o) {
      for (y = e5.input.charCodeAt(e5.position); is_WHITE_SPACE$1(y); ) y = e5.input.charCodeAt(++e5.position);
      if (y === 58) y = e5.input.charCodeAt(++e5.position), is_WS_OR_EOL$1(y) || throwError$1(e5, "a whitespace character is expected after the key-value separator within a block mapping"), g && (storeMappingPair$1(e5, u, p, f, h, null), f = h = m = null), d = true, g = false, s = false, f = e5.tag, h = e5.result;
      else if (d) throwError$1(e5, "can not read an implicit mapping pair; a colon is missed");
      else return e5.tag = l, e5.anchor = c, true;
    } else if (d) throwError$1(e5, "can not read a block mapping entry; a multiline key may not be an implicit key");
    else return e5.tag = l, e5.anchor = c, true;
    else break;
    if ((e5.line === o || e5.lineIndent > t) && (composeNode$1(e5, t, CONTEXT_BLOCK_OUT$1, true, s) && (g ? h = e5.result : m = e5.result), g || (storeMappingPair$1(e5, u, p, f, h, m, o, a), f = h = m = null), skipSeparationSpace$1(e5, true, -1), y = e5.input.charCodeAt(e5.position)), e5.lineIndent > t && y !== 0) throwError$1(e5, "bad indentation of a mapping entry");
    else if (e5.lineIndent < t) break;
  }
  return g && storeMappingPair$1(e5, u, p, f, h, null), d && (e5.tag = l, e5.anchor = c, e5.kind = "mapping", e5.result = u), d;
}
function readTagProperty$1(e5) {
  var t, n = false, r = false, s, o, a;
  if (a = e5.input.charCodeAt(e5.position), a !== 33) return false;
  if (e5.tag !== null && throwError$1(e5, "duplication of a tag property"), a = e5.input.charCodeAt(++e5.position), a === 60 ? (n = true, a = e5.input.charCodeAt(++e5.position)) : a === 33 ? (r = true, s = "!!", a = e5.input.charCodeAt(++e5.position)) : s = "!", t = e5.position, n) {
    do
      a = e5.input.charCodeAt(++e5.position);
    while (a !== 0 && a !== 62);
    e5.position < e5.length ? (o = e5.input.slice(t, e5.position), a = e5.input.charCodeAt(++e5.position)) : throwError$1(e5, "unexpected end of the stream within a verbatim tag");
  } else {
    for (; a !== 0 && !is_WS_OR_EOL$1(a); ) a === 33 && (r ? throwError$1(e5, "tag suffix cannot contain exclamation marks") : (s = e5.input.slice(t - 1, e5.position + 1), PATTERN_TAG_HANDLE$1.test(s) || throwError$1(e5, "named tag handle cannot contain such characters"), r = true, t = e5.position + 1)), a = e5.input.charCodeAt(++e5.position);
    o = e5.input.slice(t, e5.position), PATTERN_FLOW_INDICATORS$1.test(o) && throwError$1(e5, "tag suffix cannot contain flow indicator characters");
  }
  return o && !PATTERN_TAG_URI$1.test(o) && throwError$1(e5, "tag name cannot contain such characters: " + o), n ? e5.tag = o : _hasOwnProperty$5.call(e5.tagMap, s) ? e5.tag = e5.tagMap[s] + o : s === "!" ? e5.tag = "!" + o : s === "!!" ? e5.tag = "tag:yaml.org,2002:" + o : throwError$1(e5, 'undeclared tag handle "' + s + '"'), true;
}
function readAnchorProperty$1(e5) {
  var t, n;
  if (n = e5.input.charCodeAt(e5.position), n !== 38) return false;
  for (e5.anchor !== null && throwError$1(e5, "duplication of an anchor property"), n = e5.input.charCodeAt(++e5.position), t = e5.position; n !== 0 && !is_WS_OR_EOL$1(n) && !is_FLOW_INDICATOR$1(n); ) n = e5.input.charCodeAt(++e5.position);
  return e5.position === t && throwError$1(e5, "name of an anchor node must contain at least one character"), e5.anchor = e5.input.slice(t, e5.position), true;
}
function readAlias$1(e5) {
  var t, n, r;
  if (r = e5.input.charCodeAt(e5.position), r !== 42) return false;
  for (r = e5.input.charCodeAt(++e5.position), t = e5.position; r !== 0 && !is_WS_OR_EOL$1(r) && !is_FLOW_INDICATOR$1(r); ) r = e5.input.charCodeAt(++e5.position);
  return e5.position === t && throwError$1(e5, "name of an alias node must contain at least one character"), n = e5.input.slice(t, e5.position), _hasOwnProperty$5.call(e5.anchorMap, n) || throwError$1(e5, 'unidentified alias "' + n + '"'), e5.result = e5.anchorMap[n], skipSeparationSpace$1(e5, true, -1), true;
}
function composeNode$1(e5, t, n, r, s) {
  var o, a, l, c = 1, u = false, p = false, f, h, m, g, d;
  if (e5.listener !== null && e5.listener("open", e5), e5.tag = null, e5.anchor = null, e5.kind = null, e5.result = null, o = a = l = CONTEXT_BLOCK_OUT$1 === n || CONTEXT_BLOCK_IN$1 === n, r && skipSeparationSpace$1(e5, true, -1) && (u = true, e5.lineIndent > t ? c = 1 : e5.lineIndent === t ? c = 0 : e5.lineIndent < t && (c = -1)), c === 1) for (; readTagProperty$1(e5) || readAnchorProperty$1(e5); ) skipSeparationSpace$1(e5, true, -1) ? (u = true, l = o, e5.lineIndent > t ? c = 1 : e5.lineIndent === t ? c = 0 : e5.lineIndent < t && (c = -1)) : l = false;
  if (l && (l = u || s), (c === 1 || CONTEXT_BLOCK_OUT$1 === n) && (CONTEXT_FLOW_IN$1 === n || CONTEXT_FLOW_OUT$1 === n ? g = t : g = t + 1, d = e5.position - e5.lineStart, c === 1 ? l && (readBlockSequence$1(e5, d) || readBlockMapping$1(e5, d, g)) || readFlowCollection$1(e5, g) ? p = true : (a && readBlockScalar$1(e5, g) || readSingleQuotedScalar$1(e5, g) || readDoubleQuotedScalar$1(e5, g) ? p = true : readAlias$1(e5) ? (p = true, (e5.tag !== null || e5.anchor !== null) && throwError$1(e5, "alias node should not have any properties")) : readPlainScalar$1(e5, g, CONTEXT_FLOW_IN$1 === n) && (p = true, e5.tag === null && (e5.tag = "?")), e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result)) : c === 0 && (p = l && readBlockSequence$1(e5, d))), e5.tag !== null && e5.tag !== "!") if (e5.tag === "?") {
    for (e5.result !== null && e5.kind !== "scalar" && throwError$1(e5, 'unacceptable node kind for !<?> tag; it should be "scalar", not "' + e5.kind + '"'), f = 0, h = e5.implicitTypes.length; f < h; f += 1) if (m = e5.implicitTypes[f], m.resolve(e5.result)) {
      e5.result = m.construct(e5.result), e5.tag = m.tag, e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result);
      break;
    }
  } else _hasOwnProperty$5.call(e5.typeMap[e5.kind || "fallback"], e5.tag) ? (m = e5.typeMap[e5.kind || "fallback"][e5.tag], e5.result !== null && m.kind !== e5.kind && throwError$1(e5, "unacceptable node kind for !<" + e5.tag + '> tag; it should be "' + m.kind + '", not "' + e5.kind + '"'), m.resolve(e5.result) ? (e5.result = m.construct(e5.result), e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result)) : throwError$1(e5, "cannot resolve a node with !<" + e5.tag + "> explicit tag")) : throwError$1(e5, "unknown tag !<" + e5.tag + ">");
  return e5.listener !== null && e5.listener("close", e5), e5.tag !== null || e5.anchor !== null || p;
}
function readDocument$1(e5) {
  var t = e5.position, n, r, s, o = false, a;
  for (e5.version = null, e5.checkLineBreaks = e5.legacy, e5.tagMap = {}, e5.anchorMap = {}; (a = e5.input.charCodeAt(e5.position)) !== 0 && (skipSeparationSpace$1(e5, true, -1), a = e5.input.charCodeAt(e5.position), !(e5.lineIndent > 0 || a !== 37)); ) {
    for (o = true, a = e5.input.charCodeAt(++e5.position), n = e5.position; a !== 0 && !is_WS_OR_EOL$1(a); ) a = e5.input.charCodeAt(++e5.position);
    for (r = e5.input.slice(n, e5.position), s = [], r.length < 1 && throwError$1(e5, "directive name must not be less than one character in length"); a !== 0; ) {
      for (; is_WHITE_SPACE$1(a); ) a = e5.input.charCodeAt(++e5.position);
      if (a === 35) {
        do
          a = e5.input.charCodeAt(++e5.position);
        while (a !== 0 && !is_EOL$1(a));
        break;
      }
      if (is_EOL$1(a)) break;
      for (n = e5.position; a !== 0 && !is_WS_OR_EOL$1(a); ) a = e5.input.charCodeAt(++e5.position);
      s.push(e5.input.slice(n, e5.position));
    }
    a !== 0 && readLineBreak$1(e5), _hasOwnProperty$5.call(directiveHandlers$1, r) ? directiveHandlers$1[r](e5, r, s) : throwWarning$1(e5, 'unknown document directive "' + r + '"');
  }
  if (skipSeparationSpace$1(e5, true, -1), e5.lineIndent === 0 && e5.input.charCodeAt(e5.position) === 45 && e5.input.charCodeAt(e5.position + 1) === 45 && e5.input.charCodeAt(e5.position + 2) === 45 ? (e5.position += 3, skipSeparationSpace$1(e5, true, -1)) : o && throwError$1(e5, "directives end mark is expected"), composeNode$1(e5, e5.lineIndent - 1, CONTEXT_BLOCK_OUT$1, false, true), skipSeparationSpace$1(e5, true, -1), e5.checkLineBreaks && PATTERN_NON_ASCII_LINE_BREAKS$1.test(e5.input.slice(t, e5.position)) && throwWarning$1(e5, "non-ASCII line breaks are interpreted as content"), e5.documents.push(e5.result), e5.position === e5.lineStart && testDocumentSeparator$1(e5)) {
    e5.input.charCodeAt(e5.position) === 46 && (e5.position += 3, skipSeparationSpace$1(e5, true, -1));
    return;
  }
  if (e5.position < e5.length - 1) throwError$1(e5, "end of the stream or a document separator is expected");
  else return;
}
function loadDocuments$1(e5, t) {
  e5 = String(e5), t = t || {}, e5.length !== 0 && (e5.charCodeAt(e5.length - 1) !== 10 && e5.charCodeAt(e5.length - 1) !== 13 && (e5 += `
`), e5.charCodeAt(0) === 65279 && (e5 = e5.slice(1)));
  var n = new State$3(e5, t), r = e5.indexOf("\0");
  for (r !== -1 && (n.position = r, throwError$1(n, "null byte is not allowed in input")), n.input += "\0"; n.input.charCodeAt(n.position) === 32; ) n.lineIndent += 1, n.position += 1;
  for (; n.position < n.length - 1; ) readDocument$1(n);
  return n.documents;
}
function loadAll$2(e5, t, n) {
  t !== null && typeof t == "object" && typeof n > "u" && (n = t, t = null);
  var r = loadDocuments$1(e5, n);
  if (typeof t != "function") return r;
  for (var s = 0, o = r.length; s < o; s += 1) t(r[s]);
}
function load$2(e5, t) {
  var n = loadDocuments$1(e5, t);
  if (n.length !== 0) {
    if (n.length === 1) return n[0];
    throw new YAMLException$3("expected a single document in the stream, but found more");
  }
}
function safeLoadAll$1(e5, t, n) {
  return typeof t == "object" && t !== null && typeof n > "u" && (n = t, t = null), loadAll$2(e5, t, common$2.extend({ schema: DEFAULT_SAFE_SCHEMA$1 }, n));
}
function safeLoad$1(e5, t) {
  return load$2(e5, common$2.extend({ schema: DEFAULT_SAFE_SCHEMA$1 }, t));
}
loader$2.loadAll = loadAll$2;
loader$2.load = load$2;
loader$2.safeLoadAll = safeLoadAll$1;
loader$2.safeLoad = safeLoad$1;
var dumper$2 = {}, common$1 = common$7, YAMLException$2 = exception$1, DEFAULT_FULL_SCHEMA = default_full, DEFAULT_SAFE_SCHEMA = default_safe, _toString$3 = Object.prototype.toString, _hasOwnProperty$4 = Object.prototype.hasOwnProperty, CHAR_TAB$1 = 9, CHAR_LINE_FEED$1 = 10, CHAR_CARRIAGE_RETURN$1 = 13, CHAR_SPACE$1 = 32, CHAR_EXCLAMATION$1 = 33, CHAR_DOUBLE_QUOTE$1 = 34, CHAR_SHARP$1 = 35, CHAR_PERCENT$1 = 37, CHAR_AMPERSAND$1 = 38, CHAR_SINGLE_QUOTE$1 = 39, CHAR_ASTERISK$1 = 42, CHAR_COMMA$1 = 44, CHAR_MINUS$1 = 45, CHAR_COLON$1 = 58, CHAR_EQUALS$1 = 61, CHAR_GREATER_THAN$1 = 62, CHAR_QUESTION$1 = 63, CHAR_COMMERCIAL_AT$1 = 64, CHAR_LEFT_SQUARE_BRACKET$1 = 91, CHAR_RIGHT_SQUARE_BRACKET$1 = 93, CHAR_GRAVE_ACCENT$1 = 96, CHAR_LEFT_CURLY_BRACKET$1 = 123, CHAR_VERTICAL_LINE$1 = 124, CHAR_RIGHT_CURLY_BRACKET$1 = 125, ESCAPE_SEQUENCES$1 = {};
ESCAPE_SEQUENCES$1[0] = "\\0";
ESCAPE_SEQUENCES$1[7] = "\\a";
ESCAPE_SEQUENCES$1[8] = "\\b";
ESCAPE_SEQUENCES$1[9] = "\\t";
ESCAPE_SEQUENCES$1[10] = "\\n";
ESCAPE_SEQUENCES$1[11] = "\\v";
ESCAPE_SEQUENCES$1[12] = "\\f";
ESCAPE_SEQUENCES$1[13] = "\\r";
ESCAPE_SEQUENCES$1[27] = "\\e";
ESCAPE_SEQUENCES$1[34] = '\\"';
ESCAPE_SEQUENCES$1[92] = "\\\\";
ESCAPE_SEQUENCES$1[133] = "\\N";
ESCAPE_SEQUENCES$1[160] = "\\_";
ESCAPE_SEQUENCES$1[8232] = "\\L";
ESCAPE_SEQUENCES$1[8233] = "\\P";
var DEPRECATED_BOOLEANS_SYNTAX$1 = ["y", "Y", "yes", "Yes", "YES", "on", "On", "ON", "n", "N", "no", "No", "NO", "off", "Off", "OFF"];
function compileStyleMap$1(e5, t) {
  var n, r, s, o, a, l, c;
  if (t === null) return {};
  for (n = {}, r = Object.keys(t), s = 0, o = r.length; s < o; s += 1) a = r[s], l = String(t[a]), a.slice(0, 2) === "!!" && (a = "tag:yaml.org,2002:" + a.slice(2)), c = e5.compiledTypeMap.fallback[a], c && _hasOwnProperty$4.call(c.styleAliases, l) && (l = c.styleAliases[l]), n[a] = l;
  return n;
}
function encodeHex$1(e5) {
  var t, n, r;
  if (t = e5.toString(16).toUpperCase(), e5 <= 255) n = "x", r = 2;
  else if (e5 <= 65535) n = "u", r = 4;
  else if (e5 <= 4294967295) n = "U", r = 8;
  else throw new YAMLException$2("code point within a string may not be greater than 0xFFFFFFFF");
  return "\\" + n + common$1.repeat("0", r - t.length) + t;
}
function State$2(e5) {
  this.schema = e5.schema || DEFAULT_FULL_SCHEMA, this.indent = Math.max(1, e5.indent || 2), this.noArrayIndent = e5.noArrayIndent || false, this.skipInvalid = e5.skipInvalid || false, this.flowLevel = common$1.isNothing(e5.flowLevel) ? -1 : e5.flowLevel, this.styleMap = compileStyleMap$1(this.schema, e5.styles || null), this.sortKeys = e5.sortKeys || false, this.lineWidth = e5.lineWidth || 80, this.noRefs = e5.noRefs || false, this.noCompatMode = e5.noCompatMode || false, this.condenseFlow = e5.condenseFlow || false, this.implicitTypes = this.schema.compiledImplicit, this.explicitTypes = this.schema.compiledExplicit, this.tag = null, this.result = "", this.duplicates = [], this.usedDuplicates = null;
}
function indentString$1(e5, t) {
  for (var n = common$1.repeat(" ", t), r = 0, s = -1, o = "", a, l = e5.length; r < l; ) s = e5.indexOf(`
`, r), s === -1 ? (a = e5.slice(r), r = l) : (a = e5.slice(r, s + 1), r = s + 1), a.length && a !== `
` && (o += n), o += a;
  return o;
}
function generateNextLine$1(e5, t) {
  return `
` + common$1.repeat(" ", e5.indent * t);
}
function testImplicitResolving$1(e5, t) {
  var n, r, s;
  for (n = 0, r = e5.implicitTypes.length; n < r; n += 1) if (s = e5.implicitTypes[n], s.resolve(t)) return true;
  return false;
}
function isWhitespace$1(e5) {
  return e5 === CHAR_SPACE$1 || e5 === CHAR_TAB$1;
}
function isPrintable$1(e5) {
  return 32 <= e5 && e5 <= 126 || 161 <= e5 && e5 <= 55295 && e5 !== 8232 && e5 !== 8233 || 57344 <= e5 && e5 <= 65533 && e5 !== 65279 || 65536 <= e5 && e5 <= 1114111;
}
function isNsChar(e5) {
  return isPrintable$1(e5) && !isWhitespace$1(e5) && e5 !== 65279 && e5 !== CHAR_CARRIAGE_RETURN$1 && e5 !== CHAR_LINE_FEED$1;
}
function isPlainSafe$1(e5, t) {
  return isPrintable$1(e5) && e5 !== 65279 && e5 !== CHAR_COMMA$1 && e5 !== CHAR_LEFT_SQUARE_BRACKET$1 && e5 !== CHAR_RIGHT_SQUARE_BRACKET$1 && e5 !== CHAR_LEFT_CURLY_BRACKET$1 && e5 !== CHAR_RIGHT_CURLY_BRACKET$1 && e5 !== CHAR_COLON$1 && (e5 !== CHAR_SHARP$1 || t && isNsChar(t));
}
function isPlainSafeFirst$1(e5) {
  return isPrintable$1(e5) && e5 !== 65279 && !isWhitespace$1(e5) && e5 !== CHAR_MINUS$1 && e5 !== CHAR_QUESTION$1 && e5 !== CHAR_COLON$1 && e5 !== CHAR_COMMA$1 && e5 !== CHAR_LEFT_SQUARE_BRACKET$1 && e5 !== CHAR_RIGHT_SQUARE_BRACKET$1 && e5 !== CHAR_LEFT_CURLY_BRACKET$1 && e5 !== CHAR_RIGHT_CURLY_BRACKET$1 && e5 !== CHAR_SHARP$1 && e5 !== CHAR_AMPERSAND$1 && e5 !== CHAR_ASTERISK$1 && e5 !== CHAR_EXCLAMATION$1 && e5 !== CHAR_VERTICAL_LINE$1 && e5 !== CHAR_EQUALS$1 && e5 !== CHAR_GREATER_THAN$1 && e5 !== CHAR_SINGLE_QUOTE$1 && e5 !== CHAR_DOUBLE_QUOTE$1 && e5 !== CHAR_PERCENT$1 && e5 !== CHAR_COMMERCIAL_AT$1 && e5 !== CHAR_GRAVE_ACCENT$1;
}
function needIndentIndicator$1(e5) {
  var t = /^\n* /;
  return t.test(e5);
}
var STYLE_PLAIN$1 = 1, STYLE_SINGLE$1 = 2, STYLE_LITERAL$1 = 3, STYLE_FOLDED$1 = 4, STYLE_DOUBLE$1 = 5;
function chooseScalarStyle$1(e5, t, n, r, s) {
  var o, a, l, c = false, u = false, p = r !== -1, f = -1, h = isPlainSafeFirst$1(e5.charCodeAt(0)) && !isWhitespace$1(e5.charCodeAt(e5.length - 1));
  if (t) for (o = 0; o < e5.length; o++) {
    if (a = e5.charCodeAt(o), !isPrintable$1(a)) return STYLE_DOUBLE$1;
    l = o > 0 ? e5.charCodeAt(o - 1) : null, h = h && isPlainSafe$1(a, l);
  }
  else {
    for (o = 0; o < e5.length; o++) {
      if (a = e5.charCodeAt(o), a === CHAR_LINE_FEED$1) c = true, p && (u = u || o - f - 1 > r && e5[f + 1] !== " ", f = o);
      else if (!isPrintable$1(a)) return STYLE_DOUBLE$1;
      l = o > 0 ? e5.charCodeAt(o - 1) : null, h = h && isPlainSafe$1(a, l);
    }
    u = u || p && o - f - 1 > r && e5[f + 1] !== " ";
  }
  return !c && !u ? h && !s(e5) ? STYLE_PLAIN$1 : STYLE_SINGLE$1 : n > 9 && needIndentIndicator$1(e5) ? STYLE_DOUBLE$1 : u ? STYLE_FOLDED$1 : STYLE_LITERAL$1;
}
function writeScalar$1(e5, t, n, r) {
  e5.dump = (function() {
    if (t.length === 0) return "''";
    if (!e5.noCompatMode && DEPRECATED_BOOLEANS_SYNTAX$1.indexOf(t) !== -1) return "'" + t + "'";
    var s = e5.indent * Math.max(1, n), o = e5.lineWidth === -1 ? -1 : Math.max(Math.min(e5.lineWidth, 40), e5.lineWidth - s), a = r || e5.flowLevel > -1 && n >= e5.flowLevel;
    function l(c) {
      return testImplicitResolving$1(e5, c);
    }
    switch (chooseScalarStyle$1(t, a, e5.indent, o, l)) {
      case STYLE_PLAIN$1:
        return t;
      case STYLE_SINGLE$1:
        return "'" + t.replace(/'/g, "''") + "'";
      case STYLE_LITERAL$1:
        return "|" + blockHeader$1(t, e5.indent) + dropEndingNewline$1(indentString$1(t, s));
      case STYLE_FOLDED$1:
        return ">" + blockHeader$1(t, e5.indent) + dropEndingNewline$1(indentString$1(foldString$1(t, o), s));
      case STYLE_DOUBLE$1:
        return '"' + escapeString$1(t) + '"';
      default:
        throw new YAMLException$2("impossible error: invalid scalar style");
    }
  })();
}
function blockHeader$1(e5, t) {
  var n = needIndentIndicator$1(e5) ? String(t) : "", r = e5[e5.length - 1] === `
`, s = r && (e5[e5.length - 2] === `
` || e5 === `
`), o = s ? "+" : r ? "" : "-";
  return n + o + `
`;
}
function dropEndingNewline$1(e5) {
  return e5[e5.length - 1] === `
` ? e5.slice(0, -1) : e5;
}
function foldString$1(e5, t) {
  for (var n = /(\n+)([^\n]*)/g, r = (function() {
    var u = e5.indexOf(`
`);
    return u = u !== -1 ? u : e5.length, n.lastIndex = u, foldLine$1(e5.slice(0, u), t);
  })(), s = e5[0] === `
` || e5[0] === " ", o, a; a = n.exec(e5); ) {
    var l = a[1], c = a[2];
    o = c[0] === " ", r += l + (!s && !o && c !== "" ? `
` : "") + foldLine$1(c, t), s = o;
  }
  return r;
}
function foldLine$1(e5, t) {
  if (e5 === "" || e5[0] === " ") return e5;
  for (var n = / [^ ]/g, r, s = 0, o, a = 0, l = 0, c = ""; r = n.exec(e5); ) l = r.index, l - s > t && (o = a > s ? a : l, c += `
` + e5.slice(s, o), s = o + 1), a = l;
  return c += `
`, e5.length - s > t && a > s ? c += e5.slice(s, a) + `
` + e5.slice(a + 1) : c += e5.slice(s), c.slice(1);
}
function escapeString$1(e5) {
  for (var t = "", n, r, s, o = 0; o < e5.length; o++) {
    if (n = e5.charCodeAt(o), n >= 55296 && n <= 56319 && (r = e5.charCodeAt(o + 1), r >= 56320 && r <= 57343)) {
      t += encodeHex$1((n - 55296) * 1024 + r - 56320 + 65536), o++;
      continue;
    }
    s = ESCAPE_SEQUENCES$1[n], t += !s && isPrintable$1(n) ? e5[o] : s || encodeHex$1(n);
  }
  return t;
}
function writeFlowSequence$1(e5, t, n) {
  var r = "", s = e5.tag, o, a;
  for (o = 0, a = n.length; o < a; o += 1) writeNode$1(e5, t, n[o], false, false) && (o !== 0 && (r += "," + (e5.condenseFlow ? "" : " ")), r += e5.dump);
  e5.tag = s, e5.dump = "[" + r + "]";
}
function writeBlockSequence$1(e5, t, n, r) {
  var s = "", o = e5.tag, a, l;
  for (a = 0, l = n.length; a < l; a += 1) writeNode$1(e5, t + 1, n[a], true, true) && ((!r || a !== 0) && (s += generateNextLine$1(e5, t)), e5.dump && CHAR_LINE_FEED$1 === e5.dump.charCodeAt(0) ? s += "-" : s += "- ", s += e5.dump);
  e5.tag = o, e5.dump = s || "[]";
}
function writeFlowMapping$1(e5, t, n) {
  var r = "", s = e5.tag, o = Object.keys(n), a, l, c, u, p;
  for (a = 0, l = o.length; a < l; a += 1) p = "", a !== 0 && (p += ", "), e5.condenseFlow && (p += '"'), c = o[a], u = n[c], writeNode$1(e5, t, c, false, false) && (e5.dump.length > 1024 && (p += "? "), p += e5.dump + (e5.condenseFlow ? '"' : "") + ":" + (e5.condenseFlow ? "" : " "), writeNode$1(e5, t, u, false, false) && (p += e5.dump, r += p));
  e5.tag = s, e5.dump = "{" + r + "}";
}
function writeBlockMapping$1(e5, t, n, r) {
  var s = "", o = e5.tag, a = Object.keys(n), l, c, u, p, f, h;
  if (e5.sortKeys === true) a.sort();
  else if (typeof e5.sortKeys == "function") a.sort(e5.sortKeys);
  else if (e5.sortKeys) throw new YAMLException$2("sortKeys must be a boolean or a function");
  for (l = 0, c = a.length; l < c; l += 1) h = "", (!r || l !== 0) && (h += generateNextLine$1(e5, t)), u = a[l], p = n[u], writeNode$1(e5, t + 1, u, true, true, true) && (f = e5.tag !== null && e5.tag !== "?" || e5.dump && e5.dump.length > 1024, f && (e5.dump && CHAR_LINE_FEED$1 === e5.dump.charCodeAt(0) ? h += "?" : h += "? "), h += e5.dump, f && (h += generateNextLine$1(e5, t)), writeNode$1(e5, t + 1, p, true, f) && (e5.dump && CHAR_LINE_FEED$1 === e5.dump.charCodeAt(0) ? h += ":" : h += ": ", h += e5.dump, s += h));
  e5.tag = o, e5.dump = s || "{}";
}
function detectType$1(e5, t, n) {
  var r, s, o, a, l, c;
  for (s = n ? e5.explicitTypes : e5.implicitTypes, o = 0, a = s.length; o < a; o += 1) if (l = s[o], (l.instanceOf || l.predicate) && (!l.instanceOf || typeof t == "object" && t instanceof l.instanceOf) && (!l.predicate || l.predicate(t))) {
    if (e5.tag = n ? l.tag : "?", l.represent) {
      if (c = e5.styleMap[l.tag] || l.defaultStyle, _toString$3.call(l.represent) === "[object Function]") r = l.represent(t, c);
      else if (_hasOwnProperty$4.call(l.represent, c)) r = l.represent[c](t, c);
      else throw new YAMLException$2("!<" + l.tag + '> tag resolver accepts not "' + c + '" style');
      e5.dump = r;
    }
    return true;
  }
  return false;
}
function writeNode$1(e5, t, n, r, s, o) {
  e5.tag = null, e5.dump = n, detectType$1(e5, n, false) || detectType$1(e5, n, true);
  var a = _toString$3.call(e5.dump);
  r && (r = e5.flowLevel < 0 || e5.flowLevel > t);
  var l = a === "[object Object]" || a === "[object Array]", c, u;
  if (l && (c = e5.duplicates.indexOf(n), u = c !== -1), (e5.tag !== null && e5.tag !== "?" || u || e5.indent !== 2 && t > 0) && (s = false), u && e5.usedDuplicates[c]) e5.dump = "*ref_" + c;
  else {
    if (l && u && !e5.usedDuplicates[c] && (e5.usedDuplicates[c] = true), a === "[object Object]") r && Object.keys(e5.dump).length !== 0 ? (writeBlockMapping$1(e5, t, e5.dump, s), u && (e5.dump = "&ref_" + c + e5.dump)) : (writeFlowMapping$1(e5, t, e5.dump), u && (e5.dump = "&ref_" + c + " " + e5.dump));
    else if (a === "[object Array]") {
      var p = e5.noArrayIndent && t > 0 ? t - 1 : t;
      r && e5.dump.length !== 0 ? (writeBlockSequence$1(e5, p, e5.dump, s), u && (e5.dump = "&ref_" + c + e5.dump)) : (writeFlowSequence$1(e5, p, e5.dump), u && (e5.dump = "&ref_" + c + " " + e5.dump));
    } else if (a === "[object String]") e5.tag !== "?" && writeScalar$1(e5, e5.dump, t, o);
    else {
      if (e5.skipInvalid) return false;
      throw new YAMLException$2("unacceptable kind of an object to dump " + a);
    }
    e5.tag !== null && e5.tag !== "?" && (e5.dump = "!<" + e5.tag + "> " + e5.dump);
  }
  return true;
}
function getDuplicateReferences$1(e5, t) {
  var n = [], r = [], s, o;
  for (inspectNode$1(e5, n, r), s = 0, o = r.length; s < o; s += 1) t.duplicates.push(n[r[s]]);
  t.usedDuplicates = new Array(o);
}
function inspectNode$1(e5, t, n) {
  var r, s, o;
  if (e5 !== null && typeof e5 == "object") if (s = t.indexOf(e5), s !== -1) n.indexOf(s) === -1 && n.push(s);
  else if (t.push(e5), Array.isArray(e5)) for (s = 0, o = e5.length; s < o; s += 1) inspectNode$1(e5[s], t, n);
  else for (r = Object.keys(e5), s = 0, o = r.length; s < o; s += 1) inspectNode$1(e5[r[s]], t, n);
}
function dump$2(e5, t) {
  t = t || {};
  var n = new State$2(t);
  return n.noRefs || getDuplicateReferences$1(e5, n), writeNode$1(n, 0, e5, true, true) ? n.dump + `
` : "";
}
function safeDump$1(e5, t) {
  return dump$2(e5, common$1.extend({ schema: DEFAULT_SAFE_SCHEMA }, t));
}
dumper$2.dump = dump$2;
dumper$2.safeDump = safeDump$1;
var loader$1 = loader$2, dumper$1 = dumper$2;
function deprecated(e5) {
  return function() {
    throw new Error("Function " + e5 + " is deprecated and cannot be used.");
  };
}
jsYaml$2.Type = type$1;
jsYaml$2.Schema = schema$1;
jsYaml$2.FAILSAFE_SCHEMA = failsafe$1;
jsYaml$2.JSON_SCHEMA = json$1;
jsYaml$2.CORE_SCHEMA = core$1;
jsYaml$2.DEFAULT_SAFE_SCHEMA = default_safe;
jsYaml$2.DEFAULT_FULL_SCHEMA = default_full;
jsYaml$2.load = loader$1.load;
jsYaml$2.loadAll = loader$1.loadAll;
jsYaml$2.safeLoad = loader$1.safeLoad;
jsYaml$2.safeLoadAll = loader$1.safeLoadAll;
jsYaml$2.dump = dumper$1.dump;
jsYaml$2.safeDump = dumper$1.safeDump;
jsYaml$2.YAMLException = exception$1;
jsYaml$2.MINIMAL_SCHEMA = failsafe$1;
jsYaml$2.SAFE_SCHEMA = default_safe;
jsYaml$2.DEFAULT_SCHEMA = default_full;
jsYaml$2.scan = deprecated("scan");
jsYaml$2.parse = deprecated("parse");
jsYaml$2.compose = deprecated("compose");
jsYaml$2.addConstructor = deprecated("addConstructor");
var yaml = jsYaml$2, jsYaml$1 = yaml;
(function(module, exports) {
  const yaml = jsYaml$1, engines = module.exports;
  engines.yaml = { parse: yaml.safeLoad.bind(yaml), stringify: yaml.safeDump.bind(yaml) }, engines.json = { parse: JSON.parse.bind(JSON), stringify: function(e5, t) {
    const n = Object.assign({ replacer: null, space: 2 }, t);
    return JSON.stringify(e5, n.replacer, n.space);
  } }, engines.javascript = { parse: function parse(str, options, wrap) {
    try {
      return wrap !== false && (str = `(function() {
return ` + str.trim() + `;
}());`), eval(str) || {};
    } catch (e5) {
      if (wrap !== false && /(unexpected|identifier)/i.test(e5.message)) return parse(str, options, false);
      throw new SyntaxError(e5);
    }
  }, stringify: function() {
    throw new Error("stringifying JavaScript is not supported");
  } };
})(engines$2);
var enginesExports = engines$2.exports, utils$3 = {};
/*!
* strip-bom-string <https://github.com/jonschlinkert/strip-bom-string>
*
* Copyright (c) 2015, 2017, Jon Schlinkert.
* Released under the MIT License.
*/
var stripBomString = function(e5) {
  return typeof e5 == "string" && e5.charAt(0) === "\uFEFF" ? e5.slice(1) : e5;
};
(function(e5) {
  const t = stripBomString, n = kindOf;
  e5.define = function(r, s, o) {
    Reflect.defineProperty(r, s, { enumerable: false, configurable: true, writable: true, value: o });
  }, e5.isBuffer = function(r) {
    return n(r) === "buffer";
  }, e5.isObject = function(r) {
    return n(r) === "object";
  }, e5.toBuffer = function(r) {
    return typeof r == "string" ? Buffer.from(r) : r;
  }, e5.toString = function(r) {
    if (e5.isBuffer(r)) return t(String(r));
    if (typeof r != "string") throw new TypeError("expected input to be a string or buffer");
    return t(r);
  }, e5.arrayify = function(r) {
    return r ? Array.isArray(r) ? r : [r] : [];
  }, e5.startsWith = function(r, s, o) {
    return typeof o != "number" && (o = s.length), r.slice(0, o) === s;
  };
})(utils$3);
const engines$1 = enginesExports, utils$2 = utils$3;
var defaults$4 = function(e5) {
  const t = Object.assign({}, e5);
  return t.delimiters = utils$2.arrayify(t.delims || t.delimiters || "---"), t.delimiters.length === 1 && t.delimiters.push(t.delimiters[0]), t.language = (t.language || t.lang || "yaml").toLowerCase(), t.engines = Object.assign({}, engines$1, t.parsers, t.engines), t;
}, engine = function(e5, t) {
  let n = t.engines[e5] || t.engines[aliase(e5)];
  if (typeof n > "u") throw new Error('gray-matter engine "' + e5 + '" is not registered');
  return typeof n == "function" && (n = { parse: n }), n;
};
function aliase(e5) {
  switch (e5.toLowerCase()) {
    case "js":
    case "javascript":
      return "javascript";
    case "coffee":
    case "coffeescript":
    case "cson":
      return "coffee";
    case "yaml":
    case "yml":
      return "yaml";
    default:
      return e5;
  }
}
const typeOf$1 = kindOf, getEngine$1 = engine, defaults$3 = defaults$4;
var stringify$2 = function(e5, t, n) {
  if (t == null && n == null) switch (typeOf$1(e5)) {
    case "object":
      t = e5.data, n = {};
      break;
    case "string":
      return e5;
    default:
      throw new TypeError("expected file to be a string or object");
  }
  const r = e5.content, s = defaults$3(n);
  if (t == null) {
    if (!s.data) return e5;
    t = s.data;
  }
  const o = e5.language || s.language, a = getEngine$1(o, s);
  if (typeof a.stringify != "function") throw new TypeError('expected "' + o + '.stringify" to be a function');
  t = Object.assign({}, e5.data, t);
  const l = s.delimiters[0], c = s.delimiters[1], u = a.stringify(t, n).trim();
  let p = "";
  return u !== "{}" && (p = newline(l) + newline(u) + newline(c)), typeof e5.excerpt == "string" && e5.excerpt !== "" && r.indexOf(e5.excerpt.trim()) === -1 && (p += newline(e5.excerpt) + newline(c)), p + newline(r);
};
function newline(e5) {
  return e5.slice(-1) !== `
` ? e5 + `
` : e5;
}
const defaults$2 = defaults$4;
var excerpt$1 = function(e5, t) {
  const n = defaults$2(t);
  if (e5.data == null && (e5.data = {}), typeof n.excerpt == "function") return n.excerpt(e5, n);
  const r = e5.data.excerpt_separator || n.excerpt_separator;
  if (r == null && (n.excerpt === false || n.excerpt == null)) return e5;
  const s = typeof n.excerpt == "string" ? n.excerpt : r || n.delimiters[0], o = e5.content.indexOf(s);
  return o !== -1 && (e5.excerpt = e5.content.slice(0, o)), e5;
};
const typeOf = kindOf, stringify$1 = stringify$2, utils$1 = utils$3;
var toFile$1 = function(e5) {
  return typeOf(e5) !== "object" && (e5 = { content: e5 }), typeOf(e5.data) !== "object" && (e5.data = {}), e5.contents && e5.content == null && (e5.content = e5.contents), utils$1.define(e5, "orig", utils$1.toBuffer(e5.content)), utils$1.define(e5, "language", e5.language || ""), utils$1.define(e5, "matter", e5.matter || ""), utils$1.define(e5, "stringify", function(t, n) {
    return n && n.language && (e5.language = n.language), stringify$1(e5, t, n);
  }), e5.content = utils$1.toString(e5.content), e5.isEmpty = false, e5.excerpt = "", e5;
};
const getEngine = engine, defaults$1 = defaults$4;
var parse$1 = function(e5, t, n) {
  const r = defaults$1(n), s = getEngine(e5, r);
  if (typeof s.parse != "function") throw new TypeError('expected "' + e5 + '.parse" to be a function');
  return s.parse(t, r);
};
const fs = require$$0$2, sections = sectionMatter, defaults = defaults$4, stringify = stringify$2, excerpt = excerpt$1, engines = enginesExports, toFile = toFile$1, parse = parse$1, utils = utils$3;
function matter(e5, t) {
  if (e5 === "") return { data: {}, content: e5, excerpt: "", orig: e5 };
  let n = toFile(e5);
  const r = matter.cache[n.content];
  if (!t) {
    if (r) return n = Object.assign({}, r), n.orig = r.orig, n;
    matter.cache[n.content] = n;
  }
  return parseMatter(n, t);
}
function parseMatter(e5, t) {
  const n = defaults(t), r = n.delimiters[0], s = `
` + n.delimiters[1];
  let o = e5.content;
  n.language && (e5.language = n.language);
  const a = r.length;
  if (!utils.startsWith(o, r, a)) return excerpt(e5, n), e5;
  if (o.charAt(a) === r.slice(-1)) return e5;
  o = o.slice(a);
  const l = o.length, c = matter.language(o, n);
  c.name && (e5.language = c.name, o = o.slice(c.raw.length));
  let u = o.indexOf(s);
  return u === -1 && (u = l), e5.matter = o.slice(0, u), e5.matter.replace(/^\s*#[^\n]+/gm, "").trim() === "" ? (e5.isEmpty = true, e5.empty = e5.content, e5.data = {}) : e5.data = parse(e5.language, e5.matter, n), u === l ? e5.content = "" : (e5.content = o.slice(u + s.length), e5.content[0] === "\r" && (e5.content = e5.content.slice(1)), e5.content[0] === `
` && (e5.content = e5.content.slice(1))), excerpt(e5, n), (n.sections === true || typeof n.section == "function") && sections(e5, n.section), e5;
}
matter.engines = engines;
matter.stringify = function(e5, t, n) {
  return typeof e5 == "string" && (e5 = matter(e5, n)), stringify(e5, t, n);
};
matter.read = function(e5, t) {
  const n = fs.readFileSync(e5, "utf8"), r = matter(n, t);
  return r.path = e5, r;
};
matter.test = function(e5, t) {
  return utils.startsWith(e5, defaults(t).delimiters[0]);
};
matter.language = function(e5, t) {
  const r = defaults(t).delimiters[0];
  matter.test(e5) && (e5 = e5.slice(r.length));
  const s = e5.slice(0, e5.search(/\r?\n/));
  return { raw: s, name: s ? s.trim() : "" };
};
matter.cache = {};
matter.clearCache = function() {
  matter.cache = {};
};
var grayMatter = matter;
const matter$1 = Hm(grayMatter), markdownSerializer = { format: "markdown", async serializeDocumentFileContents(e5, t) {
  const n = (e5 == null ? void 0 : e5.body) || "", r = { ...e5 };
  return delete r.body, matter$1.stringify(n, r);
}, async deserializeDocumentFileContents(e5, t) {
  const n = matter$1(e5);
  return { ...n.data, body: n.content };
} }, rawSerializer = { format: "raw", async serializeDocumentFileContents(e5, t) {
  var _a4;
  return "" + ((_a4 = e5.body) != null ? _a4 : "");
}, async deserializeDocumentFileContents(e5, t) {
  return { body: e5 };
} };
/*! js-yaml 4.1.1 https://github.com/nodeca/js-yaml @license MIT */
function isNothing(e5) {
  return typeof e5 > "u" || e5 === null;
}
function isObject(e5) {
  return typeof e5 == "object" && e5 !== null;
}
function toArray(e5) {
  return Array.isArray(e5) ? e5 : isNothing(e5) ? [] : [e5];
}
function extend(e5, t) {
  var n, r, s, o;
  if (t) for (o = Object.keys(t), n = 0, r = o.length; n < r; n += 1) s = o[n], e5[s] = t[s];
  return e5;
}
function repeat(e5, t) {
  var n = "", r;
  for (r = 0; r < t; r += 1) n += e5;
  return n;
}
function isNegativeZero(e5) {
  return e5 === 0 && Number.NEGATIVE_INFINITY === 1 / e5;
}
var isNothing_1 = isNothing, isObject_1 = isObject, toArray_1 = toArray, repeat_1 = repeat, isNegativeZero_1 = isNegativeZero, extend_1 = extend, common = { isNothing: isNothing_1, isObject: isObject_1, toArray: toArray_1, repeat: repeat_1, isNegativeZero: isNegativeZero_1, extend: extend_1 };
function formatError(e5, t) {
  var n = "", r = e5.reason || "(unknown reason)";
  return e5.mark ? (e5.mark.name && (n += 'in "' + e5.mark.name + '" '), n += "(" + (e5.mark.line + 1) + ":" + (e5.mark.column + 1) + ")", !t && e5.mark.snippet && (n += `

` + e5.mark.snippet), r + " " + n) : r;
}
function YAMLException$1(e5, t) {
  Error.call(this), this.name = "YAMLException", this.reason = e5, this.mark = t, this.message = formatError(this, false), Error.captureStackTrace ? Error.captureStackTrace(this, this.constructor) : this.stack = new Error().stack || "";
}
YAMLException$1.prototype = Object.create(Error.prototype);
YAMLException$1.prototype.constructor = YAMLException$1;
YAMLException$1.prototype.toString = function e(t) {
  return this.name + ": " + formatError(this, t);
};
var exception = YAMLException$1;
function getLine(e5, t, n, r, s) {
  var o = "", a = "", l = Math.floor(s / 2) - 1;
  return r - t > l && (o = " ... ", t = r - l + o.length), n - r > l && (a = " ...", n = r + l - a.length), { str: o + e5.slice(t, n).replace(/\t/g, "\u2192") + a, pos: r - t + o.length };
}
function padStart(e5, t) {
  return common.repeat(" ", t - e5.length) + e5;
}
function makeSnippet(e5, t) {
  if (t = Object.create(t || null), !e5.buffer) return null;
  t.maxLength || (t.maxLength = 79), typeof t.indent != "number" && (t.indent = 1), typeof t.linesBefore != "number" && (t.linesBefore = 3), typeof t.linesAfter != "number" && (t.linesAfter = 2);
  for (var n = /\r?\n|\r|\0/g, r = [0], s = [], o, a = -1; o = n.exec(e5.buffer); ) s.push(o.index), r.push(o.index + o[0].length), e5.position <= o.index && a < 0 && (a = r.length - 2);
  a < 0 && (a = r.length - 1);
  var l = "", c, u, p = Math.min(e5.line + t.linesAfter, s.length).toString().length, f = t.maxLength - (t.indent + p + 3);
  for (c = 1; c <= t.linesBefore && !(a - c < 0); c++) u = getLine(e5.buffer, r[a - c], s[a - c], e5.position - (r[a] - r[a - c]), f), l = common.repeat(" ", t.indent) + padStart((e5.line - c + 1).toString(), p) + " | " + u.str + `
` + l;
  for (u = getLine(e5.buffer, r[a], s[a], e5.position, f), l += common.repeat(" ", t.indent) + padStart((e5.line + 1).toString(), p) + " | " + u.str + `
`, l += common.repeat("-", t.indent + p + 3 + u.pos) + `^
`, c = 1; c <= t.linesAfter && !(a + c >= s.length); c++) u = getLine(e5.buffer, r[a + c], s[a + c], e5.position - (r[a] - r[a + c]), f), l += common.repeat(" ", t.indent) + padStart((e5.line + c + 1).toString(), p) + " | " + u.str + `
`;
  return l.replace(/\n$/, "");
}
var snippet = makeSnippet, TYPE_CONSTRUCTOR_OPTIONS = ["kind", "multi", "resolve", "construct", "instanceOf", "predicate", "represent", "representName", "defaultStyle", "styleAliases"], YAML_NODE_KINDS = ["scalar", "sequence", "mapping"];
function compileStyleAliases(e5) {
  var t = {};
  return e5 !== null && Object.keys(e5).forEach(function(n) {
    e5[n].forEach(function(r) {
      t[String(r)] = n;
    });
  }), t;
}
function Type$1(e5, t) {
  if (t = t || {}, Object.keys(t).forEach(function(n) {
    if (TYPE_CONSTRUCTOR_OPTIONS.indexOf(n) === -1) throw new exception('Unknown option "' + n + '" is met in definition of "' + e5 + '" YAML type.');
  }), this.options = t, this.tag = e5, this.kind = t.kind || null, this.resolve = t.resolve || function() {
    return true;
  }, this.construct = t.construct || function(n) {
    return n;
  }, this.instanceOf = t.instanceOf || null, this.predicate = t.predicate || null, this.represent = t.represent || null, this.representName = t.representName || null, this.defaultStyle = t.defaultStyle || null, this.multi = t.multi || false, this.styleAliases = compileStyleAliases(t.styleAliases || null), YAML_NODE_KINDS.indexOf(this.kind) === -1) throw new exception('Unknown kind "' + this.kind + '" is specified for "' + e5 + '" YAML type.');
}
var type = Type$1;
function compileList(e5, t) {
  var n = [];
  return e5[t].forEach(function(r) {
    var s = n.length;
    n.forEach(function(o, a) {
      o.tag === r.tag && o.kind === r.kind && o.multi === r.multi && (s = a);
    }), n[s] = r;
  }), n;
}
function compileMap() {
  var e5 = { scalar: {}, sequence: {}, mapping: {}, fallback: {}, multi: { scalar: [], sequence: [], mapping: [], fallback: [] } }, t, n;
  function r(s) {
    s.multi ? (e5.multi[s.kind].push(s), e5.multi.fallback.push(s)) : e5[s.kind][s.tag] = e5.fallback[s.tag] = s;
  }
  for (t = 0, n = arguments.length; t < n; t += 1) arguments[t].forEach(r);
  return e5;
}
function Schema$1(e5) {
  return this.extend(e5);
}
Schema$1.prototype.extend = function e2(t) {
  var n = [], r = [];
  if (t instanceof type) r.push(t);
  else if (Array.isArray(t)) r = r.concat(t);
  else if (t && (Array.isArray(t.implicit) || Array.isArray(t.explicit))) t.implicit && (n = n.concat(t.implicit)), t.explicit && (r = r.concat(t.explicit));
  else throw new exception("Schema.extend argument should be a Type, [ Type ], or a schema definition ({ implicit: [...], explicit: [...] })");
  n.forEach(function(o) {
    if (!(o instanceof type)) throw new exception("Specified list of YAML types (or a single Type object) contains a non-Type object.");
    if (o.loadKind && o.loadKind !== "scalar") throw new exception("There is a non-scalar type in the implicit list of a schema. Implicit resolving of such types is not supported.");
    if (o.multi) throw new exception("There is a multi type in the implicit list of a schema. Multi tags can only be listed as explicit.");
  }), r.forEach(function(o) {
    if (!(o instanceof type)) throw new exception("Specified list of YAML types (or a single Type object) contains a non-Type object.");
  });
  var s = Object.create(Schema$1.prototype);
  return s.implicit = (this.implicit || []).concat(n), s.explicit = (this.explicit || []).concat(r), s.compiledImplicit = compileList(s, "implicit"), s.compiledExplicit = compileList(s, "explicit"), s.compiledTypeMap = compileMap(s.compiledImplicit, s.compiledExplicit), s;
};
var schema = Schema$1, str = new type("tag:yaml.org,2002:str", { kind: "scalar", construct: function(e5) {
  return e5 !== null ? e5 : "";
} }), seq = new type("tag:yaml.org,2002:seq", { kind: "sequence", construct: function(e5) {
  return e5 !== null ? e5 : [];
} }), map = new type("tag:yaml.org,2002:map", { kind: "mapping", construct: function(e5) {
  return e5 !== null ? e5 : {};
} }), failsafe = new schema({ explicit: [str, seq, map] });
function resolveYamlNull(e5) {
  if (e5 === null) return true;
  var t = e5.length;
  return t === 1 && e5 === "~" || t === 4 && (e5 === "null" || e5 === "Null" || e5 === "NULL");
}
function constructYamlNull() {
  return null;
}
function isNull(e5) {
  return e5 === null;
}
var _null = new type("tag:yaml.org,2002:null", { kind: "scalar", resolve: resolveYamlNull, construct: constructYamlNull, predicate: isNull, represent: { canonical: function() {
  return "~";
}, lowercase: function() {
  return "null";
}, uppercase: function() {
  return "NULL";
}, camelcase: function() {
  return "Null";
}, empty: function() {
  return "";
} }, defaultStyle: "lowercase" });
function resolveYamlBoolean(e5) {
  if (e5 === null) return false;
  var t = e5.length;
  return t === 4 && (e5 === "true" || e5 === "True" || e5 === "TRUE") || t === 5 && (e5 === "false" || e5 === "False" || e5 === "FALSE");
}
function constructYamlBoolean(e5) {
  return e5 === "true" || e5 === "True" || e5 === "TRUE";
}
function isBoolean(e5) {
  return Object.prototype.toString.call(e5) === "[object Boolean]";
}
var bool = new type("tag:yaml.org,2002:bool", { kind: "scalar", resolve: resolveYamlBoolean, construct: constructYamlBoolean, predicate: isBoolean, represent: { lowercase: function(e5) {
  return e5 ? "true" : "false";
}, uppercase: function(e5) {
  return e5 ? "TRUE" : "FALSE";
}, camelcase: function(e5) {
  return e5 ? "True" : "False";
} }, defaultStyle: "lowercase" });
function isHexCode(e5) {
  return 48 <= e5 && e5 <= 57 || 65 <= e5 && e5 <= 70 || 97 <= e5 && e5 <= 102;
}
function isOctCode(e5) {
  return 48 <= e5 && e5 <= 55;
}
function isDecCode(e5) {
  return 48 <= e5 && e5 <= 57;
}
function resolveYamlInteger(e5) {
  if (e5 === null) return false;
  var t = e5.length, n = 0, r = false, s;
  if (!t) return false;
  if (s = e5[n], (s === "-" || s === "+") && (s = e5[++n]), s === "0") {
    if (n + 1 === t) return true;
    if (s = e5[++n], s === "b") {
      for (n++; n < t; n++) if (s = e5[n], s !== "_") {
        if (s !== "0" && s !== "1") return false;
        r = true;
      }
      return r && s !== "_";
    }
    if (s === "x") {
      for (n++; n < t; n++) if (s = e5[n], s !== "_") {
        if (!isHexCode(e5.charCodeAt(n))) return false;
        r = true;
      }
      return r && s !== "_";
    }
    if (s === "o") {
      for (n++; n < t; n++) if (s = e5[n], s !== "_") {
        if (!isOctCode(e5.charCodeAt(n))) return false;
        r = true;
      }
      return r && s !== "_";
    }
  }
  if (s === "_") return false;
  for (; n < t; n++) if (s = e5[n], s !== "_") {
    if (!isDecCode(e5.charCodeAt(n))) return false;
    r = true;
  }
  return !(!r || s === "_");
}
function constructYamlInteger(e5) {
  var t = e5, n = 1, r;
  if (t.indexOf("_") !== -1 && (t = t.replace(/_/g, "")), r = t[0], (r === "-" || r === "+") && (r === "-" && (n = -1), t = t.slice(1), r = t[0]), t === "0") return 0;
  if (r === "0") {
    if (t[1] === "b") return n * parseInt(t.slice(2), 2);
    if (t[1] === "x") return n * parseInt(t.slice(2), 16);
    if (t[1] === "o") return n * parseInt(t.slice(2), 8);
  }
  return n * parseInt(t, 10);
}
function isInteger(e5) {
  return Object.prototype.toString.call(e5) === "[object Number]" && e5 % 1 === 0 && !common.isNegativeZero(e5);
}
var int = new type("tag:yaml.org,2002:int", { kind: "scalar", resolve: resolveYamlInteger, construct: constructYamlInteger, predicate: isInteger, represent: { binary: function(e5) {
  return e5 >= 0 ? "0b" + e5.toString(2) : "-0b" + e5.toString(2).slice(1);
}, octal: function(e5) {
  return e5 >= 0 ? "0o" + e5.toString(8) : "-0o" + e5.toString(8).slice(1);
}, decimal: function(e5) {
  return e5.toString(10);
}, hexadecimal: function(e5) {
  return e5 >= 0 ? "0x" + e5.toString(16).toUpperCase() : "-0x" + e5.toString(16).toUpperCase().slice(1);
} }, defaultStyle: "decimal", styleAliases: { binary: [2, "bin"], octal: [8, "oct"], decimal: [10, "dec"], hexadecimal: [16, "hex"] } }), YAML_FLOAT_PATTERN = new RegExp("^(?:[-+]?(?:[0-9][0-9_]*)(?:\\.[0-9_]*)?(?:[eE][-+]?[0-9]+)?|\\.[0-9_]+(?:[eE][-+]?[0-9]+)?|[-+]?\\.(?:inf|Inf|INF)|\\.(?:nan|NaN|NAN))$");
function resolveYamlFloat(e5) {
  return !(e5 === null || !YAML_FLOAT_PATTERN.test(e5) || e5[e5.length - 1] === "_");
}
function constructYamlFloat(e5) {
  var t, n;
  return t = e5.replace(/_/g, "").toLowerCase(), n = t[0] === "-" ? -1 : 1, "+-".indexOf(t[0]) >= 0 && (t = t.slice(1)), t === ".inf" ? n === 1 ? Number.POSITIVE_INFINITY : Number.NEGATIVE_INFINITY : t === ".nan" ? NaN : n * parseFloat(t, 10);
}
var SCIENTIFIC_WITHOUT_DOT = /^[-+]?[0-9]+e/;
function representYamlFloat(e5, t) {
  var n;
  if (isNaN(e5)) switch (t) {
    case "lowercase":
      return ".nan";
    case "uppercase":
      return ".NAN";
    case "camelcase":
      return ".NaN";
  }
  else if (Number.POSITIVE_INFINITY === e5) switch (t) {
    case "lowercase":
      return ".inf";
    case "uppercase":
      return ".INF";
    case "camelcase":
      return ".Inf";
  }
  else if (Number.NEGATIVE_INFINITY === e5) switch (t) {
    case "lowercase":
      return "-.inf";
    case "uppercase":
      return "-.INF";
    case "camelcase":
      return "-.Inf";
  }
  else if (common.isNegativeZero(e5)) return "-0.0";
  return n = e5.toString(10), SCIENTIFIC_WITHOUT_DOT.test(n) ? n.replace("e", ".e") : n;
}
function isFloat(e5) {
  return Object.prototype.toString.call(e5) === "[object Number]" && (e5 % 1 !== 0 || common.isNegativeZero(e5));
}
var float = new type("tag:yaml.org,2002:float", { kind: "scalar", resolve: resolveYamlFloat, construct: constructYamlFloat, predicate: isFloat, represent: representYamlFloat, defaultStyle: "lowercase" }), json = failsafe.extend({ implicit: [_null, bool, int, float] }), core = json, YAML_DATE_REGEXP = new RegExp("^([0-9][0-9][0-9][0-9])-([0-9][0-9])-([0-9][0-9])$"), YAML_TIMESTAMP_REGEXP = new RegExp("^([0-9][0-9][0-9][0-9])-([0-9][0-9]?)-([0-9][0-9]?)(?:[Tt]|[ \\t]+)([0-9][0-9]?):([0-9][0-9]):([0-9][0-9])(?:\\.([0-9]*))?(?:[ \\t]*(Z|([-+])([0-9][0-9]?)(?::([0-9][0-9]))?))?$");
function resolveYamlTimestamp(e5) {
  return e5 === null ? false : YAML_DATE_REGEXP.exec(e5) !== null || YAML_TIMESTAMP_REGEXP.exec(e5) !== null;
}
function constructYamlTimestamp(e5) {
  var t, n, r, s, o, a, l, c = 0, u = null, p, f, h;
  if (t = YAML_DATE_REGEXP.exec(e5), t === null && (t = YAML_TIMESTAMP_REGEXP.exec(e5)), t === null) throw new Error("Date resolve error");
  if (n = +t[1], r = +t[2] - 1, s = +t[3], !t[4]) return new Date(Date.UTC(n, r, s));
  if (o = +t[4], a = +t[5], l = +t[6], t[7]) {
    for (c = t[7].slice(0, 3); c.length < 3; ) c += "0";
    c = +c;
  }
  return t[9] && (p = +t[10], f = +(t[11] || 0), u = (p * 60 + f) * 6e4, t[9] === "-" && (u = -u)), h = new Date(Date.UTC(n, r, s, o, a, l, c)), u && h.setTime(h.getTime() - u), h;
}
function representYamlTimestamp(e5) {
  return e5.toISOString();
}
var timestamp = new type("tag:yaml.org,2002:timestamp", { kind: "scalar", resolve: resolveYamlTimestamp, construct: constructYamlTimestamp, instanceOf: Date, represent: representYamlTimestamp });
function resolveYamlMerge(e5) {
  return e5 === "<<" || e5 === null;
}
var merge = new type("tag:yaml.org,2002:merge", { kind: "scalar", resolve: resolveYamlMerge }), BASE64_MAP = `ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=
\r`;
function resolveYamlBinary(e5) {
  if (e5 === null) return false;
  var t, n, r = 0, s = e5.length, o = BASE64_MAP;
  for (n = 0; n < s; n++) if (t = o.indexOf(e5.charAt(n)), !(t > 64)) {
    if (t < 0) return false;
    r += 6;
  }
  return r % 8 === 0;
}
function constructYamlBinary(e5) {
  var t, n, r = e5.replace(/[\r\n=]/g, ""), s = r.length, o = BASE64_MAP, a = 0, l = [];
  for (t = 0; t < s; t++) t % 4 === 0 && t && (l.push(a >> 16 & 255), l.push(a >> 8 & 255), l.push(a & 255)), a = a << 6 | o.indexOf(r.charAt(t));
  return n = s % 4 * 6, n === 0 ? (l.push(a >> 16 & 255), l.push(a >> 8 & 255), l.push(a & 255)) : n === 18 ? (l.push(a >> 10 & 255), l.push(a >> 2 & 255)) : n === 12 && l.push(a >> 4 & 255), new Uint8Array(l);
}
function representYamlBinary(e5) {
  var t = "", n = 0, r, s, o = e5.length, a = BASE64_MAP;
  for (r = 0; r < o; r++) r % 3 === 0 && r && (t += a[n >> 18 & 63], t += a[n >> 12 & 63], t += a[n >> 6 & 63], t += a[n & 63]), n = (n << 8) + e5[r];
  return s = o % 3, s === 0 ? (t += a[n >> 18 & 63], t += a[n >> 12 & 63], t += a[n >> 6 & 63], t += a[n & 63]) : s === 2 ? (t += a[n >> 10 & 63], t += a[n >> 4 & 63], t += a[n << 2 & 63], t += a[64]) : s === 1 && (t += a[n >> 2 & 63], t += a[n << 4 & 63], t += a[64], t += a[64]), t;
}
function isBinary(e5) {
  return Object.prototype.toString.call(e5) === "[object Uint8Array]";
}
var binary = new type("tag:yaml.org,2002:binary", { kind: "scalar", resolve: resolveYamlBinary, construct: constructYamlBinary, predicate: isBinary, represent: representYamlBinary }), _hasOwnProperty$3 = Object.prototype.hasOwnProperty, _toString$2 = Object.prototype.toString;
function resolveYamlOmap(e5) {
  if (e5 === null) return true;
  var t = [], n, r, s, o, a, l = e5;
  for (n = 0, r = l.length; n < r; n += 1) {
    if (s = l[n], a = false, _toString$2.call(s) !== "[object Object]") return false;
    for (o in s) if (_hasOwnProperty$3.call(s, o)) if (!a) a = true;
    else return false;
    if (!a) return false;
    if (t.indexOf(o) === -1) t.push(o);
    else return false;
  }
  return true;
}
function constructYamlOmap(e5) {
  return e5 !== null ? e5 : [];
}
var omap = new type("tag:yaml.org,2002:omap", { kind: "sequence", resolve: resolveYamlOmap, construct: constructYamlOmap }), _toString$1 = Object.prototype.toString;
function resolveYamlPairs(e5) {
  if (e5 === null) return true;
  var t, n, r, s, o, a = e5;
  for (o = new Array(a.length), t = 0, n = a.length; t < n; t += 1) {
    if (r = a[t], _toString$1.call(r) !== "[object Object]" || (s = Object.keys(r), s.length !== 1)) return false;
    o[t] = [s[0], r[s[0]]];
  }
  return true;
}
function constructYamlPairs(e5) {
  if (e5 === null) return [];
  var t, n, r, s, o, a = e5;
  for (o = new Array(a.length), t = 0, n = a.length; t < n; t += 1) r = a[t], s = Object.keys(r), o[t] = [s[0], r[s[0]]];
  return o;
}
var pairs = new type("tag:yaml.org,2002:pairs", { kind: "sequence", resolve: resolveYamlPairs, construct: constructYamlPairs }), _hasOwnProperty$2 = Object.prototype.hasOwnProperty;
function resolveYamlSet(e5) {
  if (e5 === null) return true;
  var t, n = e5;
  for (t in n) if (_hasOwnProperty$2.call(n, t) && n[t] !== null) return false;
  return true;
}
function constructYamlSet(e5) {
  return e5 !== null ? e5 : {};
}
var set = new type("tag:yaml.org,2002:set", { kind: "mapping", resolve: resolveYamlSet, construct: constructYamlSet }), _default = core.extend({ implicit: [timestamp, merge], explicit: [binary, omap, pairs, set] }), _hasOwnProperty$1 = Object.prototype.hasOwnProperty, CONTEXT_FLOW_IN = 1, CONTEXT_FLOW_OUT = 2, CONTEXT_BLOCK_IN = 3, CONTEXT_BLOCK_OUT = 4, CHOMPING_CLIP = 1, CHOMPING_STRIP = 2, CHOMPING_KEEP = 3, PATTERN_NON_PRINTABLE = /[\x00-\x08\x0B\x0C\x0E-\x1F\x7F-\x84\x86-\x9F\uFFFE\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF]/, PATTERN_NON_ASCII_LINE_BREAKS = /[\x85\u2028\u2029]/, PATTERN_FLOW_INDICATORS = /[,\[\]\{\}]/, PATTERN_TAG_HANDLE = /^(?:!|!!|![a-z\-]+!)$/i, PATTERN_TAG_URI = /^(?:!|[^,\[\]\{\}])(?:%[0-9a-f]{2}|[0-9a-z\-#;\/\?:@&=\+\$,_\.!~\*'\(\)\[\]])*$/i;
function _class(e5) {
  return Object.prototype.toString.call(e5);
}
function is_EOL(e5) {
  return e5 === 10 || e5 === 13;
}
function is_WHITE_SPACE(e5) {
  return e5 === 9 || e5 === 32;
}
function is_WS_OR_EOL(e5) {
  return e5 === 9 || e5 === 32 || e5 === 10 || e5 === 13;
}
function is_FLOW_INDICATOR(e5) {
  return e5 === 44 || e5 === 91 || e5 === 93 || e5 === 123 || e5 === 125;
}
function fromHexCode(e5) {
  var t;
  return 48 <= e5 && e5 <= 57 ? e5 - 48 : (t = e5 | 32, 97 <= t && t <= 102 ? t - 97 + 10 : -1);
}
function escapedHexLen(e5) {
  return e5 === 120 ? 2 : e5 === 117 ? 4 : e5 === 85 ? 8 : 0;
}
function fromDecimalCode(e5) {
  return 48 <= e5 && e5 <= 57 ? e5 - 48 : -1;
}
function simpleEscapeSequence(e5) {
  return e5 === 48 ? "\0" : e5 === 97 ? "\x07" : e5 === 98 ? "\b" : e5 === 116 || e5 === 9 ? "	" : e5 === 110 ? `
` : e5 === 118 ? "\v" : e5 === 102 ? "\f" : e5 === 114 ? "\r" : e5 === 101 ? "\x1B" : e5 === 32 ? " " : e5 === 34 ? '"' : e5 === 47 ? "/" : e5 === 92 ? "\\" : e5 === 78 ? "\x85" : e5 === 95 ? "\xA0" : e5 === 76 ? "\u2028" : e5 === 80 ? "\u2029" : "";
}
function charFromCodepoint(e5) {
  return e5 <= 65535 ? String.fromCharCode(e5) : String.fromCharCode((e5 - 65536 >> 10) + 55296, (e5 - 65536 & 1023) + 56320);
}
function setProperty(e5, t, n) {
  t === "__proto__" ? Object.defineProperty(e5, t, { configurable: true, enumerable: true, writable: true, value: n }) : e5[t] = n;
}
var simpleEscapeCheck = new Array(256), simpleEscapeMap = new Array(256);
for (var i = 0; i < 256; i++) simpleEscapeCheck[i] = simpleEscapeSequence(i) ? 1 : 0, simpleEscapeMap[i] = simpleEscapeSequence(i);
function State$1(e5, t) {
  this.input = e5, this.filename = t.filename || null, this.schema = t.schema || _default, this.onWarning = t.onWarning || null, this.legacy = t.legacy || false, this.json = t.json || false, this.listener = t.listener || null, this.implicitTypes = this.schema.compiledImplicit, this.typeMap = this.schema.compiledTypeMap, this.length = e5.length, this.position = 0, this.line = 0, this.lineStart = 0, this.lineIndent = 0, this.firstTabInLine = -1, this.documents = [];
}
function generateError(e5, t) {
  var n = { name: e5.filename, buffer: e5.input.slice(0, -1), position: e5.position, line: e5.line, column: e5.position - e5.lineStart };
  return n.snippet = snippet(n), new exception(t, n);
}
function throwError(e5, t) {
  throw generateError(e5, t);
}
function throwWarning(e5, t) {
  e5.onWarning && e5.onWarning.call(null, generateError(e5, t));
}
var directiveHandlers = { YAML: function e3(t, n, r) {
  var s, o, a;
  t.version !== null && throwError(t, "duplication of %YAML directive"), r.length !== 1 && throwError(t, "YAML directive accepts exactly one argument"), s = /^([0-9]+)\.([0-9]+)$/.exec(r[0]), s === null && throwError(t, "ill-formed argument of the YAML directive"), o = parseInt(s[1], 10), a = parseInt(s[2], 10), o !== 1 && throwError(t, "unacceptable YAML version of the document"), t.version = r[0], t.checkLineBreaks = a < 2, a !== 1 && a !== 2 && throwWarning(t, "unsupported YAML version of the document");
}, TAG: function e4(t, n, r) {
  var s, o;
  r.length !== 2 && throwError(t, "TAG directive accepts exactly two arguments"), s = r[0], o = r[1], PATTERN_TAG_HANDLE.test(s) || throwError(t, "ill-formed tag handle (first argument) of the TAG directive"), _hasOwnProperty$1.call(t.tagMap, s) && throwError(t, 'there is a previously declared suffix for "' + s + '" tag handle'), PATTERN_TAG_URI.test(o) || throwError(t, "ill-formed tag prefix (second argument) of the TAG directive");
  try {
    o = decodeURIComponent(o);
  } catch {
    throwError(t, "tag prefix is malformed: " + o);
  }
  t.tagMap[s] = o;
} };
function captureSegment(e5, t, n, r) {
  var s, o, a, l;
  if (t < n) {
    if (l = e5.input.slice(t, n), r) for (s = 0, o = l.length; s < o; s += 1) a = l.charCodeAt(s), a === 9 || 32 <= a && a <= 1114111 || throwError(e5, "expected valid JSON character");
    else PATTERN_NON_PRINTABLE.test(l) && throwError(e5, "the stream contains non-printable characters");
    e5.result += l;
  }
}
function mergeMappings(e5, t, n, r) {
  var s, o, a, l;
  for (common.isObject(n) || throwError(e5, "cannot merge mappings; the provided source object is unacceptable"), s = Object.keys(n), a = 0, l = s.length; a < l; a += 1) o = s[a], _hasOwnProperty$1.call(t, o) || (setProperty(t, o, n[o]), r[o] = true);
}
function storeMappingPair(e5, t, n, r, s, o, a, l, c) {
  var u, p;
  if (Array.isArray(s)) for (s = Array.prototype.slice.call(s), u = 0, p = s.length; u < p; u += 1) Array.isArray(s[u]) && throwError(e5, "nested arrays are not supported inside keys"), typeof s == "object" && _class(s[u]) === "[object Object]" && (s[u] = "[object Object]");
  if (typeof s == "object" && _class(s) === "[object Object]" && (s = "[object Object]"), s = String(s), t === null && (t = {}), r === "tag:yaml.org,2002:merge") if (Array.isArray(o)) for (u = 0, p = o.length; u < p; u += 1) mergeMappings(e5, t, o[u], n);
  else mergeMappings(e5, t, o, n);
  else !e5.json && !_hasOwnProperty$1.call(n, s) && _hasOwnProperty$1.call(t, s) && (e5.line = a || e5.line, e5.lineStart = l || e5.lineStart, e5.position = c || e5.position, throwError(e5, "duplicated mapping key")), setProperty(t, s, o), delete n[s];
  return t;
}
function readLineBreak(e5) {
  var t;
  t = e5.input.charCodeAt(e5.position), t === 10 ? e5.position++ : t === 13 ? (e5.position++, e5.input.charCodeAt(e5.position) === 10 && e5.position++) : throwError(e5, "a line break is expected"), e5.line += 1, e5.lineStart = e5.position, e5.firstTabInLine = -1;
}
function skipSeparationSpace(e5, t, n) {
  for (var r = 0, s = e5.input.charCodeAt(e5.position); s !== 0; ) {
    for (; is_WHITE_SPACE(s); ) s === 9 && e5.firstTabInLine === -1 && (e5.firstTabInLine = e5.position), s = e5.input.charCodeAt(++e5.position);
    if (t && s === 35) do
      s = e5.input.charCodeAt(++e5.position);
    while (s !== 10 && s !== 13 && s !== 0);
    if (is_EOL(s)) for (readLineBreak(e5), s = e5.input.charCodeAt(e5.position), r++, e5.lineIndent = 0; s === 32; ) e5.lineIndent++, s = e5.input.charCodeAt(++e5.position);
    else break;
  }
  return n !== -1 && r !== 0 && e5.lineIndent < n && throwWarning(e5, "deficient indentation"), r;
}
function testDocumentSeparator(e5) {
  var t = e5.position, n;
  return n = e5.input.charCodeAt(t), !!((n === 45 || n === 46) && n === e5.input.charCodeAt(t + 1) && n === e5.input.charCodeAt(t + 2) && (t += 3, n = e5.input.charCodeAt(t), n === 0 || is_WS_OR_EOL(n)));
}
function writeFoldedLines(e5, t) {
  t === 1 ? e5.result += " " : t > 1 && (e5.result += common.repeat(`
`, t - 1));
}
function readPlainScalar(e5, t, n) {
  var r, s, o, a, l, c, u, p, f = e5.kind, h = e5.result, m;
  if (m = e5.input.charCodeAt(e5.position), is_WS_OR_EOL(m) || is_FLOW_INDICATOR(m) || m === 35 || m === 38 || m === 42 || m === 33 || m === 124 || m === 62 || m === 39 || m === 34 || m === 37 || m === 64 || m === 96 || (m === 63 || m === 45) && (s = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL(s) || n && is_FLOW_INDICATOR(s))) return false;
  for (e5.kind = "scalar", e5.result = "", o = a = e5.position, l = false; m !== 0; ) {
    if (m === 58) {
      if (s = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL(s) || n && is_FLOW_INDICATOR(s)) break;
    } else if (m === 35) {
      if (r = e5.input.charCodeAt(e5.position - 1), is_WS_OR_EOL(r)) break;
    } else {
      if (e5.position === e5.lineStart && testDocumentSeparator(e5) || n && is_FLOW_INDICATOR(m)) break;
      if (is_EOL(m)) if (c = e5.line, u = e5.lineStart, p = e5.lineIndent, skipSeparationSpace(e5, false, -1), e5.lineIndent >= t) {
        l = true, m = e5.input.charCodeAt(e5.position);
        continue;
      } else {
        e5.position = a, e5.line = c, e5.lineStart = u, e5.lineIndent = p;
        break;
      }
    }
    l && (captureSegment(e5, o, a, false), writeFoldedLines(e5, e5.line - c), o = a = e5.position, l = false), is_WHITE_SPACE(m) || (a = e5.position + 1), m = e5.input.charCodeAt(++e5.position);
  }
  return captureSegment(e5, o, a, false), e5.result ? true : (e5.kind = f, e5.result = h, false);
}
function readSingleQuotedScalar(e5, t) {
  var n, r, s;
  if (n = e5.input.charCodeAt(e5.position), n !== 39) return false;
  for (e5.kind = "scalar", e5.result = "", e5.position++, r = s = e5.position; (n = e5.input.charCodeAt(e5.position)) !== 0; ) if (n === 39) if (captureSegment(e5, r, e5.position, true), n = e5.input.charCodeAt(++e5.position), n === 39) r = e5.position, e5.position++, s = e5.position;
  else return true;
  else is_EOL(n) ? (captureSegment(e5, r, s, true), writeFoldedLines(e5, skipSeparationSpace(e5, false, t)), r = s = e5.position) : e5.position === e5.lineStart && testDocumentSeparator(e5) ? throwError(e5, "unexpected end of the document within a single quoted scalar") : (e5.position++, s = e5.position);
  throwError(e5, "unexpected end of the stream within a single quoted scalar");
}
function readDoubleQuotedScalar(e5, t) {
  var n, r, s, o, a, l;
  if (l = e5.input.charCodeAt(e5.position), l !== 34) return false;
  for (e5.kind = "scalar", e5.result = "", e5.position++, n = r = e5.position; (l = e5.input.charCodeAt(e5.position)) !== 0; ) {
    if (l === 34) return captureSegment(e5, n, e5.position, true), e5.position++, true;
    if (l === 92) {
      if (captureSegment(e5, n, e5.position, true), l = e5.input.charCodeAt(++e5.position), is_EOL(l)) skipSeparationSpace(e5, false, t);
      else if (l < 256 && simpleEscapeCheck[l]) e5.result += simpleEscapeMap[l], e5.position++;
      else if ((a = escapedHexLen(l)) > 0) {
        for (s = a, o = 0; s > 0; s--) l = e5.input.charCodeAt(++e5.position), (a = fromHexCode(l)) >= 0 ? o = (o << 4) + a : throwError(e5, "expected hexadecimal character");
        e5.result += charFromCodepoint(o), e5.position++;
      } else throwError(e5, "unknown escape sequence");
      n = r = e5.position;
    } else is_EOL(l) ? (captureSegment(e5, n, r, true), writeFoldedLines(e5, skipSeparationSpace(e5, false, t)), n = r = e5.position) : e5.position === e5.lineStart && testDocumentSeparator(e5) ? throwError(e5, "unexpected end of the document within a double quoted scalar") : (e5.position++, r = e5.position);
  }
  throwError(e5, "unexpected end of the stream within a double quoted scalar");
}
function readFlowCollection(e5, t) {
  var n = true, r, s, o, a = e5.tag, l, c = e5.anchor, u, p, f, h, m, g = /* @__PURE__ */ Object.create(null), d, y, S, $;
  if ($ = e5.input.charCodeAt(e5.position), $ === 91) p = 93, m = false, l = [];
  else if ($ === 123) p = 125, m = true, l = {};
  else return false;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = l), $ = e5.input.charCodeAt(++e5.position); $ !== 0; ) {
    if (skipSeparationSpace(e5, true, t), $ = e5.input.charCodeAt(e5.position), $ === p) return e5.position++, e5.tag = a, e5.anchor = c, e5.kind = m ? "mapping" : "sequence", e5.result = l, true;
    n ? $ === 44 && throwError(e5, "expected the node content, but found ','") : throwError(e5, "missed comma between flow collection entries"), y = d = S = null, f = h = false, $ === 63 && (u = e5.input.charCodeAt(e5.position + 1), is_WS_OR_EOL(u) && (f = h = true, e5.position++, skipSeparationSpace(e5, true, t))), r = e5.line, s = e5.lineStart, o = e5.position, composeNode(e5, t, CONTEXT_FLOW_IN, false, true), y = e5.tag, d = e5.result, skipSeparationSpace(e5, true, t), $ = e5.input.charCodeAt(e5.position), (h || e5.line === r) && $ === 58 && (f = true, $ = e5.input.charCodeAt(++e5.position), skipSeparationSpace(e5, true, t), composeNode(e5, t, CONTEXT_FLOW_IN, false, true), S = e5.result), m ? storeMappingPair(e5, l, g, y, d, S, r, s, o) : f ? l.push(storeMappingPair(e5, null, g, y, d, S, r, s, o)) : l.push(d), skipSeparationSpace(e5, true, t), $ = e5.input.charCodeAt(e5.position), $ === 44 ? (n = true, $ = e5.input.charCodeAt(++e5.position)) : n = false;
  }
  throwError(e5, "unexpected end of the stream within a flow collection");
}
function readBlockScalar(e5, t) {
  var n, r, s = CHOMPING_CLIP, o = false, a = false, l = t, c = 0, u = false, p, f;
  if (f = e5.input.charCodeAt(e5.position), f === 124) r = false;
  else if (f === 62) r = true;
  else return false;
  for (e5.kind = "scalar", e5.result = ""; f !== 0; ) if (f = e5.input.charCodeAt(++e5.position), f === 43 || f === 45) CHOMPING_CLIP === s ? s = f === 43 ? CHOMPING_KEEP : CHOMPING_STRIP : throwError(e5, "repeat of a chomping mode identifier");
  else if ((p = fromDecimalCode(f)) >= 0) p === 0 ? throwError(e5, "bad explicit indentation width of a block scalar; it cannot be less than one") : a ? throwError(e5, "repeat of an indentation width identifier") : (l = t + p - 1, a = true);
  else break;
  if (is_WHITE_SPACE(f)) {
    do
      f = e5.input.charCodeAt(++e5.position);
    while (is_WHITE_SPACE(f));
    if (f === 35) do
      f = e5.input.charCodeAt(++e5.position);
    while (!is_EOL(f) && f !== 0);
  }
  for (; f !== 0; ) {
    for (readLineBreak(e5), e5.lineIndent = 0, f = e5.input.charCodeAt(e5.position); (!a || e5.lineIndent < l) && f === 32; ) e5.lineIndent++, f = e5.input.charCodeAt(++e5.position);
    if (!a && e5.lineIndent > l && (l = e5.lineIndent), is_EOL(f)) {
      c++;
      continue;
    }
    if (e5.lineIndent < l) {
      s === CHOMPING_KEEP ? e5.result += common.repeat(`
`, o ? 1 + c : c) : s === CHOMPING_CLIP && o && (e5.result += `
`);
      break;
    }
    for (r ? is_WHITE_SPACE(f) ? (u = true, e5.result += common.repeat(`
`, o ? 1 + c : c)) : u ? (u = false, e5.result += common.repeat(`
`, c + 1)) : c === 0 ? o && (e5.result += " ") : e5.result += common.repeat(`
`, c) : e5.result += common.repeat(`
`, o ? 1 + c : c), o = true, a = true, c = 0, n = e5.position; !is_EOL(f) && f !== 0; ) f = e5.input.charCodeAt(++e5.position);
    captureSegment(e5, n, e5.position, false);
  }
  return true;
}
function readBlockSequence(e5, t) {
  var n, r = e5.tag, s = e5.anchor, o = [], a, l = false, c;
  if (e5.firstTabInLine !== -1) return false;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = o), c = e5.input.charCodeAt(e5.position); c !== 0 && (e5.firstTabInLine !== -1 && (e5.position = e5.firstTabInLine, throwError(e5, "tab characters must not be used in indentation")), !(c !== 45 || (a = e5.input.charCodeAt(e5.position + 1), !is_WS_OR_EOL(a)))); ) {
    if (l = true, e5.position++, skipSeparationSpace(e5, true, -1) && e5.lineIndent <= t) {
      o.push(null), c = e5.input.charCodeAt(e5.position);
      continue;
    }
    if (n = e5.line, composeNode(e5, t, CONTEXT_BLOCK_IN, false, true), o.push(e5.result), skipSeparationSpace(e5, true, -1), c = e5.input.charCodeAt(e5.position), (e5.line === n || e5.lineIndent > t) && c !== 0) throwError(e5, "bad indentation of a sequence entry");
    else if (e5.lineIndent < t) break;
  }
  return l ? (e5.tag = r, e5.anchor = s, e5.kind = "sequence", e5.result = o, true) : false;
}
function readBlockMapping(e5, t, n) {
  var r, s, o, a, l, c, u = e5.tag, p = e5.anchor, f = {}, h = /* @__PURE__ */ Object.create(null), m = null, g = null, d = null, y = false, S = false, $;
  if (e5.firstTabInLine !== -1) return false;
  for (e5.anchor !== null && (e5.anchorMap[e5.anchor] = f), $ = e5.input.charCodeAt(e5.position); $ !== 0; ) {
    if (!y && e5.firstTabInLine !== -1 && (e5.position = e5.firstTabInLine, throwError(e5, "tab characters must not be used in indentation")), r = e5.input.charCodeAt(e5.position + 1), o = e5.line, ($ === 63 || $ === 58) && is_WS_OR_EOL(r)) $ === 63 ? (y && (storeMappingPair(e5, f, h, m, g, null, a, l, c), m = g = d = null), S = true, y = true, s = true) : y ? (y = false, s = true) : throwError(e5, "incomplete explicit mapping pair; a key node is missed; or followed by a non-tabulated empty line"), e5.position += 1, $ = r;
    else {
      if (a = e5.line, l = e5.lineStart, c = e5.position, !composeNode(e5, n, CONTEXT_FLOW_OUT, false, true)) break;
      if (e5.line === o) {
        for ($ = e5.input.charCodeAt(e5.position); is_WHITE_SPACE($); ) $ = e5.input.charCodeAt(++e5.position);
        if ($ === 58) $ = e5.input.charCodeAt(++e5.position), is_WS_OR_EOL($) || throwError(e5, "a whitespace character is expected after the key-value separator within a block mapping"), y && (storeMappingPair(e5, f, h, m, g, null, a, l, c), m = g = d = null), S = true, y = false, s = false, m = e5.tag, g = e5.result;
        else if (S) throwError(e5, "can not read an implicit mapping pair; a colon is missed");
        else return e5.tag = u, e5.anchor = p, true;
      } else if (S) throwError(e5, "can not read a block mapping entry; a multiline key may not be an implicit key");
      else return e5.tag = u, e5.anchor = p, true;
    }
    if ((e5.line === o || e5.lineIndent > t) && (y && (a = e5.line, l = e5.lineStart, c = e5.position), composeNode(e5, t, CONTEXT_BLOCK_OUT, true, s) && (y ? g = e5.result : d = e5.result), y || (storeMappingPair(e5, f, h, m, g, d, a, l, c), m = g = d = null), skipSeparationSpace(e5, true, -1), $ = e5.input.charCodeAt(e5.position)), (e5.line === o || e5.lineIndent > t) && $ !== 0) throwError(e5, "bad indentation of a mapping entry");
    else if (e5.lineIndent < t) break;
  }
  return y && storeMappingPair(e5, f, h, m, g, null, a, l, c), S && (e5.tag = u, e5.anchor = p, e5.kind = "mapping", e5.result = f), S;
}
function readTagProperty(e5) {
  var t, n = false, r = false, s, o, a;
  if (a = e5.input.charCodeAt(e5.position), a !== 33) return false;
  if (e5.tag !== null && throwError(e5, "duplication of a tag property"), a = e5.input.charCodeAt(++e5.position), a === 60 ? (n = true, a = e5.input.charCodeAt(++e5.position)) : a === 33 ? (r = true, s = "!!", a = e5.input.charCodeAt(++e5.position)) : s = "!", t = e5.position, n) {
    do
      a = e5.input.charCodeAt(++e5.position);
    while (a !== 0 && a !== 62);
    e5.position < e5.length ? (o = e5.input.slice(t, e5.position), a = e5.input.charCodeAt(++e5.position)) : throwError(e5, "unexpected end of the stream within a verbatim tag");
  } else {
    for (; a !== 0 && !is_WS_OR_EOL(a); ) a === 33 && (r ? throwError(e5, "tag suffix cannot contain exclamation marks") : (s = e5.input.slice(t - 1, e5.position + 1), PATTERN_TAG_HANDLE.test(s) || throwError(e5, "named tag handle cannot contain such characters"), r = true, t = e5.position + 1)), a = e5.input.charCodeAt(++e5.position);
    o = e5.input.slice(t, e5.position), PATTERN_FLOW_INDICATORS.test(o) && throwError(e5, "tag suffix cannot contain flow indicator characters");
  }
  o && !PATTERN_TAG_URI.test(o) && throwError(e5, "tag name cannot contain such characters: " + o);
  try {
    o = decodeURIComponent(o);
  } catch {
    throwError(e5, "tag name is malformed: " + o);
  }
  return n ? e5.tag = o : _hasOwnProperty$1.call(e5.tagMap, s) ? e5.tag = e5.tagMap[s] + o : s === "!" ? e5.tag = "!" + o : s === "!!" ? e5.tag = "tag:yaml.org,2002:" + o : throwError(e5, 'undeclared tag handle "' + s + '"'), true;
}
function readAnchorProperty(e5) {
  var t, n;
  if (n = e5.input.charCodeAt(e5.position), n !== 38) return false;
  for (e5.anchor !== null && throwError(e5, "duplication of an anchor property"), n = e5.input.charCodeAt(++e5.position), t = e5.position; n !== 0 && !is_WS_OR_EOL(n) && !is_FLOW_INDICATOR(n); ) n = e5.input.charCodeAt(++e5.position);
  return e5.position === t && throwError(e5, "name of an anchor node must contain at least one character"), e5.anchor = e5.input.slice(t, e5.position), true;
}
function readAlias(e5) {
  var t, n, r;
  if (r = e5.input.charCodeAt(e5.position), r !== 42) return false;
  for (r = e5.input.charCodeAt(++e5.position), t = e5.position; r !== 0 && !is_WS_OR_EOL(r) && !is_FLOW_INDICATOR(r); ) r = e5.input.charCodeAt(++e5.position);
  return e5.position === t && throwError(e5, "name of an alias node must contain at least one character"), n = e5.input.slice(t, e5.position), _hasOwnProperty$1.call(e5.anchorMap, n) || throwError(e5, 'unidentified alias "' + n + '"'), e5.result = e5.anchorMap[n], skipSeparationSpace(e5, true, -1), true;
}
function composeNode(e5, t, n, r, s) {
  var o, a, l, c = 1, u = false, p = false, f, h, m, g, d, y;
  if (e5.listener !== null && e5.listener("open", e5), e5.tag = null, e5.anchor = null, e5.kind = null, e5.result = null, o = a = l = CONTEXT_BLOCK_OUT === n || CONTEXT_BLOCK_IN === n, r && skipSeparationSpace(e5, true, -1) && (u = true, e5.lineIndent > t ? c = 1 : e5.lineIndent === t ? c = 0 : e5.lineIndent < t && (c = -1)), c === 1) for (; readTagProperty(e5) || readAnchorProperty(e5); ) skipSeparationSpace(e5, true, -1) ? (u = true, l = o, e5.lineIndent > t ? c = 1 : e5.lineIndent === t ? c = 0 : e5.lineIndent < t && (c = -1)) : l = false;
  if (l && (l = u || s), (c === 1 || CONTEXT_BLOCK_OUT === n) && (CONTEXT_FLOW_IN === n || CONTEXT_FLOW_OUT === n ? d = t : d = t + 1, y = e5.position - e5.lineStart, c === 1 ? l && (readBlockSequence(e5, y) || readBlockMapping(e5, y, d)) || readFlowCollection(e5, d) ? p = true : (a && readBlockScalar(e5, d) || readSingleQuotedScalar(e5, d) || readDoubleQuotedScalar(e5, d) ? p = true : readAlias(e5) ? (p = true, (e5.tag !== null || e5.anchor !== null) && throwError(e5, "alias node should not have any properties")) : readPlainScalar(e5, d, CONTEXT_FLOW_IN === n) && (p = true, e5.tag === null && (e5.tag = "?")), e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result)) : c === 0 && (p = l && readBlockSequence(e5, y))), e5.tag === null) e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result);
  else if (e5.tag === "?") {
    for (e5.result !== null && e5.kind !== "scalar" && throwError(e5, 'unacceptable node kind for !<?> tag; it should be "scalar", not "' + e5.kind + '"'), f = 0, h = e5.implicitTypes.length; f < h; f += 1) if (g = e5.implicitTypes[f], g.resolve(e5.result)) {
      e5.result = g.construct(e5.result), e5.tag = g.tag, e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result);
      break;
    }
  } else if (e5.tag !== "!") {
    if (_hasOwnProperty$1.call(e5.typeMap[e5.kind || "fallback"], e5.tag)) g = e5.typeMap[e5.kind || "fallback"][e5.tag];
    else for (g = null, m = e5.typeMap.multi[e5.kind || "fallback"], f = 0, h = m.length; f < h; f += 1) if (e5.tag.slice(0, m[f].tag.length) === m[f].tag) {
      g = m[f];
      break;
    }
    g || throwError(e5, "unknown tag !<" + e5.tag + ">"), e5.result !== null && g.kind !== e5.kind && throwError(e5, "unacceptable node kind for !<" + e5.tag + '> tag; it should be "' + g.kind + '", not "' + e5.kind + '"'), g.resolve(e5.result, e5.tag) ? (e5.result = g.construct(e5.result, e5.tag), e5.anchor !== null && (e5.anchorMap[e5.anchor] = e5.result)) : throwError(e5, "cannot resolve a node with !<" + e5.tag + "> explicit tag");
  }
  return e5.listener !== null && e5.listener("close", e5), e5.tag !== null || e5.anchor !== null || p;
}
function readDocument(e5) {
  var t = e5.position, n, r, s, o = false, a;
  for (e5.version = null, e5.checkLineBreaks = e5.legacy, e5.tagMap = /* @__PURE__ */ Object.create(null), e5.anchorMap = /* @__PURE__ */ Object.create(null); (a = e5.input.charCodeAt(e5.position)) !== 0 && (skipSeparationSpace(e5, true, -1), a = e5.input.charCodeAt(e5.position), !(e5.lineIndent > 0 || a !== 37)); ) {
    for (o = true, a = e5.input.charCodeAt(++e5.position), n = e5.position; a !== 0 && !is_WS_OR_EOL(a); ) a = e5.input.charCodeAt(++e5.position);
    for (r = e5.input.slice(n, e5.position), s = [], r.length < 1 && throwError(e5, "directive name must not be less than one character in length"); a !== 0; ) {
      for (; is_WHITE_SPACE(a); ) a = e5.input.charCodeAt(++e5.position);
      if (a === 35) {
        do
          a = e5.input.charCodeAt(++e5.position);
        while (a !== 0 && !is_EOL(a));
        break;
      }
      if (is_EOL(a)) break;
      for (n = e5.position; a !== 0 && !is_WS_OR_EOL(a); ) a = e5.input.charCodeAt(++e5.position);
      s.push(e5.input.slice(n, e5.position));
    }
    a !== 0 && readLineBreak(e5), _hasOwnProperty$1.call(directiveHandlers, r) ? directiveHandlers[r](e5, r, s) : throwWarning(e5, 'unknown document directive "' + r + '"');
  }
  if (skipSeparationSpace(e5, true, -1), e5.lineIndent === 0 && e5.input.charCodeAt(e5.position) === 45 && e5.input.charCodeAt(e5.position + 1) === 45 && e5.input.charCodeAt(e5.position + 2) === 45 ? (e5.position += 3, skipSeparationSpace(e5, true, -1)) : o && throwError(e5, "directives end mark is expected"), composeNode(e5, e5.lineIndent - 1, CONTEXT_BLOCK_OUT, false, true), skipSeparationSpace(e5, true, -1), e5.checkLineBreaks && PATTERN_NON_ASCII_LINE_BREAKS.test(e5.input.slice(t, e5.position)) && throwWarning(e5, "non-ASCII line breaks are interpreted as content"), e5.documents.push(e5.result), e5.position === e5.lineStart && testDocumentSeparator(e5)) {
    e5.input.charCodeAt(e5.position) === 46 && (e5.position += 3, skipSeparationSpace(e5, true, -1));
    return;
  }
  if (e5.position < e5.length - 1) throwError(e5, "end of the stream or a document separator is expected");
  else return;
}
function loadDocuments(e5, t) {
  e5 = String(e5), t = t || {}, e5.length !== 0 && (e5.charCodeAt(e5.length - 1) !== 10 && e5.charCodeAt(e5.length - 1) !== 13 && (e5 += `
`), e5.charCodeAt(0) === 65279 && (e5 = e5.slice(1)));
  var n = new State$1(e5, t), r = e5.indexOf("\0");
  for (r !== -1 && (n.position = r, throwError(n, "null byte is not allowed in input")), n.input += "\0"; n.input.charCodeAt(n.position) === 32; ) n.lineIndent += 1, n.position += 1;
  for (; n.position < n.length - 1; ) readDocument(n);
  return n.documents;
}
function loadAll$1(e5, t, n) {
  t !== null && typeof t == "object" && typeof n > "u" && (n = t, t = null);
  var r = loadDocuments(e5, n);
  if (typeof t != "function") return r;
  for (var s = 0, o = r.length; s < o; s += 1) t(r[s]);
}
function load$1(e5, t) {
  var n = loadDocuments(e5, t);
  if (n.length !== 0) {
    if (n.length === 1) return n[0];
    throw new exception("expected a single document in the stream, but found more");
  }
}
var loadAll_1 = loadAll$1, load_1 = load$1, loader = { loadAll: loadAll_1, load: load_1 }, _toString = Object.prototype.toString, _hasOwnProperty = Object.prototype.hasOwnProperty, CHAR_BOM = 65279, CHAR_TAB = 9, CHAR_LINE_FEED = 10, CHAR_CARRIAGE_RETURN = 13, CHAR_SPACE = 32, CHAR_EXCLAMATION = 33, CHAR_DOUBLE_QUOTE = 34, CHAR_SHARP = 35, CHAR_PERCENT = 37, CHAR_AMPERSAND = 38, CHAR_SINGLE_QUOTE = 39, CHAR_ASTERISK = 42, CHAR_COMMA = 44, CHAR_MINUS = 45, CHAR_COLON = 58, CHAR_EQUALS = 61, CHAR_GREATER_THAN = 62, CHAR_QUESTION = 63, CHAR_COMMERCIAL_AT = 64, CHAR_LEFT_SQUARE_BRACKET = 91, CHAR_RIGHT_SQUARE_BRACKET = 93, CHAR_GRAVE_ACCENT = 96, CHAR_LEFT_CURLY_BRACKET = 123, CHAR_VERTICAL_LINE = 124, CHAR_RIGHT_CURLY_BRACKET = 125, ESCAPE_SEQUENCES = {};
ESCAPE_SEQUENCES[0] = "\\0";
ESCAPE_SEQUENCES[7] = "\\a";
ESCAPE_SEQUENCES[8] = "\\b";
ESCAPE_SEQUENCES[9] = "\\t";
ESCAPE_SEQUENCES[10] = "\\n";
ESCAPE_SEQUENCES[11] = "\\v";
ESCAPE_SEQUENCES[12] = "\\f";
ESCAPE_SEQUENCES[13] = "\\r";
ESCAPE_SEQUENCES[27] = "\\e";
ESCAPE_SEQUENCES[34] = '\\"';
ESCAPE_SEQUENCES[92] = "\\\\";
ESCAPE_SEQUENCES[133] = "\\N";
ESCAPE_SEQUENCES[160] = "\\_";
ESCAPE_SEQUENCES[8232] = "\\L";
ESCAPE_SEQUENCES[8233] = "\\P";
var DEPRECATED_BOOLEANS_SYNTAX = ["y", "Y", "yes", "Yes", "YES", "on", "On", "ON", "n", "N", "no", "No", "NO", "off", "Off", "OFF"], DEPRECATED_BASE60_SYNTAX = /^[-+]?[0-9_]+(?::[0-9_]+)+(?:\.[0-9_]*)?$/;
function compileStyleMap(e5, t) {
  var n, r, s, o, a, l, c;
  if (t === null) return {};
  for (n = {}, r = Object.keys(t), s = 0, o = r.length; s < o; s += 1) a = r[s], l = String(t[a]), a.slice(0, 2) === "!!" && (a = "tag:yaml.org,2002:" + a.slice(2)), c = e5.compiledTypeMap.fallback[a], c && _hasOwnProperty.call(c.styleAliases, l) && (l = c.styleAliases[l]), n[a] = l;
  return n;
}
function encodeHex(e5) {
  var t, n, r;
  if (t = e5.toString(16).toUpperCase(), e5 <= 255) n = "x", r = 2;
  else if (e5 <= 65535) n = "u", r = 4;
  else if (e5 <= 4294967295) n = "U", r = 8;
  else throw new exception("code point within a string may not be greater than 0xFFFFFFFF");
  return "\\" + n + common.repeat("0", r - t.length) + t;
}
var QUOTING_TYPE_SINGLE = 1, QUOTING_TYPE_DOUBLE = 2;
function State(e5) {
  this.schema = e5.schema || _default, this.indent = Math.max(1, e5.indent || 2), this.noArrayIndent = e5.noArrayIndent || false, this.skipInvalid = e5.skipInvalid || false, this.flowLevel = common.isNothing(e5.flowLevel) ? -1 : e5.flowLevel, this.styleMap = compileStyleMap(this.schema, e5.styles || null), this.sortKeys = e5.sortKeys || false, this.lineWidth = e5.lineWidth || 80, this.noRefs = e5.noRefs || false, this.noCompatMode = e5.noCompatMode || false, this.condenseFlow = e5.condenseFlow || false, this.quotingType = e5.quotingType === '"' ? QUOTING_TYPE_DOUBLE : QUOTING_TYPE_SINGLE, this.forceQuotes = e5.forceQuotes || false, this.replacer = typeof e5.replacer == "function" ? e5.replacer : null, this.implicitTypes = this.schema.compiledImplicit, this.explicitTypes = this.schema.compiledExplicit, this.tag = null, this.result = "", this.duplicates = [], this.usedDuplicates = null;
}
function indentString(e5, t) {
  for (var n = common.repeat(" ", t), r = 0, s = -1, o = "", a, l = e5.length; r < l; ) s = e5.indexOf(`
`, r), s === -1 ? (a = e5.slice(r), r = l) : (a = e5.slice(r, s + 1), r = s + 1), a.length && a !== `
` && (o += n), o += a;
  return o;
}
function generateNextLine(e5, t) {
  return `
` + common.repeat(" ", e5.indent * t);
}
function testImplicitResolving(e5, t) {
  var n, r, s;
  for (n = 0, r = e5.implicitTypes.length; n < r; n += 1) if (s = e5.implicitTypes[n], s.resolve(t)) return true;
  return false;
}
function isWhitespace(e5) {
  return e5 === CHAR_SPACE || e5 === CHAR_TAB;
}
function isPrintable(e5) {
  return 32 <= e5 && e5 <= 126 || 161 <= e5 && e5 <= 55295 && e5 !== 8232 && e5 !== 8233 || 57344 <= e5 && e5 <= 65533 && e5 !== CHAR_BOM || 65536 <= e5 && e5 <= 1114111;
}
function isNsCharOrWhitespace(e5) {
  return isPrintable(e5) && e5 !== CHAR_BOM && e5 !== CHAR_CARRIAGE_RETURN && e5 !== CHAR_LINE_FEED;
}
function isPlainSafe(e5, t, n) {
  var r = isNsCharOrWhitespace(e5), s = r && !isWhitespace(e5);
  return (n ? r : r && e5 !== CHAR_COMMA && e5 !== CHAR_LEFT_SQUARE_BRACKET && e5 !== CHAR_RIGHT_SQUARE_BRACKET && e5 !== CHAR_LEFT_CURLY_BRACKET && e5 !== CHAR_RIGHT_CURLY_BRACKET) && e5 !== CHAR_SHARP && !(t === CHAR_COLON && !s) || isNsCharOrWhitespace(t) && !isWhitespace(t) && e5 === CHAR_SHARP || t === CHAR_COLON && s;
}
function isPlainSafeFirst(e5) {
  return isPrintable(e5) && e5 !== CHAR_BOM && !isWhitespace(e5) && e5 !== CHAR_MINUS && e5 !== CHAR_QUESTION && e5 !== CHAR_COLON && e5 !== CHAR_COMMA && e5 !== CHAR_LEFT_SQUARE_BRACKET && e5 !== CHAR_RIGHT_SQUARE_BRACKET && e5 !== CHAR_LEFT_CURLY_BRACKET && e5 !== CHAR_RIGHT_CURLY_BRACKET && e5 !== CHAR_SHARP && e5 !== CHAR_AMPERSAND && e5 !== CHAR_ASTERISK && e5 !== CHAR_EXCLAMATION && e5 !== CHAR_VERTICAL_LINE && e5 !== CHAR_EQUALS && e5 !== CHAR_GREATER_THAN && e5 !== CHAR_SINGLE_QUOTE && e5 !== CHAR_DOUBLE_QUOTE && e5 !== CHAR_PERCENT && e5 !== CHAR_COMMERCIAL_AT && e5 !== CHAR_GRAVE_ACCENT;
}
function isPlainSafeLast(e5) {
  return !isWhitespace(e5) && e5 !== CHAR_COLON;
}
function codePointAt(e5, t) {
  var n = e5.charCodeAt(t), r;
  return n >= 55296 && n <= 56319 && t + 1 < e5.length && (r = e5.charCodeAt(t + 1), r >= 56320 && r <= 57343) ? (n - 55296) * 1024 + r - 56320 + 65536 : n;
}
function needIndentIndicator(e5) {
  var t = /^\n* /;
  return t.test(e5);
}
var STYLE_PLAIN = 1, STYLE_SINGLE = 2, STYLE_LITERAL = 3, STYLE_FOLDED = 4, STYLE_DOUBLE = 5;
function chooseScalarStyle(e5, t, n, r, s, o, a, l) {
  var c, u = 0, p = null, f = false, h = false, m = r !== -1, g = -1, d = isPlainSafeFirst(codePointAt(e5, 0)) && isPlainSafeLast(codePointAt(e5, e5.length - 1));
  if (t || a) for (c = 0; c < e5.length; u >= 65536 ? c += 2 : c++) {
    if (u = codePointAt(e5, c), !isPrintable(u)) return STYLE_DOUBLE;
    d = d && isPlainSafe(u, p, l), p = u;
  }
  else {
    for (c = 0; c < e5.length; u >= 65536 ? c += 2 : c++) {
      if (u = codePointAt(e5, c), u === CHAR_LINE_FEED) f = true, m && (h = h || c - g - 1 > r && e5[g + 1] !== " ", g = c);
      else if (!isPrintable(u)) return STYLE_DOUBLE;
      d = d && isPlainSafe(u, p, l), p = u;
    }
    h = h || m && c - g - 1 > r && e5[g + 1] !== " ";
  }
  return !f && !h ? d && !a && !s(e5) ? STYLE_PLAIN : o === QUOTING_TYPE_DOUBLE ? STYLE_DOUBLE : STYLE_SINGLE : n > 9 && needIndentIndicator(e5) ? STYLE_DOUBLE : a ? o === QUOTING_TYPE_DOUBLE ? STYLE_DOUBLE : STYLE_SINGLE : h ? STYLE_FOLDED : STYLE_LITERAL;
}
function writeScalar(e5, t, n, r, s) {
  e5.dump = (function() {
    if (t.length === 0) return e5.quotingType === QUOTING_TYPE_DOUBLE ? '""' : "''";
    if (!e5.noCompatMode && (DEPRECATED_BOOLEANS_SYNTAX.indexOf(t) !== -1 || DEPRECATED_BASE60_SYNTAX.test(t))) return e5.quotingType === QUOTING_TYPE_DOUBLE ? '"' + t + '"' : "'" + t + "'";
    var o = e5.indent * Math.max(1, n), a = e5.lineWidth === -1 ? -1 : Math.max(Math.min(e5.lineWidth, 40), e5.lineWidth - o), l = r || e5.flowLevel > -1 && n >= e5.flowLevel;
    function c(u) {
      return testImplicitResolving(e5, u);
    }
    switch (chooseScalarStyle(t, l, e5.indent, a, c, e5.quotingType, e5.forceQuotes && !r, s)) {
      case STYLE_PLAIN:
        return t;
      case STYLE_SINGLE:
        return "'" + t.replace(/'/g, "''") + "'";
      case STYLE_LITERAL:
        return "|" + blockHeader(t, e5.indent) + dropEndingNewline(indentString(t, o));
      case STYLE_FOLDED:
        return ">" + blockHeader(t, e5.indent) + dropEndingNewline(indentString(foldString(t, a), o));
      case STYLE_DOUBLE:
        return '"' + escapeString(t) + '"';
      default:
        throw new exception("impossible error: invalid scalar style");
    }
  })();
}
function blockHeader(e5, t) {
  var n = needIndentIndicator(e5) ? String(t) : "", r = e5[e5.length - 1] === `
`, s = r && (e5[e5.length - 2] === `
` || e5 === `
`), o = s ? "+" : r ? "" : "-";
  return n + o + `
`;
}
function dropEndingNewline(e5) {
  return e5[e5.length - 1] === `
` ? e5.slice(0, -1) : e5;
}
function foldString(e5, t) {
  for (var n = /(\n+)([^\n]*)/g, r = (function() {
    var u = e5.indexOf(`
`);
    return u = u !== -1 ? u : e5.length, n.lastIndex = u, foldLine(e5.slice(0, u), t);
  })(), s = e5[0] === `
` || e5[0] === " ", o, a; a = n.exec(e5); ) {
    var l = a[1], c = a[2];
    o = c[0] === " ", r += l + (!s && !o && c !== "" ? `
` : "") + foldLine(c, t), s = o;
  }
  return r;
}
function foldLine(e5, t) {
  if (e5 === "" || e5[0] === " ") return e5;
  for (var n = / [^ ]/g, r, s = 0, o, a = 0, l = 0, c = ""; r = n.exec(e5); ) l = r.index, l - s > t && (o = a > s ? a : l, c += `
` + e5.slice(s, o), s = o + 1), a = l;
  return c += `
`, e5.length - s > t && a > s ? c += e5.slice(s, a) + `
` + e5.slice(a + 1) : c += e5.slice(s), c.slice(1);
}
function escapeString(e5) {
  for (var t = "", n = 0, r, s = 0; s < e5.length; n >= 65536 ? s += 2 : s++) n = codePointAt(e5, s), r = ESCAPE_SEQUENCES[n], !r && isPrintable(n) ? (t += e5[s], n >= 65536 && (t += e5[s + 1])) : t += r || encodeHex(n);
  return t;
}
function writeFlowSequence(e5, t, n) {
  var r = "", s = e5.tag, o, a, l;
  for (o = 0, a = n.length; o < a; o += 1) l = n[o], e5.replacer && (l = e5.replacer.call(n, String(o), l)), (writeNode(e5, t, l, false, false) || typeof l > "u" && writeNode(e5, t, null, false, false)) && (r !== "" && (r += "," + (e5.condenseFlow ? "" : " ")), r += e5.dump);
  e5.tag = s, e5.dump = "[" + r + "]";
}
function writeBlockSequence(e5, t, n, r) {
  var s = "", o = e5.tag, a, l, c;
  for (a = 0, l = n.length; a < l; a += 1) c = n[a], e5.replacer && (c = e5.replacer.call(n, String(a), c)), (writeNode(e5, t + 1, c, true, true, false, true) || typeof c > "u" && writeNode(e5, t + 1, null, true, true, false, true)) && ((!r || s !== "") && (s += generateNextLine(e5, t)), e5.dump && CHAR_LINE_FEED === e5.dump.charCodeAt(0) ? s += "-" : s += "- ", s += e5.dump);
  e5.tag = o, e5.dump = s || "[]";
}
function writeFlowMapping(e5, t, n) {
  var r = "", s = e5.tag, o = Object.keys(n), a, l, c, u, p;
  for (a = 0, l = o.length; a < l; a += 1) p = "", r !== "" && (p += ", "), e5.condenseFlow && (p += '"'), c = o[a], u = n[c], e5.replacer && (u = e5.replacer.call(n, c, u)), writeNode(e5, t, c, false, false) && (e5.dump.length > 1024 && (p += "? "), p += e5.dump + (e5.condenseFlow ? '"' : "") + ":" + (e5.condenseFlow ? "" : " "), writeNode(e5, t, u, false, false) && (p += e5.dump, r += p));
  e5.tag = s, e5.dump = "{" + r + "}";
}
function writeBlockMapping(e5, t, n, r) {
  var s = "", o = e5.tag, a = Object.keys(n), l, c, u, p, f, h;
  if (e5.sortKeys === true) a.sort();
  else if (typeof e5.sortKeys == "function") a.sort(e5.sortKeys);
  else if (e5.sortKeys) throw new exception("sortKeys must be a boolean or a function");
  for (l = 0, c = a.length; l < c; l += 1) h = "", (!r || s !== "") && (h += generateNextLine(e5, t)), u = a[l], p = n[u], e5.replacer && (p = e5.replacer.call(n, u, p)), writeNode(e5, t + 1, u, true, true, true) && (f = e5.tag !== null && e5.tag !== "?" || e5.dump && e5.dump.length > 1024, f && (e5.dump && CHAR_LINE_FEED === e5.dump.charCodeAt(0) ? h += "?" : h += "? "), h += e5.dump, f && (h += generateNextLine(e5, t)), writeNode(e5, t + 1, p, true, f) && (e5.dump && CHAR_LINE_FEED === e5.dump.charCodeAt(0) ? h += ":" : h += ": ", h += e5.dump, s += h));
  e5.tag = o, e5.dump = s || "{}";
}
function detectType(e5, t, n) {
  var r, s, o, a, l, c;
  for (s = n ? e5.explicitTypes : e5.implicitTypes, o = 0, a = s.length; o < a; o += 1) if (l = s[o], (l.instanceOf || l.predicate) && (!l.instanceOf || typeof t == "object" && t instanceof l.instanceOf) && (!l.predicate || l.predicate(t))) {
    if (n ? l.multi && l.representName ? e5.tag = l.representName(t) : e5.tag = l.tag : e5.tag = "?", l.represent) {
      if (c = e5.styleMap[l.tag] || l.defaultStyle, _toString.call(l.represent) === "[object Function]") r = l.represent(t, c);
      else if (_hasOwnProperty.call(l.represent, c)) r = l.represent[c](t, c);
      else throw new exception("!<" + l.tag + '> tag resolver accepts not "' + c + '" style');
      e5.dump = r;
    }
    return true;
  }
  return false;
}
function writeNode(e5, t, n, r, s, o, a) {
  e5.tag = null, e5.dump = n, detectType(e5, n, false) || detectType(e5, n, true);
  var l = _toString.call(e5.dump), c = r, u;
  r && (r = e5.flowLevel < 0 || e5.flowLevel > t);
  var p = l === "[object Object]" || l === "[object Array]", f, h;
  if (p && (f = e5.duplicates.indexOf(n), h = f !== -1), (e5.tag !== null && e5.tag !== "?" || h || e5.indent !== 2 && t > 0) && (s = false), h && e5.usedDuplicates[f]) e5.dump = "*ref_" + f;
  else {
    if (p && h && !e5.usedDuplicates[f] && (e5.usedDuplicates[f] = true), l === "[object Object]") r && Object.keys(e5.dump).length !== 0 ? (writeBlockMapping(e5, t, e5.dump, s), h && (e5.dump = "&ref_" + f + e5.dump)) : (writeFlowMapping(e5, t, e5.dump), h && (e5.dump = "&ref_" + f + " " + e5.dump));
    else if (l === "[object Array]") r && e5.dump.length !== 0 ? (e5.noArrayIndent && !a && t > 0 ? writeBlockSequence(e5, t - 1, e5.dump, s) : writeBlockSequence(e5, t, e5.dump, s), h && (e5.dump = "&ref_" + f + e5.dump)) : (writeFlowSequence(e5, t, e5.dump), h && (e5.dump = "&ref_" + f + " " + e5.dump));
    else if (l === "[object String]") e5.tag !== "?" && writeScalar(e5, e5.dump, t, o, c);
    else {
      if (l === "[object Undefined]") return false;
      if (e5.skipInvalid) return false;
      throw new exception("unacceptable kind of an object to dump " + l);
    }
    e5.tag !== null && e5.tag !== "?" && (u = encodeURI(e5.tag[0] === "!" ? e5.tag.slice(1) : e5.tag).replace(/!/g, "%21"), e5.tag[0] === "!" ? u = "!" + u : u.slice(0, 18) === "tag:yaml.org,2002:" ? u = "!!" + u.slice(18) : u = "!<" + u + ">", e5.dump = u + " " + e5.dump);
  }
  return true;
}
function getDuplicateReferences(e5, t) {
  var n = [], r = [], s, o;
  for (inspectNode(e5, n, r), s = 0, o = r.length; s < o; s += 1) t.duplicates.push(n[r[s]]);
  t.usedDuplicates = new Array(o);
}
function inspectNode(e5, t, n) {
  var r, s, o;
  if (e5 !== null && typeof e5 == "object") if (s = t.indexOf(e5), s !== -1) n.indexOf(s) === -1 && n.push(s);
  else if (t.push(e5), Array.isArray(e5)) for (s = 0, o = e5.length; s < o; s += 1) inspectNode(e5[s], t, n);
  else for (r = Object.keys(e5), s = 0, o = r.length; s < o; s += 1) inspectNode(e5[r[s]], t, n);
}
function dump$1(e5, t) {
  t = t || {};
  var n = new State(t);
  n.noRefs || getDuplicateReferences(e5, n);
  var r = e5;
  return n.replacer && (r = n.replacer.call({ "": r }, "", r)), writeNode(n, 0, r, true, true) ? n.dump + `
` : "";
}
var dump_1 = dump$1, dumper = { dump: dump_1 };
function renamed(e5, t) {
  return function() {
    throw new Error("Function yaml." + e5 + " is removed in js-yaml 4. Use yaml." + t + " instead, which is now safe by default.");
  };
}
var Type = type, Schema = schema, FAILSAFE_SCHEMA = failsafe, JSON_SCHEMA = json, CORE_SCHEMA = core, DEFAULT_SCHEMA = _default, load = loader.load, loadAll = loader.loadAll, dump = dumper.dump, YAMLException = exception, types = { binary, float, map, null: _null, pairs, set, timestamp, bool, int, merge, omap, seq, str }, safeLoad = renamed("safeLoad", "load"), safeLoadAll = renamed("safeLoadAll", "loadAll"), safeDump = renamed("safeDump", "dump"), jsYaml = { Type, Schema, FAILSAFE_SCHEMA, JSON_SCHEMA, CORE_SCHEMA, DEFAULT_SCHEMA, load, loadAll, dump, YAMLException, types, safeLoad, safeLoadAll, safeDump };
const yamlSerializer = { format: "yaml", async serializeDocumentFileContents(e5, t) {
  return jsYaml.dump(e5, { noRefs: true });
}, async deserializeDocumentFileContents(e5, t) {
  return jsYaml.load(e5, { schema: jsYaml.JSON_SCHEMA });
} }, defaultDevUser = { id: "dev", email: "dev@local.test", name: "Dev Editor" };
function resolveAuth(e5) {
  var _a4, _b;
  if (e5.mode === "dev") {
    const t = (_a4 = e5.devToken) != null ? _a4 : "dev-local-laika-token", n = (_b = e5.devUser) != null ? _b : defaultDevUser;
    return { authenticateAccessToken: async (r) => {
      if (r !== t) throw new qi("Unknown token (createCustomLaika is in dev mode)");
      return { ...n };
    } };
  }
  return { authenticateAccessToken: e5.authenticateAccessToken, authenticateApiToken: e5.authenticateApiToken };
}
function createCustomLaika(e5) {
  var _a4, _b, _c2, _d;
  const t = (_a4 = e5.basePath) != null ? _a4 : "/api/decap", n = (_b = e5.configKey) != null ? _b : "config", r = (_c2 = e5.logger) != null ? _c2 : console, s = (_d = e5.seedConfigOnFirstRequest) != null ? _d : true, o = new DecapContentBaseSettingsProvider({ storage: e5.storage, configKey: n }), a = new ContentBaseDocumentsRepository(e5.storage, o), l = new ContentBaseAssetsRepository(e5.storage, o), c = decapApi({ documents: a, storage: e5.storage, assets: l, basePath: t, logger: r, ...resolveAuth(e5.auth) });
  let u = false;
  return { fetch: async (f) => {
    var _a5, _b2;
    if (s && !u) {
      u = true;
      const h = `${n}.yml`;
      try {
        await runTask$1(e5.storage.getObject(h));
      } catch (m) {
        if (m instanceof Mi) try {
          await runTask$1(e5.storage.createObject({ key: h, content: stringify$3(e5.decapConfig) }));
        } catch (g) {
          (_a5 = r.warn) == null ? void 0 : _a5.call(r, "createCustomLaika: failed to seed config:", g);
        }
        else (_b2 = r.warn) == null ? void 0 : _b2.call(r, "createCustomLaika: failed to check config presence:", m);
      }
    }
    return c.fetch(f);
  }, authenticateRequest: c.authenticateRequest, storage: e5.storage, documents: a, assets: l, decapConfig: e5.decapConfig };
}
const safeText = async (e5) => {
  try {
    return await e5.text();
  } catch {
    return "";
  }
}, bind = (e5) => {
  if (e5 == null) return { type: "null" };
  if (typeof e5 == "string") return { type: "text", value: e5 };
  if (typeof e5 == "number") return Number.isInteger(e5) ? { type: "integer", value: String(e5) } : { type: "float", value: e5 };
  if (typeof e5 == "bigint") return { type: "integer", value: e5.toString() };
  if (typeof e5 == "boolean") return { type: "integer", value: e5 ? "1" : "0" };
  if (e5 instanceof Uint8Array) {
    let t = "";
    for (const n of e5) t += String.fromCharCode(n);
    return { type: "blob", base64: btoa(t) };
  }
  throw new Ge(`Cannot bind value of type ${typeof e5} to a libSQL argument`);
}, dec = new TextDecoder(), unbind = (e5) => {
  switch (e5.type) {
    case "null":
      return null;
    case "integer":
      return e5.value;
    case "float":
      return e5.value;
    case "text":
      return e5.value;
    case "blob": {
      const t = atob(e5.base64), n = new Uint8Array(t.length);
      for (let r = 0; r < t.length; r += 1) n[r] = t.charCodeAt(r);
      return dec.decode(n);
    }
  }
}, rowToObject = (e5, t) => {
  const n = {};
  for (let r = 0; r < e5.length; r += 1) {
    const s = e5[r], o = t[r];
    s && o && (n[s.name] = unbind(o));
  }
  return n;
}, errorForResponse = (e5, t, n) => {
  var _a4;
  let r = "";
  try {
    const s = JSON.parse(t), o = (_a4 = s.message) != null ? _a4 : s.error;
    o && (r = `: ${o}`);
  } catch {
  }
  switch (e5) {
    case 401:
      return we$2(new qi(`libSQL authentication failed for ${n}${r}`));
    case 403:
      return we$2(new Vi(`libSQL access denied for ${n}${r}`));
    case 404:
      return we$2(new Mi(`libSQL not found: ${n}`));
    case 429:
      return we$2(new Zi(`libSQL rate-limited request for ${n}`));
    default:
      return e5 >= 500 ? we$2(new qe$2(`libSQL returned HTTP ${e5} for ${n}`)) : we$2(new Ge(`libSQL returned HTTP ${e5} for ${n}${r}`));
  }
}, errorForStatement = (e5, t) => {
  var _a4, _b;
  const n = (_a4 = e5.message) != null ? _a4 : "";
  return /UNIQUE constraint failed|SQLITE_CONSTRAINT/i.test(n) || ((_b = e5.code) == null ? void 0 : _b.startsWith("SQLITE_CONSTRAINT")) ? new Wi(`libSQL unique constraint failed for ${t}: ${n}`) : new Ge(`libSQL statement failed for ${t}: ${n}`);
};
class LibSqlDataSource {
  constructor(t) {
    __publicField$1(this, "fetchImpl");
    __publicField$1(this, "auth");
    __publicField$1(this, "baseUrl");
    var _a4, _b;
    if (this.fetchImpl = (_a4 = t.fetch) != null ? _a4 : globalThis.fetch, typeof this.fetchImpl != "function") throw new Ge("No `fetch` implementation available; pass one via LibSqlDataSourceOptions.fetch");
    this.auth = (_b = t.auth) != null ? _b : {}, this.baseUrl = t.url.replace(/\/+$/, "");
  }
  async execute(t, n = []) {
    const r = { sql: t, args: n.map(bind) }, s = await this.pipelineRequest([{ type: "execute", stmt: r }], `execute: ${t.slice(0, 60)}`);
    if (X$1(s)) return we$2(s.failure);
    const o = s.success.results[0];
    return o ? o.type === "error" ? we$2(errorForStatement(o.error, t.slice(0, 60))) : o.response.type !== "execute" ? we$2(new Ge("libSQL execute returned non-execute response")) : lt(o.response.result) : we$2(new Ge("libSQL execute returned no result"));
  }
  async batch(t) {
    if (t.length === 0) return lt([]);
    const n = t.map((l, c) => {
      var _a4;
      const u = { sql: l.sql, args: ((_a4 = l.args) != null ? _a4 : []).map(bind) };
      return c === 0 ? { stmt: u } : { stmt: u, condition: { type: "ok", step: c - 1 } };
    }), r = await this.pipelineRequest([{ type: "batch", batch: { steps: n } }], "batch");
    if (X$1(r)) return we$2(r.failure);
    const s = r.success.results[0];
    if (!s) return we$2(new Ge("libSQL batch returned no result"));
    if (s.type === "error") return we$2(errorForStatement(s.error, "batch"));
    if (s.response.type !== "batch") return we$2(new Ge("libSQL batch returned non-batch response"));
    const o = s.response.result.step_results, a = s.response.result.step_errors;
    for (let l = 0; l < a.length; l += 1) {
      const c = a[l];
      if (c) return we$2(errorForStatement(c, `batch step ${l}`));
    }
    return lt(o.filter((l) => l !== null));
  }
  async pipelineRequest(t, n) {
    const r = await this.accessToken();
    let s;
    try {
      s = await this.fetchImpl(`${this.baseUrl}/v2/pipeline`, { method: "POST", headers: { "Content-Type": "application/json", ...r ? { Authorization: `Bearer ${r}` } : {} }, body: JSON.stringify({ requests: t }) });
    } catch (a) {
      return we$2(new qe$2("libSQL unreachable", { cause: a }));
    }
    if (!s.ok) return errorForResponse(s.status, await safeText(s), n);
    const o = await s.json();
    return lt(o);
  }
  async accessToken() {
    return this.auth.tokenProvider ? await this.auth.tokenProvider() : this.auth.token;
  }
}
const DEFAULT_TABLE = "laika_storage", DEFAULT_IGNORE_LIST = ["**/.keep", "**/.DS_Store", "**/Thumbs.db", "**/desktop.ini", "**/.contentbase", "**/.laikacms"], TYPE_FILE = "file", TYPE_FOLDER = "folder", liftResult = (e5) => K(Dm(() => e5), Nm), stripSlashes = (e5) => e5.replace(/^\/+|\/+$/g, ""), splitPath = (e5) => {
  const t = stripSlashes(e5), n = t.lastIndexOf("/");
  return n === -1 ? { parent: "", name: t } : { parent: t.slice(0, n), name: t.slice(n + 1) };
}, validateIdentifier = (e5) => {
  if (!/^[A-Za-z_][A-Za-z0-9_]*$/.test(e5)) throw new zi(`Invalid SQL identifier: ${e5}`);
}, decodeRow = (e5, t) => {
  var _a4, _b, _c2;
  const n = rowToObject(e5, t);
  return { Path: String((_a4 = n.Path) != null ? _a4 : ""), Parent: String((_b = n.Parent) != null ? _b : ""), Name: String((_c2 = n.Name) != null ? _c2 : ""), Type: n.Type === TYPE_FILE ? TYPE_FILE : TYPE_FOLDER, Extension: n.Extension === null || n.Extension === void 0 ? null : String(n.Extension), Content: n.Content === null || n.Content === void 0 ? null : String(n.Content) };
};
class LibSqlStorageRepository extends StorageRepository {
  constructor(t) {
    super();
    __publicField$1(this, "dataSource");
    __publicField$1(this, "tableName");
    __publicField$1(this, "serializerRegistry");
    __publicField$1(this, "defaultFileExtension");
    __publicField$1(this, "availableExtensions");
    __publicField$1(this, "excludeFilter");
    __publicField$1(this, "determineExtension");
    const { dataSource: n, tableName: r = DEFAULT_TABLE, serializerRegistry: s, defaultFileExtension: o, ignoreList: a = DEFAULT_IGNORE_LIST, determineExtension: l = defaultDetermineExtension } = t;
    validateIdentifier(r), this.tableName = r, this.dataSource = n, this.serializerRegistry = s, this.defaultFileExtension = o.startsWith(".") ? o.slice(1) : o, this.availableExtensions = Object.keys(s), this.determineExtension = l, this.excludeFilter = a.map((c) => makeRe(c, { dot: true, partial: true })).filter((c) => c !== false);
  }
  stripExtension(t) {
    for (const n of this.availableExtensions) if (t.endsWith(`.${n}`)) return t.slice(0, -(n.length + 1));
    return t;
  }
  resolveExtension(t, n) {
    const r = this.determineExtension(t, { metadata: n, defaultExtension: this.defaultFileExtension });
    return r && this.serializerRegistry[r] ? r : this.defaultFileExtension;
  }
  async serialize(t, n) {
    const r = t.startsWith(".") ? t.slice(1) : t, s = this.serializerRegistry[r];
    if (!s) throw new zi(`No serializer found for file extension: .${r}. Available formats: ${this.availableExtensions.join(", ")}`);
    return s.serializeDocumentFileContents(n, {});
  }
  async deserialize(t, n) {
    const r = t.startsWith(".") ? t.slice(1) : t, s = this.serializerRegistry[r];
    if (!s) throw new zi(`No serializer found for file extension: .${r}. Available formats: ${this.availableExtensions.join(", ")}`);
    return s.deserializeDocumentFileContents(n, {});
  }
  filePath(t, n) {
    return `${stripSlashes(this.stripExtension(t))}.${n}`;
  }
  async findFileRow(t) {
    const { parent: n, name: r } = splitPath(this.stripExtension(t)), s = await this.dataSource.execute(`SELECT Path, Parent, Name, Type, Extension, Content
       FROM ${this.tableName}
       WHERE Type = ? AND Parent = ? AND Name = ?
       LIMIT 1`, [TYPE_FILE, n, r]);
    if (X$1(s)) return null;
    const o = s.success.rows[0];
    return o ? decodeRow(s.success.cols, o) : null;
  }
  async hasFolder(t) {
    const n = stripSlashes(t);
    if (n === "") {
      const s = await this.dataSource.execute(`SELECT 1 FROM ${this.tableName} LIMIT 1`, []);
      return X$1(s) ? false : s.success.rows.length > 0;
    }
    const r = await this.dataSource.execute(`SELECT 1 FROM ${this.tableName}
       WHERE (Path = ? AND Type = ?)
          OR Parent = ?
       LIMIT 1`, [n, TYPE_FOLDER, n]);
    return X$1(r) ? false : r.success.rows.length > 0;
  }
  getObject(t) {
    return sc(() => ne$1({ self: this }, function* () {
      var _a4;
      const n = yield* Dm(() => this.findFileRow(t));
      if (!n) return yield* j(new Mi(`libSQL row not found: ${t}`));
      const r = (_a4 = n.Extension) != null ? _a4 : this.defaultFileExtension, s = yield* Dm(() => {
        var _a5;
        return this.deserialize(r, (_a5 = n.Content) != null ? _a5 : "");
      }), o = n.Parent === "" ? n.Name : `${n.Parent}/${n.Name}`, a = (/* @__PURE__ */ new Date()).toISOString();
      return { type: "object", key: o, createdAt: a, updatedAt: a, content: s, metadata: { extension: r, revisionId: n.Path } };
    }));
  }
  getFolder(t) {
    return sc(() => ne$1({ self: this }, function* () {
      if (!(yield* Dm(() => this.hasFolder(t)))) return yield* j(new Mi(`libSQL folder not found: ${t || "<root>"}`));
      const r = (/* @__PURE__ */ new Date()).toISOString();
      return { type: "folder", key: t, createdAt: r, updatedAt: r };
    }));
  }
  getAtom(t) {
    return sc(() => ne$1({ self: this }, function* () {
      if (yield* Dm(() => this.findFileRow(t))) return yield* us(this.getObject(t));
      const r = yield* di(us(this.getFolder(t)));
      return jt(r) ? r.success : yield* j(new zi(`Path not found: ${t}`));
    }));
  }
  createObject(t) {
    return sc(() => ne$1({ self: this }, function* () {
      var _a4;
      if (!t.content) return yield* j(new zi("Object content is required for creation"));
      const { parent: n, name: r } = splitPath(this.stripExtension(t.key)), s = yield* Dm(() => this.findFileRow(t.key));
      if (s) return yield* j(new Wi(`An object with key "${t.key}" already exists with extension .${(_a4 = s.Extension) != null ? _a4 : this.defaultFileExtension}`));
      const o = this.resolveExtension(t.key, t.metadata), a = yield* Dm(() => this.serialize(o, t.content)), l = this.filePath(t.key, o);
      return yield* liftResult(this.dataSource.execute(`INSERT INTO ${this.tableName} (Path, Parent, Name, Type, Extension, Content)
           VALUES (?, ?, ?, ?, ?, ?)`, [l, n, r, TYPE_FILE, o, a])), yield* us(this.getObject(t.key));
    }));
  }
  updateObject(t) {
    return sc(() => ne$1({ self: this }, function* () {
      var _a4;
      const n = yield* Dm(() => this.findFileRow(t.key));
      if (!n) return yield* j(new Mi(`libSQL row not found: ${t.key}`));
      if (t.content) {
        const r = (_a4 = n.Extension) != null ? _a4 : this.defaultFileExtension, s = yield* Dm(() => this.serialize(r, t.content));
        yield* liftResult(this.dataSource.execute(`UPDATE ${this.tableName} SET Content = ? WHERE Path = ?`, [s, n.Path]));
      }
      return yield* us(this.getObject(t.key));
    }));
  }
  createOrUpdateObject(t) {
    return sc(() => ne$1({ self: this }, function* () {
      var _a4, _b;
      const { parent: n, name: r } = splitPath(this.stripExtension(t.key)), o = (_b = (_a4 = yield* Dm(() => this.findFileRow(t.key))) == null ? void 0 : _a4.Extension) != null ? _b : this.resolveExtension(t.key, t.metadata), a = t.content ? yield* Dm(() => this.serialize(o, t.content)) : "", l = this.filePath(t.key, o);
      return yield* liftResult(this.dataSource.execute(`INSERT INTO ${this.tableName} (Path, Parent, Name, Type, Extension, Content)
           VALUES (?, ?, ?, ?, ?, ?)
           ON CONFLICT(Path) DO UPDATE SET Content = excluded.Content, Extension = excluded.Extension`, [l, n, r, TYPE_FILE, o, a])), yield* us(this.getObject(t.key));
    }));
  }
  createFolder(t) {
    return sc(() => ne$1({ self: this }, function* () {
      const n = stripSlashes(t.key);
      if (n === "") return yield* us(this.getFolder(""));
      const { parent: r, name: s } = splitPath(n);
      return yield* liftResult(this.dataSource.execute(`INSERT INTO ${this.tableName} (Path, Parent, Name, Type)
           VALUES (?, ?, ?, ?)
           ON CONFLICT(Path) DO NOTHING`, [n, r, s, TYPE_FOLDER])), yield* us(this.getFolder(n));
    }));
  }
  removeAtoms(t) {
    return ls((n) => ne$1({ self: this }, function* () {
      const r = t.map((c) => stripSlashes(c)).filter((c) => c !== ""), s = t.length - r.length;
      if (r.length === 0) {
        for (let c = 0; c < s; c++) yield* n.recoverableError(new zi("Refusing to delete empty key"));
        return { removed: 0, skipped: s };
      }
      const o = yield* Dm(async () => {
        const c = [];
        for (const u of r) {
          const p = await this.findFileRow(u);
          c.push({ key: u, resolved: p });
        }
        return c;
      }), a = o.filter((c) => c.resolved !== null), l = o.filter((c) => c.resolved === null);
      a.length > 0 && (yield* liftResult(this.dataSource.batch(a.map((c) => ({ sql: `DELETE FROM ${this.tableName} WHERE Path = ?`, args: [c.resolved.Path] })))));
      for (const c of a) yield* n.data(c.key);
      for (const c of l) yield* n.recoverableError(new Mi(`libSQL row not found: ${c.key}`));
      return { removed: a.length, skipped: s + l.length };
    }));
  }
  listAtomSummaries(t, n) {
    return ls((r) => ne$1({ self: this }, function* () {
      const s = yield* this.collectFilteredSummaries(t, n);
      return s.length > 0 && (yield* r.dataMany(s)), { total: s.length };
    }));
  }
  listAtoms(t, n) {
    return ls((r) => ne$1({ self: this }, function* () {
      const s = yield* this.collectFilteredSummaries(t, n);
      for (const o of s) if (o.type === "object-summary") {
        const a = yield* di(us(this.getObject(o.key)));
        X$1(a) ? yield* r.recoverableError(a.failure) : yield* r.data(a.success);
      } else {
        const a = yield* di(us(this.getFolder(o.key)));
        X$1(a) ? yield* r.recoverableError(a.failure) : yield* r.data(a.success);
      }
      return { total: s.length };
    }));
  }
  collectFilteredSummaries(t, n) {
    return ne$1({ self: this }, function* () {
      const r = stripSlashes(t), s = yield* liftResult(this.dataSource.execute(`SELECT Path, Parent, Name, Type, Extension, Content
         FROM ${this.tableName}
         WHERE Parent = ?`, [r])), c = [...s.rows.map((u) => decodeRow(s.cols, u)).map((u) => {
        const p = u.Parent === "" ? u.Name : `${u.Parent}/${u.Name}`;
        return u.Type === TYPE_FILE ? { type: "object-summary", key: p } : { type: "folder-summary", key: p };
      }).filter((u) => this.excludeFilter.every((p) => !p.test(u.key)))].sort((u, p) => naturalCompare(u.key, p.key));
      return applyPagination(c, n.pagination);
    });
  }
  getCapabilities() {
    return Lh({ compatibilityDate: mm.make("2026-05-20"), fileExtensions: { supported: true, description: "Each object is one row in a libSQL/SQLite table; the extension is stored in the `Extension` column.", supportedExtensions: this.serializerRegistry }, pagination: { supported: true, description: "In-memory slicing over SELECT results; LIMIT/OFFSET pushdown not yet wired.", styles: { offset: true, page: true, cursor: false } } });
  }
}
function requireEnv(e5) {
  const t = process.env[e5];
  if (!t) throw new Error(`Missing required env var: ${e5}`);
  return t;
}
const dataSource = new LibSqlDataSource({ url: requireEnv("LIBSQL_URL"), auth: { token: process.env.LIBSQL_AUTH_TOKEN } }), storage = new LibSqlStorageRepository({ dataSource, tableName: (_a3 = process.env.LIBSQL_TABLE) != null ? _a3 : "laika_storage", serializerRegistry: { md: markdownSerializer, yaml: yamlSerializer, yml: yamlSerializer, json: jsonSerializer, raw: rawSerializer }, defaultFileExtension: "md" }), laika = createCustomLaika({ storage, decapConfig: { backend: { name: "laika", api_url: "/api/decap" }, media_folder: "public/uploads", public_folder: "/uploads", collections: e }, basePath: "/api/decap", auth: { mode: "dev" } });

var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, key + "" , value);
function z(t = {}) {
  let e, s = false;
  const o = (n) => {
    if (e && e !== n) throw new Error("Context conflict");
  };
  let a;
  if (t.asyncContext) {
    const n = t.AsyncLocalStorage || globalThis.AsyncLocalStorage;
    n ? a = new n() : console.warn("[unctx] `AsyncLocalStorage` is not provided.");
  }
  const i = () => {
    if (a) {
      const n = a.getStore();
      if (n !== void 0) return n;
    }
    return e;
  };
  return { use: () => {
    const n = i();
    if (n === void 0) throw new Error("Context is not available");
    return n;
  }, tryUse: () => i(), set: (n, c) => {
    c || o(n), e = n, s = true;
  }, unset: () => {
    e = void 0, s = false;
  }, call: (n, c) => {
    o(n), e = n;
    try {
      return a ? a.run(n, c) : c();
    } finally {
      s || (e = void 0);
    }
  }, async callAsync(n, c) {
    e = n;
    const u = () => {
      e = n;
    }, g = () => e === n ? u : void 0;
    h.add(g);
    try {
      const H = a ? a.run(n, c) : c();
      return s || (e = void 0), await H;
    } finally {
      h.delete(g);
    }
  } };
}
function J(t = {}) {
  const e = {};
  return { get(s, o = {}) {
    return e[s] || (e[s] = z({ ...t, ...o })), e[s];
  } };
}
const d = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof global < "u" ? global : {}, y = "__unctx__", Q = d[y] || (d[y] = J()), V = (t, e = {}) => Q.get(t, e), m = "__unctx_async_handlers__", h = d[m] || (d[m] = /* @__PURE__ */ new Set());
function X(t) {
  let e;
  const s = w(t), o = { duplex: "half", method: t.method, headers: t.headers };
  return t.node.req.body instanceof ArrayBuffer ? new Request(s, { ...o, body: t.node.req.body }) : new Request(s, { ...o, get body() {
    return e || (e = oe(t), e);
  } });
}
function Y(t) {
  var _a;
  return (_a = t.web) != null ? _a : t.web = { request: X(t), url: w(t) }, t.web.request;
}
function Z() {
  return ue$1();
}
const x$1 = /* @__PURE__ */ Symbol("$HTTPEvent");
function ee(t) {
  return typeof t == "object" && (t instanceof H3Event || (t == null ? void 0 : t[x$1]) instanceof H3Event || (t == null ? void 0 : t.__is_event__) === true);
}
function r(t) {
  return function(...e) {
    var _a;
    let s = e[0];
    if (ee(s)) e[0] = s instanceof H3Event || s.__is_event__ ? s : s[x$1];
    else {
      if (!((_a = globalThis.app.config.server.experimental) == null ? void 0 : _a.asyncContext)) throw new Error("AsyncLocalStorage was not enabled. Use the `server.experimental.asyncContext: true` option in your app configuration to enable it. Or, pass the instance of HTTPEvent that you have as the first argument to the function.");
      if (s = Z(), !s) throw new Error("No HTTPEvent found in AsyncLocalStorage. Make sure you are using the function within the server runtime.");
      e.unshift(s);
    }
    return t(...e);
  };
}
const w = r(getRequestURL), te = r(getRequestIP), R$1 = r(setResponseStatus), v = r(getResponseStatus), se = r(getResponseStatusText), l = r(getResponseHeaders), b = r(getResponseHeader), ne = r(setResponseHeader), re = r(appendResponseHeader), _e$1 = r(sendRedirect), xe$1 = r(getCookie), we$1 = r(setCookie), Ce$1 = r(setHeader), oe = r(getRequestWebStream), ae = r(removeResponseHeader), ce$1 = r(Y);
function ie() {
  var _a;
  return V("nitro-app", { asyncContext: !!((_a = globalThis.app.config.server.experimental) == null ? void 0 : _a.asyncContext), AsyncLocalStorage: AsyncLocalStorage });
}
function ue$1() {
  return ie().use().event;
}
const p = "solidFetchEvent";
function le$1(t) {
  return { request: ce$1(t), response: fe$1(t), clientAddress: te(t), locals: {}, nativeEvent: t };
}
function de$1(t) {
  return { ...t };
}
function He$1(t) {
  if (!t.context[p]) {
    const e = le$1(t);
    t.context[p] = e;
  }
  return t.context[p];
}
let pe$1 = class pe {
  constructor(e) {
    __publicField(this, "event");
    this.event = e;
  }
  get(e) {
    const s = b(this.event, e);
    return Array.isArray(s) ? s.join(", ") : s || null;
  }
  has(e) {
    return this.get(e) !== null;
  }
  set(e, s) {
    return ne(this.event, e, s);
  }
  delete(e) {
    return ae(this.event, e);
  }
  append(e, s) {
    re(this.event, e, s);
  }
  getSetCookie() {
    const e = b(this.event, "Set-Cookie");
    return Array.isArray(e) ? e : [e];
  }
  forEach(e) {
    return Object.entries(l(this.event)).forEach(([s, o]) => e(Array.isArray(o) ? o.join(", ") : o, s, this));
  }
  entries() {
    return Object.entries(l(this.event)).map(([e, s]) => [e, Array.isArray(s) ? s.join(", ") : s])[Symbol.iterator]();
  }
  keys() {
    return Object.keys(l(this.event))[Symbol.iterator]();
  }
  values() {
    return Object.values(l(this.event)).map((e) => Array.isArray(e) ? e.join(", ") : e)[Symbol.iterator]();
  }
  [Symbol.iterator]() {
    return this.entries()[Symbol.iterator]();
  }
};
function fe$1(t) {
  return { get status() {
    return v(t);
  }, set status(e) {
    R$1(t, e);
  }, get statusText() {
    return se(t);
  }, set statusText(e) {
    R$1(t, v(t), e);
  }, headers: new pe$1(t) };
}
function C$1(t, e, s) {
  if (typeof t != "function") throw new Error("Export from a 'use server' module must be a function");
  const o = "";
  return new Proxy(t, { get(a, i, n) {
    return i === "url" ? `${o}/_server?id=${encodeURIComponent(e)}&name=${encodeURIComponent(s)}` : i === "GET" ? n : a[i];
  }, apply(a, i, n) {
    const c = getRequestEvent();
    if (!c) throw new Error("Cannot call server function outside of a request");
    const u = de$1(c);
    return u.locals.serverFunctionMeta = { id: e + "#" + s }, u.serverOnly = true, provideRequestEvent(u, () => t.apply(i, n));
  } });
}
const ge$1 = C$1(async () => {
  const { items: t } = await collectStream(laika.documents.listRecordSummaries({ pagination: { page: 1, perPage: 100 }, folder: "posts", depth: 1, type: "published" }));
  return t.filter((e) => e.type === "published-summary").sort((e, s) => e.updatedAt && s.updatedAt ? s.updatedAt.localeCompare(e.updatedAt) : s.key.localeCompare(e.key));
}, "src_lib_content_ts--getPosts_query", "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/lib/content.ts?tsr-directive-use-server="), qe$1 = query(ge$1, "posts"), ye$1 = C$1(async (t) => runTask$1(laika.documents.getDocument(`posts/${t}`)), "src_lib_content_ts--getPost_query", "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/lib/content.ts?tsr-directive-use-server="), Ae$1 = query(ye$1, "post");

const $ = { NORMAL: 0, WILDCARD: 1, PLACEHOLDER: 2 };
function le(e = {}) {
  const r = { options: e, rootNode: M(), staticRoutesMap: {} }, t = (n) => e.strictTrailingSlash ? n : n.replace(/\/$/, "") || "/";
  if (e.routes) for (const n in e.routes) P(r, t(n), e.routes[n]);
  return { ctx: r, lookup: (n) => ce(r, t(n)), insert: (n, a) => P(r, t(n), a), remove: (n) => pe(r, t(n)) };
}
function ce(e, r) {
  const t = e.staticRoutesMap[r];
  if (t) return t.data;
  const n = r.split("/"), a = {};
  let s = false, l = null, o = e.rootNode, i = null;
  for (let c = 0; c < n.length; c++) {
    const u = n[c];
    o.wildcardChildNode !== null && (l = o.wildcardChildNode, i = n.slice(c).join("/"));
    const E = o.children.get(u);
    if (E === void 0) {
      if (o && o.placeholderChildren.length > 1) {
        const y = n.length - c;
        o = o.placeholderChildren.find((d) => d.maxDepth === y) || null;
      } else o = o.placeholderChildren[0] || null;
      if (!o) break;
      o.paramName && (a[o.paramName] = u), s = true;
    } else o = E;
  }
  return (o === null || o.data === null) && l !== null && (o = l, a[o.paramName || "_"] = i, s = true), o ? s ? { ...o.data, params: s ? a : void 0 } : o.data : null;
}
function P(e, r, t) {
  let n = true;
  const a = r.split("/");
  let s = e.rootNode, l = 0;
  const o = [s];
  for (const i of a) {
    let c;
    if (c = s.children.get(i)) s = c;
    else {
      const u = ue(i);
      c = M({ type: u, parent: s }), s.children.set(i, c), u === $.PLACEHOLDER ? (c.paramName = i === "*" ? `_${l++}` : i.slice(1), s.placeholderChildren.push(c), n = false) : u === $.WILDCARD && (s.wildcardChildNode = c, c.paramName = i.slice(3) || "_", n = false), o.push(c), s = c;
    }
  }
  for (const [i, c] of o.entries()) c.maxDepth = Math.max(o.length - i, c.maxDepth || 0);
  return s.data = t, n === true && (e.staticRoutesMap[r] = s), s;
}
function pe(e, r) {
  let t = false;
  const n = r.split("/");
  let a = e.rootNode;
  for (const s of n) if (a = a.children.get(s), !a) return t;
  if (a.data) {
    const s = n.at(-1) || "";
    a.data = null, Object.keys(a.children).length === 0 && a.parent && (a.parent.children.delete(s), a.parent.wildcardChildNode = null, a.parent.placeholderChildren = []), t = true;
  }
  return t;
}
function M(e = {}) {
  return { type: e.type || $.NORMAL, maxDepth: 0, parent: e.parent || null, children: /* @__PURE__ */ new Map(), data: e.data || null, paramName: e.paramName || null, wildcardChildNode: null, placeholderChildren: [] };
}
function ue(e) {
  return e.startsWith("**") ? $.WILDCARD : e[0] === ":" || e === "*" ? $.PLACEHOLDER : $.NORMAL;
}
const de = { preload: () => qe$1() }, me = { preload: ({ params: e }) => Ae$1(e.slug) }, O = [{ page: true, $component: { src: "src/routes/admin.tsx?pick=default&pick=$css", build: () => import('../build/admin2.mjs'), import: () => import('../build/admin2.mjs') }, path: "/admin", filePath: "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/routes/admin.tsx" }, { page: true, $component: { src: "src/routes/index.tsx?pick=default&pick=$css", build: () => import('../build/index2.mjs'), import: () => import('../build/index2.mjs') }, $$route: { require: () => ({ route: de }), src: "src/routes/index.tsx?pick=route" }, path: "/", filePath: "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/routes/index.tsx" }, { page: true, $component: { src: "src/routes/blog/[slug].tsx?pick=default&pick=$css", build: () => import('../build/_slug_2.mjs'), import: () => import('../build/_slug_2.mjs') }, $$route: { require: () => ({ route: me }), src: "src/routes/blog/[slug].tsx?pick=route" }, path: "/blog/:slug", filePath: "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/routes/blog/[slug].tsx" }, { page: false, $DELETE: { src: "src/routes/api/decap/[...path].ts?pick=DELETE", build: () => import('../build/_...path_8.mjs'), import: () => import('../build/_...path_8.mjs') }, $GET: { src: "src/routes/api/decap/[...path].ts?pick=GET", build: () => import('../build/_...path_22.mjs'), import: () => import('../build/_...path_22.mjs') }, $HEAD: { src: "src/routes/api/decap/[...path].ts?pick=HEAD", build: () => import('../build/_...path_32.mjs'), import: () => import('../build/_...path_32.mjs') }, $OPTIONS: { src: "src/routes/api/decap/[...path].ts?pick=OPTIONS", build: () => import('../build/_...path_42.mjs'), import: () => import('../build/_...path_42.mjs') }, $PATCH: { src: "src/routes/api/decap/[...path].ts?pick=PATCH", build: () => import('../build/_...path_52.mjs'), import: () => import('../build/_...path_52.mjs') }, $POST: { src: "src/routes/api/decap/[...path].ts?pick=POST", build: () => import('../build/_...path_62.mjs'), import: () => import('../build/_...path_62.mjs') }, $PUT: { src: "src/routes/api/decap/[...path].ts?pick=PUT", build: () => import('../build/_...path_72.mjs'), import: () => import('../build/_...path_72.mjs') }, path: "/api/decap/*path", filePath: "/Users/sempostma/projects/laikacms-wt/lcms-083-decapadminhtml-collections-divergence/apps/starter-solidstart-turso/src/routes/api/decap/[...path].ts" }], he = fe(O.filter((e) => e.page));
function fe(e) {
  function r(t, n, a, s) {
    const l = Object.values(t).find((o) => a.startsWith(o.id + "/"));
    return l ? (r(l.children || (l.children = []), n, a.slice(l.id.length)), t) : (t.push({ ...n, id: a, path: a.replace(/\([^)/]+\)/g, "").replace(/\/+/g, "/") }), t);
  }
  return e.sort((t, n) => t.path.length - n.path.length).reduce((t, n) => r(t, n, n.path, n.path), []);
}
function ge(e, r) {
  const t = Ee.lookup(e);
  if (t && t.route) {
    const n = t.route, a = r === "HEAD" ? n.$HEAD || n.$GET : n[`$${r}`];
    if (a === void 0) return;
    const s = n.page === true && n.$component !== void 0;
    return { handler: a, params: t.params, isPage: s };
  }
}
function $e(e) {
  return e.$HEAD || e.$GET || e.$POST || e.$PUT || e.$PATCH || e.$DELETE;
}
const Ee = le({ routes: O.reduce((e, r) => {
  if (!$e(r)) return e;
  let t = r.path.replace(/\([^)/]+\)/g, "").replace(/\/+/g, "/").replace(/\*([^/]*)/g, (n, a) => `**:${a}`).split("/").map((n) => n.startsWith(":") || n.startsWith("*") ? n : encodeURIComponent(n)).join("/");
  if (/:[^/]*\?/g.test(t)) throw new Error(`Optional parameters are not supported in API routes: ${t}`);
  if (e[t]) throw new Error(`Duplicate API routes for "${t}" found at "${e[t].route.path}" and "${r.path}"`);
  return e[t] = { route: r }, e;
}, {}) });
var ve = " ";
const ye = { style: (e) => ssrElement("style", e.attrs, () => e.children, true), link: (e) => ssrElement("link", e.attrs, void 0, true), script: (e) => e.attrs.src ? ssrElement("script", mergeProps(() => e.attrs, { get id() {
  return e.key;
} }), () => ssr(ve), true) : null, noscript: (e) => ssrElement("noscript", e.attrs, () => escape$2(e.children), true) };
function C(e, r) {
  let { tag: t, attrs: { key: n, ...a } = { key: void 0 }, children: s } = e;
  return ye[t]({ attrs: { ...a, nonce: r }, key: n, children: s });
}
function we(e, r, t, n = "default") {
  return lazy(async () => {
    var _a;
    {
      const s = (await e.import())[n], o = (await ((_a = r.inputs) == null ? void 0 : _a[e.src].assets())).filter((c) => c.tag === "style" || c.attrs.rel === "stylesheet");
      return { default: (c) => [...o.map((u) => C(u)), createComponent(s, c)] };
    }
  });
}
function I() {
  function e(t) {
    return { ...t, ...t.$$route ? t.$$route.require().route : void 0, info: { ...t.$$route ? t.$$route.require().route.info : {}, filesystem: true }, component: t.$component && we(t.$component, globalThis.MANIFEST.client, globalThis.MANIFEST.ssr), children: t.children ? t.children.map(e) : void 0 };
  }
  return he.map(e);
}
let R;
const Ce = isServer ? () => getRequestEvent().routes : () => R || (R = I());
function Se(e) {
  const r = xe$1(e.nativeEvent, "flash");
  if (r) try {
    let t = JSON.parse(r);
    if (!t || !t.result) return;
    const n = [...t.input.slice(0, -1), new Map(t.input[t.input.length - 1])], a = t.error ? new Error(t.result) : t.result;
    return { input: n, url: t.url, pending: false, result: t.thrown ? void 0 : a, error: t.thrown ? a : void 0 };
  } catch (t) {
    console.error(t);
  } finally {
    we$1(e.nativeEvent, "flash", "", { maxAge: 0 });
  }
}
async function Ae(e) {
  const r = globalThis.MANIFEST.client;
  return globalThis.MANIFEST.ssr, e.response.headers.set("Content-Type", "text/html"), Object.assign(e, { manifest: await r.json(), assets: [...await r.inputs[r.handler].assets()], router: { submission: Se(e) }, routes: I(), complete: false, $islands: /* @__PURE__ */ new Set() });
}
const Ne = /* @__PURE__ */ new Set([301, 302, 303, 307, 308]);
function S(e) {
  return e.status && Ne.has(e.status) ? e.status : 302;
}
function be(e, r, t = {}, n) {
  return eventHandler({ handler: (a) => {
    const s = He$1(a);
    return provideRequestEvent(s, async () => {
      const l = ge(new URL(s.request.url).pathname, s.request.method);
      if (l) {
        const d = await l.handler.import(), f = s.request.method === "HEAD" ? d.HEAD || d.GET : d[s.request.method];
        s.params = l.params || {}, sharedConfig.context = { event: s };
        const b = await f(s);
        if (b !== void 0) return b;
        if (s.request.method !== "GET") throw new Error(`API handler for ${s.request.method} "${s.request.url}" did not return a response.`);
        if (!l.isPage) return;
      }
      const o = await r(s), i = typeof t == "function" ? await t(o) : { ...t }, c = i.mode || "stream";
      if (i.nonce && (o.nonce = i.nonce), c === "sync") {
        const d = renderToString(() => (sharedConfig.context.event = o, e(o)), i);
        if (o.complete = true, o.response && o.response.headers.get("Location")) {
          const f = S(o.response);
          return _e$1(a, o.response.headers.get("Location"), f);
        }
        return d;
      }
      if (i.onCompleteAll) {
        const d = i.onCompleteAll;
        i.onCompleteAll = (f) => {
          D(o)(f), d(f);
        };
      } else i.onCompleteAll = D(o);
      if (i.onCompleteShell) {
        const d = i.onCompleteShell;
        i.onCompleteShell = (f) => {
          x(o, a)(), d(f);
        };
      } else i.onCompleteShell = x(o, a);
      const u = renderToStream(() => (sharedConfig.context.event = o, e(o)), i);
      if (o.response && o.response.headers.get("Location")) {
        const d = S(o.response);
        return _e$1(a, o.response.headers.get("Location"), d);
      }
      if (c === "async") return u;
      const { writable: E, readable: y } = new TransformStream();
      return u.pipeTo(E), y;
    });
  } });
}
function x(e, r) {
  return () => {
    if (e.response && e.response.headers.get("Location")) {
      const t = S(e.response);
      R$1(r, t), Ce$1(r, "Location", e.response.headers.get("Location"));
    }
  };
}
function D(e) {
  return ({ write: r }) => {
    e.complete = true;
    const t = e.response && e.response.headers.get("Location");
    t && r(`<script>window.location="${t}"<\/script>`);
  };
}
function ke(e, r, t) {
  return be(e, Ae, r);
}
function Pe() {
  return createComponent$1(Router, { get children() {
    return createComponent$1(Suspense, { get children() {
      return createComponent$1(Ce, {});
    } });
  } });
}
const _ = isServer ? (e) => {
  const r = getRequestEvent();
  return r.response.status = e.code, r.response.statusText = e.text, onCleanup(() => !r.nativeEvent.handled && !r.complete && (r.response.status = 200)), null;
} : (e) => null;
var Re = ["<span", ' style="font-size:1.5em;text-align:center;position:fixed;left:0px;bottom:55%;width:100%;">', "</span>"], xe = ["<span", ' style="font-size:1.5em;text-align:center;position:fixed;left:0px;bottom:55%;width:100%;">500 | Internal Server Error</span>'];
const De = (e) => {
  const r = isServer ? "500 | Internal Server Error" : "Error | Uncaught Client Exception";
  return createComponent$1(ErrorBoundary, { fallback: (t) => (console.error(t), [ssr(Re, ssrHydrationKey(), escape$2(r)), createComponent$1(_, { code: 500 })]), get children() {
    return e.children;
  } });
}, Le = (e) => {
  let r = false;
  const t = catchError(() => e.children, (n) => {
    console.error(n), r = !!n;
  });
  return r ? [ssr(xe, ssrHydrationKey()), createComponent$1(_, { code: 500 })] : t;
};
var L = ["<script", ">", "<\/script>"], He = ["<script", ' type="module"', " async", "><\/script>"], Me = ["<script", ' type="module" async', "><\/script>"];
const Oe = ssr("<!DOCTYPE html>");
function q(e, r, t = []) {
  for (let n = 0; n < r.length; n++) {
    const a = r[n];
    if (a.path !== e[0].path) continue;
    let s = [...t, a];
    if (a.children) {
      const l = e.slice(1);
      if (l.length === 0 || (s = q(l, a.children, s), !s)) continue;
    }
    return s;
  }
}
function Ie(e) {
  const r = getRequestEvent(), t = r.nonce;
  let n = [];
  return Promise.resolve().then(async () => {
    let a = [];
    if (r.router && r.router.matches) {
      const s = [...r.router.matches];
      for (; s.length && (!s[0].info || !s[0].info.filesystem); ) s.shift();
      const l = s.length && q(s, r.routes);
      if (l) {
        const o = globalThis.MANIFEST.client.inputs;
        for (let i = 0; i < l.length; i++) {
          const c = l[i], u = o[c.$component.src];
          a.push(u.assets());
        }
      }
    }
    n = await Promise.all(a).then((s) => [...new Map(s.flat().map((l) => [l.attrs.key, l])).values()].filter((l) => l.attrs.rel === "modulepreload" && !r.assets.find((o) => o.attrs.key === l.attrs.key)));
  }), useAssets(() => n.length ? n.map((a) => C(a)) : void 0), createComponent$1(NoHydration, { get children() {
    return [Oe, createComponent$1(Le, { get children() {
      return createComponent$1(e.document, { get assets() {
        return [createComponent$1(HydrationScript, {}), r.assets.map((a) => C(a, t))];
      }, get scripts() {
        return t ? [ssr(L, ssrHydrationKey() + ssrAttribute("nonce", escape$2(t, true), false), `window.manifest = ${JSON.stringify(r.manifest)}`), ssr(He, ssrHydrationKey(), ssrAttribute("nonce", escape$2(t, true), false), ssrAttribute("src", escape$2(globalThis.MANIFEST.client.inputs[globalThis.MANIFEST.client.handler].output.path, true), false))] : [ssr(L, ssrHydrationKey(), `window.manifest = ${JSON.stringify(r.manifest)}`), ssr(Me, ssrHydrationKey(), ssrAttribute("src", escape$2(globalThis.MANIFEST.client.inputs[globalThis.MANIFEST.client.handler].output.path, true), false))];
      }, get children() {
        return createComponent$1(Hydration, { get children() {
          return createComponent$1(De, { get children() {
            return createComponent$1(Pe, {});
          } });
        } });
      } });
    } })];
  } });
}
var _e = ['<head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">', "</head>"], qe = ["<html", ' lang="en">', '<body><div id="app">', "</div><!--$-->", "<!--/--></body></html>"];
const Ze = ke(() => createComponent$1(Ie, { document: ({ assets: e, children: r, scripts: t }) => ssr(qe, ssrHydrationKey(), createComponent$1(NoHydration, { get children() {
  return ssr(_e, escape$2(e));
} }), escape$2(r), escape$2(t)) }));

const handlers = [
  { route: '', handler: _cVoaHD, lazy: false, middleware: true, method: undefined },
  { route: '/_server', handler: So$1, lazy: false, middleware: true, method: undefined },
  { route: '/', handler: Ze, lazy: false, middleware: true, method: undefined }
];

function createNitroApp() {
  const config = useRuntimeConfig();
  const hooks = createHooks();
  const captureError = (error, context = {}) => {
    const promise = hooks.callHookParallel("error", error, context).catch((error_) => {
      console.error("Error while capturing another error", error_);
    });
    if (context.event && isEvent(context.event)) {
      const errors = context.event.context.nitro?.errors;
      if (errors) {
        errors.push({ error, context });
      }
      if (context.event.waitUntil) {
        context.event.waitUntil(promise);
      }
    }
  };
  const h3App = createApp({
    debug: destr(false),
    onError: (error, event) => {
      captureError(error, { event, tags: ["request"] });
      return errorHandler(error, event);
    },
    onRequest: async (event) => {
      event.context.nitro = event.context.nitro || { errors: [] };
      const fetchContext = event.node.req?.__unenv__;
      if (fetchContext?._platform) {
        event.context = {
          _platform: fetchContext?._platform,
          // #3335
          ...fetchContext._platform,
          ...event.context
        };
      }
      if (!event.context.waitUntil && fetchContext?.waitUntil) {
        event.context.waitUntil = fetchContext.waitUntil;
      }
      event.fetch = (req, init) => fetchWithEvent(event, req, init, { fetch: localFetch });
      event.$fetch = (req, init) => fetchWithEvent(event, req, init, {
        fetch: $fetch
      });
      event.waitUntil = (promise) => {
        if (!event.context.nitro._waitUntilPromises) {
          event.context.nitro._waitUntilPromises = [];
        }
        event.context.nitro._waitUntilPromises.push(promise);
        if (event.context.waitUntil) {
          event.context.waitUntil(promise);
        }
      };
      event.captureError = (error, context) => {
        captureError(error, { event, ...context });
      };
      await nitroApp$1.hooks.callHook("request", event).catch((error) => {
        captureError(error, { event, tags: ["request"] });
      });
    },
    onBeforeResponse: async (event, response) => {
      await nitroApp$1.hooks.callHook("beforeResponse", event, response).catch((error) => {
        captureError(error, { event, tags: ["request", "response"] });
      });
    },
    onAfterResponse: async (event, response) => {
      await nitroApp$1.hooks.callHook("afterResponse", event, response).catch((error) => {
        captureError(error, { event, tags: ["request", "response"] });
      });
    }
  });
  const router = createRouter({
    preemptive: true
  });
  const nodeHandler = toNodeListener(h3App);
  const localCall = (aRequest) => b$5(
    nodeHandler,
    aRequest
  );
  const localFetch = (input, init) => {
    if (!input.toString().startsWith("/")) {
      return globalThis.fetch(input, init);
    }
    return C$6(
      nodeHandler,
      input,
      init
    ).then((response) => normalizeFetchResponse(response));
  };
  const $fetch = createFetch({
    fetch: localFetch,
    Headers: Headers$1,
    defaults: { baseURL: config.app.baseURL }
  });
  globalThis.$fetch = $fetch;
  h3App.use(createRouteRulesHandler({ localFetch }));
  for (const h of handlers) {
    let handler = h.lazy ? lazyEventHandler(h.handler) : h.handler;
    if (h.middleware || !h.route) {
      const middlewareBase = (config.app.baseURL + (h.route || "/")).replace(
        /\/+/g,
        "/"
      );
      h3App.use(middlewareBase, handler);
    } else {
      const routeRules = getRouteRulesForPath(
        h.route.replace(/:\w+|\*\*/g, "_")
      );
      if (routeRules.cache) {
        handler = cachedEventHandler(handler, {
          group: "nitro/routes",
          ...routeRules.cache
        });
      }
      router.use(h.route, handler, h.method);
    }
  }
  h3App.use(config.app.baseURL, router.handler);
  {
    const _handler = h3App.handler;
    h3App.handler = (event) => {
      const ctx = { event };
      return nitroAsyncContext.callAsync(ctx, () => _handler(event));
    };
  }
  const app = {
    hooks,
    h3App,
    router,
    localCall,
    localFetch,
    captureError
  };
  return app;
}
function runNitroPlugins(nitroApp2) {
  for (const plugin of plugins) {
    try {
      plugin(nitroApp2);
    } catch (error) {
      nitroApp2.captureError(error, { tags: ["plugin"] });
      throw error;
    }
  }
}
const nitroApp$1 = createNitroApp();
function useNitroApp() {
  return nitroApp$1;
}
runNitroPlugins(nitroApp$1);

const debug = (...args) => {
};
function GracefulShutdown(server, opts) {
  opts = opts || {};
  const options = Object.assign(
    {
      signals: "SIGINT SIGTERM",
      timeout: 3e4,
      development: false,
      forceExit: true,
      onShutdown: (signal) => Promise.resolve(signal),
      preShutdown: (signal) => Promise.resolve(signal)
    },
    opts
  );
  let isShuttingDown = false;
  const connections = {};
  let connectionCounter = 0;
  const secureConnections = {};
  let secureConnectionCounter = 0;
  let failed = false;
  let finalRun = false;
  function onceFactory() {
    let called = false;
    return (emitter, events, callback) => {
      function call() {
        if (!called) {
          called = true;
          return Reflect.apply(callback, this, arguments);
        }
      }
      for (const e of events) {
        emitter.on(e, call);
      }
    };
  }
  const signals = options.signals.split(" ").map((s) => s.trim()).filter((s) => s.length > 0);
  const once = onceFactory();
  once(process, signals, (signal) => {
    debug("received shut down signal", signal);
    shutdown(signal).then(() => {
      if (options.forceExit) {
        process.exit(failed ? 1 : 0);
      }
    }).catch((error) => {
      debug("server shut down error occurred", error);
      process.exit(1);
    });
  });
  function isFunction(functionToCheck) {
    const getType = Object.prototype.toString.call(functionToCheck);
    return /^\[object\s([A-Za-z]+)?Function]$/.test(getType);
  }
  function destroy(socket, force = false) {
    if (socket._isIdle && isShuttingDown || force) {
      socket.destroy();
      if (socket.server instanceof http.Server) {
        delete connections[socket._connectionId];
      } else {
        delete secureConnections[socket._connectionId];
      }
    }
  }
  function destroyAllConnections(force = false) {
    debug("Destroy Connections : " + (force ? "forced close" : "close"));
    let counter = 0;
    let secureCounter = 0;
    for (const key of Object.keys(connections)) {
      const socket = connections[key];
      const serverResponse = socket._httpMessage;
      if (serverResponse && !force) {
        if (!serverResponse.headersSent) {
          serverResponse.setHeader("connection", "close");
        }
      } else {
        counter++;
        destroy(socket);
      }
    }
    debug("Connections destroyed : " + counter);
    debug("Connection Counter    : " + connectionCounter);
    for (const key of Object.keys(secureConnections)) {
      const socket = secureConnections[key];
      const serverResponse = socket._httpMessage;
      if (serverResponse && !force) {
        if (!serverResponse.headersSent) {
          serverResponse.setHeader("connection", "close");
        }
      } else {
        secureCounter++;
        destroy(socket);
      }
    }
    debug("Secure Connections destroyed : " + secureCounter);
    debug("Secure Connection Counter    : " + secureConnectionCounter);
  }
  server.on("request", (req, res) => {
    req.socket._isIdle = false;
    if (isShuttingDown && !res.headersSent) {
      res.setHeader("connection", "close");
    }
    res.on("finish", () => {
      req.socket._isIdle = true;
      destroy(req.socket);
    });
  });
  server.on("connection", (socket) => {
    if (isShuttingDown) {
      socket.destroy();
    } else {
      const id = connectionCounter++;
      socket._isIdle = true;
      socket._connectionId = id;
      connections[id] = socket;
      socket.once("close", () => {
        delete connections[socket._connectionId];
      });
    }
  });
  server.on("secureConnection", (socket) => {
    if (isShuttingDown) {
      socket.destroy();
    } else {
      const id = secureConnectionCounter++;
      socket._isIdle = true;
      socket._connectionId = id;
      secureConnections[id] = socket;
      socket.once("close", () => {
        delete secureConnections[socket._connectionId];
      });
    }
  });
  process.on("close", () => {
    debug("closed");
  });
  function shutdown(sig) {
    function cleanupHttp() {
      destroyAllConnections();
      debug("Close http server");
      return new Promise((resolve, reject) => {
        server.close((err) => {
          if (err) {
            return reject(err);
          }
          return resolve(true);
        });
      });
    }
    debug("shutdown signal - " + sig);
    if (options.development) {
      debug("DEV-Mode - immediate forceful shutdown");
      return process.exit(0);
    }
    function finalHandler() {
      if (!finalRun) {
        finalRun = true;
        if (options.finally && isFunction(options.finally)) {
          debug("executing finally()");
          options.finally();
        }
      }
      return Promise.resolve();
    }
    function waitForReadyToShutDown(totalNumInterval) {
      debug(`waitForReadyToShutDown... ${totalNumInterval}`);
      if (totalNumInterval === 0) {
        debug(
          `Could not close connections in time (${options.timeout}ms), will forcefully shut down`
        );
        return Promise.resolve(true);
      }
      const allConnectionsClosed = Object.keys(connections).length === 0 && Object.keys(secureConnections).length === 0;
      if (allConnectionsClosed) {
        debug("All connections closed. Continue to shutting down");
        return Promise.resolve(false);
      }
      debug("Schedule the next waitForReadyToShutdown");
      return new Promise((resolve) => {
        setTimeout(() => {
          resolve(waitForReadyToShutDown(totalNumInterval - 1));
        }, 250);
      });
    }
    if (isShuttingDown) {
      return Promise.resolve();
    }
    debug("shutting down");
    return options.preShutdown(sig).then(() => {
      isShuttingDown = true;
      cleanupHttp();
    }).then(() => {
      const pollIterations = options.timeout ? Math.round(options.timeout / 250) : 0;
      return waitForReadyToShutDown(pollIterations);
    }).then((force) => {
      debug("Do onShutdown now");
      if (force) {
        destroyAllConnections(force);
      }
      return options.onShutdown(sig);
    }).then(finalHandler).catch((error) => {
      const errString = typeof error === "string" ? error : JSON.stringify(error);
      debug(errString);
      failed = true;
      throw errString;
    });
  }
  function shutdownManual() {
    return shutdown("manual");
  }
  return shutdownManual;
}

function getGracefulShutdownConfig() {
  return {
    disabled: !!process.env.NITRO_SHUTDOWN_DISABLED,
    signals: (process.env.NITRO_SHUTDOWN_SIGNALS || "SIGTERM SIGINT").split(" ").map((s) => s.trim()),
    timeout: Number.parseInt(process.env.NITRO_SHUTDOWN_TIMEOUT || "", 10) || 3e4,
    forceExit: !process.env.NITRO_SHUTDOWN_NO_FORCE_EXIT
  };
}
function setupGracefulShutdown(listener, nitroApp) {
  const shutdownConfig = getGracefulShutdownConfig();
  if (shutdownConfig.disabled) {
    return;
  }
  GracefulShutdown(listener, {
    signals: shutdownConfig.signals.join(" "),
    timeout: shutdownConfig.timeout,
    forceExit: shutdownConfig.forceExit,
    onShutdown: async () => {
      await new Promise((resolve) => {
        const timeout = setTimeout(() => {
          console.warn("Graceful shutdown timeout, force exiting...");
          resolve();
        }, shutdownConfig.timeout);
        nitroApp.hooks.callHook("close").catch((error) => {
          console.error(error);
        }).finally(() => {
          clearTimeout(timeout);
          resolve();
        });
      });
    }
  });
}

const cert = process.env.NITRO_SSL_CERT;
const key = process.env.NITRO_SSL_KEY;
const nitroApp = useNitroApp();
const server = cert && key ? new Server({ key, cert }, toNodeListener(nitroApp.h3App)) : new Server$1(toNodeListener(nitroApp.h3App));
const port = destr(process.env.NITRO_PORT || process.env.PORT) || 3e3;
const host = process.env.NITRO_HOST || process.env.HOST;
const path = process.env.NITRO_UNIX_SOCKET;
const listener = server.listen(path ? { path } : { port, host }, (err) => {
  if (err) {
    console.error(err);
    process.exit(1);
  }
  const protocol = cert && key ? "https" : "http";
  const addressInfo = listener.address();
  if (typeof addressInfo === "string") {
    console.log(`Listening on unix socket ${addressInfo}`);
    return;
  }
  const baseURL = (useRuntimeConfig().app.baseURL || "").replace(/\/$/, "");
  const url = `${protocol}://${addressInfo.family === "IPv6" ? `[${addressInfo.address}]` : addressInfo.address}:${addressInfo.port}${baseURL}`;
  console.log(`Listening on ${url}`);
});
trapUnhandledNodeErrors();
setupGracefulShutdown(listener, nitroApp);
const nodeServer = {};

export { ig$1 as $, Ae$4 as A, Bi$2 as B, Ge$2 as C, Dd$1 as D, Ee$3 as E, bm$1 as F, Gm$1 as G, H$2 as H, Ie$3 as I, Fm$1 as J, ng$1 as K, sg$1 as L, ug$1 as M, Nh$1 as N, fg$1 as O, yg$1 as P, Qm$1 as Q, gg$1 as R, S$2 as S, cg$1 as T, Um$1 as U, tg$1 as V, Wm$1 as W, X$4 as X, Ye$2 as Y, Sg$1 as Z, hg$1 as _, e as a, Ie$1 as a0, Nh as a1, jt as a2, S$1 as a3, Dd as a4, X$1 as a5, b$1 as a6, vm as a7, Au as a8, ji as a9, gg as aA, cg as aB, tg as aC, Sg as aD, hg as aE, ig as aF, nodeServer as aG, Hm as aa, lt as ab, we$2 as ac, Wm as ad, ne$1 as ae, sc as af, ls as ag, Gm as ah, Ye as ai, qm as aj, Dm as ak, Um as al, j as am, Bi as an, Qm as ao, di as ap, Em as aq, us as ar, Ge as as, bm as at, Fm as au, ng as av, sg as aw, ug as ax, fg as ay, yg as az, Ae$1 as b, collectStream$1 as c, laika as d, e$1 as e, b$4 as f, go$1 as g, Au$1 as h, ji$2 as i, jt$2 as j, Hm$1 as k, laika$1 as l, lt$2 as m, ne$4 as n, ls$2 as o, qm$1 as p, qe$1 as q, runTask$1$1 as r, sc$1 as s, Dm$1 as t, j$2 as u, vm$1 as v, we$5 as w, di$2 as x, Em$1 as y, us$2 as z };
//# sourceMappingURL=nitro.mjs.map
