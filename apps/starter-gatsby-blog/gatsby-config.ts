import type { GatsbyConfig } from 'gatsby';

const config: GatsbyConfig = {
  siteMetadata: {
    title: 'LaikaCMS Gatsby Blog',
    description: 'A minimal Gatsby blog powered by LaikaCMS.',
  },
};

export default config;
