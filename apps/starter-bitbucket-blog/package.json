{
  "name": "@laikacms/starter-bitbucket-blog",
  "version": "0.0.1",
  "private": true,
  "description": "Starter blog backed by Bitbucket (Cloud REST v2 API). All writes go through a single POST /src multipart endpoint — one round-trip per commit. Completes the git-platform triumvirate alongside starter-github-blog and starter-gitlab-blog. Decap admin from CDN.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "catalog:server",
    "@laikacms/bitbucket": "workspace:*",
    "@laikacms/decap-integrations": "workspace:*",
    "hono": "catalog:server",
    "laikacms": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:types",
    "tsx": "catalog:tools",
    "typescript": "catalog:tools"
  },
  "engines": {
    "node": "22.x"
  }
}
