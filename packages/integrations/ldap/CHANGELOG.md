# @laikacms/ldap

## 1.0.0

### Minor

- Initial release. First export `@laikacms/ldap/storage-ldap` — a `StorageRepository` backed by an
  LDAP directory. Client-agnostic via the structural `LdapOps` interface.
