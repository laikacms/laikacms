# @laikacms/backblaze

## 1.0.0

### Minor

- Initial release. First export `@laikacms/backblaze/storage-b2` — a `StorageRepository` backed by
  Backblaze B2 via the native API (not the S3-compatible mode). Two-phase upload + mandatory SHA-1
  verification + file versioning.
