# @laikacms/atproto

## 1.0.0

### Minor

- Initial release. First export `@laikacms/atproto/storage-atproto` — a `StorageRepository` backed
  by AT Protocol via the XRPC repo endpoints. Works with Bluesky's hosted PDS and self-hosted `pds`.
