# @laikacms/convex

## 1.0.0

### Minor

- Initial release. First export `@laikacms/convex/storage-convex` — a `StorageRepository` backed by
  Convex via the HTTP RPC endpoint. Users provide a reference `convex/laika.ts` module (documented
  in the README); the package handles the wire-shape adapter and function-path routing.
