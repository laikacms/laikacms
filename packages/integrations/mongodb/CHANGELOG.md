# @laikacms/mongodb

## 1.0.0

### Minor

- Initial release. First export `@laikacms/mongodb/storage-mongodb` — a `StorageRepository` backed
  by a single MongoDB collection. Driver-agnostic via the structural `MongoCollectionLike`
  interface.
