# @laikacms/influxdb

## 1.0.0

### Minor

- Initial release. First export `@laikacms/influxdb/storage-influxdb` — a `StorageRepository` backed
  by InfluxDB v2 via the HTTP API. Line protocol writes, Flux pipeline DSL reads, annotated CSV
  responses.
