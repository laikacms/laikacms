# @laikacms/meilisearch

## 1.0.0

### Minor

- Initial release. First export
  `@laikacms/meilisearch/storage-meilisearch` — a `StorageRepository`
  backed by a single MeiliSearch index. Async-by-default mutations
  via the Tasks API; bulk delete via `delete-batch` with primary-key
  array.
