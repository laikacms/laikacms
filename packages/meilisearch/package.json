{
  "name": "@laikacms/meilisearch",
  "version": "1.0.0",
  "description": "MeiliSearch-backed StorageRepository for Laika CMS. Async-by-default mutations via the Tasks API, bulk delete via document-ID arrays, SQL-like filter DSL. Runtime-agnostic — only depends on `fetch`.",
  "keywords": [
    "headless-cms",
    "content-management",
    "meilisearch",
    "search",
    "storage"
  ],
  "author": {
    "name": "Sem Postma",
    "email": "sem@laikacms.com",
    "url": "https://sem-postma.com"
  },
  "homepage": "https://github.com/laikacms/laikacms",
  "bugs": {
    "email": "sem@laikacms.com",
    "url": "https://github.com/laikacms/laikacms/issues"
  },
  "license": "MIT",
  "repository": "https://github.com/laikacms/laikacms",
  "type": "module",
  "sideEffects": false,
  "files": ["dist", "README.md", "LICENSE", "CHANGELOG.md"],
  "exports": {
    "./storage-meilisearch": {
      "types": "./dist/storage-meilisearch/index.d.ts",
      "import": "./dist/storage-meilisearch/index.js",
      "default": "./dist/storage-meilisearch/index.js"
    }
  },
  "scripts": {
    "clean": "rimraf dist .turbo",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "laikacms": "workspace:*",
    "effect": "catalog:default",
    "minimatch": "^10.0.3"
  },
  "devDependencies": {
    "@types/node": "catalog:types",
    "rimraf": "catalog:tools",
    "typescript": "catalog:tools",
    "vitest": "catalog:tools"
  },
  "engines": { "node": ">=22" }
}
