import type { SerializedEditorState } from 'lexical';

export type { SerializedEditorState } from 'lexical';

/** A serialized Lexical document — the editor-state JSON (`{ root: … }`). */
export type SerializedLexicalDocument = SerializedEditorState;
