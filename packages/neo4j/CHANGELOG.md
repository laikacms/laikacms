# @laikacms/neo4j

## 1.0.0

### Minor

- Initial release. First export `@laikacms/neo4j/storage-neo4j` — a
  `StorageRepository` backed by Neo4j via the transactional HTTP
  endpoint. Works against self-hosted Neo4j and AuraDB.
