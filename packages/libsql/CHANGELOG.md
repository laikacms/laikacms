# @laikacms/libsql

## 1.0.0

### Minor

- Initial release. First export `@laikacms/libsql/storage-libsql` —
  a `StorageRepository` backed by libSQL via the hrana HTTP pipeline
  protocol. Works with Turso, sqld, and Fly libSQL.
