export type GlobalUI = {
    isFetching: boolean;
    useOpenAuthoring: boolean;
};
/**
 * Reducer for some global UI state that we want to share between components
 */
declare const globalUI: any;
export default globalUI;
