import type { CmsConfig } from '../types/redux';
declare const config: any;
export declare function selectLocale(state: CmsConfig): string;
export declare function selectUseWorkflow(state: CmsConfig): boolean;
export default config;
